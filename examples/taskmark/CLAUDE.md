# TaskMark

A markdown task manager CLI.

## Commands

- `node bin/taskmark.js add "text"` — add a task
- `node bin/taskmark.js list` — list tasks
- `node bin/taskmark.js done <id>` — complete a task
- `node bin/taskmark.js search "query"` — search tasks
