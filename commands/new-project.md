---
description: "Alias for /grd:init — Initialize a new R&D project from idea to roadmap"
argument-hint: "[--auto @document.md | --yolo]"
---

This command has been renamed to `/grd:init`. Run `/grd:init` instead.

All arguments are passed through: `/grd:init $ARGUMENTS`
