---
phase: 75-hook-events-and-plugin-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - lib/worktree.ts
  - bin/grd-tools.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "plugin.json registers StopFailure hook event with a command handler that invokes grd-tools.js stop-failure-hook"
    - "plugin.json registers PostCompact hook event with a command handler that invokes grd-tools.js post-compact-hook"
    - "StopFailure handler logs failure details (error type, affected subprocess) to .planning/autopilot/autopilot.log when in evolve/autopilot context"
    - "PostCompact handler acknowledges compaction and continues (informational only)"
    - "allowRead sandbox setting awareness is documented in plugin.json or handler comments"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with StopFailure and PostCompact hook registrations"
      contains: "StopFailure"
    - path: "lib/worktree.ts"
      provides: "Hook handler implementations for StopFailure and PostCompact"
      exports: ["cmdStopFailureHook", "cmdPostCompactHook"]
    - path: "bin/grd-tools.ts"
      provides: "CLI routing for stop-failure-hook and post-compact-hook subcommands"
      contains: "stop-failure-hook"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "bin/grd-tools.ts"
      via: "hook command invocation"
      pattern: "stop-failure-hook|post-compact-hook"
    - from: "bin/grd-tools.ts"
      to: "lib/worktree.ts"
      via: "handler import and call"
      pattern: "cmdStopFailureHook|cmdPostCompactHook"
---

<objective>
Register StopFailure and PostCompact hook events in plugin.json and implement their handler functions.

Purpose: StopFailure (v2.1.78) fires when a turn ends due to API errors (rate limit, auth failure). GRD uses this to log failures in evolve/autopilot subprocesses for retry logic. PostCompact (v2.1.76) fires after context compaction completes. GRD acknowledges it (informational). Both hooks enable future use even if current handlers are minimal.

Output: Updated plugin.json with two new hook registrations, two new handler functions in lib/worktree.ts, CLI routing in bin/grd-tools.ts.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude-plugin/plugin.json
@lib/worktree.ts
@bin/grd-tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StopFailure and PostCompact handler functions to lib/worktree.ts</name>
  <files>lib/worktree.ts</files>
  <action>
    Add two new hook handler functions to lib/worktree.ts, following the exact pattern of the existing cmdTeammateIdleHook/cmdTaskCompletedHook/cmdInstructionsLoadedHook handlers:

    1. **cmdStopFailureHook(cwd: string, raw: boolean): void**
       - Read environment variables from hook payload:
         - `STOP_REASON` — reason the turn stopped (e.g., "rate_limit", "auth_failure", "api_error")
         - `ERROR_MESSAGE` — error message text (optional)
         - `AGENT_ID` — agent identifier (optional)
       - Check if `.planning/autopilot/autopilot.log` exists (indicating an active autopilot/evolve session)
       - If autopilot.log exists, append a timestamped failure entry: `[ISO timestamp] STOP_FAILURE: reason={STOP_REASON} error={ERROR_MESSAGE} agent={AGENT_ID}`
       - Return JSON result: `{ ok: true, hook: 'StopFailure', stop_reason, error_message, agent_id, logged: boolean }`
       - Add JSDoc comment documenting the hook event (v2.1.78), environment variables, and that this enables evolve/autopilot retry logic
       - Add comment noting: `// Note: allowRead sandbox setting (v2.1.77) can re-allow read access within denyRead regions — relevant for plugin data paths`

    2. **cmdPostCompactHook(_cwd: string, raw: boolean): void**
       - Read environment variables from hook payload:
         - `AGENT_ID` — agent identifier (optional)
         - `AGENT_TYPE` — agent type (optional)
       - Return JSON result: `{ ok: true, hook: 'PostCompact', agent_id, agent_type, acknowledged: true }`
       - Add JSDoc comment documenting: PostCompact (v2.1.76) fires after context compaction. Informational only — GRD acknowledges and continues. Future use: could trigger context reload or state refresh.

    3. Export both functions from the module's `module.exports` object (add to the existing exports alongside cmdTeammateIdleHook etc.)

    Use the same output pattern as existing hooks: if `raw` is true, use `console.log()` with formatted text; otherwise `console.log(JSON.stringify(result))` then `process.exit(0)`.
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) — type-check passes with new functions
    grep -c "cmdStopFailureHook\|cmdPostCompactHook" lib/worktree.ts — both functions defined and exported
  </verify>
  <done>
    Two new hook handler functions exist in lib/worktree.ts, properly typed, with JSDoc documenting the hook events and environment variables. Both exported from module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register hooks in plugin.json and route CLI commands in bin/grd-tools.ts</name>
  <files>.claude-plugin/plugin.json, bin/grd-tools.ts</files>
  <action>
    1. **Update .claude-plugin/plugin.json** — add two new hook event registrations after the existing InstructionsLoaded entry, following the exact JSON structure of existing hooks:

       ```json
       "StopFailure": [
         {
           "hooks": [
             {
               "type": "command",
               "command": "node \"${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js\" stop-failure-hook 2>/dev/null || true",
               "timeout": 5
             }
           ]
         }
       ],
       "PostCompact": [
         {
           "hooks": [
             {
               "type": "command",
               "command": "node \"${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js\" post-compact-hook 2>/dev/null || true",
               "timeout": 5
             }
           ]
         }
       ]
       ```

    2. **Update bin/grd-tools.ts** — add CLI routing for the two new subcommands:
       - Add to the COMMANDS array (near the existing teammate-idle-hook, task-completed-hook, instructions-loaded-hook entries):
         ```
         { command: 'stop-failure-hook', handler: (_args, cwd, raw) => cmdStopFailureHook(cwd, raw) },
         { command: 'post-compact-hook', handler: (_args, cwd, raw) => cmdPostCompactHook(cwd, raw) },
         ```
       - Add `cmdStopFailureHook` and `cmdPostCompactHook` to the destructured import from `lib/worktree.ts`
       - Add `'stop-failure-hook'` and `'post-compact-hook'` to the TOP_LEVEL_COMMANDS array (near the other hook commands)

    Ensure the plugin.json remains valid JSON after editing.
  </action>
  <verify>
    node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json','utf8'))" (Level 1: Sanity) — valid JSON
    npx tsc --noEmit — type-check passes
    grep "StopFailure\|PostCompact" .claude-plugin/plugin.json — both hooks registered
    grep "stop-failure-hook\|post-compact-hook" bin/grd-tools.ts — both commands routed
  </verify>
  <done>
    plugin.json has 8 registered hook events (SessionStart, WorktreeCreate, WorktreeRemove, TeammateIdle, TaskCompleted, InstructionsLoaded, StopFailure, PostCompact). bin/grd-tools.ts routes both new subcommands to their handlers.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- plugin.json is valid JSON with StopFailure and PostCompact hook entries
- TypeScript compiles without errors (`npx tsc --noEmit`)
- Both handler functions exist in lib/worktree.ts and are exported
- Both CLI subcommands are routed in bin/grd-tools.ts
- Running `node bin/grd-tools.js stop-failure-hook` and `node bin/grd-tools.js post-compact-hook` produces valid JSON output
</verification>

<success_criteria>
plugin.json registers 8 hook events (up from 6). StopFailure handler logs failures to autopilot.log when in evolve/autopilot context. PostCompact handler acknowledges compaction. allowRead sandbox setting awareness documented in handler comments.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/75-hook-events-and-plugin-infrastructure/75-01-SUMMARY.md`
</output>
