---
phase: 74-model-mappings-and-capability-flags
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/backend.ts, lib/types.ts, lib/context/execute.ts, tests/unit/backend.test.ts]
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "BACKEND_CAPABILITIES.codex.smart_approvals is true; all other backends have smart_approvals: false"
    - "BACKEND_CAPABILITIES.gemini has plan_mode: true, sandbox_gvisor: true, sandbox_lxc: false"
    - "BACKEND_CAPABILITIES.claude.mcp_elicitation is true; other backends have mcp_elicitation: false"
    - "BackendCapabilities interface includes smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, model_overrides, max_output_tokens fields"
    - "cmdInitExecutePhase JSON output includes model_overrides_available when backend supports it"
    - "max_output_tokens documented as { default: 64000, upper_bound: 128000 } for claude backend"
  artifacts:
    - path: "lib/types.ts"
      provides: "Extended BackendCapabilities interface with new fields"
      contains: "smart_approvals"
    - path: "lib/backend.ts"
      provides: "BACKEND_CAPABILITIES with new flags populated for all backends"
      contains: "mcp_elicitation"
    - path: "tests/unit/backend.test.ts"
      provides: "Tests verifying new capability flags for all backends"
      contains: "smart_approvals"
  key_links:
    - from: "lib/backend.ts"
      to: "lib/types.ts"
      via: "BackendCapabilities type import"
      pattern: "import.*BackendCapabilities"
    - from: "lib/context/execute.ts"
      to: "lib/backend.ts"
      via: "getBackendCapabilities call"
      pattern: "getBackendCapabilities"
---

<objective>
Add new capability flags to BackendCapabilities for all backends: smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, model_overrides, and max_output_tokens.

Purpose: GRD agents need to know which backend features are available to adapt their workflows (e.g., skip plan_mode steps on Claude, use mcp_elicitation only on Claude, leverage smart_approvals on Codex). The max_output_tokens field lets evolve prompt sizing account for actual output limits.
Output: Extended BackendCapabilities type, populated BACKEND_CAPABILITIES for all backends, model_overrides_available in init context, and passing tests.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/backend.ts
@lib/types.ts
@lib/context/execute.ts
@tests/unit/backend.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BackendCapabilities type and populate flags</name>
  <files>lib/types.ts, lib/backend.ts</files>
  <action>
    1. In lib/types.ts, add the following fields to the BackendCapabilities interface:
       ```
       smart_approvals: boolean;
       plan_mode: boolean;
       sandbox_gvisor: boolean;
       sandbox_lxc: boolean;
       mcp_elicitation: boolean;
       model_overrides: boolean;
       max_output_tokens: { default: number; upper_bound: number } | null;
       ```
       The max_output_tokens field is nullable because not all backends document their output token limits.

    2. In lib/backend.ts, update BACKEND_CAPABILITIES for each backend:

       **claude:**
       - smart_approvals: false (Claude Code does not have guardian subagent review)
       - plan_mode: false (Claude Code does not have a plan mode feature)
       - sandbox_gvisor: false (Claude uses permission-based sandboxing, not gVisor)
       - sandbox_lxc: false
       - mcp_elicitation: true (Claude Code v2.1.73+ supports MCP elicitation)
       - model_overrides: true (Claude Code supports /model command and model overrides)
       - max_output_tokens: { default: 64000, upper_bound: 128000 } (Opus 4.6 / Sonnet 4.6, REQ-107)

       **codex:**
       - smart_approvals: true (Codex routes reviews through guardian subagent, REQ-111)
       - plan_mode: false
       - sandbox_gvisor: false
       - sandbox_lxc: false
       - mcp_elicitation: false
       - model_overrides: true (Codex supports model selection)
       - max_output_tokens: null (not documented for GPT-5.4)

       **gemini:**
       - smart_approvals: false
       - plan_mode: true (enabled by default in Gemini CLI v0.34, REQ-114)
       - sandbox_gvisor: true (native gVisor sandboxing, REQ-114)
       - sandbox_lxc: false (experimental, not stable — set false until GA)
       - mcp_elicitation: false
       - model_overrides: true (Gemini CLI supports model selection)
       - max_output_tokens: null

       **opencode:**
       - smart_approvals: false
       - plan_mode: false
       - sandbox_gvisor: false
       - sandbox_lxc: false
       - mcp_elicitation: false
       - model_overrides: true (OpenCode supports model selection via config)
       - max_output_tokens: null

       **overstory:**
       - smart_approvals: false, plan_mode: false, sandbox_gvisor: false, sandbox_lxc: false
       - mcp_elicitation: false, model_overrides: true, max_output_tokens: null

       **superpowers:**
       - smart_approvals: false, plan_mode: false, sandbox_gvisor: false, sandbox_lxc: false
       - mcp_elicitation: false, model_overrides: true, max_output_tokens: null

       **grd:**
       - smart_approvals: false, plan_mode: false, sandbox_gvisor: false, sandbox_lxc: false
       - mcp_elicitation: false, model_overrides: false, max_output_tokens: null

    3. Run `npm run build:check` to verify no type errors.
  </action>
  <verify>
    npm run build:check passes.
    Grep lib/types.ts for 'smart_approvals' — must appear in BackendCapabilities.
    Grep lib/backend.ts for 'mcp_elicitation: true' — must appear exactly once (claude).
    Grep lib/backend.ts for 'smart_approvals: true' — must appear exactly once (codex).
  </verify>
  <done>
    BackendCapabilities interface extended with 7 new fields.
    All 7 backends have the new flags populated with correct values.
    Type check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add model_overrides_available to init context and write tests</name>
  <files>lib/context/execute.ts, tests/unit/backend.test.ts</files>
  <action>
    1. In lib/context/execute.ts, find the cmdInitExecutePhase function. After the
       `backend_capabilities` field in the result object, add:
       ```
       model_overrides_available: backendCaps.model_overrides === true,
       ```
       This surfaces the flag so executor agents know if they can override models.

    2. In tests/unit/backend.test.ts, add a new describe block for the capability flags:

       ```
       describe('new capability flags', () => {
         test('codex has smart_approvals: true', () => {
           expect(BACKEND_CAPABILITIES.codex.smart_approvals).toBe(true);
         });

         test('only codex has smart_approvals: true', () => {
           for (const backend of VALID_BACKENDS) {
             if (backend === 'codex') continue;
             expect(BACKEND_CAPABILITIES[backend].smart_approvals).toBe(false);
           }
         });

         test('gemini has plan_mode and sandbox_gvisor', () => {
           expect(BACKEND_CAPABILITIES.gemini.plan_mode).toBe(true);
           expect(BACKEND_CAPABILITIES.gemini.sandbox_gvisor).toBe(true);
           expect(BACKEND_CAPABILITIES.gemini.sandbox_lxc).toBe(false);
         });

         test('claude has mcp_elicitation: true', () => {
           expect(BACKEND_CAPABILITIES.claude.mcp_elicitation).toBe(true);
         });

         test('only claude has mcp_elicitation: true', () => {
           for (const backend of VALID_BACKENDS) {
             if (backend === 'claude') continue;
             expect(BACKEND_CAPABILITIES[backend].mcp_elicitation).toBe(false);
           }
         });

         test('claude has max_output_tokens with correct limits', () => {
           expect(BACKEND_CAPABILITIES.claude.max_output_tokens).toEqual({
             default: 64000,
             upper_bound: 128000,
           });
         });

         test('non-claude backends have null max_output_tokens', () => {
           for (const backend of VALID_BACKENDS) {
             if (backend === 'claude') continue;
             expect(BACKEND_CAPABILITIES[backend].max_output_tokens).toBeNull();
           }
         });

         test('all backends have model_overrides flag', () => {
           for (const backend of VALID_BACKENDS) {
             expect(typeof BACKEND_CAPABILITIES[backend].model_overrides).toBe('boolean');
           }
         });
       });
       ```

    3. Run `npx jest tests/unit/backend.test.ts` to confirm all tests pass.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts -- all tests pass with 0 failures.
    Grep lib/context/execute.ts for 'model_overrides_available' -- must appear.
  </verify>
  <done>
    model_overrides_available field added to cmdInitExecutePhase output.
    8 new tests verify capability flags for all backends.
    Backend test suite passes with 0 failures.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npm run build:check` passes (no type errors from extended interface)
- `npx jest tests/unit/backend.test.ts` passes with 0 failures
- `grep -c 'smart_approvals' lib/backend.ts` returns 7 (one per backend)
- `grep -c 'mcp_elicitation' lib/backend.ts` returns 7 (one per backend)
- `grep 'model_overrides_available' lib/context/execute.ts` returns the init context field
</verification>

<success_criteria>
1. BackendCapabilities interface has smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, model_overrides, max_output_tokens
2. BACKEND_CAPABILITIES.codex.smart_approvals is true; all others false
3. BACKEND_CAPABILITIES.gemini.plan_mode is true, sandbox_gvisor is true, sandbox_lxc is false
4. BACKEND_CAPABILITIES.claude.mcp_elicitation is true; all others false
5. BACKEND_CAPABILITIES.claude.max_output_tokens is { default: 64000, upper_bound: 128000 }
6. cmdInitExecutePhase includes model_overrides_available field
7. All tests pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/74-model-mappings-and-capability-flags/74-02-SUMMARY.md`
</output>
