---
phase: 74-model-mappings-and-capability-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/backend.ts, lib/types.ts, tests/unit/backend.test.ts]
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "DEFAULT_BACKEND_MODELS.codex.haiku is 'gpt-5.4-mini'"
    - "DEFAULT_BACKEND_MODELS.gemini.opus is 'gemini-3.1-pro' and sonnet is 'gemini-3.1-flash'"
    - "DEFAULT_BACKEND_MODELS.opencode reflects current GPT-5.4 availability"
    - "All existing backend detection tests still pass after model mapping changes"
  artifacts:
    - path: "lib/backend.ts"
      provides: "Updated DEFAULT_BACKEND_MODELS with new model mappings"
      contains: "gpt-5.4-mini"
    - path: "tests/unit/backend.test.ts"
      provides: "Updated test assertions for new model mappings"
      contains: "gpt-5.4-mini"
  key_links:
    - from: "lib/backend.ts"
      to: "lib/types.ts"
      via: "ModelTierMap type import"
      pattern: "import.*ModelTierMap"
    - from: "tests/unit/backend.test.ts"
      to: "lib/backend.ts"
      via: "DEFAULT_BACKEND_MODELS import"
      pattern: "DEFAULT_BACKEND_MODELS"
---

<objective>
Update model name mappings for Codex, Gemini, and OpenCode backends to reflect the latest model releases as of March 2026.

Purpose: Ensure GRD's abstract tier resolution (opus/sonnet/haiku) maps to the correct concrete model names for each backend, enabling proper model selection in evolve, autopilot, and subagent workflows.
Output: Updated DEFAULT_BACKEND_MODELS in lib/backend.ts with passing test assertions.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/backend.ts
@lib/types.ts
@tests/unit/backend.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DEFAULT_BACKEND_MODELS mappings</name>
  <files>lib/backend.ts</files>
  <action>
    Update the DEFAULT_BACKEND_MODELS constant in lib/backend.ts:

    1. **Codex haiku tier:** Change `haiku: 'gpt-5.3-codex-spark'` to `haiku: 'gpt-5.4-mini'`.
       The sonnet tier stays as `gpt-5.3-codex-spark` (still the mid-tier model).
       Rationale: GPT-5.4-mini is 2x faster than GPT-5.4, ideal for subagent/discovery work (REQ-110).

    2. **Gemini sonnet tier:** Change `sonnet: 'gemini-3-flash'` to `sonnet: 'gemini-3.1-flash'`.
       The opus tier (`gemini-3.1-pro`) and haiku tier (`gemini-3.1-flash-lite`) are already correct.
       Rationale: Gemini 3.1 Flash is the updated sonnet-equivalent (REQ-113).

    3. **OpenCode:** Verify the current mappings are correct. The existing mappings
       (`anthropic/claude-opus-4-6`, `anthropic/claude-sonnet-4-6`, `anthropic/claude-haiku-4-5`)
       already reflect GPT-5.4 availability since OpenCode uses provider-prefixed model names
       and routes through Anthropic by default. No changes needed unless the model list format
       has changed (REQ-116).

    4. Update the module header comment to reflect the new model versions:
       - Codex CLI line: add "GPT-5.4-mini" to the list
       - Gemini CLI line: change "3 Flash" to "3.1 Flash"

    Do NOT change claude, overstory, superpowers, or grd mappings.
  </action>
  <verify>
    npm run build:check passes (tsc --noEmit).
    Grep lib/backend.ts for 'gpt-5.4-mini' — must appear in codex.haiku.
    Grep lib/backend.ts for 'gemini-3.1-flash' — must appear in gemini.sonnet.
  </verify>
  <done>
    DEFAULT_BACKEND_MODELS.codex.haiku is 'gpt-5.4-mini',
    DEFAULT_BACKEND_MODELS.gemini.sonnet is 'gemini-3.1-flash',
    OpenCode mappings verified unchanged, module header updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update model mapping test assertions</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Update the DEFAULT_BACKEND_MODELS test assertions in tests/unit/backend.test.ts:

    1. Find the codex model mapping test (looks for `DEFAULT_BACKEND_MODELS.codex`).
       Update the expected haiku value from `'gpt-5.3-codex-spark'` to `'gpt-5.4-mini'`.
       Update the test description if it mentions the old model name.

    2. Find the gemini model mapping test (looks for `DEFAULT_BACKEND_MODELS.gemini`).
       Update the expected sonnet value from `'gemini-3-flash'` to `'gemini-3.1-flash'`.
       Update the test description if it mentions the old model name.

    3. Verify the opencode test assertions match the unchanged opencode mappings.

    4. Run `npx jest tests/unit/backend.test.ts` to confirm all tests pass.

    Do NOT add new tests — this task only updates existing assertions. New capability
    flag tests are handled in plan 74-02.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts -- all tests pass with 0 failures.
  </verify>
  <done>
    All DEFAULT_BACKEND_MODELS test assertions match the updated mappings.
    Backend test suite passes with 0 failures.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npm run build:check` passes (no type errors)
- `npx jest tests/unit/backend.test.ts` passes with 0 failures
- `grep 'gpt-5.4-mini' lib/backend.ts` returns the codex haiku mapping
- `grep 'gemini-3.1-flash' lib/backend.ts` returns the gemini sonnet mapping
</verification>

<success_criteria>
1. DEFAULT_BACKEND_MODELS.codex is { opus: 'gpt-5.4', sonnet: 'gpt-5.3-codex-spark', haiku: 'gpt-5.4-mini' }
2. DEFAULT_BACKEND_MODELS.gemini is { opus: 'gemini-3.1-pro', sonnet: 'gemini-3.1-flash', haiku: 'gemini-3.1-flash-lite' }
3. DEFAULT_BACKEND_MODELS.opencode unchanged and correct
4. All backend.test.ts tests pass
5. Type check passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/74-model-mappings-and-capability-flags/74-01-SUMMARY.md`
</output>
