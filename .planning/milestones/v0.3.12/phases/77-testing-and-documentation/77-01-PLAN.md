---
phase: 77-testing-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/backend.test.ts
  - tests/unit/context.test.ts
  - tests/unit/agent-audit.test.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "backend.test.ts verifies smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, and max_output_tokens flags for all four primary backends (claude, codex, gemini, opencode)"
    - "backend.test.ts verifies gpt-5.4-mini for Codex haiku tier, gemini-3.1-pro and gemini-3.1-flash for Gemini opus/sonnet, GPT-5.4 for OpenCode"
    - "context.test.ts verifies mcp_elicitation_available and model_overrides_available fields appear in cmdInitExecutePhase JSON output"
    - "agent-audit.test.ts verifies StopFailure and PostCompact hook registrations exist in plugin.json"
    - "agent-audit.test.ts verifies all 20 agents have effort, maxTurns, and disallowedTools frontmatter fields"
    - "npm test passes with 0 failures"
  artifacts:
    - path: "tests/unit/backend.test.ts"
      provides: "New capability flag and model mapping test assertions"
    - path: "tests/unit/context.test.ts"
      provides: "Init context tests for mcp_elicitation_available and model_overrides_available"
    - path: "tests/unit/agent-audit.test.ts"
      provides: "StopFailure/PostCompact hook tests and agent frontmatter field tests"
  key_links:
    - from: "tests/unit/backend.test.ts"
      to: "lib/backend.ts"
      via: "import BACKEND_CAPABILITIES, DEFAULT_BACKEND_MODELS"
      pattern: "BACKEND_CAPABILITIES|DEFAULT_BACKEND_MODELS"
    - from: "tests/unit/context.test.ts"
      to: "lib/context.ts"
      via: "import cmdInitExecutePhase"
      pattern: "cmdInitExecutePhase"
    - from: "tests/unit/agent-audit.test.ts"
      to: ".claude-plugin/plugin.json"
      via: "JSON parse and hook assertions"
      pattern: "plugin.json"
---

<objective>
Add unit tests covering all new model mappings, capability flags, init context fields, hook registrations, and agent frontmatter fields introduced in Phases 74-76.

Purpose: Ensure all v0.3.12 changes are covered by automated tests so regressions are caught immediately. This is the testing half of Phase 77 (REQ-118).

Output: Updated test files with new describe blocks and assertions for the 6 new capability flags, 3 model mapping updates, 2 init context fields, 2 hook registrations, and 3 agent frontmatter fields.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@lib/backend.ts
@lib/context.ts
@tests/unit/backend.test.ts
@tests/unit/context.test.ts
@tests/unit/agent-audit.test.ts
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capability flag and model mapping tests to backend.test.ts</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Add a new describe block 'v0.3.12 capability flags' within the existing BACKEND_CAPABILITIES test section of backend.test.ts.

    For capability flags, add tests that verify for ALL FOUR primary backends (claude, codex, gemini, opencode):
    - `smart_approvals`: true for codex, false for claude/gemini/opencode
    - `plan_mode`: true for gemini, false for claude/codex/opencode
    - `sandbox_gvisor`: true for gemini, false for claude/codex/opencode
    - `sandbox_lxc`: true for gemini, false for claude/codex/opencode
    - `mcp_elicitation`: true for claude, false for codex/gemini/opencode
    - `max_output_tokens`: present for claude (value should be 128000 or similar), absent or falsy for others

    Use the existing test pattern from backend.test.ts where BACKEND_CAPABILITIES is imported and tested directly:
    ```
    test('smart_approvals is true only for codex', () => {
      expect(BACKEND_CAPABILITIES.codex.smart_approvals).toBe(true);
      expect(BACKEND_CAPABILITIES.claude.smart_approvals).toBe(false);
      expect(BACKEND_CAPABILITIES.gemini.smart_approvals).toBe(false);
      expect(BACKEND_CAPABILITIES.opencode.smart_approvals).toBe(false);
    });
    ```

    For model mappings, add a describe block 'v0.3.12 model mappings' that verifies:
    - `DEFAULT_BACKEND_MODELS.codex.haiku` is `'gpt-5.4-mini'`
    - `DEFAULT_BACKEND_MODELS.gemini.opus` is `'gemini-3.1-pro'`
    - `DEFAULT_BACKEND_MODELS.gemini.sonnet` is `'gemini-3.1-flash'`
    - `DEFAULT_BACKEND_MODELS.opencode.opus` contains `'gpt-5.4'` or appropriate model string reflecting GPT-5.4 availability

    Follow the existing test() naming convention (natural language descriptions, not it()).
    Do NOT lower any coverage thresholds.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts --no-coverage (Level 1: Sanity)
    All new tests pass. Existing tests unaffected.
  </verify>
  <done>
    backend.test.ts has new test blocks for all 6 capability flags across 4 backends and all model mapping updates. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add init context and hook registration tests</name>
  <files>tests/unit/context.test.ts, tests/unit/agent-audit.test.ts</files>
  <action>
    **context.test.ts changes:**

    Add a new describe block within the cmdInitExecutePhase section, following the pattern of the existing 'effort_level fields' and 'cron_available in backend_capabilities' describe blocks.

    Add a describe block 'mcp_elicitation_available and model_overrides_available fields' that:
    1. Uses the existing tmpDir fixture from the parent describe
    2. Calls captureOutput(() => cmdInitExecutePhase(tmpDir, '1', new Set(), false))
    3. Parses stdout as JSON
    4. Asserts `parsed.mcp_elicitation_available` is a boolean (true when claude backend)
    5. Asserts `parsed.model_overrides_available` is a boolean
    6. Both fields must be present in the JSON output (not undefined)

    **agent-audit.test.ts changes:**

    Add tests to the existing 'plugin.json hook registration' describe block:

    1. Test 'StopFailure hook is registered':
       - `pluginJson.hooks.StopFailure` is defined and is an array
       - First entry has hooks array with at least one command hook
       - Command string contains 'stop-failure' or similar handler identifier
       - Has error suppression (2>/dev/null || true)
       - Has a timeout between 1-60 seconds

    2. Test 'PostCompact hook is registered':
       - Same structure as StopFailure test
       - Command string contains 'post-compact' or similar handler identifier

    3. Update the 'all hooks have error suppression' test to include StopFailure and PostCompact in the hookNames array

    4. Update the 'hook timeout values are reasonable' test to include StopFailure and PostCompact

    Add a new describe block 'Agent frontmatter — effort, maxTurns, disallowedTools' that:
    1. Reads all 20 agent files from agents/ directory
    2. Tests 'all agents have effort field in frontmatter' — parse YAML frontmatter, check `effort` exists and is one of 'low', 'medium', 'high'
    3. Tests 'bounded agents have maxTurns field' — at minimum, verify that the field exists in the frontmatter YAML of agents that should be bounded (check that at least some agents have it, not necessarily all)
    4. Tests 'agents with restrictions have disallowedTools field' — verify that the field exists in frontmatter for agents that should have it (check that at least some agents have it)

    Follow existing test patterns: use test() not it(), natural language names.
  </action>
  <verify>
    npx jest tests/unit/context.test.ts tests/unit/agent-audit.test.ts --no-coverage (Level 1: Sanity)
    All new tests pass. Existing tests unaffected.
  </verify>
  <done>
    context.test.ts verifies mcp_elicitation_available and model_overrides_available fields in init context JSON.
    agent-audit.test.ts verifies StopFailure and PostCompact hooks in plugin.json and effort/maxTurns/disallowedTools in agent frontmatter.
    All tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/backend.test.ts tests/unit/context.test.ts tests/unit/agent-audit.test.ts --no-coverage` — all new and existing tests pass
- `npm test` — full suite passes with 0 failures

Level 2 (Proxy): N/A — this is pure test code
Level 3 (Deferred): N/A
</verification>

<success_criteria>
1. All 6 new capability flags tested across 4 backends in backend.test.ts
2. Model mapping assertions for codex haiku (gpt-5.4-mini), gemini opus/sonnet (3.1-pro/3.1-flash), opencode (GPT-5.4) present
3. Init context tests verify mcp_elicitation_available and model_overrides_available fields
4. StopFailure and PostCompact hook registration tests pass in agent-audit.test.ts
5. Agent frontmatter tests verify effort/maxTurns/disallowedTools fields exist
6. `npm test` passes with 0 failures — no coverage thresholds lowered
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/77-testing-and-documentation/77-01-SUMMARY.md`
</output>
