---
phase: 77-testing-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "CLAUDE.md documents the /effort slash command interaction with GRD's effort profile system"
    - "CLAUDE.md contains a capability flags table listing all backend capabilities including the 6 new flags (smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, max_output_tokens)"
    - "CLAUDE.md documents agent frontmatter fields (effort, maxTurns, disallowedTools)"
    - "CLAUDE.md documents CLAUDE_PLUGIN_DATA usage boundary (project state vs plugin state)"
    - "CLAUDE.md or inline comments document Codex realtime websocket sessions, Gemini tracker tools and A2A timeout, and OpenCode worktree session fix"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated documentation covering all v0.3.12 changes"
      contains: "effort"
  key_links:
    - from: "CLAUDE.md"
      to: "lib/backend.ts"
      via: "documents BACKEND_CAPABILITIES and EFFORT_PROFILES"
      pattern: "capability|effort"
---

<objective>
Update CLAUDE.md with documentation for all v0.3.12 changes: new capability flags, agent frontmatter fields, /effort slash command interaction, CLAUDE_PLUGIN_DATA usage boundary, and backend-specific notes for Codex, Gemini, and OpenCode.

Purpose: Ensure developers and Claude agents have accurate documentation of all backend features, capability flags, and configuration options added in v0.3.12. This covers REQ-109, REQ-112, REQ-115, REQ-117, REQ-119.

Output: Updated CLAUDE.md with new sections for backend capabilities, agent frontmatter, and backend-specific notes.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@lib/backend.ts
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend capabilities and agent frontmatter documentation to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
    Add the following new sections to CLAUDE.md, placed AFTER the existing "Testing" section and BEFORE the "Gotchas" section:

    **1. Backend Capabilities section:**
    Add a `## Backend Capabilities` section with a table showing all capability flags for the 4 primary backends (claude, codex, gemini, opencode). Include all flags: subagents, parallel, teams, hooks, mcp, native_worktree_isolation, effort, http_hooks, cron, smart_approvals, plan_mode, sandbox_gvisor, sandbox_lxc, mcp_elicitation, max_output_tokens. Source values from BACKEND_CAPABILITIES in lib/backend.ts.

    **2. Agent Frontmatter section:**
    Add a `## Agent Frontmatter` section documenting the three agent frontmatter fields:
    - `effort` (low/medium/high) — controls reasoning depth, sourced from EFFORT_PROFILES in backend.ts
    - `maxTurns` — caps the number of turns an agent can take
    - `disallowedTools` — restricts which tools an agent can use
    Note that effort values are set per-agent and per-profile (quality/balanced/budget).

    **3. Effort Slash Command note:**
    Within the Agent Frontmatter section or as a subsection, add a note:
    - `/effort` slash command (Claude Code v2.1.76+) lets users override effort level mid-session
    - GRD sets effort via agent frontmatter using EFFORT_PROFILES; user `/effort` overrides take precedence
    - This means a user can lower effort for a fast iteration or raise it for thorough analysis, independent of GRD's profile system

    **4. Plugin Data Boundary note:**
    Add a `## Plugin Data` section documenting:
    - `.planning/` = project-scoped state (plans, roadmap, config, research)
    - `CLAUDE_PLUGIN_DATA` = plugin-scoped state that survives plugin updates, used for cross-project config (scheduler state, evolve global config)
    - Clear boundary: project artifacts go in `.planning/`, plugin infrastructure goes in `CLAUDE_PLUGIN_DATA`

    **5. Backend-Specific Notes section:**
    Add a `## Backend-Specific Notes` section with subsections for each backend:

    - **Codex CLI (0.115.0+):** Note realtime websocket sessions and filesystem RPC capabilities available but not currently used by GRD. Smart approvals route review requests through a guardian subagent.
    - **Gemini CLI (v0.31-v0.34):** Note tracker CRUD tools (v0.34), A2A agent timeout increase to 30 minutes, browser agent (experimental, v0.31), generalist agent (v0.32), plan mode (enabled by default in v0.34), gVisor and LXC sandboxing.
    - **OpenCode (v1.2.25-v1.2.27):** Note fix for lost sessions across worktrees and orphan branches (v1.2.27), relevant to GRD's worktree isolation. Note 5-minute chunk timeout increase (from 2 min). Multi-account workspace authentication and non-OpenAI Azure completions endpoint support.

    Keep all additions concise — bullet points preferred over prose. Match the existing terse style of CLAUDE.md. Do NOT remove or modify any existing content.
  </action>
  <verify>
    Read CLAUDE.md and confirm:
    1. Backend Capabilities table exists with all flags
    2. Agent Frontmatter section exists with effort/maxTurns/disallowedTools
    3. /effort interaction note present
    4. CLAUDE_PLUGIN_DATA boundary documented
    5. Backend-specific notes for Codex, Gemini, OpenCode present
    (Level 1: Sanity — visual inspection)
  </verify>
  <done>
    CLAUDE.md updated with 4-5 new sections covering backend capabilities table, agent frontmatter fields, /effort interaction, CLAUDE_PLUGIN_DATA boundary, and backend-specific notes for Codex/Gemini/OpenCode. Existing content unchanged.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- CLAUDE.md contains "Backend Capabilities" section with capability flags table
- CLAUDE.md contains "Agent Frontmatter" section with effort/maxTurns/disallowedTools
- CLAUDE.md mentions /effort slash command interaction
- CLAUDE.md contains CLAUDE_PLUGIN_DATA documentation
- CLAUDE.md contains backend-specific notes for Codex, Gemini, OpenCode
- `npm run lint` passes (no lint errors introduced)

Level 2 (Proxy): N/A — documentation only
Level 3 (Deferred): N/A
</verification>

<success_criteria>
1. CLAUDE.md has capability flags table with all 15 flags across 4 backends
2. Agent frontmatter fields documented (effort, maxTurns, disallowedTools)
3. /effort slash command interaction with EFFORT_PROFILES documented
4. CLAUDE_PLUGIN_DATA vs .planning/ boundary clearly stated
5. Codex realtime/filesystem RPC, Gemini tracker/A2A/sandboxing, OpenCode worktree fix documented
6. Existing CLAUDE.md content unchanged
7. `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/77-testing-and-documentation/77-02-SUMMARY.md`
</output>
