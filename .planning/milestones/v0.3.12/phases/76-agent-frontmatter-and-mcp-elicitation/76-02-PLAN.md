---
phase: 76-agent-frontmatter-and-mcp-elicitation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/backend.ts
  - lib/context/execute.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "BackendCapabilities interface includes `mcp_elicitation: boolean` field"
    - "BACKEND_CAPABILITIES.claude has `mcp_elicitation: true`; all other backends have `mcp_elicitation: false`"
    - "cmdInitExecutePhase JSON output includes `mcp_elicitation_available` boolean field"
    - "cmdInitExecutePhase JSON output includes `model_overrides_available` boolean field"
    - "model_overrides_available is true when .claude/settings.json or ~/.claude/settings.json contains modelOverrides key"
  artifacts:
    - path: "lib/types.ts"
      provides: "Updated BackendCapabilities with mcp_elicitation field"
      contains: "mcp_elicitation"
    - path: "lib/backend.ts"
      provides: "BACKEND_CAPABILITIES with mcp_elicitation set per backend"
      contains: "mcp_elicitation"
    - path: "lib/context/execute.ts"
      provides: "cmdInitExecutePhase with mcp_elicitation_available and model_overrides_available"
      contains: "mcp_elicitation_available"
  key_links:
    - from: "lib/context/execute.ts"
      to: "lib/backend.ts"
      via: "getBackendCapabilities().mcp_elicitation"
      pattern: "backendCaps\\.mcp_elicitation"
    - from: "lib/context/execute.ts"
      to: "lib/types.ts"
      via: "BackendCapabilities type import"
      pattern: "BackendCapabilities"
---

<objective>
Add `mcp_elicitation_available` and `model_overrides_available` fields to the execute-phase init context per REQ-105 and REQ-106.

Purpose: Agents need to know their execution environment. MCP elicitation (Claude Code v2.1.76) allows MCP servers to request structured input mid-task. modelOverrides (v2.1.73) lets users remap models. Surfacing these in init context lets agents adapt behavior.
Output: Updated types, capability flags, and init context with two new environment-awareness fields.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/types.ts
@lib/backend.ts
@lib/context/execute.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mcp_elicitation capability flag to backend infrastructure</name>
  <files>lib/types.ts, lib/backend.ts</files>
  <action>
    1. In lib/types.ts, add `mcp_elicitation: boolean;` to the `BackendCapabilities` interface, after the `cron` field.

    2. In lib/backend.ts, add `mcp_elicitation` to each backend in `BACKEND_CAPABILITIES`:
       - claude: `mcp_elicitation: true` (Claude Code v2.1.76+ supports it)
       - codex: `mcp_elicitation: false`
       - gemini: `mcp_elicitation: false`
       - opencode: `mcp_elicitation: false`
       - overstory: `mcp_elicitation: false`
       - superpowers: `mcp_elicitation: false`
       - grd: `mcp_elicitation: false`

    Place the new field after `cron` in each backend's capability object to maintain consistent ordering with the interface.
  </action>
  <verify>
    Run `npm run build:check` to confirm TypeScript compiles with the new field.
    Grep lib/backend.ts for `mcp_elicitation` — should appear 7 times (once per backend). (Level 1: Sanity)
  </verify>
  <done>BackendCapabilities interface and all 7 backend entries include mcp_elicitation boolean field. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Surface mcp_elicitation_available and model_overrides_available in init context</name>
  <files>lib/context/execute.ts</files>
  <action>
    In `cmdInitExecutePhase`, add two new fields to the `result` object after the `overstory_config` block (around line 316, before the closing of the result object):

    1. `mcp_elicitation_available`:
       ```typescript
       mcp_elicitation_available: backendCaps.mcp_elicitation === true,
       ```
       This is true only for Claude backend.

    2. `model_overrides_available`:
       Detect whether the user has `modelOverrides` configured. Check two locations:
       - Project-level: `${cwd}/.claude/settings.json`
       - User-level: `~/.claude/settings.json`

       Read each file, parse JSON, check for `modelOverrides` key with a non-empty object value.

       ```typescript
       model_overrides_available: (() => {
         try {
           const locations = [
             path.join(cwd, '.claude', 'settings.json'),
             path.join(process.env.HOME || '', '.claude', 'settings.json'),
           ];
           for (const loc of locations) {
             if (!fs.existsSync(loc)) continue;
             const data = JSON.parse(fs.readFileSync(loc, 'utf-8'));
             if (data && typeof data === 'object' && data.modelOverrides &&
                 typeof data.modelOverrides === 'object' && Object.keys(data.modelOverrides).length > 0) {
               return true;
             }
           }
           return false;
         } catch {
           return false;
         }
       })(),
       ```

    Add a brief comment above the two new fields: `// MCP elicitation and model overrides awareness (REQ-105, REQ-106)`.

    Also add `mcp_elicitation_available` and `model_overrides_available` to `cmdInitPlanPhase` output in the same file (if it exists) since planners also benefit from knowing the execution environment. If the function follows the same pattern as cmdInitExecutePhase, add the same two fields. If the structure differs significantly, skip the planner init and note the deviation.
  </action>
  <verify>
    Run `npm run build:check` to confirm TypeScript compiles.
    Run `node bin/grd-tools.js init execute-phase 76 --json 2>/dev/null | grep -E 'mcp_elicitation|model_overrides'` and confirm both fields appear. (Level 1: Sanity)
  </verify>
  <done>cmdInitExecutePhase JSON output includes `mcp_elicitation_available` (boolean) and `model_overrides_available` (boolean). Both fields derive from runtime detection, not hardcoded values.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npm run build:check` passes (TypeScript compiles with new BackendCapabilities field)
- `mcp_elicitation` appears in BackendCapabilities interface
- `mcp_elicitation` appears 7 times in BACKEND_CAPABILITIES (one per backend)
- cmdInitExecutePhase output includes both new fields
- Only claude backend has mcp_elicitation: true
</verification>

<success_criteria>
- BackendCapabilities has mcp_elicitation field, set correctly per backend
- cmdInitExecutePhase includes mcp_elicitation_available and model_overrides_available
- model_overrides_available correctly detects .claude/settings.json modelOverrides config
- TypeScript compiles cleanly with zero new errors
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.12/phases/76-agent-frontmatter-and-mcp-elicitation/76-02-SUMMARY.md`
</output>
