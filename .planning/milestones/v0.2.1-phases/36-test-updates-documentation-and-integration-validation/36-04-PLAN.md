---
phase: 36-test-updates-documentation-and-integration-validation
plan: 04
type: execute
wave: 3
depends_on: [2, 3]
files_modified:
  - CLAUDE.md
  - docs/long-term-roadmap-tutorial.md
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "CLAUDE.md Planning Directory section shows milestones/{milestone}/phases/ hierarchy"
    - "docs/long-term-roadmap-tutorial.md references milestone-scoped research paths"
    - "docs/CHANGELOG.md is NOT modified (historical references preserved)"
    - "DEFER-34-01 validated: commands produce milestone-scoped paths end-to-end"
    - "DEFER-35-01 validated: migration command works on old-style layout"
    - "DEFER-35-02 validated: milestone completion writes archived.json marker"
    - "Full test suite passes with zero regressions"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated documentation with new hierarchy"
      contains: "milestones/"
    - path: "docs/long-term-roadmap-tutorial.md"
      provides: "Updated tutorial with milestone-scoped paths"
      contains: "milestones/"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/milestones/"
      via: "documentation accurately describes on-disk directory layout"
      pattern: "milestones.*phases"
---

<objective>
Update project documentation to reflect the new milestone-scoped hierarchy and resolve deferred validations from Phases 34 and 35.

Purpose: REQ-68 requires CLAUDE.md's Planning Directory section to reflect the new hierarchy. REQ-69 requires docs/ files to be updated. Three deferred validations (DEFER-34-01, DEFER-35-01, DEFER-35-02) must be validated end-to-end now that all code is integrated.

Output: Updated documentation, resolved deferred validations, final passing test suite.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-test-updates-documentation-and-integration-validation/36-02-SUMMARY.md
@.planning/phases/36-test-updates-documentation-and-integration-validation/36-03-SUMMARY.md
@CLAUDE.md
@docs/long-term-roadmap-tutorial.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and docs/ with new hierarchy</name>
  <files>CLAUDE.md, docs/long-term-roadmap-tutorial.md</files>
  <action>
    1. Update CLAUDE.md "Planning Directory" section. Replace the current tree:

       Current:
       ```
       .planning/
       ├── PROJECT.md
       ├── ...
       ├── research/
       ├── phases/
       │   └── {NN}-{name}/
       ├── codebase/
       └── todos/
       ```

       New:
       ```
       .planning/
       ├── PROJECT.md              # Product vision, research objectives, quality targets
       ├── ROADMAP.md              # Phase structure with verification levels
       ├── STATE.md                # Living memory with baselines, deferred validations
       ├── BASELINE.md             # Current quantitative performance metrics
       ├── PRODUCT-QUALITY.md      # Product-level quality targets and gaps
       ├── REQUIREMENTS.md         # Requirements with traceability
       ├── config.json             # GRD configuration
       ├── TRACKER.md              # Issue tracker mapping (created at runtime)
       └── milestones/
           ├── {milestone}/                    # e.g., v0.2.1 (active milestone)
           │   ├── phases/
           │   │   └── {NN}-{name}/
           │   │       ├── {NN}-RESEARCH.md    # Phase research with paper references
           │   │       ├── {NN}-CONTEXT.md     # User decisions from discuss-phase
           │   │       ├── {NN}-{MM}-PLAN.md   # Execution plan with verification_level
           │   │       ├── {NN}-{MM}-SUMMARY.md # Execution results with experiment data
           │   │       ├── {NN}-{MM}-REVIEW.md # Code review findings (per wave)
           │   │       ├── {NN}-EVAL.md        # Tiered evaluation plan and results
           │   │       └── {NN}-VERIFICATION.md # Tiered verification report
           │   ├── research/                   # Milestone-scoped research knowledge base
           │   │   ├── LANDSCAPE.md            # SoTA map (methods, benchmarks, trends)
           │   │   ├── PAPERS.md               # Paper index with summaries
           │   │   ├── BENCHMARKS.md           # Evaluation metrics and datasets
           │   │   ├── KNOWHOW.md              # Paper-to-production gap knowledge
           │   │   └── deep-dives/             # Individual paper analyses
           │   │       └── {paper-slug}.md
           │   ├── codebase/                   # Codebase analysis (from map-codebase)
           │   └── todos/                      # Milestone-scoped captured ideas
           │       ├── pending/
           │       └── completed/
           └── anonymous/                      # Operations without a milestone
               ├── quick/
               │   └── {N}-{slug}/
               │       └── {N}-SUMMARY.md
               ├── research/
               └── todos/
       ```

    2. Update CLAUDE.md test count from "1,577 tests" to the current count (1,631+)
       after confirming with npm test output from Plans 02/03.

    3. Update CLAUDE.md lib/ module count if paths.js was added (should be 19 modules
       if lib/paths.js was added in Phase 32; currently shows 18).

    4. In docs/long-term-roadmap-tutorial.md, update 2 references:
       - Line 292: `.planning/research/LANDSCAPE.md`
         -> `.planning/milestones/{milestone}/research/LANDSCAPE.md`
         (or describe it generically since the tutorial is conceptual)
       - Line 298: `.planning/research/deep-dives/flamingo.md`
         -> `.planning/milestones/{milestone}/research/deep-dives/flamingo.md`

    5. DO NOT modify docs/CHANGELOG.md. Its references to old paths (line 10 and line 200)
       are historical entries documenting what happened in past versions. Changing historical
       changelog entries is incorrect -- they should reflect what was true at the time.

    6. Verify no other docs/ files reference old paths:
       grep -rn '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' docs/ --include='*.md'
       Only CHANGELOG.md should remain (preserved historical references).
  </action>
  <verify>
    grep '\.planning/phases/' CLAUDE.md returns 0 results (Level 1: Sanity)
    grep 'milestones/' CLAUDE.md returns multiple results confirming new hierarchy (Level 1: Sanity)
    grep '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' docs/long-term-roadmap-tutorial.md returns 0 results (Level 2: Proxy)
    docs/CHANGELOG.md unchanged (git diff shows no changes) (Level 1: Sanity)
  </verify>
  <done>
    CLAUDE.md Planning Directory section reflects milestone-scoped hierarchy (REQ-68),
    docs/long-term-roadmap-tutorial.md updated (REQ-69),
    CHANGELOG.md preserved as historical record.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve deferred validations and run final full test suite</name>
  <files></files>
  <action>
    Resolve three deferred validations that were deferred to Phase 36:

    DEFER-34-01: End-to-end command execution with milestone-scoped paths
    - Create a temporary directory with a STATE.md containing a milestone (e.g., v1.0)
    - Create the milestone-scoped directory structure
    - Run multiple grd-tools commands that produce path output (init execute-phase,
      state load, phase-plan-index) and verify all paths in JSON output use
      milestones/{milestone}/ prefix
    - Run: node bin/grd-tools.js init execute-phase 1 in the temp dir
    - Verify output JSON contains phases_dir, research_dir, codebase_dir fields
      with milestone-scoped paths
    - Document results

    DEFER-35-01: Real-world migration on a live project with old-style layout
    - Create a temporary directory mimicking an old-style .planning/ layout:
      .planning/phases/01-test/ with files, .planning/research/ with files,
      .planning/todos/pending/ with files
    - Add STATE.md with a real milestone (e.g., v2.0)
    - Run: node bin/grd-tools.js migrate-dirs
    - Verify all files moved to milestones/v2.0/phases/, milestones/v2.0/research/,
      milestones/v2.0/todos/
    - Run migrate-dirs again to verify idempotency
    - Document results

    DEFER-35-02: End-to-end milestone completion flow with new-style layout
    - Create a temporary directory with milestone-scoped phases already under
      milestones/v1.0/phases/
    - Add required STATE.md, config.json, ROADMAP.md
    - Run: node bin/grd-tools.js milestone complete --name v1.0
    - Verify archived.json exists in milestones/v1.0/
    - Verify no redundant v1.0-phases/ archive was created
    - Document results

    After resolving all three:
    1. Run npm test -- confirm full suite passes (1,631+ tests, zero failures)
    2. Run npm run lint -- confirm zero errors
    3. Run npm run format:check -- confirm clean formatting
    4. Update .planning/STATE.md:
       - Resolve DEFER-34-01, DEFER-35-01, DEFER-35-02 (mark as RESOLVED)
       - Update test count
       - Update active phase to Phase 36 complete
  </action>
  <verify>
    DEFER-34-01 validation output shows milestone-scoped paths (Level 2: Proxy)
    DEFER-35-01 validation output shows successful migration (Level 2: Proxy)
    DEFER-35-02 validation output shows archived.json created (Level 2: Proxy)
    npm test passes with 1,631+ tests, zero failures (Level 2: Proxy)
    npm run lint exits 0 (Level 1: Sanity)
    npm run format:check exits 0 (Level 1: Sanity)
  </verify>
  <done>
    All three deferred validations resolved and documented,
    full test suite passes with zero regressions,
    STATE.md updated with resolved deferrals.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): CLAUDE.md contains milestones/ hierarchy, CHANGELOG.md unchanged
Level 2 (Proxy): All 3 deferred validations pass end-to-end
Level 2 (Proxy): npm test full suite passes with 1,631+ tests, zero failures (REQ-66)
Level 2 (Proxy): Zero old-path references remain in codebase (except CHANGELOG.md historical entries and migrate-dirs tests)
</verification>

<success_criteria>
- CLAUDE.md Planning Directory section shows milestones/{milestone}/ hierarchy (REQ-68)
- docs/long-term-roadmap-tutorial.md uses milestone-scoped paths (REQ-69)
- docs/CHANGELOG.md NOT modified (historical) (REQ-69 exclusion)
- DEFER-34-01 resolved: commands produce milestone-scoped paths
- DEFER-35-01 resolved: migration script works end-to-end on old-style layout
- DEFER-35-02 resolved: milestone completion with new-style layout produces archived.json
- npm test reports 1,631+ tests passing with zero failures (REQ-66)
- STATE.md updated with resolved deferrals and current position
</success_criteria>

<output>
After completion, create `.planning/phases/36-test-updates-documentation-and-integration-validation/36-04-SUMMARY.md`
</output>
