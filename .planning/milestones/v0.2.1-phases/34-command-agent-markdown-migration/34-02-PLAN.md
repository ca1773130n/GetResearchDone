---
phase: 34-command-agent-markdown-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/execute-phase.md
  - commands/verify-work.md
  - commands/discuss-phase.md
  - commands/map-codebase.md
  - commands/pause-work.md
  - commands/add-todo.md
  - commands/check-todos.md
  - commands/quick.md
  - commands/progress.md
  - commands/resume-project.md
  - commands/remove-phase.md
  - commands/complete-milestone.md
  - commands/add-phase.md
  - commands/insert-phase.md
  - commands/plan-milestone-gaps.md
  - commands/new-milestone.md
  - commands/help.md
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "Zero occurrences of hardcoded `.planning/phases/` in operational bash commands in execute-phase.md, verify-work.md, discuss-phase.md, pause-work.md, progress.md, resume-project.md, remove-phase.md, complete-milestone.md, add-phase.md, insert-phase.md, plan-milestone-gaps.md — all replaced with `${phases_dir}`, `${phase_dir}`, or equivalent init-derived variables"
    - "Zero occurrences of hardcoded `.planning/codebase/` in operational paths in map-codebase.md — replaced with `${codebase_dir}`"
    - "Zero occurrences of hardcoded `.planning/todos/` in operational paths in add-todo.md and check-todos.md — replaced with `${todos_dir}`"
    - "Zero occurrences of hardcoded `.planning/quick/` in operational paths in quick.md — replaced with `${quick_dir}`"
    - "All 17 command files remain syntactically valid markdown with intact YAML frontmatter"
  artifacts:
    - path: "commands/execute-phase.md"
      provides: "Execute-phase command with phase paths from init context"
    - path: "commands/verify-work.md"
      provides: "Verify-work command with phase paths from init context"
    - path: "commands/map-codebase.md"
      provides: "Map-codebase command with codebase_dir from init context"
    - path: "commands/add-todo.md"
      provides: "Add-todo command with todos_dir from init context"
    - path: "commands/quick.md"
      provides: "Quick command with quick_dir from init context"
  key_links:
    - from: "commands/execute-phase.md"
      to: "lib/context.js"
      via: "grd-tools.js init execute-phase — provides phases_dir, phase_dir"
      pattern: "init execute-phase"
    - from: "commands/map-codebase.md"
      to: "lib/context.js"
      via: "grd-tools.js init map-codebase — provides codebase_dir"
      pattern: "init map-codebase"
    - from: "commands/add-todo.md"
      to: "lib/context.js"
      via: "grd-tools.js init todos — provides todos_dir"
      pattern: "init todos"
---

<objective>
Migrate the remaining 17 command markdown files from hardcoded `.planning/` subdirectory paths to init-context-derived variables. These files use phase, codebase, todos, and quick directory paths and together account for the remaining ~36 hardcoded path occurrences in commands/.

Purpose: Complete REQ-57 by ensuring every command markdown file consumes milestone-scoped paths from init context rather than constructing paths with hardcoded strings. Many of these commands already call init — they just need to parse and use the new path fields.

Output: 17 updated command markdown files with all operational paths using init-derived variables
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@commands/execute-phase.md — target (1 path occurrence in commit line)
@commands/verify-work.md — target (5 occurrences)
@commands/discuss-phase.md — target (1 occurrence)
@commands/map-codebase.md — target (8 occurrences)
@commands/pause-work.md — target (4 occurrences)
@commands/add-todo.md — target (5 occurrences)
@commands/check-todos.md — target (1 occurrence)
@commands/quick.md — target (3 occurrences)
@commands/progress.md — target (3 occurrences)
@commands/resume-project.md — target (3 occurrences)
@commands/remove-phase.md — target (2 occurrences)
@commands/complete-milestone.md — target (2 occurrences)
@commands/add-phase.md — target (1 occurrence)
@commands/insert-phase.md — target (1 occurrence)
@commands/plan-milestone-gaps.md — target (1 occurrence)
@commands/new-milestone.md — target (1 occurrence)
@commands/help.md — target (1 occurrence, documentation only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate phase-related commands (execute-phase, verify-work, discuss-phase, pause-work, progress, resume-project, remove-phase, complete-milestone, add-phase, insert-phase, plan-milestone-gaps)</name>
  <files>commands/execute-phase.md, commands/verify-work.md, commands/discuss-phase.md, commands/pause-work.md, commands/progress.md, commands/resume-project.md, commands/remove-phase.md, commands/complete-milestone.md, commands/add-phase.md, commands/insert-phase.md, commands/plan-milestone-gaps.md</files>
  <action>
    Most of these commands already call init and already have `phase_dir` from init JSON. After Phase 33, init also provides `phases_dir`. The migration is to replace remaining hardcoded `.planning/phases/` references with `${phases_dir}` or `${phase_dir}` from init.

    **commands/execute-phase.md (1 occurrence in commit command):**

    Already calls `init execute-phase` and uses `phase_dir` extensively. After Phase 33, `phases_dir` is also available.

    - Line 585: `--files .planning/ROADMAP.md .planning/STATE.md .planning/phases/{phase_dir}/*-VERIFICATION.md .planning/REQUIREMENTS.md`
      -> `--files .planning/ROADMAP.md .planning/STATE.md ${phase_dir}/*-VERIFICATION.md .planning/REQUIREMENTS.md`
      Note: `phase_dir` from init already contains the full relative path like `phases/34-command-agent-markdown-migration`. Use it directly.

    **commands/verify-work.md (5 occurrences):**

    Already calls `init verify-work` and has `phase_dir`. Replace remaining hardcoded paths:
    - Line 203: `Write to .planning/phases/XX-name/{phase}-UAT.md` -> `Write to ${phase_dir}/{phase}-UAT.md`
    - Line 365: `--files ".planning/phases/XX-name/{phase}-UAT.md"` -> `--files "${phase_dir}/{phase}-UAT.md"`
    - Line 461: `--files ".planning/phases/XX-name/{phase}-UAT.md"` -> `--files "${phase_dir}/{phase}-UAT.md"`
    - Line 495: `@.planning/phases/{phase_dir}/{phase}-UAT.md` -> `@${phase_dir}/{phase}-UAT.md`
    - Line 551: `@.planning/phases/{phase_dir}/*-PLAN.md` -> `@${phase_dir}/*-PLAN.md`

    **commands/discuss-phase.md (1 occurrence):**

    Already calls `init phase-op` and has `phase_dir`. The line 145 output message uses `phase_dir` correctly. Check for remaining hardcoded references. The path at line 145 (`Created: .planning/phases/${PADDED_PHASE}-${SLUG}/${PADDED_PHASE}-CONTEXT.md`) should use `${phase_dir}/${padded_phase}-CONTEXT.md`.

    **commands/pause-work.md (4 occurrences):**

    Does NOT call init. Add init call:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init resume)
    ```
    Parse JSON for: `phases_dir`, `phase_dir`.

    Replace:
    - Line 19: `ls -lt .planning/phases/*/PLAN.md` -> `ls -lt ${phases_dir}/*/PLAN.md`
    - Line 40: `Write handoff to .planning/phases/XX-name/.continue-here.md` -> `Write handoff to ${phase_dir}/.continue-here.md`
    - Line 50: `--files .planning/phases/*/.continue-here.md` -> `--files ${phase_dir}/.continue-here.md`
    - Line 56: `Handoff created: .planning/phases/[XX-name]/.continue-here.md` -> `Handoff created: ${phase_dir}/.continue-here.md`

    **commands/progress.md (3 occurrences):**

    Already calls `init progress`. After Phase 33, `phases_dir` available.
    - Lines 145-147: `ls -1 .planning/phases/[current-phase-dir]/*-PLAN.md` -> `ls -1 ${phases_dir}/[current-phase-dir]/*-PLAN.md` (or use `phase_dir` if available)

    **commands/resume-project.md (3 occurrences):**

    Already calls `init resume`. After Phase 33, `phases_dir` available.
    - Line 48: `ls .planning/phases/*/.continue-here*.md` -> `ls ${phases_dir}/*/.continue-here*.md`
    - Line 50: `for plan in .planning/phases/*/*-PLAN.md` -> `for plan in ${phases_dir}/*/*-PLAN.md`
    - Line 94: `Read .planning/todos/pending/` -> `Read ${todos_dir}/pending/`

    **commands/remove-phase.md (2 occurrences):**

    Already calls `init phase-op`. `phase_dir` available.
    - Line 47: `Delete: .planning/phases/{target}-{slug}/` -> `Delete: ${phase_dir}/`
    - Line 74: `Deleted: .planning/phases/{target}-{slug}/` -> `Deleted: ${phase_dir}/`

    **commands/complete-milestone.md (2 occurrences):**

    Does NOT call init. Add init call:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init milestone-op)
    ```
    Parse JSON for: `phases_dir`.

    Replace:
    - Line 44: `for summary in .planning/phases/*-*/*-SUMMARY.md` -> `for summary in ${phases_dir}/*-*/*-SUMMARY.md`
    - Line 142: `.planning/phases/ directory is empty` -> `${phases_dir}/ directory is empty`

    **commands/add-phase.md (1 occurrence):**

    Already calls `init phase-op`. After Phase 33, `phases_dir` available.
    - Line 85: `Directory: .planning/phases/{phase-num}-{slug}/` -> `Directory: ${phases_dir}/{phase-num}-{slug}/`

    **commands/insert-phase.md (1 occurrence):**

    Already calls `init phase-op`. After Phase 33, `phases_dir` available.
    - Line 83: `Directory: .planning/phases/{decimal-phase}-{slug}/` -> `Directory: ${phases_dir}/{decimal-phase}-{slug}/`

    **commands/plan-milestone-gaps.md (1 occurrence):**

    Already calls `init plan-milestone-gaps`. After Phase 33, `phases_dir` available.
    - Line 183: `mkdir -p ".planning/phases/{NN}-{name}"` -> `mkdir -p "${phases_dir}/{NN}-{name}"`

    **CRITICAL: Update spawn prompts to pass path variables to agents**

    Commands in this batch that spawn agents must include resolved path variables in the Task() spawn prompt:

    - **execute-phase.md** (spawns `grd-executor`, `grd-verifier`, `grd-code-reviewer`): Add to each spawn prompt:
      ```
      PATHS:
      research_dir: ${research_dir}
      phases_dir: ${phases_dir}
      phase_dir: ${phase_dir}
      codebase_dir: ${codebase_dir}
      ```
    - **verify-work.md** (spawns `grd-verifier`): Add to spawn prompt:
      ```
      PATHS:
      research_dir: ${research_dir}
      phase_dir: ${phase_dir}
      ```
    - **discuss-phase.md** (spawns no agents, but passes context): No spawn prompt changes needed
    - **plan-milestone-gaps.md** (spawns `grd-planner`): Add to spawn prompt:
      ```
      PATHS:
      research_dir: ${research_dir}
      phases_dir: ${phases_dir}
      phase_dir: ${phase_dir}
      ```

    **After all 11 files are modified, verify each file:**
    - YAML frontmatter is valid
    - Zero occurrences of `.planning/phases/` in operational paths (grep check)
    - Commands that spawn agents include PATHS block in spawn prompts
  </action>
  <verify>
    grep -c "\.planning/phases/" commands/execute-phase.md commands/verify-work.md commands/discuss-phase.md commands/pause-work.md commands/progress.md commands/resume-project.md commands/remove-phase.md commands/complete-milestone.md commands/add-phase.md commands/insert-phase.md commands/plan-milestone-gaps.md — should return 0 for operational paths in each file (Level 1: Sanity)
    Frontmatter validation: each file starts with `---` and has valid YAML block (Level 1: Sanity)
  </verify>
  <done>11 phase-related command files migrated: all operational .planning/phases/ paths replaced with ${phases_dir} or ${phase_dir} from init context; init calls added to pause-work.md, complete-milestone.md; YAML frontmatter intact</done>
</task>

<task type="auto">
  <name>Task 2: Migrate utility commands (map-codebase, add-todo, check-todos, quick, new-milestone, help)</name>
  <files>commands/map-codebase.md, commands/add-todo.md, commands/check-todos.md, commands/quick.md, commands/new-milestone.md, commands/help.md</files>
  <action>
    **commands/map-codebase.md (8 occurrences):**

    Already calls `init map-codebase`. After Phase 33, `codebase_dir` is in init output.
    Add `codebase_dir` to the Parse JSON line.

    Replace all `.planning/codebase/` paths:
    - Line 6: Description -> `${codebase_dir}/`
    - Line 10: Output description -> `${codebase_dir}/`
    - Line 49: `ls -la .planning/codebase/` -> `ls -la ${codebase_dir}/`
    - Line 50: `wc -l .planning/codebase/*.md` -> `wc -l ${codebase_dir}/*.md`
    - Line 58: `grep ... .planning/codebase/*.md` -> `grep ... ${codebase_dir}/*.md`
    - Line 66: `--files .planning/codebase/*.md` -> `--files ${codebase_dir}/*.md`
    - Line 74: `Created .planning/codebase/:` -> `Created ${codebase_dir}/:`
    - Line 94: Success criteria -> `${codebase_dir}/`

    **commands/add-todo.md (5 occurrences):**

    Already calls `init todos`. After Phase 33, `todos_dir` available from init.
    Add `todos_dir` to the Parse JSON.

    Replace all `.planning/todos/` paths:
    - Line 24: `mkdir -p .planning/todos/pending .planning/todos/done` -> `mkdir -p ${todos_dir}/pending ${todos_dir}/done`
    - Line 41: `grep -l -i "..." .planning/todos/pending/*.md` -> `grep -l -i "..." ${todos_dir}/pending/*.md`
    - Line 52: `Write to .planning/todos/pending/${date}-${slug}.md` -> `Write to ${todos_dir}/pending/${date}-${slug}.md`
    - Line 57: `--files .planning/todos/pending/[filename] .planning/STATE.md` -> `--files ${todos_dir}/pending/[filename] .planning/STATE.md`
    - Line 63: `Todo saved: .planning/todos/pending/[filename]` -> `Todo saved: ${todos_dir}/pending/[filename]`

    **commands/check-todos.md (1 occurrence):**

    Already calls `init todos`. After Phase 33, `todos_dir` available.
    - Line 74: `--files .planning/todos/done/[filename] .planning/STATE.md` -> `--files ${todos_dir}/done/[filename] .planning/STATE.md`

    **commands/quick.md (3 occurrences):**

    Already calls `init quick`. After Phase 33, `quick_dir` available from init.
    Add `quick_dir` to the Parse JSON.

    Replace all `.planning/quick/` paths:
    - Line 7: Description -> `${quick_dir}/`
    - Line 56: `QUICK_DIR=".planning/quick/${next_num}-${slug}"` -> `QUICK_DIR="${quick_dir}/${next_num}-${slug}"`
    - Line 162: Success criteria -> `${quick_dir}/NNN-slug/`

    **commands/new-milestone.md (1 occurrence):**

    Already calls `init new-milestone`. After Phase 33, `research_dir` available.
    - Line 86: `@.planning/research/SUMMARY.md (if exists)` -> `@${research_dir}/SUMMARY.md (if exists)`

    **commands/help.md (1 occurrence):**

    This is purely documentation. The single occurrence on line 178 describes map-codebase output:
    `agents. Produce .planning/codebase/ docs.`

    This is a help text description — leave it as-is or update to generic wording:
    `agents. Produce codebase analysis docs in the codebase directory.`

    Since help.md has no init call and doesn't execute paths, a minimal wording update suffices.

    **CRITICAL: Update spawn prompts to pass path variables to agents**

    - **map-codebase.md** (spawns `grd-codebase-mapper`): Add to spawn prompt:
      ```
      PATHS:
      codebase_dir: ${codebase_dir}
      ```

    **After all 6 files are modified, verify each file:**
    - YAML frontmatter is valid
    - Zero occurrences of hardcoded `.planning/codebase/`, `.planning/todos/`, `.planning/quick/` in operational paths
    - Commands that spawn agents include PATHS block in spawn prompts
  </action>
  <verify>
    grep -c "\.planning/codebase/\|\.planning/todos/\|\.planning/quick/" commands/map-codebase.md commands/add-todo.md commands/check-todos.md commands/quick.md — should return 0 for operational paths (Level 1: Sanity)
    grep -c "\.planning/research/" commands/new-milestone.md — should return 0 (Level 1: Sanity)
    Frontmatter validation: each file starts with `---` and has valid YAML block (Level 1: Sanity)
  </verify>
  <done>6 utility command files migrated: map-codebase uses ${codebase_dir}, add-todo/check-todos use ${todos_dir}, quick uses ${quick_dir}, new-milestone uses ${research_dir}, help.md generalized; YAML frontmatter intact</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Each modified file has valid YAML frontmatter; `grep -rn "\.planning/(phases|codebase|todos|quick)/" commands/execute-phase.md commands/verify-work.md commands/discuss-phase.md commands/map-codebase.md commands/pause-work.md commands/add-todo.md commands/check-todos.md commands/quick.md commands/progress.md commands/resume-project.md commands/remove-phase.md commands/complete-milestone.md commands/add-phase.md commands/insert-phase.md commands/plan-milestone-gaps.md commands/new-milestone.md commands/help.md` returns zero results in operational paths
Level 2 (Proxy): N/A — markdown files have no unit tests
Level 3 (Deferred): Phase 36 integration tests validate commands work with new path variables end-to-end
</verification>

<success_criteria>
- Zero hardcoded `.planning/phases/`, `.planning/codebase/`, `.planning/todos/`, `.planning/quick/` paths in operational bash commands across all 17 command files
- Commands that lacked init calls now include them (pause-work.md, complete-milestone.md)
- All 17 files have valid YAML frontmatter
- All path references use init-context-derived variables
- Commands that spawn agents include PATHS blocks in their Task() spawn prompts (execute-phase, verify-work, map-codebase, plan-milestone-gaps)
</success_criteria>

<output>
After completion, create `.planning/phases/34-command-agent-markdown-migration/34-02-SUMMARY.md`
</output>
