---
phase: 34-command-agent-markdown-migration
plan: 04
type: execute
wave: 2
depends_on: [34-01, 34-02, 34-03]
files_modified: []
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "Zero occurrences of hardcoded `.planning/phases/` in operational paths across all 28 commands/*.md files and 16 agents/*.md files (excluding CLAUDE.md, templates, and documentation-only references that describe the conceptual structure)"
    - "Zero occurrences of hardcoded `.planning/research/` in operational paths across all commands/*.md and agents/*.md files"
    - "Zero occurrences of hardcoded `.planning/codebase/`, `.planning/todos/`, `.planning/quick/` in operational paths across all commands/*.md and agents/*.md files"
    - "All 44 modified markdown files have valid YAML frontmatter (proper `---` delimiters)"
    - "`npm test` passes with zero regressions (all 1,615+ tests pass)"
  artifacts: []
  key_links: []
---

<objective>
Comprehensive verification sweep across all 28 command and 16 agent markdown files to confirm zero hardcoded `.planning/` subdirectory paths remain in operational contexts. Fix any remaining occurrences found by Plans 34-01, 34-02, 34-03.

Purpose: The verification sweep serves as the quality gate for Phase 34. It confirms that REQ-57 and REQ-58 are fully satisfied. Any remaining hardcoded paths discovered during this sweep are fixed in-place.

Output: Verification evidence confirming zero hardcoded paths remain; any fixups applied
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-command-agent-markdown-migration/34-01-PLAN.md
@.planning/phases/34-command-agent-markdown-migration/34-02-PLAN.md
@.planning/phases/34-command-agent-markdown-migration/34-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive hardcoded path sweep and fixup</name>
  <files>commands/*.md, agents/*.md</files>
  <action>
    Run a comprehensive grep sweep across all command and agent markdown files to find any remaining hardcoded `.planning/` subdirectory paths in operational contexts.

    **Sweep 1: Commands — .planning/phases/**
    ```bash
    grep -rn "\.planning/phases/" commands/*.md | grep -v "^.*#.*" | grep -v "CLAUDE.md"
    ```
    Expected: zero results. If any found, fix them by replacing with `${phases_dir}/` or `${phase_dir}`.

    **Sweep 2: Commands — .planning/research/**
    ```bash
    grep -rn "\.planning/research/" commands/*.md
    ```
    Expected: zero results. If any found, replace with `${research_dir}/`.

    **Sweep 3: Commands — .planning/codebase/**
    ```bash
    grep -rn "\.planning/codebase/" commands/*.md
    ```
    Expected: zero results. If any found, replace with `${codebase_dir}/`.

    **Sweep 4: Commands — .planning/todos/**
    ```bash
    grep -rn "\.planning/todos/" commands/*.md
    ```
    Expected: zero results. If any found, replace with `${todos_dir}/`.

    **Sweep 5: Commands — .planning/quick/**
    ```bash
    grep -rn "\.planning/quick/" commands/*.md
    ```
    Expected: zero results. If any found, replace with `${quick_dir}/`.

    **Sweep 6: Agents — .planning/research/**
    ```bash
    grep -rn "\.planning/research/" agents/*.md
    ```
    Expected: zero results.

    **Sweep 7: Agents — .planning/phases/**
    ```bash
    grep -rn "\.planning/phases/" agents/*.md
    ```
    Expected: zero results.

    **Sweep 8: Agents — .planning/codebase/**
    ```bash
    grep -rn "\.planning/codebase/" agents/*.md
    ```
    Expected: zero results.

    **Sweep 9: Agents — .planning/todos/ and .planning/quick/**
    ```bash
    grep -rn "\.planning/todos/\|\.planning/quick/" agents/*.md
    ```
    Expected: zero results.

    **Sweep 10: YAML frontmatter validation**
    For every file in commands/*.md and agents/*.md, verify:
    - File starts with `---`
    - Has a closing `---` before content
    - Frontmatter contains `description:` field

    ```bash
    for f in commands/*.md agents/*.md; do
      HEAD=$(head -1 "$f")
      if [ "$HEAD" != "---" ]; then
        echo "INVALID FRONTMATTER: $f"
      fi
    done
    ```

    **Sweep 11: Full test suite**
    ```bash
    npm test
    ```
    All 1,615+ tests must pass. Although command/agent markdown files don't have direct unit tests, this confirms no regressions in the broader system.

    **If any sweep finds remaining hardcoded paths:**
    1. Read the file to understand context
    2. Determine if it's an operational path (needs fixing) or documentation-only description
    3. Fix operational paths using the same patterns from Plans 01-03
    4. Re-run the sweep to confirm the fix

    **Document all sweep results** in the SUMMARY for traceability.
  </action>
  <verify>
    All 9 grep sweeps return zero results (Level 1: Sanity)
    YAML frontmatter validation passes for all 44 files (Level 1: Sanity)
    npm test — all 1,615+ tests pass (Level 1: Sanity)
  </verify>
  <done>Comprehensive verification sweep complete: zero hardcoded .planning/ subdirectory paths remain in operational contexts across all 28 command and 16 agent markdown files; all frontmatter valid; all tests pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All grep sweeps return zero results for hardcoded paths in operational contexts; all frontmatter valid; `npm test` passes
Level 2 (Proxy): N/A — structural verification only
Level 3 (Deferred): Phase 36 end-to-end integration tests validate the full pipeline works with milestone-scoped paths
</verification>

<success_criteria>
- All 9 grep sweeps across commands/*.md and agents/*.md return zero results for hardcoded `.planning/(phases|research|codebase|todos|quick)/` paths
- All 44 markdown files have valid YAML frontmatter
- `npm test` passes with all 1,615+ tests and zero regressions
- Any remaining hardcoded paths found during sweep are fixed in-place
</success_criteria>

<output>
After completion, create `.planning/phases/34-command-agent-markdown-migration/34-04-SUMMARY.md`
</output>
