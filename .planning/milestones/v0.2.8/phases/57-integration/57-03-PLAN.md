---
phase: 57-integration
plan: 03
type: execute
wave: 2
depends_on: ["57-01", "57-02"]
files_modified:
  - tests/integration/evolve-e2e.test.js
autonomous: true
verification_level: deferred
must_haves:
  truths:
    - "A full evolve iteration (discover -> select -> plan -> execute -> review -> persist) completes without errors when run programmatically against a test fixture project"
    - "Markdown splitting correctly splits a large file (>25,000 tokens), writes index + partials, and reading back through GRD readers returns identical logical content"
    - "Iteration handoff works: two consecutive evolve runs produce state files where the second inherits remaining items and increments iteration counter"
    - "DEFER-54-01 validated: real-world large markdown files (STATE.md, ROADMAP.md scale) split correctly into partials with heading-boundary alignment"
    - "DEFER-55-01 validated: work item discovery on the GRD codebase itself produces categorized, actionable items across the six dimensions"
    - "DEFER-56-01 validated: full evolve loop produces meaningful code changes (not just whitespace/cosmetic), tracked in evolution notes"
  artifacts:
    - path: "tests/integration/evolve-e2e.test.js"
      provides: "End-to-end integration tests for evolve loop and markdown splitting"
      min_lines: 150
  key_links:
    - from: "tests/integration/evolve-e2e.test.js"
      to: "lib/"
      via: "direct module imports for evolve and markdown-split modules"
      pattern: "require.*lib/"
---

<objective>
Create end-to-end integration tests that validate the full evolve loop, markdown splitting on real-world files, iteration handoff, and all three deferred validations from phases 54-56.

Purpose: This is the core integration plan. Phase 57 success criteria #1 (dogfooding), #2 (markdown splitting during evolve), and #3 (iteration handoff) are validated here. Additionally, deferred validations DEFER-54-01, DEFER-55-01, and DEFER-56-01 are collected and resolved.

Output: Comprehensive integration test file, deferred validation results documented.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/integration/e2e-workflow.test.js
@tests/integration/cli.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end integration tests for evolve loop and markdown splitting</name>
  <files>tests/integration/evolve-e2e.test.js</files>
  <action>
    Create tests/integration/evolve-e2e.test.js following the patterns established in tests/integration/e2e-workflow.test.js (temp dir with .planning/ fixture, direct module imports, cleanup).

    The test file must contain these describe blocks:

    **1. "E2E: Markdown splitting on real-world files" (DEFER-54-01)**
    - Create a temp fixture with a large markdown file (synthesize content >25,000 tokens by repeating realistic STATE.md-like content with headings, tables, and code blocks)
    - Call the markdown splitting function to split the file
    - Assert: original file becomes an index with partial links
    - Assert: partial files exist on disk with heading-aligned boundaries
    - Assert: reading back through the GRD reader function (state.js or utils.js reader) returns content identical to the original pre-split content (round-trip integrity)
    - Assert: re-running split on already-split files is idempotent (no changes)
    - Assert: files below threshold are left untouched

    **2. "E2E: Work item discovery quality" (DEFER-55-01)**
    - Set up a temp fixture mimicking a real project (lib/ with JS files, tests/, .planning/ with config.json)
    - Call the work item discovery function
    - Assert: returns a categorized list with items across the six dimensions (productivity, quality, usability, consistency, stability, new features)
    - Assert: each item has at minimum: category, description, and some form of actionability signal
    - Assert: discovery loads and merges with previous iteration state if present
    - Assert: deduplication works (running discovery twice with same codebase doesn't produce duplicates)

    **3. "E2E: Full evolve iteration" (criterion #1, DEFER-56-01)**
    - Set up a test fixture project directory with lib/, tests/, .planning/ (config.json, STATE.md, ROADMAP.md)
    - Call the evolve orchestration function (or the core engine sequence: discover -> select -> plan -> persist)
    - Assert: function completes without throwing
    - Assert: iteration state file is created on disk with expected structure (selected items, remaining items, iteration metadata)
    - Assert: evolution notes file (EVOLUTION.md or equivalent) is created/appended
    - Note: The full loop with actual subagent execution cannot be tested in a unit-test context; instead, test the orchestration logic and state persistence. Mark the live-run assertion as a comment documenting it was validated during dogfooding.

    **4. "E2E: Iteration handoff" (criterion #3)**
    - Run evolve (or its core state management) once -> capture state file
    - Run evolve a second time in the same fixture -> capture second state file
    - Assert: second state file iteration counter is incremented
    - Assert: second state file inherits remaining items from first
    - Assert: completed items from first iteration are not re-selected

    **5. "E2E: Markdown splitting during evolve iterations" (criterion #2)**
    - Set up a fixture where evolve would generate/grow files
    - After an evolve pass, artificially grow a markdown file past the split threshold
    - Call the splitting function
    - Assert: the grown file is correctly split
    - Assert: subsequent reads through GRD readers still work

    Follow existing test patterns:
    - Use fs.mkdtempSync for isolation
    - Clean up in afterAll
    - 15s timeout per test (jest.config.js default)
    - Use direct module imports (not CLI subprocess) for speed
  </action>
  <verify>
    npx jest tests/integration/evolve-e2e.test.js --verbose 2>&1 | tail -30
    - All integration tests pass (Level 3: Deferred -- this is the deferred validation collection point)
  </verify>
  <done>
    Integration test file exists with 5+ describe blocks covering all Phase 57 success criteria and all 3 deferred validations. All tests pass. Deferred validations DEFER-54-01, DEFER-55-01, DEFER-56-01 are resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and document deferred validation outcomes</name>
  <files>tests/integration/evolve-e2e.test.js</files>
  <action>
    1. Run the complete test suite: `npx jest --coverage`
       - Confirm ALL tests pass (unit + integration + golden)
       - Confirm total test count is >= 1,983 (original) plus new tests
       - Confirm all per-file coverage thresholds pass

    2. If any existing tests fail, investigate and fix:
       - Check if new modules introduced side effects on existing code
       - Check if require() order or module loading changed
       - Fix without lowering coverage thresholds

    3. Document deferred validation outcomes in the SUMMARY for this plan:
       - DEFER-54-01: Status (RESOLVED/PARTIALLY RESOLVED), evidence, any caveats
       - DEFER-55-01: Status, evidence, item quality assessment
       - DEFER-56-01: Status, evidence, whether changes were meaningful vs cosmetic

    4. If any deferred validation cannot be fully resolved in automated tests (e.g., DEFER-56-01 requires live model execution), document what was verified programmatically and what requires manual dogfooding, marking it accordingly.
  </action>
  <verify>
    npx jest --coverage 2>&1 | grep -E "(Tests:|Test Suites:|Snapshots:)"
    - Test Suites: all passed
    - Tests: all passed, count >= 1,983
    (Level 3: Deferred)
  </verify>
  <done>
    Full regression suite passes with zero failures, all per-file thresholds met, deferred validation outcomes documented with evidence and status.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Integration test file exists and is valid JS (require() doesn't throw)
Level 2 (Proxy): Individual integration test blocks pass in isolation
Level 3 (Deferred): Full test suite passes, all deferred validations resolved with documented evidence
</verification>

<success_criteria>
- tests/integration/evolve-e2e.test.js exists with 5+ describe blocks
- All integration tests pass: markdown splitting, discovery quality, full evolve iteration, iteration handoff, splitting during evolve
- Full regression suite (npx jest --coverage) passes with 0 failures
- Total test count >= 1,983 + new integration tests
- DEFER-54-01 resolved: markdown splitting validated on large files
- DEFER-55-01 resolved: work item discovery produces categorized actionable items
- DEFER-56-01 resolved (or documented as requiring live dogfood): evolve loop produces meaningful changes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-03-SUMMARY.md`
</output>
