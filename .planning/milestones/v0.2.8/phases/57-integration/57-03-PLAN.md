---
phase: 57-integration
plan: 03
type: execute
wave: 2
depends_on: [57-01, 57-02]
files_modified:
  - tests/integration/evolve-e2e.test.js
autonomous: true
verification_level: deferred
must_haves:
  truths:
    - "A full evolve iteration (discover -> select -> plan -> execute -> review -> persist) completes without errors when run programmatically against a test fixture project"
    - "Iteration handoff works: two consecutive evolve runs produce state files where the second inherits remaining items and increments iteration counter"
    - "DEFER-55-01 validated: work item discovery on the GRD codebase itself produces categorized, actionable items across the six dimensions"
    - "DEFER-56-01 documented: full evolve loop outcome recorded with evidence of whether changes are meaningful (requires live model execution -- test validates the orchestration mechanics, SUMMARY documents the dogfood result)"
    - "DEFER-54-01 recorded as CANNOT VALIDATE: Phase 54 (Markdown Splitting) was not executed, prerequisite code does not exist"
  artifacts:
    - path: "tests/integration/evolve-e2e.test.js"
      provides: "End-to-end integration tests for evolve loop"
      min_lines: 100
  key_links:
    - from: "tests/integration/evolve-e2e.test.js"
      to: "lib/evolve.js"
      via: "direct module imports for evolve functions"
      pattern: "require.*lib/evolve"
---

<objective>
Create end-to-end integration tests that validate the full evolve loop, iteration handoff, work item discovery quality, and resolve or document all three deferred validations from Phases 54-56.

Purpose: This is the core integration plan. Phase 57 success criteria #1 (dogfooding), #3 (iteration handoff) are validated here. Deferred validations DEFER-55-01 and DEFER-56-01 are collected and resolved/documented. DEFER-54-01 is recorded as CANNOT VALIDATE because Phase 54 was not executed. Success criteria #2 (markdown splitting during evolve) is not applicable since Phase 54 was skipped.

Output: Comprehensive integration test file, deferred validation results documented in SUMMARY.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/evolve.js
@tests/integration/e2e-workflow.test.js
@tests/integration/cli.test.js
@.planning/milestones/v0.2.8/phases/55-evolve-core/55-01-SUMMARY.md
@.planning/milestones/v0.2.8/phases/55-evolve-core/55-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end integration tests for evolve loop and deferred validations</name>
  <files>tests/integration/evolve-e2e.test.js</files>
  <action>
    Create tests/integration/evolve-e2e.test.js following the patterns established in tests/integration/e2e-workflow.test.js (temp dir with .planning/ fixture, direct module imports, cleanup).

    The test file must contain these describe blocks:

    **1. "E2E: Work item discovery quality" (DEFER-55-01)**
    - Set up a temp fixture mimicking a real project: create lib/ with 3-5 JS files containing realistic code patterns (some with TODO comments, some with long functions, some with inconsistent naming), create tests/ dir, create .planning/ with config.json (minimal: `{ "autonomous_mode": true }`) and a stub STATE.md.
    - Import `analyzeCodebaseForItems` and `runDiscovery` from lib/evolve.js.
    - Call `analyzeCodebaseForItems(tmpDir)` on the fixture.
    - Assert: returns an array of work items.
    - Assert: items span at least 2 of the 6 dimensions (productivity, quality, usability, consistency, stability, new-features). The fixture should be designed so that quality items (e.g., missing tests, TODOs) and consistency items (e.g., naming mismatches) are discoverable.
    - Assert: each item has required fields: id (string), dimension (string), description (string), effort (string), source (string), status (string).
    - Call `runDiscovery(tmpDir)` which should merge with empty prior state.
    - Assert: returns { items, merged } where items is non-empty and merged handles the empty-state case gracefully.
    - Assert: running discovery twice on the same fixture with merge produces no duplicates (dedup by id).

    **2. "E2E: Full evolve iteration mechanics" (criterion #1 orchestration validation)**
    - Set up a test fixture project directory with lib/ (1-2 JS files), tests/ dir, .planning/ (config.json with autonomous_mode: true, STATE.md, ROADMAP.md stub).
    - Import `runEvolve` from lib/evolve.js.
    - Mock `require('../lib/autopilot').spawnClaude` (or the equivalent subprocess function) to return `{ exitCode: 0, timedOut: false, output: 'OK' }` without actually spawning processes. This validates the orchestration mechanics without requiring live model execution.
    - Call `runEvolve(tmpDir, { iterations: 1, itemsPerIteration: 2, dryRun: false })` with the mocked subprocess.
    - Assert: function completes without throwing.
    - Assert: .planning/EVOLVE-STATE.json is created on disk with expected structure (iteration number, selected items, remaining items).
    - Assert: if writeEvolutionNotes was called, .planning/EVOLUTION.md exists (or equivalent notes file).
    - If runEvolve with mocked deps does not reach the full loop (because mocks prevent real discovery), test the individual steps:
      a. Call `runDiscovery(tmpDir)` -> assert items discovered
      b. Call `selectPriorityItems(items, 2)` -> assert 2 items selected
      c. Call `advanceIteration(state, selectedItems, remainingItems)` -> assert state advanced
      d. Call `writeEvolveState(tmpDir, state)` -> assert file written
      e. Call `writeEvolutionNotes(tmpDir, iterationData)` -> assert EVOLUTION.md created

    **3. "E2E: Iteration handoff" (criterion #3)**
    - Create a fixture with .planning/ dir.
    - Import state management functions from lib/evolve.js: createInitialState, writeEvolveState, readEvolveState, advanceIteration, createWorkItem, mergeWorkItems.
    - Create initial state with 5 work items (3 selected, 2 remaining).
    - Simulate first iteration completion: mark 2 items as completed, 1 as failed.
    - Call advanceIteration() to produce the second iteration state.
    - Write the state to disk.
    - Read it back.
    - Assert: iteration counter is 2 (incremented from 1).
    - Assert: remaining items from first iteration are carried over (the 2 that were not selected + the 1 that failed, depending on carryover logic).
    - Assert: completed items from first iteration are NOT in the remaining queue.
    - Simulate second iteration: create new discovered items, merge with remaining.
    - Assert: merge deduplicates correctly (existing items win per Phase 55 decision).
    - Assert: state file on disk reflects the second iteration's data.

    **4. "E2E: Discovery on GRD codebase itself" (DEFER-55-01 real-world validation)**
    - Use the actual GRD project root (process.cwd() or __dirname-based resolution) as the cwd for discovery.
    - Call `analyzeCodebaseForItems(grdRoot)`.
    - Assert: returns a non-empty array (the GRD codebase should have improvement opportunities).
    - Assert: items cover at least 3 of the 6 dimensions (GRD has 21 modules, tests, configs -- should trigger multiple analyzers).
    - Assert: each item's description is a non-empty string (not placeholder/undefined).
    - This test validates DEFER-55-01: "are discovered items actionable?" by checking they have real descriptions from a real codebase.
    - Note: This test should have a longer timeout (e.g., 30s) since it scans the full GRD lib/ directory.

    Follow existing test patterns:
    - Use fs.mkdtempSync for isolation in tests 1-3
    - Clean up in afterAll/afterEach
    - 15s timeout per test (30s for the GRD codebase scan)
    - Use direct module imports (not CLI subprocess) for speed
    - CommonJS require, 'use strict'
  </action>
  <verify>
    npx jest tests/integration/evolve-e2e.test.js --verbose 2>&1 | tail -30
    - All integration tests pass (Level 3: Deferred -- this is the deferred validation collection point)
  </verify>
  <done>
    Integration test file exists with 4 describe blocks covering: discovery quality (DEFER-55-01), full evolve iteration mechanics (criterion #1), iteration handoff (criterion #3), and real-world discovery on GRD codebase (DEFER-55-01 validation). All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and document deferred validation outcomes</name>
  <files>tests/integration/evolve-e2e.test.js</files>
  <action>
    1. Run the complete test suite: `npx jest --coverage`
       - Confirm ALL tests pass (unit + integration + golden)
       - Confirm total test count shows no regressions (>= 2,069 from Phase 55)
       - Confirm all per-file coverage thresholds pass

    2. If any existing tests fail, investigate and fix:
       - Check if new integration tests introduced side effects (e.g., leftover temp files)
       - Check if require() order or module loading changed
       - Fix without lowering coverage thresholds

    3. Document deferred validation outcomes in the SUMMARY for this plan:

       **DEFER-54-01 (Markdown splitting on real-world files):**
       - Status: CANNOT VALIDATE
       - Reason: Phase 54 (Markdown Splitting Infrastructure) was not executed. No lib/markdown-split.js module exists. REQ-60 and REQ-61 remain unimplemented.
       - Impact: Success criteria #2 (markdown splitting during evolve) is not applicable for this milestone.

       **DEFER-55-01 (Work item discovery quality on non-trivial codebase):**
       - Status: RESOLVED (from integration test "E2E: Discovery on GRD codebase itself")
       - Evidence: Discovery produces N categorized items across M dimensions when run against the GRD codebase (21 lib/ modules, 2069+ tests). Items have real descriptions and actionable improvement suggestions.
       - Document: item count, dimension coverage, and 3-5 example items in the SUMMARY.

       **DEFER-56-01 (Full evolve loop with sonnet-tier models produces meaningful improvements):**
       - Status: PARTIALLY RESOLVED (orchestration mechanics validated programmatically; live model execution cannot be tested in CI)
       - Evidence: Integration test validates the orchestration flow completes without errors with mocked subprocess calls. All state management, iteration handoff, and evolution notes work correctly.
       - If time permits and the user approves, run `/grd:evolve --dry-run` once against the GRD codebase and document the output (item count, selected items, plan outline) in the SUMMARY.
       - Note: Full validation of "meaningful improvements" requires running with live sonnet models, which is outside the scope of automated tests.

    4. Verify that the integration test file is well-formed:
       - `node -e "require('./tests/integration/evolve-e2e.test.js')"` should not throw
       - File should be >= 100 lines
  </action>
  <verify>
    npx jest --coverage 2>&1 | grep -E "(Tests:|Test Suites:|Snapshots:)"
    - Test Suites: all passed
    - Tests: all passed, no regressions
    (Level 3: Deferred)
  </verify>
  <done>
    Full regression suite passes with zero failures, all per-file thresholds met. Deferred validation outcomes documented: DEFER-54-01 CANNOT VALIDATE (Phase 54 skipped), DEFER-55-01 RESOLVED (discovery quality confirmed on GRD codebase), DEFER-56-01 PARTIALLY RESOLVED (orchestration validated programmatically, live model execution documented if performed).
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Integration test file exists and is valid JS (require() doesn't throw)
Level 2 (Proxy): Individual integration test blocks pass in isolation
Level 3 (Deferred): Full test suite passes, deferred validations resolved/documented with evidence
</verification>

<success_criteria>
- tests/integration/evolve-e2e.test.js exists with 4 describe blocks
- All integration tests pass: discovery quality, full evolve iteration, iteration handoff, GRD codebase discovery
- Full regression suite (npx jest --coverage) passes with 0 failures
- Test count shows no regressions (>= 2,069)
- DEFER-54-01 documented as CANNOT VALIDATE (Phase 54 skipped)
- DEFER-55-01 RESOLVED: discovery produces categorized actionable items on real codebase
- DEFER-56-01 PARTIALLY RESOLVED: orchestration mechanics validated; live execution documented
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-03-SUMMARY.md`
</output>
