---
phase: 57-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/mcp-server.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "MCP COMMAND_DESCRIPTORS table in lib/mcp-server.js includes all 7 evolve-related tool entries: 5 from Phase 55 (grd_evolve_discover, grd_evolve_state, grd_evolve_advance, grd_evolve_reset, grd_evolve_init) plus 2 from Phase 56 (grd_evolve_run, grd_evolve_init)"
    - "Each evolve MCP tool has name, description, params, and execute function defined"
    - "MCP tool invocation for each evolve tool returns structured JSON (not errors or undefined)"
    - "Existing MCP tools (non-evolve) continue to function without regression"
    - "commands/evolve.md exists as a registered slash command with valid frontmatter"
  artifacts:
    - path: "tests/unit/mcp-server.test.js"
      provides: "Tests validating all evolve MCP tool registrations"
      contains: "grd_evolve"
  key_links:
    - from: "lib/mcp-server.js"
      to: "lib/evolve.js"
      via: "require and COMMAND_DESCRIPTORS execute callbacks"
      pattern: "require.*evolve"
    - from: "commands/evolve.md"
      to: "bin/grd-tools.js"
      via: "CLI delegation"
      pattern: "grd-tools.js"
---

<objective>
Validate MCP tool registration for all evolve commands introduced in Phases 55-56 and verify the /grd:evolve slash command is properly registered.

Purpose: Phase 57 success criterion #6 requires MCP tool registration for new commands to be functional and return structured JSON. Phase 55 registered 5 evolve MCP tools; Phase 56 adds 2 more (grd_evolve_run, grd_evolve_init). This plan validates completeness and functional correctness of all 7 evolve MCP tools, plus verifies the commands/evolve.md slash command.

Note: Phase 54 (Markdown Splitting) was not executed, so there are no markdown-split MCP tools to validate. Success criterion #6 is addressed for evolve commands only.

Output: Updated mcp-server tests validating all evolve tools, slash command validation.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mcp-server.js
@lib/evolve.js
@commands/evolve.md
@tests/unit/mcp-server.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate MCP tool completeness and add invocation tests for evolve tools</name>
  <files>tests/unit/mcp-server.test.js</files>
  <action>
    1. Read lib/mcp-server.js and enumerate all COMMAND_DESCRIPTORS entries whose name starts with 'grd_evolve'. Expected after Phase 56:
       - grd_evolve_discover (Phase 55)
       - grd_evolve_state (Phase 55)
       - grd_evolve_advance (Phase 55)
       - grd_evolve_reset (Phase 55)
       - grd_evolve_init (Phase 55, may be enhanced by Phase 56)
       - grd_evolve_run (Phase 56)
       If any are missing, document in the SUMMARY as a Phase 56 execution gap rather than adding them here (Phase 57 is integration validation, not implementation).

    2. Verify each evolve MCP tool has required fields by running:
       ```js
       node -e "const m = require('./lib/mcp-server'); const defs = m.buildToolDefinitions ? m.buildToolDefinitions() : []; const evolve = defs.filter(d => d.name.includes('evolve')); evolve.forEach(t => console.log(t.name, !!t.description, !!t.inputSchema));"
       ```

    3. Open tests/unit/mcp-server.test.js and add a new describe block "v0.2.8 evolve MCP tools" that tests:
       a. Enumeration: all expected evolve tool names appear in buildToolDefinitions() output
       b. Schema completeness: each evolve tool has name, description, and inputSchema fields
       c. Invocation: for each evolve tool, call its execute function with a valid temp directory containing .planning/ structure with a minimal config.json and STATE.md. Verify the return value is not undefined and does not throw. For grd_evolve_run, use --dry-run or mock spawnClaude to avoid spawning subprocesses.
       Follow existing test patterns in the file (temp dir setup, fixture creation, cleanup in afterAll).

    4. Verify commands/evolve.md exists and has valid YAML frontmatter with 'description' field:
       ```bash
       node -e "const fm = require('./lib/frontmatter'); const r = fm.readFrontmatter('commands/evolve.md'); console.log(r.data.description ? 'OK' : 'MISSING');"
       ```
       If the file does not exist (Phase 56 not yet executed), document as a gap.

    5. Run: `npx jest tests/unit/mcp-server.test.js --coverage`
       - All tests pass including new evolve tool tests
       - mcp-server.js coverage stays above threshold (87% lines per jest.config.js, or 85% if Phase 56 adjusted)
       - Existing MCP tool tests do not regress

    6. Run: `npx jest --coverage` for full regression
       - Confirm total test count has no regressions
  </action>
  <verify>
    npx jest tests/unit/mcp-server.test.js --verbose 2>&1 | tail -30
    - New evolve MCP tool tests pass (Level 2: Proxy)
    - Existing MCP tests do not regress
    npx jest --coverage 2>&1 | grep "Test Suites:\|Tests:"
    - All tests pass, no regressions (Level 2: Proxy)
  </verify>
  <done>
    All evolve MCP tools from Phases 55-56 are validated: present in COMMAND_DESCRIPTORS, have complete schemas, and return structured JSON on invocation. commands/evolve.md exists with valid frontmatter. mcp-server.test.js passes with coverage above threshold. No regressions in the full test suite.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): lib/mcp-server.js loads without require() errors, buildToolDefinitions() completes
Level 2 (Proxy): All evolve MCP tool invocation tests pass, existing tool tests don't regress, mcp-server.js coverage above threshold
</verification>

<success_criteria>
- COMMAND_DESCRIPTORS includes entries for all Phase 55-56 evolve commands (5-7 tools depending on Phase 56 execution)
- Each evolve tool has name, description, params, execute
- MCP tool invocation tests pass for evolve entries (return structured JSON, not errors)
- commands/evolve.md exists with valid frontmatter
- Existing MCP tool tests continue to pass (no regression)
- mcp-server.js coverage remains above jest.config.js threshold
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-02-SUMMARY.md`
</output>
