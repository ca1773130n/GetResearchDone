---
phase: 57-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp-server.js
  - tests/unit/mcp-server.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "MCP COMMAND_DESCRIPTORS table in lib/mcp-server.js includes tool entries for evolve-related commands and markdown-split commands introduced in phases 54-56"
    - "Each new MCP tool has name, description, params, and execute function defined"
    - "MCP tool invocation returns structured JSON (not errors or undefined)"
    - "Existing MCP tools continue to function without regression"
  artifacts:
    - path: "lib/mcp-server.js"
      provides: "MCP tool registrations for new commands"
      contains: "grd_evolve"
    - path: "tests/unit/mcp-server.test.js"
      provides: "Tests for new MCP tool registrations"
      contains: "evolve"
  key_links:
    - from: "lib/mcp-server.js"
      to: "lib/commands.js"
      via: "require and execute function wiring"
      pattern: "require.*commands"
---

<objective>
Validate and complete MCP tool registration for all new commands introduced in phases 54-56, ensuring the `/grd:evolve` and markdown-split functionality is exposed through the MCP server.

Purpose: Phase 57 success criterion #6 requires MCP tool registration for new commands (evolve, markdown-split) to be functional and return structured JSON. This plan verifies existing registrations and adds any missing ones.

Output: Updated lib/mcp-server.js with complete tool registrations, updated mcp-server tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mcp-server.js
@lib/commands.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete MCP tool registrations for new commands</name>
  <files>lib/mcp-server.js</files>
  <action>
    1. Read lib/mcp-server.js and enumerate all entries in COMMAND_DESCRIPTORS.
    2. Identify new lib/ exports created by phases 54-56 (evolve module exports, markdown-split module exports) by reading their module.exports.
    3. For each new exported command function that should be exposed as an MCP tool:
       a. Add an import at the top of mcp-server.js (following existing require() pattern)
       b. Add a COMMAND_DESCRIPTORS entry with:
          - name: following grd_{category}_{action} convention (e.g., grd_evolve_run, grd_markdown_split)
          - description: clear one-line description
          - params: array of parameter objects with name, type, required, description
          - execute: function wiring to the imported command
    4. Verify no duplicate tool names exist in COMMAND_DESCRIPTORS.
    5. Ensure the buildToolDefinitions() function correctly transforms the new entries.

    Note: If phases 54-56 already registered their MCP tools, this task validates completeness rather than creating from scratch. Check the existing descriptors first before adding.
  </action>
  <verify>
    node -e "const m = require('./lib/mcp-server'); const defs = m.buildToolDefinitions ? m.buildToolDefinitions() : []; console.log('Tools:', defs.length); const evolve = defs.filter(d => d.name.includes('evolve') || d.name.includes('split') || d.name.includes('markdown')); console.log('New tools:', evolve.map(d => d.name));"
    - New tool names appear in output (Level 2: Proxy)
  </verify>
  <done>All new commands from phases 54-56 have MCP tool entries in COMMAND_DESCRIPTORS, tool definitions build without error, new tool count is correct.</done>
</task>

<task type="auto">
  <name>Task 2: Add MCP tool invocation tests for new commands</name>
  <files>tests/unit/mcp-server.test.js</files>
  <action>
    1. Open tests/unit/mcp-server.test.js and review the existing test patterns for MCP tool validation.
    2. Add a new describe block "New v0.2.8 MCP tools" that tests:
       a. Each new tool name appears in buildToolDefinitions() output
       b. Each new tool has required fields (name, description, inputSchema)
       c. Each new tool's execute function can be called with valid args and returns JSON (not undefined/error) when given a valid temp directory with .planning/ structure
    3. Follow the existing test fixture patterns (temp dir with .planning/ copy).
    4. Run: `npx jest tests/unit/mcp-server.test.js --coverage`
       - All tests pass
       - mcp-server.js coverage stays above threshold (87% lines per jest.config.js)
  </action>
  <verify>
    npx jest tests/unit/mcp-server.test.js --verbose 2>&1 | tail -20
    - New test cases pass (Level 2: Proxy)
  </verify>
  <done>MCP tool tests cover all new v0.2.8 commands, mcp-server.test.js passes with coverage above 87% lines.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): lib/mcp-server.js loads without require() errors
Level 2 (Proxy): buildToolDefinitions() includes new tools, MCP tool tests pass, existing tool tests don't regress
</verification>

<success_criteria>
- COMMAND_DESCRIPTORS includes entries for all new Phase 54-56 commands
- Each new tool has name, description, params, execute
- MCP tool tests pass for new entries
- Existing MCP tool tests continue to pass (no regression)
- mcp-server.js coverage remains above 87% lines
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-02-SUMMARY.md`
</output>
