---
phase: 57-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.js
  - tests/unit/evolve.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "jest.config.js contains per-file coverage thresholds for lib/evolve.js at 85%+ lines after Phase 56 expands the module with orchestrator functions"
    - "All evolve.js exports (Phase 55 core engine + Phase 56 orchestrator) have corresponding test coverage in tests/unit/evolve.test.js"
    - "npx jest --coverage passes with zero failures and all per-file thresholds met"
    - "Total test count is at least 2,069 (pre-Phase-57 baseline from Phase 55 completion)"
  artifacts:
    - path: "jest.config.js"
      provides: "Coverage thresholds for evolve.js after Phase 56 expansion"
      contains: "evolve.js"
    - path: "tests/unit/evolve.test.js"
      provides: "Unit tests covering both core engine and orchestrator functions"
      contains: "runEvolve"
  key_links:
    - from: "jest.config.js"
      to: "lib/evolve.js"
      via: "coverage threshold entry"
      pattern: "lib/evolve\\.js.*lines"
    - from: "tests/unit/evolve.test.js"
      to: "lib/evolve.js"
      via: "require and function-level testing"
      pattern: "require.*evolve"
---

<objective>
Validate and enforce test coverage thresholds for lib/evolve.js after Phase 56 expands the module with orchestrator functions, and confirm the full regression suite passes with zero regressions.

Purpose: Phase 57 success criterion #4 requires all new lib/ modules maintain 85%+ line coverage with per-file thresholds enforced in jest.config.js. Criterion #5 requires the full regression suite passes with zero regressions. Phase 56 adds ~9 new exports to lib/evolve.js (orchestrator functions); coverage thresholds may need recalibration and any gaps in test coverage must be filled.

Note: Phase 54 (Markdown Splitting) was not executed, so no lib/markdown-split.js module exists. This plan covers only lib/evolve.js which is the sole new module from the v0.2.8 milestone.

Output: Validated/updated jest.config.js coverage thresholds, any additional unit tests needed for evolve.js orchestrator functions, full passing test suite.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@jest.config.js
@lib/evolve.js
@tests/unit/evolve.test.js
@.planning/milestones/v0.2.8/phases/56-evolve-orchestrator/56-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit evolve.js coverage and fill any gaps from Phase 56 orchestrator additions</name>
  <files>jest.config.js, tests/unit/evolve.test.js</files>
  <action>
    1. Run `npx jest tests/unit/evolve.test.js --coverage` and examine the per-function coverage report for lib/evolve.js. Phase 56 Plan 02 should have already added orchestrator tests, but coverage may have dipped below 85% lines if the orchestrator functions (runEvolve, writeEvolutionNotes, cmdEvolve, build*Prompt) have edge cases not yet covered.

    2. Check which functions in lib/evolve.js have below-threshold coverage by examining the Istanbul report:
       - Phase 55 exports (19 functions): should already be well-covered (87%+ from Phase 55)
       - Phase 56 exports (9 new: SONNET_MODEL, runEvolve, cmdInitEvolve enhanced, cmdEvolve, writeEvolutionNotes, buildDiscoverPrompt, buildPlanPrompt, buildExecutePrompt, buildReviewPrompt): verify these have tests

    3. For any uncovered or under-covered functions, add targeted tests to tests/unit/evolve.test.js:
       - If runEvolve has untested error paths (e.g., spawnClaude failure, empty discovery results), add test cases with mocked dependencies
       - If writeEvolutionNotes has untested edge cases (e.g., empty decisions/patterns arrays), add assertions
       - If any build*Prompt function lacks coverage, add a simple "returns non-empty string" test

    4. Examine jest.config.js evolve.js threshold entry. Phase 55 set `{ lines: 85, functions: 94, branches: 65 }`. Phase 56 Plan 02 may have adjusted this. Ensure:
       - lines >= 85 (floor requirement)
       - functions and branches thresholds reflect actual achievable coverage after Phase 56 additions
       - If Phase 56 lowered thresholds, verify the justification (orchestrator functions with mocked deps may naturally have lower branch coverage)

    5. Run the full test suite: `npx jest --coverage`
       - Confirm total test count >= 2,069 (no regressions from Phase 55 baseline)
       - Confirm all per-file thresholds pass including evolve.js
       - Confirm no other lib/ modules had coverage regressions from Phase 56 changes (especially mcp-server.js which Phase 56 modifies)
  </action>
  <verify>
    npx jest --coverage 2>&1 | tail -40
    - All tests pass (0 failures)
    - Total test count >= 2,069 (no regressions)
    - All per-file thresholds in jest.config.js pass (Level 2: Proxy)
    - evolve.js specifically shows 85%+ line coverage
  </verify>
  <done>
    jest.config.js has validated coverage thresholds for lib/evolve.js at 85%+ lines. All evolve.js exports (core engine + orchestrator) have test coverage. Full test suite passes with zero regressions and test count >= 2,069.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): jest.config.js is valid JS, can be require()'d without error
Level 2 (Proxy): `npx jest --coverage` passes with all per-file thresholds met, evolve.js at 85%+ lines, and test count shows no regressions
</verification>

<success_criteria>
- lib/evolve.js has a per-file threshold in jest.config.js at 85%+ lines
- All evolve.js exports (Phase 55 core + Phase 56 orchestrator) have test coverage
- Full test suite passes: `npx jest --coverage` exits 0
- Test count >= 2,069 (no regression from Phase 55 completion)
- No other lib/ modules had coverage regressions from Phase 56 changes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-01-SUMMARY.md`
</output>
