---
phase: 57-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - jest.config.js
  - tests/unit/evolve.test.js
  - tests/unit/markdown-split.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "jest.config.js contains per-file coverage thresholds (85%+ lines) for lib/evolve.js and lib/markdown-split.js (or whatever modules Phase 54-56 created)"
    - "All new lib/ modules created in phases 54-56 have corresponding unit test files in tests/unit/"
    - "npx jest --coverage passes with zero failures and all per-file thresholds met"
    - "Total test count is 1,983+ (no regressions from phases 54-56)"
  artifacts:
    - path: "jest.config.js"
      provides: "Coverage thresholds for all new modules"
      contains: "lines: 85"
  key_links:
    - from: "jest.config.js"
      to: "lib/"
      via: "coverage threshold entries"
      pattern: "lib/.*\\.js.*lines"
---

<objective>
Enforce test coverage thresholds for all new modules introduced in phases 54-56 and validate that the full regression suite passes with zero regressions.

Purpose: Phase 57 success criterion #4 requires all new lib/ modules maintain 85%+ line coverage with per-file thresholds enforced in jest.config.js. Criterion #5 requires the full regression suite (1,983+ tests) passes with zero regressions.

Output: Updated jest.config.js with per-file thresholds, any missing unit test files for new modules, full passing test suite.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage thresholds for new modules and validate regression suite</name>
  <files>jest.config.js</files>
  <action>
    1. Inventory all new lib/ modules created by phases 54-56 by comparing the current lib/ directory listing against the known 20 modules (autopilot.js, backend.js, cleanup.js, commands.js, context.js, deps.js, frontmatter.js, gates.js, long-term-roadmap.js, mcp-server.js, parallel.js, paths.js, phase.js, roadmap.js, scaffold.js, state.js, tracker.js, utils.js, verify.js, worktree.js).

    2. For each new module found (expected: evolve-related module(s) and/or markdown-split module), add a per-file coverage threshold entry in jest.config.js following the existing pattern:
       ```
       './lib/{module-name}.js': { lines: 85, functions: 85, branches: 70 },
       ```
       Use 85% lines as the floor (matching Phase 51 convention), with functions and branches calibrated from actual coverage.

    3. Verify each new lib/ module has a corresponding test file at tests/unit/{module-name}.test.js. If any test file is missing, create a skeleton test file that imports the module and has at least one describe block with a placeholder test marked `test.todo()` -- this ensures the module is exercised during coverage collection.

    4. Run the full test suite: `npx jest --coverage`
       - Confirm total test count is >= 1,983 (zero regressions)
       - Confirm all per-file thresholds pass (including new entries)
       - If any new module falls below 85% lines, examine which functions lack coverage and add targeted unit tests to bring them above threshold

    5. If coverage for a new module is initially low (e.g., <80%), write focused tests covering the module's exported functions. Test at least: happy path for each export, edge cases for key logic, error paths for invalid inputs.
  </action>
  <verify>
    npx jest --coverage 2>&1 | tail -30
    - All tests pass (0 failures)
    - Total test count >= 1,983
    - All per-file thresholds in jest.config.js pass (Level 2: Proxy)
  </verify>
  <done>
    jest.config.js contains per-file thresholds for every new lib/ module from phases 54-56, all thresholds pass at 85%+ lines, total test count is 1,983+ with zero regressions.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): jest.config.js is valid JS, can be require()'d without error
Level 2 (Proxy): `npx jest --coverage` passes with all per-file thresholds met and test count >= 1,983
</verification>

<success_criteria>
- Every new lib/ module has a per-file threshold in jest.config.js at 85%+ lines
- Every new lib/ module has a corresponding test file in tests/unit/
- Full test suite passes: `npx jest --coverage` exits 0
- Test count is 1,983+ (zero regression from prior milestones)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.8/phases/57-integration/57-01-SUMMARY.md`
</output>
