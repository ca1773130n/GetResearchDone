---
phase: 28-pr-workflow
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - commands/execute-phase.md
  - agents/grd-executor.md
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "execute-phase.md handle_branching step creates a worktree instead of switching branches in-place when branching_strategy is phase"
    - "execute-phase.md passes worktree_path to executor agent prompts so agents know their working directory"
    - "execute-phase.md has a push_pr step after all plans complete that calls worktree push-pr"
    - "execute-phase.md has a cleanup_worktree step that removes worktree after PR (or on failure via finally pattern)"
    - "Executor agent grd-executor.md instructions tell agents to use worktree_path as cwd for file operations when provided"
    - "PR title/body template in execute-phase.md includes phase number, milestone, and aggregated plan summaries"
  artifacts:
    - path: "commands/execute-phase.md"
      provides: "Worktree-aware execute-phase orchestrator template"
      contains: "worktree"
    - path: "agents/grd-executor.md"
      provides: "Worktree-aware executor agent instructions"
      contains: "worktree_path"
  key_links:
    - from: "commands/execute-phase.md"
      to: "lib/worktree.js"
      via: "grd-tools worktree create/push-pr/remove CLI calls"
      pattern: "worktree"
    - from: "commands/execute-phase.md"
      to: "agents/grd-executor.md"
      via: "worktree_path passed in agent spawn prompt"
      pattern: "worktree_path"
---

<objective>
Modify the execute-phase command template and executor agent to use worktree-based isolation. The orchestrator creates a worktree, passes the path to executors, and creates a PR after completion.

Purpose: This is the glue that connects Phase 27's worktree infrastructure to the actual execution workflow. After this, every phase execution will produce an isolated PR from a worktree.

Output: Updated execute-phase.md with worktree lifecycle steps, updated grd-executor.md with worktree awareness.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/execute-phase.md
@agents/grd-executor.md
@lib/worktree.js
@.planning/phases/28-pr-workflow/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add worktree lifecycle to execute-phase.md template</name>
  <files>commands/execute-phase.md</files>
  <action>
    Modify `commands/execute-phase.md` to integrate worktree-based execution. This is a MARKDOWN template (not code), so changes are to the instruction text, not to JavaScript.

    **Changes to make:**

    **1. Replace the `handle_branching` step** with a new `setup_worktree` step that:

    Condition: `branching_strategy` is `"phase"` or `"milestone"` (same as before).
    When `branching_strategy` is `"none"`: skip worktree creation entirely, continue on current directory (no worktree isolation).

    New step content:
    ```
    <step name="setup_worktree" condition="branching_strategy != none">
    Create an isolated worktree for this phase execution using pre-computed fields from init:

    1. **Check for uncommitted changes on current branch:**
       ```bash
       DIRTY=$(git status --porcelain)
       ```
       If non-empty: warn "Uncommitted changes detected. Stash or commit before worktree creation." Stop.

    2. **Create worktree:**
       ```bash
       WT_RESULT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js worktree create --phase "${PHASE_NUMBER}" --slug "${PHASE_SLUG}")
       ```
       Parse JSON result for `path` and `branch`. If `error` field present: report error and stop.
       Store: `WORKTREE_PATH` = result.path, `WORKTREE_BRANCH` = result.branch

    3. **Verify worktree is ready:**
       ```bash
       ls "${WORKTREE_PATH}/.planning" && echo "Worktree ready"
       ```

    All executor agents will receive `WORKTREE_PATH` and operate within it.
    The main checkout remains clean on its original branch.
    </step>
    ```

    **2. Modify executor agent spawn prompts** (both `execute_waves` and `execute_waves_teams` steps):

    Add `worktree_path` to the agent prompt context. In both the Task() spawn blocks, add:

    ```
    <worktree>
    Working directory: ${WORKTREE_PATH}
    All file operations (Read, Write, Edit) and Bash commands MUST use this
    directory as the working root. Use absolute paths prefixed with this directory
    for all file operations. For Bash commands, use: cd "${WORKTREE_PATH}" && ...
    If WORKTREE_PATH is not set (branching_strategy=none), use the normal project root.
    </worktree>
    ```

    **3. Add a new `push_and_create_pr` step** after `aggregate_results` and before `auto_trigger_eval`:

    ```
    <step name="push_and_create_pr" condition="branching_strategy != none">
    Push the worktree branch and create a PR:

    1. **Collect plan summaries for PR body:**
       Read each SUMMARY.md in the phase directory. Extract the one-liner from each.
       Build a PR body:
       ```
       ## Phase ${PHASE_NUMBER}: ${PHASE_NAME}

       **Milestone:** ${MILESTONE_VERSION}

       ### Plans Completed
       ${for each plan: "- **${plan_id}:** ${one_liner}"}

       ### Verification
       See ${PHASE_DIR}/${PHASE_NUMBER}-VERIFICATION.md
       ```

    2. **Push branch and create PR:**
       ```bash
       PR_RESULT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js worktree push-pr \
         --phase "${PHASE_NUMBER}" \
         --title "Phase ${PHASE_NUMBER}: ${PHASE_NAME} (${MILESTONE_VERSION})" \
         --body "${PR_BODY}" \
         --base "${BASE_BRANCH}")
       ```
       Parse JSON for `pr_url`, `error`.

       If `error`: report "PR creation failed: ${error}". The branch may still have been pushed.
       Offer: "Retry PR creation?" or "Continue without PR (branch pushed)?" or "Skip PR entirely?"

       If success: report "PR created: ${pr_url}"

    3. **Report PR:**
       ```
       ## PR Created

       **URL:** ${pr_url}
       **Branch:** ${WORKTREE_BRANCH} → ${BASE_BRANCH}
       **Phase:** ${PHASE_NUMBER}: ${PHASE_NAME}
       ```
    </step>
    ```

    **4. Add a `cleanup_worktree` step** after `update_roadmap` (near the end):

    ```
    <step name="cleanup_worktree" condition="branching_strategy != none">
    Remove the worktree after PR creation (or on any failure path):

    ```bash
    node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js worktree remove --phase "${PHASE_NUMBER}"
    ```

    This is idempotent — if the worktree was already removed (crash, manual cleanup), it returns success with `already_gone: true`.

    **IMPORTANT:** This step MUST run even if earlier steps failed. Treat it as a finally block. If the orchestrator encounters any error after worktree creation, it should still attempt cleanup before reporting the error.
    </step>
    ```

    **5. Update the `failure_handling` section** to mention worktree cleanup:
    Add: "After any failure, always run worktree cleanup before stopping. Worktree removal is idempotent and safe to call even if the worktree was never created."

    **6. Update the `resumption` section** to note:
    "On resumption, check if a worktree already exists for this phase (via `worktree list`). If it does, reuse it instead of creating a new one. If it doesn't, create a fresh one."

    **Key design decisions preserved:**
    - `branching_strategy: "none"` skips worktree entirely (backwards compatible)
    - PR is created by the ORCHESTRATOR after all plans complete, not by individual executors
    - Worktree cleanup is idempotent and runs on all paths (success, failure, resumption)
    - The old `handle_branching` step is fully REPLACED (not kept alongside)
  </action>
  <verify>
    Read the modified execute-phase.md and verify:
    1. `setup_worktree` step exists with worktree create command
    2. Agent spawn prompts include `<worktree>` section with WORKTREE_PATH
    3. `push_and_create_pr` step exists with worktree push-pr command
    4. `cleanup_worktree` step exists with worktree remove command
    5. No references to old `handle_branching` step remain (the old git checkout -b pattern)
    6. `branching_strategy: "none"` still skips all worktree steps (backward compatible)

    ```bash
    grep -c "worktree" commands/execute-phase.md
    grep -c "handle_branching" commands/execute-phase.md
    grep -c "WORKTREE_PATH" commands/execute-phase.md
    grep -c "push_and_create_pr" commands/execute-phase.md
    grep -c "cleanup_worktree" commands/execute-phase.md
    ```
    Expect: worktree count > 10, handle_branching count = 0, WORKTREE_PATH count > 3, push_and_create_pr = 1+, cleanup_worktree = 1+.
  </verify>
  <done>
    commands/execute-phase.md has worktree lifecycle: setup_worktree replaces handle_branching,
    agent prompts include worktree_path, push_and_create_pr creates PR after plans,
    cleanup_worktree runs idempotent removal. branching_strategy=none is backwards compatible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make grd-executor.md worktree-aware</name>
  <files>agents/grd-executor.md</files>
  <action>
    Modify `agents/grd-executor.md` to handle the `<worktree>` context block that execute-phase now passes in spawn prompts.

    **Changes to make:**

    **1. Add a `<worktree_execution>` section** after the `<execution_flow>` section (before `<deviation_rules>`):

    ```markdown
    <worktree_execution>
    ## Working Directory: Worktree Isolation

    When spawned with a `<worktree>` block in your prompt, you are operating in an isolated git worktree — NOT the main checkout.

    **Rules:**
    1. **All file paths are relative to the worktree.** The worktree is a full copy of the repo. Use the `WORKTREE_PATH` from your prompt as the root for all operations.
    2. **Bash commands:** Always prefix with `cd "${WORKTREE_PATH}" &&` or use absolute paths within the worktree.
    3. **Read/Write/Edit tools:** Use absolute paths: `${WORKTREE_PATH}/lib/worktree.js` not `lib/worktree.js`.
    4. **Git commits happen in the worktree.** The worktree has its own branch. Commits go to that branch automatically.
    5. **State updates (.planning/STATE.md):** These should be written to the MAIN repo (not the worktree), because STATE.md is shared state. Use the original project root (without WORKTREE_PATH prefix) for STATE.md operations.
    6. **SUMMARY.md:** Write to the worktree's .planning/phases/ directory (within WORKTREE_PATH). The orchestrator handles merging.

    **When NO `<worktree>` block is present:** Operate normally in the current working directory (backwards compatible).
    </worktree_execution>
    ```

    **2. Update the `load_project_state` step** to note:
    Add after the existing INIT loading: "If your prompt includes a `<worktree>` block, use `WORKTREE_PATH` as the working directory for all subsequent operations except STATE.md updates."

    **3. Update the `task_commit_protocol` section:**
    Add a note: "When operating in a worktree, git commands automatically use the worktree's branch. Ensure you `cd` to `WORKTREE_PATH` before running git commands."

    **4. Update the `state_updates` section:**
    Add a note: "STATE.md lives in the main repo, not the worktree. Use the original project root (the cwd from your init context, NOT WORKTREE_PATH) when running grd-tools state commands."

    **Design rationale:**
    - Source code changes go to the worktree (isolated branch)
    - STATE.md updates go to the main repo (shared coordination state)
    - SUMMARY.md goes to the worktree (will be on the PR branch)
    - The orchestrator handles the boundary between worktree and main repo
  </action>
  <verify>
    Read the modified grd-executor.md and verify:
    1. `<worktree_execution>` section exists with 6 rules
    2. Rules clearly distinguish worktree operations from main repo operations
    3. STATE.md explicitly directed to main repo, not worktree
    4. Backward compatibility noted (no worktree block = normal operation)

    ```bash
    grep -c "worktree" agents/grd-executor.md
    grep -c "WORKTREE_PATH" agents/grd-executor.md
    ```
    Expect: worktree count > 8, WORKTREE_PATH count > 4.
  </verify>
  <done>
    agents/grd-executor.md includes worktree_execution section with clear rules for
    operating in worktree vs main repo. Backward compatible (no worktree = normal operation).
    STATE.md correctly directed to main repo. Git commits correctly use worktree branch.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): commands/execute-phase.md contains setup_worktree, push_and_create_pr, cleanup_worktree steps. agents/grd-executor.md contains worktree_execution section.
Level 2 (Proxy): No old handle_branching references remain. Worktree commands referenced correctly match the CLI from Plan 01. Both files parse as valid markdown.
Level 3 (Deferred): End-to-end execution of a phase in a worktree with actual PR creation (Phase 31).
</verification>

<success_criteria>
- execute-phase.md creates worktree before spawning agents
- execute-phase.md passes WORKTREE_PATH in agent spawn prompts
- execute-phase.md creates PR after all plans complete via worktree push-pr
- execute-phase.md cleans up worktree on all paths (success and failure)
- grd-executor.md has worktree_execution rules for isolated operation
- Backward compatibility preserved when branching_strategy=none
- No references to old handle_branching pattern remain
</success_criteria>

<output>
After completion, create `.planning/phases/28-pr-workflow/28-02-SUMMARY.md`
</output>
