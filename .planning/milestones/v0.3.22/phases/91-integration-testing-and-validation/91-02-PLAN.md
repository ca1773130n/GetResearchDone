---
phase: 91-integration-testing-and-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/unit/autopilot.test.ts]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "parseWriteIntent tests cover: YAML dash-list with quoted paths, inline array with spaces around commas, multiline with trailing whitespace, frontmatter with no files_modified key, and edge case of files_modified followed immediately by another YAML key on the next line"
    - "buildWaves tests cover: phases with overlapping files_modified split into separate waves, three-way overlap produces three sub-waves, forceParallel keeps overlapping phases in one wave, phases with no filesModified entry treated as no-conflict, mixed dependency and file-conflict waves"
    - "compareWriteIntent and formatWriteIntentMismatch tests cover: large file lists, duplicate entries in declared/actual, and formatWriteIntentMismatch producing [WRITE-INTENT-MISMATCH] log entries with correct plan ID and file names"
    - "npm test passes; npm run lint and npm run build:check pass with zero errors"
  artifacts:
    - path: "tests/unit/autopilot.test.ts"
      provides: "Extended write-intent and wave builder unit tests"
  key_links:
    - from: "tests/unit/autopilot.test.ts"
      to: "lib/autopilot.ts"
      via: "imports parseWriteIntent, compareWriteIntent, formatWriteIntentMismatch, buildWaves"
      pattern: "parseWriteIntent|compareWriteIntent|formatWriteIntentMismatch|buildWaves"
---

<objective>
Add comprehensive unit tests for write-intent parsing, comparison, mismatch formatting, and the buildWaves wave builder with file-conflict detection.

Purpose: Satisfy REQ-177 (write-intent wave builder tests) — verifying files_modified parsing from PLAN.md, overlapping phase detection, --force-parallel override, and [WRITE-INTENT-MISMATCH] log output.
Output: Extended tests/unit/autopilot.test.ts with 10-12 new test cases.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/autopilot.ts
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extended parseWriteIntent edge cases</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add new test cases inside the existing `describe('parseWriteIntent', ...)` block. Existing tests cover: dash-list, inline array, empty string, missing field, empty inline array, single file. Add:

    1. **Quoted paths in dash-list** — frontmatter with `- "lib/file with spaces.ts"` or `- 'lib/quoted.ts'`:
       - Verify parseWriteIntent strips quotes and returns clean paths

    2. **Inline array with extra whitespace** — `files_modified: [ lib/a.ts ,  lib/b.ts ]`:
       - Verify whitespace is trimmed from each entry

    3. **Multiline dash-list with trailing whitespace** — entries have trailing spaces:
       - Verify trailing whitespace is trimmed

    4. **files_modified immediately followed by another YAML key** — tests that the dash-list parser stops at the next YAML key (e.g., `autonomous: true` on the line after the last dash item):
       - Verify only the dash-list items are returned, not the next key's value

    5. **files_modified with mixed indentation** — tabs vs spaces in dash items:
       - Verify both are parsed correctly (the regex uses `[ \t]+`)

    These tests exercise the regex paths in parseWriteIntent: the inline array regex (`/^files_modified:\s*\[([^\]]*)\]\s*$/m`) and the dash-list loop (`/^[ \t]+-[ \t]+(.+)$/`).
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts -t "parseWriteIntent" --no-coverage (Level 2: Proxy)
    All parseWriteIntent tests pass.
  </verify>
  <done>5 new parseWriteIntent edge-case tests pass covering quoted paths, whitespace handling, trailing whitespace, next-key boundary, and mixed indentation</done>
</task>

<task type="auto">
  <name>Task 2: buildWaves file-conflict splitting and forceParallel edge cases</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add new test cases inside the existing `describe('buildWaves', ...)` block. Existing tests cover: basic overlapping split, no-conflict pass-through, forceParallel override, dependency-based waves. Add:

    1. **Three-way file overlap produces three sub-waves** — Three independent phases (no deps) all declare the same file in files_modified. buildWaves with filesModified should produce three separate waves (one phase per wave):
       - phases: [{number:'1', name:'a'}, {number:'2', name:'b'}, {number:'3', name:'c'}]
       - filesModified: {'1': ['shared.ts'], '2': ['shared.ts'], '3': ['shared.ts']}
       - Assert: result has 3 waves, each with exactly one phase

    2. **Partial overlap — only conflicting phases separated** — Three phases: A and B share a file, C has no overlap. All independent. Assert A and B in separate waves, C in the first wave with whichever doesn't conflict:
       - filesModified: {'1': ['shared.ts', 'a.ts'], '2': ['shared.ts', 'b.ts'], '3': ['c.ts']}
       - Assert: waves[0] contains phases 1 and 3, waves[1] contains phase 2 (or equivalent non-overlapping arrangement)

    3. **forceParallel with three-way overlap** — Same setup as test 1 but with `forceParallel: true`. Assert all three phases remain in one wave.

    4. **Phases with no filesModified entry treated as conflict-free** — Phase has no entry in the filesModified map. Assert it is placed in wave alongside other phases without triggering a split:
       - phases: [{number:'1', name:'a'}, {number:'2', name:'b'}]
       - filesModified: {'1': ['lib/a.ts']}  (phase 2 missing from map)
       - Assert: both phases in same wave

    5. **Mixed dependency and file-conflict waves** — Phase B depends on A. Phase C is independent but overlaps files with B. Assert: wave 1 = [A, C cannot be with B], wave structure respects both dependency order and file conflicts:
       - phases: [{number:'1', name:'a'}, {number:'2', name:'b', depends_on:'1'}, {number:'3', name:'c'}]
       - filesModified: {'2': ['shared.ts'], '3': ['shared.ts']}
       - Assert: A and C in wave 1 (independent, no file conflict), B alone in wave 2 (depends on A, and wave splitting ensures no overlap with C)

    6. **formatWriteIntentMismatch with multiple unexpected and untouched** — 3 unexpected, 2 untouched files. Assert line count is 5, each line has `[WRITE-INTENT-MISMATCH]` prefix and correct plan ID.

    7. **compareWriteIntent with duplicate entries** — declared=['a.ts', 'a.ts'], actual=['a.ts']. Verify behavior is reasonable (matches contains 'a.ts', untouched contains the second 'a.ts' since Set-based comparison removes duplicate from actual).
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts -t "buildWaves|compareWriteIntent|formatWriteIntentMismatch" --no-coverage (Level 2: Proxy)
    All wave builder and write-intent tests pass.
  </verify>
  <done>7 new tests pass covering three-way overlap, partial overlap, forceParallel edge case, missing filesModified entry, mixed dependency/file-conflict waves, multiple mismatch formatting, and duplicate entries. npm test passes.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): npx jest tests/unit/autopilot.test.ts --no-coverage — all tests pass
Level 2 (Proxy): npm test — full suite passes; coverage on lib/autopilot.ts at or above 83% lines
Level 3 (Deferred): npm run lint && npm run build:check — zero errors
</verification>

<success_criteria>
12 new test cases for parseWriteIntent, buildWaves, compareWriteIntent, and formatWriteIntentMismatch covering all edge cases specified in REQ-177. npm test passes with all tests green.
</success_criteria>

<output>
After completion, create `${phase_dir}/91-02-SUMMARY.md`
</output>
