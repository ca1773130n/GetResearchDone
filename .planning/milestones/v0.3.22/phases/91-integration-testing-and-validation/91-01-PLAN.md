---
phase: 91-integration-testing-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/unit/autopilot.test.ts]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "runPostPhasePipeline has tests for full success path (all 4 steps pass), code-review failure path, rebase success path (no conflicts), push-rebased failure path, merge (gh pr merge) failure path, and mergeQueue integration (Step 4 enqueued through shared queue)"
    - "createMergeQueue tests verify that multiple runPostPhasePipeline calls sharing one queue serialize only Step 4 while Steps 1-3 run concurrently"
    - "Conflict resolution path tests verify: rebase fails -> spawnStep called with buildConflictResolvePrompt -> success rebases and merges; failure aborts rebase and returns structured error with conflicting files and manual steps"
    - "npm test passes; npm run lint and npm run build:check pass with zero errors"
  artifacts:
    - path: "tests/unit/autopilot.test.ts"
      provides: "Extended post-phase pipeline and merge queue unit tests"
  key_links:
    - from: "tests/unit/autopilot.test.ts"
      to: "lib/autopilot.ts"
      via: "imports runPostPhasePipeline, createMergeQueue"
      pattern: "runPostPhasePipeline|createMergeQueue"
---

<objective>
Add deep unit tests for runPostPhasePipeline covering every step's success/failure path and mergeQueue serialization integration.

Purpose: Satisfy REQ-175 (post-phase pipeline unit tests) and REQ-176 (serial merge queue tests) — achieving 85%+ line coverage on the pipeline code paths introduced in phases 87-88.
Output: Extended tests/unit/autopilot.test.ts with 12-15 new test cases.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/autopilot.ts
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: runPostPhasePipeline deep coverage — all 4 steps success/failure</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add new test cases inside the existing `describe('runPostPhasePipeline', ...)` block.
    The existing tests cover: simplify failure, create-pr failure, simplify timeout, and conflict resolution failure halt message. What is missing:

    1. **Full success path** — Mock all 4 steps to succeed:
       - `spawn` mock returns exitCode 0 for simplify (call 0), code-review (call 1)
       - `execFileSync` mock: pushAndCreatePR returns PR URL (mock `git push` and `gh pr create`), `git rebase main` succeeds (exitCode 0), `git rev-parse --abbrev-ref HEAD` returns branch name, `git push --force-with-lease` succeeds, `gh pr merge` succeeds
       - Assert: `result.status === 'completed'`, `result.prUrl` is the mocked URL

    2. **Code-review step failure** — Simplify succeeds, PR created, then spawn returns non-zero for code-review:
       - Assert: `result.failedStep === 'code-review'`, `result.prUrl` is set (PR was already created), `result.reason` contains exit code

    3. **Code-review step timeout** — Same as above but spawn child simulates timeout (use `child.kill` with SIGTERM pattern or set timedOut flag):
       - Assert: `result.reason` contains 'timeout'

    4. **Rebase succeeds (no conflicts)** — Full pipeline mocked, rebase returns exitCode 0 (no conflicts), force-push succeeds, merge succeeds:
       - Assert: `result.status === 'completed'`
       - Assert: spawn was called exactly 2 times (simplify + code-review, NOT conflict resolution)

    5. **Push-rebased failure** — Rebase succeeds, but `git push --force-with-lease` returns non-zero:
       - Assert: `result.failedStep === 'push-rebased'`, `result.reason` contains 'push failed'

    6. **Merge (gh pr merge) failure** — Rebase and push succeed, but `gh pr merge` throws:
       - Assert: `result.failedStep === 'merge'`, `result.reason` contains the error

    7. **Conflict resolution success path** — Rebase fails with conflicts, spawn conflict resolver succeeds (exitCode 0), then force-push and merge succeed:
       - Assert: `result.status === 'completed'`, spawn was called 3 times (simplify, code-review, conflict-resolve)

    Mock pattern: Use the existing `createMockChild(exitCode)` helper and `jest.spyOn(childProcess, 'spawn')` / `jest.spyOn(childProcess, 'execFileSync')` pattern already established in the test file. Track spawn call count to distinguish simplify/code-review/conflict-resolve calls.

    For `execFileSync` mocking, handle these commands:
    - `git push` → return empty string (success) or throw (failure)
    - `gh pr create` → return 'https://github.com/test/repo/pull/42'
    - `git rebase main` → return empty string (success) or throw with status 1 (conflict)
    - `git rev-parse --abbrev-ref HEAD` → return 'grd/phase-48'
    - `git push --force-with-lease` → return empty string or throw
    - `gh pr merge` → return empty string or throw
    - `git diff --name-only --diff-filter=U` → return conflicting file list
    - `git rebase --abort` → return empty string
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts -t "runPostPhasePipeline" --no-coverage (Level 2: Proxy)
    All new tests pass. No existing tests break.
  </verify>
  <done>7 new runPostPhasePipeline tests pass covering full success, code-review failure/timeout, no-conflict rebase, push failure, merge failure, and conflict resolution success</done>
</task>

<task type="auto">
  <name>Task 2: mergeQueue integration and concurrent pipeline serialization tests</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add new test cases to the existing `describe('createMergeQueue', ...)` block and/or a new `describe('mergeQueue + runPostPhasePipeline integration', ...)` block.

    1. **mergeQueue integration with runPostPhasePipeline** — Call runPostPhasePipeline with a `mergeQueue` option. Mock all steps to succeed. Verify that Step 4 (rebase+merge) is enqueued via the mergeQueue by:
       - Creating a real mergeQueue via `createMergeQueue()`
       - Passing it in opts
       - Asserting the pipeline completes successfully
       - This confirms the `mergeQueue ? mergeQueue.enqueue(runStep4) : runStep4()` code path

    2. **Two pipelines share one mergeQueue — Step 4 serialized** — Launch two runPostPhasePipeline calls concurrently (Promise.all) with the same mergeQueue. Mock spawn and execFileSync for both. Track the order of merge operations (when `gh pr merge` is called for each phase). Assert:
       - Both pipelines complete
       - The `gh pr merge` calls happen sequentially (never interleaved)
       - The first pipeline to reach Step 4 merges first

    3. **Conflict resolution spawns subprocess with correct arguments** — When rebase fails with conflicts, verify that `spawnStep` is called with `buildConflictResolvePrompt(phaseNum, cwd, wtPath)` by checking the prompt passed to spawn includes the phase number and references conflict resolution.

    4. **Autopilot halts with structured error when conflict subprocess exits non-zero** — Already partially tested, but add an explicit test that the error message includes: phase number, "conflicting files" listing, "Manual steps" with `git checkout`, `git rebase main`, and `git rebase --continue`.

    Use `delay()` helper (already defined in the test file) to simulate varying execution times for the two concurrent pipelines, ensuring the merge queue ordering behavior is observable.
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts -t "mergeQueue" --no-coverage (Level 2: Proxy)
    All merge queue tests pass. Then run full suite: npm test
  </verify>
  <done>4 new merge queue tests pass: pipeline integration, two-pipeline serialization, conflict subprocess args, structured halt error. npm test passes with zero errors.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): npx jest tests/unit/autopilot.test.ts --no-coverage — all tests pass, no timeouts
Level 2 (Proxy): npm test — full suite passes; coverage on lib/autopilot.ts remains at or above 83% lines
Level 3 (Deferred): npm run lint && npm run build:check — zero errors
</verification>

<success_criteria>
11+ new test cases for runPostPhasePipeline and createMergeQueue covering every pipeline step's success/failure path, mergeQueue enqueue integration, concurrent pipeline serialization, conflict resolution subprocess invocation, and structured halt errors. npm test passes with all tests green.
</success_criteria>

<output>
After completion, create `${phase_dir}/91-01-SUMMARY.md`
</output>
