---
phase: 90-autopilot-mode-changes-and-parallel-execution
plan: "03"
type: execute
wave: 2
depends_on:
  - "90-01"
  - "90-02"
files_modified:
  - tests/unit/autopilot.test.ts
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "SC3 (parallel worktree execution) is verified: buildWaves groups independent phases, worktree creation/cleanup works"
    - "SC5 (milestone wireup) is verified: wireup runs after all phases complete in milestone mode"
    - "All 5 success criteria for phase 90 are satisfied"
    - "Full test suite passes with zero regressions"
  artifacts:
    - path: "tests/unit/autopilot.test.ts"
      provides: "Integration tests verifying SC3 worktree parallelism and SC5 milestone wireup"
  key_links: []
---

<objective>
Verify SC3 (worktree-isolated parallel execution) and SC5 (milestone wireup step) are fully implemented, and run the complete test suite to confirm all 5 success criteria for phase 90 are satisfied. Both features were implemented in phases 87-89; this plan adds focused verification tests.

Purpose: REQ-172 (Milestone Wireup Step), REQ-173 (Worktree-Isolated Phase Execution) — verification only.
Output: Tests confirming parallel execution and wireup behavior; full SC checklist verified.
</objective>

<execution_context>
Already-implemented features:
- SC3: Wave-based parallel execution with worktrees exists at lines 1460-1593 of lib/autopilot.ts. buildWaves() (line 940) uses dependency graph + splitWave for file-conflict detection. Each phase gets a worktree via getWorktreePath/getWorktreeBranch/execGit worktree add. Dependent phases are in later waves and block until earlier waves complete.
- SC5: Milestone wireup at lines 1607-1635. After all phases complete in milestone mode (isMilestoneMode && !stoppedAt && phasesCompleted === phasesAttempted), buildWireupPrompt() is spawned and results reported.

This plan adds test coverage confirming these behaviors.
</execution_context>

<context>
@lib/autopilot.ts
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task id="1" type="auto">
<name>Add worktree parallel execution tests</name>
<description>
In `tests/unit/autopilot.test.ts`, add or verify tests in the `buildWaves` describe block:

1. "groups independent phases into same wave" — phases with no depends_on should be in wave 1 together
2. "puts dependent phases in later waves" — phase B depends_on A: A in wave 1, B in wave 2
3. "splits wave on file conflicts" — two phases in same wave with overlapping files_modified should be split into separate sub-waves (uses splitWave via buildWaves)

These tests may already exist from phase 89. Check first — if they exist and pass, add a comment confirming SC3 and skip to task 2.
</description>
<done_criteria>
- buildWaves correctly groups independent phases and separates dependent ones
- File-conflict splitting is tested
- All wave-related tests pass
</done_criteria>
</task>

<task id="2" type="auto">
<name>Verify milestone wireup integration</name>
<description>
In `tests/unit/autopilot.test.ts`, verify that milestone wireup code exists and is reachable:

1. Check that `buildWireupPrompt` is exported and returns a non-empty string
2. Add a test: "buildWireupPrompt returns non-empty prompt" — call buildWireupPrompt(), verify it returns a string with length > 0

The actual wireup execution is tested via the runAutopilot integration (too complex to unit-test the subprocess spawning), but we can verify the prompt builder works.

If a buildWireupPrompt test already exists, verify it passes and skip adding a duplicate.
</description>
<done_criteria>
- buildWireupPrompt returns a non-empty string
- Test verifying wireup prompt builder passes
</done_criteria>
</task>

<task id="3" type="auto">
<name>Run full test suite and verify all success criteria</name>
<description>
Run the complete test suite to verify zero regressions:

```bash
npm run build:check
npm test
```

Then verify each success criterion:
- SC1: `gd autopilot` with no args enters milestone mode — confirmed by milestone-mode test from 90-01
- SC2: No --resume flag, --phase-from/--phase-to only — confirmed by stale-reference scan from 90-01
- SC3: Independent phases concurrent in worktrees — confirmed by buildWaves tests
- SC4: Atomic writes via temp+rename — confirmed by atomic write tests from 90-02
- SC5: Wireup runs in milestone mode after all phases — confirmed by buildWireupPrompt test and code inspection

Document verification results.
</description>
<done_criteria>
- npm run build:check passes with zero errors
- npm test passes with zero failures
- All 5 success criteria documented as satisfied
</done_criteria>
</task>

</tasks>

<verification>
```bash
npm run build:check
npm test
```
</verification>

<success_criteria>
- SC3 parallel worktree execution verified with tests
- SC5 milestone wireup verified with tests
- Full test suite passes with zero regressions
- All 5 success criteria for phase 90 confirmed
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create 90-03-SUMMARY.md with the full SC verification checklist and test results.
</output>
