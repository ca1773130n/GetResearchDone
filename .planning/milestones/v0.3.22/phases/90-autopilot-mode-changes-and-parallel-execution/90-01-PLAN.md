---
phase: 90-autopilot-mode-changes-and-parallel-execution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/autopilot.ts
  - commands/autopilot.md
  - tests/unit/autopilot.test.ts
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "No --resume flag exists in AutopilotOptions, cmdAutopilot, or cmdMultiMilestoneAutopilot"
    - "CLI uses --phase-from/--phase-to exclusively; no --from/--to aliases exist"
    - "gd autopilot with no arguments enters milestone mode (isMilestoneMode is true)"
    - "Auto-resume skips fully executed phases at execution step via isPhaseExecuted check"
    - "Auto-resume skips already-planned phases at plan step via isPhasePlanned check"
    - "commands/autopilot.md documents --phase-from/--phase-to and auto-resume behavior"
  artifacts:
    - path: "lib/autopilot.ts"
      provides: "Verified milestone-mode default, auto-resume logic, and flag cleanup"
    - path: "commands/autopilot.md"
      provides: "Updated skill definition with --phase-from/--phase-to and auto-resume docs"
    - path: "tests/unit/autopilot.test.ts"
      provides: "Tests verifying milestone-mode default and auto-resume behavior"
  key_links: []
---

<objective>
Verify and test that SC1 (milestone-mode default) and SC2 (flag cleanup) are fully implemented. Analysis shows both are already done: `--resume` never existed in AutopilotOptions, `--phase-from`/`--phase-to` are the only range flags, milestone mode triggers when neither is set, and auto-resume skips planned/executed phases. This plan adds verification tests and confirms no stale references remain.

Purpose: REQ-170 (Always-On Auto-Resume) and REQ-171 (Milestone Mode Default).
Output: Tests confirming milestone-mode and auto-resume behavior; verified clean flag state.
</objective>

<execution_context>
CRITICAL: The features in SC1 and SC2 are ALREADY IMPLEMENTED in lib/autopilot.ts. This plan is verification-only — confirming existing behavior with tests and scanning for stale references.

Key locations:
- Line 1608: `const isMilestoneMode = options.milestone === true || (!phaseFrom && !phaseTo);` — milestone mode default
- Lines 1247, 1437: `isPhasePlanned`/`isPhaseExecuted` auto-resume checks
- Lines 1912-1914: `cmdAutopilot` uses `--phase-from`/`--phase-to`
- AutopilotOptions interface (lines 216-227): no `--resume` field exists
</execution_context>

<context>
@lib/autopilot.ts
@commands/autopilot.md
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task id="1" type="auto">
<name>Verify no stale --from/--to/--resume references in codebase</name>
<description>
Search the entire codebase for any remaining references to `--from`, `--to` (as autopilot flags, not substring of other flags), or `--resume` in the autopilot context.

Steps:
1. Run `grep -r '\-\-resume' lib/ commands/ tests/ bin/` — should return zero hits related to autopilot
2. Run `grep -r '\-\-from\b' lib/ commands/ tests/ bin/` — should return zero autopilot-related hits
3. Run `grep -r '\-\-to\b' lib/ commands/ tests/ bin/` — should return zero autopilot-related hits (--phase-to is fine)

If any stale references are found, remove them. If none found, this task is a no-op verification.
</description>
<done_criteria>
- Zero stale --resume, --from, --to references in autopilot-related code
- Only --phase-from and --phase-to exist as range flags
</done_criteria>
</task>

<task id="2" type="auto">
<name>Add milestone-mode default test</name>
<description>
In `tests/unit/autopilot.test.ts`, add a test in the `runAutopilot` or `cmdAutopilot` describe block verifying that calling with empty options triggers milestone mode.

Test: "enters milestone mode when no phase-from/phase-to specified"
- Create a mock scenario where `options = {}` (no phaseFrom, no phaseTo, no milestone flag)
- Verify that the autopilot would treat this as milestone mode
- This can be tested indirectly by checking that `resolvePhaseRange` is called with `(cwd, undefined, undefined)` which triggers full-milestone resolution

Since runAutopilot has complex subprocess dependencies, test the milestone-mode boolean logic directly:
```typescript
// Verify milestone mode triggers correctly
const testMilestoneMode = (opts: { milestone?: boolean; phaseFrom?: string | null; phaseTo?: string | null }) => {
  return opts.milestone === true || (!opts.phaseFrom && !opts.phaseTo);
};
expect(testMilestoneMode({})).toBe(true);
expect(testMilestoneMode({ milestone: true })).toBe(true);
expect(testMilestoneMode({ phaseFrom: '90' })).toBe(false);
expect(testMilestoneMode({ phaseTo: '91' })).toBe(false);
```
</description>
<done_criteria>
- Test verifying milestone-mode default behavior passes
- Covers: empty options, explicit --milestone, phase-from set, phase-to set
</done_criteria>
</task>

<task id="3" type="auto">
<name>Add auto-resume skip-logic tests</name>
<description>
In `tests/unit/autopilot.test.ts`, add tests verifying the auto-resume contract:

1. "isPhasePlanned returns true when plan files exist" — create a phase dir with a PLAN.md, verify isPhasePlanned returns true
2. "isPhaseExecuted returns true when SUMMARY.md exists" — create a phase dir with SUMMARY.md, verify isPhaseExecuted returns true
3. "isPhasePlanned returns false for empty phase dir" — empty dir → false
4. "isPhaseExecuted returns false when no SUMMARY.md" — dir with only PLAN.md → false

These tests may already exist. If so, verify they pass and skip adding duplicates.
</description>
<done_criteria>
- Auto-resume skip-logic is tested for both plan and execute steps
- All tests pass
</done_criteria>
</task>

</tasks>

<verification>
```bash
npm run build:check
npx jest tests/unit/autopilot.test.ts --no-coverage -t "milestone mode|auto-resume|isPhasePlanned|isPhaseExecuted"
```
</verification>

<success_criteria>
- No stale --resume/--from/--to references exist in the codebase
- Milestone-mode default behavior is tested
- Auto-resume skip logic is tested for both plan and execute steps
- All existing tests continue to pass
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create 90-01-SUMMARY.md confirming SC1 and SC2 are verified.
</output>
