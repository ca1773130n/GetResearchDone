---
phase: 87-post-phase-pipeline-core
plan: 01
type: standard
phase_type: implement
wave: 1
autonomous: true
depends_on: null
files_modified:
  - lib/autopilot.ts
verification_level: proxy
---

<objective>
Implement `buildSimplifyPrompt()` and `buildCodeReviewPrompt()` prompt builders in `lib/autopilot.ts`.
These functions produce prompts for the post-phase pipeline subprocess steps.
`buildSimplifyPrompt(phaseNum)` targets changed files for code quality review.
`buildCodeReviewPrompt(prUrl)` targets the PR diff for BLOCKER/WARNING finding and fix.
Both subprocess invocations strip CLAUDE session env vars via `_buildSpawnConfig()`.
</objective>

<context>
@.planning/REQUIREMENTS.md (REQ-160, REQ-162)
@.planning/ROADMAP.md (Phase 87 success criteria 1, 3)
@lib/autopilot.ts (existing subprocess patterns)
@lib/backend.ts (buildBackendEnv pattern)
</context>

<tasks>

<task id="1" type="auto">
<name>Implement buildSimplifyPrompt</name>
<description>
Add `buildSimplifyPrompt(phaseNum: string): string` to lib/autopilot.ts.
Prompt instructs the subprocess to examine changed files via `git diff main...HEAD`,
check for duplicated logic, overly complex code, unused imports, dead code,
inconsistent naming/style, and make targeted improvements preserving functionality.
</description>
<done_criteria>
- Function exported from module.exports
- Prompt includes phase number
- Prompt targets git diff for changed files
</done_criteria>
</task>

<task id="2" type="auto">
<name>Implement buildCodeReviewPrompt</name>
<description>
Add `buildCodeReviewPrompt(prUrl: string): string` to lib/autopilot.ts.
Prompt instructs subprocess to review PR at given URL using `gh pr diff`,
classify issues as BLOCKER or WARNING, fix all findings, commit and push.
</description>
<done_criteria>
- Function exported from module.exports
- Prompt includes PR URL
- Prompt classifies findings as BLOCKER/WARNING
- Prompt instructs to fix and push
</done_criteria>
</task>

<task id="3" type="auto">
<name>Implement buildConflictResolvePrompt</name>
<description>
Add `buildConflictResolvePrompt(phaseNum: string): string` to lib/autopilot.ts.
Prompt instructs subprocess to resolve rebase conflicts by examining both versions,
preserving phase intent while incorporating main changes, then `git add` and
`git rebase --continue`.
</description>
<done_criteria>
- Function exported from module.exports
- Prompt includes phase number
- Prompt instructs to resolve conflicts and continue rebase
</done_criteria>
</task>

<task id="4" type="auto">
<name>Add unit tests for prompt builders</name>
<description>
Add tests in tests/unit/autopilot.test.ts verifying each prompt builder
includes the expected parameters (phase number, PR URL) and key instructions.
</description>
<done_criteria>
- Tests for buildSimplifyPrompt, buildCodeReviewPrompt, buildConflictResolvePrompt
- All tests pass
</done_criteria>
</task>

</tasks>

<verification>
```bash
npx jest tests/unit/autopilot.test.ts -t "post-phase pipeline prompt builders" --no-coverage
```
</verification>

<success_criteria>
- [ ] buildSimplifyPrompt returns prompt with phase number and git diff instruction
- [ ] buildCodeReviewPrompt returns prompt with PR URL and BLOCKER/WARNING classification
- [ ] buildConflictResolvePrompt returns prompt with phase number and rebase resolution
- [ ] All three functions exported from module.exports
- [ ] Subprocess env stripping via _buildSpawnConfig strips CLAUDE env vars
- [ ] Unit tests pass
</success_criteria>

<must_haves>
- Prompt builders produce actionable prompts for claude -p subprocess
- CLAUDE session env vars stripped before spawning (via _buildSpawnConfig)
</must_haves>
