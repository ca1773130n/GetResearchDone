---
phase: 89-write-intent-manifests-and-wave-builder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/autopilot.ts
  - lib/context/execute.ts
  - tests/unit/autopilot.test.ts
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "parseWriteIntent() correctly parses both YAML dash-list and inline-array formats"
    - "buildPlanPrompt() includes files_modified instruction for the planner"
    - "cmdInitExecutePhase emits parsed files_modified in its JSON output"
    - "All existing buildPlanPrompt tests continue to pass"
  artifacts:
    - path: "lib/autopilot.ts"
      provides: "parseWriteIntent() function and updated buildPlanPrompt()"
    - path: "lib/context/execute.ts"
      provides: "files_modified field in cmdInitExecutePhase output"
    - path: "tests/unit/autopilot.test.ts"
      provides: "6 new unit tests for parseWriteIntent"
  key_links: []
---

<objective>
Add write-intent declaration support: (1) update `buildPlanPrompt()` to instruct the planner to include `files_modified:` in PLAN.md frontmatter, (2) add `parseWriteIntent()` to extract the declared file list, and (3) wire the parsed data into `cmdInitExecutePhase` output.

Purpose: REQ-167 — Enable plans to declare which files they intend to modify, providing the foundation for conflict-aware wave scheduling in plan 89-02.
Output: Exported `parseWriteIntent()` function, enhanced planner prompt, and `files_modified` arrays in execution context JSON.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@lib/autopilot.ts
@lib/context/execute.ts
@lib/frontmatter.ts
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task id="1" type="auto">
<name>Add parseWriteIntent function to lib/autopilot.ts</name>
<description>
Create a `parseWriteIntent(frontmatterContent: string): string[]` function that extracts the `files_modified` field from PLAN.md frontmatter content.

Implementation:
1. Accept raw frontmatter string (the content between `---` markers)
2. Parse `files_modified:` field supporting two YAML formats:
   - Dash-list: `files_modified:\n  - lib/foo.ts\n  - lib/bar.ts`
   - Inline array: `files_modified: [lib/foo.ts, lib/bar.ts]`
3. Return `string[]` of file paths. Return `[]` for missing/empty field.
4. Handle edge cases: empty string input, missing field, empty array `[]`, whitespace.

Place near `buildWaves()` (around line 840). Add to `module.exports`.
</description>
<done_criteria>
- Function exists and is exported from lib/autopilot.ts
- Handles both YAML formats correctly
- Returns [] for empty/missing input
- TypeScript compiles cleanly
</done_criteria>
</task>

<task id="2" type="auto">
<name>Update buildPlanPrompt to include files_modified instruction</name>
<description>
Modify `buildPlanPrompt()` at line 380 of `lib/autopilot.ts` to include an instruction telling the planner to declare `files_modified` in PLAN.md frontmatter.

Add to the prompt string (before the closing backtick):
"Ensure each PLAN.md includes a `files_modified:` field in its YAML frontmatter listing the lib/ modules and other files the plan expects to modify."

Keep it concise — one sentence appended to the existing prompt text.
</description>
<done_criteria>
- buildPlanPrompt output contains "files_modified" instruction
- Existing prompt structure preserved
- All existing buildPlanPrompt tests still pass
</done_criteria>
</task>

<task id="3" type="auto">
<name>Wire files_modified into cmdInitExecutePhase output</name>
<description>
In `lib/context/execute.ts`, function `cmdInitExecutePhase` (line 171):

1. After the plan inventory section (~line 266-270), for each plan in `phaseInfo.plans`, read the plan file and extract `files_modified` using the frontmatter parser (from lib/frontmatter.ts `extractFrontmatter`).
2. Add a `plan_files_modified` field to the result JSON: a map of plan ID to its `files_modified` array.
   Example: `{ "89-01": ["lib/autopilot.ts", "lib/context/execute.ts"], "89-02": ["lib/parallel.ts"] }`
3. Import `extractFrontmatter` if not already imported.

This data will be consumed by the wave builder in plan 89-02.
</description>
<done_criteria>
- cmdInitExecutePhase output includes plan_files_modified field
- Each plan's files_modified is correctly extracted from frontmatter
- Plans without files_modified get an empty array
- TypeScript compiles cleanly
</done_criteria>
</task>

<task id="4" type="auto">
<name>Add 6 unit tests for parseWriteIntent</name>
<description>
In `tests/unit/autopilot.test.ts`, add a new `describe('parseWriteIntent')` block after the `buildWaves` tests (after line 855).

Import `parseWriteIntent` in the existing import block (line 17-48).

Tests to write:
1. "parses dash-list format" — `files_modified:\n  - lib/a.ts\n  - lib/b.ts` → `['lib/a.ts', 'lib/b.ts']`
2. "parses inline array format" — `files_modified: [lib/a.ts, lib/b.ts]` → `['lib/a.ts', 'lib/b.ts']`
3. "returns empty array for empty string" — `''` → `[]`
4. "returns empty array when field missing" — frontmatter without files_modified → `[]`
5. "returns empty array for empty inline array" — `files_modified: []` → `[]`
6. "handles single file" — `files_modified:\n  - lib/only.ts` → `['lib/only.ts']`
</description>
<done_criteria>
- All 6 tests pass
- Tests cover both YAML formats and edge cases
- No existing tests broken
</done_criteria>
</task>

</tasks>

<verification>
```bash
npm run build:check
npx jest tests/unit/autopilot.test.ts --no-coverage -t "parseWriteIntent|buildPlanPrompt|buildWaves"
```
</verification>

<success_criteria>
- parseWriteIntent() exported and handles both YAML formats
- buildPlanPrompt() includes files_modified instruction
- cmdInitExecutePhase outputs plan_files_modified map
- 6 new unit tests pass
- All existing tests continue to pass
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create 89-01-SUMMARY.md with accomplishments, task commits, files modified, and any deviations.
</output>
