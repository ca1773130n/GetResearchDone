---
phase: 88-serial-merge-queue-and-conflict-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/autopilot.ts, tests/unit/autopilot.test.ts]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "buildConflictResolvePrompt() receives phaseNum AND cwd, reads the phase's ROADMAP.md goal and PLAN.md summaries, and includes them in the prompt"
    - "The conflict-resolve prompt includes git diff output showing both versions of conflicting files (ours vs theirs)"
    - "The conflict-resolve prompt explicitly instructs to preserve changes from both versions"
    - "When conflict resolution subprocess exits non-zero, the returned PostPipelineResult includes the phase number, conflicting file list, and manual resolution steps"
    - "CLAUDE session env vars are stripped from the conflict-resolve subprocess (inherited from _buildSpawnConfig)"
  artifacts:
    - path: "lib/autopilot.ts"
      provides: "Enhanced buildConflictResolvePrompt with phase context and conflict detail, plus improved failure reporting"
      contains: "buildConflictResolvePrompt"
    - path: "tests/unit/autopilot.test.ts"
      provides: "Tests for enhanced conflict prompt content and halt-on-failure behavior"
      contains: "buildConflictResolvePrompt"
  key_links:
    - from: "lib/autopilot.ts (buildConflictResolvePrompt)"
      to: "lib/autopilot.ts (findPhaseInternal)"
      via: "reads phase directory to find PLAN.md files for plan summaries"
      pattern: "findPhaseInternal"
    - from: "lib/autopilot.ts (rebase step)"
      to: "lib/autopilot.ts (buildConflictResolvePrompt)"
      via: "passes cwd and phaseNum to get enriched prompt"
      pattern: "buildConflictResolvePrompt.*cwd"
---

<objective>
Enhance buildConflictResolvePrompt() with full phase intent context (goal, plan summaries, conflicting file versions) and improve the conflict-halting logic to report actionable information when resolution fails.

Purpose: The current buildConflictResolvePrompt() is a bare stub that only mentions the phase number. REQ-166 requires the subprocess to receive both conflicting file versions, the phase goal, plan summary, and explicit merge-preservation instructions. When resolution fails, the halt message must identify the phase, conflicting files, and manual steps.

Output: Enhanced buildConflictResolvePrompt(phaseNum, cwd) with full context injection, improved PostPipelineResult failure reporting with conflict file details.
</objective>

<execution_context>
@.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.21/references/execute-plan.md
@.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.21/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/autopilot.ts
@lib/utils.ts
@tests/unit/autopilot.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance buildConflictResolvePrompt with phase context and conflict details</name>
  <files>lib/autopilot.ts</files>
  <action>
    1. Change `buildConflictResolvePrompt(phaseNum: string)` signature to `buildConflictResolvePrompt(phaseNum: string, cwd: string, wtPath: string)`:
       - `cwd` is the project root (needed to find phase info via findPhaseInternal)
       - `wtPath` is the worktree path (needed to run git commands to get conflict info)

    2. Inside the function, gather phase context:
       a. Call `findPhaseInternal(cwd, phaseNum)` to get phase info
       b. If found, read the ROADMAP.md section for this phase to extract the Goal line. Use `fs.readFileSync` on `.planning/ROADMAP.md`, find `#### Phase ${phaseNum}:`, extract the `**Goal**:` line.
       c. Read the first PLAN.md's `<objective>` section for a plan summary. Use `phaseInfo.plans[0]` path if available, read it, extract text between `<objective>` and `</objective>` tags.

    3. Gather conflicting file information:
       a. Run `execGit(wtPath, ['diff', '--name-only', '--diff-filter=U'])` to get the list of files with unresolved conflicts.
       b. For each conflicting file (up to 5 to avoid prompt bloat), run:
          - `execGit(wtPath, ['diff', '--', filePath])` to get the diff showing conflict markers (both versions)

    4. Build the enhanced prompt with this structure:
       ```
       You are resolving merge conflicts from rebasing phase {phaseNum}'s branch onto main.

       ## Phase Context

       **Phase Goal:** {goal from ROADMAP.md or "Phase {phaseNum} implementation"}
       **Plan Summary:** {objective text from PLAN.md or "See phase plans for details"}

       ## Conflicting Files

       The following files have conflicts:
       {list of conflicting file paths}

       ## Conflict Diffs

       {For each file: ### {filePath}\n```\n{diff output}\n```}

       ## Instructions

       For each conflicting file:
       1. Examine both the incoming changes (from phase {phaseNum}'s branch) and the changes from main
       2. Resolve by PRESERVING CHANGES FROM BOTH VERSIONS — do not discard either side unless they are truly redundant
       3. The phase's intent was: {goal} — ensure the resolution maintains this intent
       4. After resolving all conflicts, run `git add` on each resolved file
       5. Complete the rebase with `git rebase --continue`

       If a conflict cannot be automatically resolved (e.g., fundamentally incompatible changes), exit with a non-zero status code.
       ```

    5. Update the call site in `runPostPhasePipeline()` (Step 4, around line 486) to pass `cwd` and `wtPath`:
       `buildConflictResolvePrompt(phaseNum, cwd, wtPath)`

    6. Enhance the failure return when conflict resolution fails (around line 490-499):
       - Before returning, gather the conflicting file list: `execGit(wtPath, ['diff', '--name-only', '--diff-filter=U'])`
       - Include in the reason: `conflict resolution failed for files: {fileList}. Manual steps: git checkout {branch}, git rebase main, resolve conflicts manually, git rebase --continue`
       - Add a `conflictFiles` field to the return if useful, or encode it in the reason string for backward compatibility.

    IMPORTANT: All file reads (ROADMAP.md, PLAN.md) must be wrapped in try/catch — if any read fails, use fallback text. The prompt must always be valid even if context gathering partially fails.

    IMPORTANT: The `_buildSpawnConfig` already strips CLAUDE env vars, so no additional work needed for env stripping. The conflict-resolve subprocess goes through `spawnStep` which uses `spawnClaudeAsync` which uses `_buildSpawnConfig`.
  </action>
  <verify>
    npm run build:check (TypeScript compiles)
    npx jest tests/unit/autopilot.test.ts -t "buildConflictResolvePrompt" (Level 2: Proxy)
  </verify>
  <done>
    buildConflictResolvePrompt accepts cwd and wtPath, reads phase goal and plan summary, includes conflicting file diffs, instructs to preserve both sides. Failure reporting includes file list and manual steps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for enhanced conflict prompt and halt behavior</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add tests in a `describe('buildConflictResolvePrompt')` block:

    1. **Includes phase goal from ROADMAP.md**: Set up a temp directory with a `.planning/ROADMAP.md` containing a Phase 88 entry with a Goal line. Call `buildConflictResolvePrompt('88', tmpDir, tmpWtDir)`. Assert the returned prompt contains the goal text.

    2. **Includes plan summary from PLAN.md**: Set up a phase directory with a PLAN.md containing an `<objective>` section. Call the function. Assert the prompt contains the objective text.

    3. **Includes conflicting file list**: Mock `execGit` (or set up actual git conflicts in a temp repo) to return conflict file names. Assert the prompt lists them.

    4. **Graceful fallback on missing context**: Call with a cwd that has no ROADMAP.md. Assert the prompt still contains the phase number, instructions, and does not throw.

    5. **Preserves both versions instruction**: Call the function with any valid setup. Assert the prompt contains "PRESERVING CHANGES FROM BOTH VERSIONS" or equivalent instruction text.

    For tests that need git state, consider mocking `execGit` via jest.mock or setting up minimal temp git repos. Match the existing test patterns in the file.

    Also add a test for the enhanced failure reporting:

    6. **Halt message includes conflicting files and manual steps**: This tests the runPostPhasePipeline integration — mock the rebase to fail, mock conflict resolution to fail, verify the returned PostPipelineResult.reason includes file names and manual steps.
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts -t "buildConflictResolvePrompt" --verbose (Level 2: Proxy — all pass)
  </verify>
  <done>
    6+ tests verifying prompt content (goal, plan summary, file list, both-versions instruction), graceful fallback, and halt message quality. All pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` — TypeScript compiles with no errors
Level 2 (Proxy): `npx jest tests/unit/autopilot.test.ts -t "buildConflictResolvePrompt"` — all tests pass
Level 2 (Proxy): Manual inspection confirms prompt includes phase goal, plan summary, conflict diffs, and both-versions instruction
</verification>

<success_criteria>
- buildConflictResolvePrompt() reads phase goal from ROADMAP.md and plan summary from PLAN.md
- Prompt includes git diff output for conflicting files
- Prompt explicitly instructs to preserve changes from both versions
- Failure on conflict resolution reports phase number, conflicting file names, and manual resolution steps
- All existing tests continue to pass alongside new ones
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.22/phases/88-serial-merge-queue-and-conflict-resolution/88-02-SUMMARY.md`
</output>
