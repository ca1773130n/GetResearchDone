---
phase: 81-mcp-tools-testing-and-integration
plan: 03
type: execute
wave: 2
depends_on: ["81-01", "81-02"]
files_modified:
  - tests/integration/wireup-e2e.test.ts
autonomous: true
verification_level: full

must_haves:
  truths:
    - "Integration test runs full wireup iteration on fixture project with at least 2 known unwired features"
    - "Test validates complete flow: discover -> generate scenarios -> execute -> detect issues -> report"
    - "Test fixture project has synthetic unwired features that the discovery engine reliably finds"
    - "grd_wireup_run MCP tool returns structured JSON with features_discovered, scenarios_run, issues_found fields"
    - "All integration tests pass (npm test zero failures)"
  artifacts:
    - path: "tests/integration/wireup-e2e.test.ts"
      provides: "End-to-end integration test for wireup flow"
      min_lines: 100
  key_links:
    - from: "tests/integration/wireup-e2e.test.ts"
      to: "lib/wireup/index.ts"
      via: "direct module import for orchestrator functions via barrel"
      pattern: "require.*wireup"
    - from: "tests/integration/wireup-e2e.test.ts"
      to: "lib/mcp-server.ts"
      via: "MCP server tool invocation test"
      pattern: "McpServer|handleMessage"
    - from: "tests/integration/wireup-e2e.test.ts"
      to: "bin/grd-tools.ts"
      via: "grd-tools.js wireup run dispatch test"
      pattern: "grd-tools"
---

<objective>
Write an integration test that validates the complete wireup flow end-to-end on a fixture project with known unwired features.

Purpose: Prove the full discover -> generate -> execute -> detect -> report pipeline works as a unit, and that the MCP tool returns correct structured output.
Output: tests/integration/wireup-e2e.test.ts with passing E2E tests.
</objective>

<execution_context>
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/references/execute-plan.md
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/wireup/index.ts
@lib/wireup/orchestrator.ts
@lib/mcp-server.ts
@bin/grd-tools.ts
@tests/integration/evolve-e2e.test.ts
@.planning/milestones/v0.3.13/phases/81-mcp-tools-testing-and-integration/81-01-SUMMARY.md
@.planning/milestones/v0.3.13/phases/81-mcp-tools-testing-and-integration/81-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wireup E2E fixture and full-flow integration test</name>
  <files>tests/integration/wireup-e2e.test.ts</files>
  <action>
    Create tests/integration/wireup-e2e.test.ts following the pattern from evolve-e2e.test.ts.

    1. Add jest.mock for lib/autopilot (spawnClaudeAsync) at the top to prevent real Claude subprocess spawns.

    2. Create a fixture builder function `createWireupFixture(tmpDir)` that builds a project with:
       - .planning/ directory with config.json (balanced ceremony, wireup-compatible)
       - .planning/STATE.md with valid state content
       - At least 2 known unwired features:
         a) A lib/ file (e.g., lib/api-handler.ts) exporting a function that is never imported by any other file in the fixture
         b) A routes/ file (e.g., routes/users.ts) with an endpoint handler that has no corresponding test file
       - A minimal package.json for the fixture project

    3. Write integration test suite:

       **Full Flow Test:**
       - test('runs complete wireup iteration: discover -> generate -> execute -> detect -> report')
         - Call the wireup orchestrator function on the fixture directory
         - Assert discovery found >= 2 unwired features
         - Assert scenarios were generated for each feature
         - Assert scenario execution produced pass/fail results
         - Assert missing connection detection classified issues
         - Assert WIREUP-REPORT.md was created in the wireup directory
         - Assert WIREUP-STATE.json was written with correct fields

       **MCP Tool Integration:**
       - test('grd_wireup_run MCP tool returns structured JSON via McpServer')
         - Create McpServer instance with fixture cwd
         - Send tools/call request for grd_wireup_run
         - Parse response and assert it contains features_discovered, scenarios_run, issues_found fields
         - Verify JSON-RPC response structure matches protocol

       **Discovery Accuracy:**
       - test('discovers the specific unwired features planted in the fixture')
         - Call discovery function directly
         - Assert results include both planted unwired features by file path

       **grd-tools.js Dispatch (covers success criterion 5):**
       - test('grd-tools.js wireup run dispatches to cmdWireupRun and returns structured JSON')
         - Require bin/grd-tools.ts (or invoke via child_process with mocked wireup module)
         - Verify that `wireup run` routing resolves to the cmdWireupRun function
         - Assert the response contains features_discovered, scenarios_run, issues_found fields
         - This validates the full path: grd-tools.js -> routing -> cmdWireupRun -> structured JSON output

    4. Use beforeEach/afterEach for temp directory lifecycle. Track all temp dirs for cleanup.

    5. Mock HTTP requests and CLI execution within scenarios to avoid real network/subprocess calls — the integration test validates the orchestration flow, not actual HTTP servers.
  </action>
  <verify>
    npx jest tests/integration/wireup-e2e.test.ts --verbose 2>&1 | grep -E "PASS|FAIL|Tests:"
    All integration tests pass (Level 2: Proxy)
  </verify>
  <done>
    Integration test validates full wireup flow end-to-end on fixture project.
    MCP tool test confirms grd_wireup_run returns structured JSON with features_discovered, scenarios_run, issues_found.
    npm test passes with zero failures across all test files.
  </done>
</task>

</tasks>

<verification>
Level 2 (Proxy): npx jest tests/integration/wireup-e2e.test.ts --verbose passes all tests.
Level 1 (Sanity): npm test passes with 0 failures across all test suites.
Level 3 (Deferred): Full pipeline validation with real unwired project (requires phases 78-80 complete).
</verification>

<success_criteria>
- tests/integration/wireup-e2e.test.ts exists with 100+ lines
- Full-flow test validates discover -> generate -> execute -> detect -> report sequence
- MCP tool test confirms grd_wireup_run returns structured JSON
- grd-tools.js dispatch test verifies wireup run routing returns structured JSON with features_discovered, scenarios_run, issues_found
- Fixture has at least 2 known unwired features that discovery reliably finds
- npm test passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.13/phases/81-mcp-tools-testing-and-integration/81-03-SUMMARY.md`
</output>
