---
phase: 81-mcp-tools-testing-and-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/wireup.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tests/unit/wireup.test.ts achieves >= 85% line coverage on lib/wireup.ts"
    - "Unit tests cover discovery engine with mock filesystem (no real project needed)"
    - "Unit tests cover scenario generation from discovered features"
    - "Unit tests cover scenario execution with mocked HTTP and CLI calls"
    - "Unit tests cover state read/write/advance round-trip on temp directories"
    - "Unit tests cover missing connection detection and classification"
    - "All tests pass with npm test (zero failures)"
  artifacts:
    - path: "tests/unit/wireup.test.ts"
      provides: "Comprehensive unit tests for lib/wireup.ts"
      min_lines: 200
  key_links:
    - from: "tests/unit/wireup.test.ts"
      to: "lib/wireup.ts"
      via: "direct require of exported functions"
      pattern: "require.*wireup"
---

<objective>
Write comprehensive unit tests for lib/wireup.ts achieving 85%+ line coverage.

Purpose: Validate all wireup functions in isolation — discovery, scenario generation, execution, state management, and missing connection detection — using mocked filesystems and mocked subprocess/HTTP calls.
Output: tests/unit/wireup.test.ts with full coverage.
</objective>

<execution_context>
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/references/execute-plan.md
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/wireup.ts
@tests/unit/evolve.test.ts
@tests/helpers/setup.js
@tests/helpers/fixtures.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for discovery engine and scenario generation</name>
  <files>tests/unit/wireup.test.ts</files>
  <action>
    Create tests/unit/wireup.test.ts following the patterns from evolve.test.ts and mcp-server.test.ts.

    1. At the top, add jest.mock for any modules that would spawn subprocesses (e.g., lib/autopilot if wireup uses spawnClaudeAsync). Must be before any require of lib/wireup.

    2. Import wireup functions directly from lib/wireup.ts (not via CLI).

    3. Create a custom fixture builder function (like evolve.test.ts pattern) that builds a minimal project directory with:
       - .planning/ with config.json and STATE.md
       - Synthetic source files that have known unwired features:
         - An exported function that is never imported elsewhere
         - An API route handler file without corresponding integration test
         - A config option defined but not exposed via CLI

    4. Write describe blocks covering:

       **Discovery Engine:**
       - test('discovers exported-but-uncalled functions from mock filesystem')
       - test('discovers API endpoints without integration tests')
       - test('discovers config options without CLI surface')
       - test('returns empty array for fully-wired project')
       - test('returns structured results with category, file, and suggested action')

       **Scenario Generation:**
       - test('generates scenarios with step_type, parameters, and expected_outcome fields')
       - test('generates HTTP scenarios for API endpoint features')
       - test('generates CLI scenarios for command features')
       - test('handles features with no applicable scenario type')

    5. Use beforeEach/afterEach with temp directory creation and cleanup (follow evolve.test.ts tmpDirs pattern).

    6. Each test should be specific — assert on exact field names and types, not just "result is truthy".
  </action>
  <verify>
    npx jest tests/unit/wireup.test.ts --no-coverage --verbose 2>&1 | grep -E "PASS|FAIL|Tests:"
    All discovery and scenario tests pass (Level 2: Proxy)
  </verify>
  <done>Discovery engine tests pass with mock filesystem. Scenario generation tests validate output structure and field types.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for execution, state management, and connection detection</name>
  <files>tests/unit/wireup.test.ts</files>
  <action>
    Continue in tests/unit/wireup.test.ts, adding describe blocks for remaining functionality:

    **Scenario Execution (mocked):**
    - Mock execFileSync (from child_process) for CLI scenario execution
    - Mock http/https request for HTTP scenario execution
    - test('executes CLI scenario and captures stdout, stderr, exit code')
    - test('executes HTTP scenario and captures status, body, headers')
    - test('reports pass when actual matches expected outcome')
    - test('reports fail when actual differs from expected outcome')
    - test('handles execution timeout gracefully')

    **State Management:**
    - test('readWireupState returns null for non-existent state file')
    - test('writeWireupState creates WIREUP-STATE.json with correct structure')
    - test('readWireupState round-trips with writeWireupState')
    - test('state contains features_discovered, scenarios_generated, scenarios_passed, scenarios_failed, fixes_applied, iteration_history fields')
    - test('advanceWireupState increments iteration and preserves history')

    **Missing Connection Detection:**
    - test('classifies missing-route issues from failed HTTP scenarios')
    - test('classifies missing-import issues from module resolution failures')
    - test('classifies unconnected-handler from route without middleware')
    - test('assigns confidence level (high/medium/low) to each issue')
    - test('returns structured report with issue_type, source_file, target_file, suggested_fix')

    Follow the mocking patterns from evolve.test.ts — use jest.mock at file level for subprocess dependencies, jest.spyOn for specific method overrides within tests.

    Ensure cleanup of all temp directories in afterEach.
  </action>
  <verify>
    npx jest tests/unit/wireup.test.ts --coverage --coverageReporters=text 2>&1 | grep -E "wireup|All files"
    Coverage for lib/wireup.ts should be >= 85% lines (Level 2: Proxy)
  </verify>
  <done>All wireup unit tests pass. lib/wireup.ts coverage >= 85% lines. npm test passes with zero failures.</done>
</task>

</tasks>

<verification>
Level 2 (Proxy): npx jest tests/unit/wireup.test.ts --coverage reports >= 85% line coverage.
Level 1 (Sanity): npm test passes with 0 failures across all test files.
Level 3 (Deferred): Coverage thresholds enforced via jest.config.js (added in Plan 01).
</verification>

<success_criteria>
- tests/unit/wireup.test.ts exists with 200+ lines of tests
- All test suites pass (zero failures)
- lib/wireup.ts line coverage >= 85%
- Tests cover: discovery, scenario generation, execution (mocked), state management, connection detection
- No subprocess leaks (all Claude spawns mocked)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.13/phases/81-mcp-tools-testing-and-integration/81-02-SUMMARY.md`
</output>
