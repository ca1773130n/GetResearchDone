---
phase: 81-mcp-tools-testing-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp-server.ts
  - jest.config.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Five wireup MCP tools registered in COMMAND_DESCRIPTORS: grd_wireup_discover, grd_wireup_run, grd_wireup_state, grd_wireup_scenarios, grd_wireup_report"
    - "Each tool has name, description, params array, and execute function matching evolve tool patterns"
    - "Tool parameter schemas match wireup function signatures (e.g. grd_wireup_run accepts target, dry_run; grd_wireup_discover accepts no args)"
    - "mcp-server.ts imports wireup cmd functions from lib/wireup"
  artifacts:
    - path: "lib/mcp-server.ts"
      provides: "MCP server with wireup tool registrations"
      contains: "grd_wireup_discover"
  key_links:
    - from: "lib/mcp-server.ts"
      to: "lib/wireup.ts"
      via: "require('./wireup') with typed destructuring"
      pattern: "require.*wireup"
---

<objective>
Register five wireup MCP tools in mcp-server.ts following the existing evolve tool patterns.

Purpose: Expose wireup functionality (discover, run, state, scenarios, report) via MCP protocol so agents and grd-tools.js can invoke them.
Output: Updated lib/mcp-server.ts with wireup tool section in COMMAND_DESCRIPTORS.
</objective>

<execution_context>
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/references/execute-plan.md
@/Users/neo/.claude-personal1/plugins/cache/claude-plugin-marketplace/grd/0.3.11/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mcp-server.ts
@lib/wireup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wireup imports and MCP tool registrations to mcp-server.ts</name>
  <files>lib/mcp-server.ts</files>
  <action>
    Follow the exact pattern used for evolve tools (lines ~150-166 for imports, lines ~2480-2570 for COMMAND_DESCRIPTORS entries).

    1. Add typed import block for wireup cmd functions near the evolve imports:
       ```
       const {
         cmdWireupDiscover,
         cmdWireupRun,
         cmdWireupState,
         cmdWireupScenarios,
         cmdWireupReport,
       }: {
         cmdWireupDiscover: (cwd: string, args: string[], raw: boolean) => unknown;
         cmdWireupRun: (cwd: string, args: string[], raw: boolean) => Promise<unknown>;
         cmdWireupState: (cwd: string, args: string[], raw: boolean) => void;
         cmdWireupScenarios: (cwd: string, args: string[], raw: boolean) => void;
         cmdWireupReport: (cwd: string, args: string[], raw: boolean) => void;
       } = require('./wireup');
       ```
       Adjust function signatures to match the actual exports from lib/wireup.ts (check the module first).

    2. Add a `// -- Wireup Tools --` section in COMMAND_DESCRIPTORS after the evolve section. Register five tools:

       - `grd_wireup_discover`: "Discover unwired features in the project via filesystem analysis". No params. Calls cmdWireupDiscover(cwd, [], false).

       - `grd_wireup_run`: "Run a full wireup iteration: discover, generate scenarios, execute, detect issues, report". Params: target (string, optional — focus on specific feature), dry_run (boolean, optional). Calls cmdWireupRun with CLI args built from params.

       - `grd_wireup_state`: "Read the current wireup iteration state (features discovered, scenarios, fixes applied)". No params. Calls cmdWireupState(cwd, [], false).

       - `grd_wireup_scenarios`: "List generated wireup scenarios for discovered features". No params. Calls cmdWireupScenarios(cwd, [], false).

       - `grd_wireup_report`: "Get the latest wireup report with pass/fail results and issue summary". No params. Calls cmdWireupReport(cwd, [], false).

    3. Ensure each tool descriptor follows the exact CommandDescriptor interface: { name, description, params, execute }.
  </action>
  <verify>
    npm run build:check (TypeScript compiles without errors) (Level 1: Sanity)
    Grep lib/mcp-server.ts for all five tool names to confirm registration.
  </verify>
  <done>Five wireup MCP tools registered in COMMAND_DESCRIPTORS with correct parameter schemas and execute functions. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add wireup coverage threshold to jest.config.js</name>
  <files>jest.config.js</files>
  <action>
    Add a per-file coverage threshold entry for `./lib/wireup.ts` in jest.config.js coverageThreshold section:
    ```
    './lib/wireup.ts': { lines: 85, functions: 85, branches: 70 },
    ```
    Place it alphabetically among the existing entries (after `./lib/verify.ts` and before `./lib/worktree.ts`).
    The 85% lines threshold matches REQ-132's requirement.
  </action>
  <verify>
    node -e "const c = require('./jest.config.js'); console.log(c.coverageThreshold['./lib/wireup.ts'])"
    Should output: { lines: 85, functions: 85, branches: 70 } (Level 1: Sanity)
  </verify>
  <done>jest.config.js has wireup.ts coverage threshold at 85% lines, ready to enforce coverage in Plan 02.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): npm run build:check passes — TypeScript sees wireup imports and tool registrations without errors.
Level 1 (Sanity): Grep confirms all five tool names appear in COMMAND_DESCRIPTORS.
Level 1 (Sanity): jest.config.js contains wireup.ts threshold entry.
</verification>

<success_criteria>
- Five wireup MCP tools (grd_wireup_discover, grd_wireup_run, grd_wireup_state, grd_wireup_scenarios, grd_wireup_report) appear in COMMAND_DESCRIPTORS
- Import block for wireup cmd functions exists and is typed
- jest.config.js has coverage threshold for lib/wireup.ts at 85% lines
- npm run build:check passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.13/phases/81-mcp-tools-testing-and-integration/81-01-SUMMARY.md`
</output>
