---
phase: 80-browser-execution-and-auto-fix
plan: 03
type: execute
wave: 2
depends_on: ["80-01", "80-02"]
files_modified:
  - lib/wireup.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "generateWireupReport() writes WIREUP-REPORT.md to .planning/milestones/{milestone}/wireup/ directory"
    - "Report contains: features tested count, scenarios run/passed/failed/skipped, issues found, fixes applied (verified/failed/skipped), remaining unwired features"
    - "Each iteration appends to an ## Iteration History section at the bottom of WIREUP-REPORT.md with timestamp, enabling trend tracking across runs"
    - "Report includes a ## Requires Manual Review section listing medium/low confidence issues with suggested fixes"
    - "Report is valid markdown with consistent heading structure and table formatting"
  artifacts:
    - path: "lib/wireup.ts"
      provides: "Report generation function that writes WIREUP-REPORT.md"
      exports: ["generateWireupReport"]
  key_links:
    - from: "lib/wireup.ts"
      to: "lib/wireup.ts"
      via: "report reads wireup state for iteration data"
      pattern: "readWireupState|WireupState"
    - from: "lib/wireup.ts"
      to: "lib/paths.ts"
      via: "uses milestone-scoped path resolution for wireup directory"
      pattern: "currentMilestone|milestonesDir"
---

<objective>
Implement WIREUP-REPORT.md generation with iteration history and trend tracking.

Purpose: After each wireup iteration, a structured report provides actionable visibility into what was tested, what passed, what failed, what was auto-fixed, and what remains. The appended iteration history enables trend tracking across runs — teams can see whether unwired features are decreasing over time.

Output: `generateWireupReport()` function in `lib/wireup.ts` that writes a markdown report to the wireup directory.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/wireup.ts
@lib/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement generateWireupReport() with iteration history</name>
  <files>lib/wireup.ts</files>
  <action>
    Add the report generation function to `lib/wireup.ts`:

    1. **Define the report data type:**
       ```typescript
       interface WireupReportData {
         milestone: string;
         iteration: number;
         timestamp: string;
         features_tested: number;
         scenarios: {
           total: number;
           passed: number;
           failed: number;
           skipped: number;
         };
         issues_found: MissingConnection[];
         fixes: {
           applied: FixAttempt[];
           verified: number;
           failed: number;
           skipped: number;
         };
         remaining_unwired: string[];
         manual_review: MissingConnection[];
       }
       ```

    2. **`generateWireupReport(cwd, reportData)` function:**
       - **Resolve output path:** Use `lib/paths.ts` to get the milestone directory, then construct `.planning/milestones/{milestone}/wireup/WIREUP-REPORT.md`. Create the wireup directory if it does not exist (`fs.mkdirSync` with `{ recursive: true }`).
       - **Build the report markdown** with these sections:

         ```markdown
         # Wireup Report

         **Milestone:** {milestone}
         **Iteration:** {iteration}
         **Generated:** {timestamp}

         ## Summary

         | Metric | Count |
         |--------|-------|
         | Features Tested | {N} |
         | Scenarios Run | {N} |
         | Scenarios Passed | {N} |
         | Scenarios Failed | {N} |
         | Scenarios Skipped | {N} |
         | Issues Found | {N} |
         | Fixes Applied | {N} |
         | Fixes Verified | {N} |
         | Fixes Failed | {N} |
         | Remaining Unwired | {N} |

         ## Issues Found

         | # | Type | Source | Target | Confidence | Fix Status |
         |---|------|--------|--------|------------|------------|
         (one row per issue)

         ## Fixes Applied

         | # | Issue Type | Description | Status | Re-run Passed |
         |---|------------|-------------|--------|---------------|
         (one row per fix attempt)

         ## Requires Manual Review

         (for each medium/low confidence issue:)
         ### {issue_type}: {source_file} -> {target_file}
         **Confidence:** {confidence}
         **Suggested Fix:** {suggested_fix}

         ## Remaining Unwired Features

         - {feature_1}
         - {feature_2}
         ...
         ```

       - **Append iteration history:** Read the existing WIREUP-REPORT.md if it exists. Extract the `## Iteration History` section if present. Append a new row to the history table:

         ```markdown
         ## Iteration History

         | Iteration | Date | Scenarios | Passed | Failed | Skipped | Issues | Fixes | Verified |
         |-----------|------|-----------|--------|--------|---------|--------|-------|----------|
         (append new row with current iteration data)
         ```

         If the report file already exists, preserve the existing iteration history rows and append the new one. If the file does not exist, create the history table with just the current row.

       - **Write the report:** Overwrite the main report sections (Summary through Remaining Unwired) but preserve and append to the Iteration History section. Use `fs.writeFileSync` to write the complete file.

    3. **`extractIterationHistory(existingContent: string): string` helper:**
       - Parse existing WIREUP-REPORT.md content
       - Find the `## Iteration History` section
       - Extract and return just the table rows (excluding the header row)
       - Return empty string if no history section exists

    4. **`formatReportPath(cwd: string): string` helper:**
       - Use `currentMilestone(cwd)` from `lib/paths.ts` to resolve the milestone
       - Return `path.join(cwd, '.planning', 'milestones', milestone, 'wireup', 'WIREUP-REPORT.md')`

    5. Export `generateWireupReport`, `formatReportPath`, and `WireupReportData` type from the module.

    Use typed require for paths.ts:
    ```typescript
    const { currentMilestone }: { currentMilestone: (cwd: string) => string } = require('./paths');
    ```
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) — type-check passes
    grep -c "generateWireupReport\|formatReportPath\|extractIterationHistory" lib/wireup.ts — all three functions defined
    node -e "const w = require('./lib/wireup'); console.log(typeof w.generateWireupReport)" — prints 'function' (Level 2: Proxy)
  </verify>
  <done>
    generateWireupReport() writes a structured WIREUP-REPORT.md with summary table, issues found, fixes applied, manual review section, remaining unwired features, and appended iteration history for trend tracking. Report path is milestone-scoped via lib/paths.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire report generation into wireup orchestrator flow</name>
  <files>lib/wireup.ts</files>
  <action>
    Connect the report generation to the wireup iteration lifecycle:

    1. **Update `runWireupIteration()` (or the main orchestration function from Phase 79):**
       - After all scenarios are executed and auto-fixes attempted, call `generateWireupReport()` with the collected data
       - Build `WireupReportData` from:
         - `features_tested`: count of distinct features with executed scenarios
         - `scenarios.total/passed/failed/skipped`: aggregate from all scenario results (HTTP, CLI, and browser)
         - `issues_found`: the full list of detected missing connections
         - `fixes.applied/verified/failed/skipped`: from auto-fix results
         - `remaining_unwired`: features that still have at least one failing or skipped scenario
         - `manual_review`: medium/low confidence issues from `partitionByConfidence()`
       - Record the report path in wireup state: `state.last_report_path = reportPath`

    2. **Update wireup state after report generation:**
       - Update `WIREUP-STATE.json` with latest iteration stats:
         - `state.iteration_history.push({ iteration, timestamp, features_tested, scenarios_passed, scenarios_failed, issues_found, fixes_verified })`
       - This mirrors the evolve state `history` array pattern

    3. **Return the report path** from the orchestration function so the calling command can display it to the user.

    Ensure the report is always generated, even if all scenarios passed (a clean report is valuable for tracking).
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) — type-check passes
    grep "generateWireupReport\|last_report_path\|iteration_history" lib/wireup.ts — report wired into orchestrator flow (Level 2: Proxy)
  </verify>
  <done>
    Report generation is integrated into the wireup iteration flow. Every iteration produces a WIREUP-REPORT.md. Iteration stats are persisted to WIREUP-STATE.json for trend tracking. Report path returned to caller.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- TypeScript compiles without errors (`npx tsc --noEmit`)
- generateWireupReport() exists and is exported from lib/wireup.ts
- formatReportPath() returns a path under .planning/milestones/{milestone}/wireup/

Level 2 (Proxy):
- Calling generateWireupReport() with sample data creates WIREUP-REPORT.md on disk with correct heading structure
- Running generateWireupReport() twice appends a second row to the Iteration History table without losing the first row
- Report contains all required sections: Summary, Issues Found, Fixes Applied, Requires Manual Review, Remaining Unwired Features, Iteration History

Level 3 (Deferred):
- Live Playwright MCP scenario execution with real browser (deferred — tracked in ROADMAP.md deferred validations)
</verification>

<success_criteria>
WIREUP-REPORT.md is written to the milestone wireup directory after each iteration. Report includes all metrics (features tested, scenarios run/passed/failed, issues found, fixes applied, remaining unwired). Iteration history is appended for trend tracking. Manual review section surfaces non-auto-fixable issues with suggested fixes.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.13/phases/80-browser-execution-and-auto-fix/80-03-SUMMARY.md`
</output>
