---
phase: 79-wireup-orchestrator-and-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/wireup.md
  - lib/wireup/orchestrator.ts
  - lib/wireup/index.ts
  - lib/context.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "commands/wireup.md exists with valid YAML frontmatter including description and argument-hint fields; --target <feature> optional argument is documented"
    - "The wireup orchestrator function runWireup() calls discoverUnwiredFeatures, generateScenarios, executeScenarios, and detectMissingConnections in sequence and returns a structured WireupResult"
    - "All spawnClaude calls in the wireup orchestrator use SONNET_MODEL constant from lib/wireup/state.ts — no opus model references"
    - "cmdInitWireup context builder exists in lib/context.ts and returns backend, models, config, milestone, and wireup state information"
  artifacts:
    - path: "commands/wireup.md"
      provides: "GRD slash command definition for /grd:wireup"
      contains: "wireup"
    - path: "lib/wireup/orchestrator.ts"
      provides: "Wireup orchestrator with runWireup function"
      contains: "runWireup"
    - path: "lib/wireup/index.ts"
      provides: "Barrel re-export for lib/wireup/"
      contains: "module.exports"
  key_links:
    - from: "commands/wireup.md"
      to: "lib/wireup/orchestrator.ts"
      via: "CLI invocation through grd-tools.js routing"
      pattern: "grd-tools.js.*wireup"
    - from: "lib/wireup/orchestrator.ts"
      to: "lib/wireup/index.ts"
      via: "barrel re-export"
      pattern: "orchestratorModule"
    - from: "lib/wireup/orchestrator.ts"
      to: "lib/autopilot.ts"
      via: "imports spawnClaudeAsync for subagent spawns"
      pattern: "spawnClaudeAsync"
---

<objective>
Register the `/grd:wireup` slash command and implement the wireup orchestrator that ties Phase 78's discovery/scenario/state modules into a single end-to-end flow.

Purpose: This is the central coordination layer — it makes the wireup feature usable as a single command. Without it, Phase 78's modules are disconnected utilities.
Output: `commands/wireup.md` slash command, `lib/wireup/orchestrator.ts` with `runWireup()`, updated barrel export in `lib/wireup/index.ts`, and `cmdInitWireup` in `lib/context.ts`.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/evolve.md
@lib/evolve/orchestrator.ts
@lib/evolve/index.ts
@lib/evolve/cli.ts
@lib/context.ts
@lib/autopilot.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /grd:wireup slash command and cmdInitWireup context builder</name>
  <files>commands/wireup.md, lib/context.ts</files>
  <action>
    Create `commands/wireup.md` following the exact pattern of `commands/evolve.md`:
    - YAML frontmatter with `description` and `argument-hint` fields
    - `description`: "Discover unwired features and test integration wiring end-to-end"
    - `argument-hint`: "[--target <feature>] [--dry-run]"
    - Body: invoke `node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js wireup run $ARGUMENTS`
    - Document the orchestration flow: discover -> generate scenarios -> execute HTTP/CLI scenarios -> detect missing connections -> report pass/fail summary
    - Document `--target <feature>` to focus wireup on a specific feature area
    - Document `--dry-run` to discover and generate scenarios without executing
    - Add the same `run_in_background` guidance as evolve.md (this is a long-running command)
    - Note that all operations use sonnet model ceiling (no opus-class models)

    Add `cmdInitWireup` function to `lib/context.ts` following the `cmdInitEvolve` pattern:
    - Accept `(cwd: string, raw: boolean)` parameters
    - Import `readWireupState` from `../wireup` (or `./wireup`)
    - Return JSON context bundle with: backend, capabilities, sonnet_model, wireup_state (from readWireupState), milestone info, wireup_dir path, config flags
    - Register in the exports of context.ts

    Also add routing for `wireup` in `bin/grd-tools.ts` (or the ROUTE_DESCRIPTORS table) so `grd-tools.js wireup run` dispatches to the wireup CLI handler. Follow the `evolve run` routing pattern.
  </action>
  <verify>
    ```bash
    # Verify commands/wireup.md exists with valid frontmatter
    head -5 commands/wireup.md | grep -q 'description:'
    grep -q '\-\-target' commands/wireup.md
    grep -q 'wireup run' commands/wireup.md

    # Verify cmdInitWireup exists in context.ts
    grep -q 'cmdInitWireup' lib/context.ts

    # Verify routing exists
    grep -q 'wireup' bin/grd-tools.ts || grep -q 'wireup' bin/grd-tools.js
    ```
    All commands exit 0. (Level 2: Proxy)
  </verify>
  <done>commands/wireup.md exists with valid frontmatter, --target documented, cmdInitWireup registered in context.ts, and wireup routing added to grd-tools.</done>
</task>

<task type="auto">
  <name>Task 2: Implement wireup orchestrator and barrel re-export</name>
  <files>lib/wireup/orchestrator.ts, lib/wireup/index.ts</files>
  <action>
    Create `lib/wireup/orchestrator.ts` following `lib/evolve/orchestrator.ts` structure:

    1. Import types from `./types` (WireupOptions, WireupResult, WireupScenario, WireupFeature, MissingConnection, ScenarioResult)
    2. Import from `./state`: SONNET_MODEL, readWireupState, writeWireupState
    3. Import from `./discovery`: discoverUnwiredFeatures (Phase 78)
    4. Import from `./scenarios`: generateScenarios (Phase 78)
    5. Import from `../autopilot`: spawnClaudeAsync
    6. Import from `../utils`: loadConfig, output, error

    Implement `runWireup(cwd: string, options: WireupOptions): Promise<WireupResult>`:
    - Step 1: Read wireup state via readWireupState(cwd)
    - Step 2: Call discoverUnwiredFeatures(cwd) to get features list
    - Step 3: If options.target, filter features to matching target
    - Step 4: Call generateScenarios(cwd, features) to get scenarios
    - Step 5: If options.dryRun, return early with discovery+scenario results
    - Step 6: Execute HTTP/CLI scenarios (delegate to executeScenarios from plan 79-02 — for now, import and call it; it will be implemented in plan 02)
    - Step 7: Call detectMissingConnections(cwd, failedResults) (delegate to plan 79-03 — import stub)
    - Step 8: Update wireup state via writeWireupState with iteration results
    - Step 9: Return WireupResult with features_discovered, scenarios_run, scenarios_passed, scenarios_failed, issues_found, pass_fail_summary

    CRITICAL: All spawnClaudeAsync calls MUST use `model: SONNET_MODEL`. Import SONNET_MODEL from `./state` — do NOT hardcode 'sonnet' or use any opus model constant.

    Implement `cmdWireup(cwd: string, args: string[], raw: boolean): Promise<void>`:
    - Parse CLI args: --target, --dry-run, --timeout, --max-turns
    - Call runWireup(cwd, parsedOptions)
    - Output result via output()

    Update `lib/wireup/index.ts` barrel to re-export:
    - From orchestrator: runWireup, cmdWireup
    - From state: all existing exports (readWireupState, writeWireupState, SONNET_MODEL, etc.)
    - From discovery: discoverUnwiredFeatures
    - From scenarios: generateScenarios
    - Ensure all Phase 78 symbols plus new orchestrator symbols are accessible

    Follow code style: 'use strict' at top, CommonJS require with typed destructuring, no `any` types, prefix unused args with `_`.
  </action>
  <verify>
    ```bash
    # Verify orchestrator exists with key functions
    grep -q 'runWireup' lib/wireup/orchestrator.ts
    grep -q 'cmdWireup' lib/wireup/orchestrator.ts
    grep -q 'SONNET_MODEL' lib/wireup/orchestrator.ts

    # Verify no opus model references
    if grep -i 'opus' lib/wireup/orchestrator.ts; then echo "FAIL: opus model reference found"; exit 1; fi

    # Verify barrel re-exports
    grep -q 'runWireup' lib/wireup/index.ts
    grep -q 'cmdWireup' lib/wireup/index.ts

    # Type check
    npx tsc --noEmit lib/wireup/orchestrator.ts 2>&1 | head -20
    ```
    All grep checks pass; no opus references; type check has no errors. (Level 2: Proxy)
  </verify>
  <done>lib/wireup/orchestrator.ts implements runWireup() orchestrating discover->generate->execute->detect->report flow using SONNET_MODEL exclusively. lib/wireup/index.ts barrel updated with all public symbols.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- commands/wireup.md has valid YAML frontmatter
- lib/wireup/orchestrator.ts compiles without errors
- No opus model references in wireup orchestrator

Level 2 (Proxy):
- cmdInitWireup returns a valid context bundle
- runWireup function signature accepts WireupOptions and returns WireupResult
- Barrel export in index.ts re-exports all public symbols
- grd-tools.js wireup routing dispatches correctly
</verification>

<success_criteria>
- /grd:wireup slash command registered with --target documented
- Orchestrator calls discover -> generate -> execute -> detect in sequence
- All model references use SONNET_MODEL constant
- cmdInitWireup context builder follows existing cmdInitEvolve pattern
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.13/phases/79-wireup-orchestrator-and-execution/79-01-SUMMARY.md`
</output>
