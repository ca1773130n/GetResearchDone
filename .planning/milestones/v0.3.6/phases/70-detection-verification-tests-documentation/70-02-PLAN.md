---
phase: 70-detection-verification-tests-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/backend.ts
  - CLAUDE.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "detectBackend() env var checks confirmed valid: CLAUDE_CODE_* for claude, CODEX_HOME for codex, GEMINI_CLI_HOME for gemini, OPENCODE for opencode"
    - "Filesystem clue paths confirmed valid: .claude-plugin/plugin.json, .codex/config.toml, .gemini/settings.json, opencode.json"
    - "CODEX_THREAD_ID handling reviewed — removed or kept based on current Codex CLI documentation"
    - "CLAUDE.md agent model profiles table uses abstract tiers (opus/sonnet/haiku) — no concrete model name changes needed"
    - "CLAUDE.md or lib/backend.ts JSDoc updated to note OpenCode is actively maintained (not archived)"
    - "No references to deprecated 'gemini-3-pro' or 'gpt-5.3-codex' remain outside test migration comments"
  artifacts:
    - path: "lib/backend.ts"
      provides: "Verified detection logic with updated comments"
    - path: "CLAUDE.md"
      provides: "Updated documentation reflecting current ecosystem"
  key_links:
    - "detectBackend() env var checks <-> current CLI env var patterns"
    - "filesystem clue paths <-> current CLI config file locations"
---

<objective>
Verify backend detection env vars and filesystem clue paths are current, and update documentation to reflect the March 2026 backend ecosystem.

Purpose: Detection logic must correctly identify each backend. Documentation must not reference stale model names or incorrectly describe OpenCode as archived. The agent model profiles table in CLAUDE.md uses abstract tiers and needs no model name changes, but ecosystem notes should be current.

Output: Verified lib/backend.ts detection logic with updated comments; updated CLAUDE.md documentation.
</objective>

<execution_context>
@.planning/milestones/v0.3.6/research/LANDSCAPE.md
</execution_context>

<context>
@lib/backend.ts
@CLAUDE.md
@.planning/milestones/v0.3.6/research/LANDSCAPE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update detection env vars and filesystem clues</name>
  <files>lib/backend.ts</files>
  <action>
    Review detectBackend() in lib/backend.ts against LANDSCAPE.md findings:

    1. **CLAUDE_CODE_* prefix** — LANDSCAPE.md confirms STILL VALID. No change needed.

    2. **CODEX_HOME** — LANDSCAPE.md confirms STILL VALID. No change needed.

    3. **CODEX_THREAD_ID** — LANDSCAPE.md says "may be deprecated (no docs mention)".
       Decision: KEEP for backward compatibility. Codex users with older versions may still have it set.
       Add a code comment noting it may be deprecated but is kept for backward compat.

    4. **GEMINI_CLI_HOME** — LANDSCAPE.md says "NEEDS VERIFICATION".
       The env var is standard for Gemini CLI config directory. Keep as-is.
       If Phase 69 already verified and kept it, no change needed.

    5. **OPENCODE** — LANDSCAPE.md confirms STILL VALID. No change needed.
       LANDSCAPE.md also notes `OPENCODE_PID` env var — do NOT add this for detection
       (it's a process management var, not a presence indicator).

    6. **Filesystem clues** — All four paths appear current per LANDSCAPE.md:
       - `.claude-plugin/plugin.json` — valid (Claude Code plugin manifest)
       - `.codex/config.toml` — valid (Codex config)
       - `.gemini/settings.json` — valid (Gemini settings)
       - `opencode.json` — valid (OpenCode project config)

    7. **Update JSDoc/comments in lib/backend.ts:**
       - Update the module-level JSDoc to note current versions (March 2026)
       - Add comment noting OpenCode is actively maintained under anomalyco/opencode
       - Add comment about CODEX_THREAD_ID backward compat status

    IMPORTANT: Do NOT change any detection logic or constants — only comments/JSDoc.
    The constants were already updated by Phase 69. This task only verifies and documents.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts -t "detectBackend" --verbose 2>&1 | tail -20
    Check: All detectBackend tests still pass after comment-only changes (Level 1: Sanity)
  </verify>
  <done>Detection env vars and filesystem paths verified current; JSDoc comments updated with ecosystem notes.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md documentation</name>
  <files>CLAUDE.md</files>
  <action>
    Review CLAUDE.md for backend-related documentation that needs updating:

    1. **Agent Model Profiles table** (~line 269):
       The table uses abstract tier names (opus, sonnet, haiku) — NOT concrete model names.
       No changes needed to this table.

    2. **Search CLAUDE.md for concrete model name references:**
       - Grep for 'gemini-3-pro', 'gpt-5.3', 'claude-opus-4-5', 'claude-sonnet-4-5'
       - If found, update to current names
       - If not found, no changes needed

    3. **Search CLAUDE.md for "OpenCode" or "opencode" references:**
       - If any text implies OpenCode is archived/deprecated, update to note it is actively maintained
       - If no such references exist, no changes needed

    4. **Search for any stale backend references in other docs:**
       - Grep docs/ directory for stale model names
       - Grep .planning/ for stale model names in non-phase files
       - Note findings but only fix CLAUDE.md in this task

    IMPORTANT: Only modify CLAUDE.md. Minimal changes — do not rewrite sections unnecessarily.
  </action>
  <verify>
    grep -n 'gemini-3-pro\|gpt-5.3-codex\|claude-opus-4-5\|claude-sonnet-4-5' CLAUDE.md || echo "No stale references found"
    Check: No stale model names in CLAUDE.md (Level 1: Sanity)
  </verify>
  <done>CLAUDE.md verified clean of stale model references; OpenCode status accurate; agent profiles table unchanged (already uses abstract tiers).</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- npx jest tests/unit/backend.test.ts passes (detection tests unaffected by comment changes)
- npm run lint passes on lib/backend.ts
- No stale model name references in CLAUDE.md
- No detection logic changes — only comments and documentation
</verification>

<success_criteria>
- detectBackend() env var checks verified against March 2026 ecosystem
- Filesystem clue paths verified current
- CODEX_THREAD_ID backward compat documented in code comment
- CLAUDE.md free of stale model names
- OpenCode correctly described as actively maintained
- npm test passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.6/phases/70-detection-verification-tests-documentation/70-02-SUMMARY.md`
</output>
