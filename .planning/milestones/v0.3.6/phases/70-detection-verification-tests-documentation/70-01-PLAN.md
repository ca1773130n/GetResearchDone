---
phase: 70-detection-verification-tests-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/backend.test.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Phase 69 SUMMARY files verified to exist before making any changes"
    - "All DEFAULT_BACKEND_MODELS assertions use updated model names from Phase 69 (gpt-5.4, gemini-3.1-pro, gemini-3.1-flash-lite, claude-opus-4-6, etc.)"
    - "All BACKEND_CAPABILITIES assertions reflect Phase 69 updates (gemini subagents: true, gemini parallel: true)"
    - "resolveBackendModel test.each table uses Phase 69 model names for all 12 backend/tier combinations"
    - "getBackendCapabilities exact-match assertions updated for codex and gemini capability changes"
    - "parseOpenCodeModels tests use 4-6 model names (claude-opus-4-6 etc.) instead of 4-5"
    - "Deprecated model migration tests added if Phase 69 created migration code, or documented as N/A"
    - "npx jest tests/unit/backend.test.ts passes with 0 failures"
  artifacts:
    - path: "tests/unit/backend.test.ts"
      provides: "Updated backend test suite matching Phase 69 constants"
  key_links:
    - "test assertions <-> DEFAULT_BACKEND_MODELS constants in lib/backend.ts"
    - "test assertions <-> BACKEND_CAPABILITIES constants in lib/backend.ts"
---

<objective>
Update all test assertions in backend.test.ts to match the new model mappings and capability flags set by Phase 69.

Purpose: Tests must reflect the current backend constants to pass. Phase 69 updates model names (codex: gpt-5.4, gemini: gemini-3.1-pro, opencode: claude-opus-4-6) and capability flags (gemini subagents: true, gemini parallel: true). Every hardcoded assertion in the test file must be updated to match.

Output: Updated tests/unit/backend.test.ts with all assertions matching Phase 69 constants.
</objective>

<execution_context>
@.planning/milestones/v0.3.6/research/LANDSCAPE.md
</execution_context>

<context>
@lib/backend.ts
@tests/unit/backend.test.ts
@.planning/milestones/v0.3.6/research/LANDSCAPE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update model mapping assertions</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Read the CURRENT values of DEFAULT_BACKEND_MODELS from lib/backend.ts (Phase 69 will have already updated these).
    Then update tests/unit/backend.test.ts to match. Specific locations:

    1. **DEFAULT_BACKEND_MODELS describe block** (~lines 120-142):
       - codex test: Update expected values to match current codex entry in DEFAULT_BACKEND_MODELS
       - gemini test: Update expected values to match current gemini entry in DEFAULT_BACKEND_MODELS
       - opencode test: Update expected values to match current opencode entry in DEFAULT_BACKEND_MODELS
       - Update test description strings to reflect new model names

    2. **resolveBackendModel test.each table** (~lines 411-426):
       - Update all 12 rows to match current DEFAULT_BACKEND_MODELS values
       - Rows for codex (opus, sonnet, haiku), gemini (opus, sonnet, haiku), opencode (opus, sonnet, haiku) all need updating

    3. **Config override fallback tests** (~lines 439-459):
       - Any test that asserts a default codex/gemini/opencode model name as fallback must use the new names

    4. **resolveBackendModel with cwd param** (~line 707):
       - Update backward-compatible test assertion for opencode opus default

    5. **parseOpenCodeModels tests** (~lines 552-636):
       - Update all 'anthropic/claude-opus-4-5' references to 'anthropic/claude-opus-4-6'
       - Update all 'anthropic/claude-sonnet-4-5' references to 'anthropic/claude-sonnet-4-6'
       - Leave claude-haiku-4-5 as-is (haiku 4.5 is still current per LANDSCAPE.md)

    PRE-CHECK: Confirm Phase 69 has been executed by checking for SUMMARY files in
    .planning/milestones/v0.3.6/phases/69-model-mappings-capabilities-deprecation/.
    If no summaries exist, STOP — Phase 69 must complete first.

    IMPORTANT: Read lib/backend.ts first to confirm the exact Phase 69 values before updating tests.
    Do NOT guess the values -- they must exactly match what Phase 69 set.
    If Phase 69's Plan 02 already updated some test assertions, verify they match current constants
    rather than blindly re-writing them.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts --verbose 2>&1 | head -80
    Check: All DEFAULT_BACKEND_MODELS and resolveBackendModel tests pass (Level 1: Sanity)
  </verify>
  <done>All model mapping assertions match Phase 69 constants; no test failures related to model names.</done>
</task>

<task type="auto">
  <name>Task 2: Update capability assertions</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Read the CURRENT values of BACKEND_CAPABILITIES from lib/backend.ts (Phase 69 will have already updated these).
    Then update tests/unit/backend.test.ts to match. Specific locations:

    1. **BACKEND_CAPABILITIES describe block** (~lines 181-211):
       - codex test (~line 181): Update expected capability object to match current BACKEND_CAPABILITIES.codex
       - gemini test (~line 192): Update expected capability object to match current BACKEND_CAPABILITIES.gemini
       - Test description strings: Update to reflect new capability values

    2. **getBackendCapabilities describe block** (~lines 496-515):
       - codex test: Update exact-match assertion
       - gemini test: Update exact-match assertion

    IMPORTANT: Read lib/backend.ts first to confirm the exact Phase 69 capability values.
    Per LANDSCAPE.md, expected changes:
    - gemini: subagents 'experimental' -> true, parallel false -> true
    - codex: hooks false -> true (if Phase 69 changed this)
    But ONLY change what Phase 69 actually changed in lib/backend.ts.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts --verbose 2>&1 | grep -E "(PASS|FAIL|capabilities)"
    Check: All BACKEND_CAPABILITIES and getBackendCapabilities tests pass (Level 1: Sanity)
  </verify>
  <done>All capability assertions match Phase 69 constants; codex and gemini capability tests updated.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite sanity check</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    After Tasks 1 and 2, run the full test suite to verify no regressions:

    1. Run: `npx jest tests/unit/backend.test.ts --verbose`
    2. Verify: 0 failures, all tests pass
    3. Run: `npm test` to verify no cascading failures in other test files
    4. Grep the codebase for stale model references:
       - Search for 'gemini-3-pro' (should only appear in migration comments if any)
       - Search for 'gpt-5.3-codex' (should only appear in migration comments if any)
       - Search for 'claude-opus-4-5' (should only appear in migration comments or parseOpenCodeModels tests)
    5. If stale references found outside tests, note them but do NOT modify files outside tests/unit/backend.test.ts (those are other plans' responsibility)
  </action>
  <verify>
    npm test 2>&1 | tail -5
    Check: Full suite passes with 0 failures (Level 1: Sanity)
  </verify>
  <done>Full test suite passes; no stale model references remain in test file except intentional migration comments. Any stale references found outside backend.test.ts are listed in SUMMARY.md for follow-up, not fixed in this plan.</done>
</task>

<task type="auto">
  <name>Task 4: Add new tests for deprecated model handling</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Check if Phase 69 added any deprecated model handling code (e.g., migration helpers,
    deprecation warnings, backward-compat shims). Look for:
    - New exported functions in lib/backend.ts related to deprecation/migration
    - Any console.warn or deprecation notice logic
    - Migration path from old model names to new ones

    If Phase 69 added such code, add tests for it:
    1. Test that deprecated model names trigger appropriate warnings/fallbacks
    2. Test the migration path (old name → new name resolution)

    If Phase 69 did NOT add any deprecation handling code (i.e., it was a straight constant update
    with no migration path), then this task is a no-op — document in SUMMARY.md that no
    deprecation code was added by Phase 69, so no deprecation tests are needed.

    REQ-90 calls for "Add test for deprecated model migration path" — but this only applies
    if Phase 69 created such a path. Verify and act accordingly.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts --verbose 2>&1 | tail -10
    Check: All tests pass including any new deprecation tests (Level 1: Sanity)
  </verify>
  <done>Deprecated model handling tests added if Phase 69 created migration code; documented as N/A if no migration path exists.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- npx jest tests/unit/backend.test.ts passes with 0 failures
- npm test passes with 0 failures across full suite
- No hardcoded references to pre-Phase-69 model names in backend.test.ts (except migration comments)
</verification>

<success_criteria>
- All 40+ backend test assertions use Phase 69 model names and capability flags
- Full test suite (npm test) passes with 0 failures
- No stale model name references in tests/unit/backend.test.ts
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.6/phases/70-detection-verification-tests-documentation/70-01-SUMMARY.md`
</output>
