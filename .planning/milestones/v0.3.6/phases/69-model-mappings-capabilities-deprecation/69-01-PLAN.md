---
wave: 1
depends_on: []
files_modified:
  - lib/backend.ts
autonomous: true
verification_level: sanity
estimated_effort: small
---

# Plan 69-01: Update Model Mappings and Capability Constants

## Objective

Update `DEFAULT_BACKEND_MODELS` and `BACKEND_CAPABILITIES` constants in `lib/backend.ts` to reflect the current AI CLI ecosystem as of March 2026. This covers REQ-82, REQ-83, REQ-84, REQ-85, REQ-86, and REQ-88.

## Tasks

<task id="1" action="edit" file="lib/backend.ts">
Update `DEFAULT_BACKEND_MODELS.gemini` to replace deprecated models:
- Change `opus` from `'gemini-3-pro'` to `'gemini-3.1-pro'` (REQ-82: gemini-3-pro deprecated, shutdown March 9 2026)
- Keep `sonnet` as `'gemini-3-flash'` (unchanged)
- Change `haiku` from `'gemini-2.5-flash'` to `'gemini-3.1-flash-lite'` (REQ-82: new replacement model)
</task>

<task id="2" action="edit" file="lib/backend.ts">
Update `DEFAULT_BACKEND_MODELS.codex` to add GPT-5.4:
- Change `opus` from `'gpt-5.3-codex'` to `'gpt-5.4'` (REQ-83: GPT-5.4 is now the most capable model)
- Keep `sonnet` as `'gpt-5.3-codex-spark'` (unchanged)
- Keep `haiku` as `'gpt-5.3-codex-spark'` (unchanged)
</task>

<task id="3" action="edit" file="lib/backend.ts">
Update `DEFAULT_BACKEND_MODELS.opencode` to current Claude model versions:
- Change `opus` from `'anthropic/claude-opus-4-5'` to `'anthropic/claude-opus-4-6'` (REQ-84, REQ-88)
- Change `sonnet` from `'anthropic/claude-sonnet-4-5'` to `'anthropic/claude-sonnet-4-6'` (REQ-84, REQ-88)
- Keep `haiku` as `'anthropic/claude-haiku-4-5'` (unchanged — Haiku 4.5 is still current)
</task>

<task id="4" action="edit" file="lib/backend.ts">
Update `BACKEND_CAPABILITIES.gemini`:
- Change `subagents` from `'experimental'` to `true` (REQ-85: Gemini subagents are now GA)
- Change `parallel` from `false` to `true` (REQ-85: contiguous parallel admission now available)
</task>

<task id="5" action="edit" file="lib/backend.ts">
Update `BACKEND_CAPABILITIES.codex`:
- Change `hooks` from `false` to `true` (REQ-86: Codex now has plugin/hooks system)
- Change `teams` from `false` to `true` (REQ-86: Codex now has multi-agent /agent command)
</task>

## Verification

- `npx jest tests/unit/backend.test.ts` — will FAIL after this plan (expected: test assertions still reference old values). Plan 69-02 fixes tests.
- Manually verify each constant matches the success criteria from the roadmap.
- `npm run build:check` — TypeScript type-check passes (no type changes, only string literal values).

## must_haves

artifacts: []
key_links:
  - lib/backend.ts
invariants:
  - DEFAULT_BACKEND_MODELS.gemini.opus equals 'gemini-3.1-pro'
  - DEFAULT_BACKEND_MODELS.gemini.haiku equals 'gemini-3.1-flash-lite'
  - DEFAULT_BACKEND_MODELS.codex.opus equals 'gpt-5.4'
  - DEFAULT_BACKEND_MODELS.opencode.opus equals 'anthropic/claude-opus-4-6'
  - DEFAULT_BACKEND_MODELS.opencode.sonnet equals 'anthropic/claude-sonnet-4-6'
  - BACKEND_CAPABILITIES.gemini.subagents equals true
  - BACKEND_CAPABILITIES.gemini.parallel equals true
  - BACKEND_CAPABILITIES.codex.hooks equals true
  - BACKEND_CAPABILITIES.codex.teams equals true
