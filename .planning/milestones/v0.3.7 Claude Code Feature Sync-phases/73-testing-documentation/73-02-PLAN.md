---
phase: 73-testing-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/context.test.ts
  - CLAUDE.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Init context tests verify effort_level field in JSON output when backend supports effort"
    - "Init context tests verify cron_available field in JSON output"
    - "CLAUDE.md agent model profiles table includes an Effort column with per-profile effort levels"
    - "CLAUDE.md documents TeammateIdle, TaskCompleted, and InstructionsLoaded hook events"
    - "CLAUDE.md includes a memory model section clarifying STATE.md vs auto-memory interaction"
    - "npm test passes with 0 failures"
  artifacts:
    - path: "tests/unit/context.test.ts"
      provides: "Updated context tests with effort_level and cron_available coverage"
      contains: "effort_level"
    - path: "CLAUDE.md"
      provides: "Updated documentation with effort levels, hook events, and memory model"
      contains: "effort"
  key_links:
    - from: "tests/unit/context.test.ts"
      to: "lib/context/base.ts"
      via: "require and test assertions"
      pattern: "effort_level"
    - from: "CLAUDE.md"
      to: "lib/backend.ts"
      via: "documents capability flags and effort levels"
      pattern: "effort"
---

<objective>
Add init context tests for effort_level and cron_available fields, and update CLAUDE.md with effort level documentation, new hook events, and memory model clarification.

Purpose: Satisfy REQ-99 (Auto-Memory Awareness), REQ-100 (Feature Sync Tests — context portion), and REQ-101 (Feature Sync Documentation).
Output: Updated `tests/unit/context.test.ts` and `CLAUDE.md`.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/context/base.ts
@lib/context/execute.ts
@tests/unit/context.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add init context tests for effort_level and cron_available</name>
  <files>tests/unit/context.test.ts</files>
  <action>
    In `tests/unit/context.test.ts`, add tests verifying the new fields added by Phases 71-72 in init context output:

    1. Add a test that verifies `effort_level` is present in the init JSON output when the backend is claude (which supports effort). The value should be a string ('high', 'medium', or 'low') determined by the agent role and model profile.

    2. Add a test that verifies `effort_level` is absent or null when the backend does not support effort (e.g., codex).

    3. Add a test that verifies `cron_available` boolean field is present in the autopilot init context JSON output. For claude backend, this should be `true`. For other backends, `false`.

    4. Add a test that verifies `effort_level` value changes based on model profile:
       - quality profile + planner role -> 'high'
       - budget profile + verifier role -> 'low'

    Follow existing test patterns in the file (use the same mock/setup patterns, same describe block organization).
  </action>
  <verify>
    npx jest tests/unit/context.test.ts (Level 1: Sanity)
    All new tests pass. No regressions.
  </verify>
  <done>
    effort_level and cron_available fields tested in init context output.
    Tests cover backend-conditional behavior (present for claude, absent for others).
    Profile-dependent effort level values verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with effort levels, hook events, and memory model</name>
  <files>CLAUDE.md</files>
  <action>
    Make three documentation updates to CLAUDE.md:

    1. **Agent Model Profiles table — add Effort column:**
       Update the existing table in the "## Agent Model Profiles" section. Add an "Effort" column showing the effort level per profile. The table should become:

       ```
       | Agent | Quality | Balanced | Budget | Effort (Quality) | Effort (Balanced) | Effort (Budget) |
       |-------|---------|----------|--------|-------------------|-------------------|-----------------|
       | grd-planner | opus | opus | sonnet | high | high | medium |
       | grd-executor | opus | sonnet | sonnet | high | medium | medium |
       | grd-surveyor | opus | sonnet | sonnet | high | medium | medium |
       | grd-deep-diver | opus | sonnet | haiku | high | medium | low |
       | grd-eval-planner | opus | opus | sonnet | high | high | medium |
       | grd-product-owner | opus | opus | sonnet | high | high | medium |
       | grd-code-reviewer | opus | sonnet | haiku | high | medium | low |
       | grd-verifier | sonnet | sonnet | haiku | medium | low | low |
       ```

    2. **Document new hook events:**
       In the "## Configuration" section, after the existing `.planning/config.json` controls list, add a new subsection:

       ```
       ## Hook Events

       GRD registers hooks in `.claude-plugin/plugin.json` for Claude Code lifecycle events:

       | Event | When | GRD Usage |
       |-------|------|-----------|
       | SessionStart | Claude Code session begins | Verify .planning/ exists |
       | WorktreeCreate | Native worktree created | Copy .planning/ to worktree |
       | WorktreeRemove | Native worktree removed | Clean up worktree state |
       | TeammateIdle | Teammate has no pending work | Available for task routing |
       | TaskCompleted | A task finishes execution | Progress tracking, next-step routing |
       | InstructionsLoaded | Instructions/CLAUDE.md loaded | Context verification |
       ```

    3. **Add memory model section:**
       After the "## Autonomous Mode (YOLO)" section, add:

       ```
       ## Memory Model

       GRD uses a dual-memory architecture:

       - **STATE.md** (structured, persistent) — Project decisions, metrics, deferred validations, phase progress, and accumulated context. Read/written by GRD tools. Source of truth for project state across sessions.
       - **Auto-memory** (Claude Code native, session-level) — Claude Code's `/memory` command (v2.1.59+) manages session-level context automatically. Stores user preferences, workflow patterns, and frequently-used context.

       **Interaction model:** STATE.md and auto-memory are complementary, not competing. STATE.md handles project-specific structured data that must survive across milestones. Auto-memory handles ephemeral session preferences. GRD never reads or writes auto-memory directly — it operates exclusively through STATE.md and .planning/ files.
       ```

    Preserve all existing content. Only add/modify the specific sections described above.
  </action>
  <verify>
    - Read CLAUDE.md and verify the three sections exist
    - Verify the agent model profiles table has effort columns
    - Verify hook events table includes TeammateIdle, TaskCompleted, InstructionsLoaded
    - Verify memory model section exists
    (Level 1: Sanity)
  </verify>
  <done>
    CLAUDE.md updated with:
    - Effort columns in agent model profiles table
    - Hook events documentation section
    - Memory model section clarifying STATE.md vs auto-memory
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/context.test.ts` passes with 0 failures
- CLAUDE.md contains "Effort" in the agent model profiles table
- CLAUDE.md contains "TeammateIdle" in hook events documentation
- CLAUDE.md contains "Memory Model" section heading
- `npm test` passes with 0 failures across the full test suite
</verification>

<success_criteria>
1. Init context tests cover effort_level (conditional on backend) and cron_available fields
2. CLAUDE.md agent model profiles table has effort columns
3. CLAUDE.md documents all 3 new hook events
4. CLAUDE.md has memory model section explaining STATE.md vs auto-memory
5. Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.7/phases/73-testing-documentation/73-02-SUMMARY.md`
</output>
