---
phase: 72-hook-events-tool-updates
plan: 03
type: execute
wave: 2
depends_on: ['72-02']
files_modified: []
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Grep for ${CLAUDE_PLUGIN_ROOT}/commands/ inside commands/ returns 0 results, proving no same-directory command-to-command references exist"
    - "Grep for ${CLAUDE_PLUGIN_ROOT}/agents/ inside agents/ returns 0 results, proving no same-directory agent-to-agent references exist"
    - "All ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js references remain unchanged (grd-tools lives in bin/, not the skill directory)"
    - "All cross-directory references (commands/ -> agents/, -> references/, -> templates/) remain unchanged"
    - "No CLAUDE_SKILL_DIR migration is needed — the zero-result audit greps are the explicit deliverable proving this"
  artifacts: []
  key_links: []
---

<objective>
Verify via grep audit that no ${CLAUDE_PLUGIN_ROOT} references need migration to ${CLAUDE_SKILL_DIR}. This plan produces no file changes — the deliverable is the audit results themselves, which prove zero same-directory references exist in commands/ or agents/.

Purpose: Claude Code v2.1.69 introduced ${CLAUDE_SKILL_DIR} for same-directory references. This audit confirms that all current ${CLAUDE_PLUGIN_ROOT} usages are cross-directory (bin/, agents/ from commands/, references/, templates/) and therefore must remain unchanged. The zero-migration outcome is the explicit deliverable.

Output: Audit grep results documented in SUMMARY.md confirming no migration needed.

Requirements: REQ-97
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@commands/execute-phase.md
@commands/plan-phase.md
@commands/new-project.md
@agents/grd-executor.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify audit results — no migrations needed</name>
  <files>commands/execute-phase.md, commands/plan-phase.md, commands/new-project.md</files>
  <action>
    Perform a verification pass to confirm the audit results:

    1. Grep all files in `commands/` for `${CLAUDE_PLUGIN_ROOT}/commands/` — should return 0 results (no same-directory command-to-command references exist).

    2. Grep all files in `agents/` for `${CLAUDE_PLUGIN_ROOT}/agents/` — should return 0 results (no same-directory agent-to-agent references exist).

    3. Grep all files in `commands/` for `${CLAUDE_PLUGIN_ROOT}/agents/` — should return results (these are valid cross-directory references that must stay as ${CLAUDE_PLUGIN_ROOT}).

    4. Grep all files for `${CLAUDE_PLUGIN_ROOT}/bin/` — should return many results (these must stay as ${CLAUDE_PLUGIN_ROOT}).

    Document the verification results in the SUMMARY.md. No code changes are needed from this task — it is a verification-only task confirming the audit.
  </action>
  <verify>
    Confirm grep for ${CLAUDE_PLUGIN_ROOT}/commands/ in commands/ returns 0 results.
    Confirm grep for ${CLAUDE_PLUGIN_ROOT}/agents/ in agents/ returns 0 results.
    These zero-result greps prove no same-directory references exist, validating the decision not to migrate any references.
  </verify>
  <done>Audit greps confirm 0 same-directory references in commands/ and agents/, proving no CLAUDE_SKILL_DIR migration is needed</done>
</task>

</tasks>

<verification_criteria>
- [ ] Grep for ${CLAUDE_PLUGIN_ROOT}/commands/ in commands/ returns 0 results
- [ ] Grep for ${CLAUDE_PLUGIN_ROOT}/agents/ in agents/ returns 0 results
- [ ] All ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js references confirmed unchanged
- [ ] All cross-directory references confirmed unchanged
- [ ] Audit results documented in SUMMARY.md
</verification_criteria>
