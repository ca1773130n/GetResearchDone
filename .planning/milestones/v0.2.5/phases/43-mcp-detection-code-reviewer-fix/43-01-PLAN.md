---
phase: 43-mcp-detection-code-reviewer-fix
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/backend.js
  - lib/context.js
  - agents/grd-code-reviewer.md
  - tests/unit/backend.test.js
  - tests/unit/context.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "detectWebMcp() returns true when MCP_CHROME_DEVTOOLS or equivalent env var / config entry is present, false otherwise"
    - "cmdInitExecutePhase JSON output includes webmcp_available boolean field"
    - "cmdInitPlanPhase JSON output includes webmcp_available boolean field"
    - "cmdInitVerifyWork JSON output includes webmcp_available boolean field"
    - "When webmcp_available is false, webmcp_skip_reason is set to a human-readable string explaining why"
    - "grd-code-reviewer agent prompt explicitly excludes VERIFICATION.md from artifact validation"
    - "Code review during execute-phase does not produce blocker for missing VERIFICATION.md"
    - "All existing tests continue to pass (no regressions)"
  artifacts:
    - path: "lib/backend.js"
      provides: "detectWebMcp function for Chrome DevTools MCP availability"
      contains: "detectWebMcp"
    - path: "lib/context.js"
      provides: "webmcp_available field in init JSON outputs"
      contains: "webmcp_available"
    - path: "agents/grd-code-reviewer.md"
      provides: "VERIFICATION.md exclusion instruction"
      contains: "VERIFICATION.md"
    - path: "tests/unit/backend.test.js"
      provides: "Tests for detectWebMcp function"
      contains: "detectWebMcp"
    - path: "tests/unit/context.test.js"
      provides: "Tests for webmcp_available in init outputs"
      contains: "webmcp_available"
  key_links:
    - from: "lib/context.js"
      to: "lib/backend.js"
      via: "import detectWebMcp"
      pattern: "detectWebMcp"
    - from: "lib/context.js"
      to: "lib/backend.js"
      via: "call in cmdInitExecutePhase"
      pattern: "webmcp_available.*detectWebMcp"
---

<objective>
Add MCP availability detection to init JSON outputs and fix code reviewer false blocker on VERIFICATION.md.

Purpose: Phase 44 (WebMCP Workflow Integration) needs to know whether Chrome DevTools MCP is available so execute-phase, verify-phase, and eval-planner can conditionally invoke WebMCP tools. The code reviewer fix prevents false blockers that halt execution when VERIFICATION.md does not yet exist (it is created by verify-phase, which runs after code review).

Output: Updated lib/backend.js with detectWebMcp(), updated lib/context.js with webmcp_available in init outputs, fixed grd-code-reviewer.md prompt, and comprehensive tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@lib/backend.js
@lib/context.js
@agents/grd-code-reviewer.md
@tests/unit/backend.test.js
@tests/unit/context.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detectWebMcp to backend.js and integrate into init workflows</name>
  <files>lib/backend.js, lib/context.js, tests/unit/backend.test.js, tests/unit/context.test.js</files>
  <action>
    **1. Add `detectWebMcp()` to `lib/backend.js`:**

    Create a new exported function `detectWebMcp(cwd)` that detects Chrome DevTools MCP availability using a detection waterfall (same pattern as `detectBackend`):

    a. **Config check:** Read `.planning/config.json` — if `webmcp.enabled` is explicitly `true` or `false`, return that value. This allows users to override detection.

    b. **Environment variable check:** Check for `CHROME_DEVTOOLS_MCP` or `WEBMCP_AVAILABLE` env vars. If either is truthy (`"true"`, `"1"`), return `{ available: true, source: "env" }`. If either is explicitly `"false"` or `"0"`, return `{ available: false, source: "env", reason: "Disabled via environment variable" }`.

    c. **Claude Code MCP settings check:** Look for the Chrome DevTools MCP server configuration. Check if the file `~/.claude.json` (user home) contains an `mcpServers` key with a server entry whose name matches `/chrome|devtools|playwright|browser/i`. If found, return `{ available: true, source: "mcp-config" }`.

    d. **Default:** Return `{ available: false, source: "default", reason: "Chrome DevTools MCP not detected in config, environment, or MCP server settings" }`.

    The function signature:
    ```js
    function detectWebMcp(cwd) {
      // Returns: { available: boolean, source: string, reason?: string }
    }
    ```

    Export `detectWebMcp` from the module alongside existing exports.

    **2. Integrate into `cmdInitExecutePhase` in `lib/context.js`:**

    Import `detectWebMcp` from `./backend` (add to the existing destructured import).

    In the `result` object of `cmdInitExecutePhase` (after the `ceremony_level` field), add:
    ```js
    // WebMCP availability (REQ-96)
    webmcp_available: detectWebMcp(cwd).available,
    webmcp_skip_reason: detectWebMcp(cwd).available ? null : detectWebMcp(cwd).reason,
    ```

    Optimize to avoid calling detectWebMcp three times — store result in a local variable:
    ```js
    const webmcp = detectWebMcp(cwd);
    // ... then in result object:
    webmcp_available: webmcp.available,
    webmcp_skip_reason: webmcp.available ? null : webmcp.reason,
    ```

    **3. Also integrate into `cmdInitPlanPhase` and `cmdInitVerifyWork`:**

    These are the other init workflows that Phase 44 agents consume. Add the same `webmcp_available` and `webmcp_skip_reason` fields using the same pattern. The `cmdInitPlanPhase` result object is built similarly to `cmdInitExecutePhase` — add the webmcp fields in the same position (after ceremony_level or at the end of the result object).

    For `cmdInitVerifyWork`, add the fields to provide webmcp status to the grd-verifier agent.

    **4. Add tests to `tests/unit/backend.test.js`:**

    Add a new `describe('detectWebMcp')` block with these test cases:
    - Returns `{ available: false }` when no config, no env vars, no MCP settings
    - Returns `{ available: true, source: "config" }` when `webmcp.enabled: true` in config.json
    - Returns `{ available: false, source: "config" }` when `webmcp.enabled: false` in config.json
    - Returns `{ available: true, source: "env" }` when `CHROME_DEVTOOLS_MCP=true` env var is set
    - Returns `{ available: true, source: "env" }` when `WEBMCP_AVAILABLE=1` env var is set
    - Returns `{ available: false }` with reason when nothing detected

    Mock `fs.readFileSync` for config and `~/.claude.json` reads. Mock `process.env` for env var tests. Follow the existing test patterns in the file (use `jest.spyOn` and `beforeEach`/`afterEach` for cleanup).

    **5. Add tests to `tests/unit/context.test.js`:**

    Add test cases in the existing `cmdInitExecutePhase` describe block (or create a new sub-describe):
    - `webmcp_available` field is present in output (boolean)
    - `webmcp_skip_reason` is null when webmcp is available
    - `webmcp_skip_reason` is a non-empty string when webmcp is not available

    Mock `detectWebMcp` via `jest.mock('./backend')` or by setting up the test environment so detection returns a predictable result.

    **IMPORTANT:** Do NOT break any existing tests. The `detectWebMcp` function defaults to `{ available: false }` which means existing test suites that don't set up MCP configuration will continue to pass with `webmcp_available: false` in their outputs.
  </action>
  <verify>
    npx jest tests/unit/backend.test.js --verbose (Level 1: Sanity)
    npx jest tests/unit/context.test.js --verbose (Level 1: Sanity)
    npm test -- --silent 2>&1 | tail -5 (full suite passes)
  </verify>
  <done>
    detectWebMcp() exported from lib/backend.js with config/env/mcp-settings detection waterfall.
    cmdInitExecutePhase, cmdInitPlanPhase, and cmdInitVerifyWork all include webmcp_available (boolean) and webmcp_skip_reason (string|null) in their JSON output.
    New tests pass. All existing tests pass with no regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix grd-code-reviewer VERIFICATION.md false blocker</name>
  <files>agents/grd-code-reviewer.md</files>
  <action>
    **1. Add VERIFICATION.md exclusion to the grd-code-reviewer agent prompt:**

    In `agents/grd-code-reviewer.md`, add a new section inside `<review_flow>` after the `<step name="load_context">` step (before `<step name="stage_1_spec_compliance">`). Add an explicit exclusion rule:

    ```markdown
    <step name="artifact_exclusions" priority="high">
    ## Artifact Exclusions

    The following files are created by LATER workflow steps and MUST NOT be flagged as missing during code review:

    - **VERIFICATION.md** — Created by `grd-verifier` during the `verify_phase_goal` step, which runs AFTER code review. Missing VERIFICATION.md is NEVER a blocker or warning.
    - **EVAL-RESULTS.md** — Created by the eval report step, which runs AFTER code review.

    **Rule:** When checking plan alignment or artifact existence, skip any artifact path matching `*-VERIFICATION.md` or `*VERIFICATION.md` or `*-EVAL-RESULTS.md`. Do not report these as findings at any severity level.
    </step>
    ```

    **2. Also add a guard in the Stage 1 spec compliance section (1.1 Plan Alignment):**

    After the existing "Cross-reference: every plan task should have a corresponding commit" bullet, add:

    ```markdown
    - **Exclude post-review artifacts:** Do NOT flag VERIFICATION.md, EVAL-RESULTS.md, or REVIEW.md files as missing. These are created by workflow steps that run after code review.
    ```

    **3. Verify the change is self-contained:**

    This is a prompt-only change — no JavaScript code changes needed. The grd-code-reviewer is an LLM agent whose behavior is governed by its markdown prompt. Adding explicit exclusion instructions is the correct fix pattern for this class of false positive.

    **IMPORTANT:** Preserve all existing content in the file. Only ADD the new exclusion sections. Do not modify or remove any existing review logic.
  </action>
  <verify>
    Read agents/grd-code-reviewer.md and confirm the exclusion section exists (Level 1: Sanity)
    grep -c "VERIFICATION.md" agents/grd-code-reviewer.md should return >= 3 (the original mention plus new exclusions)
  </verify>
  <done>
    grd-code-reviewer.md contains explicit VERIFICATION.md exclusion in artifact_exclusions step and plan alignment guard.
    No existing review logic removed or modified.
    Code review of a phase that has not yet run verify-phase will not produce a blocker for missing VERIFICATION.md.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/backend.test.js` passes with new detectWebMcp tests
- `npx jest tests/unit/context.test.js` passes with new webmcp_available tests
- `npm test` full suite passes (no regressions)
- `node bin/grd-tools.js init execute-phase 43 2>/dev/null | grep webmcp` shows webmcp_available field
- `grep "VERIFICATION.md" agents/grd-code-reviewer.md` shows exclusion text

Level 2 (Proxy): N/A for this phase (no proxy metrics apply)

Level 3 (Deferred): Full WebMCP integration testing deferred to Phase 44 (requires actual Chrome DevTools MCP server running)
</verification>

<success_criteria>
1. `cmdInitExecutePhase` JSON output includes `webmcp_available` boolean field
2. `cmdInitPlanPhase` and `cmdInitVerifyWork` also include `webmcp_available`
3. When Chrome DevTools MCP is not available, `webmcp_skip_reason` provides clear explanation
4. grd-code-reviewer prompt explicitly excludes VERIFICATION.md from artifact validation
5. All 1,679+ existing tests pass with no regressions
6. New tests added for detectWebMcp and webmcp_available in init outputs
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.5/phases/43-mcp-detection-code-reviewer-fix/43-01-SUMMARY.md`
</output>
