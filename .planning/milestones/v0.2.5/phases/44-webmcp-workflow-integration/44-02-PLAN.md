---
phase: 44-webmcp-workflow-integration
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/grd-verifier.md
  - agents/grd-eval-planner.md
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "grd-verifier.md contains a new WebMCP verification step that discovers tools via hive_list_registered_tools and calls them"
    - "The verifier WebMCP step is guarded by a conditional checking webmcp_available — when false, the step is skipped with a note in VERIFICATION.md"
    - "The verifier calls generic tools (hive_get_health_status, hive_check_console_errors, hive_get_page_info) and any page-specific tools discovered"
    - "VERIFICATION.md output format includes a '## WebMCP Verification' section with tool call results"
    - "grd-eval-planner.md contains a new step that generates useWebMcpTool() call definitions when a phase modifies frontend views"
    - "The eval-planner WebMCP step is guarded by a conditional checking webmcp_available — when false, the step is skipped"
    - "EVAL.md output format includes a '## WebMCP Tool Definitions' section with useWebMcpTool() call definitions"
    - "All existing content in both agent files is preserved — changes are purely additive"
  artifacts:
    - path: "agents/grd-verifier.md"
      provides: "Verifier agent with WebMCP tool discovery and invocation"
      contains: "hive_list_registered_tools"
    - path: "agents/grd-eval-planner.md"
      provides: "Eval planner agent with WebMCP tool definition generation"
      contains: "useWebMcpTool"
  key_links:
    - from: "agents/grd-verifier.md"
      to: "Chrome DevTools MCP tools"
      via: "hive_list_registered_tools discovery then tool invocation"
      pattern: "hive_list_registered_tools"
    - from: "agents/grd-verifier.md"
      to: "VERIFICATION.md output"
      via: "WebMCP Verification section in output template"
      pattern: "WebMCP Verification"
    - from: "agents/grd-eval-planner.md"
      to: "EVAL.md output"
      via: "WebMCP Tool Definitions section in output template"
      pattern: "WebMCP Tool Definitions"
    - from: "agents/grd-eval-planner.md"
      to: "agents/grd-verifier.md"
      via: "useWebMcpTool() definitions consumed by verifier to know which page-specific tools to expect"
      pattern: "useWebMcpTool"
---

<objective>
Add WebMCP tool discovery and invocation to grd-verifier, and WebMCP tool definition generation to grd-eval-planner.

Purpose: The verifier needs to validate that web applications are healthy after phase execution by discovering and calling WebMCP tools. The eval planner needs to specify which page-specific WebMCP tools the verifier should expect to find for frontend-modifying phases. Together, these close the loop: eval planner defines expected tools, verifier discovers and calls them, results appear in VERIFICATION.md.

Output: Updated agents/grd-verifier.md with WebMCP verification step; updated agents/grd-eval-planner.md with WebMCP tool definition generation step.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@agents/grd-verifier.md
@agents/grd-eval-planner.md
@.planning/milestones/v0.2.5/phases/43-mcp-detection-code-reviewer-fix/43-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebMCP verification step to grd-verifier.md</name>
  <files>agents/grd-verifier.md</files>
  <action>
    This is a MARKDOWN PROMPT modification, not JavaScript code. The grd-verifier.md file is an LLM agent instruction file.

    **1. Add a new Step 5b: WebMCP Verification inside `<verification_process>` section:**

    Insert after "## Step 5: Level 3 -- Deferred Verification Tracking" and before "## Step 6: Experiment Verification". This places it after all standard tiered checks but before experiment and anti-pattern checks.

    Add the following step:

    ```markdown
    ## Step 5b: WebMCP Verification (if webmcp_available)

    **Skip condition:** If `webmcp_available` is not `true` (from init JSON or EVAL.md context), skip this step entirely. Include a note in VERIFICATION.md: "WebMCP verification skipped — MCP not available ({webmcp_skip_reason})."

    **When enabled:**

    **5b-1. Discover registered tools:**

    Call `hive_list_registered_tools` to get the list of all registered WebMCP tools for the current application.

    Parse the response to identify:
    - **Generic tools:** `hive_get_health_status`, `hive_check_console_errors`, `hive_get_page_info` (always expected)
    - **Page-specific tools:** Any tools beyond the generic set (these may be defined in EVAL.md via `useWebMcpTool()` definitions from grd-eval-planner)

    **5b-2. Call generic health checks:**

    ```
    hive_get_health_status → Verify backend responding
    hive_check_console_errors → Verify no JS errors
    hive_get_page_info → Verify app rendering
    ```

    Record each result with status (PASS/FAIL) and details.

    **5b-3. Call page-specific tools (if found):**

    If EVAL.md contains `useWebMcpTool()` definitions, match them against the discovered tool list:
    - For each defined tool that exists in the registered list: call it and record the result
    - For each defined tool that does NOT exist in the registered list: record as "NOT REGISTERED — tool not found"

    If no page-specific tools are defined in EVAL.md, note: "No page-specific tools defined in EVAL.md."

    **5b-4. Record results for VERIFICATION.md:**

    Store results in a structured format for inclusion in the WebMCP Verification section of the output.
    ```

    **2. Add WebMCP section to the VERIFICATION.md output template:**

    In the `<output>` section where the VERIFICATION.md structure is defined, add a new section between "## Experiment Verification" and "## Requirements Coverage":

    ```markdown
    ## WebMCP Verification

    {If webmcp_available:}

    ### Tool Discovery

    | Tool | Type | Registered | Status |
    |------|------|------------|--------|
    | hive_get_health_status | generic | Yes | PASS/FAIL |
    | hive_check_console_errors | generic | Yes | PASS/FAIL |
    | hive_get_page_info | generic | Yes | PASS/FAIL |
    | {page_specific_tool} | page-specific | Yes/No | PASS/FAIL/NOT REGISTERED |

    ### Health Check Results

    | Check | Status | Details |
    |-------|--------|---------|
    | Backend health | PASS | {response summary} |
    | Console errors | PASS | No new errors |
    | Page rendering | PASS | {page info summary} |

    ### Page-Specific Tool Results

    | Tool | Expected (from EVAL.md) | Result | Notes |
    |------|------------------------|--------|-------|
    | {tool} | {expected behavior} | {actual result} | |

    {If webmcp NOT available:}

    WebMCP verification skipped — MCP not available ({reason}).
    ```

    **3. Add webmcp_available to the verifier's context loading:**

    In "## Step 1: Load Context", add a note that `webmcp_available` is available from the init JSON passed by the orchestrator (from `cmdInitVerifyWork` output, which Phase 43 already added).

    **CRITICAL PRESERVATION RULES:**
    - Do NOT remove or modify any existing steps (Steps 0-11)
    - Add Step 5b to slot between existing Steps 5 and 6
    - Do NOT change any existing output format sections
    - Add the WebMCP section to the output template additively
    - Preserve all existing content verbatim
  </action>
  <verify>
    Read agents/grd-verifier.md and confirm:
    - grep "Step 5b\|WebMCP Verification" agents/grd-verifier.md (Level 1: Sanity)
    - grep "hive_list_registered_tools" agents/grd-verifier.md confirms tool discovery
    - grep "hive_get_health_status" agents/grd-verifier.md confirms generic health checks
    - grep "useWebMcpTool" agents/grd-verifier.md confirms page-specific tool matching
    - grep "webmcp_available" agents/grd-verifier.md confirms conditional guard
    - The output template includes a "## WebMCP Verification" section
    - All existing steps (0-11) are preserved
  </verify>
  <done>
    agents/grd-verifier.md contains:
    - Step 5b: WebMCP Verification with conditional guard on webmcp_available
    - Tool discovery via hive_list_registered_tools
    - Generic health check calls (3 tools)
    - Page-specific tool matching against EVAL.md definitions
    - WebMCP Verification section in VERIFICATION.md output template
    All existing content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add WebMCP tool definition generation to grd-eval-planner.md</name>
  <files>agents/grd-eval-planner.md</files>
  <action>
    This is a MARKDOWN PROMPT modification, not JavaScript code. The grd-eval-planner.md file is an LLM agent instruction file.

    **1. Add a new step `design_webmcp_tools` in `<execution_flow>` section:**

    Insert after `<step name="design_ablation_plan">` and before `<step name="write_eval_md">`. This positions it as the last design step before writing the output.

    ```markdown
    <step name="design_webmcp_tools" condition="webmcp_available=true AND phase modifies frontend views">
    Design WebMCP tool definitions for frontend-facing phases.

    **Skip condition:** If `webmcp_available` is not `true` (from init JSON context) OR the phase does not modify frontend views (no HTML, JSX, TSX, Vue, Svelte, CSS, or frontend route files in the plan's files_modified), skip this step entirely.

    **Frontend detection heuristic:**
    Check plan `files_modified` for patterns indicating frontend work:
    - File extensions: `.html`, `.jsx`, `.tsx`, `.vue`, `.svelte`, `.css`, `.scss`
    - Path patterns: `src/pages/`, `src/views/`, `src/components/`, `app/`, `pages/`, `routes/`
    - Keywords in plan objectives: "UI", "frontend", "page", "view", "component", "dashboard", "layout"

    **When enabled, generate `useWebMcpTool()` definitions:**

    **Generic checks (ALWAYS include when WebMCP is enabled):**

    ```yaml
    webmcp_tools:
      generic:
        - name: hive_get_health_status
          purpose: "Verify backend is responding after frontend changes"
          expected: "status: healthy"

        - name: hive_check_console_errors
          purpose: "Verify no new JavaScript errors from frontend changes"
          expected: "No new errors since phase start"

        - name: hive_get_page_info
          purpose: "Verify app renders correctly after changes"
          expected: "Page loads with expected content"
    ```

    **Page-specific tools (generate based on what the phase modifies):**

    For each frontend view/page modified by the phase, define a page-specific tool:

    ```yaml
      page_specific:
        - name: "{tool_name}"
          purpose: "{what this tool checks on the specific page}"
          page: "{URL path or page identifier}"
          expected: "{expected behavior/content}"
          useWebMcpTool_call: |
            useWebMcpTool("{tool_name}", {
              url: "{page_url}",
              checks: ["{check_1}", "{check_2}"]
            })
    ```

    Generate tool names following the convention: `hive_check_{page_slug}_{aspect}` (e.g., `hive_check_dashboard_layout`, `hive_check_settings_form_validation`).

    **If the phase modifies frontend but no specific pages can be identified** (e.g., shared CSS, base layout), only include generic checks and note: "Page-specific tools not applicable — changes affect shared layout/styling."
    </step>
    ```

    **2. Add WebMCP Tool Definitions section to the EVAL.md output template:**

    In the `<output_format>` section, after the "## Ablation Plan" section and before the "## Baselines" section, add:

    ```markdown
    ## WebMCP Tool Definitions

    {If webmcp_available AND frontend phase:}

    **Purpose:** Define WebMCP tools the grd-verifier should use to validate frontend health after phase execution.

    ### Generic Checks

    | Tool | Purpose | Expected |
    |------|---------|----------|
    | hive_get_health_status | Backend health | status: healthy |
    | hive_check_console_errors | No JS errors | No new errors |
    | hive_get_page_info | App renders | Page loads with content |

    ### Page-Specific Tools

    | Tool | Page | Purpose | Expected |
    |------|------|---------|----------|
    | {tool_name} | {page} | {purpose} | {expected} |

    ### useWebMcpTool() Definitions

    ```js
    // Generic health checks
    useWebMcpTool("hive_get_health_status", {})
    useWebMcpTool("hive_check_console_errors", { since: "phase_start" })
    useWebMcpTool("hive_get_page_info", {})

    // Page-specific checks
    useWebMcpTool("{tool_name}", {
      url: "{page_url}",
      checks: ["{check_1}", "{check_2}"]
    })
    ```

    {If webmcp NOT available:}

    WebMCP tool definitions skipped — MCP not available.

    {If not a frontend phase:}

    WebMCP tool definitions skipped — phase does not modify frontend views.
    ```

    **3. Update the `determine_verification_levels` step to mention WebMCP:**

    In the existing `<step name="determine_verification_levels">`, add to the decision tree a note that WebMCP checks are an additional verification dimension when available, not a replacement for tiered verification.

    **CRITICAL PRESERVATION RULES:**
    - Do NOT remove or modify any existing steps
    - Add design_webmcp_tools step between design_ablation_plan and write_eval_md
    - Do NOT change any existing output format sections
    - Add the WebMCP section to the EVAL.md template additively
    - Preserve all existing content verbatim
  </action>
  <verify>
    Read agents/grd-eval-planner.md and confirm:
    - grep "design_webmcp_tools\|WebMCP Tool Definitions" agents/grd-eval-planner.md (Level 1: Sanity)
    - grep "useWebMcpTool" agents/grd-eval-planner.md confirms tool call definitions
    - grep "hive_get_health_status" agents/grd-eval-planner.md confirms generic checks
    - grep "webmcp_available" agents/grd-eval-planner.md confirms conditional guard
    - grep "frontend.*detection\|Frontend detection" agents/grd-eval-planner.md confirms heuristic for identifying frontend phases
    - The EVAL.md output template includes "## WebMCP Tool Definitions" section
    - All existing steps are preserved
  </verify>
  <done>
    agents/grd-eval-planner.md contains:
    - design_webmcp_tools step with conditional guard on webmcp_available AND frontend detection
    - Generic tool definitions (3 health checks)
    - Page-specific tool definition generation based on files_modified
    - useWebMcpTool() call syntax examples
    - WebMCP Tool Definitions section in EVAL.md output template
    All existing content preserved.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- Both agents/grd-verifier.md and agents/grd-eval-planner.md are valid markdown
- grd-verifier.md contains "hive_list_registered_tools", "WebMCP Verification", "webmcp_available"
- grd-eval-planner.md contains "useWebMcpTool", "WebMCP Tool Definitions", "webmcp_available"
- All existing steps in both files preserved
- No existing content removed

Level 2 (Proxy):
- Manual read-through confirms verifier step is correctly positioned (after tiered checks, before experiment verification)
- Manual read-through confirms eval planner step is correctly positioned (after ablation design, before write_eval_md)
- The verifier references EVAL.md useWebMcpTool() definitions for page-specific tool matching
- The eval planner's frontend detection heuristic uses sensible file extension and path patterns
- Both modifications include webmcp_available conditional guards per success criteria 5

Level 3 (Deferred): Runtime validation when agents are actually spawned with Chrome DevTools MCP active — deferred to manual testing
</verification>

<success_criteria>
1. grd-verifier.md contains WebMCP verification step (Step 5b) with tool discovery and invocation
2. VERIFICATION.md output template includes WebMCP Verification section
3. grd-eval-planner.md contains design_webmcp_tools step with frontend detection heuristic
4. EVAL.md output template includes WebMCP Tool Definitions section with useWebMcpTool() syntax
5. Both agent modifications include webmcp_available conditional guards
6. The verifier consumes EVAL.md useWebMcpTool() definitions to know which page-specific tools to expect
7. All existing content in both agent files preserved
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.5/phases/44-webmcp-workflow-integration/44-02-SUMMARY.md`
</output>
