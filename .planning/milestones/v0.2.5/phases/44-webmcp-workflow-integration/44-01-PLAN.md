---
phase: 44-webmcp-workflow-integration
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/execute-phase.md
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "execute-phase.md contains a WebMCP sanity check step that runs after each plan's spot-check (step 4) and before code review"
    - "The WebMCP sanity check step is guarded by a conditional that checks webmcp_available from init JSON — when false, checks are skipped with a log message"
    - "When webmcp_available is true, three tool calls are made: hive_get_health_status, hive_check_console_errors, hive_get_page_info"
    - "First failure of any WebMCP check triggers exactly one retry of that specific check"
    - "Second consecutive failure of the same check halts execution with a clear error identifying which check failed and what the error was"
    - "The WebMCP sanity check step exists in BOTH execute_waves (standard) and execute_waves_teams flow variants"
    - "All existing execute-phase.md content is preserved — the change is purely additive"
  artifacts:
    - path: "commands/execute-phase.md"
      provides: "Execute-phase command with WebMCP sanity checks after each plan execution"
      contains: "webmcp_sanity_check"
  key_links:
    - from: "commands/execute-phase.md"
      to: "init JSON (webmcp_available)"
      via: "conditional guard reading webmcp_available from INIT"
      pattern: "webmcp_available"
    - from: "commands/execute-phase.md"
      to: "Chrome DevTools MCP tools"
      via: "hive_get_health_status, hive_check_console_errors, hive_get_page_info tool calls"
      pattern: "hive_get_health_status"
---

<objective>
Add WebMCP sanity checks to execute-phase after each plan execution, with retry/halt logic.

Purpose: When Chrome DevTools MCP is available, execute-phase should verify that the web application is still healthy after each plan runs. This catches regressions early (broken backend, JS errors, render failures) before proceeding to the next wave. The checks are skipped gracefully when MCP is not available, preserving backward compatibility.

Output: Updated commands/execute-phase.md with a new WebMCP sanity check step in both standard and teams execution flows.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@commands/execute-phase.md
@.planning/milestones/v0.2.5/phases/43-mcp-detection-code-reviewer-fix/43-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebMCP sanity check step to both execute_waves flow variants in execute-phase.md</name>
  <files>commands/execute-phase.md</files>
  <action>
    This is a MARKDOWN PROMPT modification, not JavaScript code. The execute-phase.md file is an LLM agent instruction file that governs how the execute-phase orchestrator behaves.

    **1. Add WebMCP sanity check to `execute_waves` (standard flow, ~line 238-356):**

    After step 4 ("Report completion -- spot-check claims first") and BEFORE step 5 ("Handle failures"), insert a new step 4b:

    ```markdown
    4b. **WebMCP sanity checks (if `webmcp_available=true` from init):**

        **Skip condition:** If `webmcp_available` is `false` in the INIT JSON, skip this step entirely. Log: "WebMCP not available — skipping health checks (reason: {webmcp_skip_reason})"

        **When enabled, run three health checks for each completed plan in this wave:**

        ```
        ## WebMCP Health Check — Plan {PLAN_ID}

        Check 1: hive_get_health_status
        → Verify backend is responding. Expected: status field indicates healthy.

        Check 2: hive_check_console_errors
        → Verify no new JavaScript errors. Expected: no new errors since plan execution started.

        Check 3: hive_get_page_info
        → Verify app is rendering. Expected: page has content and is interactive.
        ```

        **Retry logic:**
        - If ANY check fails, log which check failed and the error
        - Retry the FAILED check(s) once (not all three — only the ones that failed)
        - If the retry also fails → HALT execution with:
          ```
          ## WebMCP Health Check FAILED

          **Plan:** {PLAN_ID}
          **Failed check:** {check_name} (e.g., hive_check_console_errors)
          **Error:** {error_details}
          **Attempts:** 2/2

          Execution halted. The web application is in an unhealthy state after plan {PLAN_ID}.

          Options:
          - Fix the issue and re-run: `/grd:execute-phase {PHASE}`
          - Skip WebMCP checks: set `webmcp.enabled: false` in `.planning/config.json`
          ```
        - If all checks pass (including any retried ones): continue to step 5

        **Important:** These checks call Chrome DevTools MCP tools directly via the orchestrator's tool access (not via a subagent). The orchestrator already has access to MCP tools from its tool list.
    ```

    **2. Add the SAME WebMCP sanity check to `execute_waves_teams` flow (~line 108-236):**

    After step 6 ("Spot-check results") and BEFORE step 7 ("Code review"), insert:

    ```markdown
    6b. **WebMCP sanity checks (if `webmcp_available=true` from init):**

        Same logic as standard flow step 4b. Skip when `webmcp_available` is false, run three health checks, retry failed checks once, halt on second failure.
    ```

    This can be a brief reference: "Apply the same WebMCP sanity check logic described in the standard `execute_waves` step 4b."

    **3. Add webmcp_available to the fields parsed from INIT JSON:**

    In the `<step name="initialize">` section (~line 20-34), add `webmcp_available` and `webmcp_skip_reason` to the "Parse JSON for:" list. These fields were added to the init output by Phase 43.

    **CRITICAL PRESERVATION RULES:**
    - Do NOT remove or modify any existing steps
    - Do NOT change step numbering for existing steps (1-7 in standard, 1-8 in teams)
    - Add the new step as 4b (standard) and 6b (teams) to slot between existing steps
    - Preserve all existing content verbatim — this is purely additive
  </action>
  <verify>
    Read commands/execute-phase.md and confirm:
    - grep "webmcp_sanity_check\|WebMCP sanity check\|webmcp_available" commands/execute-phase.md (Level 1: Sanity)
    - grep "hive_get_health_status" commands/execute-phase.md confirms all three health check tool names present
    - grep "hive_check_console_errors" commands/execute-phase.md
    - grep "hive_get_page_info" commands/execute-phase.md
    - grep "retry" commands/execute-phase.md confirms retry logic is documented
    - The step appears in BOTH execute_waves and execute_waves_teams sections
    - Existing step numbering (1-7 standard, 1-8 teams) is preserved
    - webmcp_available appears in the initialize step's "Parse JSON for:" field list
  </verify>
  <done>
    commands/execute-phase.md contains a WebMCP sanity check step (4b in standard flow, 6b in teams flow) that:
    - Guards on webmcp_available from init JSON
    - Calls hive_get_health_status, hive_check_console_errors, hive_get_page_info
    - Retries failed checks once
    - Halts execution on second consecutive failure with clear error
    - Skips gracefully when webmcp_available is false
    All existing content preserved verbatim.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- commands/execute-phase.md is valid markdown
- File contains "webmcp_available" in the initialize step field list
- File contains three health check tool names
- File contains retry/halt logic
- Both standard and teams flow variants have the new step
- No existing steps removed or renumbered

Level 2 (Proxy):
- Manual read-through confirms the step is correctly positioned between spot-checks and failure handling (standard) / spot-checks and code review (teams)
- The skip condition correctly references webmcp_available from init JSON
- The retry logic matches REQ-97 specification (first failure retries, second halts)

Level 3 (Deferred): Runtime validation when execute-phase is actually run with Chrome DevTools MCP active — deferred to manual testing
</verification>

<success_criteria>
1. commands/execute-phase.md contains WebMCP sanity check step in BOTH flow variants
2. The step is guarded by webmcp_available conditional
3. Three tool calls specified: hive_get_health_status, hive_check_console_errors, hive_get_page_info
4. First failure triggers retry, second failure halts with clear error
5. Graceful skip when webmcp_available is false
6. webmcp_available listed in initialize step's parsed fields
7. All existing execute-phase content preserved
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.5/phases/44-webmcp-workflow-integration/44-01-SUMMARY.md`
</output>
