---
phase: 105-ai-panel-fallback-hardening
plan: 03
type: execute
wave: 3
depends_on: ["105-02"]
files_modified:
  - tests/unit/research/milestone-verification.test.ts
autonomous: true
verification_level: proxy
hypothesis: "The full v0.5.0 checkpoint milestone satisfies its four proof obligations (R1 no unattended pause, R3 pre-0.5.0 back-compat, R4 DESIGN answers survive debug pinning, R5 no double-asking on re-plan/resume) and this can be proven offline with an injected checkpointHandler, without lowering any per-file jest coverage threshold."
predicted_outcome: "tests/unit/research/milestone-verification.test.ts is green: enumerated unattended call sites (bench/portfolio/harness/autopilot/cli-kb) all resolve resolveInteractive → active:false (no pause); a pre-0.5.0 thread.json fixture resumes bit-identically; a DESIGN-answer-then-debug-replan run keeps the user contract pinned; a resume after answering never re-emits the same question; git diff shows no reduced thresholds in jest.config.js."
eval_metrics:
  primary: "milestone-verification.test.ts pass rate (R1/R3/R4/R5)"
  secondary: ["full research suite green", "zero jest threshold reductions"]
  baseline: "Phases 101-104 individual VERIFICATION.md all PASS"
  target: "all four R-obligations asserted green in one offline suite"
must_haves:
  truths:
    - "R1: resolveInteractive returns active:false for every unattended entry point (bench.ts, portfolio.ts, harness path, autopilot/GRD_AUTOPILOT, cli-kb.ts) — no unattended path can pause (REQ-209)"
    - "R3: a pre-0.5.0 thread.json fixture (no checkpoint fields) resumes bit-identically — back-compat preserved"
    - "R4: a DESIGN checkpoint answer applied before the debug-loop pin means the committed metric/comparator/target is the USER-edited contract, never re-overwritten by the model's original"
    - "R5: after a checkpoint is answered and the loop resumes, the same question is NOT re-emitted on debug re-plan or resume (consumeAnswered one-shot holds)"
    - "No per-file jest coverage threshold in jest.config.js is lowered by this milestone"
  artifacts:
    - path: "tests/unit/research/milestone-verification.test.ts"
      provides: "offline deterministic R1/R3/R4/R5 proof suite with injected checkpointHandler"
      contains: "R1"
  key_links:
    - from: "tests/unit/research/milestone-verification.test.ts"
      to: "lib/research/checkpoints.ts resolveInteractive/consumeAnswered"
      via: "direct assertion of unattended-inactive + one-shot consume"
      pattern: "resolveInteractive|consumeAnswered"
    - from: "tests/unit/research/milestone-verification.test.ts"
      to: "lib/research/orchestrator.ts runLoop"
      via: "injected checkpointHandler/spawn drives R4/R5 end-to-end"
      pattern: "checkpointHandler"
---

<objective>
Author the v0.5.0 milestone verification suite (REQ-209): one offline, deterministic test
file that proves the four cross-phase proof obligations R1/R3/R4/R5 hold now that all
checkpoint machinery (101-104) plus the panel fallback (105-01/02) are in place. This is
the Integration Phase's automated verification.

Purpose: Individual phase VERIFICATION.md files each proved their slice; this suite proves
the SEAMS between them — that no unattended caller can pause, back-compat survives, and the
DESIGN-answer/debug-pin interaction (R4) and re-plan/resume de-dupe (R5) are correct
end-to-end. All via injected `checkpointHandler`/`spawn` — no live backend, no coverage
threshold lowered.
Output: tests/unit/research/milestone-verification.test.ts.
Research basis: ROADMAP Phase 105 SC4; REQ-209; Phase 101 caller-audit (5 sites) + Phase 102
R4 pin ordering + consumeAnswered one-shot semantics.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@lib/research/checkpoints.ts
@lib/research/orchestrator.ts
@lib/research/portfolio.ts
@lib/research/bench.ts
@tests/unit/research/checkpoints.test.ts
@tests/unit/research/orchestrator.test.ts

R2 is intentionally absent from the obligation set (roadmap lists R1/R3/R4/R5). Reuse the
existing 0.4.16 back-compat fixtures authored in Phase 101 (per STATE decision
[Phase 101]: hand-authored, cross-checked vs git 3c179fe) for R3.
</context>

<tasks>

<task type="auto">
  <name>Task 1: R1 (no unattended pause) + R3 (pre-0.5.0 back-compat)</name>
  <files>tests/unit/research/milestone-verification.test.ts</files>
  <action>
    Create the suite file with two obligation blocks:
    - R1: enumerate the 5 unattended entry points (bench.ts, portfolio.ts, harness path,
      autopilot / GRD_AUTOPILOT env, cli-kb.ts). For each, assert `resolveInteractive(cfg, opts)`
      returns `{ active: false }` under that caller's opts (noGates / concurrency>1 /
      GRD_AUTOPILOT / nonInteractive). Extend the Phase 101 caller-audit spirit: prove no
      unattended path can reach a pausing checkpointHandler. Include a fallback:'panel' variant
      to confirm panel resolution is also non-pausing (ties to 105-02).
    - R3: load the pre-0.5.0 thread.json fixture (reuse Phase 101 frozen fixture) and assert a
      resume produces a bit-identical thread round-trip (JSON.stringify equality) — no checkpoint
      field pollutes an old thread.
  </action>
  <verify>
    npx jest tests/unit/research/milestone-verification.test.ts -t "R1|R3" (Level 2: Proxy)
    Assert: all 5 sites active:false; pre-0.5.0 fixture round-trips byte-identically.
  </verify>
  <done>R1 (5 sites non-pausing incl. panel) and R3 (back-compat bit-identical) asserted green.</done>
</task>

<task type="auto">
  <name>Task 2: R4 (DESIGN answers survive debug pin) + R5 (no double-ask) + coverage guard</name>
  <files>tests/unit/research/milestone-verification.test.ts</files>
  <action>
    Add two more obligation blocks (injected spawn + checkpointHandler drive runLoop offline):
    - R4: simulate a DESIGN checkpoint where the human/panel edits the metric contract, then
      the debug-loop runs. Assert the COMMITTED pin equals the edited contract, never the
      model's original (mirror Phase 102 R4 ordering: contract edits apply strictly BEFORE the
      debug-loop pin).
    - R5: after answering a checkpoint (consumeAnswered returns once), drive a debug re-plan and
      a resume; assert the SAME question is not re-emitted (consumeAnswered one-shot WeakSet
      holds — second read returns null; no duplicate checkpoint in checkpoints.jsonl for that
      point/iteration).
    - Coverage guard: add an assertion or a documented check that `git diff jest.config.js`
      introduces no lowered per-file thresholds (either a test that reads jest.config thresholds
      against a snapshot, or record the check in the SUMMARY with `git diff` evidence).
  </action>
  <verify>
    npx jest tests/unit/research/milestone-verification.test.ts (Level 2: Proxy)
    npm test then: git diff --stat jest.config.js shows no threshold reductions (Level 1: Sanity)
  </verify>
  <done>R4 (edited contract pinned) and R5 (one-shot, no double-ask) green; no jest threshold lowered.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): suite file compiles (build:check), lint clean, jest.config thresholds unchanged.
Level 2 (Proxy): milestone-verification.test.ts R1/R3/R4/R5 all green; full research suite green.
Level 3 (Deferred): live end-to-end steered run → 105-04 sandbox (qualitative).
</verification>

<success_criteria>
- R1: all 5 unattended entry points resolve active:false (panel variant also non-pausing).
- R3: pre-0.5.0 thread resumes bit-identically.
- R4: user/panel-edited DESIGN contract is the committed pin.
- R5: answered checkpoint never re-asks on debug re-plan/resume.
- No per-file jest coverage threshold lowered (git diff evidence in SUMMARY).
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-03-SUMMARY.md`
</output>
