---
phase: 105-ai-panel-fallback-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/research/checkpoints.ts
  - tests/unit/research/checkpoints.test.ts
autonomous: true
verification_level: sanity
hypothesis: "A degrade-safe answerViaDiscussion wrapper over resolveElicitation can answer any Checkpoint inline (answeredBy:'panel'), and every failure mode (empty synthesis, rate-limited panelist, no option match) falls back to the recommended default without throwing or pausing."
predicted_outcome: "tests/unit/research/checkpoints.test.ts is green with new answerViaDiscussion cases: exact/prefix/no-match option matching, rate-limited panelist → answeredBy:'default', empty panel → answeredBy:'default', backend-exclusion of the loop spawn backend, and the per-file jest coverage threshold on lib/research/checkpoints.ts is not lowered."
eval_metrics:
  primary: "checkpoints.test.ts pass rate + lib/research/checkpoints.ts branch coverage"
  secondary: ["no new jest threshold reductions"]
  baseline: "existing checkpoints.test.ts green (Phase 101/104)"
  target: "100% new-test pass, coverage threshold held"
must_haves:
  truths:
    - "answerViaDiscussion(cwd, checkpoint, cfg) returns CheckpointAnswer[] (one per question) — never throws, never pauses (REQ-207, SC1)"
    - "A rate-limited/logged-out panelist (detected via detectFromStdout) is treated as unavailable and NEVER as an answer — that question resolves to its recommended default (answeredBy:'default')"
    - "Option matching is exact → prefix → recommended-default; a panel answer matching an option label sets answeredBy:'panel', no match sets answeredBy:'default'"
    - "Participants passed to the panel exclude the loop's own spawn backend (no self-consultation)"
    - "Empty/failed synthesis (resolveElicitation returns '') resolves the whole checkpoint to recommended defaults (answeredBy:'default')"
  artifacts:
    - path: "lib/research/checkpoints.ts"
      provides: "answerViaDiscussion export + injectable resolveElicitation/detectFromStdout seams"
      exports: ["answerViaDiscussion"]
    - path: "tests/unit/research/checkpoints.test.ts"
      provides: "offline deterministic answerViaDiscussion coverage (injected panel + detector)"
      contains: "answerViaDiscussion"
  key_links:
    - from: "lib/research/checkpoints.ts"
      to: "lib/discussion.ts"
      via: "injected resolveElicitation dependency (default = require('../discussion').resolveElicitation)"
      pattern: "resolveElicitation"
    - from: "lib/research/checkpoints.ts"
      to: "lib/scheduler.ts detectFromStdout"
      via: "injected detector for rate-limit guard"
      pattern: "detectFromStdout"
---

<objective>
Build `answerViaDiscussion` in `lib/research/checkpoints.ts`: the pure, degrade-safe
resolver that answers a `Checkpoint` via the AI panel instead of pausing for a human.
This is the P3 F6 core (REQ-207). No orchestrator wiring here — that is 105-02.

Purpose: Autonomous runs with `fallback:"panel"` must resolve checkpoints inline with a
synthesized panel decision, while every degenerate path (no panelists, rate-limited
panelist, unparseable answer) silently falls back to the recommended default — the SAME
guarantee the existing `resolveToDefaults` path already gives. No new failure mode may
enter the loop.

Output: `answerViaDiscussion` export + injected `resolveElicitation`/`detectFromStdout`
seams, fully covered by offline deterministic tests.
Research basis: FEATURES.md F6 (panel fallback); reuses `lib/discussion.ts:resolveElicitation`
(sync-spawning, returns '' on failure) and `lib/scheduler.ts:detectFromStdout`.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.5.0/research/FEATURES.md
@lib/research/checkpoints.ts
@lib/research/types.ts
@lib/discussion.ts
@tests/unit/research/checkpoints.test.ts

Note on GENOME: the "no LLM-judged scoring on the core execution path" heuristic is NOT
violated — the panel answers a human-steering question when no human is present; it does
NOT score/select on the deterministic verdict path (metric/comparator/target stays
untouched). The audit trail (checkpoints.jsonl) is identical whether human, panel, or
default answered.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: answerViaDiscussion core — panel answer → CheckpointAnswer[]</name>
  <files>lib/research/checkpoints.ts, tests/unit/research/checkpoints.test.ts</files>
  <action>
    Add `export function answerViaDiscussion(cwd, checkpoint, cfg, deps?)` where `deps`
    injects `{ resolveElicitation, participants?, synthesizer? }` (default
    `resolveElicitation` = `require('../discussion').resolveElicitation`, so tests inject a
    stub and never spawn).

    RED first — write failing tests, then implement:
    - Formats a single panel `question` + `context` string from `ck.context` plus each
      `ck.questions[].ask` and its option labels (the panel picks one label per question).
      Reuse the existing `ELICITATION`/context shape; keep it one round, one-shot.
    - Calls the injected `resolveElicitation(question, context, { participants, synthesizer, cwd })`.
    - Participants are derived to EXCLUDE the loop's own spawn backend: accept
      `cfg.loopBackend` (or `deps.participants` override) and filter it out of the panel
      participant list before calling. Test: loopBackend='claude' ⇒ 'claude' not in the
      participants passed to resolveElicitation.
    - Maps the synthesized answer to per-question `CheckpointAnswer[]` (one entry per
      `ck.questions`), `answeredBy:'panel'` when an option matched.
    - NEVER throws: wrap the whole body so any error returns recommended defaults
      (reuse/extract the existing `answersFromRecommended(ck)` helper, all `answeredBy:'default'`).

    Do NOT change `resolveElicitation` in lib/discussion.ts (spec: discussion.ts unchanged).
  </action>
  <verify>
    npx jest tests/unit/research/checkpoints.test.ts -t "answerViaDiscussion" (Level 1: Sanity)
    Assert: panel label "Continue — revise hypothesis" → matched answer with answeredBy:'panel';
    injected resolveElicitation received participants WITHOUT the loop backend.
  </verify>
  <done>answerViaDiscussion exported; happy-path panel answer produces answeredBy:'panel' CheckpointAnswer[]; loop backend excluded from participants; body cannot throw.</done>
</task>

<task type="tdd">
  <name>Task 2: option matching + rate-limit guard + empty-panel default</name>
  <files>lib/research/checkpoints.ts, tests/unit/research/checkpoints.test.ts</files>
  <action>
    Extend answerViaDiscussion with the matching + guard logic (TDD, RED→GREEN):
    - Option matching order per question: exact (case-insensitive trim) → prefix
      (option label startsWith panel token or vice-versa) → recommended default. On the
      recommended-default fallback the answer is `answeredBy:'default'` (NOT 'panel') —
      an unmatched panel answer must not masquerade as a real panel decision.
    - Rate-limit guard: inject `detectFromStdout` (default
      `require('../scheduler')` adapter or a thin local). If the panel raw output (or the
      resolveElicitation result being empty because the panelist was rate-limited) is
      flagged `rateLimited || unhealthy`, treat the panelist as UNAVAILABLE → resolve to
      recommended defaults (answeredBy:'default'). A rate-limited panelist is NEVER read as
      an answer. Since `resolveElicitation` already returns '' on spawn failure, the primary
      guard is: empty synthesis ⇒ recommended defaults for ALL questions.
    - Record `discussionFile` on the returned checkpoint metadata when the panel produced a
      real answer (mirror the Checkpoint record shape; wire the field, executor confirms
      exact key name against types.ts).

    Tests (all offline, inject stubs):
    - exact match, prefix match, no-match→default cases;
    - resolveElicitation returns '' ⇒ every answer answeredBy:'default';
    - detectFromStdout reports rateLimited:true ⇒ answeredBy:'default', not 'panel'.
  </action>
  <verify>
    npx jest tests/unit/research/checkpoints.test.ts (Level 1: Sanity)
    npx jest --coverage --collectCoverageFrom='lib/research/checkpoints.ts' tests/unit/research/checkpoints.test.ts
    Check: per-file threshold in jest.config.js for lib/research/checkpoints.ts still passes (NOT lowered).
  </verify>
  <done>Exact→prefix→recommended matching verified; rate-limited/empty panel → answeredBy:'default'; discussionFile recorded on real panel answers; coverage threshold held.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): checkpoints.test.ts green; answerViaDiscussion never throws/pauses;
option matching + rate-limit guard covered with injected panel/detector.
Level 2 (Proxy): coverage of lib/research/checkpoints.ts meets existing per-file jest
threshold with no lowering.
Level 3 (Deferred): live panel spawn quality → exercised in 105-04 sandbox.
</verification>

<success_criteria>
- answerViaDiscussion returns exactly one CheckpointAnswer per question, always.
- Loop spawn backend is excluded from panel participants.
- Rate-limited/empty panel resolves to recommended defaults (answeredBy:'default').
- Exact→prefix→recommended-default matching order verified.
- lib/research/checkpoints.ts jest per-file coverage threshold unchanged and passing.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-01-SUMMARY.md`
</output>
