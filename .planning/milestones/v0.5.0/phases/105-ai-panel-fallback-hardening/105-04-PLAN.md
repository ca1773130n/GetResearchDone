---
phase: 105-ai-panel-fallback-hardening
plan: 04
type: execute
wave: 4
depends_on: ["105-03"]
files_modified:
  - .planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-04-VALIDATION.md
autonomous: false
verification_level: deferred
hypothesis: "The offline-proven checkpoint machinery behaves correctly against a real LLM backend in a throwaway sandbox: live N-candidate hypotheses are distinct and usable (DEFER-104-01), the human selection/steering UX is coherent (DEFER-104-02), and a fallback:'panel' run answers checkpoints without pausing — collecting every v0.5.0 deferred live validation the integration phase owns."
predicted_outcome: "A live `gd research` run in a mktemp -d sandbox with interactive.hypothesize + hypothesis_candidates>=2 produces >=2 distinct candidates and a coherent selection prompt; a fallback:'panel' autonomous run completes with panel-answered checkpoints (answeredBy:'panel' in checkpoints.jsonl) and never pauses; results recorded in 105-04-VALIDATION.md with DEFER IDs resolved or re-deferred."
must_haves:
  truths:
    - "A live sandbox `gd research` run generates N>=2 distinct hypothesis candidates and the selection checkpoint renders them coherently (DEFER-104-01/02)"
    - "A live `fallback:'panel'` autonomous run answers a checkpoint via the panel (answeredBy:'panel' present in checkpoints.jsonl) with no pause, or degrades to recommended defaults gracefully"
    - "The run happens ONLY in a throwaway mktemp -d with its own .planning/ — the GRD repo is never polluted (no .planning/research/threads/, no root KNOWHOW.md, no DEAD-ENDS.md mutation)"
    - "Every v0.5.0 deferred live validation (DEFER-104-01/02, DEFER-102-01 live AskUserQuestion, DEFER-101-02 panel, DEFER-101-03 R1-R5) is recorded as RESOLVED or re-DEFERRED with rationale"
  artifacts:
    - path: ".planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-04-VALIDATION.md"
      provides: "live sandbox validation record + deferred-validation disposition table"
      contains: "DEFER-104-01"
  key_links:
    - from: "105-04-VALIDATION.md"
      to: ".planning/STATE.md deferred table"
      via: "disposition of each DEFER-1xx entry"
      pattern: "DEFER-1"
---

<objective>
Exercise the v0.5.0 checkpoint feature against a real backend in a throwaway sandbox and
collect every deferred live validation this Integration Phase owns (DEFER-104-01/02,
DEFER-102-01, DEFER-101-02/03). Offline suites (105-01/02/03) proved correctness; this plan
proves the live UX/quality that only a real LLM spawn + human eyes can judge.

Purpose: The ROADMAP designates Phase 105 as the milestone Integration Phase that "collects
deferred/end-to-end verification from Phases 101-104." This is that collection step. It is a
human-verify checkpoint because the judgments are qualitative (candidate distinctness,
prompt coherence) and require a live backend account.
Output: 105-04-VALIDATION.md with a disposition for each deferred item.
Research basis: STATE.md Deferred Validations table; 104-VERIFICATION.md DEFER-104-01/02.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.5.0/phases/104-hypothesize-candidate-selection/104-VERIFICATION.md
@commands/research.md

CRITICAL (CLAUDE.md Gotchas): NEVER run live `gd research` inside the GRD repo. Use a
throwaway `mktemp -d` with its own `.planning/`. It otherwise drops
`.planning/research/threads/`, a root `KNOWHOW.md`, and mutates `.planning/DEAD-ENDS.md`.
Scratch logs go in /tmp.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live sandbox run — N-candidate generation + panel fallback</name>
  <files>.planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-04-VALIDATION.md</files>
  <action>
    In a throwaway sandbox (NEVER the repo):
    - `SANDBOX=$(mktemp -d)`; init a minimal `.planning/` (config.json with
      `research_gates.interactive.enabled:true`, `hypothesis_candidates:3`, a ROADMAP.md stub),
      run from $SANDBOX.
    - Run a live `gd research "<small falsifiable question>"` with interactive.hypothesize
      enabled; capture the generated candidates and the selection checkpoint JSON.
    - Run a second autonomous pass with `fallback:"panel"` (e.g. --no gates OR autopilot-style
      env) and inspect `$SANDBOX/.planning/research/threads/<id>/checkpoints.jsonl` for
      `answeredBy:'panel'` records and confirm the run did not pause.
    - If no live backend is available/authenticated, record the run as re-DEFERRED with the
      reason (missing backend account) rather than failing the phase — the offline suites are
      the blocking proof; this is qualitative confirmation.
    - Record raw observations (candidate texts, prompt rendering, panel answers) into
      105-04-VALIDATION.md. Confirm the GRD repo working tree is clean afterward
      (`git -C <repo> status --porcelain` shows no research artifacts).
  </action>
  <verify>
    Inspect 105-04-VALIDATION.md contains candidate samples + checkpoints.jsonl excerpt (Level 3: Deferred).
    git -C <repo> status --porcelain shows no new .planning/research or KNOWHOW.md (Level 1: Sanity).
  </verify>
  <done>Live run (or documented re-defer) recorded; repo tree confirmed clean; candidates + panel records captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of live candidate quality, selection UX, and panel behavior</name>
  <action>Surface 105-04-VALIDATION.md to the user for qualitative judgment; do not proceed until they approve or request changes to a DEFER disposition. Claude does not judge candidate quality/UX autonomously — this is the human-in-the-loop gate.</action>
  <what-built>A live sandbox `gd research` run exercising multi-candidate HYPOTHESIZE selection and the fallback:'panel' autonomous path, with raw output captured in 105-04-VALIDATION.md.</what-built>
  <how-to-verify>
    1. Open 105-04-VALIDATION.md and review the captured candidate set: are the N candidates
       genuinely DISTINCT and each a usable falsifiable hypothesis? (DEFER-104-01)
    2. Review the selection checkpoint prompt rendering: is the UX coherent — one clear
       question, recommended option marked, options readable? (DEFER-104-02)
    3. Review the fallback:'panel' checkpoints.jsonl excerpt: did the panel answer inline
       (answeredBy:'panel') without pausing, or degrade cleanly to defaults? (DEFER-101-02)
    4. Confirm the deferred-validation disposition table marks each of DEFER-104-01/02,
       DEFER-102-01, DEFER-101-02/03 as RESOLVED or re-DEFERRED with a one-line rationale.
    Expected: candidates distinct + usable; prompt coherent; panel path non-pausing; every
    DEFER item has a disposition.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (candidate quality, prompt UX, panel behavior, or a DEFER disposition to change).</resume-signal>
</task>

</tasks>

<verification>
Level 1 (Sanity): GRD repo working tree clean after the sandbox run (no leaked research artifacts).
Level 2 (Proxy): 105-04-VALIDATION.md has candidate samples, checkpoints.jsonl excerpt, and a full DEFER disposition table.
Level 3 (Deferred): human judgment on candidate distinctness + selection/steering UX quality (the checkpoint).
</verification>

<success_criteria>
- Live (or documented re-deferred) sandbox exercise recorded in 105-04-VALIDATION.md.
- Candidate distinctness + selection UX judged by a human (DEFER-104-01/02).
- fallback:'panel' inline non-pausing behavior observed or cleanly degraded (DEFER-101-02).
- Every v0.5.0 deferred live validation dispositioned RESOLVED / re-DEFERRED.
- GRD repo never polluted by the sandbox run.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-04-SUMMARY.md`
</output>
