---
phase: 105-ai-panel-fallback-hardening
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - lib/research/orchestrator.ts
  - lib/research/portfolio.ts
  - tests/unit/research/orchestrator.test.ts
  - CLAUDE.md
  - commands/settings.md
  - docs/autoresearch-tutorial.md
autonomous: true
verification_level: proxy
hypothesis: "Routing the four in-loop checkpoint emit sites through answerViaDiscussion when posture is inactive AND fallback:'panel' makes autonomous runs resolve checkpoints inline with panel answers (no pause), while portfolio's forced non-human concurrency path and telemetry counters record every panel/pause outcome — with byte-identical behavior when fallback:'recommended'."
predicted_outcome: "orchestrator.test.ts shows: fallback:'panel' autonomous run resolves a DESIGN/SEED checkpoint via injected answerViaDiscussion with status never 'paused'; fallback:'recommended' run resolves to defaults byte-identically; research.checkpoint_panel_total (and pauses_total) counters increment as asserted; CLAUDE.md/settings/tutorial document research_gates.interactive.fallback."
eval_metrics:
  primary: "orchestrator.test.ts panel-fallback path pass rate"
  secondary: ["telemetry counter assertions", "byte-identical recommended-path regression"]
  baseline: "Phase 101-104 orchestrator.test.ts green"
  target: "no pause on fallback:'panel' autonomous; counters recorded; recommended path unchanged"
must_haves:
  truths:
    - "With posture inactive AND cfg.fallback==='panel', each of the 4 emit sites (seed/design/hypothesize/decide) answers inline via answerViaDiscussion and the thread status is NEVER 'paused' (REQ-208, SC2)"
    - "With cfg.fallback==='recommended' (default) autonomous behavior is byte-identical to today — no panel spawn, recommended defaults used"
    - "Portfolio's forced non-human/concurrent mode routes checkpoints through fallback (never pauses a concurrent thread), consistent with R1"
    - "research.checkpoint_pauses_total plus new panel counters (panel-answered vs panel-unavailable/default) are recorded via incrementCounter"
    - "Docs updated: CLAUDE.md interactive config keys, gd settings skill, autoresearch tutorial interactive-steering section (REQ-208, SC3)"
  artifacts:
    - path: "lib/research/orchestrator.ts"
      provides: "panel-fallback branch at the 4 emit sites (inline resolve, no pause)"
      contains: "answerViaDiscussion"
    - path: "lib/research/portfolio.ts"
      provides: "forced non-human panel/default routing for concurrent threads"
    - path: "docs/autoresearch-tutorial.md"
      provides: "interactive-steering + panel fallback documentation section"
      contains: "fallback"
  key_links:
    - from: "lib/research/orchestrator.ts"
      to: "lib/research/checkpoints.ts answerViaDiscussion"
      via: "import + call when posture inactive and fallback==='panel'"
      pattern: "answerViaDiscussion"
    - from: "lib/research/orchestrator.ts"
      to: "lib/metrics.ts incrementCounter"
      via: "panel counter recording"
      pattern: "incrementCounter|research.checkpoint"
---

<objective>
Wire `fallback:"panel"` end-to-end: route the four in-loop checkpoint emit sites through
`answerViaDiscussion` (from 105-01) when running autonomously with `fallback:"panel"`, add
the portfolio forced-non-human path, record telemetry counters, and document it all
(REQ-208).

Purpose: A checkpoint must resolve WITHOUT a human whenever the run is unattended — via
the panel when `fallback:"panel"`, via recommended defaults otherwise. The default
`fallback:"recommended"` path must remain byte-identical to today's behavior (R5-adjacent
regression guard).
Output: panel-fallback branch at all emit sites, portfolio wiring, counters, docs.
Research basis: FEATURES.md F6 + "Priorities & touch list" (P3 F6, portfolio force-non-human).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.5.0/research/FEATURES.md
@lib/research/orchestrator.ts
@lib/research/checkpoints.ts
@lib/research/portfolio.ts
@lib/metrics.ts
@commands/settings.md
@docs/autoresearch-tutorial.md
@tests/unit/research/orchestrator.test.ts

The 4 emit sites already exist and gate on `resolveInteractive(...).active` (grep
`emitCheckpoint` in orchestrator.ts: seed ~858, hypothesize ~997, design ~1064, decide
~1234). Panel fallback fires on the posture-INACTIVE branch (autonomous), not the active
(human-pause) branch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route the 4 emit sites through panel fallback when posture inactive + fallback:'panel'</name>
  <files>lib/research/orchestrator.ts, tests/unit/research/orchestrator.test.ts</files>
  <action>
    At each of the 4 checkpoint construction sites (seed, hypothesize, design, decide),
    add a single shared helper (e.g. `resolveCheckpointInline(cwd, thread, ck, cfg, opts)`)
    used when `posture.active === false`:
    - If `cfg.fallback === 'panel'`: call `answerViaDiscussion(cwd, ck, cfg-with-loopBackend, deps)`
      to get CheckpointAnswer[], then apply them through the SAME resolution the resume path
      uses (`resolveCheckpoint` / the existing consume/fold logic) so the ledger/thread mutate
      identically to a human answer — but WITHOUT setting status:'paused'. The loop continues.
    - Else (`fallback === 'recommended'`, the default): keep TODAY's behavior exactly (skip
      emit / recommended defaults) — byte-identical.
    Inject `answerViaDiscussion` via `opts` (mirror `opts.checkpointHandler` DI seam) so tests
    stub the panel. Derive `loopBackend` from the run's spawn backend (config.backend /
    default_backend) and pass it so the panel excludes it.
    Tests (injected spawn + stubbed answerViaDiscussion): a DESIGN and a SEED checkpoint under
    autonomous posture + fallback:'panel' resolve inline (status never 'paused', chosen answer
    flows to ledger/refinedQuestion); fallback:'recommended' produces the pre-existing
    byte-identical result (assert no answerViaDiscussion call).
  </action>
  <verify>
    npx jest tests/unit/research/orchestrator.test.ts -t "panel" (Level 2: Proxy)
    Assert: fallback:'panel' autonomous → result.status !== 'paused' and answerViaDiscussion called;
    fallback:'recommended' → answerViaDiscussion NOT called, result byte-identical to baseline.
  </verify>
  <done>All 4 emit sites resolve inline (no pause) under autonomous + fallback:'panel'; recommended path unchanged; panel DI seam in opts.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio forced non-human routing + telemetry counters</name>
  <files>lib/research/portfolio.ts, lib/research/orchestrator.ts, tests/unit/research/orchestrator.test.ts</files>
  <action>
    - Portfolio: concurrent portfolio threads are non-human by construction (`concurrency>1`
      already forces `resolveInteractive` inactive). Ensure each portfolio-run thread passes
      the interactive cfg through so a `fallback:'panel'` portfolio still answers via panel
      inline and NEVER pauses a concurrent thread (R1). If portfolio currently hard-forces
      recommended-only, thread the cfg.fallback through; keep default recommended.
    - Telemetry: `research.checkpoint_pauses_total` already increments in the default (pause)
      handler. Add panel counters via `incrementCounter`: e.g.
      `research.checkpoint_panel_answered_total` (a real panel match) and
      `research.checkpoint_panel_unavailable_total` (panel empty/rate-limited → default).
      Increment at the inline-resolution site based on whether any answer has answeredBy:'panel'.
      Keep counter names stable; document them in the SUMMARY.
    - Test: a stubbed panel-answered checkpoint increments panel_answered; a stubbed
      empty/rate-limited panel increments panel_unavailable; both via injected incrementCounter.
  </action>
  <verify>
    npx jest tests/unit/research/orchestrator.test.ts -t "counter|portfolio" (Level 2: Proxy)
    Assert: injected incrementCounter receives the panel counters; concurrent portfolio thread never returns status:'paused'.
  </verify>
  <done>Portfolio concurrent path routes through fallback (never pauses); panel_answered/panel_unavailable counters recorded and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Docs — CLAUDE.md config keys, gd settings skill, autoresearch tutorial</name>
  <files>CLAUDE.md, commands/settings.md, docs/autoresearch-tutorial.md</files>
  <action>
    - CLAUDE.md: document `research_gates.interactive` keys, specifically
      `fallback: "recommended"|"panel"` (answerer when no human present) alongside the
      existing research config key list; one concise line, no ballooning.
    - commands/settings.md (gd settings skill): add interactive-steering settings to the
      configurable surface (interactive.enabled, per-point overrides, fallback), matching the
      readInteractiveConfig contract.
    - docs/autoresearch-tutorial.md: add an "Interactive steering" section covering: enabling
      via `--interactive` / config, the checkpoint points (SEED/DESIGN/HYPOTHESIZE/DECIDE),
      resume-with-answers, and the panel fallback (`fallback:"panel"` answers autonomously via
      the discussion panel, recommended defaults as the universal safety net).
    Keep docs factual and consistent with shipped behavior from 105-01/02.
  </action>
  <verify>
    grep -n "fallback" CLAUDE.md commands/settings.md docs/autoresearch-tutorial.md (Level 1: Sanity)
    grep -n "Interactive steering" docs/autoresearch-tutorial.md
  </verify>
  <done>All three docs mention interactive.fallback and the tutorial has an Interactive steering + panel fallback section.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): npm run build:check + npm run lint clean; docs grep hits present.
Level 2 (Proxy): orchestrator.test.ts panel/counter/portfolio cases green; recommended-path
byte-identical regression holds; full research suite green.
Level 3 (Deferred): live `fallback:"panel"` run against a real backend → 105-04 sandbox.
</verification>

<success_criteria>
- Autonomous + fallback:'panel' resolves all 4 checkpoint points inline, status never 'paused'.
- fallback:'recommended' behavior byte-identical to pre-105.
- Portfolio concurrent threads never pause; route through fallback.
- Panel counters recorded via incrementCounter.
- CLAUDE.md, gd settings skill, autoresearch tutorial document interactive.fallback.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/105-ai-panel-fallback-hardening/105-02-SUMMARY.md`
</output>
