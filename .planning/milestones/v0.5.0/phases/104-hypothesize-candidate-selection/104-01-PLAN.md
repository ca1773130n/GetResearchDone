---
phase: 104-hypothesize-candidate-selection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [lib/research/_prompts.ts, lib/research/agent-io.ts, tests/unit/research/prompts.test.ts, tests/unit/research/agent-io.test.ts]
autonomous: true
verification_level: proxy

eval_metrics:
  primary: "Multi-candidate prompt + parser verified by offline deterministic unit tests"
  secondary: ["existing single-block parseHypothesisOutput byte-identical", "buildHypothesizePrompt byte-identical", "N clamped [1,5] and ranked order preserved"]
  baseline: "Existing single-block __HYPOTHESIS__ contract (buildHypothesizePrompt / parseHypothesisOutput)"
  target: "all new tests pass; per-file jest thresholds on _prompts.ts + agent-io.ts hold or rise"

hypothesis: "Adding a parallel __HYPOTHESES__ multi-candidate prompt (buildHypothesesPrompt) and parser (parseHypothesesOutput) that returns a ranked, capped array — while leaving buildHypothesizePrompt and parseHypothesisOutput completely untouched — gives the N>1 selection path its inputs with zero behavior change on the N=1/disabled single-block path."
predicted_outcome: "parseHypothesesOutput parses a __HYPOTHESES__ block into a ranked candidates array (capped to N, statement-less entries dropped, malformed => empty array, never throws); buildHypothesesPrompt emits the __HYPOTHESES__ contract requesting N ranked candidates; parseHypothesisOutput and buildHypothesizePrompt produce byte-identical output to today; tsc + eslint clean."

must_haves:
  truths:
    - "parseHypothesesOutput(stdout, n?) extracts a __HYPOTHESES__ JSON block ({candidates:[{statement,rationale,predictedOutcome}, ...]}) into a ranked array preserving emit order; entries with no statement are dropped; rationale/predictedOutcome default to ''; the array is capped to n (default 5, floor of the config clamp)"
    - "parseHypothesesOutput NEVER throws and NEVER returns null: a missing/empty/malformed block or a missing/empty candidates array yields {candidates: []} (the caller degrades to the single-block cold path)"
    - "buildHypothesesPrompt(thread, priorHyps, priorVerdict, priorTakeaways, pack, pivot, n) reuses the same grounding preamble as buildHypothesizePrompt (question, GROUND-first KG instruction, DEAD-ENDS read, prior hypotheses, takeaways, pivot text) but asks for N ranked candidates and emits exactly one final __HYPOTHESES__ block ranked best-first, at most N candidates"
    - "buildHypothesizePrompt is UNTOUCHED (byte-identical output) and parseHypothesisOutput is UNTOUCHED (byte-identical behavior) — the N=1/disabled path is unchanged"
    - "Both new symbols are added to the module.exports of their files and are importable via typed require"
  artifacts:
    - path: "lib/research/_prompts.ts"
      provides: "buildHypothesesPrompt (multi-candidate __HYPOTHESES__ prompt); buildHypothesizePrompt untouched"
      exports: ["buildHypothesesPrompt"]
    - path: "lib/research/agent-io.ts"
      provides: "parseHypothesesOutput (ranked, capped candidates array); parseHypothesisOutput untouched"
      exports: ["parseHypothesesOutput"]
    - path: "tests/unit/research/prompts.test.ts"
      provides: "buildHypothesesPrompt contract tests + buildHypothesizePrompt unchanged proof"
    - path: "tests/unit/research/agent-io.test.ts"
      provides: "parseHypothesesOutput tests + parseHypothesisOutput unchanged proof"
  key_links:
    - from: "lib/research/agent-io.ts parseHypothesesOutput"
      to: "extractTaggedJson<Record<string, unknown>>(stdout, 'HYPOTHESES')"
      via: "reuse the existing brace-balanced tagged-JSON extractor"
      pattern: "extractTaggedJson.*HYPOTHESES"
    - from: "lib/research/_prompts.ts buildHypothesesPrompt"
      to: "the __HYPOTHESES__ output contract consumed by parseHypothesesOutput"
      via: "emit {\"candidates\":[{\"statement\",\"rationale\",\"predictedOutcome\"}]} ranked best-first"
      pattern: "__HYPOTHESES__"
---

<objective>
Add the N>1 inputs for HYPOTHESIZE candidate selection: a multi-candidate prompt
`buildHypothesesPrompt` emitting a `__HYPOTHESES__` block of up to N ranked candidates, and a
parser `parseHypothesesOutput` that returns a ranked, capped candidates array. The existing
single-block `buildHypothesizePrompt` / `parseHypothesisOutput` pair is left completely
untouched — it remains the N=1/disabled path with zero behavior change.

Purpose: REQ-205 (Multi-Candidate Hypothesis Generation). This is the pure, TDD-friendly
prompt+parser layer; the orchestrator wiring + selection checkpoint is Plan 104-02.
Output: buildHypothesesPrompt, parseHypothesesOutput, and their mirror tests.
Research basis: SUMMARY.md / FEATURES.md §F5 (__HYPOTHESES__ array of
{statement, rationale, predictedOutcome}, ranked, <= hypothesis_candidates; keep the old
single-block parser as the N=1 path — zero drift for existing tests).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0/research/SUMMARY.md
@lib/research/_prompts.ts
@lib/research/agent-io.ts

# Anchor points:
#  - agent-io.ts: extractTaggedJson (brace-balanced, string-aware) is at the top; reuse it.
#    parseHypothesisOutput (single __HYPOTHESIS__) and parseClarifyOutput (returns
#    {dimensions:[]} on malformed, never null) are the two style precedents — follow
#    parseClarifyOutput's degrade-safe "never null, empty array on junk" contract.
#  - _prompts.ts: buildHypothesizePrompt (single __HYPOTHESIS__) is the preamble to reuse;
#    the config clamp for N (hypothesis_candidates, [1,5]) already lives in
#    checkpoints.ts:readInteractiveConfig — this plan just takes n as a parameter (the
#    orchestrator passes the clamped value in 104-02).
#  - The Hypothesis fields the ledger persists are statement + rationale + predictedOutcome
#    (ledger.ts:15-16) — parse all three per candidate so 104-02 can append losslessly.
#  - Both files export via `module.exports = { ... }`; add the new symbols there.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (TDD): parseHypothesesOutput — ranked, capped, degrade-safe multi-candidate parser</name>
  <files>lib/research/agent-io.ts, tests/unit/research/agent-io.test.ts</files>
  <action>
    RED — add to tests/unit/research/agent-io.test.ts a `describe('parseHypothesesOutput (Phase 104)')`
    requiring the new symbol from '../../../lib/research/agent-io' (fails: undefined). Cases:
      - Well-formed 3-candidate __HYPOTHESES__ block, n=3 => 3 candidates in emit (rank) order,
        each with statement/rationale/predictedOutcome.
      - 4 candidates with n=3 => first 3 only, order preserved.
      - A candidate missing `statement` => dropped; siblings kept.
      - Missing `rationale`/`predictedOutcome` => defaulted to '' (not dropped).
      - No __HYPOTHESES__ block / `candidates` absent / non-array / invalid JSON => { candidates: [] }.
      - Extra prose after the block => still parsed (brace-balanced extractor stops at close).
      - PIN existing behavior: parseHypothesisOutput (single __HYPOTHESIS__) still parses
        statement/rationale/predictedOutcome and returns null when no statement / no block.

    GREEN — implement in lib/research/agent-io.ts, beside parseHypothesisOutput (do NOT modify it):
      `parseHypothesesOutput(stdout: string, n?: number): { candidates: Array<{ statement: string;
      rationale: string; predictedOutcome: string }> }`. Use extractTaggedJson<Record<string,
      unknown>>(stdout, 'HYPOTHESES'); if null or `candidates` not an array => return
      `{ candidates: [] }` (NEVER null, NEVER throws — mirror parseClarifyOutput's degrade contract).
      For each raw candidate in emit order: statement = String(o.statement||'').trim() and DROP if
      empty; rationale = String(o.rationale||''); predictedOutcome = String(o.predictedOutcome||'').
      Cap to `n` when provided (default cap 5 = config max), preserving order. Type raw entries as
      Record<string, unknown> (zero `any`). Append `parseHypothesesOutput` to module.exports.
  </action>
  <verify>
    TMPDIR=$(mktemp -d) npx jest tests/unit/research/agent-io.test.ts (Level 2: Proxy — new cases
    pass, parseHypothesisOutput pin stays green). npm run build:check && npm run lint (Level 1: Sanity).
  </verify>
  <done>
    parseHypothesesOutput parses ranked/capped candidates, drops statement-less entries, returns
    {candidates:[]} on any junk (never null/throws); parseHypothesisOutput byte-identical;
    exported; tsc + eslint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2 (TDD): buildHypothesesPrompt — multi-candidate __HYPOTHESES__ prompt</name>
  <files>lib/research/_prompts.ts, tests/unit/research/prompts.test.ts</files>
  <action>
    RED — add to tests/unit/research/prompts.test.ts cases requiring buildHypothesesPrompt from
    '../../../lib/research/_prompts' (fails: undefined):
      - Output contains `__HYPOTHESES__`, the `{"candidates":[{"statement","rationale",
        "predictedOutcome"}]}` contract, and mentions N (the requested candidate count).
      - The grounding `pack`, when passed, is embedded (same as the single prompt).
      - buildHypothesizePrompt is byte-identical to today: assert its output still contains the
        single `__HYPOTHESIS__` contract line and does NOT contain `__HYPOTHESES__` (pin unchanged).

    GREEN — implement in lib/research/_prompts.ts beside buildHypothesizePrompt (do NOT change its
    emitted text): `buildHypothesesPrompt(thread, priorHyps, priorVerdict, priorTakeaways=[],
    pack='', pivot=false, n: number)`. Reuse the SAME preamble (research question, GROUND-first
    Tesserae/KG instruction, "Read .planning/DEAD-ENDS.md", optional pack, prior hypotheses
    history, takeaways, priorVerdict revise line, pivot line) — factor a small local helper OR
    inline-duplicate (either is fine). Change the ask to "Generate up to N ranked, testable
    hypothesis candidates ... ranked best-first (rank 1 = most promising)". Emit exactly one final
    block: `__HYPOTHESES__` then `{"candidates":[{"statement":"...","rationale":"...",
    "predictedOutcome":"..."}]}`. Append `buildHypothesesPrompt` to module.exports.
  </action>
  <verify>
    TMPDIR=$(mktemp -d) npx jest tests/unit/research/prompts.test.ts (Level 2: Proxy — new cases
    pass, buildHypothesizePrompt pin green). npm run build:check && npm run lint (Level 1: Sanity).
  </verify>
  <done>
    buildHypothesesPrompt emits the __HYPOTHESES__ contract requesting N ranked candidates and
    reuses the grounding preamble; buildHypothesizePrompt byte-identical; exported; tsc + eslint clean.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` (tsc --noEmit, zero errors) + `npm run lint` clean.
Level 2 (Proxy): `TMPDIR=$(mktemp -d) npx jest tests/unit/research/agent-io.test.ts tests/unit/research/prompts.test.ts`
  — all new parseHypothesesOutput + buildHypothesesPrompt cases pass; the parseHypothesisOutput
  and buildHypothesizePrompt "unchanged" pins stay green.
Level 3 (Deferred): live N-candidate generation from a real backend — deferred to phase verification.
</verification>

<success_criteria>
- `__HYPOTHESES__` multi-candidate prompt generates up to N candidates (N = hypothesis_candidates,
  passed clamped [1,5]) and parseHypothesesOutput parses them, ranked and capped (REQ-205 / SC1).
- The existing single-block parser (parseHypothesisOutput) and prompt (buildHypothesizePrompt)
  are untouched and remain the N=1/disabled path (SC1).
- parseHypothesesOutput is degrade-safe (empty array on junk, never null/throws).
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/104-hypothesize-candidate-selection/104-01-SUMMARY.md`
</output>
