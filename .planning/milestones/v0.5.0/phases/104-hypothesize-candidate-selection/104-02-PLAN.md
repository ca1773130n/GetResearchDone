---
phase: 104-hypothesize-candidate-selection
plan: 02
type: tdd
wave: 2
depends_on: ["104-01"]
files_modified: [lib/research/orchestrator.ts, tests/unit/research/orchestrator.test.ts]
autonomous: true
verification_level: proxy

eval_metrics:
  primary: "Selection checkpoint pre-ledger gating verified by offline deterministic tests (injected checkpointHandler/runner/spawn)"
  secondary: ["ZERO ledger pollution from unchosen candidates", "freeform statement path", "seeded/resume/crash-recovery skip", "byte-identical default (single-block) flow", "no double-ask on resume"]
  baseline: "Phase 102/103 emit/consume pattern (buildDesignCheckpoint + consumeAnswered + cold-HYPOTHESIZE branch ~L848-884)"
  target: "all new tests pass; per-file jest threshold on orchestrator.ts holds or rises"

hypothesis: "A selection checkpoint emitted inside the cold-HYPOTHESIZE branch — strictly BEFORE appendHypothesis — that persists all candidates in the checkpoint record (never the ledger) and appends only the chosen one on resume, gives human candidate selection with zero ledger pollution while the default (gate-off) single-block path stays byte-identical and the seeded/resume/crash-recovery paths (which already carry a committed hypothesis) never reach it."
predicted_outcome: "With interactive.hypothesize active and >=2 parsed candidates, runLoop pauses with pendingCheckpoint.point='hypothesize' type='selection' and the ledger for this iteration is empty at the pause; resume appends ONLY the chosen candidate (freeform 'Other' => user-authored statement) and proceeds to DESIGN; gate-off runs emit no selection checkpoint and use the untouched single-block path (byte-identical); seeded/execute-resume/crash-recovery paths never emit a selection checkpoint; consumeAnswered is one-shot (no double-ask); tsc + eslint clean."

must_haves:
  truths:
    - "The selection checkpoint is emitted ONLY in the cold-HYPOTHESIZE else-branch (the branch that today spawns grd-hypothesizer + appendHypothesis), when resolveSelectPosture is active AND >=2 candidates parse; it is point='hypothesize' type='selection', one question q1 'Which hypothesis should iteration {i} test?', one option per candidate (rank-1 recommended), freeform:true ('Other' => user-authored)"
    - "The checkpoint pauses STRICTLY BEFORE appendHypothesis — at the pause the ledger has NO hypothesis for this iteration; unchosen candidates live ONLY in the checkpoint record (context holds the full candidate objects for lossless resume), never in the ledger (zero pollution)"
    - "On resume: consumeAnswered(resumedCheckpoint,'hypothesize',iteration) recovers the chosen candidate — a matched option label => the full {statement,rationale,predictedOutcome} candidate (parsed from the checkpoint), a freeform answer => statement=answer.text, rationale='user-provided at checkpoint', predictedOutcome='' — appends ONLY that one hypothesis and falls through to DESIGN"
    - "resolveSelectPosture mirrors resolveDesignPosture: default-OFF, no unattended pause; gate = cfg.hypothesize (opts.interactive.points override); active = posture.active && hypothesizeEnabled && (cfg.every_iteration || thread.iteration === 1)"
    - "The checkpoint is skipped for seeded (synthesis hypothesis adopted), execute-gate resume, and crash-recovery (resumable + no plan) paths — these branch earlier in the if/else and never enter the cold else-branch, so they carry their already-committed hypothesis untouched (SC4)"
    - "Default config (interactive off) and the N=1 path are byte-identical: the existing single-block spawn (buildHypothesizePrompt + parseHypothesisOutput + appendHypothesis) runs exactly as today; when >=2 candidates cannot be parsed (empty array) the branch degrades to that single-block path"
    - "No double-ask: consumeAnswered is one-shot (WeakSet); the deterministic verdict path (evaluateVerdict, committed contract pin, shouldTerminate/decideBranch) is untouched"
  artifacts:
    - path: "lib/research/orchestrator.ts"
      provides: "resolveSelectPosture + buildSelectCheckpoint + selection emit (pre-appendHypothesis) + selection consume (reconstruct chosen, append-one)"
      exports: []
    - path: "tests/unit/research/orchestrator.test.ts"
      provides: "HYPOTHESIZE candidate selection test block"
  key_links:
    - from: "lib/research/orchestrator.ts selection emit"
      to: "the cold-HYPOTHESIZE else-branch, BEFORE appendHypothesis (~L848-884)"
      via: "emit when resolveSelectPosture active && candidates.length>=2; return paused before any ledger append"
      pattern: "buildHypothesesPrompt|parseHypothesesOutput|emitCheckpoint"
    - from: "lib/research/orchestrator.ts selection consume"
      to: "appendHypothesis (chosen candidate only) then DESIGN"
      via: "consumeAnswered(resumedCheckpoint,'hypothesize',iteration) recovers chosen from checkpoint context; freeform => answer.text"
      pattern: "consumeAnswered.*hypothesize"
    - from: "selection checkpoint record"
      to: "the full candidate set (unchosen included) — NEVER the ledger"
      via: "candidates persisted in checkpoint context (JSON); only chosen appended on resume"
      pattern: "point: 'hypothesize'"
---

<objective>
Add the HYPOTHESIZE candidate-selection checkpoint: when interactive.hypothesize is active,
spawn the multi-candidate hypothesizer (104-01's buildHypothesesPrompt + parseHypothesesOutput),
pause with a `selection` checkpoint listing the ranked candidates, and on resume append ONLY the
chosen one to the ledger — a freeform "Other" becomes a user-authored hypothesis. Unchosen
candidates never touch the ledger (zero pollution). The default (gate-off / N=1) single-block
path and the seeded/resume/crash-recovery paths are byte-identical.

Purpose: REQ-206 (Hypothesis Selection Checkpoint) — depends on 104-01 (REQ-205). Copies the
Phase 102 DESIGN and Phase 103 SEED emit/consume pattern (resolve*Posture + build*Checkpoint +
emitCheckpoint + consumeAnswered). The selection sits INSIDE the cold-HYPOTHESIZE else-branch,
strictly before appendHypothesis — the seeded / execute-resume / crash-recovery branches short
earlier in the same if/else and thus skip it automatically.
Output: resolveSelectPosture, buildSelectCheckpoint, the emit + consume wiring, and tests.
Research basis: SUMMARY.md / FEATURES.md §F5 + ARCHITECTURE.md §2.4(2)/§2.5; 103-03-SUMMARY.md pattern.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0/research/SUMMARY.md
@.planning/milestones/v0.5.0/phases/103-seed-interview-decide-branch/103-03-SUMMARY.md
@lib/research/orchestrator.ts
@lib/research/checkpoints.ts
@lib/research/ledger.ts

# Anchor points (orchestrator.ts):
#  - The cold-HYPOTHESIZE else-branch is ~L848-884: `else { // HYPOTHESIZE (cold) ... spawn
#    buildHypothesizePrompt -> parseHypothesisOutput -> hyp = {...} -> appendHypothesis }`.
#    The selection emit + consume live HERE. The earlier if/else arms — seededHyp (~L830),
#    `resumable && !planFile` crash-recovery (~L838), designResolution==='revise' (~L844), and
#    above them the approve/execute-resume fast-paths (~L811-828) — all set `hyp` from an
#    already-committed source and MUST remain untouched (that is SC4's skip-for-free).
#  - Posture precedent: resolveSeedPosture (~L544) / resolveDesignPosture (~L519). buildSeed
#    Checkpoint (~L564) is the build precedent. consumeAnswered one-shot: seed consume (~L774),
#    design consume (~L741). Emit+pause return shape: SEED emit (~L795-802).
#  - readInteractiveConfig(cwd) yields cfg.hypothesize, cfg.hypothesis_candidates (already
#    clamped [1,5]), cfg.every_iteration — pass cfg.hypothesis_candidates as N to
#    buildHypothesesPrompt/parseHypothesesOutput.
#  - renderCheckpointQuestions (thread.ts ~L95) renders questions ONLY, not `context` — so the
#    checkpoint `context` is a safe lossless store for the full candidate array (JSON).
#  - Ledger append is appendHypothesis(cwd, threadId, hyp) (ledger.ts). "Zero pollution" =
#    the emit/return-paused happens with NO appendHypothesis call for this iteration.
#  - depends_on 104-01 (imports buildHypothesesPrompt + parseHypothesesOutput). Same file as
#    103 work — sequential wave.
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolveSelectPosture + buildSelectCheckpoint + selection emit (pre-appendHypothesis)</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    1. Import buildHypothesesPrompt + parseHypothesesOutput (104-01) via the existing typed
       requires of './_prompts' and './agent-io'.

    2. Add `resolveSelectPosture(cwd, opts, config, thread)` mirroring resolveDesignPosture (~L519):
       read InteractiveConfig, apply opts.interactive?.enabled override, posture via
       resolveInteractive({noGates, autonomousMode}); hypothesizeEnabled = opts.interactive?.points
       ? points.includes('hypothesize') : cfg.hypothesize; active = posture.active &&
       hypothesizeEnabled && (cfg.every_iteration || thread.iteration === 1). Return {active, cfg}.

    3. Add `buildSelectCheckpoint(thread, candidates, round)` (mirror buildSeedCheckpoint ~L564):
       checkpoint_version 1, id makeCheckpointId(thread.iteration,'hypothesize',round),
       point:'hypothesize', type:'selection', iteration, round, createdAt,
       context = JSON.stringify(candidates) (FULL {statement,rationale,predictedOutcome} objects —
       lossless resume store; renderCheckpointQuestions does NOT render context), ONE question q1
       ask `Which hypothesis should iteration ${thread.iteration} test?`, options =
       candidates.map((c,i) => ({ label: c.statement, description: c.rationale,
       ...(i===0 ? {recommended:true} : {}) })), freeform: true. Satisfies validateCheckpoint
       (one question; >=1 option; exactly one recommended — rank-1; options are NOT capped at 4,
       only questions are; N is clamped <=5 anyway).

    4. In the cold-HYPOTHESIZE else-branch (~L848-884), compute
       `const selPosture = resolveSelectPosture(cwd, opts, config, thread);` after the grounding
       `pack`/`pivot` is prepared. WHEN selPosture.active:
         - spawn buildHypothesesPrompt({id:thread.id, question:effectiveQuestion}, priorHyps,
           priorVerdict, priorTakeaways, pack, pivot, selPosture.cfg.hypothesis_candidates) via
           spawnAndParse with parseHypothesesOutput; candidates = value?.candidates ?? [].
         - candidates.length === 0 => DEGRADE: run the EXISTING single-block spawn
           (buildHypothesizePrompt + parseHypothesisOutput) + append, unchanged (never wedge).
         - candidates.length === 1 => set hyp from that candidate + appendHypothesis directly
           (no pointless 1-option pause); proceed to DESIGN.
         - candidates.length >= 2 => round = (thread.checkpointRounds?.hypothesize ?? 0)+1;
           ck = buildSelectCheckpoint(thread, candidates, round);
           emitCheckpoint(cwd, thread, ck, {checkpointHandler: opts.checkpointHandler}); if
           thread.status==='paused' return { threadId: thread.id, status:'paused',
           iterations: thread.iteration, paused:true, pendingCheckpoint: thread.pendingCheckpoint
           ?? undefined } — BEFORE any appendHypothesis (ZERO ledger pollution).
       WHEN selPosture inactive => the existing single-block cold path runs UNCHANGED.

    Do NOT touch evaluateVerdict, the committed contract pin, shouldTerminate, or decideBranch.
    The change is additive within the cold-HYPOTHESIZE branch + two new helpers.
  </action>
  <verify>
    npm run build:check && npm run lint (Level 1: Sanity — strict compile + lint clean).
    (Level 2: Proxy — exercised by Task 3 tests.)
  </verify>
  <done>
    resolveSelectPosture + buildSelectCheckpoint exist; with the gate active and >=2 candidates
    the cold branch emits a point='hypothesize' type='selection' checkpoint and pauses BEFORE any
    appendHypothesis; gate-off / 0-candidate / 1-candidate paths never pause; compiles + lints clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Selection consume — reconstruct chosen candidate, append ONE, proceed to DESIGN</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    At the TOP of the cold-HYPOTHESIZE else-branch (before the grounding/spawn from Task 1), add:
    `const selAns = consumeAnswered(resumedCheckpoint ?? null, 'hypothesize', thread.iteration);`
    If selAns:
      - read the q1 answer (find questionId 'q1'); label = a.label, text = a.text.
      - Parse candidates from resumedCheckpoint.context (guarded JSON.parse; [] on failure).
      - MATCHED: candidate whose statement === label => hyp gets its full
        {statement, rationale, predictedOutcome}.
      - FREEFORM (no candidate matches label but text present): statement = text,
        rationale = 'user-provided at checkpoint', predictedOutcome = ''.
      - Fallback (label present, no match, no text): statement = label, rationale = '',
        predictedOutcome = '' (never wedge).
      - hyp = { id: nextHypothesisId(priorHyps), iteration: thread.iteration, statement, rationale,
        predictedOutcome, status:'testing', parentId: lastHyp ? lastHyp.id : null, verdict: null };
        appendHypothesis(cwd, thread.id, hyp) — the ONLY hypothesis appended (unchosen never enter
        the ledger). SKIP the spawn + emit; fall through to DESIGN exactly as the cold path
        continues after `hyp` is set (thread.currentStation='design'; the experiment-runner spawn).
    No-op when selAns is null. This consume is SCOPED to the cold else-branch (not loop-top like
    DECIDE) because on a selection resume the iteration's hypothesis was never appended, so
    `resumable` is undefined and the if/else naturally lands in the cold else — where the consume
    lives. consumeAnswered's (point,iteration) check + WeakSet one-shot keep it independent of
    seed/design/decide consumes and prevent double-consumption.
  </action>
  <verify>
    npm run build:check && npm run lint (Level 1: Sanity).
    (Level 2: Proxy — exercised by Task 3 tests.)
  </verify>
  <done>
    A resumed selection answer appends ONLY the chosen candidate (matched => full fields;
    freeform => user-authored statement + 'user-provided at checkpoint' rationale) and proceeds to
    DESIGN; unchosen candidates never enter the ledger; no-op when no selection answer is present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deterministic offline tests for the selection checkpoint</name>
  <files>tests/unit/research/orchestrator.test.ts</files>
  <action>
    Add `describe('HYPOTHESIZE candidate selection (Phase 104)')` using injected spawn (returns a
    __HYPOTHESES__ block with 3 candidates when the multi prompt is detected, a __HYPOTHESIS__
    block otherwise), injected runner, and an injected checkpointHandler. Cover:
    - PRE-LEDGER PAUSE + ZERO POLLUTION: gate on, >=2 candidates => runLoop pauses with
      pendingCheckpoint.point==='hypothesize' && type==='selection'; readLedger has NO hypothesis
      for thread.iteration at the pause (the unchosen candidates are NOT in the ledger).
    - MATCHED RESUME: resume with q1.label === candidate[1].statement => exactly ONE hyp in the
      ledger with that statement/rationale/predictedOutcome; the other two statements never appear;
      loop proceeds to DESIGN (experiment-runner spawn fires).
    - FREEFORM RESUME: resume with label 'Other' + text 'my own hypothesis' => ledger hyp.statement
      === 'my own hypothesis', rationale === 'user-provided at checkpoint'.
    - BYTE-IDENTICAL DEFAULT / N=1: gate off => single-block prompt used, exactly one hyp appended,
      NO selection checkpoint emitted. Also: gate on but parseHypothesesOutput yields 0 candidates
      => degrades to single-block (one hyp, no pause).
    - SKIP PATHS (SC4): (a) seeded synthesis hypothesis adopted (origin 'synthesis', station 'seed')
      => no selection checkpoint; (b) execute-gate resume (approved.execute + plan on disk) => reuse
      path, no selection checkpoint; (c) crash-recovery (resumable + no plan) => reuse hyp, no
      selection checkpoint. Assert none emit pendingCheckpoint.point==='hypothesize'.
    - NO DOUBLE-ASK: a matched resume advances once; re-entry does not re-consume/re-pause.
    Keep everything offline/deterministic (injected spawn/runner/checkpointHandler; TMPDIR temp cwd).
  </action>
  <verify>
    TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts
    (Level 2: Proxy — all selection tests pass; existing orchestrator + SEED + DECIDE tests green.)
  </verify>
  <done>
    All selection behaviors proven offline (pre-ledger pause + zero pollution, matched append-one,
    freeform statement, byte-identical default/N=1, seeded/execute-resume/crash-recovery skip, no
    double-ask); orchestrator.ts per-file jest threshold holds or rises; full research suite passes.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` + `npm run lint` clean; grep to confirm evaluateVerdict /
committed-contract pin / shouldTerminate / decideBranch call sites are unchanged (diff is
additive within the cold-HYPOTHESIZE branch + two new helpers).
Level 2 (Proxy): `TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts` — all
selection tests pass (pre-ledger pause + zero pollution, matched-resume append-one, freeform
statement, seeded/execute-resume/crash-recovery skip, byte-identical default, no double-ask);
existing orchestrator + SEED + DECIDE tests still green.
Level 3 (Deferred): live human candidate selection against a real backend — deferred to phase verification.
</verification>

<success_criteria>
- The selection checkpoint pauses strictly BEFORE any ledger append; only the chosen candidate is
  ever appended — zero pollution from unchosen candidates (REQ-206 / SC2).
- A freeform answer produces a user-authored hypothesis statement that flows into the ledger
  identically to a selected candidate (SC3).
- The checkpoint is skipped automatically for seeded, resume, and crash-recovery paths (SC4).
- Default config / N=1 path is byte-identical to today (single-block prompt+parser untouched, SC1).
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/104-hypothesize-candidate-selection/104-02-SUMMARY.md`
</output>
