---
phase: 103-seed-interview-decide-branch
plan: 03
type: execute
wave: 2
depends_on: ["103-02"]
files_modified: [lib/research/orchestrator.ts, tests/unit/research/orchestrator.test.ts]
autonomous: true
verification_level: proxy

eval_metrics:
  primary: "DECIDE branch behavior verified by offline deterministic tests (injected checkpointHandler/runner/spawn)"
  secondary: ["verdict math untouched (supported still finalizes)", "byte-identical default-config flow", "zero double-ask on resume"]
  baseline: "Phase 102 DESIGN checkpoint pattern + the existing DECIDE+terminate block (~L804)"
  target: "all new tests pass; per-file jest threshold on orchestrator.ts holds or rises"

hypothesis: "A DECIDE checkpoint that fires ONLY on the would-continue path (never when shouldTerminate/decideBranch already finalize) lets a human choose continue/pivot/stop/adjust-budget as pure continuation-override, leaving evaluateVerdict, the committed contract pin, and shouldTerminate/decideBranch byte-identical."
predicted_outcome: "When the loop would continue (verdict not terminal, budget remaining) and interactive.decide is active, the loop pauses with pendingCheckpoint.point='decide'; resume routes continue->next iteration, pivot->pendingPivot+iterate, adjust-budget->maxIterations bumped+iterate, stop->finalize(finding+kg_write gate); a supported verdict still finalizes untouched; interactive-off runs never emit a decide checkpoint (byte-identical)."

must_haves:
  truths:
    - "The DECIDE checkpoint emits ONLY in the would-continue branch (!term.done && branch !== 'finalize'); a terminal verdict (finalize path) is NEVER delayed by a checkpoint"
    - "The checkpoint is point='decide' type='branch', single round, with options continue (recommended)/pivot/stop/adjust-budget and a context evidence summary (verdict, metric vs target, iteration/maxIterations, latest takeaway)"
    - "On resume: continue -> iteration+1 and loop; pivot -> thread.pendingPivot=true then iteration+1 and loop; adjust-budget -> thread.maxIterations bumped then iteration+1 and loop; stop -> finalize path (build finding from ledger+takeaways+persisted result.json, terminal status 'exhausted', writeFinding, kg_write gate, finishKgSync)"
    - "The DECIDE consume runs at the TOP of the loop (mirrors DESIGN) because the just-measured hypothesis is no longer 'testing'; it short-circuits to advance or finalize and never re-spawns HYPOTHESIZE/DESIGN/RUN for the completed iteration"
    - "Verdict math is untouched: evaluateVerdict, the committed metric-contract pin, and shouldTerminate/decideBranch are byte-identical; DECIDE overrides CONTINUATION only, never the verdict"
    - "Default config (interactive off) is byte-identical: no decide checkpoint emitted, term/branch drive the loop exactly as today"
    - "No double-ask: consumeAnswered one-shot; a resumed decide answer advances/finalizes once"
  artifacts:
    - path: "lib/research/orchestrator.ts"
      provides: "resolveDecidePosture + buildDecideCheckpoint + DECIDE emit (would-continue) + DECIDE consume (loop-top advance/finalize)"
    - path: "tests/unit/research/orchestrator.test.ts"
      provides: "DECIDE branch checkpoint test block"
  key_links:
    - from: "lib/research/orchestrator.ts DECIDE emit"
      to: "the would-continue branch after shouldTerminate/decideBranch (~L805)"
      via: "emit only when !term.done && branch !== 'finalize'"
      pattern: "shouldTerminate|decideBranch|emitCheckpoint"
    - from: "lib/research/orchestrator.ts DECIDE consume (loop top)"
      to: "thread.iteration / thread.pendingPivot / thread.maxIterations / finalize block"
      via: "continue|pivot|adjust-budget advance; stop finalizes"
      pattern: "consumeAnswered.*decide"
    - from: "DECIDE stop finalize path"
      to: "lib/research/finding.ts buildFinding + checkGate('kg_write') + finishKgSync"
      via: "reuse the existing finalize block (buildFinding accepts null lastResult)"
      pattern: "buildFinding|finishKgSync"
---

<objective>
Add the DECIDE branch checkpoint: when the autoresearch loop WOULD continue to another
iteration, and interactive.decide is active, pause and let a human steer
continue/pivot/stop/adjust-budget — a pure override of CONTINUATION. It never delays a
terminal verdict and never touches the deterministic verdict math (evaluateVerdict, the
committed contract pin, shouldTerminate/decideBranch).

Purpose: REQ-204. Copy the Phase 102 DESIGN emit/consume pattern. Emit sits in the
would-continue branch of the existing DECIDE+terminate block; consume sits at the loop TOP
(the just-measured hypothesis is no longer 'testing', so a resumed decide answer must
short-circuit to advance the iteration or finalize — never re-run the completed experiment).
Output: resolveDecidePosture, buildDecideCheckpoint, the emit + consume wiring, and tests.
Research basis: SUMMARY.md §4 (verdict no-touch list) + REQ-204; 102-01-SUMMARY.md pattern.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0/research/SUMMARY.md
@.planning/milestones/v0.5.0/phases/102-design-approval-skill-checkpoint-loop/102-01-SUMMARY.md
@lib/research/orchestrator.ts
@lib/research/checkpoints.ts
@lib/research/finding.ts

# Anchor points (orchestrator.ts): the DECIDE+terminate block is ~L804-836 (term =
# shouldTerminate, branch = decideBranch, then `if (term.done || branch === 'finalize')`
# finalize else `thread.iteration += 1`). The finalize block reads result (current iteration)
# to buildFinding — on a DECIDE-stop RESUME, read `${iterDir}/result.json` if present else
# pass null (buildFinding accepts `ExperimentResult | {metrics} | null`). Loop-top consume
# pattern to mirror: the DESIGN consume at ~L544. depends_on 103-02 (same file — sequential).
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolveDecidePosture + buildDecideCheckpoint + emit on would-continue</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    1. Add `resolveDecidePosture(cwd, opts, config, thread)` mirroring resolveDesignPosture:
       compute posture via resolveInteractive(cfg,{noGates,autonomousMode}); set
       `decidePointEnabled = opts.interactive?.points ? points.includes('decide') : cfg.decide`.
       active = `posture.active && decidePointEnabled`. NOTE: DECIDE is NOT gated to iteration 1
       (it is continuation steering — every would-continue point may pause); `every_iteration`
       does not apply.

    2. Add `buildDecideCheckpoint(thread, hyp, plan, result, outcome, takeaways)`: point:'decide',
       type:'branch', id makeCheckpointId(thread.iteration,'decide',1), round 1 (single round),
       context = an evidence summary string: verdict, `plan.metricKey plan.comparator plan.target`
       vs the measured metric from result.metrics, `iteration N of maxIterations`, and the latest
       takeaway content. ONE question q1 ask "The loop would continue — how should it proceed?"
       with options: `Continue` (recommended, "run another iteration"), `Pivot`
       ("force a substantially different hypothesis next"), `Stop`
       ("finalize now with the current evidence"), `Adjust budget`
       ("extend max iterations and continue"). Exactly one recommended.

    3. In the DECIDE+terminate block, AFTER `term`/`branch`/`incrementCounter` and the optional
       eval-report, keep the existing `if (term.done || branch === 'finalize') { ...finalize... }`
       EXACTLY as-is. In the ELSE path (would-continue, currently just `thread.iteration += 1`),
       BEFORE incrementing, add: if `resolveDecidePosture(...).active`, build the decide
       checkpoint and `emitCheckpoint(cwd, thread, ck, { checkpointHandler: opts.checkpointHandler })`;
       `if (thread.status === 'paused') return { threadId, status:'paused',
       iterations: thread.iteration, paused:true, pendingCheckpoint: thread.pendingCheckpoint ??
       undefined };`. When interactive is off (posture inactive) fall straight through to the
       existing `thread.iteration += 1; thread.status='active'; saveThread; ` — byte-identical.

    Do NOT modify evaluateVerdict, the committed contract pin (~L708/L750), shouldTerminate, or
    decideBranch. The emit is strictly additive in the would-continue branch.
  </action>
  <verify>
    npm run build:check && npm run lint (Level 1: Sanity — strict compile + lint clean)
    grep to confirm shouldTerminate/decideBranch/evaluateVerdict call sites are unchanged.
  </verify>
  <done>
    resolveDecidePosture + buildDecideCheckpoint exist; a would-continue iteration with
    interactive.decide active emits a point='decide' checkpoint and pauses; terminal verdicts
    still finalize with no checkpoint; verdict math untouched; compiles strict, lints clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: DECIDE consume at loop top — advance (continue/pivot/adjust) or finalize (stop)</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    At the TOP of the runLoop for-body (parallel to the DESIGN consume, placed BEFORE it so a
    resumed decide answer is handled first), add:
      `const decideAns = consumeAnswered(resumedCheckpoint ?? null, 'decide', thread.iteration);`
    If `decideAns`:
      - read the q1 label (find answer with questionId 'q1').
      - STOP -> finalize the CURRENT iteration without re-running it: set thread.currentStation
        ='finalize'; thread.status = 'exhausted' (terminal — a would-continue point is never a
        supported verdict); read the persisted result: `const rp = path.join(iterDir,
        'result.json'); const lastResult = fs.existsSync(rp) ? JSON.parse(fs.readFileSync(rp,
        'utf8')) : null;` build finding via buildFinding(thread, readLedger, readTakeaways,
        lastResult) + reconstructabilitySection when a result exists (else skip that section);
        writeFinding; then the kg_write GATE exactly like the finalize block
        (`checkGate(thread,'kg_write', approved.kg_write)` — approved.kg_write is false on
        resume, so a live kg_write gate pauses with pendingGate 'kg_write'); else
        `return await finishKgSync(cwd, thread, undefined, 'exhausted', opts.kgClient)`.
      - CONTINUE / PIVOT / ADJUST BUDGET -> advance and loop:
        * PIVOT: `thread.pendingPivot = true;`
        * ADJUST BUDGET: `thread.maxIterations += DECIDE_BUDGET_BUMP;` (add a module const
          `DECIDE_BUDGET_BUMP = 2` — deterministic; documented). Optionally also honor a numeric
          bump parsed from a freeform answer if present, else the const.
        * then (all three) `thread.iteration += 1; thread.status='active'; saveThread(cwd,
          thread); continue;` (re-enter the loop for the next cold HYPOTHESIZE).
      - Unknown/absent label -> treat as CONTINUE (never wedge).
    Guard so this block is a no-op when there is no resumed decide answer (decideAns null) —
    the normal same-process flow reaches DECIDE at the bottom, never here.

    Ensure the loop-top ordering is: DECIDE consume -> (existing) DESIGN consume/abort -> SEED
    (from 103-02) -> reuse tree. consumeAnswered's point check keeps them independent; the
    WeakSet one-shot prevents re-consumption.
  </action>
  <verify>
    npm run build:check && npm run lint (Level 1: Sanity)
    (Level 2: Proxy — exercised by Task 3 tests)
  </verify>
  <done>
    A resumed decide answer at loop top advances the iteration (continue/pivot/adjust-budget)
    or finalizes (stop, reading persisted result.json, exhausted, kg_write gate) without
    re-running the completed experiment; no-op when no decide answer is present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deterministic offline tests for the DECIDE branch checkpoint</name>
  <files>tests/unit/research/orchestrator.test.ts</files>
  <action>
    Add `describe('DECIDE branch checkpoint (Phase 103)')` using an injected runner (canned
    metric that yields a non-terminal verdict so the loop WOULD continue), injected spawn, and
    an injected checkpointHandler. Cover:
    - WOULD-CONTINUE + interactive.decide on: pauses with pendingCheckpoint.point='decide'
      after MEASURE/LEARN, before iteration increments.
    - TERMINAL verdict NOT delayed: a supported verdict (or budget-exhausted) finalizes with
      NO decide checkpoint — assert status terminal and no pause (verdict no-touch proof).
    - CONTINUE resume: iteration advances; loop runs the next hypothesis.
    - PIVOT resume: thread.pendingPivot true on the next iteration (assert the next HYPOTHESIZE
      pivot path fires).
    - ADJUST-BUDGET resume: thread.maxIterations increased by DECIDE_BUDGET_BUMP; loop continues.
    - STOP resume: finalizes — status 'exhausted', FINDING written (from persisted result.json),
      kg_write gate honored (set kg_write:false so it completes); no re-run of the completed
      experiment (assert runner/hypothesizer not re-invoked for the completed iteration).
    - BYTE-IDENTICAL default: interactive off -> no decide checkpoint; identical loop flow.
    - NO DOUBLE-ASK: a decide resume advances once; re-entry does not re-consume.
    Keep everything offline/deterministic.
  </action>
  <verify>
    TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts
    (Level 2: Proxy — all DECIDE tests pass; existing orchestrator + SEED tests still green)
  </verify>
  <done>
    All eight DECIDE behaviors proven offline (would-continue-only gating, terminal-untouched,
    continue/pivot/adjust/stop routing, byte-identical default, no double-ask); orchestrator.ts
    per-file jest threshold holds or rises; full research suite passes.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): tsc --noEmit + eslint clean; shouldTerminate/decideBranch/evaluateVerdict
call sites unchanged (grep diff).
Level 2 (Proxy): offline deterministic tests prove would-continue-only emission, terminal
verdict never delayed, continue/pivot/adjust-budget/stop routing, byte-identical default, no
double-ask.
Level 3 (Deferred): live human DECIDE round against a real backend — deferred to phase verification.
</verification>

<success_criteria>
- DECIDE checkpoint fires ONLY when the loop would continue; single round;
  continue/pivot/stop/adjust-budget with an evidence summary (REQ-204).
- Pivot -> pendingPivot; stop -> finalize path; evaluateVerdict, contract pin, and
  shouldTerminate/decideBranch untouched (continuation override only).
- Default config byte-identical; emission copies the Phase 102 emit/consume pattern.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/103-seed-interview-decide-branch/103-03-SUMMARY.md`
</output>
