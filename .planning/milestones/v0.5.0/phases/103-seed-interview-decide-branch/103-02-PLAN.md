---
phase: 103-seed-interview-decide-branch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/research/_prompts.ts, lib/research/agent-io.ts, lib/research/orchestrator.ts, tests/unit/research/agent-io.test.ts, tests/unit/research/orchestrator.test.ts]
autonomous: true
verification_level: proxy

eval_metrics:
  primary: "SEED clarification behavior verified by offline deterministic tests (injected checkpointHandler/spawn)"
  secondary: ["byte-identical default-config thread flow", "zero double-ask on resume"]
  baseline: "Phase 102 DESIGN checkpoint pattern (resolveDesignPosture/emit/consume) — copied, not reinvented"
  target: "all new tests pass; per-file jest thresholds on orchestrator.ts/agent-io.ts hold or rise"

hypothesis: "Copying the Phase 102 emit/consume pattern to a new SEED station — a buildClarifyPrompt spawn whose __CLARIFY__ output drives a point='seed' type='clarification' checkpoint — refines an ambiguous question into thread.refinedQuestion for bare-CLI users, while zero ambiguous dimensions cost one spawn and zero pauses and thread.question stays verbatim."
predicted_outcome: "With interactive.seed active and an ambiguous question, the loop pauses BEFORE HYPOTHESIZE with pendingCheckpoint.point='seed'; resume folds answers into thread.refinedQuestion (thread.question unchanged); zero ambiguous dimensions => no pause (one spawn); seeded threads and interactive-off runs never spawn the clarifier and are byte-identical to today."

must_haves:
  truths:
    - "parseClarifyOutput extracts a __CLARIFY__ block into { dimensions: [{ ask, options:[{label,description,recommended?}], freeform? }] }; malformed/empty => zero dimensions (no checkpoint)"
    - "When interactive.seed is active (iteration 1, not seeded, refinedQuestion unset) and the clarifier returns >=1 dimension, runLoop emits a point='seed' type='clarification' Checkpoint and pauses BEFORE any HYPOTHESIZE spawn"
    - "Zero ambiguous dimensions => NO checkpoint: one clarifier spawn, zero pauses; thread.refinedQuestion is set to thread.question verbatim to mark SEED done (once-per-thread idempotency)"
    - "On resume, consumeAnswered(resumedCheckpoint,'seed',iteration) folds the chosen labels/text into thread.refinedQuestion; thread.question is NEVER mutated (it seeds threadId)"
    - "HYPOTHESIZE grounding + prompt use thread.refinedQuestion ?? thread.question; thread.question passed nowhere as mutated"
    - "SEED is skipped entirely for seeded threads (thread.seededFrom set) and when interactive steering is off/auto-skipped (R1) — default-config flow is byte-identical to today (no clarifier spawn)"
    - "No double-ask: consumeAnswered one-shot; a resumed seed answer folds once and falls through to HYPOTHESIZE"
  artifacts:
    - path: "lib/research/_prompts.ts"
      provides: "buildClarifyPrompt — grd-hypothesizer/clarifier prompt emitting a __CLARIFY__ block"
      exports: ["buildClarifyPrompt"]
    - path: "lib/research/agent-io.ts"
      provides: "parseClarifyOutput — __CLARIFY__ tagged-JSON parser"
      exports: ["parseClarifyOutput"]
    - path: "lib/research/orchestrator.ts"
      provides: "resolveSeedPosture + SEED station (consume/emit/fold) wired at loop top before HYPOTHESIZE"
    - path: "tests/unit/research/orchestrator.test.ts"
      provides: "SEED clarification checkpoint test block"
  key_links:
    - from: "lib/research/orchestrator.ts SEED station"
      to: "lib/research/checkpoints.ts emitCheckpoint/consumeAnswered"
      via: "point='seed' emit + one-shot consume (mirrors DESIGN station)"
      pattern: "emitCheckpoint|consumeAnswered"
    - from: "lib/research/orchestrator.ts HYPOTHESIZE grounding"
      to: "thread.refinedQuestion"
      via: "effectiveQuestion = thread.refinedQuestion ?? thread.question"
      pattern: "refinedQuestion"
    - from: "lib/research/orchestrator.ts"
      to: "lib/research/agent-io.ts parseClarifyOutput / _prompts.ts buildClarifyPrompt"
      via: "spawnAndParse of the clarifier"
      pattern: "buildClarifyPrompt|parseClarifyOutput"
---

<objective>
Give bare-`gd`-CLI users the SEED clarification the skill layer gives interactive users: a
thin orchestrator-side path that spawns a clarifier (`buildClarifyPrompt`), parses its
`__CLARIFY__` block, and — only when ambiguous dimensions exist and interactive.seed is
active — emits a `point:'seed', type:'clarification'` Checkpoint before HYPOTHESIZE. Answers
fold into `thread.refinedQuestion`; `thread.question` stays verbatim (it seeds `threadId`).
Zero ambiguous dimensions cost one spawn and zero pauses.

Purpose: REQ-203. Copy the Phase 102 DESIGN emit/consume pattern verbatim (resolveDesignPosture
-> resolveSeedPosture; buildDesignCheckpoint -> the clarify checkpoint; consume-at-loop-top).
Output: buildClarifyPrompt, parseClarifyOutput, and the wired SEED station + tests.
Research basis: SUMMARY.md §4 C4 + REQ-203; the established pattern in 102-01-SUMMARY.md.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.5.0/research/SUMMARY.md
@.planning/milestones/v0.5.0/phases/102-design-approval-skill-checkpoint-loop/102-01-SUMMARY.md
@lib/research/orchestrator.ts
@lib/research/_prompts.ts
@lib/research/agent-io.ts
@lib/research/checkpoints.ts

# Pattern to copy exactly (orchestrator.ts): resolveDesignPosture (~L503), buildDesignCheckpoint
# (~L466), the loop-top consume block (~L544) and the GATE-1 emit branch (~L667). The SEED
# station reuses emitCheckpoint/consumeAnswered/makeCheckpointId/resolveInteractive from
# checkpoints.ts (already imported at ~L31). thread.refinedQuestion is defined but currently
# UNUSED — HYPOTHESIZE currently grounds on thread.question (~L626 groundQuery, ~L637 prompt).
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildClarifyPrompt + parseClarifyOutput (test-first)</name>
  <files>lib/research/_prompts.ts, lib/research/agent-io.ts, tests/unit/research/agent-io.test.ts</files>
  <action>
    Write the parser test FIRST (RED), then implement (GREEN).

    In lib/research/agent-io.ts, add `parseClarifyOutput(stdout)` reusing the existing
    `extractTaggedJson<T>(stdout, 'CLARIFY')` helper (same mechanism as parseHypothesisOutput).
    Return shape: `{ dimensions: Array<{ ask: string; options: Array<{ label: string;
    description?: string; recommended?: boolean }>; freeform?: boolean }> }`. Normalize
    defensively: missing/empty/malformed block OR missing `dimensions` array => return
    `{ dimensions: [] }` (zero ambiguous dimensions is the well-formed-question path — NEVER
    surface junk). Cap dimensions at 4 (drop extras) and ensure each dimension has at least
    one option; if a dimension has options but none marked recommended, mark the first
    recommended (checkpoint validation requires exactly one recommended per question). Export
    it via module.exports alongside the existing parsers.

    In lib/research/_prompts.ts, add `buildClarifyPrompt(thread: { id: string; question: string })`
    returning a grd-hypothesizer-style prompt that instructs the agent to identify ONLY
    genuinely ambiguous, high-impact dimensions that block a falsifiable metric target
    (what is measured, baseline/target, conditions), preferring multiple-choice options with
    one recommended; if the question is already unambiguous, emit an EMPTY dimensions array.
    End with the exact single-block contract:
      __CLARIFY__
      {"dimensions":[{"ask":"...","options":[{"label":"...","description":"...","recommended":true}],"freeform":false}]}
    Export via module.exports.

    In tests/unit/research/agent-io.test.ts add cases: (a) valid block with 2 dimensions ->
    parsed with options; (b) empty dimensions -> `{ dimensions: [] }`; (c) no __CLARIFY__ tag
    / malformed JSON -> `{ dimensions: [] }`; (d) a dimension missing `recommended` -> first
    option auto-marked recommended; (e) >4 dimensions -> capped to 4.
  </action>
  <verify>
    npx jest tests/unit/research/agent-io.test.ts (Level 1: Sanity — parser unit tests pass)
    node -e "require('./lib/research/_prompts.js')" style: buildClarifyPrompt is exported.
  </verify>
  <done>
    parseClarifyOutput and buildClarifyPrompt exist and are exported; parser tests cover
    valid/empty/malformed/recommended-normalization/cap; empty & malformed both yield zero
    dimensions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the SEED station into runLoop (consume/emit/fold before HYPOTHESIZE)</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    COPY the Phase 102 DESIGN pattern (do not invent a new one).

    1. Add `resolveSeedPosture(cwd, opts, config, thread)` mirroring resolveDesignPosture:
       read interactive config, honor `opts.interactive.enabled` one-shot override, compute
       `posture = resolveInteractive(cfg, { noGates, autonomousMode })`, and set
       `seedPointEnabled = opts.interactive?.points ? points.includes('seed') : cfg.seed`.
       active = `posture.active && seedPointEnabled && thread.iteration === 1 &&
       !thread.seededFrom && thread.refinedQuestion === undefined` (once per thread; seeded
       threads never clarify; refinedQuestion===undefined is the not-yet-done marker).

    2. Add a `buildSeedCheckpoint(thread, dimensions, round)` helper: point:'seed',
       type:'clarification', id via makeCheckpointId(thread.iteration,'seed',round),
       context = the (verbatim) thread.question, questions = dimensions mapped to
       { id:`q${i+1}`, ask, options, freeform } (already normalized to <=4 with one
       recommended each by parseClarifyOutput). round is 1 (single round for seed).

    3. In runLoop, add a SEED block guarded by `designResolution === null` (i.e. not on a
       design approve/revise/abort resume) and placed AFTER the design-consume/abort handling
       and BEFORE the `let hyp; let plan;` reuse tree:
       - CONSUME first: `const sAns = consumeAnswered(resumedCheckpoint ?? null, 'seed',
         thread.iteration);` If present, FOLD the answers into thread.refinedQuestion — compose
         a refined question string from thread.question plus each answer's chosen `label`
         (and `text` when freeform) — set `thread.refinedQuestion`, saveThread, and fall
         through to HYPOTHESIZE. Do NOT mutate thread.question.
       - Else if `seedPosture.active`: spawn the clarifier with spawnAndParse(spawn,
         buildClarifyPrompt(thread), 'grd-hypothesizer', parseClarifyOutput, spawnRetries).
         Degrade-safe: on spawn failure/unparseable, treat as zero dimensions.
         * If dimensions.length === 0: set `thread.refinedQuestion = thread.question`
           (mark SEED done, verbatim), saveThread, continue (no pause — one spawn, zero pauses).
         * If dimensions.length >= 1: build the seed checkpoint, `emitCheckpoint(cwd, thread,
           ck, { checkpointHandler: opts.checkpointHandler })`; `if (thread.status ===
           'paused') return { threadId, status:'paused', iterations: thread.iteration,
           paused:true, pendingCheckpoint: thread.pendingCheckpoint ?? undefined };`

    4. Make HYPOTHESIZE use the refined question WITHOUT mutating thread.question: introduce
       `const effectiveQuestion = thread.refinedQuestion ?? thread.question;` and use it for
       `groundQuery` (~L626), `buildGroundingPack(r.results, effectiveQuestion)` (~L632), and
       the hypothesizer prompt by passing `{ id: thread.id, question: effectiveQuestion }` to
       buildHypothesizePrompt. Leave the seeded-hypothesis and reuse branches untouched.

    Strict mode: no `any`; import buildClarifyPrompt/parseClarifyOutput via typed requires
    matching the existing destructured-require style. Ensure `resumedCheckpoint` seed consume
    coexists with the existing design consume (both call consumeAnswered with their own point;
    the WeakSet one-shot guard already prevents cross-consumption).
  </action>
  <verify>
    npm run build:check (tsc --noEmit passes — strict, zero any)
    npm run lint (eslint clean on lib/)
    (Level 1: Sanity — compiles and lints)
  </verify>
  <done>
    resolveSeedPosture + buildSeedCheckpoint + SEED consume/emit/fold block exist; HYPOTHESIZE
    grounds on effectiveQuestion; thread.question is never assigned; compiles strict, lints clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deterministic offline tests for the SEED clarification checkpoint</name>
  <files>tests/unit/research/orchestrator.test.ts</files>
  <action>
    Add a `describe('SEED clarification checkpoint (Phase 103)')` block using injected
    `spawn` (returns a canned __CLARIFY__ block) and an injected `checkpointHandler`
    (deterministic, non-pausing where needed), mirroring the DESIGN checkpoint tests from
    102-01. Cover:
    - AMBIGUOUS + interactive.seed on: run pauses with status 'paused' and
      pendingCheckpoint.point === 'seed' BEFORE any HYPOTHESIZE spawn (assert the hypothesizer
      spawn was NOT called).
    - RESUME folds refinedQuestion: resume with answers -> thread.refinedQuestion is set from
      the chosen labels, thread.question is UNCHANGED, and HYPOTHESIZE proceeds grounding on
      the refined question (assert the grounding/hypothesizer received the refined text).
    - UNAMBIGUOUS (empty dimensions): exactly ONE clarifier spawn, NO pause, refinedQuestion
      set to the verbatim question, loop proceeds.
    - SEEDED thread (seededFrom set): SEED skipped — no clarifier spawn.
    - BYTE-IDENTICAL default: interactive off -> no clarifier spawn, thread.question used for
      grounding, thread flow identical to a pre-103 baseline (no refinedQuestion written).
    - NO DOUBLE-ASK: after a seed resume, re-entering the loop does not re-emit a seed
      checkpoint (refinedQuestion now defined; consumeAnswered one-shot).
    Keep tests offline/deterministic (no network, no real spawns); set kg_write:false in the
    interactive config helper so runs don't pause at the kg_write gate.
  </action>
  <verify>
    TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts
    (Level 2: Proxy — all SEED tests pass; existing orchestrator tests still green)
  </verify>
  <done>
    Six SEED behaviors proven offline; orchestrator.ts + agent-io.ts per-file jest thresholds
    hold or rise; full research test suite passes.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): tsc --noEmit + eslint clean; parseClarifyOutput/buildClarifyPrompt exported.
Level 2 (Proxy): offline deterministic tests prove pause-before-HYPOTHESIZE on ambiguity,
refinedQuestion fold on resume (question verbatim), zero-pause on unambiguous, seeded skip,
byte-identical default, and no double-ask.
Level 3 (Deferred): live clarifier spawn against a real backend — deferred to phase verification.
</verification>

<success_criteria>
- buildClarifyPrompt + __CLARIFY__ parse raise zero checkpoints when zero ambiguous dimensions
  are detected (one spawn, zero pauses) (REQ-203).
- Answers fold into thread.refinedQuestion without mutating thread.question (seeds threadId).
- Checkpoint skipped for seeded threads and when interactive is off (default byte-identical).
- Emission copies the Phase 102 emit/consume pattern (resolveSeedPosture/emitCheckpoint/consumeAnswered).
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/103-seed-interview-decide-branch/103-02-SUMMARY.md`
</output>
