---
phase: 102-design-approval-skill-checkpoint-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/research/orchestrator.ts
  - tests/unit/research/orchestrator.test.ts
autonomous: true
verification_level: proxy
hypothesis: "Emitting a design-approval Checkpoint at the existing GATE-1/execute-gate site (consuming the execute gate on approve, applying contract edits strictly before the debug-loop pin, capping revise at 2, abandoning on abort) delivers end-to-end human-steered DESIGN approval with byte-identical behavior when interactive is off."
predicted_outcome: "With interactive.design active on iteration 1, runLoop pauses ONCE returning pendingCheckpoint (point='design'); resume with an 'approve' answer + edited target runs the experiment against the edited target (survives the debug-loop pin); a 2nd revise re-plans and a 3rd resolves to the approve default; abort yields status 'abandoned'; with interactive absent/off every orchestrator.test.ts assertion is unchanged and no checkpoints.jsonl is written."
must_haves:
  truths:
    - "Interactive.design active + iteration 1 (or every_iteration) ⇒ runLoop emits ONE approval checkpoint at the GATE-1 site (pendingCheckpoint.point='design', type='approval') and returns { status:'paused', paused:true, pendingCheckpoint } — the execute-gate checkGate pause is NOT also raised (one pause, never two)"
    - "'Approve & run' answer sets approved.execute for this iteration and proceeds directly to RUN reusing the persisted plan.json — DESIGN is never re-spawned on approve-resume (execute-gate resumable precedent)"
    - "Contract edits from the checkpoint answer (metricKey/comparator/target/language) are written into `plan` and persisted to plan.json BEFORE the `committed` snapshot is taken, so the debug-loop pin preserves the user's edited contract (R4) — MEASURE judges the edited target"
    - "consumeAnswered(resumedCheckpoint,'design',iteration) is one-shot: a debug re-plan, resurvey, or a second runLoop pass never re-asks the design checkpoint (R5); debug loop never raises a checkpoint"
    - "'Revise' bumps thread.checkpointRounds.design and re-runs DESIGN; when rounds reach interactive.max_rounds (default 2) the next resolution falls back to the recommended 'approve' default (no infinite revise)"
    - "'Abort' sets thread.status='abandoned', saves, and returns a terminal abandoned ResearchResult (no RUN, no ledger mutation beyond the existing testing hypothesis)"
    - "Interactive absent/disabled (default config) ⇒ the GATE-1 code path is byte-identical to today: existing checkGate('execute') pause, no emitCheckpoint call, no checkpoints.jsonl, all pre-existing orchestrator.test.ts assertions pass unchanged"
  artifacts:
    - path: "lib/research/orchestrator.ts"
      provides: "Design-approval checkpoint emission + resume/consume wiring at the GATE-1/DESIGN site"
    - path: "tests/unit/research/orchestrator.test.ts"
      provides: "R4/R5 + revise-cap + abort + byte-identical-default proofs via injected checkpointHandler"
  key_links:
    - from: "lib/research/orchestrator.ts"
      to: "lib/research/checkpoints.ts"
      via: "require emitCheckpoint / consumeAnswered / makeCheckpointId / resolveInteractive / readInteractiveConfig"
      pattern: "require\\('./checkpoints'\\)"
    - from: "design-approval emission"
      to: "committed metric-contract pin (orchestrator.ts committed snapshot)"
      via: "contract edits applied to `plan` + plan.json before the `const committed = {...plan...}` line"
      pattern: "committed"
---

<objective>
Wire the DESIGN approval checkpoint into `runLoop` at the existing GATE-1 (execute-gate)
site in `lib/research/orchestrator.ts`. This is the FIRST emission call site for the
checkpoints module shipped standalone in Phase 101 — it establishes the emit→pause→
resume→consume pattern that Phases 103/104 will copy (per 101-CONTEXT hybrid-churn).

Purpose: give a human one combined pause to review + approve/revise/abort the exact
experiment plan the execute gate protects, editing the metric contract if desired, with
zero behavior change when interactive steering is off (REQ-199, research §4 C5/R4/R5).
Output: emission + consume wiring in orchestrator.ts and a deterministic offline test
suite (injected checkpointHandler) proving R4, R5, revise-cap-2, abort, and byte-identical
default behavior.
Research basis: .planning/milestones/v0.5.0/research/SUMMARY.md §2 F3, §4 decisions 1/3/5,
§5 risks R4/R5; REQ-199.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v0.5.0/research/SUMMARY.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-CONTEXT.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-02-SUMMARY.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-04-SUMMARY.md
@lib/research/orchestrator.ts
@lib/research/checkpoints.ts
@lib/research/types.ts

# Prior-phase groundwork this plan builds on (verdict: confirmed):
# 101-02 shipped lib/research/checkpoints.ts (emitCheckpoint/resolveCheckpoint/
#   consumeAnswered/readInteractiveConfig/resolveInteractive/makeCheckpointId/validateCheckpoint,
#   CheckpointHandler DI seam, checkpoints.jsonl IO) with ZERO orchestrator import (R1 lock).
# 101-04 shipped the resumeResearch resume-with-answers branch (orchestrator.ts:696-717) that
#   already calls resolveCheckpoint and re-enters runLoop with a resolved `resumedCheckpoint`;
#   runLoop already threads a DORMANT `_resumedCheckpoint` param (orchestrator.ts:427) — this
#   plan makes it LIVE at the DESIGN station. checkpoints.ts is standalone, so this is the first
#   emission site; do NOT touch verdict/pin internals beyond inserting contract edits pre-pin.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit the design-approval checkpoint at the GATE-1 site (fresh path)</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
  Insert design-approval emission into `runLoop` at the DESIGN → GATE-1 block
  (currently orchestrator.ts ~lines 497-521, the `// GATE 1 — execute` section that
  runs after `plan` is spawned and `planFile` written).

  1. Resolve interactive posture once per loop iteration, near the top of the loop body
     (after `const iterDir`/`planFile` are computed). Read config with
     `readInteractiveConfig(cwd)` and gate with `resolveInteractive(cfg, opts)` passing
     the SAME unattended signals the caller-audit relies on: `noGates: opts.noGates`,
     `autonomousMode` from `config.autonomous_mode`, `autopilot`/`env` for GRD_AUTOPILOT,
     `concurrency` (portfolio), `nonInteractive`. Honor the one-shot `opts.interactive`
     override (`enabled`/`points`) exactly as 101-04 defined it. Compute
     `designActive = interactive.active && cfg.design && (cfg.every_iteration || thread.iteration === 1)`.
     Require ALL of `require('./checkpoints')` symbols via a single typed `require` (match
     the resolveCheckpoint require already in resumeResearch): `emitCheckpoint`,
     `consumeAnswered`, `makeCheckpointId`, `resolveInteractive`, `readInteractiveConfig`.

  2. At the GATE-1 site, branch on `designActive`:
     - When `designActive` is FALSE: leave the existing `checkGate(thread,'execute',...)`
       block EXACTLY as-is (byte-identical default path — this is a must_have).
     - When `designActive` is TRUE: do NOT call `checkGate('execute')`. Instead, first
       attempt to consume a resumed answer (Task 2 handles the consume/apply logic; here
       just call into it). If there is NO consumable answer for ('design', thread.iteration),
       build and emit a fresh approval checkpoint:
         * `round = (thread.checkpointRounds?.design ?? 0) + 1`
         * `id = makeCheckpointId(thread.iteration, 'design', round)`
         * Build a Checkpoint `{ checkpoint_version:1, id, point:'design', type:'approval',
           iteration: thread.iteration, round, createdAt: new Date().toISOString(),
           context: <one-line hypothesis + the pinned contract: metricKey comparator target,
             language, scriptPath>, questions: [...] }`.
         * Questions (≤4, each exactly ONE `recommended:true` — validateCheckpoint enforces):
           Q1 `ask:"Approve this experiment plan and run it?"` options
           `Approve & run (recommended)`, `Revise the plan`, `Abort this thread`;
           Q2 (freeform:true) `ask:"Edit the metric contract? (metricKey/comparator/target/
           language — leave as-is to keep the shown values)"` options: a single
           `Keep as designed (recommended)` option (freeform text carries edits). Keep it to
           these 2 questions; the metric-contract fields ride Q2's freeform `text`.
         * Call `emitCheckpoint(cwd, thread, ck, deps)` where `deps.checkpointHandler` comes
           from `opts` (add `opts.checkpointHandler?: CheckpointHandler` to ResearchOptions,
           default = the module pause handler) so tests inject a deterministic answerer.
           The default handler pauses (sets pendingCheckpoint, status='paused', saves,
           increments `research.checkpoint_pauses_total`).
         * After emit, if `thread.status === 'paused'` (default pause handler ran):
           set `thread.currentStation='run'` is NOT correct here — leave currentStation at
           'design'; save is already done by the handler. Return
           `{ threadId: thread.id, status:'paused', iterations: thread.iteration,
             paused:true, pendingCheckpoint: thread.pendingCheckpoint }` — ONE pause, mirroring
           the execute-gate early return shape but carrying pendingCheckpoint (NOT pendingGate).

  Keep the change surgical: the fresh-emit path is only reachable when `designActive` and
  no consumable answer exists. Do NOT alter RUN, the `committed` snapshot, the debug loop,
  MEASURE, `evaluateVerdict`, `shouldTerminate`, `decideBranch`, or the kg_write gate
  (research §4 decision 5 no-touch list).
  </action>
  <verify>
  `npm run build:check` clean; `npm run lint` clean.
  `npx jest tests/unit/research/orchestrator.test.ts` — all pre-existing tests pass
  (Level 1: Sanity — proves the default path is byte-identical since no test enables
  interactive.design yet).
  </verify>
  <done>
  runLoop emits a validated design-approval checkpoint and returns a paused result with
  pendingCheckpoint when interactive.design is active on iteration 1; the execute-gate
  checkGate path is untouched when it is not. Build/lint clean; existing suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consume + apply approve/revise/abort on resume, with contract edits pre-pin</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
  Make the DORMANT `_resumedCheckpoint` param LIVE at the DESIGN station (rename to
  `resumedCheckpoint`; the resumeResearch branch at orchestrator.ts:714-716 already passes
  the resolved checkpoint in). When `designActive` (from Task 1), BEFORE emitting a fresh
  checkpoint, call:
    `const answers = consumeAnswered(resumedCheckpoint, 'design', thread.iteration);`
  `consumeAnswered` is one-shot (WeakSet) and only returns on point+iteration match — this
  is the R5 guard (a debug re-plan or second loop pass gets `null` and never re-asks).

  When `answers` is non-null, interpret Q1's chosen `label`:

  - APPROVE ("Approve & run"): reuse the ALREADY-PERSISTED plan — set `hyp = resumable`
    (the iteration's `status:'testing'` hypothesis) and `plan = JSON.parse(planFile)`; DO NOT
    re-spawn DESIGN (research §4: "resume reuses the persisted approved plan — never
    re-derives"; mirrors the orchestrator.ts:445 resumable fast-path). Then apply any
    contract edits parsed from Q2's freeform `text` (parse `key: value` lines for the
    whitelisted keys `metricKey|comparator|target|language` ONLY; ignore anything else;
    coerce `target` to number when the parse is numeric). Write the edited `plan` back to
    `planFile` with `fs.writeFileSync(planFile, JSON.stringify(plan, null, 2))`. Set
    `approved.execute` semantics so control falls through to RUN this iteration. CRITICAL:
    this all happens BEFORE the `const committed = { metricKey, comparator, target, language }`
    snapshot (orchestrator.ts:542) so the debug-loop pin freezes the USER-EDITED contract,
    not the model's original (R4). Add a code comment tying this to R4.

  - REVISE ("Revise the plan"): bump `thread.checkpointRounds = { ...thread.checkpointRounds,
    design: (thread.checkpointRounds?.design ?? 0) + 1 }`, save, then fall through to a fresh
    DESIGN spawn + a NEW checkpoint emission (round incremented). Cap: if the bumped round
    would EXCEED `interactive.max_rounds`, do NOT emit again — resolve to the recommended
    APPROVE default (reuse persisted plan, proceed to RUN) so revise can never loop forever.

  - ABORT ("Abort this thread"): set `thread.status = 'abandoned'`, `saveThread`, and return
    a terminal `{ threadId: thread.id, status:'abandoned', iterations: thread.iteration }`
    ResearchResult. No RUN, no further ledger mutation.

  Unknown/absent Q1 label ⇒ treat as APPROVE (recommended default) — never wedge.

  Add `resumedCheckpoint`/`checkpointHandler`/one-shot semantics to the relevant type
  signatures (`ResearchOptions.checkpointHandler?`, `runLoop` param rename). Keep the
  resumeResearch → runLoop call chain intact; the resolved checkpoint already flows in.
  </action>
  <verify>
  `npm run build:check` clean; `npm run lint` clean (zero `any`; unused args `_`-prefixed).
  `npx jest tests/unit/research/orchestrator.test.ts` still green (Level 1: Sanity).
  Trace-read the committed-snapshot region to confirm contract edits land BEFORE line ~542.
  </verify>
  <done>
  On resume with a consumed 'design' answer: approve reuses the persisted plan + applies
  whitelisted contract edits pre-pin then RUNs; revise re-plans with an incremented,
  capped round; abort abandons the thread. consumeAnswered one-shot prevents re-asking.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deterministic offline proofs — R4, R5, revise-cap, abort, byte-identical default</name>
  <files>tests/unit/research/orchestrator.test.ts</files>
  <action>
  Add a `describe('DESIGN approval checkpoint (Phase 102)')` block using the existing
  orchestrator test harness (injected `runner`, `spawn`, `retrieve`, hand-built threads,
  tmp cwd outside the repo — set TMPDIR or fs.mkdtempSync into os.tmpdir()). Inject
  interactive config via `.planning/config.json` written into the tmp cwd with
  `research_gates.interactive.enabled=true, design=true` (and `every_iteration` where a
  test needs iteration>1). Drive answers via an injected `opts.checkpointHandler` /
  `opts.checkpointAnswers` on resume rather than pausing.

  Tests (each an observable proof):
  1. EMIT/ONE-PAUSE: interactive.design on, iteration 1 ⇒ first runResearch returns
     `status:'paused'`, `paused:true`, `res.pendingCheckpoint.point==='design'`,
     `type==='approval'`; assert the execute-gate pause (`pendingGate:'execute'`) did NOT
     fire (one pause, never two).
  2. R4 — CONTRACT EDIT SURVIVES THE PIN: resume with an APPROVE answer whose Q2 freeform
     `text` edits `target` (e.g. to 0.9); make the runner report a metric and assert MEASURE
     judged against the EDITED target (verdict reflects 0.9, not the model's original). Force
     a debug retry (`research_max_debug_depth>0`, runner fails once then succeeds) and assert
     the pinned `committed.target` is the edited value (checkpoints.jsonl / plan.json shows
     the edit; contractDrift does NOT revert it).
  3. R5 — NO DOUBLE-ASK: after an APPROVE resume, assert the loop RUNs without emitting a
     second design checkpoint in the same iteration (consumeAnswered one-shot); a debug
     re-plan does not re-pause.
  4. REVISE CAP: resume REVISE twice (rounds 1→2) ⇒ re-plans; a 3rd would exceed
     `max_rounds:2` ⇒ resolves to APPROVE default and RUNs (no further pause). Assert
     `thread.checkpointRounds.design` and that no 4th checkpoint record is written.
  5. ABORT: resume with ABORT ⇒ `res.status==='abandoned'`, no result.json for a new RUN.
  6. BYTE-IDENTICAL DEFAULT: with interactive absent/disabled, an execute-gate run pauses
     with `pendingGate:'execute'` and writes NO checkpoints.jsonl — assert an existing
     execute-gate test still passes verbatim.

  Keep tests offline/deterministic (no network, injected deps). Do not lower any jest
  per-file threshold in jest.config.js; keep orchestrator.ts coverage at/above its current
  entry (94.2% stmts / 82.1% branch per 101-04).
  </action>
  <verify>
  `TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts` — all green under
  coverage; orchestrator.ts threshold not lowered (Level 2: Proxy — R4/R5/cap/abort/default
  behaviors proven via injected answerers).
  Clean stray temp dirs: `find . -maxdepth 1 -type d -name 'grd-*' -exec rm -rf {} +`.
  </verify>
  <done>
  Six proofs green: one-pause emit, R4 contract-edit-survives-pin, R5 no-double-ask,
  revise-cap-2, abort→abandoned, byte-identical default. No threshold lowered.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` + `npm run lint` clean; full orchestrator.test.ts
suite green (default path byte-identical).
Level 2 (Proxy): the six Phase-102 proofs above exercise emit/consume/approve/revise/abort
and R4/R5 via injected checkpointHandler on a tmp cwd.
Level 3 (Deferred): live end-to-end emit→pause→human-answer→resume through the real skill +
CLI is exercised at phase verification and the milestone R1/R3/R4/R5 suite (Phase 105).
</verification>

<success_criteria>
- Interactive.design active (iter 1 / every_iteration) ⇒ exactly ONE pause carrying a valid
  design-approval Checkpoint; approve consumes the execute gate and reuses the persisted plan.
- Contract edits apply before the committed pin and survive the debug loop (R4).
- consumeAnswered one-shot prevents any re-ask on resume/debug re-plan (R5).
- Revise capped at max_rounds (2 → default); abort → abandoned.
- Interactive off ⇒ byte-identical to today; no checkpoints.jsonl; existing suite unchanged.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/102-design-approval-skill-checkpoint-loop/102-01-SUMMARY.md`.
</output>
