---
phase: 101-checkpoint-core-plumbing-config
plan: 04
type: execute
wave: 3
depends_on: ["101-01", "101-02", "101-03"]
files_modified:
  - lib/research/orchestrator.ts
  - lib/research/cli.ts
  - bin/grd-tools.ts
  - tests/unit/research/orchestrator.test.ts
  - tests/unit/research/cli.test.ts
autonomous: true
verification_level: sanity

hypothesis: "A resume-with-answers branch in resumeResearch (before pendingGate handling) plus --answers <file|->/--interactive/--no-interactive flag plumbing and a grep-style caller-audit test deliver the full resume path with ZERO behavior change for threads that have no pendingCheckpoint, and prove no unattended call site can pause interactively."
predicted_outcome: "orchestrator.test proves a hand-built pendingCheckpoint thread resumes with answers recorded to checkpoints.jsonl / defaults on bare resume; the two 0.4.16 fixtures resume bit-identically through the existing pendingGate path; a caller-audit test enumerates all 5 sites (portfolio/bench/cli/cli-kb/index) and fails if a 6th appears; --no-gates implies --no-interactive."

must_haves:
  truths:
    - "resumeResearch gains a branch BEFORE pendingGate handling: when thread.pendingCheckpoint?.status is pending (or the field is set), record answers (from opts.checkpointAnswers, else every question → its recommended option answeredBy:'default'), append checkpoints.jsonl via resolveCheckpoint, clear pendingCheckpoint, set status 'active', then runLoop(..., {resumedCheckpoint}). --no-gates on resume also clears interactive (mark skipped, proceed with defaults)."
    - "ResearchOptions gains optional checkpointAnswers and a one-shot interactive override; ResearchResult gains optional pendingCheckpoint?: Checkpoint. runLoop accepts an optional {resumedCheckpoint} that is DORMANT this phase (no emission sites) — threaded but unconsumed."
    - "gd research resume <id> [--answers <file|->] parses answers from a FILE or stdin ('-') ONLY — never answer text in argv (R8); malformed/missing file → bare-resume defaults. --interactive (one-shot enable, optional =point,list) and --no-interactive parse into opts; --no-gates implies --no-interactive."
    - "A caller-audit test enumerates all 5 runResearch/resumeResearch call sites (portfolio.ts, bench.ts, cli.ts, cli-kb.ts, index.ts) by grep-style discovery and asserts each unattended path completes with interactive gates forced ON in config but noGates/autonomous set — a NEW 6th caller fails the test until it declares its interactive posture."
    - "Both frozen 0.4.16 fixtures resume bit-identically: the paused-execute fixture flows through the existing pendingGate:'execute' path (NOT the checkpoint branch, since pendingCheckpoint is undefined); the terminal-supported fixture short-circuits unchanged."
  artifacts:
    - path: "lib/research/orchestrator.ts"
      provides: "resumeResearch checkpoint-answers branch + ResearchResult.pendingCheckpoint + ResearchOptions.checkpointAnswers/interactive + dormant runLoop resumedCheckpoint param"
      contains: "pendingCheckpoint"
    - path: "lib/research/cli.ts"
      provides: "cmdResearchResume --answers wiring (checkpointAnswers on ResearchOptions)"
    - path: "bin/grd-tools.ts"
      provides: "research subcommand --answers <file|->, --interactive, --no-interactive parsing"
      contains: "--answers"
    - path: "tests/unit/research/orchestrator.test.ts"
      provides: "resume-with-answers, bare-resume-defaults, 0.4.16 bit-identical resume, caller-audit"
  key_links:
    - from: "lib/research/orchestrator.ts"
      to: "lib/research/checkpoints.ts"
      via: "resolveCheckpoint/consumeAnswered/readInteractiveConfig import"
      pattern: "require\\('./checkpoints'\\)"
    - from: "bin/grd-tools.ts"
      to: "lib/research/cli.ts"
      via: "cmdResearchResume(opts.checkpointAnswers)"
      pattern: "cmdResearchResume"
    - from: "tests/unit/research/orchestrator.test.ts"
      to: "tests/fixtures/research-threads/*/thread.json"
      via: "resumeResearch bit-identical replay"
      pattern: "loadThread|resumeResearch"
---

<objective>
Wire the resume path: a checkpoint-answers branch in `resumeResearch`, `--answers <file|->` +
`--interactive`/`--no-interactive` flag plumbing (file/stdin only, never argv text), the
`ResearchResult.pendingCheckpoint` field, and the two proof tests — the 5-site caller-audit and
the 0.4.16 bit-identical resume.

Purpose: This closes REQ-198 and the phase exit criterion "all plumbing exists, wired to the 5
unattended call sites, ZERO behavior change with default config." The resume branch and the
runLoop `resumedCheckpoint` param are DORMANT this phase (nothing emits a checkpoint yet — locked
hybrid-churn strategy), but must be fully unit-testable via hand-constructed threads + DI.
Output: resumeResearch branch, CLI flags, ResearchResult field, caller-audit + back-compat tests.
Research basis: SUMMARY.md §4.3 (resume plumbing, consumeAnswered analog of approved.execute at
orchestrator.ts:410-414), ARCHITECTURE.md §2.5 (resume-with-answers branch, §1.4 existing resume
path :647-672), FEATURES.md F1 (resume without --answers = recommended defaults = timeout),
PITFALLS.md R1 (caller-audit across 5 sites) / R8 (file-or-stdin only) / R3 (bit-identical
fixture resume), CONTEXT.md (--no-interactive; --no-gates implies --no-interactive; grep-style
caller-audit so a 6th caller fails), REQ-198.
</objective>

<context>
@.planning/milestones/v0.5.0/research/ARCHITECTURE.md
@.planning/milestones/v0.5.0/research/PITFALLS.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-CONTEXT.md
@lib/research/orchestrator.ts
@lib/research/cli.ts
@bin/grd-tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: resumeResearch checkpoint-answers branch + ResearchResult/ResearchOptions/runLoop plumbing</name>
  <files>lib/research/orchestrator.ts</files>
  <action>
    In lib/research/orchestrator.ts:
    - Extend ResearchOptions (:60-70) with `checkpointAnswers?: Record<string, { label: string; text?: string }>`
      and a one-shot interactive override `interactive?: { enabled?: boolean; points?: string[] }`.
    - Extend ResearchResult (:72-81) with `pendingCheckpoint?: Checkpoint;` (import type Checkpoint
      from './types').
    - Add a branch in resumeResearch (:647-672) BEFORE the `const pending = thread.pendingGate`
      handling: if `thread.pendingCheckpoint` is set (pending), then
        require('./checkpoints') → resolveCheckpoint(cwd, thread, thread.pendingCheckpoint, answers)
        where answers = opts.checkpointAnswers, else undefined (resolveCheckpoint fills each
        question with its recommended option, answeredBy:'default' — the timeout behavior).
        This appends checkpoints.jsonl, clears thread.pendingCheckpoint, sets status 'active',
        saveThread; then continue into runLoop passing { resumedCheckpoint: <the resolved record> }.
      If opts.noGates is set, treat the checkpoint as skipped-to-defaults (do not require answers) —
      mirrors the existing `--no-gates` force at :658-660.
    - Thread the OPTIONAL `resumedCheckpoint` through runLoop's signature (:390-392) as a 5th-arg
      opts field or a dedicated param. It is DORMANT: no emission point consumes it this phase
      (zero emission call sites — locked). Add a one-line comment that consumeAnswered wiring lands
      in Phase 102. Do NOT add any checkpoint emission in runLoop.
    - Keep the existing pendingGate paths (:661-671) byte-identical for threads with no
      pendingCheckpoint.
    Zero `any`; import type Checkpoint; match existing require style.
  </action>
  <verify>
    `npm run build:check` clean; `npx jest tests/unit/research/orchestrator.test.ts` still green for existing gate-resume cases (Level 1: Sanity).
  </verify>
  <done>resumeResearch resolves a pending checkpoint (answers or defaults) and calls runLoop with a dormant resumedCheckpoint; ResearchResult.pendingCheckpoint exists; pendingGate paths unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: --answers <file|-> / --interactive / --no-interactive CLI plumbing</name>
  <files>bin/grd-tools.ts, lib/research/cli.ts</files>
  <action>
    In bin/grd-tools.ts research subcommand (:2258-2302):
    - Parse `--answers <file|->`: read the JSON answers object from the file, or from stdin when the
      value is `-` (fs.readFileSync(0,'utf8')). NEVER read answer text from argv (R8). Malformed/missing
      → leave opts.checkpointAnswers undefined (bare-resume defaults). Attach as opts.checkpointAnswers.
      Wire it into the `sub === 'resume'` path.
    - Parse `--interactive` (bare = one-shot enable; optional `--interactive=seed,design` value list →
      opts.interactive.points) and `--no-interactive` (one-shot disable). `--no-gates` implies
      `--no-interactive` (set opts.interactive.enabled=false when noGates). These are plumbed into
      ResearchOptions; they are DORMANT this phase (no emission consumes them) but must parse cleanly.
      Follow the existing flag-parse style (args.includes / indexOf) and the maxIterations skip logic
      so an --answers value is not swallowed into the question string on the start path.
    In lib/research/cli.ts cmdResearchResume (:29-34): pass opts.checkpointAnswers straight through to
    resumeResearch (opts already forwarded — confirm checkpointAnswers rides ResearchOptions).
    Keep --json→--raw JSON output behavior intact.
  </action>
  <verify>
    `npx jest tests/unit/research/cli.test.ts` passes (Level 1: Sanity).
    Manual: `node bin/grd-tools.js research resume nonexistent --answers -` errors cleanly (thread not found), does not crash on stdin parse.
  </verify>
  <done>--answers <file|-> (file/stdin only), --interactive (+=list), --no-interactive parse into ResearchOptions; --no-gates implies --no-interactive; cmdResearchResume forwards checkpointAnswers.</done>
</task>

<task type="auto">
  <name>Task 3: 5-site caller-audit test + 0.4.16 bit-identical resume test</name>
  <files>tests/unit/research/orchestrator.test.ts, tests/unit/research/cli.test.ts</files>
  <action>
    Caller-audit test (PITFALLS R1 / CONTEXT "grep-style discovery so a NEW 6th caller fails"):
    - Enumerate the 5 known runResearch/resumeResearch call sites by grepping lib/research for
      `runResearch|resumeResearch` imports/usages and assert the discovered set of source files
      equals exactly { portfolio.ts, bench.ts, cli.ts, cli-kb.ts, index.ts } (index.ts re-exports).
      A 6th file surfacing fails the test until it declares an interactive posture. (Implement as a
      deterministic fs+regex scan over lib/research/*.ts, excluding orchestrator.ts's own defs and
      test files.)
    - For the unattended paths, assert completion with interactive forced ON but skipped: build a
      config with research_gates.interactive.enabled=true, then verify resolveInteractive(...,{noGates:true})
      and resolveInteractive(...,{/*autonomous*/}) / concurrency>1 report inactive (import from
      checkpoints.ts) — i.e. no unattended path can pause interactively.

    Bit-identical resume test (PITFALLS R3 / uses 101-01 fixtures):
    - Copy each frozen fixture into a mktemp thread dir; run resumeResearch with injected
      spawn/runner/retrieve stubs (repo DI convention) sufficient to exercise the resume ENTRY only.
    - paused-execute-0416: assert resumeResearch enters the existing pendingGate:'execute' branch
      (NOT the new checkpoint branch — pendingCheckpoint is undefined) and that thread.json after the
      pendingGate-clear step matches the pre-0.5.0 expectations (status active, pendingGate consumed).
    - terminal-supported-0416: assert the TERMINAL short-circuit returns unchanged (no re-run).
    - Assert neither fixture ever touches the checkpoint branch or writes checkpoints.jsonl.

    Resume-with-answers unit test (hand-built thread, no emission needed):
    - Construct a thread.json WITH a pending Checkpoint (design approval, one question, one
      recommended option). resumeResearch with opts.checkpointAnswers={q1:{label:'Approve & run'}} →
      assert checkpoints.jsonl gets one appended record with answeredBy:'human', pendingCheckpoint
      cleared, status active. Bare resume (no --answers) → assert the record resolves to the
      recommended option with answeredBy:'default' (timeout behavior). Stub runLoop (inject) so the
      test asserts the resume ENTRY only.
  </action>
  <verify>
    `TMPDIR=$(mktemp -d) npx jest tests/unit/research/orchestrator.test.ts tests/unit/research/cli.test.ts` passes and holds orchestrator/portfolio/bench thresholds (Level 1: Sanity).
    Caller-audit set == exactly the 5 files; both fixtures resume without hitting the checkpoint branch.
  </verify>
  <done>Caller-audit enumerates exactly 5 sites (6th fails); both 0.4.16 fixtures resume bit-identically via the existing pendingGate path; resume-with-answers records answers (human) / defaults (bare) to checkpoints.jsonl.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): orchestrator/cli tests green; tsc + lint clean; no per-file threshold lowered; caller-audit == 5 sites; fixtures resume bit-identically.
Level 2 (Proxy): resume-with-answers records to checkpoints.jsonl and bare resume falls back to recommended defaults — the deterministic timeout behavior with no wall-clock timer.
Level 3 (Deferred): live end-to-end checkpoint emit→pause→resume — Phase 102; the full R1/R3/R4/R5 milestone suite — Phase 105 (REQ-209).
</verification>

<success_criteria>
- resumeResearch resolves pending checkpoints (answers→human, bare→default) before pendingGate handling; runLoop carries a dormant resumedCheckpoint.
- gd research resume --answers <file|-> reads file/stdin only (never argv); --interactive/--no-interactive parse; --no-gates implies --no-interactive.
- ResearchResult.pendingCheckpoint exists; cmdResearchResume forwards checkpointAnswers.
- Caller-audit proves exactly 5 unattended sites; both 0.4.16 fixtures resume bit-identically; zero behavior change under default config.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-04-SUMMARY.md`
</output>
