---
phase: 101-checkpoint-core-plumbing-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/research/types.ts
  - lib/research/thread.ts
  - tests/unit/research/types.test.ts
  - tests/unit/research/thread.test.ts
  - tests/fixtures/research-threads/paused-execute-0416/thread.json
  - tests/fixtures/research-threads/terminal-supported-0416/thread.json
autonomous: true
verification_level: sanity

hypothesis: "Adding the Checkpoint type family and three OPTIONAL ResearchThread fields (pendingCheckpoint?/refinedQuestion?/checkpointRounds?) in lib/research/types.ts, while leaving the pendingGate union and ThreadStatus closed, keeps loadThread/saveThread byte-identical for any pre-0.5.0 thread.json."
predicted_outcome: "tsc --noEmit passes; a frozen 0.4.16 thread.json fixture parses via loadThread and re-serializes via JSON.stringify(thread,null,2) byte-identically; renderThreadLog omits the checkpoint line when pendingCheckpoint is absent."

must_haves:
  truths:
    - "lib/research/types.ts exports a Checkpoint interface with checkpoint_version:1, id pattern ck-<iter>-<point>-r<round>, point 'seed'|'hypothesize'|'design'|'decide', type 'clarification'|'selection'|'approval'|'branch', iteration, round, createdAt, optional context, questions[] (each {id, ask, options:[{label,description,recommended?}], freeform?}), optional answers[] ({questionId,label,text?,answeredBy:'human'|'panel'|'default'}), optional resolvedAt/discussionFile."
    - "ResearchThread gains ONLY optional pendingCheckpoint?: Checkpoint|null, refinedQuestion?: string, checkpointRounds?: Partial<Record<CheckpointPoint,number>> — pendingGate union and ThreadStatus are UNCHANGED (status:'paused' reused)."
    - "An InteractiveConfig interface exists for the resolved research_gates.interactive shape (enabled, per-point seed/hypothesize/design/decide, max_rounds, max_questions, hypothesis_candidates, every_iteration, fallback)."
    - "renderThreadLog renders a guarded '- **pending checkpoint:** <point> (<n> questions)' line ONLY when thread.pendingCheckpoint is set; output is byte-identical to today when it is absent."
    - "A frozen pre-0.5.0 (0.4.16) thread.json fixture loads via loadThread and re-serializes byte-identically (proves optional-field back-compat)."
  artifacts:
    - path: "lib/research/types.ts"
      provides: "Checkpoint type family + optional ResearchThread fields + InteractiveConfig type"
      contains: "checkpoint_version"
    - path: "lib/research/thread.ts"
      provides: "renderThreadLog guarded checkpoint line"
    - path: "tests/fixtures/research-threads/paused-execute-0416/thread.json"
      provides: "0.4.16 thread paused at the execute gate (pendingGate:'execute')"
    - path: "tests/fixtures/research-threads/terminal-supported-0416/thread.json"
      provides: "0.4.16 terminal (status:'supported') thread"
    - path: "tests/unit/research/types.test.ts"
      provides: "Checkpoint/InteractiveConfig type-shape + fixture back-compat assertions"
  key_links:
    - from: "lib/research/types.ts"
      to: "lib/research/thread.ts"
      via: "ResearchThread.pendingCheckpoint consumed by renderThreadLog"
      pattern: "pendingCheckpoint"
    - from: "tests/unit/research/thread.test.ts"
      to: "tests/fixtures/research-threads/*/thread.json"
      via: "loadThread round-trip"
      pattern: "loadThread|JSON.stringify"
---

<objective>
Add the typed Checkpoint schema and the three additive OPTIONAL ResearchThread fields
to `lib/research/types.ts`, plus a guarded checkpoint line in `renderThreadLog`, and
freeze two real 0.4.16-generated thread fixtures that prove back-compat.

Purpose: This is the foundation every later v0.5.0 phase builds on. It ships STANDALONE —
no emission call sites are added in orchestrator.ts this phase (locked hybrid-churn
strategy). The exit criterion is ZERO behavior change under default config, so the entire
value of this plan is: new types compile, all fields optional, old threads resume
bit-identically.
Output: Checkpoint types, InteractiveConfig type, optional thread fields, guarded log line,
two frozen fixtures.
Research basis: SUMMARY.md §4.2 (schema, "do NOT widen pendingGate"), ARCHITECTURE.md §2.2
(optional-field back-compat), PITFALLS.md R3 (bare JSON.parse loadThread, closed unions,
TERMINAL mirrors), REQ-194.
</objective>

<context>
@.planning/STATE.md
@.planning/milestones/v0.5.0/research/SUMMARY.md
@.planning/milestones/v0.5.0/research/ARCHITECTURE.md
@.planning/milestones/v0.5.0/research/PITFALLS.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-CONTEXT.md
@lib/research/types.ts
@lib/research/thread.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Checkpoint type family + optional ResearchThread fields + InteractiveConfig</name>
  <files>lib/research/types.ts</files>
  <action>
    In lib/research/types.ts (the module that already owns ResearchThread, ThreadGates,
    Station, ThreadStatus, defaultGates — grounding overrides REQ-194's loose "lib/types.ts"
    reference; Checkpoint MUST co-locate with ResearchThread because ResearchThread.pendingCheckpoint
    references it). Follow existing code style: 'use strict', CommonJS, zero `any`, exported
    interfaces/types.

    Add these types (SUMMARY.md §4.2 + FEATURES.md F1 schema are the locked source of truth;
    where they differ prefer SUMMARY.md):
    - `export type CheckpointPoint = 'seed' | 'hypothesize' | 'design' | 'decide';`
    - `export type CheckpointType = 'clarification' | 'selection' | 'approval' | 'branch';`
    - `export type CheckpointAnsweredBy = 'human' | 'panel' | 'default';`
    - `export interface CheckpointOption { label: string; description: string; recommended?: boolean; }`
    - `export interface CheckpointQuestion { id: string; ask: string; options: CheckpointOption[]; freeform?: boolean; }`
    - `export interface CheckpointAnswer { questionId: string; label: string; text?: string; answeredBy: CheckpointAnsweredBy; }`
    - `export interface Checkpoint { checkpoint_version: 1; id: string; point: CheckpointPoint; type: CheckpointType; iteration: number; round: number; createdAt: string; context?: string; questions: CheckpointQuestion[]; answers?: CheckpointAnswer[]; resolvedAt?: string; discussionFile?: string; }`
      - id format is `ck-<iteration>-<point>-r<round>` (documented in a comment; construction lives in checkpoints.ts, plan 101-02).
      - Comment the question `id` as a per-checkpoint label NOT stable across rounds (plan-phase precedent); dedupe key across rounds is the `ask` TEXT.
    - `export interface InteractiveConfig { enabled: boolean; seed: boolean; hypothesize: boolean; design: boolean; decide: boolean; max_rounds: number; max_questions: number; hypothesis_candidates: number; every_iteration: boolean; fallback: 'recommended' | 'panel'; }`
      (the RESOLVED shape readInteractiveConfig returns in 101-02; declared here as the shared type.)

    Extend ResearchThread with ONLY these optional fields (append after existing optionals like
    errorReason so field order for existing threads is preserved on serialize):
    - `pendingCheckpoint?: Checkpoint | null;`
    - `refinedQuestion?: string;`
    - `checkpointRounds?: Partial<Record<CheckpointPoint, number>>;`

    DO NOT touch: the `pendingGate: 'execute' | 'kg_write' | null` union, ThreadStatus,
    ThreadGates ({execute,kg_write} stays 2-key — interactive config is nested under
    research_gates and read separately, NOT stored on the thread), or defaultGates().
    PITFALLS R3.2/R3.3: widening pendingGate or adding a ThreadStatus would break the
    hardcoded TERMINAL mirrors in portfolio.ts:74 and paper.ts:15 — do not.
  </action>
  <verify>
    `npm run build:check` (tsc --noEmit) passes (Level 1: Sanity).
    `npx tsc --noEmit` shows no error referencing pendingGate/ThreadStatus widening.
  </verify>
  <done>Checkpoint family, CheckpointAnsweredBy, InteractiveConfig, and three optional ResearchThread fields exist and compile; pendingGate union and ThreadStatus unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Guarded checkpoint line in renderThreadLog</name>
  <files>lib/research/thread.ts</files>
  <action>
    In renderThreadLog (lib/research/thread.ts:69-86), add a guarded line rendered ONLY when
    `t.pendingCheckpoint` is set, mirroring the existing conditional-spread style used for
    seededFrom/resurveyCount/errorReason:
      `...(t.pendingCheckpoint ? [`- **pending checkpoint:** ${t.pendingCheckpoint.point} (${t.pendingCheckpoint.questions.length} questions)`] : []),`
    Place it directly after the existing `- **pending gate:** ...` line. When pendingCheckpoint
    is absent (every thread this phase, since nothing emits), output MUST be byte-identical to
    today. No other change to thread.ts.
  </action>
  <verify>
    `npx jest tests/unit/research/thread.test.ts` passes (Level 1: Sanity).
  </verify>
  <done>renderThreadLog emits the checkpoint line only when pendingCheckpoint is present; existing threads render unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze 0.4.16 thread fixtures + back-compat round-trip test</name>
  <files>tests/fixtures/research-threads/paused-execute-0416/thread.json, tests/fixtures/research-threads/terminal-supported-0416/thread.json, tests/unit/research/types.test.ts, tests/unit/research/thread.test.ts</files>
  <action>
    Produce two frozen pre-0.5.0 thread.json fixtures (CONTEXT locked decision: "Freeze real
    0.4.16-generated threads … scrub machine-specific paths … never regenerated"):
    1. PREFERRED: in a `mktemp -d` sandbox OUTSIDE the repo (CLAUDE.md gotcha — never create
       research artifacts in the repo), check out the 0.4.16 release (tag/commit 3c179fe) and
       run `gd research` far enough to (a) pause at the execute gate and (b) reach a terminal
       'supported' thread; copy each `thread.json`.
    2. FALLBACK (deterministic, if backends/sandbox unavailable): hand-author each thread.json
       to EXACTLY match 0.4.16's `saveThread` serialization — `JSON.stringify(thread, null, 2)`
       over the 0.4.16 ResearchThread shape (fields in createThread insertion order: id,
       question, status, iteration, maxIterations, baseMaxIterations, gates:{execute,kg_write},
       budgetUsed, modelProfile, tokenProfile, currentStation, pendingGate, createdAt). This is
       equivalent for the back-compat property because the fixture's job is to prove NO new
       required field exists.
    Scrub any absolute/machine paths. The paused fixture: `status:'paused'`, `pendingGate:'execute'`,
    `currentStation:'run'`. The terminal fixture: `status:'supported'`. Neither has
    pendingCheckpoint/refinedQuestion/checkpointRounds keys.

    Add a test in tests/unit/research/thread.test.ts (and/or types.test.ts):
    - `loadThread`-style read of each fixture (read file, JSON.parse as ResearchThread) succeeds
      and yields the expected status/pendingGate with pendingCheckpoint === undefined.
    - Re-serialize with `JSON.stringify(thread, null, 2)` and assert it EQUALS the raw fixture
      bytes (bit-identical round-trip) — the R3 back-compat proof at the type level.
    Add a test in tests/unit/research/types.test.ts asserting a fully-formed Checkpoint literal
    (checkpoint_version:1, one question with exactly one recommended option) type-checks and a
    ResearchThread literal with pendingCheckpoint compiles.
  </action>
  <verify>
    `TMPDIR=$(mktemp -d) npx jest tests/unit/research/types.test.ts tests/unit/research/thread.test.ts` passes (Level 1: Sanity).
    Fixture byte-identical round-trip assertion passes.
  </verify>
  <done>Two frozen 0.4.16 fixtures checked into tests/fixtures/; back-compat round-trip test proves optional-field additions do not perturb old thread.json serialization.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` passes; `npx jest tests/unit/research/types.test.ts tests/unit/research/thread.test.ts` green; fixture round-trip is byte-identical.
Level 2 (Proxy): n/a — pure type + fixture plumbing, no runtime metric.
Level 3 (Deferred): full-loop resume of the fixtures through resumeResearch is validated in plan 101-04.
</verification>

<success_criteria>
- Checkpoint type family + InteractiveConfig exported from lib/research/types.ts; tsc clean.
- ResearchThread has exactly three new OPTIONAL fields; pendingGate union + ThreadStatus untouched.
- renderThreadLog checkpoint line is guarded and byte-identical-when-absent.
- Two frozen 0.4.16 fixtures round-trip bit-identically.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-01-SUMMARY.md`
</output>
