---
phase: 101-checkpoint-core-plumbing-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/research/gates.ts
  - lib/research/bench.ts
  - lib/commands/config.ts
  - tests/unit/research/gates.test.ts
  - tests/unit/research/bench.test.ts
  - tests/unit/commands/config.test.ts
autonomous: true
verification_level: sanity

hypothesis: "Deriving resolveGates' no-gates return from the single defaultGates() source, pinning research_gates.interactive.enabled:false in BENCH_WORKDIR_CONFIG, and preserving unknown research_gates keys through the settings save/restore path together guarantee no future gate can silently default-on for an unattended caller."
predicted_outcome: "gates.test proves resolveGates(_,true) zeroes every key of defaultGates(); bench.test asserts BENCH_WORKDIR_CONFIG pins interactive off; config round-trip test proves a research_gates.interactive nested object survives settings save/restore; all existing thresholds still met."

must_haves:
  truths:
    - "resolveGates(config, noGates=true) returns an all-false object derived from a single defaultGates() source (Object.keys(defaultGates()) → false), not a hardcoded {execute:false,kg_write:false} literal — so a future gate cannot be forgotten (R1 mitigation 2)."
    - "resolveGates default-config behavior is byte-identical to today: execute = experiment_execution!==false, kg_write = kg_write!==false (the interactive keys live in research_gates.interactive and are NOT part of ThreadGates)."
    - "BENCH_WORKDIR_CONFIG pins research_gates.interactive:{enabled:false} (belt-and-braces alongside the existing experiment_execution:false/kg_write:false and noGates:true)."
    - "The gd settings / autonomous-mode save-restore path preserves unknown nested research_gates keys (e.g. research_gates.interactive) across a round-trip (R7)."
  artifacts:
    - path: "lib/research/gates.ts"
      provides: "resolveGates single-source all-off refactor"
      contains: "defaultGates"
    - path: "lib/research/bench.ts"
      provides: "BENCH_WORKDIR_CONFIG interactive pin"
      contains: "interactive"
    - path: "lib/commands/config.ts"
      provides: "unknown-research_gates-key-preserving settings round-trip"
    - path: "tests/unit/research/gates.test.ts"
      provides: "all-off single-source assertion + unchanged-default assertion"
  key_links:
    - from: "lib/research/gates.ts"
      to: "lib/research/types.ts"
      via: "defaultGates() as the single off-source"
      pattern: "defaultGates"
    - from: "lib/research/bench.ts"
      to: "BENCH_WORKDIR_CONFIG"
      via: "frozen research_gates.interactive pin"
      pattern: "interactive"
---

<objective>
Land the default-OFF gate-safety guarantees: refactor `resolveGates(noGates)` to zero ALL gates
from the single `defaultGates()` source, pin `research_gates.interactive` off in
`BENCH_WORKDIR_CONFIG`, and make the settings save/restore path preserve unknown nested
`research_gates` keys.

Purpose: This is the R1 (Critical) + R7 (Medium) safety layer. It has NO dependency on the
Checkpoint type — it touches only gates.ts, bench.ts, and the settings config path — so it runs
in Wave 1 parallel with 101-01. Zero behavior change under default config; the value is purely
"no future gate can hang an unattended caller."
Output: gates.ts single-source refactor, bench pin, config round-trip preservation, tests.
Research basis: PITFALLS.md R1 (resolveGates hardcodes {execute,kg_write}; must derive from one
source; pin bench belt-and-braces) and R7 (settings must round-trip unknown research_gates keys),
SUMMARY.md §2 (C2 resolution), CONTEXT.md (warn+clamp posture; keep exported API back-compatible),
REQ-197/REQ-196.
</objective>

<context>
@.planning/milestones/v0.5.0/research/PITFALLS.md
@.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-CONTEXT.md
@lib/research/gates.ts
@lib/research/bench.ts
@lib/commands/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolveGates single-source all-off refactor</name>
  <files>lib/research/gates.ts, tests/unit/research/gates.test.ts</files>
  <action>
    In lib/research/gates.ts, change resolveGates so the noGates path derives its all-false
    object from the single defaultGates() source instead of the hardcoded literal:
      `import { defaultGates } from './types'` (typed require, matching repo style — thread.ts:6
      already does `const { defaultGates } = require('./types') as { defaultGates: () => ThreadGates }`).
      `if (noGates) { const on = defaultGates(); return Object.fromEntries(Object.keys(on).map((k) => [k, false])) as ThreadGates; }`
    Leave the default-config path unchanged: `execute: rg.experiment_execution !== false,
    kg_write: rg.kg_write !== false`. ThreadGates stays {execute,kg_write} — the interactive keys
    are NOT gates on the thread (they are read separately by readInteractiveConfig, 101-02). Keep
    the exported checkGate untouched and the exported resolveGates signature backward-compatible
    (CONTEXT: "keep the exported API backward-compatible; single-source all gate defaults").

    Add tests to tests/unit/research/gates.test.ts:
    - resolveGates(anything, true) returns { execute:false, kg_write:false } AND equals
      Object.fromEntries(Object.keys(defaultGates()).map(k=>[k,false])) (single-source proof —
      this test will automatically cover any future gate added to defaultGates).
    - Default-config resolveGates matches today (execute/kg_write !== false semantics) for
      {}, {experiment_execution:false}, {kg_write:false}.
  </action>
  <verify>
    `npx jest tests/unit/research/gates.test.ts` passes and meets the gates.ts threshold (90/100/80) (Level 1: Sanity).
    `npm run build:check` clean.
  </verify>
  <done>resolveGates zeroes all gates from defaultGates(); default-config behavior byte-identical; gates.ts threshold held.</done>
</task>

<task type="auto">
  <name>Task 2: Pin research_gates.interactive off in BENCH_WORKDIR_CONFIG</name>
  <files>lib/research/bench.ts, tests/unit/research/bench.test.ts</files>
  <action>
    In lib/research/bench.ts, extend the frozen BENCH_WORKDIR_CONFIG (line ~119) so its
    research_gates also pins interactive off (belt-and-braces alongside the existing
    experiment_execution:false / kg_write:false and the noGates:true passed at the run site ~359):
      `research_gates: { experiment_execution: false, kg_write: false, interactive: { enabled: false } },`
    Update the adjacent comment ("both gates off (belt-and-braces with noGates)") to mention the
    interactive pin. No other bench.ts change.

    Add/extend a test in tests/unit/research/bench.test.ts asserting BENCH_WORKDIR_CONFIG.research_gates.interactive.enabled === false (and that the object is still frozen).
  </action>
  <verify>
    `npx jest tests/unit/research/bench.test.ts` passes and meets bench.ts threshold (95/100/85) (Level 1: Sanity).
  </verify>
  <done>BENCH_WORKDIR_CONFIG pins interactive.enabled:false; bench threshold held.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve unknown research_gates keys through settings save/restore</name>
  <files>lib/commands/config.ts, tests/unit/commands/config.test.ts</files>
  <action>
    R7: gd settings + autonomous-mode save/restore (the _saved_research_gates mechanism visible in
    .planning/config.json) must NOT drop a nested research_gates.interactive object. Investigate the
    research_gates read/write path in lib/commands/config.ts (typed research_gates at :39 and the
    default at :158) plus wherever autonomous_mode toggling snapshots research_gates into
    _saved_research_gates and restores it. If the serialization is a typed narrow (only the 3 typed
    keys survive), widen the round-trip to preserve unknown keys — e.g. spread the raw parsed
    research_gates object through save and restore rather than reconstructing from the typed subset.
    Keep the change minimal and backward-compatible (Claude's Discretion on exact mechanism).

    Add a test to tests/unit/commands/config.test.ts (create if absent) that writes a config whose
    research_gates includes an `interactive: { enabled: true, design: false }` nested object, runs
    the settings save-and-restore (or autonomous-mode toggle) path, re-reads the config, and
    asserts the nested interactive object is preserved verbatim.
  </action>
  <verify>
    `npx jest tests/unit/commands/config.test.ts` passes (Level 1: Sanity).
    Round-trip test proves research_gates.interactive survives save/restore.
    `npm run lint` clean on lib/commands/config.ts.
  </verify>
  <done>Settings save/restore preserves unknown nested research_gates keys; round-trip test green.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): gates/bench/config tests green; tsc + lint clean; no existing per-file threshold lowered.
Level 2 (Proxy): resolveGates(_,true) single-source proof is the deterministic guarantee that the R1 caller-audit (101-04) then verifies end-to-end across all 5 call sites.
Level 3 (Deferred): live bench/portfolio run with interactive forced on in config — validated by the 101-04 caller-audit and Phase 105 milestone suite.
</verification>

<success_criteria>
- resolveGates noGates path derives all-false from defaultGates(); default-config behavior unchanged.
- BENCH_WORKDIR_CONFIG pins research_gates.interactive.enabled:false.
- Settings save/restore preserves an unknown research_gates.interactive nested key.
- All existing thresholds held.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.0/phases/101-checkpoint-core-plumbing-config/101-03-SUMMARY.md`
</output>
