---
phase: 92-cfg-formalization
plan: 03
type: execute
wave: 2
depends_on: [92-01]
files_modified: [tests/unit/invariants.test.ts, jest.config.js]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tests/unit/invariants.test.ts has 25+ test cases covering validateStructural, validateSemantic, validateCrossPhase, validateResearchArtifacts, and extractPlanArtifact"
    - "validateStructural tests cover: valid plan passes, missing objective rejected, empty files_modified rejected, non-number wave rejected, non-boolean autonomous rejected, empty strings rejected"
    - "validateSemantic tests cover: valid paths pass, absolute paths rejected, .. traversal rejected, missing extensions warned"
    - "validateCrossPhase tests cover: valid plan set passes, duplicate provides rejected, unsatisfied requires rejected, empty provides/requires warned"
    - "validateResearchArtifacts tests cover: missing files OK, LANDSCAPE without table rows errored, RESEARCH without ## Method errored, valid research passes"
    - "extractPlanArtifact tests cover: valid plan parsed, missing frontmatter returns defaults, missing objective returns empty string"
    - "jest.config.js has coverage threshold for lib/invariants.ts at 90% lines, 90% functions, 90% statements, 85% branches"
    - "npm test passes with all thresholds met"
  artifacts:
    - path: "tests/unit/invariants.test.ts"
      provides: "Comprehensive unit tests for lib/invariants.ts"
    - path: "jest.config.js"
      provides: "Coverage threshold for lib/invariants.ts"
  key_links:
    - from: "tests/unit/invariants.test.ts"
      to: "lib/invariants.ts"
      via: "imports all five functions"
      pattern: "validateStructural|validateSemantic|validateCrossPhase|validateResearchArtifacts|extractPlanArtifact"
---

<objective>
Write comprehensive unit tests for all validation functions in lib/invariants.ts and add coverage thresholds to jest.config.js.

Purpose: Satisfy REQ-181 (CFG Validation Tests) — 90%+ line coverage on lib/invariants.ts with edge case testing.
Output: tests/unit/invariants.test.ts with 25+ test cases, jest.config.js updated.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/invariants.ts
@lib/types.ts
@tests/unit/gates.test.ts
@tests/helpers/fixtures.ts
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/invariants.test.ts with 25+ test cases</name>
  <files>tests/unit/invariants.test.ts</files>
  <action>
    Create test file following the established pattern from gates.test.ts:
    - Use `createFixtureDir` / `cleanupFixtureDir` for filesystem tests
    - Use `require('../../lib/invariants')` to import all five functions

    **Test groups:**

    **describe('extractPlanArtifact')** (3+ tests):
    - Valid plan content with frontmatter and objective tag -> returns correct PlanArtifact
    - Content with no frontmatter -> returns defaults (empty strings/arrays)
    - Content with frontmatter but no objective tag -> objective is empty string

    **describe('validateStructural')** (6+ tests):
    - Valid PlanArtifact -> {valid: true, errors: [], warnings: [...]}
    - Missing objective (empty string) -> error
    - Empty files_modified array -> error
    - Wave as 0 or negative -> error
    - Autonomous not boolean (string 'true') -> error
    - Empty type -> error
    - Multiple errors at once -> all reported

    **describe('validateSemantic')** (5+ tests):
    - Valid file paths -> passes
    - Absolute path (/etc/passwd) -> error
    - Path with .. traversal -> error
    - File path without extension -> warning
    - Objective referencing known lib/ module -> no warning about module reference

    **describe('validateCrossPhase')** (5+ tests):
    - Valid plan set with matching provides/requires -> passes
    - Duplicate provides across two plans -> error
    - Unsatisfied requires (no plan provides it) -> error
    - All plans have empty provides/requires -> warning
    - Single plan with no cross-phase issues -> passes

    **describe('validateResearchArtifacts')** (6+ tests):
    - Non-existent directory -> passes (no research artifacts to validate)
    - Directory with valid LANDSCAPE.md (has table) -> passes
    - LANDSCAPE.md without table rows -> error
    - RESEARCH.md without ## Method section -> error
    - RESEARCH.md without ## Tradeoffs section -> error
    - Valid RESEARCH.md with both sections -> passes

    For filesystem-dependent tests (validateResearchArtifacts), use tmpDir with fs.mkdirSync/writeFileSync.
    For pure-logic tests (structural, semantic, crossPhase), construct PlanArtifact objects inline.
  </action>
  <verify>npx jest tests/unit/invariants.test.ts --no-coverage</verify>
  <done>All 25+ tests pass with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage threshold for lib/invariants.ts to jest.config.js</name>
  <files>jest.config.js</files>
  <action>
    Add coverage threshold entry for lib/invariants.ts in the coverageThreshold object in jest.config.js:

    ```javascript
    './lib/invariants.ts': { lines: 90, functions: 90, statements: 90, branches: 85 },
    ```

    Place it alphabetically among the existing entries (after `./lib/gates.ts`).
  </action>
  <verify>npm test</verify>
  <done>npm test passes with coverage thresholds met for lib/invariants.ts (90%+ lines)</done>
</task>

</tasks>

<verification>
```bash
npm test
npx jest tests/unit/invariants.test.ts --coverage
```
Expected: all tests green, lib/invariants.ts >= 90% lines coverage.
</verification>

<success_criteria>
- [ ] tests/unit/invariants.test.ts has 25+ test cases
- [ ] All five functions tested with valid and invalid inputs
- [ ] Edge cases covered: empty fields, null values, missing keys
- [ ] jest.config.js has 90% coverage threshold for lib/invariants.ts
- [ ] npm test passes with all thresholds met
</success_criteria>
