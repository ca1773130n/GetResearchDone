---
phase: 96-closed-loop-metric-driven-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/grd-critique-agent.md
  - lib/autopilot.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "grd-critique-agent.md defines a post-phase critique agent with three branch protocols (macro, geometry, generative)"
    - "Agent frontmatter sets effort: low, maxTurns: 20, tools: Read, Bash, Grep, Glob"
    - "Agent instructions describe iterative refinement: collect metrics -> classify branch -> apply fix -> re-measure"
    - "buildCritiqueAgentPrompt in autopilot.ts constructs a prompt referencing the critique agent definition and passing branch/metrics context"
    - "runRefinementLoop in autopilot.ts implements the iterative spawn loop with convergence check"
  artifacts:
    - path: "agents/grd-critique-agent.md"
      provides: "Critique agent definition with three refinement branch protocols"
    - path: "lib/autopilot.ts"
      provides: "buildCritiqueAgentPrompt and runRefinementLoop functions"
      exports: ["buildCritiqueAgentPrompt", "runRefinementLoop"]
  key_links:
    - from: "lib/autopilot.ts"
      to: "agents/grd-critique-agent.md"
      via: "fs.existsSync check before spawning"
      pattern: "grd-critique-agent"
    - from: "lib/autopilot.ts"
      to: "lib/refinement.ts"
      via: "require for collectMetrics, checkConvergence, classifyBranch, buildCritiquePrompt"
      pattern: "require.*refinement"
---

<objective>
Create the grd-critique-agent definition and add the iterative refinement loop to autopilot.ts.

Purpose: The critique agent is the actor that receives a classified refinement branch and applies targeted fixes. The refinement loop in autopilot.ts orchestrates the closed-loop: collect metrics, classify branch, spawn critique agent, re-measure, check convergence. This adapts NERFIFY's 3-branch refinement (Macro/Geometry/Generative) into GRD's domain.

Output: Agent definition file and two new functions in autopilot.ts.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/autopilot.ts
@lib/refinement.ts
@agents/grd-knowledge-miner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grd-critique-agent.md agent definition</name>
  <files>agents/grd-critique-agent.md</files>
  <action>
    Create agents/grd-critique-agent.md following the established agent definition pattern (see grd-knowledge-miner.md for structure). The agent receives a critique prompt with a classified branch and current metrics, then applies targeted fixes.

    Frontmatter:
    - name: grd-critique-agent
    - description: Post-phase critique agent that applies targeted refinements based on metric-driven branch classification. Implements three refinement strategies: Macro (coverage recovery), Geometry (type error resolution), Generative (lint pattern fixes).
    - tools: Read, Write, Bash, Grep, Glob
    - color: red
    - effort: low
    - maxTurns: 20

    Sections to include:

    **<role>**: You are a GRD critique agent. You receive a classified refinement branch (macro/geometry/generative) with current project metrics and apply targeted fixes to improve the identified weak dimension. You operate within a closed refinement loop — your changes will be re-measured automatically.

    **<branch_protocols>**: Three subsections:

    1. **Macro (metric-minima guided patching)**: Adapted from NERFIFY PSNR-minima ROI analysis.
       - Analyze test coverage report to find files with lowest coverage
       - Prioritize files identified in minima regions (coverage dips between phases)
       - Add missing test cases for uncovered branches/lines
       - Focus on high-ROI: files frequently modified but poorly covered

    2. **Geometry (structural validation)**: Adapted from NERFIFY geometry branch.
       - Parse tsc error output to categorize errors by type (missing types, wrong assignability, missing exports)
       - Fix type errors systematically: start with leaf modules (no dependents), work toward root
       - Check export consistency: every module.exports key has a matching function/const
       - Verify import chains are correct

    3. **Generative (artifact analysis)**: Adapted from NERFIFY VLM-guided analysis.
       - Parse eslint output to cluster violations by rule
       - Fix violations by cluster (e.g., all no-unused-vars together, all no-explicit-any together)
       - Identify code patterns that generate lint violations and refactor the pattern, not just individual instances

    **<constraints>**:
    - Maximum 5 files modified per iteration (keep changes focused)
    - Run npm run build:check after every change to avoid regression
    - Never lower coverage thresholds in jest.config.js
    - Never disable eslint rules — fix the code, not the config

    **<output_format>**: Emit a structured summary block:
    ```
    CRITIQUE-RESULT
    branch: {macro|geometry|generative}
    files_modified: [list]
    metrics_before: { coverage: N%, type_errors: N, lint: N }
    metrics_after: { coverage: N%, type_errors: N, lint: N }
    END-CRITIQUE-RESULT
    ```
  </action>
  <verify>
    cat agents/grd-critique-agent.md | head -8  # Verify frontmatter
    grep -c "branch_protocols\|Macro\|Geometry\|Generative" agents/grd-critique-agent.md  # At least 4 matches
  </verify>
  <done>grd-critique-agent.md exists with valid frontmatter (effort: low, maxTurns: 20), three branch protocols, constraints, and structured output format</done>
</task>

<task type="auto">
  <name>Task 2: Add buildCritiqueAgentPrompt and runRefinementLoop to autopilot.ts</name>
  <files>lib/autopilot.ts</files>
  <action>
    Add two new functions to lib/autopilot.ts, following the same patterns as buildKnowledgeMiningPrompt and runKnowledgeMining:

    **buildCritiqueAgentPrompt(phaseNum: string, branch: CritiqueBranch, metrics: RefinementMetrics, targets: RefinementMetrics, minimaRegions: MinimaRegion[]): string**
    - Import CritiqueBranch, RefinementMetrics, MinimaRegion, ConvergenceConfig from types
    - Import buildCritiquePrompt from lib/refinement.ts
    - Wrap the refinement prompt in the standard agent invocation format
    - Include: "You are the grd-critique-agent. Your branch is {branch}."
    - Include metric context from buildCritiquePrompt
    - Include phase number for file path context

    **runRefinementLoop(cwd: string, phaseNum: string, options: { scheduler?: Scheduler | null, log: (msg: string) => void, maxIterations?: number, targets?: RefinementMetrics }): Promise<void>**
    - Check for agents/grd-critique-agent.md existence; skip if absent (same pattern as runKnowledgeMining)
    - Import collectMetrics, checkConvergence, classifyBranch, detectMinima from lib/refinement.ts
    - Default convergence config: epsilon_coverage: 0.5, epsilon_type_errors: 0, epsilon_lint: 1, max_iterations: maxIterations ?? 3
    - Default targets: test_coverage_pct: 80, type_error_count: 0, lint_violation_count: 0
    - Loop:
      1. Run `npm test -- --coverage` via spawnStep, capture output
      2. Run `npm run build:check` via spawnStep, capture output
      3. Run `npm run lint` via spawnStep, capture output
      4. Call collectMetrics with the three outputs
      5. Push MetricSnapshot to history array
      6. Call checkConvergence(history, convergenceConfig)
      7. If converged: writeStatusMarker, log, break
      8. Call classifyBranch(currentMetrics, targets) and detectMinima(history)
      9. Build critique prompt via buildCritiqueAgentPrompt
      10. Spawn critique agent via spawnStep
      11. writeStatusMarker for each iteration
    - Non-blocking: wrap entire loop in try/catch, log failures, never reject
    - Write status markers: 'refinement-loop' with 'started', 'iteration-N', 'converged'/'max-iterations'/'failed'

    Add both functions to the module.exports at the bottom of autopilot.ts.
  </action>
  <verify>
    npm run build:check 2>&1 | tail -5  # Type check passes
    npm run lint 2>&1 | tail -5  # Lint passes
    grep "runRefinementLoop\|buildCritiqueAgentPrompt" lib/autopilot.ts | wc -l  # At least 4 matches (def + export)
  </verify>
  <done>buildCritiqueAgentPrompt and runRefinementLoop added to autopilot.ts, exported, build and lint pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): agents/grd-critique-agent.md exists with valid frontmatter, lib/autopilot.ts exports new functions
Level 2 (Proxy): npm run build:check passes, npm run lint passes, grep confirms function definitions and exports
</verification>

<success_criteria>
- agents/grd-critique-agent.md has three branch protocols, structured output format, appropriate effort/maxTurns
- buildCritiqueAgentPrompt constructs a valid critique prompt with branch and metrics
- runRefinementLoop implements iterative collection -> classification -> spawning -> convergence check
- Both functions exported from autopilot.ts
- npm run build:check and npm run lint pass
</success_criteria>

<output>
After completion, create `${phase_dir}/96-02-SUMMARY.md`
</output>
