---
phase: 96-closed-loop-metric-driven-refinement
plan: 03
type: execute
wave: 2
depends_on: ["96-01", "96-02"]
files_modified:
  - lib/autopilot.ts
  - tests/unit/autopilot.test.ts
  - tests/unit/refinement.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "runRefinementLoop is called in the autopilot post-phase pipeline after knowledge mining and before the post-pipeline steps"
    - "Refinement loop is non-blocking — failures do not halt autopilot execution"
    - "Refinement loop is skippable via config flag or missing agent definition"
    - "Autopilot test suite covers runRefinementLoop integration (mock spawnStep, verify convergence path and max-iteration path)"
    - "Refinement test suite covers buildCritiquePrompt output for all three branches"
    - "All existing tests continue to pass — no regressions"
  artifacts:
    - path: "lib/autopilot.ts"
      provides: "Refinement loop wired into autopilot execute-wave sequence"
    - path: "tests/unit/autopilot.test.ts"
      provides: "Integration tests for runRefinementLoop"
    - path: "tests/unit/refinement.test.ts"
      provides: "Additional tests for buildCritiquePrompt branch variants"
  key_links:
    - from: "lib/autopilot.ts"
      to: "lib/refinement.ts"
      via: "require and function calls in execute-wave sequence"
      pattern: "runRefinementLoop"
    - from: "tests/unit/autopilot.test.ts"
      to: "lib/autopilot.ts"
      via: "jest mock of spawnStep and refinement module"
      pattern: "runRefinementLoop"
---

<objective>
Wire the refinement loop into the autopilot post-phase pipeline and add integration tests covering the full closed-loop flow.

Purpose: Connects the refinement engine (plan 01) and critique agent (plan 02) into the production autopilot flow. After knowledge mining completes for a phase, the refinement loop runs to iteratively improve metrics before the post-pipeline (simplify/PR/review/merge) begins. This closes the loop: execute phase -> mine knowledge -> refine metrics -> post-pipeline.

Output: Updated autopilot.ts with wired refinement, comprehensive tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/autopilot.ts
@lib/refinement.ts
@tests/unit/autopilot.test.ts
@${phase_dir}/96-01-SUMMARY.md
@${phase_dir}/96-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runRefinementLoop into autopilot execute-wave sequence and add config flag</name>
  <files>lib/autopilot.ts, lib/types.ts, lib/utils.ts</files>
  <action>
    Wire the refinement loop into autopilot.ts at the point after knowledge mining and before the post-phase pipeline launch. Follow the exact pattern used by runKnowledgeMining:

    1. In the execute-wave sequence (around line 1600, after `await runKnowledgeMining(...)`), add:
    ```typescript
    // Refinement loop (non-blocking — runRefinementLoop never rejects)
    await runRefinementLoop(cwd, task.phaseNum, { scheduler, log });
    ```

    2. Add a `refinement_loop` config flag:
       - In lib/types.ts GrdConfig: add `refinement_loop?: boolean` with doc comment "When true, run post-phase metric-driven refinement loop. Default: false"
       - In lib/utils.ts KNOWN_CONFIG_KEYS: add 'refinement_loop'
       - In runRefinementLoop: check `loadConfig(cwd).refinement_loop !== true` as an early-exit (skip when not explicitly enabled, same as citation_gate pattern)

    3. Ensure the refinement loop placement is:
       - AFTER knowledge mining (knowledge entries should be written before refinement touches code)
       - BEFORE post-phase pipeline launch (so refinements are included in the simplify/PR/review/merge flow)

    4. Run npm run build:check and npm run lint to verify no regressions.
  </action>
  <verify>
    npm run build:check 2>&1 | tail -3
    npm run lint 2>&1 | tail -3
    grep -n "runRefinementLoop" lib/autopilot.ts  # Shows wiring point and function definition
  </verify>
  <done>runRefinementLoop wired after knowledge mining in autopilot execute-wave, config flag added, build and lint pass</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for refinement loop and buildCritiquePrompt branch coverage</name>
  <files>tests/unit/autopilot.test.ts, tests/unit/refinement.test.ts</files>
  <action>
    Add tests to both test files:

    **tests/unit/refinement.test.ts** — add a new describe block for buildCritiquePrompt:
    - Test 'macro' branch: prompt contains "coverage" and "macro" and metric values
    - Test 'geometry' branch: prompt contains "type error" and "geometry"
    - Test 'generative' branch: prompt contains "lint" and "generative"
    - Test with empty minimaRegions: still produces valid prompt
    - Test with multiple minimaRegions: includes top 3 regions

    **tests/unit/autopilot.test.ts** — add a new describe block for runRefinementLoop:
    - Mock the refinement module (jest.mock('../../../lib/refinement')) to control collectMetrics, checkConvergence, classifyBranch, detectMinima, buildCritiquePrompt return values
    - Mock spawnStep/spawnClaudeAsync to avoid real subprocess spawning
    - Mock fs.existsSync for agent definition check
    - Mock loadConfig to return { refinement_loop: true } or { refinement_loop: false }

    Test cases:
    1. "skips when agent definition missing" — fs.existsSync returns false, verify runRefinementLoop returns without spawning
    2. "skips when refinement_loop config is false" — verify early exit
    3. "converges after 2 iterations" — mock checkConvergence to return false then true, verify spawnStep called twice for critique agent
    4. "stops at max_iterations" — mock checkConvergence to always return false, verify loop stops at default max (3)
    5. "catches errors without rejecting" — mock spawnStep to throw, verify runRefinementLoop resolves (non-blocking)
    6. "writes status markers" — verify writeStatusMarker called with 'refinement-loop' and appropriate statuses

    Run the full test suite to verify no regressions:
    npm test 2>&1 | tail -20
  </action>
  <verify>
    npx jest tests/unit/refinement.test.ts --coverage 2>&1 | grep -E "Tests:|PASS|refinement"
    npx jest tests/unit/autopilot.test.ts 2>&1 | grep -E "Tests:|PASS|runRefinementLoop"
    npm test 2>&1 | tail -5  # Full suite passes
  </verify>
  <done>buildCritiquePrompt branch tests added (5 tests), runRefinementLoop integration tests added (6 tests), full test suite passes with no regressions</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): npm run build:check passes, grep confirms runRefinementLoop in autopilot execute-wave
Level 2 (Proxy): npm test passes with no regressions, new tests cover convergence/max-iterations/skip/error paths
</verification>

<success_criteria>
- runRefinementLoop is called in autopilot execute-wave after knowledge mining and before post-pipeline
- refinement_loop config flag controls enablement (default: false, must opt-in)
- 6 new autopilot tests cover: skip (agent missing), skip (config false), converge, max iterations, error handling, status markers
- 5 new refinement tests cover: all three buildCritiquePrompt branches, empty/multiple minimaRegions
- Full test suite (npm test) passes with no regressions
- npm run build:check and npm run lint pass
</success_criteria>

<output>
After completion, create `${phase_dir}/96-03-SUMMARY.md`
</output>
