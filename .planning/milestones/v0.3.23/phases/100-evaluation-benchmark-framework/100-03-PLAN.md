---
phase: 100-evaluation-benchmark-framework
plan: 03
type: execute
wave: 2
depends_on: ["100-01", "100-02"]
files_modified:
  - agents/grd-eval-planner.md
  - agents/grd-eval-reporter.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "grd-eval-planner agent defines role, tools, and execution flow for planning benchmark evaluations"
    - "grd-eval-reporter agent defines role, tools, and execution flow for reporting benchmark results"
    - "grd-eval-planner references lib/benchmark.ts loadCorpus and evaluateEntry functions"
    - "grd-eval-reporter references lib/benchmark.ts formatBenchmarkReport function"
    - "Both agents follow existing agent markdown conventions (frontmatter with name, description, tools, effort)"
    - "Both agents reference the IntegrationCategory taxonomy for corpus organization"
  artifacts:
    - path: "agents/grd-eval-planner.md"
      provides: "Agent definition for benchmark evaluation planning"
      contains: "loadCorpus"
    - path: "agents/grd-eval-reporter.md"
      provides: "Agent definition for benchmark result reporting"
      contains: "formatBenchmarkReport"
  key_links:
    - from: "agents/grd-eval-planner.md"
      to: "lib/benchmark.ts"
      via: "references loadCorpus, classifyEntry, evaluateEntry for evaluation orchestration"
      pattern: "benchmark"
    - from: "agents/grd-eval-reporter.md"
      to: "lib/benchmark.ts"
      via: "references formatBenchmarkReport, loadCorpus for result aggregation"
      pattern: "benchmark"
---

<objective>
Create grd-eval-planner and grd-eval-reporter agent definitions that integrate with the benchmark module.

Purpose: These agents close the loop between benchmark corpus management and evaluation reporting. The eval-planner orchestrates which benchmark entries to evaluate and how, while the eval-reporter aggregates results into actionable reports. Both reference lib/benchmark.ts functions established in Plans 01-02.

Output: Two agent markdown files in agents/ directory following existing GRD agent conventions.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@agents/grd-planner.md
@agents/grd-verifier.md
@.planning/milestones/v0.3.23/phases/100-evaluation-benchmark-framework/100-01-SUMMARY.md
@.planning/milestones/v0.3.23/phases/100-evaluation-benchmark-framework/100-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend grd-eval-planner agent definition with benchmark integration</name>
  <files>agents/grd-eval-planner.md</files>
  <action>
    Extend agents/grd-eval-planner.md (file already exists with evaluation-tier role definitions — preserve all existing content) by adding a benchmark integration section. Follow the existing agent convention (see agents/grd-planner.md for format reference).

    Frontmatter:
    - name: grd-eval-planner
    - description: Plans benchmark evaluations by selecting corpus entries, configuring scoring rubrics, and orchestrating evaluation runs against paper-to-code synthesis outputs.
    - tools: Read, Bash, Glob, Grep
    - effort: medium

    Role section:
    - Plans evaluation runs against the benchmark corpus
    - Selects which BenchmarkEntry items to evaluate (filter by category, tags, recency)
    - Configures ScoringRubric overrides if needed (default rubric via createDefaultRubric)
    - Orchestrates evaluation by calling evaluateEntry for each selected entry
    - Manages corpus updates via saveCorpusEntry when new papers are added

    Execution flow:
    1. Load corpus: call loadCorpus from lib/benchmark.ts to get available entries
    2. Filter entries by requested criteria (category, tags, or "all")
    3. For each entry: gather semantic summary (from prior agent evaluation or manual input), build output, run output
    4. Call evaluateEntry to produce BenchmarkResult for each
    5. Collect all results and hand off to grd-eval-reporter

    Reference the IntegrationCategory taxonomy:
    - directly-integrable: papers whose methods can be implemented from the paper alone
    - requires-external-models: papers needing pretrained weights or foundation models
    - out-of-scope: papers requiring hardware/proprietary resources
    - novelty-coverage: papers whose primary contribution is a novel technique

    Include corpus management guidance:
    - Corpus stored as JSON files in .planning/benchmark/corpus/
    - Each file is one BenchmarkEntry (named {id}.json)
    - Results stored in .planning/benchmark/results/
  </action>
  <verify>
    File exists at agents/grd-eval-planner.md with valid YAML frontmatter (name, description, tools, effort fields).
    Contains references to loadCorpus, evaluateEntry, IntegrationCategory.
    Existing evaluation-tier content is preserved (not overwritten).
  </verify>
  <done>grd-eval-planner.md extended with benchmark integration section, existing content preserved</done>
</task>

<task type="auto">
  <name>Task 2: Extend grd-eval-reporter agent definition with benchmark reporting</name>
  <files>agents/grd-eval-reporter.md</files>
  <action>
    Extend agents/grd-eval-reporter.md (file already exists with evaluation-tier role definitions — preserve all existing content) by adding a benchmark reporting section. Follow existing agent conventions.

    Frontmatter:
    - name: grd-eval-reporter
    - description: Aggregates benchmark evaluation results into structured reports with per-category breakdowns, trend analysis, and actionable improvement recommendations.
    - tools: Read, Bash, Glob, Grep, Write
    - effort: low

    Role section:
    - Consumes BenchmarkResult[] from evaluation runs
    - Generates formatted reports using formatBenchmarkReport from lib/benchmark.ts
    - Provides per-category analysis (how well does synthesis perform for each IntegrationCategory?)
    - Identifies weakest areas and suggests improvement priorities
    - Tracks evaluation trends across multiple runs (if historical results available)

    Execution flow:
    1. Load results from .planning/benchmark/results/ directory
    2. Load corpus entries via loadCorpus for metadata lookup
    3. Call formatBenchmarkReport to generate the base report
    4. Enhance with per-category breakdown:
       - Group results by IntegrationCategory
       - Compute average composite score per category
       - Identify best/worst performing entries per category
    5. Add trend section (if prior EVAL.md reports exist):
       - Compare current average composite vs. prior runs
       - Note improving/declining categories
    6. Write report to phase EVAL.md or .planning/benchmark/REPORT.md

    Report structure guidance:
    - H1: Benchmark Evaluation Report
    - H2: Summary (entry count, overall average, run timestamp)
    - H2: Results Table (from formatBenchmarkReport)
    - H2: Category Breakdown (per-category averages and insights)
    - H2: Improvement Priorities (weakest areas, suggested next steps)
    - H2: Trends (if historical data available)
  </action>
  <verify>
    File exists at agents/grd-eval-reporter.md with valid YAML frontmatter (name, description, tools, effort fields).
    Contains references to formatBenchmarkReport, loadCorpus, IntegrationCategory.
    Existing evaluation-tier content is preserved (not overwritten).
  </verify>
  <done>grd-eval-reporter.md extended with benchmark reporting section, existing content preserved</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Both agent files exist in agents/ directory with valid YAML frontmatter. References to lib/benchmark.ts functions present.
</verification>

<success_criteria>
- agents/grd-eval-planner.md exists with valid frontmatter and references loadCorpus, evaluateEntry, IntegrationCategory
- agents/grd-eval-reporter.md exists with valid frontmatter and references formatBenchmarkReport, loadCorpus
- Both agents follow existing GRD agent markdown conventions
- Agent responsibilities are clearly separated (planner = orchestration, reporter = aggregation)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.23/phases/100-evaluation-benchmark-framework/100-03-SUMMARY.md`
</output>
