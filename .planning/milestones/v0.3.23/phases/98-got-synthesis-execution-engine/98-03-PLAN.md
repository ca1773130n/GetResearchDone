---
phase: 98-got-synthesis-execution-engine
plan: 03
type: standard
phase_type: implement
verification_level: proxy
autonomous: true
wave: 3
provides:
  - "tests/unit/got.test.ts:test-suite"
requires:
  - "lib/got.ts:freezeInterfaces"
  - "lib/got.ts:executeArtifactDAG"
  - "lib/got.ts:buildNodePrompt"
  - "lib/got.ts:runSmokeTest"
  - "lib/autopilot.ts:buildWavesFromPlans"
files_modified:
  - tests/unit/got.test.ts
  - jest.config.js
---

<objective>
Create comprehensive unit tests for the GoT execution engine (lib/got.ts) and add coverage thresholds.

Target: 15 new tests in got.test.ts covering all 4 exported functions.
Also add got.ts coverage threshold to jest.config.js (>= 80% lines).

Output: tests/unit/got.test.ts with 15 tests + jest.config.js updated.
</objective>

<context>
@lib/got.ts
@lib/types.ts
@lib/deps.ts
@tests/unit/deps.test.ts
@jest.config.js
</context>

<tasks>

<task id="01" type="auto" tdd="false">
  <name>Create tests/unit/got.test.ts with freezeInterfaces tests</name>
  <description>
  Create `tests/unit/got.test.ts` following the project test pattern (see deps.test.ts for style).

  Import: freezeInterfaces, executeArtifactDAG, buildNodePrompt, runSmokeTest from lib/got.
  Import: buildArtifactDAG from lib/deps (to build test DAGs).
  Import types as needed.

  Create a `makePlan` helper (similar to deps.test.ts) for creating test PlanArtifact objects.

  **describe('freezeInterfaces')** — 4 tests:
  1. `returns empty array for empty DAG` — freezeInterfaces on buildArtifactDAG([]) returns []
  2. `creates contract for each provides artifact` — 1 plan with 2 provides → 2 FrozenInterface objects
  3. `includes plan_id and artifact in contract string` — verify contract string contains the plan_id and artifact name
  4. `handles multiple plans with provides` — 3 plans, each with 1 provide → 3 FrozenInterface objects with correct plan_ids
  </description>
  <done_criteria>
  - tests/unit/got.test.ts created
  - 4 freezeInterfaces tests pass
  </done_criteria>
</task>

<task id="02" type="auto" tdd="false">
  <name>Add buildNodePrompt tests</name>
  <description>
  **describe('buildNodePrompt')** — 3 tests:
  1. `includes node provides and requires in prompt` — verify prompt string contains the artifact names
  2. `includes frozen contracts for required artifacts` — build a DAG with 2 plans (A provides X, B requires X), freeze interfaces, call buildNodePrompt for B → prompt contains frozen contract for X
  3. `includes phase context paths` — verify prompt contains phase_name and phase_dir from context
  </description>
  <done_criteria>
  - 3 buildNodePrompt tests pass
  </done_criteria>
</task>

<task id="03" type="auto" tdd="false">
  <name>Add runSmokeTest tests</name>
  <description>
  **describe('runSmokeTest')** — 3 tests:
  1. `passes when all provides are in artifacts_produced` — node provides ['X', 'Y'], result has both → passed: true, missing: []
  2. `fails when artifacts are missing` — node provides ['X', 'Y'], result has only ['X'] → passed: false, missing: ['Y']
  3. `fails when execution failed` — result.success = false → passed: false regardless of artifacts
  </description>
  <done_criteria>
  - 3 runSmokeTest tests pass
  </done_criteria>
</task>

<task id="04" type="auto" tdd="false">
  <name>Add executeArtifactDAG tests</name>
  <description>
  **describe('executeArtifactDAG')** — 5 tests:
  1. `returns empty result for empty DAG` — executeArtifactDAG on empty DAG → waves: [], results: [], success: true
  2. `groups independent nodes into same wave` — 2 plans with no deps → 1 wave with both
  3. `groups dependent nodes into sequential waves` — A provides X, B requires X → wave 0: [A], wave 1: [B]
  4. `dryRun produces stub success results` — verify all results have success: true and artifacts match provides
  5. `handles diamond dependency correctly` — A→(B,C)→D pattern → A in wave 0, B+C in wave 1, D in wave 2
  </description>
  <done_criteria>
  - 5 executeArtifactDAG tests pass
  - All 15 tests pass total
  </done_criteria>
</task>

<task id="05" type="auto" tdd="false">
  <name>Add got.ts coverage threshold to jest.config.js</name>
  <description>
  Add coverage threshold for `./lib/got.ts` in jest.config.js coverageThreshold section:
  ```
  './lib/got.ts': { lines: 80, functions: 85, branches: 70 },
  ```
  Place it alphabetically among the other lib entries.

  Then run the full test suite to verify:
  - All 15 new tests pass
  - Coverage thresholds met
  - No regression in existing tests
  </description>
  <done_criteria>
  - jest.config.js has got.ts coverage threshold
  - `npm test` passes with all tests green
  - got.ts coverage meets threshold (>= 80% lines)
  - No regression in existing test suite
  </done_criteria>
</task>

</tasks>

<verification>
```bash
npx jest tests/unit/got.test.ts --no-coverage
npx jest tests/unit/got.test.ts --coverage --coverageReporters=text 2>&1 | grep "got.ts"
npm test
```
</verification>

<success_criteria>
- tests/unit/got.test.ts created with 15 passing tests
- got.ts coverage >= 80% lines in jest.config.js
- Full test suite passes with zero regressions
- `npm run build:check` passes
- `npm run lint` passes
</success_criteria>
