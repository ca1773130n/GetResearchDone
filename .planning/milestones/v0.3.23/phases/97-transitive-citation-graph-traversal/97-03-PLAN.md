---
phase: 97-transitive-citation-graph-traversal
plan: 03
type: execute
wave: 3
depends_on: ["97-01", "97-02"]
files_modified:
  - agents/grd-phase-researcher.md
  - lib/context/execute.ts
  - tests/integration/citations-pipeline.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "grd-phase-researcher.md Step 8 calls traverseCitationGraph after buildCitationGraph and processes unresolved_leaves"
    - "grd-phase-researcher.md Step 8 calls fetchExternalPaper for each unresolved leaf when transitive_citation_gate is enabled"
    - "cmdInitPlanPhase result includes transitive_citation_gate_enabled boolean from loadConfig"
    - "tests/integration/citations-pipeline.test.ts exists with exactly 4 tests"
    - "All 4 integration pipeline tests pass"
    - "Full test suite (citations + gates + integration) passes with no regressions"
  artifacts:
    - path: "agents/grd-phase-researcher.md"
      provides: "Transitive traversal step in citation recovery pass, transitive_citation_gate awareness"
    - path: "lib/context/execute.ts"
      provides: "transitive_citation_gate_enabled in cmdInitPlanPhase output"
    - path: "tests/integration/citations-pipeline.test.ts"
      provides: "4 end-to-end pipeline integration tests"
  key_links:
    - from: "agents/grd-phase-researcher.md"
      to: "lib/citations.ts"
      via: "traverseCitationGraph, fetchExternalPaper grd-tools.js calls"
      pattern: "agent document references"
    - from: "lib/context/execute.ts"
      to: "lib/utils.ts"
      via: "loadConfig transitive_citation_gate_enabled field"
      pattern: "config flag propagation"
---

<objective>
Wire the Phase 97 transitive traversal and auto-retrieval capabilities into the grd-phase-researcher agent document and expose the transitive_citation_gate_enabled flag from cmdInitPlanPhase. Add 4 integration pipeline tests that verify the end-to-end chain from buildCitationGraph through traverseCitationGraph and resolveTransitiveDeps.

Purpose: Wave 1 and Wave 2 delivered the implementation. Wave 3 makes it accessible: the researcher agent now knows to run transitive traversal during citation recovery, the plan-phase init context exposes whether the gate is enabled, and an integration test guards the full pipeline chain against regression.

Output: Updated grd-phase-researcher.md (Step 8 extended), updated lib/context/execute.ts (one new config field), new tests/integration/citations-pipeline.test.ts with 4 tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/grd-phase-researcher.md
@lib/context/execute.ts
@lib/citations.ts
@lib/gates.ts
@lib/utils.ts
@.planning/milestones/v0.3.23/phases/97-transitive-citation-graph-traversal/97-01-SUMMARY.md
@.planning/milestones/v0.3.23/phases/97-transitive-citation-graph-traversal/97-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend grd-phase-researcher.md Step 8 with transitive traversal and update cmdInitPlanPhase</name>
  <files>agents/grd-phase-researcher.md, lib/context/execute.ts</files>
  <action>
    **Part A — agents/grd-phase-researcher.md**

    Locate `## Step 8: Citation Recovery Pass` in the `<execution_flow>` section (around line 536). Extend the step by inserting a new sub-step after the existing step 2 (findUnresolved) and before the existing step 3 (for each unresolved node):

    Insert as new sub-step 3 (renumber the existing 3/4/5 to 4/5/6):

    ```markdown
    3. Run transitive citation graph traversal to discover indirect dependencies:
       ```bash
       node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js traverseCitationGraph "${research_dir}"
       ```
       This performs BFS traversal from all root nodes (default max_depth=3, max_nodes=50).
       The result's `unresolved_leaves` array contains papers that are transitive dependencies
       but not yet in PAPERS.md.

    3b. If `transitive_citation_gate` is enabled in project config, attempt auto-retrieval for each
       unresolved leaf node using fetchExternalPaper:
       ```bash
       node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js fetchExternalPaper "<slug>"
       ```
       For each leaf that resolves successfully, note the result in the `## Citation Recovery` section
       of RESEARCH.md. For leaves that cannot be resolved, log them as CITATION_UNRESOLVED_TRANSITIVE
       (warning severity — does not block planning).
    ```

    Also update sub-step 6 (formerly step 5) to add transitive_citation_gate awareness:

    In the sentence "check whether the `citation_gate` is active" — extend to also mention:
    "Additionally, if `transitive_citation_gate` is enabled and transitive unresolved leaves remain,
    the plan-phase gate will produce warning-severity CITATION_UNRESOLVED_TRANSITIVE violations
    (these are informational and do not block planning)."

    **Part B — lib/context/execute.ts**

    In the `cmdInitPlanPhase` function, locate the `result` object construction (the block starting around line 495 with `const result: Record<string, unknown> = {`).

    Add one new field after the `discussion_before_planning` / `discussion_enabled` block (around line 563):

    ```ts
    // Citation traversal config
    transitive_citation_gate_enabled: !!(config as unknown as Record<string, unknown>).transitive_citation_gate,
    ```

    This allows the plan-phase orchestrator to surface whether the transitive gate is active in its context output, so the researcher agent can read it from the init payload.

    Run: npm run build:check and npm run lint — both must pass with zero errors.
  </action>
  <verify>
    npm run build:check 2>&1 | tail -3
    npm run lint 2>&1 | tail -3
    Expected: zero errors in both commands.
    grep -n "transitive_citation_gate_enabled" lib/context/execute.ts
    Expected: 1 line found in the result block.
    grep -n "traverseCitationGraph\|transitive_citation_gate" agents/grd-phase-researcher.md
    Expected: at least 2 lines found (Step 8 additions).
  </verify>
  <done>grd-phase-researcher.md Step 8 extended with transitive traversal sub-steps; cmdInitPlanPhase includes transitive_citation_gate_enabled; build and lint pass</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/integration/citations-pipeline.test.ts with 4 integration tests</name>
  <files>tests/integration/citations-pipeline.test.ts</files>
  <action>
    Create tests/integration/citations-pipeline.test.ts as a CommonJS Jest test file using 'use strict'. The file tests the full pipeline chain: buildCitationGraph → traverseCitationGraph → resolveTransitiveDeps, using temporary directories and mock fixtures.

    **Test setup:**
    - `beforeEach`: Create a temp dir via `fs.mkdtempSync`, write minimal .md fixture files into it
    - `afterEach`: Remove the temp dir recursively

    **Test 1: full pipeline — build, traverse, resolve**
    - Write two .md files into the temp dir:
      - `a.md` with a missing_components section referencing paper `b` and paper `c`
      - `b.md` with a missing_components section referencing paper `d`
    - Call `buildCitationGraph(tempDir)` → verify nodes include a, b, c, d
    - Call `traverseCitationGraph(graph)` → verify `depth_reached >= 1`, `total_visited >= 2`
    - Call `resolveTransitiveDeps(graph)` → verify returned graph has all nodes from traversal merged

    **Test 2: cycle handling — BFS terminates without infinite loop**
    - Write two .md files with mutual references:
      - `x.md` missing_components referencing `y`
      - `y.md` missing_components referencing `x`
    - Call `buildCitationGraph`, then `traverseCitationGraph`
    - Assert: result is defined (no crash), `total_visited <= max_nodes`, no duplicate slugs in `visited_nodes`

    **Test 3: depth and node limiting**
    - Write a linear chain of .md files: `p1.md` → `p2.md` → `p3.md` → `p4.md` (each references the next)
    - Call `traverseCitationGraph(graph, { max_depth: 1, max_nodes: 100 })`
    - Assert: `depth_reached <= 1` (does not traverse beyond depth 1 constraint)
    - Call `traverseCitationGraph(graph, { max_depth: 100, max_nodes: 2 })`
    - Assert: `total_visited <= 2` (max_nodes limit respected)

    **Test 4: auto-retrieval failure handling — fetchExternalPaper null does not crash resolveTransitiveDeps**
    - Build a graph with one unresolved node (slug: 'unknown-paper')
    - Call `resolveTransitiveDeps(graph, { fetchFn: async () => null })` — mock fetch that always returns null
    - Assert: function completes without throwing, returned graph contains the unresolved node
    - Assert: the unresolved node has `resolved: false` (was not marked resolved by failed fetch)

    Import pattern (typed requires):
    ```ts
    const { buildCitationGraph, traverseCitationGraph, resolveTransitiveDeps } =
      require('../../lib/citations') as {
        buildCitationGraph: (dir: string) => CitationGraph;
        traverseCitationGraph: (g: CitationGraph, opts?: Partial<TraversalOptions>) => TraversalResult;
        resolveTransitiveDeps: (g: CitationGraph, opts?: Partial<TraversalOptions>) => CitationGraph;
      };
    ```

    Import types: `import type { CitationGraph, TraversalOptions, TraversalResult } from '../../lib/types';`

    Run the tests after writing:
    `npx jest tests/integration/citations-pipeline.test.ts --no-coverage --no-color 2>&1 | grep -E "Tests:|PASS|FAIL"`
    All 4 tests MUST pass.

    Then run the full regression check:
    `npx jest tests/unit/citations.test.ts tests/unit/gates.test.ts tests/integration/citations-pipeline.test.ts --no-coverage --no-color 2>&1 | grep -E "Tests:|Test Suites:|FAIL"`
    Expected: no FAIL lines, all three suites pass.
  </action>
  <verify>
    npx jest tests/integration/citations-pipeline.test.ts --no-coverage --no-color 2>&1 | grep -E "Tests:|Test Suites:"
    Expected: Test Suites: 1 passed, Tests: 4 passed, 4 total.
    npx jest tests/unit/citations.test.ts tests/unit/gates.test.ts tests/integration/citations-pipeline.test.ts --no-coverage --no-color 2>&1 | grep -E "FAIL|Tests:|Test Suites:"
    Expected: no FAIL; all three suites pass.
  </verify>
  <done>tests/integration/citations-pipeline.test.ts created with 4 passing tests; no regressions in citations or gates unit tests</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): grd-phase-researcher.md contains 'traverseCitationGraph' and 'fetchExternalPaper' in Step 8; lib/context/execute.ts contains 'transitive_citation_gate_enabled'; tests/integration/citations-pipeline.test.ts exists
Level 2 (Proxy): npx jest tests/integration/citations-pipeline.test.ts — 4/4 tests pass; full three-suite regression run passes with zero failures
</verification>

<success_criteria>
- grd-phase-researcher.md Step 8 instructs the researcher to run transitive traversal and auto-retrieval
- transitive_citation_gate_enabled surfaced in cmdInitPlanPhase output
- 4 integration tests cover: full pipeline, cycle handling, depth/node limiting, auto-retrieval failure
- Zero regressions in existing 50+ citations tests and 52+ gates tests
- npm run build:check and npm run lint pass
</success_criteria>

<output>
After completion, create `${phase_dir}/97-03-SUMMARY.md`
</output>
