---
phase: 94-graph-of-thought-synthesis
plan: 03
type: tdd
wave: 2
depends_on: ["94-01"]
files_modified: [tests/unit/deps.test.ts, tests/unit/parallel.test.ts]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Unit tests for buildArtifactDAG cover: empty input, single plan, multi-plan DAG construction, providers map correctness, topological sort order"
    - "Unit tests for validateArtifactDAG cover: valid acyclic graph, single cycle, multi-node cycle (A→B→C→A), missing dependency detection, unused provides warnings, duplicate provides warnings"
    - "Unit tests for buildWaves cover: plans with only depends_on (no DAG), plans with artifact DAG, mixed depends_on + artifact constraints, backward compatibility with empty provides/requires"
    - "Coverage on new code in lib/deps.ts is 85%+ lines"
    - "Coverage on new buildWaves code in lib/parallel.ts is 85%+ lines"
    - "All tests pass with npx jest tests/unit/deps.test.ts tests/unit/parallel.test.ts"
  artifacts:
    - path: "tests/unit/deps.test.ts"
      provides: "Test suite for buildArtifactDAG and validateArtifactDAG"
      min_lines: 150
    - path: "tests/unit/parallel.test.ts"
      provides: "Test suite for buildWaves with artifact DAG integration"
      min_lines: 100
  key_links:
    - from: "tests/unit/deps.test.ts"
      to: "lib/deps.ts"
      via: "imports and tests buildArtifactDAG, validateArtifactDAG"
      pattern: "buildArtifactDAG|validateArtifactDAG"
    - from: "tests/unit/parallel.test.ts"
      to: "lib/parallel.ts"
      via: "imports and tests buildWaves"
      pattern: "buildWaves"
---

<objective>
Add comprehensive unit tests for artifact DAG construction, validation, cycle detection, topological sorting, and wave builder integration. Also implement dependency context injection into executor prompts.

Purpose: Satisfy REQ-189 (GoT Synthesis Tests) and the dependency context injection part of REQ-188. Achieve 85%+ line coverage on new code in lib/deps.ts and lib/parallel.ts.
Output: Extended test files for deps and parallel modules, dependency context injection in executor flow.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/deps.ts
@lib/parallel.ts
@lib/types.ts
@tests/unit/deps.test.ts
@tests/unit/parallel.test.ts
@.planning/milestones/v0.3.23/phases/94-graph-of-thought-synthesis/94-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildArtifactDAG and validateArtifactDAG tests to tests/unit/deps.test.ts</name>
  <files>tests/unit/deps.test.ts</files>
  <action>
    Add a new `describe('buildArtifactDAG', ...)` block and `describe('validateArtifactDAG', ...)` block to the existing test file. Use the PlanArtifact interface to construct test fixtures.

    **Helper:** Create a `makePlan` factory function that returns a PlanArtifact with sensible defaults, overridable via partial argument:
    ```typescript
    function makePlan(overrides: Partial<PlanArtifact>): PlanArtifact {
      return {
        objective: 'test', files_modified: ['lib/test.ts'], phase: '94-graph-of-thought-synthesis',
        plan: 1, type: 'execute', wave: 1, depends_on: [], autonomous: true,
        provides: [], requires: [], integration_points: [],
        ...overrides,
      };
    }
    ```

    **buildArtifactDAG tests:**

    1. `returns empty DAG for empty input` — `buildArtifactDAG([])` returns `{ nodes: [], edges: [], sorted_plans: [], providers: {} }`
    2. `builds single-node DAG for one plan` — one plan with `provides: ["lib/foo.ts:bar"]`, no requires. Expect 1 node, 0 edges, sorted_plans has 1 entry, providers maps the artifact.
    3. `builds edges from requires to provides` — plan A provides `["lib/foo.ts:X"]`, plan B requires `["lib/foo.ts:X"]`. Expect 1 edge from B to A, sorted order is [A, B].
    4. `handles multiple provides and requires` — plan A provides `["X", "Y"]`, plan B requires `["X"]`, plan C requires `["Y", "X"]`. Expect 2 edges to A, sorted order starts with A.
    5. `providers map is correct` — 3 plans each providing different artifacts. Verify providers map has all 3 entries pointing to correct plan IDs.
    6. `topological sort is correct for diamond dependency` — A provides X, B provides Y (requires X), C provides Z (requires X), D requires Y and Z. Sorted: A first, D last.
    7. `handles plans with no provides or requires` — plans with empty arrays get nodes but no edges. They appear in sorted_plans.
    8. `integration_points create soft edges` — plan A provides `["X"]`, plan B has `integration_points: ["X"]`. Expect edge with type 'integration'.

    **validateArtifactDAG tests:**

    1. `valid for acyclic graph` — linear chain A→B→C. `valid: true`, no cycles, no missing.
    2. `detects simple two-node cycle` — A requires B's artifact, B requires A's artifact. Expect `valid: false`, cycles array non-empty.
    3. `detects multi-node cycle (A→B→C→A)` — three plans forming a cycle. Expect cycle path containing all three.
    4. `detects missing dependency` — plan requires artifact "X" but no plan provides "X". Expect missing_deps with `{ plan: plan_id, artifact: "X" }`.
    5. `warns on unused provides` — plan provides "X" but no plan requires "X". Expect warnings array non-empty.
    6. `warns on duplicate provides` — two plans both provide "X". Expect warnings about duplicate.
    7. `valid with no issues` — clean DAG returns `valid: true`, empty cycles, empty missing_deps.
  </action>
  <verify>npx jest tests/unit/deps.test.ts --coverage</verify>
  <done>All buildArtifactDAG and validateArtifactDAG tests pass; coverage on new code in lib/deps.ts is 85%+</done>
</task>

<task type="auto">
  <name>Task 2: Add buildWaves tests to tests/unit/parallel.test.ts</name>
  <files>tests/unit/parallel.test.ts</files>
  <action>
    Add a new `describe('buildWaves', ...)` block to the existing test file.

    Reuse the `makePlan` helper pattern (or import if shared). For tests that need an artifactDAG, construct it via `buildArtifactDAG` from lib/deps.ts or build a minimal object literal.

    **buildWaves tests:**

    1. `returns single wave for independent plans` — 3 plans with no depends_on and no requires. All in wave 1.
    2. `respects depends_on without artifact DAG` — plan B depends_on plan A. Expect wave 1: [A], wave 2: [B]. No artifactDAG provided (backward compatible).
    3. `respects artifact DAG constraints` — plan A provides "X", plan B requires "X". With artifactDAG, expect wave 1: [A], wave 2: [B].
    4. `merges depends_on and artifact DAG constraints` — plan B depends_on A via explicit depends_on AND requires A's artifact. Should still be wave 1: [A], wave 2: [B] (no duplication).
    5. `artifact DAG adds constraints beyond depends_on` — plan A, B, C. B depends_on A (explicit). C requires B's artifact (only via DAG, no explicit depends_on). Expect wave 1: [A], wave 2: [B], wave 3: [C].
    6. `handles empty plans array` — returns empty array.
    7. `backward compatible with plans missing provides/requires` — plans with empty provides/requires arrays are assigned waves based only on depends_on.
    8. `handles diamond dependency pattern` — A independent, B and C depend on A, D depends on B and C. Expect wave 1: [A], wave 2: [B, C], wave 3: [D].

    **Dependency context injection test:**

    9. `buildWaves wave assignments match plan wave frontmatter values` — verify that the wave numbers assigned by buildWaves are consistent (wave 1 plans have no unmet dependencies).
  </action>
  <verify>npx jest tests/unit/parallel.test.ts --coverage</verify>
  <done>All buildWaves tests pass; coverage on new buildWaves code in lib/parallel.ts is 85%+</done>
</task>

</tasks>

<verification>
```bash
npx jest tests/unit/deps.test.ts tests/unit/parallel.test.ts --coverage
npm run build:check
npm run lint
```
Expected: all tests pass, 85%+ coverage on new code in lib/deps.ts and lib/parallel.ts, build and lint pass.
</verification>

<success_criteria>
- [ ] buildArtifactDAG tests cover empty input, single plan, multi-plan, diamond dependency, providers map
- [ ] validateArtifactDAG tests cover acyclic, two-node cycle, multi-node cycle, missing deps, unused provides, duplicate provides
- [ ] buildWaves tests cover depends_on only, artifact DAG only, mixed, backward compatibility, diamond pattern
- [ ] 85%+ line coverage on new code in lib/deps.ts and lib/parallel.ts
- [ ] All tests pass, build:check and lint pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.23/phases/94-graph-of-thought-synthesis/94-03-SUMMARY.md`
</output>
