---
phase: 47-integration-regression-testing
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/worktree.test.js
  - tests/unit/agent-audit.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "WorktreeCreate hook is registered in plugin.json with correct command path and timeout"
    - "WorktreeRemove hook is registered in plugin.json with correct command path and timeout"
    - "Hook handlers are no-op when .planning/ directory does not exist (GRD inactive)"
    - "Hook handlers are no-op when branching_strategy is 'none'"
    - "WorktreeCreate handler renames branch to GRD convention when phase number is extractable from worktree path"
    - "WorktreeCreate handler does NOT rename branch already following GRD convention (grd/ prefix)"
    - "WorktreeRemove handler extracts phase and milestone metadata from GRD-pattern worktree paths"
    - "WorktreeRemove handler handles non-GRD paths and null paths gracefully without crash"
    - "All existing worktree.test.js tests pass without modification (zero regressions)"
  artifacts:
    - path: "tests/unit/worktree.test.js"
      provides: "Extended hook handler tests covering all edge cases"
    - path: "tests/unit/agent-audit.test.js"
      provides: "Extended plugin.json hook registration validation"
  key_links:
    - from: "tests/unit/worktree.test.js"
      to: "lib/worktree.js"
      via: "cmdWorktreeHookCreate and cmdWorktreeHookRemove imports"
      pattern: "require.*worktree"
    - from: "tests/unit/agent-audit.test.js"
      to: ".claude-plugin/plugin.json"
      via: "plugin.json hook registration validation"
      pattern: "require.*plugin"
---

<objective>
Validate that WorktreeCreate and WorktreeRemove hooks are correctly registered in plugin.json and that hook handlers behave correctly across all edge cases: GRD inactive, branching disabled, GRD-pattern paths, non-GRD paths, null paths, and already-compliant branch names.

Purpose: Fulfill success criteria #4 (hook registration and behavior verification).
Output: Extended test files validating hook registration and handler behavior.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/worktree.js
@.claude-plugin/plugin.json
@tests/unit/worktree.test.js
@tests/unit/agent-audit.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plugin.json hook registration tests</name>
  <files>tests/unit/agent-audit.test.js</files>
  <action>
    Extend `tests/unit/agent-audit.test.js` with a new describe block `'plugin.json hook registration'`:

    1. Load `.claude-plugin/plugin.json` using `require()` or `JSON.parse(fs.readFileSync(...))`.

    2. Test `'hooks object exists in plugin.json'`:
       - Verify `pluginJson.hooks` is defined and is an object

    3. Test `'WorktreeCreate hook is registered'`:
       - Verify `pluginJson.hooks.WorktreeCreate` is an array with at least one entry
       - Verify the first entry has a `hooks` array
       - Verify the first hook entry has `type: 'command'`
       - Verify the command string contains `worktree-hook-create`
       - Verify the command string contains `$WORKTREE_PATH` and `$WORKTREE_BRANCH`
       - Verify timeout is a number > 0

    4. Test `'WorktreeRemove hook is registered'`:
       - Same structure as WorktreeCreate but with `worktree-hook-remove`
       - Verify the command string contains `$WORKTREE_PATH` and `$WORKTREE_BRANCH`

    5. Test `'SessionStart hook is registered'`:
       - Verify `pluginJson.hooks.SessionStart` is an array
       - Verify it contains a command that references `verify-path-exists`

    6. Test `'all hooks have error suppression (2>/dev/null || true)'`:
       - For each hook in WorktreeCreate, WorktreeRemove, SessionStart: verify the command string ends with `2>/dev/null || true`
       - This ensures hooks never block Claude Code even on failure

    Use the plugin.json path relative to the project root: `path.join(__dirname, '..', '..', '.claude-plugin', 'plugin.json')`.
  </action>
  <verify>
    `npx jest tests/unit/agent-audit.test.js --verbose` passes with all new tests green (Level 1: Sanity)
  </verify>
  <done>Plugin.json hook registration validated: WorktreeCreate, WorktreeRemove, and SessionStart hooks all present with correct command templates, env variables, timeouts, and error suppression</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive hook handler edge-case tests</name>
  <files>tests/unit/worktree.test.js</files>
  <action>
    Extend `tests/unit/worktree.test.js` with a new describe block `'Phase 47: hook handler comprehensive edge cases'` AFTER the existing `cmdWorktreeHookRemove` describe block. Do NOT modify any existing tests.

    **cmdWorktreeHookCreate edge cases:**
    1. Test `'returns skipped when .planning directory does not exist'` — create a temp dir WITHOUT .planning, call cmdWorktreeHookCreate, verify `{ skipped: true, reason: 'no .planning directory' }`
    2. Test `'returns skipped when branching_strategy is none'` — set config.branching_strategy='none', call cmdWorktreeHookCreate, verify `{ skipped: true, reason: 'branching disabled' }`
    3. Test `'does not rename when branch already starts with grd/'` — call with branch='grd/v0.2.6/47-test', verify `renamed: false` and `reason` contains 'already follows GRD convention'
    4. Test `'attempts rename when worktree path ends with phase number'` — call with path ending in a number (e.g., `/tmp/worktree-47`) and a non-grd branch name, verify the handler attempts rename (output has `hooked: true`)
    5. Test `'handles empty string worktree path without crash'` — call with wtPath='', verify no crash, returns `hooked: true`
    6. Test `'handles empty string branch without crash'` — call with wtBranch='', verify no crash

    **cmdWorktreeHookRemove edge cases:**
    1. Test `'returns skipped when .planning directory does not exist'` — same pattern as create
    2. Test `'returns skipped when branching_strategy is none'` — same pattern as create
    3. Test `'extracts metadata from grd-worktree-v0.2.6-47 path format'` — verify phase_detected='47' and milestone_detected='v0.2.6'
    4. Test `'does not include metadata for path not matching GRD pattern'` — call with '/tmp/my-worktree', verify no phase_detected or milestone_detected
    5. Test `'handles undefined worktree path gracefully'` — call with undefined, verify no crash
    6. Test `'handles worktree path with special characters'` — call with path containing spaces or dashes, verify no crash

    Each test should use the existing `repoDir` setup from the beforeAll in the test file (or create its own temp fixture as needed). Follow the exact `captureOutput` + `JSON.parse` pattern used in the existing hook tests.
  </action>
  <verify>
    `npx jest tests/unit/worktree.test.js --verbose` passes with all existing and new tests green. New hook edge-case tests: at least 12 new tests. (Level 1: Sanity)
  </verify>
  <done>
    At least 12 new hook handler edge-case tests added. All existing worktree tests pass without modification (zero regressions). Hook handlers validated for GRD-inactive, branching-disabled, GRD-pattern paths, non-GRD paths, null/undefined/empty inputs.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/worktree.test.js tests/unit/agent-audit.test.js --verbose` all pass
- Existing worktree test count preserved (no tests removed or modified)
- plugin.json hook structure validated programmatically
- Hook handlers tested for all edge cases: inactive, disabled, GRD paths, non-GRD paths, null/empty inputs
</verification>

<success_criteria>
- Zero existing test regressions in worktree.test.js
- Plugin.json validated: WorktreeCreate, WorktreeRemove, SessionStart hooks all registered
- At least 12 new hook handler edge-case tests
- At least 6 new plugin.json registration tests
- All hooks verified to have error suppression suffix
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/47-integration-regression-testing/47-02-SUMMARY.md`
</output>
