---
phase: 47-integration-regression-testing
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/integration/worktree-parallel-e2e.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "All existing integration tests in worktree-parallel-e2e.test.js pass without modification (zero regressions)"
    - "New integration tests validate the native isolation flow end-to-end: cmdInitExecutePhase(claude backend) → isolation_mode='native' → buildParallelContext(nativeWorktreeAvailable=true) → phases have worktree_path=null and native_isolation=true"
    - "New integration tests validate the manual isolation flow end-to-end: cmdInitExecutePhase(codex backend) → isolation_mode='manual' → buildParallelContext(nativeWorktreeAvailable=false) → phases have worktree_path set and native_isolation=false"
    - "New integration tests validate that cmdInitExecuteParallel correctly wires native_worktree_isolation from backend capabilities into buildParallelContext"
    - "New integration tests validate the full pipeline: parallel independence check → context build → per-phase isolation mode consistency"
  artifacts:
    - path: "tests/integration/worktree-parallel-e2e.test.js"
      provides: "Cross-module integration tests for native and manual worktree isolation end-to-end"
  key_links:
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/context.js"
      via: "cmdInitExecutePhase import for isolation_mode verification"
      pattern: "require.*context"
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/parallel.js"
      via: "buildParallelContext and cmdInitExecuteParallel imports"
      pattern: "require.*parallel"
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/backend.js"
      via: "getBackendCapabilities for native isolation check"
      pattern: "require.*backend"
---

<objective>
Add cross-module integration tests that validate the complete native and manual worktree isolation pipelines end-to-end, ensuring that context.js, parallel.js, backend.js, and worktree.js all work together correctly for both Claude Code (native) and non-Claude-Code (manual) backends.

Purpose: Fulfill success criteria #1 (zero regressions on integration tests), #2 (native path end-to-end), and #3 (manual path end-to-end) at the integration level.
Output: Extended integration test file with new describe blocks for native/manual cross-module flows.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/context.js
@lib/parallel.js
@lib/backend.js
@lib/worktree.js
@tests/integration/worktree-parallel-e2e.test.js
@.planning/milestones/v0.2.6/phases/46-hybrid-worktree-execution/46-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing integration tests and verify zero regressions</name>
  <files>tests/integration/worktree-parallel-e2e.test.js</files>
  <action>
    Run the existing integration test suite:

    ```bash
    npx jest tests/integration/worktree-parallel-e2e.test.js --verbose 2>&1
    ```

    Verify ALL tests pass. Record exact test count and any warnings.
    Do NOT modify any existing tests.
  </action>
  <verify>
    `npx jest tests/integration/worktree-parallel-e2e.test.js` exits with code 0 (Level 1: Sanity)
  </verify>
  <done>All existing integration tests pass, regression baseline established</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-module native/manual isolation integration tests</name>
  <files>tests/integration/worktree-parallel-e2e.test.js</files>
  <action>
    Add a new top-level describe block `'Phase 47: Native vs Manual Isolation Integration'` at the END of the file. Do NOT modify any existing tests.

    Import additional modules at the top of the file (if not already imported):
    ```js
    const { cmdInitExecutePhase } = require('../../lib/context');
    const { getBackendCapabilities, BACKEND_CAPABILITIES } = require('../../lib/backend');
    ```

    **Test group 1: `'native isolation end-to-end (claude backend)'`**

    Create a fixture git repo with `config.json` containing `backend: 'claude'` and `branching_strategy: 'phase'`. Create phase directories for 2 phases.

    1. Test `'cmdInitExecutePhase reports native isolation for claude backend'`:
       - Call cmdInitExecutePhase with the fixture dir
       - Verify: `backend === 'claude'`, `native_worktree_available === true`, `isolation_mode === 'native'`, `main_repo_path` is a non-null string

    2. Test `'buildParallelContext with nativeWorktreeAvailable=true produces null worktree_path'`:
       - Call buildParallelContext with `{ nativeWorktreeAvailable: true }`
       - Verify: every phase context has `worktree_path === null` and `native_isolation === true`
       - Verify: every phase context still has a valid `worktree_branch` string (branch naming is not skipped)

    3. Test `'cmdInitExecuteParallel wires native isolation from capabilities into parallel context'`:
       - Override backend to 'claude' via config.json
       - Call cmdInitExecuteParallel
       - Verify the output phases have `native_isolation: true` and `worktree_path: null`

    **Test group 2: `'manual isolation end-to-end (codex backend)'`**

    Create a fixture git repo with `config.json` containing `backend: 'codex'` and `branching_strategy: 'phase'`.

    1. Test `'cmdInitExecutePhase reports manual isolation for codex backend'`:
       - Verify: `backend === 'codex'`, `native_worktree_available === false`, `isolation_mode === 'manual'`, `main_repo_path` is a non-null string

    2. Test `'buildParallelContext with nativeWorktreeAvailable=false pre-computes worktree_path'`:
       - Call buildParallelContext with `{ nativeWorktreeAvailable: false }`
       - Verify: every phase context has a non-null `worktree_path` string containing `.worktrees/grd-worktree-`
       - Verify: `native_isolation === false`

    3. Test `'cmdInitExecuteParallel wires manual isolation from capabilities into parallel context'`:
       - Override backend to 'codex' via config.json
       - Call cmdInitExecuteParallel
       - Verify output phases have `native_isolation: false` and `worktree_path` is set

    **Test group 3: `'isolation mode consistency across modules'`**

    1. Test `'branching_strategy=none produces isolation_mode=none regardless of backend'`:
       - Set config `backend: 'claude'`, `branching_strategy: 'none'`
       - Call cmdInitExecutePhase, verify `isolation_mode === 'none'` and `main_repo_path === null`

    2. Test `'all four backends have correct native_worktree_isolation capability'`:
       - Loop over ['claude', 'codex', 'gemini', 'opencode']
       - For each, call getBackendCapabilities and verify native_worktree_isolation matches expected value (true only for claude)

    3. Test `'native and manual paths agree between cmdInitExecutePhase and buildParallelContext'`:
       - For a claude-backend fixture: verify that cmdInitExecutePhase.native_worktree_available matches the value passed to buildParallelContext
       - For a codex-backend fixture: same verification
       - This confirms the two modules are wired together correctly

    Use the existing `createTestGitRepo()` helper or the `createFixtureDir()` helper from the file. Follow the same `captureOutput` pattern. Clean up temp directories in afterAll.
  </action>
  <verify>
    `npx jest tests/integration/worktree-parallel-e2e.test.js --verbose` passes with all tests green (existing + new). New test count: at least 9 new integration tests. (Level 1: Sanity)
  </verify>
  <done>
    At least 9 new cross-module integration tests added covering native (claude) and manual (codex) isolation end-to-end. All existing integration tests pass without modification. Context → Parallel → Worktree module wiring validated for both paths.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/integration/worktree-parallel-e2e.test.js --verbose` all pass
- Existing integration test count preserved (no tests removed or modified)
- Native path tested: claude backend → native_worktree_available=true → isolation_mode='native' → worktree_path=null in parallel context
- Manual path tested: codex backend → native_worktree_available=false → isolation_mode='manual' → worktree_path set in parallel context
- Cross-module consistency validated between context.js and parallel.js
</verification>

<success_criteria>
- Zero existing integration test regressions
- At least 9 new integration tests
- Native isolation pipeline validated end-to-end (context → parallel → per-phase flags)
- Manual isolation pipeline validated end-to-end (context → parallel → per-phase worktree paths)
- Module wiring consistency confirmed (cmdInitExecutePhase output matches buildParallelContext input)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/47-integration-regression-testing/47-03-SUMMARY.md`
</output>
