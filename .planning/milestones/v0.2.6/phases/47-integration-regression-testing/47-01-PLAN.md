---
phase: 47-integration-regression-testing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/context.test.js
  - tests/unit/parallel.test.js
  - tests/unit/backend.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "All existing tests in context.test.js, parallel.test.js, and backend.test.js pass without modification (zero regressions)"
    - "New test cases validate cmdInitExecutePhase returns native_worktree_available: true for claude backend and false for codex/gemini/opencode backends"
    - "New test cases validate isolation_mode is 'native' for claude+phase, 'manual' for codex+phase, 'none' for any+none"
    - "New test cases validate main_repo_path is a real filesystem path when branching enabled, null when disabled"
    - "New test cases validate buildParallelContext skips worktree_path when nativeWorktreeAvailable=true and pre-computes when false"
    - "New test cases validate buildParallelContext default behavior (no options) preserves backward compatibility"
  artifacts:
    - path: "tests/unit/context.test.js"
      provides: "Extended unit tests for native/manual isolation paths in cmdInitExecutePhase"
    - path: "tests/unit/parallel.test.js"
      provides: "Extended unit tests for native/manual isolation in buildParallelContext"
    - path: "tests/unit/backend.test.js"
      provides: "Extended unit tests confirming BACKEND_CAPABILITIES.native_worktree_isolation per backend"
  key_links:
    - from: "tests/unit/context.test.js"
      to: "lib/context.js"
      via: "cmdInitExecutePhase import"
      pattern: "require.*context"
    - from: "tests/unit/parallel.test.js"
      to: "lib/parallel.js"
      via: "buildParallelContext import"
      pattern: "require.*parallel"
    - from: "tests/unit/backend.test.js"
      to: "lib/backend.js"
      via: "BACKEND_CAPABILITIES import"
      pattern: "require.*backend"
---

<objective>
Validate that all existing unit tests for context, parallel, and backend modules pass without modification (regression baseline), then add comprehensive test cases covering both the native isolation path (Claude Code backend) and the manual isolation path (non-Claude-Code backends).

Purpose: Fulfill success criteria #1 (zero regressions) and criteria #2/#3 (native and manual path validation) at the unit test level.
Output: Extended test files with new describe blocks covering all backend+branching_strategy combinations.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/context.js
@lib/parallel.js
@lib/backend.js
@tests/unit/context.test.js
@tests/unit/parallel.test.js
@tests/unit/backend.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing test suite and verify zero regressions</name>
  <files>tests/unit/context.test.js, tests/unit/parallel.test.js, tests/unit/backend.test.js, tests/integration/worktree-parallel-e2e.test.js, tests/unit/worktree.test.js</files>
  <action>
    Run the full existing test suite for all worktree-related modules:

    ```bash
    npx jest tests/unit/context.test.js tests/unit/parallel.test.js tests/unit/backend.test.js tests/unit/worktree.test.js tests/integration/worktree-parallel-e2e.test.js --verbose 2>&1
    ```

    Verify ALL tests pass. Record:
    - Total test count per file
    - Pass/fail/skip counts
    - Any warnings or deprecation notices

    This establishes the regression baseline. If any test fails, investigate and fix BEFORE proceeding (the failure is a pre-existing bug, not a regression from this phase).

    Do NOT modify any existing tests in this task.
  </action>
  <verify>
    All existing test suites pass: `npx jest tests/unit/context.test.js tests/unit/parallel.test.js tests/unit/backend.test.js tests/unit/worktree.test.js tests/integration/worktree-parallel-e2e.test.js` exits with code 0 (Level 1: Sanity)
  </verify>
  <done>All existing worktree-related tests pass, regression baseline established with exact counts logged</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive native/manual isolation unit tests</name>
  <files>tests/unit/context.test.js, tests/unit/parallel.test.js, tests/unit/backend.test.js</files>
  <action>
    Add new test cases to each file. Do NOT modify or remove any existing tests.

    **In tests/unit/backend.test.js** — add a new describe block `'BACKEND_CAPABILITIES native_worktree_isolation per backend'`:
    - Test that `BACKEND_CAPABILITIES.claude.native_worktree_isolation` is `true`
    - Test that `BACKEND_CAPABILITIES.codex.native_worktree_isolation` is `false`
    - Test that `BACKEND_CAPABILITIES.gemini.native_worktree_isolation` is `false`
    - Test that `BACKEND_CAPABILITIES.opencode.native_worktree_isolation` is `false`
    - Test that `getBackendCapabilities('claude').native_worktree_isolation` returns `true`
    - Test that `getBackendCapabilities('codex').native_worktree_isolation` returns `false`
    - Test that `getBackendCapabilities('unknown-backend')` falls back to claude capabilities (returns `true`)

    **In tests/unit/context.test.js** — add a new describe block `'Phase 47: native vs manual isolation matrix'`:
    - Test all 4 backend × 2 branching_strategy combinations for cmdInitExecutePhase:
      - claude + phase → isolation_mode='native', native_worktree_available=true, main_repo_path is string
      - claude + none → isolation_mode='none', native_worktree_available=true, main_repo_path=null
      - codex + phase → isolation_mode='manual', native_worktree_available=false, main_repo_path is string
      - codex + none → isolation_mode='none', native_worktree_available=false, main_repo_path=null
    - For each combination, configure the backend via config.json `backend` field override
    - Verify that `worktree_path` is present and non-null when isolation_mode is 'manual'
    - Verify that `worktree_branch` is present and non-null when branching_strategy is 'phase'

    **In tests/unit/parallel.test.js** — add a new describe block `'Phase 47: buildParallelContext native vs manual isolation'`:
    - Test: buildParallelContext with `nativeWorktreeAvailable: true` → every phase has `worktree_path: null` and `native_isolation: true`
    - Test: buildParallelContext with `nativeWorktreeAvailable: false` → every phase has a non-null `worktree_path` string and `native_isolation: false`
    - Test: buildParallelContext with no options object → backward-compatible behavior: `worktree_path` is set (not null), `native_isolation` is false
    - Test: buildParallelContext with `{ nativeWorktreeAvailable: true }` still returns `worktree_branch` for every phase (branch naming is not skipped, only worktree_path computation)

    Use the existing fixture helper functions (`createFixtureDir`, `writeConfig`, etc.) from each test file. Follow the same patterns already established.
  </action>
  <verify>
    Run the updated test suites:
    ```bash
    npx jest tests/unit/context.test.js tests/unit/parallel.test.js tests/unit/backend.test.js --verbose
    ```
    All tests pass (both existing and new). New test count: at least 15 new tests across the three files. (Level 1: Sanity)
  </verify>
  <done>
    At least 15 new unit tests added across backend.test.js, context.test.js, and parallel.test.js covering all backend × branching_strategy × isolation_mode combinations. All existing tests continue to pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/context.test.js tests/unit/parallel.test.js tests/unit/backend.test.js --verbose` all pass
- Existing test count preserved (no tests removed or modified)
- New tests cover both native (claude) and manual (codex/gemini/opencode) isolation paths
- Coverage thresholds in jest.config.js still met or exceeded
</verification>

<success_criteria>
- Zero existing test regressions across context, parallel, and backend unit tests
- At least 15 new test cases covering the native/manual isolation matrix
- All 4 backends validated for native_worktree_isolation capability
- All backend × branching_strategy combinations validated for isolation_mode
- buildParallelContext backward compatibility confirmed (no-options case)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/47-integration-regression-testing/47-01-SUMMARY.md`
</output>
