---
phase: 46-hybrid-worktree-execution
plan: 03
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - commands/execute-phase.md
  - agents/grd-executor.md
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "execute-phase.md has a conditional branch: when isolation_mode='native', skips setup_worktree step and passes isolation:'worktree' parameter to executor Task calls"
    - "execute-phase.md when isolation_mode='manual', preserves existing setup_worktree step and <worktree> block injection identically to v0.2.5"
    - "execute-phase.md when isolation_mode='none', preserves no-worktree behavior identically to v0.2.5"
    - "execute-phase.md native completion flow uses branch returned from executor Task result for merge/PR/keep/discard options"
    - "grd-executor.md has dual-mode operation: native mode operates naturally in working directory without <worktree> block, manual mode preserves existing <worktree> path-prefixing behavior"
    - "grd-executor.md always writes STATE.md to main_repo_path (not worktree) regardless of isolation mode"
  artifacts:
    - path: "commands/execute-phase.md"
      provides: "Hybrid orchestrator with native/manual/none isolation paths"
    - path: "agents/grd-executor.md"
      provides: "Dual-mode executor with native and manual isolation handling"
  key_links:
    - from: "commands/execute-phase.md"
      to: "lib/context.js"
      via: "init JSON fields: isolation_mode, main_repo_path, native_worktree_available"
      pattern: "isolation_mode|main_repo_path|native_worktree_available"
    - from: "agents/grd-executor.md"
      to: "commands/execute-phase.md"
      via: "context injection: isolation_mode, main_repo_path passed in executor prompt"
      pattern: "isolation_mode|main_repo_path"
---

<objective>
Update the execute-phase orchestrator and executor agent templates to implement the hybrid worktree execution model with native/manual/none isolation paths.

Purpose: This is the integration plan that wires the init JSON fields (Plan 01) and flexible worktree operations (Plan 02) into the actual orchestrator and executor behavior. The execute-phase command template needs conditional logic for native vs manual isolation, and the executor agent needs dual-mode operation instructions.

Output: Modified commands/execute-phase.md with hybrid strategy, modified agents/grd-executor.md with dual-mode execution.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@commands/execute-phase.md
@agents/grd-executor.md
@lib/context.js
@lib/worktree.js
@.planning/milestones/v0.2.6/phases/46-hybrid-worktree-execution/46-01-SUMMARY.md
@.planning/milestones/v0.2.6/phases/46-hybrid-worktree-execution/46-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update execute-phase.md with hybrid isolation strategy</name>
  <files>commands/execute-phase.md</files>
  <action>
    Modify commands/execute-phase.md to implement the hybrid worktree execution model. Changes are to the markdown orchestrator template, not to lib code.

    **1. Update the initialize step** to extract new fields from init JSON:
    - Add `isolation_mode`, `main_repo_path`, `native_worktree_available` to the list of fields parsed from INIT JSON.

    **2. Replace the setup_worktree step** with a tri-modal step:

    The current `setup_worktree` step has `condition="branching_strategy != none"`. Replace with a new step that handles three modes:

    ```
    <step name="setup_isolation" condition="branching_strategy != none">
    Three isolation modes based on `isolation_mode` from init:

    **Mode A: native (isolation_mode='native')**
    Native worktree isolation is available. Do NOT create a worktree manually.
    - Record: `ISOLATION_MODE=native`
    - Record: `MAIN_REPO_PATH` from init JSON (for STATE.md writes)
    - Note: Each executor agent will be spawned with `isolation: "worktree"` parameter
    - The branch name will be captured from each executor's Task result
    - No WORKTREE_PATH is pre-computed — Claude Code handles worktree creation

    **Mode B: manual (isolation_mode='manual')**
    Preserve existing v0.2.5 worktree creation behavior identically:
    [Keep the existing setup_worktree step content here — check dirty, create worktree, verify]
    - Record: `ISOLATION_MODE=manual`, `WORKTREE_PATH`, `WORKTREE_BRANCH`
    - Record: `MAIN_REPO_PATH` as the current working directory

    **When `branching_strategy` is `"none"`:** Skip this step entirely (unchanged).
    </step>
    ```

    **3. Update the executor spawn blocks** (both `execute_waves` and `execute_waves_teams` steps):

    In the standard flow (`execute_waves`, condition `use_teams=false`), update the Task call for spawning executors:

    - **When ISOLATION_MODE=native:** Add `isolation: "worktree"` parameter to the Task call. Replace the `<worktree>` block in the prompt with a `<native_isolation>` block:
      ```
      <native_isolation>
      Isolation mode: native
      You are running in a Claude Code-managed worktree. Your working directory
      IS the worktree — operate naturally. No path prefixing needed.
      Main repo path: ${MAIN_REPO_PATH}
      STATE.md updates must use this main repo path (not your working directory).
      </native_isolation>
      ```

    - **When ISOLATION_MODE=manual:** Keep existing `<worktree>` block unchanged:
      ```
      <worktree>
      Working directory: ${WORKTREE_PATH}
      [existing content unchanged]
      </worktree>
      ```

    - **When branching_strategy=none:** No isolation block at all (existing behavior).

    Apply the same changes to the teams flow (`execute_waves_teams`) executor spawn block.

    **4. Update the completion_flow step:**

    The current completion flow has `condition="branching_strategy != none"`. Update to handle both native and manual:

    - **When ISOLATION_MODE=native:** After all executor agents complete, capture the worktree branch name. The branch can be obtained from:
      a. The Task result metadata (if available)
      b. A `git rev-parse --abbrev-ref HEAD` in the worktree directory (if worktree path is known from Task result)
      c. The WorktreeCreate hook output (if logged)

      For the 4 completion options:
      - **Merge:** Use `worktree merge --phase {N} --branch {BRANCH_NAME}` (the new explicit branch parameter from Plan 02)
      - **PR:** Use `worktree push-pr --phase {N}` (already reads HEAD branch in the worktree)
      - **Keep:** Note the branch name for the user
      - **Discard:** Standard worktree remove

      Note: The test gate should run in the worktree directory. For native isolation, the worktree path comes from the Task result. If the worktree path is not available in the result, the orchestrator can derive it from `git worktree list` and find the branch.

    - **When ISOLATION_MODE=manual:** Preserve existing completion flow exactly as v0.2.5.

    **5. Update the resumption section:**

    Add a note: When resuming with native isolation, the orchestrator should check `git worktree list` for an existing worktree on the phase branch. If found, reuse it. If not found, native isolation will create a new one on next executor spawn.

    IMPORTANT: The execute-phase.md is a markdown template read by Claude agents, not executable code. Write clear, unambiguous conditional instructions that an agent can follow. Use `**When X:**` / `**When Y:**` patterns for clarity.
  </action>
  <verify>
    node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js verify plan-structure commands/execute-phase.md (Level 1: Sanity — verify the markdown is well-formed)
    Manually verify: the three isolation modes are clearly documented; the existing manual and none paths are preserved verbatim; the native path has complete instructions for all 4 completion options.
  </verify>
  <done>
    execute-phase.md orchestrator has tri-modal isolation handling (native/manual/none) with:
    - Native mode: skips manual worktree creation, passes isolation:'worktree' to executors, captures branch from Task result for completion flow
    - Manual mode: identical to v0.2.5 (no behavioral change)
    - None mode: no isolation (unchanged)
    All 4 completion options work for both native and manual modes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update grd-executor.md with dual-mode isolation handling</name>
  <files>agents/grd-executor.md</files>
  <action>
    Modify agents/grd-executor.md to support two isolation modes. Changes are to the agent template markdown.

    **1. Update the `<worktree_execution>` section** to become a dual-mode `<isolation_handling>` section:

    Replace the existing `<worktree_execution>` block with:

    ```markdown
    <isolation_handling>
    ## Isolation Mode Handling

    Your prompt will contain either a `<native_isolation>` block or a `<worktree>` block (or neither for no-isolation mode).

    ### Mode A: Native Isolation (`<native_isolation>` block present)

    You are operating in a Claude Code-managed worktree. Your working directory IS the isolated worktree.

    **Rules:**
    1. **All file paths work naturally.** Use relative paths or standard absolute paths — no special prefixing needed.
    2. **Bash commands work naturally.** No need to `cd` to a special directory.
    3. **Read/Write/Edit tools work naturally.** Use paths as you normally would.
    4. **Git commits happen in the worktree.** The worktree has its own branch. Commits go to that branch automatically.
    5. **STATE.md updates:** Use the `MAIN_REPO_PATH` from your `<native_isolation>` block for all STATE.md operations. STATE.md is shared state that lives in the main repository, not the worktree copy. For grd-tools state commands, run them with: `cd "${MAIN_REPO_PATH}" && node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js state ...`
    6. **SUMMARY.md:** Write to the phase directory as normal (it's in your worktree). The orchestrator handles merging.

    ### Mode B: Manual Isolation (`<worktree>` block present)

    You are operating in a GRD-managed worktree — NOT the main checkout.

    **Rules:**
    1. **All file paths are relative to the worktree.** Use the `WORKTREE_PATH` from your prompt as the root for all operations.
    2. **Bash commands:** Always prefix with `cd "${WORKTREE_PATH}" &&` or use absolute paths within the worktree.
    3. **Read/Write/Edit tools:** Use absolute paths: `${WORKTREE_PATH}/lib/worktree.js` not `lib/worktree.js`.
    4. **Git commits happen in the worktree.** The worktree has its own branch.
    5. **STATE.md updates:** Write to the MAIN repo (not the worktree). Use the original project root (without WORKTREE_PATH prefix) for STATE.md operations.
    6. **SUMMARY.md:** Write to the worktree's phase directory (within WORKTREE_PATH).

    ### Mode C: No Isolation (neither block present)

    Operate normally in the current working directory (backwards compatible). STATE.md and all files are in the same directory.

    </isolation_handling>
    ```

    **2. Update the `<state_updates>` section:**

    The current section mentions using the "original project root" for STATE.md. Update to reference the isolation mode:

    - **Native mode:** `cd "${MAIN_REPO_PATH}" && node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js state ...`
    - **Manual mode:** Use the original project root (the cwd from init context, NOT WORKTREE_PATH)
    - **No isolation:** Normal directory

    **3. Update the `<task_commit_protocol>` section:**

    Add a note for native mode: "When in native isolation mode, git commands work naturally in your working directory — no special directory handling needed."

    **4. Update the load_project_state step:**

    Currently says: "If your prompt includes a `<worktree>` block, use `WORKTREE_PATH` as the working directory."
    Update to: "If your prompt includes a `<worktree>` block, use `WORKTREE_PATH` as the working directory. If your prompt includes a `<native_isolation>` block, your working directory is already the worktree — operate naturally. Use `MAIN_REPO_PATH` from the block for STATE.md updates."

    IMPORTANT: Preserve ALL existing executor behavior for manual and no-isolation modes. The native mode is an ADDITION, not a replacement. Keep the description field in frontmatter under 200 characters.
  </action>
  <verify>
    Read agents/grd-executor.md and verify:
    - Native isolation section is clear and complete (Level 1: Sanity)
    - Manual isolation section is preserved identically to v0.2.5 (Level 2: Proxy — diff against prior version)
    - No-isolation mode is preserved
    - STATE.md handling is correct for all three modes
    - Description field is under 200 characters
  </verify>
  <done>
    grd-executor.md has dual-mode isolation handling:
    - Native mode: operates naturally, uses MAIN_REPO_PATH for STATE.md
    - Manual mode: identical to v0.2.5 with WORKTREE_PATH prefixing
    - No isolation: unchanged
    All existing executor behavior preserved for manual and no-isolation modes.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Both markdown files are well-formed and readable; no broken references
Level 2 (Proxy): Diff execute-phase.md and grd-executor.md against v0.2.5 to verify manual mode is unchanged; native mode instructions are complete and unambiguous
Level 3 (Deferred): Full execute-phase integration test with native isolation on Claude Code (Phase 47)
</verification>

<success_criteria>
- execute-phase.md has clear tri-modal isolation handling (native/manual/none)
- execute-phase.md native path passes isolation:'worktree' to executor Task calls
- execute-phase.md completion flow works with native worktree branches
- grd-executor.md has dual-mode isolation (native/manual/none)
- grd-executor.md native mode operates naturally without path prefixing
- grd-executor.md always writes STATE.md to main_repo_path regardless of mode
- Manual and no-isolation modes are preserved identically to v0.2.5
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/46-hybrid-worktree-execution/46-03-SUMMARY.md`
</output>
