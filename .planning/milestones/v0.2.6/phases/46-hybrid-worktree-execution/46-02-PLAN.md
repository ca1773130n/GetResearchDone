---
phase: 46-hybrid-worktree-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/worktree.js
  - tests/unit/worktree.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "cmdWorktreeMerge accepts an explicit branch name via options.branch parameter, bypassing the computed worktreeBranch when provided"
    - "cmdWorktreePushAndPR works with any branch name by reading the actual branch from worktree HEAD (already implemented) — verify no GRD template pattern restriction exists"
    - "cmdWorktreeHookRemove extended with optional STATE.md cleanup notification (logs phase info for orchestrator consumption)"
    - "All existing worktree tests pass without modification (zero regressions)"
  artifacts:
    - path: "lib/worktree.js"
      provides: "Flexible branch handling in merge and PR functions, extended hook remove handler"
    - path: "tests/unit/worktree.test.js"
      provides: "Tests for explicit branch parameter in merge, and extended hook remove"
  key_links:
    - from: "lib/worktree.js"
      to: "lib/utils.js"
      via: "execGit for branch operations"
      pattern: "execGit.*merge|execGit.*push"
---

<objective>
Adapt worktree merge/PR functions to accept arbitrary branch names (not restricted to GRD naming conventions) and extend the WorktreeRemove hook handler with state cleanup information.

Purpose: When Claude Code creates worktrees natively, the branch name may not follow GRD's `grd/{milestone}/{phase}-{slug}` template. The completion flow's merge and PR functions must work with whatever branch name the native isolation created. Additionally, the WorktreeRemove hook (intentionally minimal in Phase 45) needs extension for Phase 46's state cleanup.

Output: Modified lib/worktree.js with flexible branch handling and extended hook, corresponding tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@lib/worktree.js
@tests/unit/worktree.test.js
@.planning/milestones/v0.2.6/phases/45-foundation-detection/45-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit branch parameter to cmdWorktreeMerge</name>
  <files>lib/worktree.js, tests/unit/worktree.test.js</files>
  <action>
    In lib/worktree.js, modify `cmdWorktreeMerge` to accept an optional `options.branch` parameter:

    1. Currently, cmdWorktreeMerge computes the phase branch via `worktreeBranch(cwd, milestoneVersion, phase, slug || phase)`. Add a conditional: if `options.branch` is provided, use it directly as `phaseBranch` instead of computing from the template.

    2. The function currently requires `options.phase` as mandatory. When `options.branch` is provided directly, `options.phase` should still be required for the merge commit message but the branch computation should be overridden.

    3. This is a backward-compatible addition — existing callers that don't pass `options.branch` get identical behavior.

    Also verify that `cmdWorktreePushAndPR` already handles arbitrary branch names correctly. It reads the branch from `git rev-parse --abbrev-ref HEAD` on the worktree, which should work regardless of naming convention. Confirm this by reading the function — no code change should be needed, just verification.

    In tests/unit/worktree.test.js, add tests:
    - cmdWorktreeMerge with explicit options.branch uses that branch instead of computed one
    - cmdWorktreeMerge without options.branch computes branch as before (regression test)
    - cmdWorktreePushAndPR reads branch from HEAD (verify existing test covers this, or add one)

    IMPORTANT: The changes to cmdWorktreeMerge are minimal — just an `if (options.branch)` check before the existing computation. Do NOT refactor the entire function.
  </action>
  <verify>
    npx jest tests/unit/worktree.test.js --no-coverage (Level 2: Proxy — tests validate both explicit and computed branch paths)
  </verify>
  <done>
    cmdWorktreeMerge accepts explicit branch name, falling back to computed branch when not provided. cmdWorktreePushAndPR confirmed to read actual branch from worktree HEAD. All 52+ existing worktree tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend cmdWorktreeHookRemove with state cleanup info</name>
  <files>lib/worktree.js, tests/unit/worktree.test.js</files>
  <action>
    In lib/worktree.js, extend `cmdWorktreeHookRemove` (currently minimal per Phase 45 design):

    1. After the existing no-op guards (check .planning exists and branching_strategy != none), add phase metadata extraction from the worktree path:
       - Try to extract phase number from the worktree path directory name (pattern: trailing digits)
       - Try to extract milestone version from the directory name

    2. If phase metadata is extractable, include it in the output JSON:
       ```json
       {
         "hooked": true,
         "worktree_path": wtPath,
         "branch": wtBranch,
         "action": "remove_logged",
         "phase_detected": "46",
         "milestone_detected": "v0.2.6"
       }
       ```
       This enriched output allows downstream consumers (orchestrators, monitoring) to correlate worktree removal with specific phases.

    3. If phase metadata is NOT extractable (e.g., non-GRD worktree), preserve existing behavior with just `action: 'remove_logged'`.

    In tests/unit/worktree.test.js, add tests:
    - Hook remove with GRD-pattern worktree path extracts phase and milestone
    - Hook remove with non-GRD worktree path still works (action: remove_logged only)
    - Existing hook remove tests continue to pass

    IMPORTANT: This is still best-effort. No errors should be thrown if metadata extraction fails. Use try/catch or conditional logic.
  </action>
  <verify>
    npx jest tests/unit/worktree.test.js --no-coverage (Level 2: Proxy — tests validate enriched and plain hook remove output)
  </verify>
  <done>
    cmdWorktreeHookRemove extracts phase/milestone metadata when available, includes it in output, and degrades gracefully. All existing worktree tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All modified files parse without syntax errors; npm run lint passes
Level 2 (Proxy): Tests validate explicit branch merge, head-based PR branch, enriched hook remove output
Level 3 (Deferred): Full completion flow integration with native worktree branch names (Phase 47)
</verification>

<success_criteria>
- cmdWorktreeMerge works with any branch name when options.branch is provided
- cmdWorktreePushAndPR confirmed to use actual worktree HEAD branch (no template restriction)
- cmdWorktreeHookRemove outputs phase/milestone metadata when extractable
- All existing worktree tests pass (zero regressions)
- New tests cover explicit branch parameter and enriched hook output
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/46-hybrid-worktree-execution/46-02-SUMMARY.md`
</output>
