---
phase: 45-foundation-detection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/grd-baseline-assessor.md
  - agents/grd-code-reviewer.md
  - agents/grd-codebase-mapper.md
  - agents/grd-debugger.md
  - agents/grd-deep-diver.md
  - agents/grd-eval-planner.md
  - agents/grd-eval-reporter.md
  - agents/grd-executor.md
  - agents/grd-feasibility-analyst.md
  - agents/grd-integration-checker.md
  - agents/grd-migrator.md
  - agents/grd-phase-researcher.md
  - agents/grd-plan-checker.md
  - agents/grd-planner.md
  - agents/grd-product-owner.md
  - agents/grd-project-researcher.md
  - agents/grd-research-synthesizer.md
  - agents/grd-roadmapper.md
  - agents/grd-surveyor.md
  - agents/grd-verifier.md
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "All 20 agent definitions have unique, descriptive name fields in YAML frontmatter"
    - "All 20 agent definitions have concise, meaningful description fields (under 200 characters)"
    - "No agent name conflicts with Claude Code built-in agents (e.g., no generic names like 'planner' or 'executor')"
    - "All agents use the grd- prefix consistently in their name field"
    - "Each description clearly communicates what the agent does when shown in claude agents listing"
  artifacts:
    - path: "agents/grd-executor.md"
      provides: "Updated executor agent definition"
      contains: "name: grd-executor"
    - path: "agents/grd-planner.md"
      provides: "Updated planner agent definition"
      contains: "name: grd-planner"
  key_links: []
---

<objective>
Audit and update all 20 agent definitions for clean display in Claude Code's `claude agents` CLI listing.

Purpose: With Claude Code v2.1.50+ supporting `claude agents` CLI for listing registered agents, all GRD agent definitions need non-conflicting, descriptive `name` and `description` fields that display cleanly. This ensures users can identify GRD agents and their purposes at a glance.

Output: All 20 agent files in `agents/` with validated and potentially updated frontmatter.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/grd-executor.md (frontmatter only)
@agents/grd-planner.md (frontmatter only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 20 agent frontmatter and fix issues</name>
  <files>agents/grd-baseline-assessor.md, agents/grd-code-reviewer.md, agents/grd-codebase-mapper.md, agents/grd-debugger.md, agents/grd-deep-diver.md, agents/grd-eval-planner.md, agents/grd-eval-reporter.md, agents/grd-executor.md, agents/grd-feasibility-analyst.md, agents/grd-integration-checker.md, agents/grd-migrator.md, agents/grd-phase-researcher.md, agents/grd-plan-checker.md, agents/grd-planner.md, agents/grd-product-owner.md, agents/grd-project-researcher.md, agents/grd-research-synthesizer.md, agents/grd-roadmapper.md, agents/grd-surveyor.md, agents/grd-verifier.md</files>
  <action>
    Read the YAML frontmatter of all 20 agent files in `agents/`. For each agent, validate:

    1. **Name field:** Must use `grd-` prefix (already the case for all 20). Must not conflict with Claude Code built-in agent names. Built-in Claude Code agents use generic names — GRD's `grd-` prefix avoids conflicts.

    2. **Description field:** Must be concise (ideally under 160 characters for clean CLI display), meaningful, and action-oriented. The description should answer "what does this agent do?" when shown in a listing.

    Current agent inventory to audit (20 agents):
    - grd-baseline-assessor
    - grd-code-reviewer
    - grd-codebase-mapper
    - grd-debugger
    - grd-deep-diver
    - grd-eval-planner
    - grd-eval-reporter
    - grd-executor
    - grd-feasibility-analyst
    - grd-integration-checker
    - grd-migrator
    - grd-phase-researcher
    - grd-plan-checker
    - grd-planner
    - grd-product-owner
    - grd-project-researcher
    - grd-research-synthesizer
    - grd-roadmapper
    - grd-surveyor
    - grd-verifier

    **Audit checklist per agent:**
    - [ ] `name` field matches filename (minus `.md`) — verify consistency
    - [ ] `name` field uses `grd-` prefix
    - [ ] `description` field is under 200 characters
    - [ ] `description` is self-contained (understandable without reading the full agent definition)
    - [ ] No duplicate names across the 20 agents
    - [ ] `color` field is present (existing requirement)

    **Fix any issues found:**
    - If a description is too long (>200 chars), rewrite it to be more concise while preserving meaning
    - If a description references template variables like `${research_dir}` that would display poorly in CLI, replace with generic phrasing
    - If a name does not match the filename, fix the name to match
    - Do NOT change the body content of any agent — only update frontmatter fields

    **Agents with known template variable references in descriptions to fix:**
    - `grd-deep-diver`: references `${research_dir}` — replace with "research directory"
    - `grd-surveyor`: references `${research_dir}` — replace with "research directory"
    - `grd-project-researcher`: references `${research_dir}/` — replace with "research directory"

    Create a summary table documenting the audit result for each agent: name, description length, changes made (if any).
  </action>
  <verify>
    For each agent file:
    1. grep -c "^name:" agents/grd-*.md — every file has exactly one name field
    2. grep -c "^description:" agents/grd-*.md — every file has exactly one description field
    3. No two agents share the same name value
    4. No description exceeds 200 characters
    (Level 1: Sanity)
  </verify>
  <done>
    All 20 agent definitions audited. Names are unique with grd- prefix.
    Descriptions are concise, self-contained, and display cleanly.
    Template variable references in descriptions replaced with human-readable text.
    Only frontmatter was modified — agent body content unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create agent frontmatter validation test</name>
  <files>tests/unit/verify.test.js</files>
  <action>
    In `tests/unit/verify.test.js` (or create a new focused test file `tests/unit/agent-audit.test.js` if verify.test.js is not the right place), add a test block that validates all agent frontmatter programmatically:

    ```javascript
    describe('Agent frontmatter audit', () => {
      const agentDir = path.join(__dirname, '../../agents');
      const agentFiles = fs.readdirSync(agentDir).filter(f => f.endsWith('.md'));

      test('all agents have unique grd- prefixed names', () => {
        const names = new Set();
        for (const file of agentFiles) {
          const content = fs.readFileSync(path.join(agentDir, file), 'utf-8');
          const nameMatch = content.match(/^name:\s*(.+)$/m);
          expect(nameMatch).toBeTruthy();
          const name = nameMatch[1].trim();
          expect(name).toMatch(/^grd-/);
          expect(names.has(name)).toBe(false);
          names.add(name);
        }
      });

      test('all agents have descriptions under 200 characters', () => {
        for (const file of agentFiles) {
          const content = fs.readFileSync(path.join(agentDir, file), 'utf-8');
          const descMatch = content.match(/^description:\s*(.+)$/m);
          expect(descMatch).toBeTruthy();
          const desc = descMatch[1].trim();
          expect(desc.length).toBeLessThanOrEqual(200);
        }
      });

      test('no descriptions contain template variables', () => {
        for (const file of agentFiles) {
          const content = fs.readFileSync(path.join(agentDir, file), 'utf-8');
          const descMatch = content.match(/^description:\s*(.+)$/m);
          if (descMatch) {
            expect(descMatch[1]).not.toMatch(/\$\{/);
          }
        }
      });

      test('agent count is 20', () => {
        expect(agentFiles.length).toBe(20);
      });
    });
    ```

    Adjust the test structure to match the project's existing test patterns (CommonJS, Jest).
    If placing in a new file, ensure it follows the `tests/unit/*.test.js` naming convention.
  </action>
  <verify>
    npx jest tests/unit/agent-audit.test.js --verbose (or whichever file the tests were placed in)
    (Level 1: Sanity)
    All agent validation tests pass
  </verify>
  <done>
    Automated test validates all 20 agents have unique grd- names,
    descriptions under 200 chars, and no template variable references in descriptions.
    Test will catch regressions if agents are added or modified.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- Agent validation test passes
- `ls agents/grd-*.md | wc -l` outputs 20
- No description contains `${` template variable references
- All names start with `grd-`
- All descriptions under 200 characters
</verification>

<success_criteria>
1. All 20 agent definitions have validated, unique, grd-prefixed names
2. All descriptions are concise (under 200 chars), self-contained, and CLI-display-friendly
3. No template variable references in description fields
4. Automated test prevents regressions on agent frontmatter quality
5. Agent body content is completely unchanged — only frontmatter was modified
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.6/phases/45-foundation-detection/45-03-SUMMARY.md`
</output>
