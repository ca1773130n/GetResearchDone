---
phase: 73-testing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/backend.test.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "backend.test.ts verifies effort, http_hooks, and cron capability flags exist for all 4 backends"
    - "backend.test.ts verifies claude has effort: true, http_hooks: true, cron: true"
    - "backend.test.ts verifies codex/gemini/opencode have effort: false, http_hooks: false, cron: false"
    - "Tests verify resolveEffortLevel returns correct effort strings for all 3 profiles across key agent roles"
    - "All new tests pass with npx jest tests/unit/backend.test.ts"
  artifacts:
    - path: "tests/unit/backend.test.ts"
      provides: "Updated backend tests with effort/http_hooks/cron coverage"
      contains: "effort"
  key_links:
    - from: "tests/unit/backend.test.ts"
      to: "lib/backend.ts"
      via: "require and test assertions"
      pattern: "BACKEND_CAPABILITIES.*effort"
---

<objective>
Add test coverage for the three new capability flags (effort, http_hooks, cron) and the effort level resolution function added in Phase 71.

Purpose: Satisfy REQ-100 (Feature Sync Tests) by ensuring all new backend capabilities have corresponding test assertions.
Output: Updated `tests/unit/backend.test.ts` with new test blocks.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/backend.ts
@lib/types.ts
@tests/unit/backend.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capability flag tests for effort, http_hooks, and cron</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    In the existing `BACKEND_CAPABILITIES` describe block in `tests/unit/backend.test.ts`, add tests for the three new capability flags added by Phase 71:

    1. Update the test "each entry has subagents, parallel, teams, hooks, mcp, native_worktree_isolation keys" to also require `effort`, `http_hooks`, and `cron` in the `requiredKeys` array.

    2. Update the 4 per-backend `toEqual` assertions (claude, codex, gemini, opencode) to include the new flags:
       - claude: `effort: true, http_hooks: true, cron: true`
       - codex: `effort: false, http_hooks: false, cron: false`
       - gemini: `effort: false, http_hooks: false, cron: false`
       - opencode: `effort: false, http_hooks: false, cron: false`

    3. Add a new describe block `"BACKEND_CAPABILITIES effort/http_hooks/cron per backend"` with individual tests:
       - `BACKEND_CAPABILITIES.claude.effort is true`
       - `BACKEND_CAPABILITIES.claude.http_hooks is true`
       - `BACKEND_CAPABILITIES.claude.cron is true`
       - `BACKEND_CAPABILITIES.codex.effort is false`
       - `BACKEND_CAPABILITIES.gemini.effort is false`
       - `BACKEND_CAPABILITIES.opencode.effort is false`
       - `getBackendCapabilities("claude").effort returns true`
       - `getBackendCapabilities("codex").effort returns false`
       - `getBackendCapabilities("unknown-backend") falls back to claude capabilities (effort: true)`

    Follow existing test patterns exactly (same style, same assertion patterns).
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts (Level 1: Sanity)
    All new tests pass. No existing tests broken.
  </verify>
  <done>
    BACKEND_CAPABILITIES tests cover all 3 new flags across all 4 backends.
    getBackendCapabilities tests verify the flags are accessible via the function.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add effort level resolution tests</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Add a new describe block `"resolveEffortLevel(agentRole, profile)"` to test the effort level resolution function exported by Phase 71's backend.ts changes.

    Import `resolveEffortLevel` from `../../lib/backend` in the existing destructuring require at the top of the file.

    Test cases based on Phase 71's success criteria (effort level matrix from accumulated context):
    - Effort levels are: 'high', 'medium', 'low'
    - Quality profile: planner=high, executor=high, surveyor=high, deep-diver=high, eval-planner=high, product-owner=high, code-reviewer=high, verifier=medium
    - Balanced profile: planner=high, executor=medium, surveyor=medium, deep-diver=medium, eval-planner=high, product-owner=high, code-reviewer=medium, verifier=low
    - Budget profile: planner=medium, executor=medium, surveyor=medium, deep-diver=low, eval-planner=medium, product-owner=medium, code-reviewer=low, verifier=low

    Use `test.each` pattern (matching existing resolveBackendModel tests) for a matrix of:
    - [agentRole, profile, expectedEffort] triples covering all 3 profiles for at least grd-planner, grd-executor, grd-verifier

    Also test edge cases:
    - Unknown agent role returns 'medium' (safe default)
    - Unknown profile returns 'medium' (safe default)
    - Undefined/null inputs return 'medium'
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts (Level 1: Sanity)
    All effort level resolution tests pass.
  </verify>
  <done>
    resolveEffortLevel tested across quality/balanced/budget profiles for key agent roles.
    Edge cases (unknown role, unknown profile, null/undefined) handled.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/backend.test.ts` passes with 0 failures
- New tests appear in test output (count increases by ~20-25 tests)
- No regressions in existing backend tests
</verification>

<success_criteria>
1. All 4 backends have test assertions for effort, http_hooks, and cron capability flags
2. resolveEffortLevel tested for 3 profiles x 3+ agent roles = 9+ test cases
3. Edge cases covered (unknown role, unknown profile, null/undefined)
4. Full backend test suite passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.7/phases/73-testing-documentation/73-01-SUMMARY.md`
</output>
