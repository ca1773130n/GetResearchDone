---
phase: 85-mcp-tools-cli-command-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/discussion.test.ts
  - tests/integration/discussion-e2e.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tests/unit/discussion.test.ts covers all exported functions at 85%+ line coverage and 100% function coverage (threshold enforced by jest.config.js)"
    - "tests/unit/discussion.test.ts has edge case tests for buildSynthesisPrompt and buildDiscussionMarkdown behavior (verified indirectly through runDiscussion output assertions)"
    - "tests/integration/discussion-e2e.test.ts validates full pipeline: detect backends -> configure roles -> run 2-round discussion -> synthesize -> write history -> read back via listDiscussions/readDiscussion"
    - "Integration test uses testbed pattern: tmp directory with .planning/config.json, mocked execFileSync for CLI backends, cleanup on teardown"
    - "npm test passes with all new tests included; no regressions in existing 3,177 tests"
    - "npx jest tests/unit/discussion.test.ts --coverage shows 85%+ lines, 85%+ branches, 100% functions"
  artifacts:
    - tests/unit/discussion.test.ts (expanded with coverage-gap tests)
    - tests/integration/discussion-e2e.test.ts (new integration test)
  key_links:
    - "Unit tests mock child_process.execFileSync, fs, paths, and backend modules — same mock infrastructure as existing tests"
    - "Integration test imports runDiscussion, listDiscussions, readDiscussion directly from lib/discussion.ts"
    - "Integration test mocks execFileSync to simulate backend CLI responses without real CLIs"
---

<objective>
Expand discussion unit tests to guarantee 85%+ coverage and create an integration test that validates the complete multi-backend discussion pipeline end-to-end.

Purpose: Close the testing gap for the discussion module. Unit tests ensure all exported functions and edge cases are covered. The integration test validates the full pipeline flow (detect -> configure -> run -> synthesize -> persist -> read) using mocked CLIs, confirming DEFER-84-03 from Phase 84's deferred validations.

Output: Expanded unit test file; new integration test file.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/discussion.ts
@lib/backend.ts
@tests/unit/discussion.test.ts
@tests/integration/wireup-e2e.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand unit tests to cover remaining branches and edge cases</name>
  <files>tests/unit/discussion.test.ts</files>
  <action>
    Run coverage first to identify gaps:
    ```
    npx jest tests/unit/discussion.test.ts --coverage --coverageReporters=text 2>&1 | grep -A5 'discussion.ts'
    ```

    Based on the current test file analysis, the following areas likely need additional tests to push past 85% branch coverage (the file already covers all exported functions -- focus on BRANCHES):

    1. **dispatchToBackend edge cases:**
       - Test with `model` parameter set (verifies --model flag path in claude buildArgs)
       - Test timeout signal handling: mock execFileSync to throw with `signal: 'SIGTERM'` (line ~180)

    2. **runDiscussion edge cases:**
       - Test with explicit `synthesizer` option (non-default)
       - Test with `rounds` > 2 to verify multi-round context accumulation
       - Test the `cwd` option propagation to internal calls
       - Test that `phase` and `type` options flow into the markdown filename

    3. **runPrePlanningDiscussion / runPreExecutionDiscussion branch coverage:**
       - Test when `config.discussion.before_planning` is explicitly `false` (should skip)
       - Test when `config.discussion.before_execution` is not `true` (should skip -- !== false vs === true distinction)
       - Test with missing `config.discussion` section entirely (should use defaults)
       - Test when brainstormer backend is not in backend_roles

    4. **reviewPlanViaBackend / reviewCodeViaBackend branch coverage:**
       - Test when reviewer === primary backend (should return null due to self-review prevention)
       - Test when reviewer backend is unavailable
       - Test parseJSONFromResponse paths: response with json fences, response with raw JSON, response with no JSON at all

    5. **reviewPRViaBackend branch coverage:**
       - Test when reviewer === primary backend
       - Test comment severity defaulting (missing severity field defaults to 'warning')

    Add each test group in the appropriate existing describe() block. Follow the existing mock patterns (childProcess.execFileSync.mockReturnValue, backendModule.detectAvailableBackends.mockReturnValue with makeAvailability helper).

    Do NOT reorganize existing tests -- only ADD new test() calls within existing describe() blocks.
  </action>
  <verify>
    npx jest tests/unit/discussion.test.ts --coverage --coverageReporters=text 2>&1 | grep 'discussion.ts'
    Check: lines >= 85%, branches >= 85%, functions = 100% (Level 2: Proxy)
  </verify>
  <done>discussion.test.ts coverage meets 85%+ lines, 85%+ branches, 100% functions; all new tests pass; no existing tests broken</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test for full discussion pipeline</name>
  <files>tests/integration/discussion-e2e.test.ts</files>
  <action>
    Create tests/integration/discussion-e2e.test.ts following the testbed pattern from wireup-e2e.test.ts:

    **Setup (beforeAll/beforeEach):**
    - Create a tmp directory with `fs.mkdtempSync(path.join(os.tmpdir(), 'grd-discussion-e2e-'))`
    - Create `.planning/config.json` in tmpDir with:
      ```json
      {
        "backend": "claude",
        "backend_roles": { "brainstormer": "codex", "reviewer": "gemini" },
        "discussion": { "before_planning": true, "before_execution": true }
      }
      ```
    - Create `.planning/STATE.md` with minimal content
    - Create `.planning/milestones/v0.3.20/` directory structure and `discussions/` subdirectory

    **Mock strategy:**
    - jest.mock child_process to intercept execFileSync calls
    - Mock should return different responses based on the CLI being called (inspect args[0] or args[1] to determine which backend)
    - For 'codex' backend: return a brainstormer-style response JSON
    - For 'gemini' backend: return a reviewer-style response JSON
    - For 'claude' backend (synthesizer): return a synthesis response
    - Also mock `detectAvailableBackends` from lib/backend to return all 4 backends as available

    **Test cases (all in one describe block "Discussion E2E Pipeline"):**

    1. **Full pipeline: run 2-round discussion -> write -> read back**
       - Call `runDiscussion('Should we use microservices?', ['codex', 'gemini'], { rounds: 2, synthesizer: 'claude', cwd: tmpDir })`
       - Assert result has `topic`, `participants`, `rounds` (length 2), `synthesis`, `discussion_file`
       - Assert `discussion_file` path is under tmpDir's discussions directory
       - Call `listDiscussions(tmpDir)` -- assert the file appears in the list
       - Call `readDiscussion(filename, tmpDir)` -- assert content contains the topic string

    2. **Pipeline with unavailable participant**
       - Mock one backend as unavailable
       - Run discussion with that backend included
       - Assert the result's rounds contain a skipped entry for the unavailable backend
       - Assert discussion file is still written

    3. **Config-driven discussion: before_planning enabled**
       - Call `runPrePlanningDiscussion({ cwd: tmpDir, phase: '85', goal: 'Expose discussion surface', config: configWithDiscussion })`
       - Assert result is not null and has discussion_file

    **Cleanup (afterAll):**
    - `fs.rmSync(tmpDir, { recursive: true, force: true })` with safety check that path starts with os.tmpdir()

    Follow the 'use strict' + TypeScript strict patterns. Use typed requires. Set test timeout to 15000ms.
  </action>
  <verify>
    npx jest tests/integration/discussion-e2e.test.ts --verbose (Level 2: Proxy)
    Check: all tests pass, no unhandled promise rejections
  </verify>
  <done>Integration test validates full discussion pipeline: detect -> run 2 rounds -> synthesize -> write history -> read back. All 3 test cases pass with mocked CLIs.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- npx jest tests/unit/discussion.test.ts passes
- npx jest tests/integration/discussion-e2e.test.ts passes

Level 2 (Proxy):
- npx jest tests/unit/discussion.test.ts --coverage shows 85%+ lines/branches, 100% functions
- npm test passes with zero failures across all test suites
- npm run lint passes
- npm run build:check passes
</verification>

<success_criteria>
Unit tests cover discussion.ts at 85%+ line/branch coverage with 100% function coverage. Integration test validates the complete pipeline with mocked CLIs. npm test passes with all new tests. No regressions.
</success_criteria>

<output>
After completion, create `${phase_dir}/85-02-SUMMARY.md`
</output>
