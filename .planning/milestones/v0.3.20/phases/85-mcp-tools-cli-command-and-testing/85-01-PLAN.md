---
phase: 85-mcp-tools-cli-command-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp-server.ts
  - commands/discuss.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "grd_discussion_run tool is registered in COMMAND_DESCRIPTORS with topic (required string) and participants/rounds/synthesizer (optional) params, calls runDiscussion() and returns JSON"
    - "grd_discussion_config tool is registered, reads discussion section from config.json via readConfig(), and returns JSON with backend_roles and discussion settings"
    - "grd_backends_available tool is registered with no params, calls detectAvailableBackends() from lib/backend.ts, returns JSON map of backend availability and configured roles"
    - "grd_discussion_history tool is registered with optional filename param — without arg calls listDiscussions(), with arg calls readDiscussion(), returns JSON"
    - "commands/discuss.md exists with YAML frontmatter (description, argument-hint) and invokes runDiscussion() on the given topic"
    - "npm run build:check passes with zero errors after all additions"
  artifacts:
    - lib/mcp-server.ts (with 4 new discussion tool descriptors)
    - commands/discuss.md (slash command definition)
  key_links:
    - "grd_discussion_run calls runDiscussion() from lib/discussion.ts"
    - "grd_backends_available calls detectAvailableBackends() from lib/backend.ts"
    - "grd_discussion_config reads config via readConfig() from lib/backend.ts"
    - "grd_discussion_history calls listDiscussions()/readDiscussion() from lib/discussion.ts"
---

<objective>
Register four discussion MCP tools in lib/mcp-server.ts and create the /grd:discuss slash command file.

Purpose: Expose the full discussion API surface so agents and users can trigger ad-hoc multi-backend discussions, inspect available backends, read/write discussion config, and browse discussion history — all via MCP tools or slash command.

Output: Four new tool descriptors in COMMAND_DESCRIPTORS array; one new commands/discuss.md file.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/mcp-server.ts
@lib/discussion.ts
@lib/backend.ts
@commands/discuss-phase.md
@commands/quick.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register four discussion MCP tools in lib/mcp-server.ts</name>
  <files>lib/mcp-server.ts</files>
  <action>
    Add a new "Discussion Tools" section in the COMMAND_DESCRIPTORS array, immediately after the Wireup Tools section (after grd_wireup_report, around line 2637). Follow the exact same descriptor pattern used by wireup/evolve tools.

    Import the needed functions at the top of the file where other lib/ imports are:
    - `runDiscussion`, `listDiscussions`, `readDiscussion` from `./discussion`
    - `detectAvailableBackends` is already available from `./backend` (verify import exists; if not, add it)
    - `readConfig` from `./backend` (verify import exists; if not, add it)

    Add these four tool descriptors:

    **1. grd_discussion_run** — Run an ad-hoc multi-backend discussion
    - params: `topic` (string, required, "The topic or question to discuss"), `participants` (string, optional, "Comma-separated backend IDs to participate"), `rounds` (number, optional, "Number of discussion rounds (default: 2)"), `synthesizer` (string, optional, "Backend to synthesize final answer (default: claude)")
    - execute: Parse participants from comma-separated string into array. Call `runDiscussion(topic, participants, { rounds, synthesizer, cwd })`. Return the DiscussionResult as JSON string.

    **2. grd_discussion_config** — Read discussion configuration
    - params: none
    - execute: Call `readConfig(cwd)` to get config. Extract `discussion` and `backend_roles` sections. Return JSON with `{ discussion: config.discussion || {}, backend_roles: config.backend_roles || {} }`.

    **3. grd_backends_available** — List available backends with roles
    - params: none
    - execute: Call `detectAvailableBackends(cwd)`. Also read config for `backend_roles`. Return JSON with `{ backends: availabilityMap, roles: config.backend_roles || {} }`.

    **4. grd_discussion_history** — List or read discussion history
    - params: `filename` (string, optional, "Specific discussion file to read (omit to list all)")
    - execute: If filename provided, call `readDiscussion(filename, cwd)` and return content. If not, call `listDiscussions(cwd)` and return the array as JSON.

    Each tool's execute function should wrap its return value in `JSON.stringify()` for consistency with existing tool patterns. Use the `async` keyword on execute only if calling async functions (runDiscussion is async; others are sync).

    Add a comment `// -- Discussion Tools --` before the section, matching the `// -- Wireup Tools --` convention.
  </action>
  <verify>
    npm run build:check (Level 1: Sanity)
    Verify: grep -c 'grd_discussion_run\|grd_discussion_config\|grd_backends_available\|grd_discussion_history' lib/mcp-server.ts returns 4
  </verify>
  <done>Four discussion tool descriptors are present in COMMAND_DESCRIPTORS, type-check passes, tool names follow grd_ prefix convention</done>
</task>

<task type="auto">
  <name>Task 2: Create /grd:discuss slash command</name>
  <files>commands/discuss.md</files>
  <action>
    Create commands/discuss.md with the following structure, modeled after commands/quick.md and commands/discuss-phase.md:

    YAML frontmatter (between --- delimiters):
    - description: "Run an ad-hoc multi-backend discussion on a topic"
    - argument-hint: "&lt;topic&gt;"

    Body content — a prompt template that:
    1. States the purpose: run a multi-backend discussion using configured participants
    2. Instructs the agent to:
       - If no argument provided, discuss the current phase's goal (read from ROADMAP.md)
       - If argument provided, use it as the discussion topic
       - Call the `grd_discussion_run` MCP tool with the topic
       - Present each round's backend responses clearly (backend name, response excerpt)
       - Present the final synthesis prominently
       - Report which backends participated vs were skipped
    3. Keep the command definition concise (under 60 lines) — this is a slash command prompt, not an agent definition
    4. Reference `runDiscussion()` behavior: rounds default to 2, synthesizer defaults to claude, participants come from config's backend_roles or all available backends

    Do NOT add effort/maxTurns/disallowedTools frontmatter — those are for agent definitions in agents/, not slash commands in commands/.
  </action>
  <verify>
    head -3 commands/discuss.md shows valid YAML frontmatter with description and argument-hint (Level 1: Sanity)
  </verify>
  <done>commands/discuss.md exists with correct frontmatter format matching other slash commands, body instructs agent to invoke grd_discussion_run tool</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- npm run build:check passes with zero errors
- grep confirms 4 new tool names in lib/mcp-server.ts
- commands/discuss.md has valid YAML frontmatter
- npm run lint passes

Level 2 (Proxy):
- Tool count in MCP server increases by 4 (from 128 to 132)
</verification>

<success_criteria>
All four MCP tools (grd_discussion_run, grd_discussion_config, grd_backends_available, grd_discussion_history) are registered in COMMAND_DESCRIPTORS with correct params and execute functions. commands/discuss.md exists with valid frontmatter. Type-check and lint pass cleanly.
</success_criteria>

<output>
After completion, create `${phase_dir}/85-01-SUMMARY.md`
</output>
