---
phase: 82-discussion-infrastructure
plan: 03
type: tdd
wave: 2
depends_on: ["82-01", "82-02"]
files_modified:
  - tests/unit/discussion.test.ts
  - tests/unit/backend.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "tests/unit/discussion.test.ts exists with tests for dispatchToBackend"
    - "dispatchToBackend tests mock execFileSync and verify correct CLI args per backend"
    - "dispatchToBackend tests verify timeout handling returns structured error"
    - "dispatchToBackend tests verify unavailable backend returns structured error"
    - "detectAvailableBackends tests verify it probes each CLI binary and caches with TTL"
    - "Config validation tests verify backend_roles rejects invalid backend IDs"
    - "Config validation tests verify discussion section defaults and clamping"
    - "All tests pass with npm test"
  artifacts:
    - path: "tests/unit/discussion.test.ts"
      provides: "Unit tests for lib/discussion.ts"
      min_lines: 100
    - path: "tests/unit/backend.test.ts"
      provides: "Additional tests for detectAvailableBackends"
  key_links:
    - from: "tests/unit/discussion.test.ts"
      to: "lib/discussion.ts"
      via: "require and test"
      pattern: "require.*discussion"
    - from: "tests/unit/backend.test.ts"
      to: "lib/backend.ts"
      via: "require and test"
      pattern: "detectAvailableBackends"
---

<objective>
Write unit tests for the new discussion infrastructure: dispatchToBackend, detectAvailableBackends, and config validation.

Purpose: Verify all dispatch, availability detection, and config validation logic works correctly with mocked CLI binaries before integrating into higher-level discussion workflows.
Output: tests/unit/discussion.test.ts (new), tests/unit/backend.test.ts (extended).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/discussion.ts
@lib/backend.ts
@lib/types.ts
@tests/unit/backend.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for lib/discussion.ts</name>
  <files>tests/unit/discussion.test.ts</files>
  <action>
    Create tests/unit/discussion.test.ts following existing test patterns (jest, 'use strict', typed require).

    Mock `child_process` module (specifically `execFileSync`) using `jest.mock('child_process')`.
    Mock `./backend` module's `detectAvailableBackends` to return controlled availability.

    Test cases for dispatchToBackend:
    - **claude args**: When dispatching to 'claude', verify execFileSync is called with 'claude' binary, args include '--print', '-p', and the prompt text. When model is specified, args include '--model'.
    - **codex args**: When dispatching to 'codex', verify args include '-q' and the prompt.
    - **gemini args**: When dispatching to 'gemini', verify args are just [prompt].
    - **opencode args**: When dispatching to 'opencode', verify args are just [prompt].
    - **successful response**: Mock execFileSync returning stdout. Verify response has correct backend, non-empty response_text, positive duration_ms.
    - **timeout handling**: Mock execFileSync throwing an error with `killed: true`. Verify response has empty response_text and stderr mentioning 'timed out'.
    - **unavailable backend**: Mock detectAvailableBackends returning the target as unavailable. Verify response has empty response_text and stderr mentioning unavailability.
    - **non-dispatchable backend**: Call with 'overstory'. Verify error response with stderr mentioning 'not dispatchable'.
    - **custom timeout**: Pass `timeout_ms: 10000` in options. Verify execFileSync receives that timeout.
    - **stderr capture**: Mock execFileSync throwing with stderr content. Verify it appears in response.stderr.

    Test DISCUSSION_SONNET_MODEL:
    - Verify it equals 'sonnet'.
    - Verify it matches the pattern from lib/wireup/state.ts and lib/evolve/state.ts.
  </action>
  <verify>
    npx jest tests/unit/discussion.test.ts passes with all tests green. (Level 2: Proxy)
  </verify>
  <done>tests/unit/discussion.test.ts has 10+ test cases covering all dispatch paths, timeout, unavailability, and model ceiling constant.</done>
</task>

<task type="auto">
  <name>Task 2: Add detectAvailableBackends and config validation tests</name>
  <files>tests/unit/backend.test.ts</files>
  <action>
    Add a new `describe('detectAvailableBackends')` block to the existing tests/unit/backend.test.ts.

    Mock `child_process` module (specifically `execFileSync`) for the version probes.
    Before each test, call `clearAvailabilityCache()` (import from backend module).

    Test cases for detectAvailableBackends:
    - **all backends available**: Mock execFileSync returning version strings for all four CLIs. Verify result has `available: true` and version string for claude, codex, gemini, opencode. Verify overstory, superpowers, grd are `available: false`.
    - **partial availability**: Mock claude and gemini succeeding, codex and opencode throwing. Verify mixed result.
    - **no backends available**: Mock all four throwing. Verify all `available: false, version: null`.
    - **caching**: Call twice, verify execFileSync only called once (4 calls for 4 backends, not 8).
    - **cache expiry**: Call once, advance time past TTL (mock Date.now), call again, verify second probe happens.
    - **version parsing**: Mock execFileSync returning multi-line output. Verify only first line is captured as version.

    Add config validation tests (can be in a new describe block or in existing utils tests if a utils test file exists):
    - **valid backend_roles**: Config with `{"backend_roles": {"reviewer": "codex"}}` loads successfully.
    - **invalid backend_roles value**: Config with `{"backend_roles": {"reviewer": "invalid"}}` produces stderr warning.
    - **discussion section defaults**: Config with `{"discussion": {"enabled": true}}` fills in default values for missing fields.
    - **discussion max_rounds clamping**: Config with `{"discussion": {"max_rounds": 10}}` clamps to 3.
    - **discussion disabled**: Config with `{"discussion": {"enabled": false}}` loads successfully.

    Note: If config validation tests fit better in tests/unit/utils.test.ts, add them there instead.
  </action>
  <verify>
    npx jest tests/unit/backend.test.ts passes with all tests green. (Level 2: Proxy)
    npm test passes with no regressions. (Level 2: Proxy)
  </verify>
  <done>detectAvailableBackends has 6+ test cases covering availability, caching, and version parsing. Config validation has 5+ test cases covering backend_roles and discussion section.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` passes with test files compiling.
Level 2 (Proxy): `npm test` passes with all unit tests green, no regressions.
Level 2 (Proxy): Coverage for lib/discussion.ts >= 85% (dispatch paths, error paths).
</verification>

<success_criteria>
- tests/unit/discussion.test.ts exists with 10+ test cases
- All dispatch flag combinations verified (--print for claude, -q for codex, etc.)
- Timeout and unavailability error handling tested
- detectAvailableBackends caching tested (TTL, cache hit, cache miss)
- Config validation for backend_roles and discussion section tested
- npm test passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.20/phases/82-discussion-infrastructure/82-03-SUMMARY.md`
</output>
