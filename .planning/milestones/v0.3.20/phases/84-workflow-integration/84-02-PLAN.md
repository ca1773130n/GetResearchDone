---
phase: 84-workflow-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/context/execute.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "cmdInitPlanPhase output JSON includes discussion config fields: before_planning flag, brainstormer backend ID, brainstormer availability boolean"
    - "cmdInitExecutePhase output JSON includes discussion config fields: before_execution flag, brainstormer backend ID, brainstormer availability boolean"
    - "cmdInitExecutePhase output JSON includes reviewer config: reviewer backend ID, reviewer availability, code_review pr_review flag"
    - "Existing init context fields remain unchanged — no regression in plan-phase or execute-phase init output"
  artifacts:
    - lib/context/execute.ts (enriched with discussion/review context fields)
  key_links:
    - "Init context consumed by grd-plan-phase.md and grd-execute-phase.md skill orchestrators"
    - "Uses detectAvailableBackends() from lib/backend.ts and loadConfig() from lib/utils.ts"
---

<objective>
Enrich the init context JSON emitted by cmdInitPlanPhase and cmdInitExecutePhase to include discussion and reviewer configuration, so the orchestrator skill markdowns can make config-aware decisions about pre-planning discussions, pre-execution discussions, and review dispatches.

Purpose: The plan-phase and execute-phase orchestrators are markdown-defined skill files that read init context JSON to decide what to dispatch. They need discussion config flags and backend availability to invoke the new workflow integration functions.

Output: Updated lib/context/execute.ts with enriched init JSON for both plan and execute phases.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/context/execute.ts
@lib/backend.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich cmdInitPlanPhase with discussion context</name>
  <files>lib/context/execute.ts</files>
  <action>
    In the cmdInitPlanPhase function, add the following fields to the output JSON object:

    1. Add `discussion_before_planning: boolean` — read from `config.discussion?.before_planning ?? true` (default true when brainstormer configured)
    2. Add `discussion_enabled: boolean` — read from `config.discussion?.enabled ?? true`
    3. Add `brainstormer_backend: string | null` — read from `config.backend_roles?.brainstormer ?? null`
    4. Add `brainstormer_available: boolean` — call `detectAvailableBackends(cwd)` and check if the brainstormer backend is available (false if no brainstormer configured)
    5. Add `reviewer_backend: string | null` — from `config.backend_roles?.reviewer ?? null`
    6. Add `reviewer_available: boolean` — check availability of reviewer backend

    Import `detectAvailableBackends` from '../backend' if not already imported (check existing imports in the file first — it may already be available via the base module imports).

    Place these new fields in a clearly commented `// Discussion & review config` section within the output object.
  </action>
  <verify>
    npm run build:check (Level 1: Sanity)
  </verify>
  <done>cmdInitPlanPhase emits discussion_before_planning, discussion_enabled, brainstormer_backend, brainstormer_available, reviewer_backend, reviewer_available in its JSON output</done>
</task>

<task type="auto">
  <name>Task 2: Enrich cmdInitExecutePhase with discussion and review context</name>
  <files>lib/context/execute.ts</files>
  <action>
    In the cmdInitExecutePhase function, add the following fields to the output JSON object:

    1. Add `discussion_before_execution: boolean` — from `config.discussion?.before_execution ?? false` (default false)
    2. Add `discussion_enabled: boolean` — from `config.discussion?.enabled ?? true`
    3. Add `brainstormer_backend: string | null` — from `config.backend_roles?.brainstormer ?? null`
    4. Add `brainstormer_available: boolean` — availability check
    5. Add `reviewer_backend: string | null` — from `config.backend_roles?.reviewer ?? null`
    6. Add `reviewer_available: boolean` — availability check
    7. Add `pr_review_enabled: boolean` — from `config.code_review_enabled && !!config.backend_roles?.reviewer` (both must be true)

    Use the same `detectAvailableBackends(cwd)` call (cache the result in a local variable to avoid multiple calls).

    Place these new fields in a clearly commented `// Discussion & review config` section, near the existing `// Code review config` section.
  </action>
  <verify>
    npm run build:check (Level 1: Sanity)
    npm run lint (Level 1: Sanity)
  </verify>
  <done>cmdInitExecutePhase emits all discussion and review config fields, tsc and lint pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npm run build:check` passes — init context functions compile
Level 1 (Sanity): `npm run lint` passes
Level 2 (Proxy): `node -e "require('./lib/context/execute')"` loads without error
</verification>

<success_criteria>
- cmdInitPlanPhase JSON output includes 6 new discussion/review config fields
- cmdInitExecutePhase JSON output includes 7 new discussion/review config fields
- No regression in existing init context fields
- tsc and lint pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.20/phases/84-workflow-integration/84-02-SUMMARY.md`
</output>
