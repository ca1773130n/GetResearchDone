---
phase: 84-workflow-integration
plan: 03
type: tdd
wave: 2
depends_on: ["84-01", "84-02"]
files_modified:
  - tests/unit/discussion.test.ts
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Tests cover runPrePlanningDiscussion: skips when before_planning false, skips when brainstormer not configured, skips when brainstormer unavailable, dispatches when all conditions met"
    - "Tests cover runPreExecutionDiscussion: skips when before_execution false (default), dispatches when explicitly enabled"
    - "Tests cover reviewPlanViaBackend: skips when no reviewer configured, skips when reviewer is primary backend, parses valid JSON response, handles malformed JSON gracefully"
    - "Tests cover reviewCodeViaBackend: parses issues array with severity/file/line_range/description, returns approved:false when blockers present"
    - "Tests cover reviewPRViaBackend: skips when code_review_enabled false, parses comment array with file/line/body/severity"
    - "Tests cover parseJSONFromResponse (via exported functions): extracts JSON from markdown fences, handles plain JSON, returns graceful fallback on garbage"
    - "All tests pass with npm test, coverage thresholds maintained"
  artifacts:
    - tests/unit/discussion.test.ts (extended with new test suites)
  key_links:
    - "Tests mock dispatchToBackend and detectAvailableBackends via jest.mock"
    - "Tests verify config-gating logic independent of actual backend calls"
---

<objective>
Add comprehensive unit tests for the five new workflow integration functions (runPrePlanningDiscussion, runPreExecutionDiscussion, reviewPlanViaBackend, reviewCodeViaBackend, reviewPRViaBackend) to the existing tests/unit/discussion.test.ts file.

Purpose: Ensure config-gating logic, backend availability checks, JSON parsing, and graceful error handling all work correctly before the functions are wired into orchestrator skills.

Output: Extended discussion.test.ts with 20+ new test cases across 5 describe blocks.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/discussion.ts
@lib/types.ts
@tests/unit/discussion.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for pre-planning and pre-execution discussion functions</name>
  <files>tests/unit/discussion.test.ts</files>
  <action>
    Add two new `describe` blocks to the existing test file:

    **describe('runPrePlanningDiscussion')**:
    - `it('returns null when discussion.before_planning is false')` — config with `discussion: { before_planning: false, enabled: true, ... }`, verify returns null, no dispatch called
    - `it('returns null when discussion.enabled is false')` — config with `discussion: { enabled: false, ... }`, verify returns null
    - `it('returns null when no brainstormer backend configured')` — config with no `backend_roles.brainstormer`, verify returns null
    - `it('returns null when brainstormer backend is unavailable')` — mock detectAvailableBackends to return brainstormer as unavailable, verify returns null
    - `it('dispatches discussion when all conditions met')` — mock brainstormer available, config has before_planning true, verify runDiscussion called with correct topic containing phase goal and requirements, verify returns DiscussionResult
    - `it('uses default before_planning=true when discussion config omitted')` — config with brainstormer set but no discussion section, verify it dispatches (default true)

    **describe('runPreExecutionDiscussion')**:
    - `it('returns null when discussion.before_execution is false (default)')` — config with no before_execution or before_execution: false, verify returns null
    - `it('returns null when brainstormer unavailable')` — mock unavailable
    - `it('dispatches single-round discussion when before_execution is true')` — verify rounds: 1 in options, type: 'pre-execution'

    Mock `runDiscussion` (it's in the same module — use jest.spyOn or restructure mocks appropriately). Mock `detectAvailableBackends` from '../backend'.
  </action>
  <verify>
    npx jest tests/unit/discussion.test.ts --verbose (Level 1: Sanity)
  </verify>
  <done>9 new tests pass for pre-planning and pre-execution discussion functions</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for review functions (plan, code, PR)</name>
  <files>tests/unit/discussion.test.ts</files>
  <action>
    Add three new `describe` blocks:

    **describe('reviewPlanViaBackend')**:
    - `it('returns null when no reviewer configured')` — no backend_roles.reviewer
    - `it('returns null when reviewer is primary backend')` — config.backend === config.backend_roles.reviewer
    - `it('returns null when reviewer unavailable')` — mock unavailable
    - `it('parses valid JSON response into PlanReviewResult')` — mock dispatchToBackend returning `{ response_text: '```json\n{"approved":true,"concerns":[],"suggestions":["use const"]}\n```' }`, verify parsed result has correct fields
    - `it('handles malformed JSON gracefully')` — mock response with non-JSON text, verify returns approved:false with warning concern about unparseable response

    **describe('reviewCodeViaBackend')**:
    - `it('returns null when no reviewer configured')`
    - `it('parses valid review with issues')` — mock response with issues array containing blocker/warning/suggestion items, verify CodeReviewResult fields
    - `it('handles empty issues as approved')` — mock response with approved:true and empty issues array
    - `it('handles malformed JSON gracefully')` — verify graceful fallback

    **describe('reviewPRViaBackend')**:
    - `it('returns null when code_review_enabled is false')` — verify skips
    - `it('returns null when no reviewer configured')`
    - `it('parses valid PR review comments')` — mock response with comments array, verify PRReviewResult fields
    - `it('handles malformed JSON gracefully')`

    For all review function tests, mock `dispatchToBackend` directly (it IS exported from the module). Mock `detectAvailableBackends` for availability checks.
  </action>
  <verify>
    npx jest tests/unit/discussion.test.ts --verbose (Level 1: Sanity)
    npm test (Level 2: Proxy — full test suite passes, coverage thresholds maintained)
  </verify>
  <done>13+ new tests pass for review functions, full test suite green, coverage thresholds maintained</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx jest tests/unit/discussion.test.ts` — all new tests pass
Level 2 (Proxy): `npm test` — full suite passes, coverage thresholds for discussion.test.ts met
</verification>

<success_criteria>
- 20+ new test cases across 5 describe blocks
- Every config-gating path tested (enabled/disabled, configured/missing, available/unavailable)
- JSON parsing tested with valid JSON, markdown-fenced JSON, and garbage input
- Full test suite passes with maintained coverage thresholds
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.20/phases/84-workflow-integration/84-03-SUMMARY.md`
</output>
