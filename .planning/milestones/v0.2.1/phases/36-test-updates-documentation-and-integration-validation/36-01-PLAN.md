---
phase: 36-test-updates-documentation-and-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Phase 34 and Phase 35 branches are merged to main with no conflicts"
    - "All 1,631 tests pass on the integrated main branch"
    - "npm run format:check passes (Phase 35 had 3 unformatted files)"
    - "Fresh grep identifies remaining old-path references in tests/ and docs/ after merge"
  artifacts: []
  key_links: []
---

<objective>
Merge Phase 34 and Phase 35 feature branches into main and establish the integrated baseline.

Purpose: Phase 36 depends on Phases 34 and 35, which exist only in local branches. All subsequent Phase 36 work (test updates, doc updates, deferred validations) must operate against the merged codebase. This plan also runs format fixes from Phase 35's known gap and produces the definitive grep audit of remaining old-path references.

Output: main branch containing all Phase 32-35 code, passing test suite, grep report of remaining old-path references.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Phase 34 and 35 branches to main</name>
  <files></files>
  <action>
    Phase 34 branch: grd/v0.2.1/34-command-agent-markdown-migration
    Phase 35 branch: grd/v0.2.1/35-migration-script-and-archive-simplification
    Phase 35 is built on top of Phase 34. Merge sequentially:

    1. Checkout main
    2. Merge Phase 34 branch into main:
       git merge grd/v0.2.1/34-command-agent-markdown-migration --no-ff -m "Merge phase 34: command and agent markdown migration"
       If merge conflicts occur, resolve them preserving both sets of changes.
    3. Merge Phase 35 branch into main:
       git merge grd/v0.2.1/35-migration-script-and-archive-simplification --no-ff -m "Merge phase 35: migration script and archive simplification"
       If merge conflicts occur, resolve them preserving both sets of changes.
    4. Run npm run format to fix the 3 files that Phase 35 flagged as unformatted.
    5. If format changes anything, commit: "style(36): fix formatting after phase 34+35 merge"
    6. Run npm test and confirm 1,631 tests pass with zero failures.
    7. Run npm run lint and confirm zero lint errors.
  </action>
  <verify>
    git log --oneline -5 confirms merge commits present (Level 1: Sanity)
    npm test passes with 1,631+ tests, zero failures (Level 1: Sanity)
    npm run lint exits 0 (Level 1: Sanity)
    npm run format:check exits 0 (Level 1: Sanity)
  </verify>
  <done>
    main branch contains all Phase 32-35 code, all tests pass, lint and format clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit remaining old-path references after merge</name>
  <files></files>
  <action>
    After merge, run comprehensive grep to identify which files still contain old-style path references. This determines the exact scope of Plans 02, 03, and 04.

    1. Grep for old-path references in tests/:
       grep -rn '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' tests/
       Save output for Plan 02/03 reference.

    2. Grep for old-path references in docs/:
       grep -rn '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' docs/
       Save output for Plan 04 reference.

    3. Grep for old-path references in CLAUDE.md:
       grep -n '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' CLAUDE.md

    4. Grep for old-path references in the test fixture directory:
       grep -rn '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' tests/fixtures/

    5. Record the audit results in a summary noting:
       - Total old-path references remaining per file
       - Whether CHANGELOG.md references are historical (should NOT be modified)
       - Which test files need updates vs which are already clean
  </action>
  <verify>
    Audit results captured and documented (Level 1: Sanity)
    CHANGELOG.md old-path references confirmed as historical entries (Level 1: Sanity)
  </verify>
  <done>
    Complete audit of remaining old-path references documented in SUMMARY.md,
    with per-file counts and classification of which references to update vs preserve.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): main branch contains merge commits, all tests pass, lint and format clean
Level 1 (Sanity): Grep audit completed with per-file counts
</verification>

<success_criteria>
- main branch includes all Phase 32-35 changes
- npm test reports 1,631+ tests passing, zero failures
- npm run lint exits 0
- npm run format:check exits 0
- Comprehensive audit of remaining old-path references completed
- CHANGELOG.md references identified as historical (not to be modified)
</success_criteria>

<output>
After completion, create `.planning/phases/36-test-updates-documentation-and-integration-validation/36-01-SUMMARY.md`
</output>
