---
phase: 62-oversized-module-decomposition-migration
plan: 04
type: execute
wave: 2
depends_on: [62-02]
files_modified:
  - lib/commands/dashboard.ts
  - lib/commands/health.ts
  - lib/commands/search.ts
  - lib/commands/index.ts
  - lib/commands.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "3 remaining command sub-modules (dashboard, health, search) created under lib/commands/"
    - "lib/commands/index.ts barrel re-exports all 31 symbols from the original commands.js module.exports (25 own functions + 4 requirements re-exports + 2 internal helpers)"
    - "All sub-modules compile under strict:true with no any types in exported functions"
    - "No individual sub-module exceeds 600 lines"
    - "lib/commands.js is converted to a thin CJS proxy re-exporting from lib/commands/index.ts"
    - "All existing unit tests (tests/unit/commands.test.js, 4088 lines) pass unchanged"
  artifacts:
    - path: "lib/commands/dashboard.ts"
      provides: "buildDashboardData, renderDashboard, cmdDashboard, cmdPhaseDetail, plus internal helpers"
      min_lines: 500
    - path: "lib/commands/health.ts"
      provides: "cmdHealth, cmdHealthCheck"
      min_lines: 300
    - path: "lib/commands/search.ts"
      provides: "cmdSearch, cmdMigrateDirs, cmdCoverageReport"
      min_lines: 200
    - path: "lib/commands/index.ts"
      provides: "Barrel re-export of all 31 exported symbols from 10 sub-modules + requirements re-exports"
      contains: "export"
  key_links:
    - from: "lib/commands/dashboard.ts"
      to: "lib/commands/phase-info.ts"
      via: "import for readCachedRoadmap, readCachedState"
      pattern: "require.*phase-info"
    - from: "lib/commands/index.ts"
      to: "lib/commands/dashboard.ts"
      via: "barrel re-export"
      pattern: "export.*from.*dashboard"
    - from: "lib/commands/index.ts"
      to: "lib/requirements.ts"
      via: "re-export of requirement commands (pass-through from original commands.js)"
      pattern: "export.*from.*requirements"
    - from: "lib/commands/health.ts"
      to: "lib/commands/phase-info.ts"
      via: "import for readCachedRoadmap, readCachedState"
      pattern: "require.*phase-info"
---

<objective>
Complete the commands.js decomposition by creating the 3 larger sub-modules (dashboard, health, search) and the barrel index.ts, then convert commands.js to a CJS proxy.

Purpose: The dashboard (451+297 lines), health (235+133 lines), and search (61+127+80 lines) sections are the largest remaining command groups. They depend on internal helpers (readCachedRoadmap, readCachedState) extracted in Plan 02's phase-info.ts module. This plan completes the full decomposition and creates the barrel that unifies all 10 sub-modules.

Output: 3 additional TypeScript sub-modules, barrel index.ts, commands.js CJS proxy, all tests passing.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/commands.js
@lib/commands/phase-info.ts (from Plan 02 -- provides readCachedRoadmap, readCachedState)
@lib/commands/slug-timestamp.ts (from Plan 02)
@lib/commands/todo.ts (from Plan 02)
@lib/commands/config.ts (from Plan 02)
@lib/commands/progress.ts (from Plan 02)
@lib/commands/long-term-roadmap.ts (from Plan 02)
@lib/commands/quality.ts (from Plan 02)
@.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/dashboard.ts and commands/health.ts</name>
  <files>lib/commands/dashboard.ts, lib/commands/health.ts</files>
  <action>
    Extract the two largest remaining command groups:

    **lib/commands/dashboard.ts** (~550 lines):
    - Extract from "Dashboard" section (lines 1019-1470) AND "Phase Detail" section (lines 1471-1768)
    - Functions: buildDashboardData, renderDashboard, renderDashboardTui, cmdDashboard, cmdPhaseDetail
    - Internal helpers: parseDashboardActiveMilestones, parseDashboardPhases, parseDashboardShippedMilestones, parseDashboardStateSummary, makeShippedMilestone
    - This module is expected to be close to 600 lines. If it exceeds 600, extract the internal parse* helpers and makeShippedMilestone into a private `_dashboard-parsers.ts` helper imported only by dashboard.ts
    - Imports: fs, path, output, error, loadConfig, safeReadFile, normalizePhaseName from '../utils'; extractFrontmatter from '../frontmatter'; phasesDir from '../paths'
    - Import readCachedRoadmap, readCachedState from './phase-info' (cross-sub-module import within commands/)
    - Define local interfaces: DashboardData, DashboardPhase, MilestoneEntry, PhaseDetailResult, etc.

    **lib/commands/health.ts** (~370 lines):
    - Extract from "Health" section (lines 1769-2004) AND "Health Check" section (lines 2679-2812)
    - Functions: cmdHealth, cmdHealthCheck
    - cmdHealth: produces a quick health status report (roadmap, state, recent commits, config)
    - cmdHealthCheck: detailed health analysis with --fix flag for auto-repair
    - Imports: fs, path, output, error, loadConfig, safeReadFile, execGit from '../utils'; extractFrontmatter from '../frontmatter'; phasesDir, planningDir from '../paths'
    - Import readCachedRoadmap, readCachedState from './phase-info'
    - Define local interfaces: HealthStatus, HealthCheckResult, HealthIssue, etc.

    Follow established conventions (same as Plan 02).
  </action>
  <verify>
    npx tsc --noEmit lib/commands/dashboard.ts lib/commands/health.ts (Level 1: Sanity)
    wc -l lib/commands/dashboard.ts lib/commands/health.ts -- neither exceeds 600 lines (Level 1: Sanity)
  </verify>
  <done>
    Dashboard (with phase detail) and health sub-modules created, both compiling under strict:true, each under 600 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create commands/search.ts, commands/index.ts barrel, and convert commands.js to CJS proxy</name>
  <files>lib/commands/search.ts, lib/commands/index.ts, lib/commands.js</files>
  <action>
    Complete the commands decomposition:

    **lib/commands/search.ts** (~270 lines):
    - Extract from "Search" section (lines 2408-2469), "Migration" section (lines 2470-2597), "Coverage Report" section (lines 2598-2678)
    - Functions: cmdSearch, cmdMigrateDirs, cmdCoverageReport
    - Internal helper: collectMarkdownFiles (used by cmdSearch)
    - Imports: fs, path, output, error, loadConfig, safeReadFile from '../utils'; phasesDir, planningDir from '../paths'
    - cmdMigrateDirs handles directory restructuring for milestone migration
    - cmdCoverageReport analyzes test coverage across the codebase

    **lib/commands/index.ts** (barrel):
    - Re-export all symbols from all 10 sub-modules:
      - slug-timestamp: cmdGenerateSlug, cmdCurrentTimestamp
      - todo: cmdListTodos, cmdTodoComplete
      - config: cmdConfigEnsureSection, cmdConfigSet, cmdVerifyPathExists
      - phase-info: cmdFindPhase, cmdResolveModel, cmdDetectBackend, cmdCommit, cmdPhasePlanIndex, cmdSummaryExtract, cmdHistoryDigest, readCachedRoadmap, readCachedState, _stateContentCache
      - progress: cmdProgressRender
      - dashboard: buildDashboardData, renderDashboard, cmdDashboard, cmdPhaseDetail
      - health: cmdHealth, cmdHealthCheck
      - long-term-roadmap: cmdLongTermRoadmap
      - quality: cmdQualityAnalysis, cmdSetup
      - search: cmdSearch, cmdMigrateDirs, cmdCoverageReport
    - Also re-export requirement commands (pass-through from original commands.js):
      `export { cmdRequirementGet, cmdRequirementList, cmdRequirementTraceability, cmdRequirementUpdateStatus } from '../requirements';`
      Note: requirements module may still be .js -- use require-as pattern if needed
    - Use `export { ... } from './module'` syntax with section separators

    **lib/commands.js** (convert to CJS proxy):
    - Replace the entire 2,848-line file with a thin CJS proxy (~17 lines):
    ```js
    /**
     * GRD Commands -- CommonJS re-export proxy
     *
     * Canonical implementation decomposed into lib/commands/ sub-modules.
     * This proxy re-exports for runtime CJS resolution.
     */
    'use strict';
    module.exports = require('./commands/index.ts');
    ```

    Run `npx jest tests/unit/commands.test.js` to verify all tests pass through the proxy.
    Verify the barrel exports match the original module.exports exactly (same symbol set).
  </action>
  <verify>
    npx tsc --noEmit lib/commands/search.ts lib/commands/index.ts (Level 1: Sanity)
    wc -l lib/commands/*.ts -- verify all 11 files (10 sub-modules + index), none exceeds 600 (Level 2: Proxy)
    npx jest tests/unit/commands.test.js -- all tests pass (Level 2: Proxy)
    Verify export count: the barrel exports at least 31 symbols matching original module.exports (Level 2: Proxy)
  </verify>
  <done>
    All 10 command sub-modules + barrel created, lib/commands.js converted to CJS proxy, all 4088 lines of command tests pass unchanged, no sub-module exceeds 600 lines, barrel exports all 31+ symbols.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All .ts files compile with tsc --noEmit
Level 2 (Proxy): All command tests pass through CJS proxy; no sub-module exceeds 600 lines; barrel exports all 31 symbols; export count matches original
Level 3 (Deferred): Full test suite and consumer integration in Plan 05
</verification>

<success_criteria>
- lib/commands/ directory contains 10 sub-modules + index.ts (11 .ts files total)
- No sub-module exceeds 600 lines
- All 31 exports from original commands.js module.exports are available via barrel
- Requirements re-exports preserved in barrel
- All command unit tests pass without modification
- lib/commands.js is a thin CJS proxy (~17 lines)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-04-SUMMARY.md`
</output>
