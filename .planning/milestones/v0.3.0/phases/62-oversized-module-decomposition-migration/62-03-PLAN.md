---
phase: 62-oversized-module-decomposition-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/context/base.ts
  - lib/context/execute.ts
  - lib/context/project.ts
  - lib/context/research.ts
  - lib/context/agents.ts
  - lib/context/progress.ts
  - lib/context/index.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/context.js (2,546 lines) is decomposed into 7 sub-modules under lib/context/ with no sub-module exceeding 600 lines"
    - "All 48 exported functions (including buildInitContext, inferCeremonyLevel, 32 cmdInit*, _progressCachePath, _computeProgressMtimeKey) are preserved and re-exported via lib/context/index.ts barrel"
    - "All sub-modules compile under strict:true with no any types in exported functions"
    - "buildInitContext and inferCeremonyLevel are in context/base.ts and imported by other context sub-modules"
    - "All existing unit tests (tests/unit/context.test.js, 3031 lines) pass unchanged"
    - "lib/context.js is converted to a thin CJS proxy re-exporting from lib/context/index.ts"
  artifacts:
    - path: "lib/context/base.ts"
      provides: "buildInitContext, inferCeremonyLevel -- shared utilities for all cmdInit* functions"
      min_lines: 80
    - path: "lib/context/execute.ts"
      provides: "cmdInitExecutePhase, cmdInitPlanPhase, cmdInitVerifyWork, cmdInitCodeReview, cmdInitPhaseResearch"
      min_lines: 400
    - path: "lib/context/project.ts"
      provides: "cmdInitNewProject, cmdInitNewMilestone, cmdInitPhaseOp, cmdInitMilestoneOp, cmdInitTodos, cmdInitQuick, cmdInitResume, cmdInitMapCodebase"
      min_lines: 250
    - path: "lib/context/research.ts"
      provides: "cmdInitResearchWorkflow, cmdInitAssessBaseline, cmdInitDeepDive, cmdInitEvalPlan, cmdInitEvalReport, cmdInitFeasibility, cmdInitProductOwner, cmdInitProjectResearcher, cmdInitResearchSynthesizer, cmdInitRoadmapper, cmdInitSurveyor, cmdInitVerifier"
      min_lines: 400
    - path: "lib/context/agents.ts"
      provides: "cmdInitDebug, cmdInitIntegrationCheck, cmdInitMigrate, cmdInitPlanCheck, cmdInitBaselineAssessor, cmdInitCodeReviewer, cmdInitCodebaseMapper, cmdInitDebugger, cmdInitDeepDiver, cmdInitEvalPlanner, cmdInitEvalReporter, cmdInitExecutor, cmdInitFeasibilityAnalyst, cmdInitIntegrationChecker, cmdInitMigrator, cmdInitPhaseResearcher, cmdInitPlanChecker"
      min_lines: 250
    - path: "lib/context/progress.ts"
      provides: "cmdInitProgress, cmdInitPlanMilestoneGaps, _progressCachePath, _computeProgressMtimeKey"
      min_lines: 180
    - path: "lib/context/index.ts"
      provides: "Barrel re-export of all 48 exported symbols"
      contains: "export"
  key_links:
    - from: "lib/context/execute.ts"
      to: "lib/context/base.ts"
      via: "import for buildInitContext, inferCeremonyLevel"
      pattern: "require.*base"
    - from: "lib/context/project.ts"
      to: "lib/context/base.ts"
      via: "import for buildInitContext"
      pattern: "require.*base"
    - from: "lib/context/index.ts"
      to: "lib/context/base.ts"
      via: "barrel re-export"
      pattern: "export.*from.*base"
    - from: "lib/context/research.ts"
      to: "lib/context/base.ts"
      via: "import for buildInitContext, inferCeremonyLevel"
      pattern: "require.*base"
---

<objective>
Decompose and migrate lib/context.js (2,546 lines) into 7 focused TypeScript sub-modules under lib/context/ with a barrel index.ts preserving all 48 exports.

Purpose: context.js is a god-file containing 48 exported functions across 4 disparate sections. The shared buildInitContext and inferCeremonyLevel utilities are extracted to a base module, then the 32+ cmdInit* functions are grouped by workflow family (execute/plan, project management, R&D research, agent aliases, progress/gaps).

Output: lib/context/ directory with 7 .ts files, each under 600 lines, lib/context.js converted to CJS proxy.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/context.js
@lib/utils.ts (for import types)
@lib/backend.ts (for import types)
@lib/worktree.ts (for import types)
@lib/gates.ts (for import types)
@lib/paths.ts (for import types)
@lib/types.ts (for shared types)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context/base.ts, context/execute.ts, and context/project.ts</name>
  <files>lib/context/base.ts, lib/context/execute.ts, lib/context/project.ts</files>
  <action>
    Create lib/context/ directory and extract the first three sub-modules:

    **lib/context/base.ts** (~110 lines):
    - Extract: inferCeremonyLevel function (lines 49-86), buildInitContext function (lines 96-110)
    - Extract all shared imports that multiple context sub-modules need (utils, backend, paths, etc.)
    - Re-export the shared imports so sub-modules can import from './base' instead of repeating require calls
    - Define a SharedDeps type or export individual typed references
    - This is the foundation module -- all other context sub-modules import from here

    **lib/context/execute.ts** (~600 lines):
    - Extract from "Core Workflow Init Functions" section (lines 112-781): cmdInitExecutePhase, cmdInitPlanPhase, cmdInitVerifyWork, cmdInitCodeReview, cmdInitPhaseResearch
    - These are the largest and most complex init functions (execute-phase alone is ~200 lines)
    - Import: buildInitContext, inferCeremonyLevel from './base'
    - Import: fs, path, safeReadFile, loadConfig, findPhaseInternal, resolveModelForAgent, execGit, output, error from '../utils' (require-as typed cast)
    - Import: detectBackend, getBackendCapabilities, detectWebMcp from '../backend'
    - Import: worktreePath from '../worktree'
    - Import: runPreflightGates from '../gates'
    - Import: phasesDir, researchDir, codebaseDir, todosDir, standardsDir from '../paths'
    - Define local interfaces for ExecutePhaseContext, PlanPhaseContext, etc.

    **lib/context/project.ts** (~330 lines):
    - Extract from "Core Workflow Init Functions" + "Operation Workflow Init Functions" sections: cmdInitNewProject, cmdInitNewMilestone, cmdInitPhaseOp, cmdInitMilestoneOp, cmdInitTodos, cmdInitQuick, cmdInitResume, cmdInitMapCodebase
    - These are the project management / lifecycle init functions
    - Import: buildInitContext from './base'; standard typed imports from utils/backend/paths

    Follow established patterns:
    - 'use strict' at top of each file
    - JSDoc file header: `/** GRD Context/{SubModule} -- {purpose} */`
    - Section separators (// ─── Section ───)
    - require-as typed cast pattern for cross-module imports
    - Local interfaces for complex context bundle types
    - Error cast pattern (err as Error).message for catch blocks
    - Unused args prefixed with _
    - Empty catch blocks kept for optional file reads (matching existing context.js pattern -- 29 empty catches are mostly benign optional-file-read fallbacks)
  </action>
  <verify>
    npx tsc --noEmit lib/context/base.ts lib/context/execute.ts lib/context/project.ts (Level 1: Sanity)
    wc -l lib/context/*.ts -- no file exceeds 600 lines (Level 1: Sanity)
  </verify>
  <done>
    3 context sub-modules created: base.ts with shared utilities, execute.ts with execution/planning inits, project.ts with project lifecycle inits. All compile under strict:true, each under 600 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create context/research.ts, context/agents.ts, context/progress.ts, context/index.ts, and convert context.js to CJS proxy</name>
  <files>lib/context/research.ts, lib/context/agents.ts, lib/context/progress.ts, lib/context/index.ts, lib/context.js</files>
  <action>
    Extract remaining sub-modules, create barrel, and convert the original file:

    **lib/context/research.ts** (~500 lines):
    - Extract from "R&D Workflow Init" section (lines 1312-2546): cmdInitResearchWorkflow (the core function), cmdInitAssessBaseline, cmdInitDeepDive, cmdInitEvalPlan, cmdInitEvalReport, cmdInitFeasibility, cmdInitProductOwner, cmdInitProjectResearcher, cmdInitResearchSynthesizer, cmdInitRoadmapper, cmdInitSurveyor, cmdInitVerifier
    - cmdInitResearchWorkflow is a large function (~200 lines) that handles: survey, deep-dive, feasibility, eval-plan, eval-report, assess-baseline, product-plan, iterate
    - Many of the other functions are thin wrappers calling cmdInitResearchWorkflow with a specific workflow type
    - Import: buildInitContext, inferCeremonyLevel from './base'

    **lib/context/agents.ts** (~300 lines):
    - Extract remaining functions from "R&D Workflow Init" and "Operation Workflow Init" sections: cmdInitDebug, cmdInitIntegrationCheck, cmdInitMigrate, cmdInitPlanCheck
    - Extract all agent alias functions: cmdInitBaselineAssessor, cmdInitCodeReviewer, cmdInitCodebaseMapper, cmdInitDebugger, cmdInitDeepDiver, cmdInitEvalPlanner, cmdInitEvalReporter, cmdInitExecutor, cmdInitFeasibilityAnalyst, cmdInitIntegrationChecker, cmdInitMigrator, cmdInitPhaseResearcher, cmdInitPlanChecker
    - Agent aliases are thin wrappers that call other cmdInit* functions -- they need imports from sibling modules (execute, research, project)
    - Import: buildInitContext from './base'; other cmdInit* functions from sibling modules as needed

    **lib/context/progress.ts** (~200 lines):
    - Extract from "Progress Cache Helpers" section (lines 1109-1311): _progressCachePath, _computeProgressMtimeKey, cmdInitProgress, cmdInitPlanMilestoneGaps
    - These have unique caching logic and file mtime computation
    - Import: buildInitContext from './base'; fs, path, loadConfig from '../utils'

    **lib/context/index.ts** (barrel):
    - Re-export all 48 exported symbols using `export { ... } from './module'` syntax
    - Organize by sub-module with section separators
    - Ensure backward compatibility: every function that was in the original module.exports is re-exported

    **lib/context.js** (convert to CJS proxy):
    - Replace the entire 2,546-line file with a thin CJS proxy (~17 lines):
    ```js
    /**
     * GRD Context -- CommonJS re-export proxy
     *
     * Canonical implementation decomposed into lib/context/ sub-modules.
     * This proxy re-exports for runtime CJS resolution.
     */
    'use strict';
    module.exports = require('./context/index.ts');
    ```

    Run `npx jest tests/unit/context.test.js` to verify all tests pass through the proxy.
  </action>
  <verify>
    npx tsc --noEmit lib/context/research.ts lib/context/agents.ts lib/context/progress.ts lib/context/index.ts (Level 1: Sanity)
    wc -l lib/context/*.ts -- verify all 7 files, none exceeds 600 lines (Level 2: Proxy)
    npx jest tests/unit/context.test.js -- all tests pass (Level 2: Proxy)
  </verify>
  <done>
    All 7 context sub-modules created, barrel index.ts re-exports all 48 symbols, lib/context.js converted to CJS proxy, all context tests pass, no sub-module exceeds 600 lines.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All .ts files compile with tsc --noEmit
Level 2 (Proxy): All context tests pass through CJS proxy; no sub-module exceeds 600 lines; barrel exports all 48 symbols
Level 3 (Deferred): Full test suite verification in Plan 05
</verification>

<success_criteria>
- lib/context/ directory contains 7 .ts files (base, execute, project, research, agents, progress, index)
- No sub-module exceeds 600 lines
- All 48 exports from original context.js are available via lib/context/index.ts
- All context unit tests pass without modification
- lib/context.js is a thin CJS proxy (~17 lines)
- buildInitContext and inferCeremonyLevel are shared from base.ts
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-03-SUMMARY.md`
</output>
