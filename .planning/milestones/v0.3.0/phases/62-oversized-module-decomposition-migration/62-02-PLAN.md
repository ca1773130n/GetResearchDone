---
phase: 62-oversized-module-decomposition-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/commands/slug-timestamp.ts
  - lib/commands/todo.ts
  - lib/commands/config.ts
  - lib/commands/phase-info.ts
  - lib/commands/progress.ts
  - lib/commands/long-term-roadmap.ts
  - lib/commands/quality.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "7 focused sub-modules created under lib/commands/ containing the utility, config, phase-info, progress, long-term-roadmap, and quality command groups"
    - "All sub-modules compile under strict:true with no any types in exported functions"
    - "No individual sub-module exceeds 600 lines"
    - "require-as typed cast pattern used for all cross-module imports (established in Phases 59-61)"
    - "Local interfaces defined for complex parameter/return types (single-consumer types stay local)"
    - "All functions from these sections are fully TypeScript-annotated with typed parameters and return types"
  artifacts:
    - path: "lib/commands/slug-timestamp.ts"
      provides: "cmdGenerateSlug, cmdCurrentTimestamp"
      min_lines: 30
    - path: "lib/commands/todo.ts"
      provides: "cmdListTodos, cmdTodoComplete"
      min_lines: 80
    - path: "lib/commands/config.ts"
      provides: "cmdConfigEnsureSection, cmdConfigSet, cmdVerifyPathExists"
      min_lines: 130
    - path: "lib/commands/phase-info.ts"
      provides: "cmdFindPhase, cmdPhasePlanIndex, cmdSummaryExtract, cmdHistoryDigest, cmdResolveModel, cmdDetectBackend, cmdCommit"
      min_lines: 350
    - path: "lib/commands/progress.ts"
      provides: "cmdProgressRender"
      min_lines: 100
    - path: "lib/commands/long-term-roadmap.ts"
      provides: "cmdLongTermRoadmap"
      min_lines: 250
    - path: "lib/commands/quality.ts"
      provides: "cmdQualityAnalysis, cmdSetup"
      min_lines: 80
  key_links:
    - from: "lib/commands/phase-info.ts"
      to: "lib/utils.ts"
      via: "require-as typed cast import for output, error, loadConfig, findPhaseInternal, etc."
      pattern: "require.*utils.*as typeof import"
    - from: "lib/commands/long-term-roadmap.ts"
      to: "lib/long-term-roadmap.ts"
      via: "require-as typed cast import for LT roadmap CRUD functions"
      pattern: "require.*long-term-roadmap.*as typeof import"
    - from: "lib/commands/phase-info.ts"
      to: "lib/frontmatter.ts"
      via: "require-as typed cast import for extractFrontmatter"
      pattern: "require.*frontmatter.*as typeof import"
---

<objective>
Decompose and migrate the smaller command groups from lib/commands.js into focused TypeScript sub-modules under lib/commands/.

Purpose: This plan handles the first 7 sub-modules of the commands decomposition -- the smaller, self-contained function groups (slug/timestamp, todos, config, phase-info, progress, long-term-roadmap, quality). These groups have minimal internal cross-dependencies and can be extracted independently. Plan 04 (Wave 2) handles the larger dashboard/health/search modules that build on shared internal helpers.

Output: 7 TypeScript sub-modules under lib/commands/, each fully typed and under 600 lines.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/commands.js
@lib/utils.ts (for import types)
@lib/frontmatter.ts (for import types)
@lib/backend.ts (for import types)
@lib/long-term-roadmap.ts (for import types)
@lib/paths.ts (for import types)
@lib/types.ts (for shared types)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/slug-timestamp.ts, commands/todo.ts, commands/config.ts, and commands/phase-info.ts</name>
  <files>lib/commands/slug-timestamp.ts, lib/commands/todo.ts, lib/commands/config.ts, lib/commands/phase-info.ts</files>
  <action>
    Create lib/commands/ directory and extract the first 4 sub-modules from lib/commands.js sections:

    **lib/commands/slug-timestamp.ts** (~50 lines):
    - Extract from "Slug & Timestamp" section (lines 88-139): cmdGenerateSlug, cmdCurrentTimestamp
    - Imports: output from '../utils'
    - Straightforward string utility functions, minimal typing needed

    **lib/commands/todo.ts** (~100 lines):
    - Extract from "Todos" section (lines 140-242): cmdListTodos, cmdTodoComplete
    - Imports: fs, path, output, error, loadConfig from '../utils'; phasesDir/todosDir from '../paths'
    - Define local types for TodoItem and TodoListResult

    **lib/commands/config.ts** (~200 lines):
    - Extract from "Config Operations" section (lines 276-433) AND "Path Verification" section (lines 243-275): cmdConfigEnsureSection, cmdConfigSet, cmdVerifyPathExists
    - Imports: fs, path, output, error, loadConfig from '../utils'; planningDir from '../paths'
    - Define local types for config operation parameters and return types

    **lib/commands/phase-info.ts** (~400 lines):
    - Extract from: "Model Resolution & Phase Lookup" (lines 533-624), "Commit" (lines 625-691), "Phase Plan Index" (lines 692-832), "Summary Extract" (lines 833-897), "History Digest" (lines 434-532)
    - Functions: cmdFindPhase, cmdResolveModel, cmdDetectBackend, cmdCommit, cmdPhasePlanIndex, cmdSummaryExtract, cmdHistoryDigest
    - Also extract the module-level caches: readCachedRoadmap, readCachedState, _roadmapContentCache, _stateContentCache
    - Imports: fs, path, output, error, loadConfig, findPhaseInternal, execGit, MODEL_PROFILES, normalizePhaseName, safeReadFile from '../utils'; extractFrontmatter from '../frontmatter'; detectBackend, resolveBackendModel, getCachedModels, getBackendCapabilities from '../backend'; phasesDir from '../paths'
    - Define local interfaces for PlanIndexEntry, SummaryData, HistoryDigestEntry, etc.

    Follow established patterns from Phases 59-61:
    - 'use strict' at top of each file
    - JSDoc file header: `/** GRD Commands/{SubModule} -- {purpose} */`
    - Section separators (// ─── Section ───)
    - require-as typed cast pattern: `const { fn } = require('../module') as typeof import('../module');`
    - Local interfaces for single-consumer types
    - Error cast pattern `(err as Error).message` for catch blocks
    - SCREAMING_SNAKE_CASE for constants
    - Unused args prefixed with _
  </action>
  <verify>
    npx tsc --noEmit lib/commands/slug-timestamp.ts lib/commands/todo.ts lib/commands/config.ts lib/commands/phase-info.ts (Level 1: Sanity)
    wc -l lib/commands/*.ts -- no file exceeds 600 lines (Level 1: Sanity)
  </verify>
  <done>
    4 sub-modules created under lib/commands/, all compiling under strict:true with typed exports, each under 600 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create commands/progress.ts, commands/long-term-roadmap.ts, and commands/quality.ts</name>
  <files>lib/commands/progress.ts, lib/commands/long-term-roadmap.ts, lib/commands/quality.ts</files>
  <action>
    Extract remaining smaller sub-modules:

    **lib/commands/progress.ts** (~120 lines):
    - Extract from "Progress Render" section (lines 898-1018): cmdProgressRender
    - Three render formats: json, table, bar
    - Imports: fs, path, output, error, loadConfig from '../utils'; phasesDir from '../paths'
    - Uses readCachedState from './phase-info' (internal import within commands/)

    **lib/commands/long-term-roadmap.ts** (~270 lines):
    - Extract from "Long-Term Roadmap" section (lines 2033-2304): cmdLongTermRoadmap
    - This is a single large function that delegates to parseLongTermRoadmap, formatLongTermRoadmap, addLtMilestone, removeLtMilestone, updateLtMilestone, linkNormalMilestone, unlinkNormalMilestone, getLtMilestoneById, initFromRoadmap, updateRefinementHistory, validateLongTermRoadmap from '../long-term-roadmap'
    - Imports: fs, path, output, error, loadConfig from '../utils'; planningDir from '../paths'; all LT roadmap functions from '../long-term-roadmap'

    **lib/commands/quality.ts** (~100 lines):
    - Extract from "Quality Analysis" section (lines 2305-2366) AND "Setup" section (lines 2367-2403): cmdQualityAnalysis, cmdSetup
    - Imports: fs, path, output, error, loadConfig from '../utils'; runQualityAnalysis from '../cleanup'; planningDir from '../paths'

    All sub-modules follow the same conventions as Task 1.

    Do NOT create the barrel index.ts yet -- that happens in Plan 04 when the remaining modules (dashboard, health, search) are also ready.
    Do NOT modify lib/commands.js yet -- that happens in Plan 05.
  </action>
  <verify>
    npx tsc --noEmit lib/commands/progress.ts lib/commands/long-term-roadmap.ts lib/commands/quality.ts (Level 1: Sanity)
    wc -l lib/commands/*.ts -- verify all 7 files exist, none exceeds 600 lines (Level 2: Proxy)
  </verify>
  <done>
    All 7 smaller command sub-modules created under lib/commands/, all compiling, all under 600 lines, covering slug-timestamp, todo, config, phase-info, progress, long-term-roadmap, and quality function groups.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All .ts files compile with tsc --noEmit
Level 2 (Proxy): Line count check -- no sub-module exceeds 600 lines; 7 sub-modules exist
Level 3 (Deferred): Full test suite runs after barrel + proxy setup in Plans 04-05
</verification>

<success_criteria>
- 7 TypeScript sub-modules exist under lib/commands/
- Each sub-module compiles under strict:true
- No sub-module exceeds 600 lines
- All functions have full TypeScript signatures
- require-as typed cast pattern used for cross-module imports
- Local interfaces defined for complex parameter/return types
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-02-SUMMARY.md`
</output>
