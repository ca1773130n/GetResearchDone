---
phase: 62-oversized-module-decomposition-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/evolve/types.ts
  - lib/evolve/state.ts
  - lib/evolve/discovery.ts
  - lib/evolve/scoring.ts
  - lib/evolve/orchestrator.ts
  - lib/evolve/cli.ts
  - lib/evolve/index.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/evolve.ts (2,687 lines) is decomposed into 7 sub-modules under lib/evolve/ with no sub-module exceeding 600 lines"
    - "All 18 interfaces and 3 type aliases are preserved in lib/evolve/types.ts"
    - "All 39 exported functions are preserved and re-exported via lib/evolve/index.ts barrel"
    - "All sub-modules compile under strict:true with no any types in exported functions"
    - "Cross-sub-module imports use relative paths within lib/evolve/"
    - "All existing unit tests (tests/unit/evolve.test.js, 2672 lines) pass unchanged"
  artifacts:
    - path: "lib/evolve/types.ts"
      provides: "All 18 interfaces + 3 type aliases for evolve domain"
      min_lines: 180
    - path: "lib/evolve/state.ts"
      provides: "State I/O, creation, merge, iteration, work item factory"
      exports: ["evolveStatePath", "readEvolveState", "writeEvolveState", "createInitialState", "mergeWorkItems", "advanceIteration", "createWorkItem"]
    - path: "lib/evolve/discovery.ts"
      provides: "All discover* functions, codebase analysis, Claude-based discovery"
      min_lines: 400
    - path: "lib/evolve/scoring.ts"
      provides: "Scoring, priority selection, grouping"
      exports: ["scoreWorkItem", "selectPriorityItems", "groupDiscoveredItems", "selectPriorityGroups", "runGroupDiscovery"]
    - path: "lib/evolve/orchestrator.ts"
      provides: "Evolve loop, iteration handling, prompts, notes, todos"
      min_lines: 400
    - path: "lib/evolve/cli.ts"
      provides: "CLI command functions and init"
      exports: ["cmdEvolve", "cmdEvolveDiscover", "cmdEvolveState", "cmdEvolveAdvance", "cmdEvolveReset", "cmdInitEvolve"]
    - path: "lib/evolve/index.ts"
      provides: "Barrel re-export of all 39 exported symbols"
      contains: "export"
  key_links:
    - from: "lib/evolve/index.ts"
      to: "lib/evolve/state.ts"
      via: "barrel re-export"
      pattern: "export.*from.*state"
    - from: "lib/evolve/index.ts"
      to: "lib/evolve/discovery.ts"
      via: "barrel re-export"
      pattern: "export.*from.*discovery"
    - from: "lib/evolve/orchestrator.ts"
      to: "lib/evolve/state.ts"
      via: "import for readEvolveState, writeEvolveState, advanceIteration"
      pattern: "require.*state"
    - from: "lib/evolve/cli.ts"
      to: "lib/evolve/orchestrator.ts"
      via: "import for runEvolve, runDiscovery"
      pattern: "require.*orchestrator"
---

<objective>
Decompose lib/evolve.ts (2,687 lines) into 7 focused sub-modules under lib/evolve/ directory with a barrel index.ts preserving all 39 exports.

Purpose: evolve.ts is already TypeScript from Phase 61-05. This plan performs pure decomposition (no migration needed) -- splitting the monolithic file into focused sub-modules by responsibility: types, state, discovery, scoring, orchestrator, and CLI.

Output: lib/evolve/ directory with 7 .ts files, each under 600 lines, collectively exporting all 39 symbols.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/evolve.ts
@lib/evolve.js (CJS proxy -- will be updated in Plan 05)
@.planning/milestones/v0.3.0/phases/61-integration-autonomous-layer-migration/61-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evolve/types.ts, evolve/state.ts, and evolve/discovery.ts</name>
  <files>lib/evolve/types.ts, lib/evolve/state.ts, lib/evolve/discovery.ts</files>
  <action>
    Create lib/evolve/ directory and extract the first three sub-modules from lib/evolve.ts:

    **lib/evolve/types.ts** (~200 lines):
    - Extract all 18 interfaces: WorkItem, WorkItemOptions, HistoryEntry, EvolveState, EvolveGroupState, WorkGroup, GroupDiscoveryResult, ThemePattern, EvolveOptions, GroupOutcome, IterationResult, DryRunIterationResult, EvolveResult, WorktreeInfo, EvolutionNotesData, IterationContext, IterationStepResult, HandleIterationReturn
    - Extract all 3 type aliases: WorkItemEffort, WorkItemSource, WorkItemStatus
    - No imports needed (pure type definitions)
    - Use `export interface` / `export type` syntax for all types

    **lib/evolve/state.ts** (~200 lines):
    - Extract sections: Constants (EVOLVE_STATE_FILENAME, WORK_ITEM_DIMENSIONS, DEFAULT_ITEMS_PER_ITERATION, DEFAULT_PICK_PCT, SONNET_MODEL, DIMENSION_WEIGHTS, EFFORT_MODIFIERS, SOURCE_MODIFIERS), Lib File Read Cache (_libFileCache, readLibFileCached), Work Item Factory (createWorkItem), State Path (evolveStatePath), State I/O (readEvolveState, writeEvolveState), State Creation (createInitialState), Merge Logic (mergeWorkItems), Iteration Advancement (advanceIteration)
    - Import types from './types' using `import type { WorkItem, EvolveState, ... } from './types'`
    - Import from parent lib modules using require-as typed cast pattern: `const { safeReadFile, loadConfig, output, error } = require('../utils') as typeof import('../utils');`
    - Import fs, path from Node built-ins
    - Export all functions and constants that are currently exported from evolve.ts

    **lib/evolve/discovery.ts** (~600 lines):
    - Extract the entire Discovery Engine section (lines ~496-1633, 1137 lines in current evolve.ts)
    - This includes: _discoverDimension, discoverImproveFeatureItems, discoverNewFeatureItems, discoverProductivityItems, discoverQualityItems, discoverUsabilityItems, discoverConsistencyItems, discoverStabilityItems, analyzeCodebaseForItems, buildCodebaseDigest, buildDiscoveryPrompt, discoverWithClaude, parseDiscoveryOutput, runDiscovery
    - Also extract the Discovery Orchestrator section (runGroupDiscovery, ~96 lines) -- it orchestrates discovery, not evolve iteration
    - Import types from './types', state functions from './state'
    - Import external deps: spawnClaudeAsync from '../autopilot', getMilestoneInfo etc. from '../utils'
    - The 1137-line Discovery Engine must be checked: if it would exceed 600 lines, split the 7 individual `discover*Dimension` functions into a separate helper. The core framework functions (analyzeCodebaseForItems, buildCodebaseDigest, discoverWithClaude, parseDiscoveryOutput, runDiscovery, runGroupDiscovery) stay in discovery.ts, and the 7 dimension-specific discoverers (discoverImproveFeatureItems, etc.) plus _discoverDimension go to a private `_dimensions.ts` helper imported only by discovery.ts.

    Follow the established patterns:
    - 'use strict' at top of each file
    - JSDoc file header with purpose and dependencies
    - Section separators (// ─── Section ───)
    - require-as typed cast for cross-module imports
    - import type for type-only imports from ./types
  </action>
  <verify>
    npx tsc --noEmit lib/evolve/types.ts lib/evolve/state.ts lib/evolve/discovery.ts (Level 1: Sanity)
    Verify line counts: wc -l lib/evolve/types.ts lib/evolve/state.ts lib/evolve/discovery.ts -- none exceeds 600
  </verify>
  <done>
    Three sub-modules created under lib/evolve/, all compiling under strict:true, each under 600 lines, with proper typed imports between them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create evolve/scoring.ts, evolve/orchestrator.ts, evolve/cli.ts, and evolve/index.ts barrel</name>
  <files>lib/evolve/scoring.ts, lib/evolve/orchestrator.ts, lib/evolve/cli.ts, lib/evolve/index.ts</files>
  <action>
    Extract remaining sub-modules and create the barrel:

    **lib/evolve/scoring.ts** (~160 lines):
    - Extract: Scoring Heuristic (scoreWorkItem), Priority Selection (selectPriorityItems), Group Engine (groupDiscoveredItems, selectPriorityGroups), THEME_PATTERNS constant
    - Import types from './types', state constants from './state'

    **lib/evolve/orchestrator.ts** (~600 lines):
    - Extract: Evolve Loop Helpers (_runIterationStep, _handleIterationResult, buildPlanPrompt, buildExecutePrompt, buildReviewPrompt, buildGroupExecutePrompt, buildGroupReviewPrompt, buildBatchExecutePrompt, buildBatchReviewPrompt), Orchestrator Phase 56 (runEvolve), Todos Integration (writeEvolutionNotes, writeDiscoveriesToTodos)
    - Import types from './types', state functions from './state', discovery from './discovery', scoring from './scoring'
    - Import external: spawnClaudeAsync from '../autopilot', createEvolveWorktree/removeEvolveWorktree/pushAndCreatePR from '../worktree'
    - If this exceeds 600 lines, extract all build*Prompt functions into a private `_prompts.ts` helper (these are pure string template functions, easily separated)

    **lib/evolve/cli.ts** (~130 lines):
    - Extract: CLI Command Functions (cmdEvolve, cmdEvolveDiscover, cmdEvolveState, cmdEvolveAdvance, cmdEvolveReset, cmdInitEvolve)
    - Import from './state', './discovery', './orchestrator' as needed
    - Import output, error from '../utils'

    **lib/evolve/index.ts** (barrel):
    - Re-export all 39 currently exported symbols using `export { ... } from './module'` syntax
    - Organize exports by sub-module with section separators
    - Also re-export all types: `export type { WorkItem, EvolveState, ... } from './types'`

    After creating all sub-modules:
    - Verify lib/evolve.ts content is fully accounted for in sub-modules
    - Delete lib/evolve.ts (the original monolithic file)
    - Do NOT modify lib/evolve.js yet (that's Plan 05)

    Run `npx jest tests/unit/evolve.test.js` to verify all 2672 lines of tests pass.
    Note: lib/evolve.js currently proxies to lib/evolve.ts. After deleting evolve.ts, temporarily update lib/evolve.js to proxy to lib/evolve/index.ts instead: `module.exports = require('./evolve/index.ts');`
  </action>
  <verify>
    npx tsc --noEmit lib/evolve/scoring.ts lib/evolve/orchestrator.ts lib/evolve/cli.ts lib/evolve/index.ts (Level 1: Sanity)
    wc -l lib/evolve/*.ts -- verify no sub-module exceeds 600 lines (Level 2: Proxy)
    npx jest tests/unit/evolve.test.js -- all tests pass (Level 2: Proxy)
  </verify>
  <done>
    All 7 evolve sub-modules created, lib/evolve.ts deleted, lib/evolve.js proxy updated to point to lib/evolve/index.ts, all tests pass, no sub-module exceeds 600 lines, all 39 exports preserved.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All .ts files compile with tsc --noEmit
Level 2 (Proxy): All evolve tests pass; no sub-module exceeds 600 lines; barrel exports all 39 symbols
Level 3 (Deferred): Full test suite verification in Plan 05
</verification>

<success_criteria>
- lib/evolve/ directory contains 7+ .ts files (types, state, discovery, scoring, orchestrator, cli, index, optionally _dimensions and _prompts)
- No sub-module exceeds 600 lines
- All 39 exports from original evolve.ts are available via lib/evolve/index.ts
- All evolve unit tests pass without modification
- lib/evolve.ts (monolithic) is deleted
- lib/evolve.js proxy updated to point to lib/evolve/index.ts
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-01-SUMMARY.md`
</output>
