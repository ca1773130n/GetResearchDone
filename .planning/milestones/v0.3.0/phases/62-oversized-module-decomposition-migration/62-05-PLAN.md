---
phase: 62-oversized-module-decomposition-migration
plan: 05
type: execute
wave: 3
depends_on: [62-01, 62-03, 62-04]
files_modified:
  - jest.config.js
  - lib/evolve.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "jest.config.js coverage thresholds updated: commands.js -> commands/index.ts, context.js -> context/index.ts (evolve.ts already under evolve/ from Plan 01)"
    - "lib/evolve.js CJS proxy points to lib/evolve/index.ts (confirmed in Plan 01, verified here)"
    - "Full test suite (npm test) passes: all 1,631+ tests green with coverage thresholds met"
    - "No sub-module across all three decomposed modules exceeds 600 lines"
    - "All imports from bin/grd-tools.js resolve correctly via barrel re-exports (zero breakage)"
    - "All imports from lib/mcp-server.js resolve correctly via barrel re-exports (zero breakage)"
    - "Total line count across all sub-modules is within 15% of original monolithic files (no bloat from decomposition)"
  artifacts:
    - path: "jest.config.js"
      provides: "Updated coverage threshold keys for decomposed modules"
      contains: "commands/index.ts"
    - path: "lib/evolve.js"
      provides: "CJS proxy pointing to lib/evolve/index.ts"
      contains: "require('./evolve/index.ts')"
  key_links:
    - from: "bin/grd-tools.js"
      to: "lib/commands.js"
      via: "require('../lib/commands') resolves to CJS proxy -> barrel -> sub-modules"
      pattern: "require.*lib/commands"
    - from: "bin/grd-tools.js"
      to: "lib/context.js"
      via: "require('../lib/context') resolves to CJS proxy -> barrel -> sub-modules"
      pattern: "require.*lib/context"
    - from: "bin/grd-tools.js"
      to: "lib/evolve.js"
      via: "require('../lib/evolve') resolves to CJS proxy -> barrel -> sub-modules"
      pattern: "require.*lib/evolve"
    - from: "lib/mcp-server.js"
      to: "lib/commands.js"
      via: "require('./commands') resolves to CJS proxy -> barrel"
      pattern: "require.*commands"
---

<objective>
Update jest.config.js coverage thresholds for decomposed modules and run the full test suite to verify zero breakage across all 1,631+ tests.

Purpose: This final plan ensures the decomposition is fully integrated. Coverage threshold keys must be updated from monolithic file paths (commands.js, context.js) to the new barrel entry points (commands/index.ts, context/index.ts). The full test suite validates that all consumers (bin/grd-tools.js, lib/mcp-server.js, all unit tests) correctly resolve imports through the CJS proxy -> barrel -> sub-module chain.

Output: Updated jest.config.js, full green test suite, verified line counts.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@jest.config.js
@lib/evolve.js
@lib/commands.js
@lib/context.js
@.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-01-SUMMARY.md
@.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-03-SUMMARY.md
@.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update jest.config.js coverage thresholds for decomposed modules</name>
  <files>jest.config.js</files>
  <action>
    Update the coverage threshold keys in jest.config.js to reflect the new file structure:

    1. Change `'./lib/commands.js'` threshold key to `'./lib/commands/index.ts'` with the same threshold values: `{ lines: 90, functions: 95, branches: 70 }`

    2. Change `'./lib/context.js'` threshold key to `'./lib/context/index.ts'` with the same threshold values: `{ lines: 87, functions: 83, branches: 77 }`

    3. The evolve.ts threshold was already updated in Phase 61-05. If it currently points to `'./lib/evolve.ts'`, update it to `'./lib/evolve/index.ts'` with the same values: `{ lines: 85, functions: 94, branches: 70 }`

    Do NOT lower any threshold values -- maintain or exceed current levels.

    Additionally, verify that lib/evolve.js CJS proxy correctly points to lib/evolve/index.ts (should have been done in Plan 01, but confirm here). If it still points to lib/evolve.ts, update it to:
    ```js
    module.exports = require('./evolve/index.ts');
    ```
  </action>
  <verify>
    grep -n 'commands/index.ts\|context/index.ts\|evolve/index.ts\|evolve.ts' jest.config.js (Level 1: Sanity)
    Verify no threshold values were lowered compared to original (Level 1: Sanity)
  </verify>
  <done>
    jest.config.js threshold keys updated for all three decomposed modules; lib/evolve.js proxy confirmed pointing to evolve/index.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify decomposition metrics</name>
  <files>(no files modified -- verification only)</files>
  <action>
    Run comprehensive verification:

    1. **Full test suite:** `npm test`
       - All 1,631+ tests must pass
       - Coverage thresholds must be met for all modules
       - No new test failures introduced

    2. **Line count audit:** `wc -l lib/commands/*.ts lib/context/*.ts lib/evolve/*.ts`
       - Verify NO sub-module exceeds 600 lines
       - Record total line counts vs original:
         - Original: commands.js=2848, context.js=2546, evolve.ts=2687 (total=8081)
         - New total should be within 15% of 8081 (allowing for type annotations and import overhead)

    3. **Export count verification:**
       - lib/commands/index.ts exports >= 31 symbols (25 own + 4 requirements + readCachedRoadmap + readCachedState)
       - lib/context/index.ts exports >= 48 symbols
       - lib/evolve/index.ts exports >= 39 symbols

    4. **Consumer resolution check:**
       - Verify `node -e "const c = require('./lib/commands'); console.log(Object.keys(c).length)"` returns >= 31
       - Verify `node -e "const c = require('./lib/context'); console.log(Object.keys(c).length)"` returns >= 48
       - Verify `node -e "const c = require('./lib/evolve'); console.log(Object.keys(c).length)"` returns >= 39

    5. **CJS proxy verification:**
       - lib/commands.js: `wc -l` shows ~17 lines (CJS proxy)
       - lib/context.js: `wc -l` shows ~17 lines (CJS proxy)
       - lib/evolve.js: `wc -l` shows ~17 lines (CJS proxy)

    If any test fails:
    - Check if it's a coverage threshold issue (adjust key path, not threshold value)
    - Check if it's an import resolution issue (barrel re-export missing a symbol)
    - Check if it's a type error in a sub-module (fix the type annotation)
    - Do NOT lower coverage thresholds to make tests pass
  </action>
  <verify>
    npm test -- full green (Level 2: Proxy)
    wc -l lib/commands/*.ts lib/context/*.ts lib/evolve/*.ts -- no file exceeds 600 lines (Level 2: Proxy)
    Consumer resolution: node -e "..." for all three barrels returns correct export counts (Level 2: Proxy)
  </verify>
  <done>
    Full test suite passes (1,631+ tests), all coverage thresholds met, no sub-module exceeds 600 lines, all barrel exports verified, all consumer imports resolve correctly, CJS proxies confirmed.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): jest.config.js has correct threshold keys; CJS proxies point to correct barrels
Level 2 (Proxy): Full test suite passes (npm test); all sub-modules under 600 lines; export counts match expectations; consumer resolution works
Level 3 (Deferred): Phase 62 barrel re-export backward compatibility under real CLI and MCP invocation (tracked for Phase 65 integration)
</verification>

<success_criteria>
- npm test passes with all 1,631+ tests green
- jest.config.js coverage keys point to decomposed barrel entry points
- No coverage threshold was lowered
- No sub-module exceeds 600 lines across all three decomposed modules
- Export counts: commands >= 31, context >= 48, evolve >= 39
- CJS proxies (commands.js, context.js, evolve.js) are each ~17 lines
- Total new line count within 15% of original 8,081 lines
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/62-oversized-module-decomposition-migration/62-05-SUMMARY.md`
</output>
