---
phase: 63-entry-points-mcp-server-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/postinstall.ts
  - bin/postinstall.js
  - bin/grd-manifest.ts
  - bin/grd-manifest.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "bin/postinstall.ts compiles under strict:true with no any types in exported code paths"
    - "bin/grd-manifest.ts compiles under strict:true with typed CLI routing, sha256, walkDir, and all 4 commands"
    - "bin/postinstall.js is converted to a CJS proxy: module.exports = require('./postinstall.ts')"
    - "bin/grd-manifest.js is converted to a CJS proxy: module.exports = require('./grd-manifest.ts')"
    - "node bin/postinstall.js creates .planning/ directory structure identically to before migration"
    - "node bin/grd-manifest.js generate|detect|save-patches|load-patches all work identically"
    - "Shebang lines (#!/usr/bin/env node) preserved in both .ts files"
  artifacts:
    - path: "bin/postinstall.ts"
      provides: "TypeScript postinstall script with typed directory list and config object"
      min_lines: 50
    - path: "bin/grd-manifest.ts"
      provides: "TypeScript manifest CLI with typed ManifestData, DetectionResult, PatchResult interfaces"
      min_lines: 180
    - path: "bin/postinstall.js"
      provides: "CJS proxy to postinstall.ts"
      contains: "require('./postinstall.ts')"
    - path: "bin/grd-manifest.js"
      provides: "CJS proxy to grd-manifest.ts"
      contains: "require('./grd-manifest.ts')"
  key_links:
    - from: "bin/postinstall.js"
      to: "bin/postinstall.ts"
      via: "CJS proxy require"
      pattern: "require.*postinstall\\.ts"
    - from: "bin/grd-manifest.js"
      to: "bin/grd-manifest.ts"
      via: "CJS proxy require"
      pattern: "require.*grd-manifest\\.ts"
---

<objective>
Migrate bin/postinstall.js (67 lines) and bin/grd-manifest.js (218 lines) to TypeScript, then convert the original .js files into CJS proxies.

Purpose: These two bin/ entry points are self-contained (no lib/ imports) and straightforward to migrate, making them ideal first targets. postinstall.js uses only fs/path; grd-manifest.js uses fs/path/crypto. Both have clear data structures that benefit from typing.

Output: bin/postinstall.ts, bin/grd-manifest.ts (canonical implementations), bin/postinstall.js and bin/grd-manifest.js (CJS proxies).
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@bin/postinstall.js
@bin/grd-manifest.js
@lib/utils.js (CJS proxy pattern reference)
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate bin/postinstall.js to bin/postinstall.ts</name>
  <files>bin/postinstall.ts, bin/postinstall.js</files>
  <action>
    Create bin/postinstall.ts by migrating the existing bin/postinstall.js to TypeScript:

    1. Preserve the shebang line: `#!/usr/bin/env node`
    2. Add 'use strict' directive
    3. Convert `require('fs')` and `require('path')` to `import fs from 'fs'` and `import path from 'path'`
    4. Type the DIRECTORIES array as `const DIRECTORIES: readonly string[] = [...]` or `as const`
    5. Type the DEFAULT_CONFIG object with an inline interface or type literal:
       ```
       interface DefaultConfig {
         model_profile: string;
         commit_docs: boolean;
         autonomous_mode: boolean;
         research_gates: Record<string, never>;
         confirmation_gates: Record<string, never>;
         eval_config: {
           default_metrics: string[];
           baseline_tracking: boolean;
         };
       }
       ```
    6. The try/catch block: type `err` in the catch as `(err: unknown)` and use `(err as Error).message` pattern
    7. Keep the same control flow (check existsSync, mkdir, writeFile, exit)
    8. No module.exports needed -- this is a standalone script that runs and exits

    Then convert bin/postinstall.js to a CJS proxy:
    ```
    #!/usr/bin/env node
    /**
     * GRD Post-install Script -- CommonJS re-export proxy
     *
     * Canonical implementation lives in postinstall.ts; this proxy
     * re-exports it for runtime CJS resolution.
     */
    'use strict';
    require('./postinstall.ts');
    ```
    Note: postinstall is a side-effect script (no exports), so the proxy just requires it to execute.
  </action>
  <verify>
    npx tsc --noEmit bin/postinstall.ts (Level 1: Sanity)
    Create a temp dir and run `node bin/postinstall.js` in it to verify .planning/ directory creation (Level 2: Proxy)
  </verify>
  <done>
    bin/postinstall.ts compiles clean, bin/postinstall.js is a CJS proxy, and running `node bin/postinstall.js` in a fresh directory creates .planning/ with correct structure and config.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate bin/grd-manifest.js to bin/grd-manifest.ts</name>
  <files>bin/grd-manifest.ts, bin/grd-manifest.js</files>
  <action>
    Create bin/grd-manifest.ts by migrating the existing bin/grd-manifest.js to TypeScript:

    1. Preserve the shebang line: `#!/usr/bin/env node`
    2. Add 'use strict' directive
    3. Convert requires to imports: `import fs from 'fs'`, `import path from 'path'`, `import crypto from 'crypto'`
    4. Define interfaces for the data structures:
       ```
       interface ManifestData {
         version: string;
         timestamp: string;
         file_count: number;
         files: Record<string, string>;
       }

       interface DetectionResult {
         error?: string;
         version?: string;
         total_tracked?: number;
         modifications: Array<{ file: string; expected: string; actual: string }>;
         additions: string[];
         deletions: string[];
         clean: boolean;
       }

       interface PatchSaveResult {
         saved: boolean;
         reason?: string;
         error?: string;
         patch_dir?: string;
         from_version?: string;
         timestamp?: string;
         files?: string[];
         additions?: string[];
         deletions?: string[];
       }

       interface PatchLoadResult {
         found: boolean;
         reason?: string;
         patch_dir?: string;
         from_version?: string;
         timestamp?: string;
         files?: string[];
       }
       ```
    5. Type all functions with explicit return types:
       - `getPluginRoot(): string`
       - `sha256(filePath: string): string`
       - `walkDir(dir: string, base: string): string[]`
       - `readVersion(pluginRoot: string): string`
       - `generateManifest(pluginRoot: string): ManifestData`
       - `detectModifications(pluginRoot: string): DetectionResult`
       - `savePatches(pluginRoot: string, patchDirOverride?: string | null): PatchSaveResult`
       - `loadPatches(pluginRoot: string, patchDirOverride?: string | null): PatchLoadResult`
       - `main(): void`
    6. Type the switch/case in main() -- the command variable as `string | undefined`
    7. Type the `dirIndex` and `dirOverride` variables appropriately
    8. Type `entries` from `readdirSync` (already returns `fs.Dirent[]`)

    Then convert bin/grd-manifest.js to a CJS proxy:
    ```
    #!/usr/bin/env node
    /**
     * GRD Manifest -- CommonJS re-export proxy
     *
     * Canonical implementation lives in grd-manifest.ts; this proxy
     * re-exports it for runtime CJS resolution.
     */
    'use strict';
    require('./grd-manifest.ts');
    ```
    Note: grd-manifest.js is a standalone CLI script (calls main() at module level), so the proxy just requires it to execute.
  </action>
  <verify>
    npx tsc --noEmit bin/grd-manifest.ts (Level 1: Sanity)
    node bin/grd-manifest.js detect -- verify it outputs JSON detection result (Level 2: Proxy)
  </verify>
  <done>
    bin/grd-manifest.ts compiles clean with all functions typed, bin/grd-manifest.js is a CJS proxy, and all 4 CLI commands (generate, detect, save-patches, load-patches) produce identical output.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Both .ts files compile with `npx tsc --noEmit`
Level 2 (Proxy): Both bin/*.js proxies execute and produce identical behavior to pre-migration
Level 3 (Deferred): Full test suite in Plan 04
</verification>

<success_criteria>
- bin/postinstall.ts and bin/grd-manifest.ts compile under strict:true
- bin/postinstall.js and bin/grd-manifest.js are CJS proxies pointing to their .ts counterparts
- `node bin/postinstall.js` creates .planning/ directory structure correctly
- `node bin/grd-manifest.js generate` produces valid manifest JSON
- Shebang lines preserved in both .ts files
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/63-entry-points-mcp-server-migration/63-01-SUMMARY.md`
</output>
