---
phase: 61-integration-autonomous-layer-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/long-term-roadmap.ts
  - lib/long-term-roadmap.js
  - jest.config.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/long-term-roadmap.ts compiles under strict:true with zero any types in exported functions"
    - "lib/long-term-roadmap.js is a thin CommonJS proxy re-exporting from long-term-roadmap.ts"
    - "All 17 exported functions have full TypeScript signatures with typed parameters and return types"
    - "LT milestone parsing, validation, CRUD, and formatting interfaces are defined locally in the module"
    - "All existing unit tests pass with no test file changes (tests/unit/long-term-roadmap.test.js)"
    - "jest.config.js coverage threshold updated from .js to .ts extension with same thresholds"
  artifacts:
    - path: "lib/long-term-roadmap.ts"
      provides: "TypeScript long-term roadmap module with full type annotations"
      min_lines: 680
    - path: "lib/long-term-roadmap.js"
      provides: "CommonJS re-export proxy for runtime CJS resolution"
      contains: "require('./long-term-roadmap.ts')"
  key_links:
    - from: "lib/long-term-roadmap.ts"
      to: "lib/frontmatter.ts"
      via: "require-as typed cast import"
      pattern: "require.*frontmatter.*as typeof import"
    - from: "lib/long-term-roadmap.js"
      to: "lib/long-term-roadmap.ts"
      via: "CommonJS proxy re-export"
      pattern: "module\\.exports = require"
---

<objective>
Migrate lib/long-term-roadmap.js (701 lines) to TypeScript with full type annotations for all LT milestone parsing, validation, CRUD, and formatting operations.

Purpose: long-term-roadmap is the simplest of the 6 modules (depends only on frontmatter.ts) and serves as a clean first migration in the wave. Its pure markdown parsing logic is ideal for establishing interface patterns that tracker.ts and worktree.ts will follow.

Output: lib/long-term-roadmap.ts with typed interfaces, lib/long-term-roadmap.js as CJS proxy, updated jest.config.js thresholds.
</objective>

<execution_context>
@.claude/grd/references/execute-plan.md
@.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/long-term-roadmap.js
@lib/frontmatter.ts
@lib/types.ts
@lib/deps.ts (reference for migration patterns: require-as casts, local interfaces, CJS proxy)
@lib/deps.js (reference for CJS proxy format)
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate long-term-roadmap.js to long-term-roadmap.ts with typed interfaces</name>
  <files>lib/long-term-roadmap.ts, lib/long-term-roadmap.js</files>
  <action>
    Create lib/long-term-roadmap.ts by migrating all code from lib/long-term-roadmap.js with full TypeScript annotations.

    **Import pattern** (require-as typed cast, consistent with Phase 60):
    ```typescript
    const { extractFrontmatter } = require('./frontmatter') as typeof import('./frontmatter');
    ```

    **Define local interfaces** (single-consumer types, do NOT add to types.ts):

    1. `LtMilestone` — parsed LT milestone:
       - id: string, name: string, status: string, goal: string, timeframe: string
       - normal_milestones: string[], description: string, success_criteria: string[]
       - Optional fields from frontmatter if present

    2. `LongTermRoadmap` — full parsed document:
       - title: string, milestones: LtMilestone[], raw_content: string

    3. `ValidationResult` — from validateLongTermRoadmap:
       - valid: boolean, errors: string[], warnings: string[]

    4. `RefinementHistoryEntry` — { date: string, action: string, details: string }

    5. `NormalMilestoneEntry` — { version: string, name: string, status: string }

    **Type all 17 exported functions** with explicit parameter and return types:
    - `extractBoldField(text: string, fieldName: string): string | null`
    - `parseRefinementHistory(content: string): RefinementHistoryEntry[]`
    - `updateRefinementHistory(content: string, action: string, details: string): string`
    - `parseNormalMilestoneList(text: string): NormalMilestoneEntry[]`
    - `formatNormalMilestoneList(entries: NormalMilestoneEntry[]): string`
    - `parseLtMilestone(section: string): LtMilestone`
    - `parseLongTermRoadmap(content: string): LongTermRoadmap`
    - `validateLongTermRoadmap(roadmap: LongTermRoadmap): ValidationResult`
    - `generateLongTermRoadmap(milestones: LtMilestone[]): string`
    - `formatLongTermRoadmap(roadmap: LongTermRoadmap): string`
    - `extractShippedVersions(content: string): string[]`
    - `nextLtId(milestones: LtMilestone[]): string`
    - `addLtMilestone(content: string, milestone: Partial<LtMilestone>): string`
    - `removeLtMilestone(content: string, id: string): string`
    - `updateLtMilestone(content: string, id: string, updates: Partial<LtMilestone>): string`
    - `linkNormalMilestone(content: string, ltId: string, normalVersion: string): string`
    - `unlinkNormalMilestone(content: string, ltId: string, normalVersion: string): string`
    - `getLtMilestoneById(milestones: LtMilestone[], id: string): LtMilestone | undefined`
    - `initFromRoadmap(roadmapContent: string): string`

    **Internal helpers** (not exported) also get typed parameters/returns but can use more flexible types.

    **Error handling pattern:** `(err as Error).message` in catch blocks.

    **Record<string, unknown>** for any dynamic JSON/config objects — never use `any`.

    Then convert lib/long-term-roadmap.js to CJS proxy (exact pattern from lib/deps.js):
    ```javascript
    /**
     * GRD Long-Term Roadmap -- CommonJS re-export proxy
     *
     * This file exists solely for runtime compatibility: plain Node.js
     * `require('./long-term-roadmap')` resolves to .js before .ts. The canonical
     * implementation lives in long-term-roadmap.ts; this proxy re-exports it.
     *
     * Will be removed when Phase 65 establishes a runtime TS resolution
     * strategy (ts-node, dist/ build, or Node.js --experimental-strip-types).
     *
     * @see DEFER-59-01 in STATE.md
     */
    'use strict';

    module.exports = require('./long-term-roadmap.ts');
    ```
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity — compiles with no errors)
    npx jest tests/unit/long-term-roadmap.test.js (Level 1: Sanity — all tests pass unchanged)
  </verify>
  <done>
    lib/long-term-roadmap.ts compiles under strict:true with zero any in exported functions,
    all 17 exports have typed signatures, local interfaces defined for LT milestone structures,
    CJS proxy in place, all existing unit tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update jest.config.js coverage threshold for long-term-roadmap</name>
  <files>jest.config.js</files>
  <action>
    Update the coverage threshold entry in jest.config.js:
    - Change `'./lib/long-term-roadmap.js'` to `'./lib/long-term-roadmap.ts'`
    - Keep the same threshold values: `{ lines: 97, functions: 100, branches: 83 }`

    This follows the exact pattern from Phase 60 migrations (e.g., phase.js → phase.ts threshold rename).
  </action>
  <verify>
    npx jest tests/unit/long-term-roadmap.test.js --coverage (Level 1: Sanity — coverage thresholds met)
  </verify>
  <done>
    jest.config.js references long-term-roadmap.ts with same coverage thresholds,
    coverage report confirms thresholds are met.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx tsc --noEmit` passes with no errors
- `npx jest tests/unit/long-term-roadmap.test.js` — all tests pass
- `npx jest tests/unit/long-term-roadmap.test.js --coverage` — thresholds met
- `npx eslint lib/long-term-roadmap.ts` — no lint errors

Level 2 (Proxy):
- Grep for `any` in exported function signatures — zero occurrences
- Verify CJS proxy: `node -e "const m = require('./lib/long-term-roadmap'); console.log(Object.keys(m).length)"` outputs 17
</verification>

<success_criteria>
- lib/long-term-roadmap.ts compiles under strict:true
- Zero `any` types in exported function signatures
- All 17 exports have explicit TypeScript parameter and return types
- Local interfaces defined: LtMilestone, LongTermRoadmap, ValidationResult, RefinementHistoryEntry, NormalMilestoneEntry
- CJS proxy lib/long-term-roadmap.js re-exports from .ts
- All unit tests pass with coverage thresholds met
- No lint errors
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/61-integration-autonomous-layer-migration/61-01-SUMMARY.md`
</output>
