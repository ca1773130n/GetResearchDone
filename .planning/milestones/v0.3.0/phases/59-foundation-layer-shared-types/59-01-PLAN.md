---
phase: 59-foundation-layer-shared-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/paths.ts
  - lib/paths.js
  - jest.config.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/types.ts exports interfaces for Config, PhaseInfo, MilestoneInfo, BackendCapabilities, ModelProfile, ModelTierMap, WebMcpResult, BackendId, ModelTier, ModelProfileName, StateFields, RoadmapPhase, FrontmatterObject, ExecGitResult, and MCP tool descriptor types"
    - "lib/paths.ts compiles under strict:true with zero any types and explicit return type annotations on all 11 exported functions (currentMilestone, planningDir, milestonesDir, phasesDir, phaseDir, researchDir, codebaseDir, todosDir, quickDir, standardsDir, archivedPhasesDir)"
    - "lib/paths.js is deleted after migration (replaced by lib/paths.ts)"
    - "jest.config.js coverage thresholds updated: lib/paths.js entry replaced by lib/paths.ts with identical thresholds (lines:95, functions:100, branches:95)"
    - "All 10 existing JS modules that import from paths continue to resolve correctly via require('./paths')"
    - "npx tsc --noEmit passes with zero errors"
    - "npm test passes with all existing tests plus paths tests running against the .ts source"
  artifacts:
    - path: "lib/types.ts"
      provides: "Core shared TypeScript interfaces and type aliases for the entire codebase"
      min_lines: 80
    - path: "lib/paths.ts"
      provides: "Milestone-scoped path resolution (migrated from paths.js)"
      exports: ["currentMilestone", "planningDir", "milestonesDir", "phasesDir", "phaseDir", "researchDir", "codebaseDir", "todosDir", "quickDir", "standardsDir", "archivedPhasesDir"]
      min_lines: 200
  key_links:
    - from: "lib/paths.ts"
      to: "lib/types.ts"
      via: "import type"
      pattern: "import.*type.*from.*types"
    - from: "tests/unit/paths.test.js"
      to: "lib/paths.ts"
      via: "require('./paths') resolved by Jest moduleFileExtensions"
      pattern: "require.*paths"
---

<objective>
Create the core shared type definitions file and migrate lib/paths.js to TypeScript.

Purpose: Establish the type foundation that all subsequent module migrations will import from, and prove the JS-to-TS migration pattern on the simplest foundation module (paths.js: 285 lines, zero lib/ dependencies, 11 exported functions).

Output: lib/types.ts with all shared interfaces, lib/paths.ts fully typed under strict:true, lib/paths.js deleted, jest.config.js updated with .ts coverage thresholds.
</objective>

<execution_context>
@.planning/milestones/v0.3.0/phases/58-typescript-toolchain-build-pipeline/58-01-SUMMARY.md
@.planning/milestones/v0.3.0/phases/58-typescript-toolchain-build-pipeline/58-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/paths.js
@lib/backend.js
@lib/utils.js
@lib/sample.ts
@tsconfig.json
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/types.ts with core shared type definitions</name>
  <files>lib/types.ts</files>
  <action>
    Create lib/types.ts consolidating all shared type definitions needed by the foundation layer and forward-declared for higher layers. Follow the module.exports + export type pattern established in lib/sample.ts.

    Define the following interfaces and types (derive shapes from the actual runtime values in paths.js, backend.js, and utils.js):

    **Backend types (from backend.js constants and functions):**
    - `type BackendId = 'claude' | 'codex' | 'gemini' | 'opencode'`
    - `type ModelTier = 'opus' | 'sonnet' | 'haiku'`
    - `type ModelProfileName = 'quality' | 'balanced' | 'budget'`
    - `interface ModelTierMap { opus: string; sonnet: string; haiku: string }` (maps tiers to backend-specific names)
    - `interface BackendCapabilities { subagents: boolean | string; parallel: boolean; teams: boolean; hooks: boolean; mcp: boolean; native_worktree_isolation: boolean }`
    - `interface WebMcpResult { available: boolean; source: string; reason?: string }`

    **Config types (from utils.js loadConfig):**
    - `interface GrdConfig` with all fields from loadConfig defaults: model_profile, commit_docs, search_gitignored, branching_strategy, phase_branch_template, milestone_branch_template, base_branch, research, plan_checker, verifier, parallelization, code_review_enabled, code_review_timing, code_review_severity_gate, code_review_auto_fix_warnings, use_teams, team_timeout_minutes, max_concurrent_teammates, backend, backend_models, autonomous_mode, ceremony, timeouts
    - `interface GrdTimeouts` for the nested timeouts object

    **Phase and milestone types:**
    - `interface PhaseInfo` with fields from findPhaseInternal: found, directory, phase_number, phase_name, phase_slug, plans, summaries, incomplete_plans, has_research, has_context, has_verification, consistency_warning
    - `interface MilestoneInfo { version: string; name: string }`

    **State and artifact types (forward declarations for Phase 60+):**
    - `interface StateFields` with common STATE.md field names as optional string properties
    - `interface RoadmapPhase` with fields from roadmap parsing (number, name, goal, type, depends_on, duration, requirements, verification_level, status, plans_count, plans_complete)
    - `interface FrontmatterObject` as `Record<string, unknown>` with common optional typed fields
    - `interface McpToolDescriptor` with name, description, params (inputSchema), execute function type

    **Utility types:**
    - `interface ExecGitResult { exitCode: number; stdout: string; stderr: string }`
    - `interface RunCache` with init, reset, get methods typed
    - `type AgentModelProfiles = Record<string, Record<ModelProfileName, ModelTier>>`

    **Pattern:**
    - Use `'use strict'` at top
    - Use `module.exports = {}` (empty -- types.ts exports only types)
    - Use `export type { ... }` and `export interface` for all type exports
    - This allows both `import type { Config } from './types'` (TS) and `require('./types')` (JS, returns empty object) to work

    **Avoid:**
    - Do NOT use `any` anywhere in type definitions
    - Do NOT use `namespace` (use plain exports)
    - Do NOT define implementation code -- types.ts is pure type definitions
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- types.ts compiles with zero errors under strict:true
    grep -c "any" lib/types.ts should return 0 (no any types)
  </verify>
  <done>lib/types.ts exists with 15+ exported interfaces/types, compiles cleanly under strict:true, has zero any types, and follows the module.exports + export type pattern</done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/paths.js to lib/paths.ts with full type annotations</name>
  <files>lib/paths.ts, lib/paths.js, jest.config.js</files>
  <action>
    Rename lib/paths.js to lib/paths.ts and add TypeScript type annotations throughout.

    **Step 1: Rename and convert**
    - Copy lib/paths.js to lib/paths.ts
    - Delete lib/paths.js
    - Add `import type` for fs and path where needed (they are already typed via @types/node)
    - Convert `const fs = require('fs')` and `const path = require('path')` to keep require() (CommonJS pattern per project convention from sample.ts)

    **Step 2: Add type annotations to all functions**
    - `currentMilestone(cwd: string): string` -- returns milestone version or 'anonymous'
    - `inferMilestoneFromDisk(cwd: string): string` -- returns milestone version or 'anonymous'
    - `parseRoadmapMilestone(cwd: string): string | null` -- returns version or null
    - `milestoneRoot(cwd: string, milestone: string): string`
    - `milestonesDir(cwd: string): string`
    - `phasesDir(cwd: string, milestone?: string): string` -- note the optional milestone param (currently checked for undefined/null)
    - `phaseDir(cwd: string, milestone: string | undefined, phaseDirName: string): string`
    - `researchDir(cwd: string, milestone?: string): string`
    - `codebaseDir(cwd: string): string`
    - `todosDir(cwd: string, milestone?: string): string`
    - `quickDir(cwd: string, milestone?: string): string`
    - `archivedPhasesDir(cwd: string, version: string): string`
    - `standardsDir(cwd: string, milestone?: string): string`
    - `planningDir(cwd: string): string`

    **Step 3: Fix null/undefined checks for strict mode**
    - The existing `milestone === undefined || milestone === null` checks become `milestone == null` or use proper TypeScript optional parameter syntax
    - Ensure `fs.readdirSync` entries array is properly typed (Dirent[])
    - Handle RegExp match results that can be null (use proper null checks)

    **Step 4: Preserve module.exports**
    - Keep `module.exports = { ... }` at the bottom (CommonJS interop)
    - This ensures `require('./paths')` in all 10 consumer modules continues to work
    - Do NOT use `export default` or named ES exports for runtime values

    **Step 5: Update jest.config.js**
    - Replace `'./lib/paths.js': { lines: 95, functions: 100, branches: 95 }` with `'./lib/paths.ts': { lines: 95, functions: 100, branches: 95 }`
    - Keep thresholds identical (do not lower)

    **Step 6: Verify**
    - `npx tsc --noEmit` passes
    - `npx jest tests/unit/paths.test.js` passes (the .js test file uses require('../../lib/paths') which resolves to .ts via Jest moduleFileExtensions)
    - `npm run lint` passes (eslint validates .ts files via Phase 58 setup)
    - `npm test` passes (full suite, no regressions)

    **Avoid:**
    - Do NOT add `any` type annotations -- use proper types
    - Do NOT change the function logic -- this is a pure type annotation migration
    - Do NOT change the module.exports structure
    - Do NOT change test files (they remain .js and use require())
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- zero type errors
    npx jest tests/unit/paths.test.js --verbose (Level 2: Proxy) -- all tests pass with coverage thresholds met
    npm test (Level 2: Proxy) -- full test suite passes, no regressions
    test -f lib/paths.js && echo "FAIL: paths.js still exists" || echo "OK: paths.js deleted"
  </verify>
  <done>lib/paths.ts compiles under strict:true with zero any types, all 11 exported functions have explicit return types, lib/paths.js is deleted, jest.config.js thresholds updated, all existing tests pass including the 478-line paths.test.js suite</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors on lib/types.ts and lib/paths.ts
Level 1 (Sanity): `npm run lint` passes on new .ts files
Level 2 (Proxy): `npx jest tests/unit/paths.test.js` passes with coverage thresholds >= existing (lines:95, functions:100, branches:95)
Level 2 (Proxy): `npm test` full suite passes (2,676+ tests, zero regressions)
Level 3 (Deferred): CommonJS interop validated with all 10+ downstream consumers (tracked as DEFER-59-01, resolved in Phase 65)
</verification>

<success_criteria>
- lib/types.ts exists with 15+ exported interfaces/types covering Config, BackendCapabilities, PhaseInfo, MilestoneInfo, ModelProfile, and forward declarations
- lib/paths.ts compiles under strict:true with zero any types
- All 11 exported functions in paths.ts have explicit return type annotations
- lib/paths.js is deleted (not duplicated)
- jest.config.js coverage thresholds for paths updated from .js to .ts
- All 2,676+ tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-01-SUMMARY.md`
</output>
