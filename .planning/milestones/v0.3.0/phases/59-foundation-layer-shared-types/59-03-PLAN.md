---
phase: 59-foundation-layer-shared-types
plan: 03
type: execute
wave: 3
depends_on: ["59-01", "59-02"]
files_modified:
  - lib/utils.ts
  - lib/utils.js
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/utils.ts compiles under strict:true with zero any types and explicit return type annotations on all 50+ exported functions"
    - "lib/utils.ts imports from './paths' and './backend' (both now .ts) with proper types"
    - "lib/utils.ts imports shared types from './types' for GrdConfig, ExecGitResult, PhaseInfo, MilestoneInfo, AgentModelProfiles, ModelTier, RunCache"
    - "lib/utils.js is deleted after migration (replaced by lib/utils.ts)"
    - "jest.config.js coverage thresholds updated: lib/utils.js entry replaced by lib/utils.ts with identical thresholds (lines:92, functions:95, branches:85)"
    - "All 17 existing JS modules that import from utils continue to resolve correctly via require('./utils')"
    - "GIT_ALLOWED_COMMANDS, GIT_BLOCKED_COMMANDS, GIT_BLOCKED_FLAGS, MODEL_PROFILES, CODE_EXTENSIONS constants are properly typed"
    - "npx tsc --noEmit passes with zero errors"
    - "npm test passes with all 2,676+ tests with zero regressions"
  artifacts:
    - path: "lib/utils.ts"
      provides: "Shared utilities -- config loading, git operations, validation, phase resolution, output helpers (migrated from utils.js)"
      exports: ["loadConfig", "safeReadFile", "safeReadMarkdown", "safeReadJSON", "execGit", "isGitIgnored", "normalizePhaseName", "findPhaseDir", "parsePhaseNumber", "findPhaseInternal", "resolveModelInternal", "resolveModelForAgent", "getMilestoneInfo", "output", "error", "debugLog", "validatePhaseName", "validateFilePath", "validateGitRef", "validatePhaseArg", "validateFileArg", "validateSubcommand", "validateRequiredArg", "createRunCache", "walkJsFiles", "findCodeFiles", "pathExistsInternal", "generateSlugInternal", "stripShippedSections", "extractMarkdownSection", "parseIncludeFlag", "levenshteinDistance", "findClosestCommand", "clearPhaseCache"]
      min_lines: 800
  key_links:
    - from: "lib/utils.ts"
      to: "lib/backend.ts"
      via: "require for detectBackend, resolveBackendModel"
      pattern: "require.*backend"
    - from: "lib/utils.ts"
      to: "lib/paths.ts"
      via: "require for phasesDir"
      pattern: "require.*paths"
    - from: "lib/utils.ts"
      to: "lib/types.ts"
      via: "import type for GrdConfig, ExecGitResult, PhaseInfo, etc."
      pattern: "import.*type.*from.*types"
    - from: "tests/unit/utils.test.js"
      to: "lib/utils.ts"
      via: "require('./utils') resolved by Jest moduleFileExtensions"
      pattern: "require.*utils"
---

<objective>
Migrate lib/utils.js (1,263 lines, 50+ exports) to TypeScript with full type annotations, completing the foundation layer migration.

Purpose: Convert the largest and most-imported foundation module, which depends on both paths.ts and backend.ts. This is the most complex migration in Phase 59 due to the module's size, diverse function signatures (config loading, git operations, validation, caching, output), and its role as the import hub for 17 downstream modules.

Output: lib/utils.ts fully typed under strict:true, lib/utils.js deleted, jest.config.js updated, all 2,676+ tests passing.
</objective>

<execution_context>
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-01-PLAN.md
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-02-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/utils.js
@lib/types.ts
@lib/paths.ts
@lib/backend.ts
@tsconfig.json
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/utils.js to lib/utils.ts -- constants, helpers, and validation functions</name>
  <files>lib/utils.ts, lib/utils.js</files>
  <action>
    Rename lib/utils.js to lib/utils.ts and add comprehensive TypeScript type annotations. This is the largest migration task in Phase 59 (~1,263 lines).

    **Step 1: Rename and set up imports**
    - Copy lib/utils.js to lib/utils.ts
    - Delete lib/utils.js
    - Add type imports: `import type { GrdConfig, GrdTimeouts, ExecGitResult, PhaseInfo, MilestoneInfo, ModelTier, ModelProfileName, RunCache, AgentModelProfiles } from './types'`
    - Keep all CommonJS requires: `const fs = require('fs')`, `const { detectBackend, resolveBackendModel } = require('./backend')`, `const { phasesDir: getPhasesDirPath } = require('./paths')`

    **Step 2: Type the constants**
    - `const GIT_ALLOWED_COMMANDS: ReadonlySet<string>` (or `Set<string>`)
    - `const GIT_BLOCKED_COMMANDS: ReadonlySet<string>`
    - `const GIT_BLOCKED_FLAGS: ReadonlySet<string>`
    - `const MODEL_PROFILES: AgentModelProfiles` (Record mapping agent names to per-profile tier assignments)
    - `const CODE_EXTENSIONS: ReadonlySet<string>`
    - `const KNOWN_CONFIG_KEYS: ReadonlySet<string>`
    - `const _phaseCache: Map<string, string>` (internal cache)

    **Step 3: Type file I/O helpers**
    - `parseIncludeFlag(args: string[]): Set<string>`
    - `safeReadFile(filePath: string): string | null`
    - `safeReadMarkdown(filePath: string): string | null`
    - `safeReadJSON(filePath: string, defaultValue?: unknown): unknown` -- note: the default parameter type matches the return type; use a generic if appropriate: `safeReadJSON<T = null>(filePath: string, defaultValue?: T): T | Record<string, unknown>`
    - Actually, keep it simple: `safeReadJSON(filePath: string, defaultValue: unknown = null): unknown` -- callers will need to assert types
    - `extractMarkdownSection(content: string, heading: string, level?: number): string | null`

    **Step 4: Type string/distance helpers**
    - `levenshteinDistance(s1: string, s2: string): number`
    - `findClosestCommand(input: string | null, commands: string[]): string | null`

    **Step 5: Type phase utilities**
    - `clearPhaseCache(): void`
    - `normalizePhaseName(phase: string): string` -- throws on invalid input
    - `findCodeFiles(dir: string, maxDepth: number, found: string[], depth: number): string[]`
    - `findPhaseDir(phasesDir: string, phaseArg: string): string | null`
    - `parsePhaseNumber(str: string): string | null`
    - `walkJsFiles(rootDir: string, excludePatterns?: string[]): string[]`
    - `_walkJsDir(rootDir: string, currentDir: string, results: string[], excludePatterns: string[]): void` (internal)

    **Step 6: Type validation functions**
    - `validatePhaseName(phase: string): string` -- returns validated string or throws
    - `validateFilePath(filePath: string, cwd: string): string`
    - `validateGitRef(ref: string): string`
    - `validatePhaseArg(phase: string): string`
    - `validateFileArg(filePath: string, cwd: string): string`
    - `validateSubcommand(sub: string, validSubs: string[], parentCmd: string): string`
    - `validateRequiredArg(value: unknown, argName: string): unknown` -- note: the value parameter is intentionally unknown since it validates any type

    **Step 7: Type git operations**
    - `isGitIgnored(cwd: string, targetPath: string): boolean`
    - `execGit(cwd: string, args: string[], opts?: { allowBlocked?: boolean }): ExecGitResult` -- use ExecGitResult from types.ts

    **Step 8: Type config loading**
    - `loadConfig(cwd: string): GrdConfig` -- returns the fully merged config object
    - The internal `get` helper lambda needs typing: `const get = (key: string, nested?: { section: string; field: string }): unknown => { ... }`
    - The `defaults` object should be typed as GrdConfig (or a partial that gets fully populated)

    **Step 9: Type output functions**
    - `output(result: unknown, raw: boolean, rawValue?: unknown): never` -- calls process.exit(0), so return type is `never`
    - `error(message: string): never` -- calls process.exit(1)
    - `debugLog(message: string, data?: unknown): void`

    **Step 10: Type compound helpers**
    - `resolveModelInternal(cwd: string, agentType: string): string`
    - `findPhaseInternal(cwd: string, phase: string): PhaseInfo | null` -- use PhaseInfo from types.ts
    - `pathExistsInternal(cwd: string, targetPath: string): boolean`
    - `generateSlugInternal(text: string): string | null`
    - `stripShippedSections(content: string): string`
    - `getMilestoneInfo(cwd: string): MilestoneInfo` -- use MilestoneInfo from types.ts
    - `resolveModelForAgent(config: GrdConfig, agentType: string, cwd?: string): string`
    - `createRunCache(): RunCache` -- use RunCache from types.ts

    **Step 11: Type the re-exported Node built-ins**
    - The module re-exports `fs`, `path`, `os`, `execFileSync`. These are already typed via @types/node. Keep as-is in module.exports.

    **Step 12: Handle tricky strict-mode patterns**
    - `loadConfig` has a complex `try/catch` with `parsed` object -- ensure the parsed JSON is typed as `Record<string, unknown>` and individual field accesses use proper type narrowing
    - `err.status` in execGit catch block -- the Error object may not have `status`; use `(err as NodeJS.ErrnoException & { status?: number })` or a type guard
    - `err.stdout` and `err.stderr` in execGit -- same pattern, these are on ExecFileSyncError
    - The `for (const line of lines)` pattern in model parsing sections -- ensure proper null handling on `.split()` results
    - `_phaseCache` Map usage -- ensure key/value types are explicit
    - The lazy `require('./markdown-split')` inside safeReadMarkdown -- use `require` with type assertion or declare the module shape

    **Step 13: Preserve module.exports**
    - Keep the existing `module.exports = { ... }` with all ~40+ exports
    - The re-exported Node built-ins (fs, path, os, execFileSync) remain in module.exports
    - Do NOT use named ES exports for runtime values

    **Avoid:**
    - Do NOT use `any` -- use `unknown` for truly untyped values, or define proper types
    - Do NOT change function logic -- pure type annotation migration
    - Do NOT change the module.exports structure
    - Do NOT modify test files
    - Do NOT attempt to fix the `output`/`error` functions that call `process.exit()` -- keep them as `never` return type
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- zero type errors across entire project
    npm run lint (Level 1: Sanity) -- zero lint errors on lib/utils.ts
    test -f lib/utils.js && echo "FAIL: utils.js still exists" || echo "OK: utils.js deleted"
  </verify>
  <done>lib/utils.ts compiles under strict:true with zero any types, all 50+ exported functions have explicit return types, imports from ./backend and ./paths resolve to .ts modules, lib/utils.js is deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update jest.config.js and run full validation suite</name>
  <files>jest.config.js</files>
  <action>
    Update coverage thresholds and run the complete validation suite for the entire Phase 59 migration.

    **Step 1: Update jest.config.js**
    - Replace `'./lib/utils.js': { lines: 92, functions: 95, branches: 85 }` with `'./lib/utils.ts': { lines: 92, functions: 95, branches: 85 }`
    - Keep thresholds identical (do not lower)
    - Verify that lib/paths.ts and lib/backend.ts entries already exist (from Plans 01 and 02)

    **Step 2: Remove lib/sample.ts if desired**
    - The sample.ts was created in Phase 58 as a proof-of-concept. Now that we have real .ts modules, it can optionally be kept or removed. For now, KEEP it -- it has 15 tests and serves as a regression anchor. It will be removed in a later phase.

    **Step 3: Run comprehensive validation**
    - `npx tsc --noEmit` -- validates all three .ts modules compile together
    - `npx jest tests/unit/paths.test.js --verbose` -- paths test suite
    - `npx jest tests/unit/backend.test.js --verbose` -- backend test suite
    - `npx jest tests/unit/utils.test.js --verbose` -- utils test suite (1,334 lines, the largest)
    - `npm test` -- full test suite (2,676+ tests)
    - `npm run lint` -- all .ts and .js files lint cleanly

    **Step 4: Verify no any types leaked**
    - Run: `grep -n '\bany\b' lib/types.ts lib/paths.ts lib/backend.ts lib/utils.ts` -- should return zero matches (excluding comments)
    - Specifically check that no `as any` casts were introduced

    **Step 5: Verify all return types are explicit**
    - Every exported function in paths.ts, backend.ts, and utils.ts must have an explicit return type annotation (not inferred)
    - This is a phase success criterion -- spot-check by looking at the module.exports functions

    **Step 6: Verify CommonJS interop**
    - The 17 JS modules that import from utils.js must all still work. Since Jest resolves .ts via moduleFileExtensions (set up in Phase 58), all test-time require() calls resolve correctly.
    - Runtime interop (node bin/grd-tools.js) is deferred to Phase 65 (DEFER-59-01)

    **Avoid:**
    - Do NOT lower any coverage thresholds
    - Do NOT modify test files (they remain .js)
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- zero errors
    npm test (Level 2: Proxy) -- all 2,676+ tests pass, zero regressions
    npm run lint (Level 1: Sanity) -- zero errors
    npx jest --coverage --coverageReporters=text-summary (Level 2: Proxy) -- per-file thresholds met for all three .ts modules
  </verify>
  <done>jest.config.js updated with .ts coverage thresholds for all three foundation modules. Full test suite (2,676+ tests) passes. All three .ts modules compile, lint, and meet coverage thresholds. Zero any types across the foundation layer. Phase 59 complete.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors including lib/utils.ts
Level 1 (Sanity): `npm run lint` passes on all .ts files
Level 1 (Sanity): `grep -n '\bany\b' lib/types.ts lib/paths.ts lib/backend.ts lib/utils.ts` returns zero matches
Level 2 (Proxy): `npx jest tests/unit/utils.test.js` passes with coverage thresholds (lines:92, functions:95, branches:85)
Level 2 (Proxy): `npm test` full suite passes (2,676+ tests, zero regressions)
Level 2 (Proxy): All three foundation modules have explicit return type annotations on every exported function
Level 3 (Deferred): Runtime CLI path `node bin/grd-tools.js` with .ts sources (DEFER-59-01, Phase 65)
Level 3 (Deferred): All 20+ downstream JS consumers validate CommonJS interop (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/utils.ts compiles under strict:true with zero any types
- All 50+ exported functions have explicit return type annotations
- lib/utils.ts correctly imports from ./backend (now .ts) and ./paths (now .ts)
- lib/utils.js is deleted (not duplicated)
- jest.config.js coverage thresholds for utils updated from .js to .ts
- All 2,676+ tests pass with zero regressions
- Foundation layer migration complete: types.ts + paths.ts + backend.ts + utils.ts all typed and tested
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-03-SUMMARY.md`
</output>
