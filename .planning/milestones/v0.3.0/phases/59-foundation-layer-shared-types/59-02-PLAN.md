---
phase: 59-foundation-layer-shared-types
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - lib/backend.ts
  - lib/backend.js
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/backend.ts compiles under strict:true with zero any types and explicit return type annotations on all 11 exported functions"
    - "VALID_BACKENDS, DEFAULT_BACKEND_MODELS, and BACKEND_CAPABILITIES constants are typed using BackendId, ModelTierMap, and BackendCapabilities from lib/types.ts"
    - "lib/backend.js is deleted after migration (replaced by lib/backend.ts)"
    - "jest.config.js coverage thresholds updated: lib/backend.js entry replaced by lib/backend.ts with identical thresholds (lines:95, functions:100, branches:88)"
    - "All 5 existing JS modules that import from backend continue to resolve correctly via require('./backend')"
    - "npx tsc --noEmit passes with zero errors"
    - "npm test passes with all existing tests plus backend tests running against the .ts source"
  artifacts:
    - path: "lib/backend.ts"
      provides: "AI coding CLI backend detection, model resolution, and capability flags (migrated from backend.js)"
      exports: ["VALID_BACKENDS", "DEFAULT_BACKEND_MODELS", "BACKEND_CAPABILITIES", "detectBackend", "resolveBackendModel", "getBackendCapabilities", "parseOpenCodeModels", "detectModels", "getCachedModels", "clearModelCache", "detectWebMcp"]
      min_lines: 300
  key_links:
    - from: "lib/backend.ts"
      to: "lib/types.ts"
      via: "import type for BackendId, BackendCapabilities, ModelTierMap, ModelTier, WebMcpResult, GrdConfig"
      pattern: "import.*type.*from.*types"
    - from: "tests/unit/backend.test.js"
      to: "lib/backend.ts"
      via: "require('./backend') resolved by Jest moduleFileExtensions"
      pattern: "require.*backend"
    - from: "lib/utils.ts"
      to: "lib/backend.ts"
      via: "require('./backend') for detectBackend, resolveBackendModel (validated in Plan 03)"
      pattern: "require.*backend"
---

<objective>
Migrate lib/backend.js to TypeScript with full type annotations using shared types from lib/types.ts.

Purpose: Convert the second zero-dependency foundation module, proving the migration pattern on a module with richer type shapes (constants tables, dynamic model detection, cached results, environment variable parsing).

Output: lib/backend.ts fully typed under strict:true, lib/backend.js deleted, jest.config.js updated with .ts coverage thresholds.
</objective>

<execution_context>
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-01-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/backend.js
@lib/types.ts
@lib/paths.ts
@tsconfig.json
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/backend.js to lib/backend.ts with strict type annotations</name>
  <files>lib/backend.ts, lib/backend.js, jest.config.js</files>
  <action>
    Rename lib/backend.js to lib/backend.ts and add TypeScript type annotations throughout, importing shared types from lib/types.ts (created in Plan 01).

    **Step 1: Rename and convert**
    - Copy lib/backend.js to lib/backend.ts
    - Delete lib/backend.js
    - Add imports: `import type { BackendId, BackendCapabilities, ModelTierMap, ModelTier, WebMcpResult } from './types'`
    - Keep `const fs = require('fs')`, `const os = require('os')`, etc. (CommonJS require pattern)

    **Step 2: Type the constants**
    - `const VALID_BACKENDS: readonly BackendId[]` (use `as const` if needed, or explicit type annotation)
    - `const DEFAULT_BACKEND_MODELS: Record<BackendId, ModelTierMap>` -- maps each backend to its tier-to-model-name mapping
    - `const BACKEND_CAPABILITIES: Record<BackendId, BackendCapabilities>` -- maps each backend to its capability flags

    **Step 3: Type internal helpers**
    - `readConfig(cwd: string): Record<string, unknown> | null` -- returns parsed JSON or null (use Record<string, unknown> since this is the raw config before loadConfig processing; the full GrdConfig type is constructed in utils.ts loadConfig)
    - `hasEnvPrefix(prefix: string): boolean`
    - `fileExists(filePath: string): boolean`

    **Step 4: Type exported functions**
    - `detectBackend(cwd: string): BackendId`
    - `parseOpenCodeModels(stdout: string): { opus: string | null; sonnet: string | null; haiku: string | null } | null`
    - `detectModels(backend: string, cwd?: string): { opus: string | null; sonnet: string | null; haiku: string | null } | null`
    - `getCachedModels(backend: string, cwd?: string): { opus: string | null; sonnet: string | null; haiku: string | null } | null`
    - `clearModelCache(): void`
    - `resolveBackendModel(backend: string, tier: ModelTier, config?: Record<string, unknown>, cwd?: string): string | undefined` -- note: accepts broader backend string for forward compatibility, config is raw parsed JSON
    - `getBackendCapabilities(backend: string): BackendCapabilities` -- accepts broader string, returns claude defaults for unknown
    - `detectWebMcp(cwd: string): WebMcpResult`

    **Step 5: Type the model cache**
    - `const _modelCache: Map<string, { models: { opus: string | null; sonnet: string | null; haiku: string | null } | null; ts: number }>` -- be explicit about the Map's value type
    - `const MODEL_CACHE_TTL_MS: number`

    **Step 6: Handle strict mode requirements**
    - The `config?.timeouts?.backend_detect_ms` optional chaining already works in TS
    - The `execFileSync` return type needs to be `string` (encoding is 'utf-8')
    - RegExp match results that can be null need proper null checks (already handled in current code)
    - The `config && config.backend_models` pattern: use optional chaining or proper null narrowing

    **Step 7: Preserve module.exports**
    - Keep `module.exports = { VALID_BACKENDS, DEFAULT_BACKEND_MODELS, BACKEND_CAPABILITIES, detectBackend, resolveBackendModel, getBackendCapabilities, parseOpenCodeModels, detectModels, getCachedModels, clearModelCache, detectWebMcp }`
    - Do NOT use named ES exports for runtime values

    **Step 8: Update jest.config.js**
    - Replace `'./lib/backend.js': { lines: 95, functions: 100, branches: 88 }` with `'./lib/backend.ts': { lines: 95, functions: 100, branches: 88 }`
    - Keep thresholds identical

    **Avoid:**
    - Do NOT use `any` -- use `Record<string, unknown>` for untyped objects, or define proper interfaces
    - Do NOT change function logic -- pure type annotation migration
    - Do NOT modify the module.exports structure
    - Do NOT modify test files (they remain .js)
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- zero type errors
    npx jest tests/unit/backend.test.js --verbose (Level 2: Proxy) -- all 901-line test suite passes with coverage thresholds met
    npm run lint (Level 1: Sanity) -- zero lint errors on lib/backend.ts
    test -f lib/backend.js && echo "FAIL: backend.js still exists" || echo "OK: backend.js deleted"
  </verify>
  <done>lib/backend.ts compiles under strict:true with zero any types, all 11 exported functions have explicit return types, VALID_BACKENDS/DEFAULT_BACKEND_MODELS/BACKEND_CAPABILITIES use proper typed constants, lib/backend.js is deleted, all 901-line backend test suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Validate cross-module type resolution and full test suite</name>
  <files>lib/backend.ts</files>
  <action>
    After migration, run the complete validation suite to ensure nothing is broken.

    **Step 1: Verify type imports resolve**
    - `npx tsc --noEmit` must pass -- this validates that lib/backend.ts can import from lib/types.ts and that all type references resolve
    - Check that lib/utils.js still compiles (it imports from './backend' -- since allowJs:true, tsc should handle the .js importing from .ts)

    **Step 2: Run full test suite**
    - `npm test` must pass with zero regressions
    - Specifically verify that tests in: backend.test.js, utils.test.js, context.test.js, parallel.test.js, evolve.test.js all pass (these import from backend directly or transitively)

    **Step 3: Verify CommonJS interop**
    - Create a quick Node.js verification: `node -e "const b = require('./lib/backend'); console.log(typeof b.detectBackend)"` should print "function"
    - This proves that require('./backend') from a plain JS file resolves correctly to the .ts source via ts-jest in tests and via Node's module resolution (since tsconfig has allowJs:true and the .js file no longer exists, Node will not find .js and jest will find .ts via moduleFileExtensions)

    NOTE: For runtime (non-test) usage, consumers still run against the .js source files. Since we deleted backend.js and replaced with backend.ts, we need to ensure that either: (a) the build step produces dist/lib/backend.js, or (b) ts-node or similar is used at runtime. For this project, the runtime path is `node bin/grd-tools.js` which requires lib/ files. Since allowJs is true and we are not yet running from dist/, we rely on the fact that Node.js can't require .ts files directly. Therefore, we MUST ensure that either ts-node is registered, or we keep a build step.

    IMPORTANT: Check if the project uses ts-node or similar for runtime. If not, the migration strategy must ensure that either:
    - Runtime still uses compiled .js from dist/, OR
    - A require hook (ts-node/register) is added, OR
    - The .ts files are compiled in-place alongside (not practical)

    For THIS phase, the approach is: tests use ts-jest (handles .ts files), and runtime usage is DEFERRED to Phase 65 (integration validation). The .ts files work in tests but the full CLI runtime path through `node bin/grd-tools.js` is validated in Phase 65. This is already captured as DEFER-59-01.

    **Step 4: Run lint**
    - `npm run lint` must pass on all .ts files
  </action>
  <verify>
    npx tsc --noEmit (Level 1: Sanity) -- zero errors across entire project
    npm test (Level 2: Proxy) -- all 2,676+ tests pass with zero regressions
    npm run lint (Level 1: Sanity) -- zero lint errors
  </verify>
  <done>Full project compiles, lints, and tests cleanly with both lib/paths.ts and lib/backend.ts as TypeScript modules. Cross-module imports (types.ts -> paths.ts, types.ts -> backend.ts) resolve correctly. Full test suite passes.</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors including lib/backend.ts
Level 1 (Sanity): `npm run lint` passes on lib/backend.ts
Level 2 (Proxy): `npx jest tests/unit/backend.test.js` passes with coverage thresholds (lines:95, functions:100, branches:88)
Level 2 (Proxy): `npm test` full suite passes (2,676+ tests, zero regressions)
Level 3 (Deferred): Runtime CLI path `node bin/grd-tools.js` with .ts sources (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/backend.ts compiles under strict:true with zero any types
- All 11 exported functions have explicit return type annotations
- Constants (VALID_BACKENDS, DEFAULT_BACKEND_MODELS, BACKEND_CAPABILITIES) use typed interfaces from lib/types.ts
- lib/backend.js is deleted (not duplicated)
- jest.config.js coverage thresholds updated from .js to .ts
- All 2,676+ tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-02-SUMMARY.md`
</output>
