---
phase: "66-extend-autopilot-for-multi-milestone-automation"
plan: 03
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - tests/unit/autopilot.test.ts
  - jest.config.js
verification_level: proxy
autonomous: true

must_haves:
  truths:
    - "Unit tests cover isMilestoneComplete with various phase states"
    - "Unit tests cover resolveNextMilestone with LT roadmap present and absent"
    - "Unit tests cover buildNewMilestonePrompt and buildMilestoneCompletePrompt"
    - "Unit tests cover runMultiMilestoneAutopilot with mocked spawns for milestone transitions"
    - "Unit tests cover cmdMultiMilestoneAutopilot CLI flag parsing"
    - "Unit tests cover cmdInitMultiMilestoneAutopilot pre-flight output"
    - "All existing autopilot tests still pass without modification"
    - "Coverage thresholds in jest.config.js are met for the expanded autopilot.ts"
    - "npm test passes with all 2,676+ tests"
  artifacts:
    - path: "tests/unit/autopilot.test.ts"
      provides: "Multi-milestone autopilot test coverage"
      min_lines: 1800
    - path: "jest.config.js"
      provides: "Updated coverage thresholds for autopilot.ts"
  key_links:
    - from: "tests/unit/autopilot.test.ts"
      to: "lib/autopilot.ts"
      via: "require and test all new exports"
      pattern: "runMultiMilestoneAutopilot|isMilestoneComplete|resolveNextMilestone"
---

<objective>
Add comprehensive tests for the multi-milestone autopilot feature. Cover all new functions, edge cases, and ensure coverage thresholds are maintained or improved.

Purpose: Validate multi-milestone orchestration logic with mocked subprocess spawns to ensure correctness without requiring actual Claude CLI invocations.

Output: Expanded test suite in tests/unit/autopilot.test.ts with 30+ new tests covering all new functions and integration scenarios.
</objective>

<execution_context>
@tests/unit/autopilot.test.ts
@lib/autopilot.ts
@jest.config.js
@.planning/milestones/v0.3.0/phases/66-extend-autopilot-for-multi-milestone-automation/66-01-SUMMARY.md
@.planning/milestones/v0.3.0/phases/66-extend-autopilot-for-multi-milestone-automation/66-02-SUMMARY.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for new helper functions</name>
  <files>tests/unit/autopilot.test.ts</files>
  <action>
    Add new test describes in tests/unit/autopilot.test.ts for the new functions. Follow the existing test patterns (createAutopilotFixture, tmpDir-based tests, mocked child_process).

    1. **Update imports** at top of file to include new exports:
       ```typescript
       const {
         // ...existing imports...
         isMilestoneComplete,
         resolveNextMilestone,
         buildNewMilestonePrompt,
         buildMilestoneCompletePrompt,
         runMultiMilestoneAutopilot,
         cmdMultiMilestoneAutopilot,
         cmdInitMultiMilestoneAutopilot,
       } = require('../../lib/autopilot');
       ```

    2. **describe('isMilestoneComplete')** -- Test milestone completion detection:
       - Returns false when no phases exist
       - Returns false when some phases are incomplete (mixed complete/partial status)
       - Returns true when ALL phases have disk_status 'complete'
       - Returns true when all phases have roadmap_complete true
       - Returns false for an empty roadmap
       - Create fixtures by writing ROADMAP.md with phases that have various completion states and create matching phase directories with plans and summaries

    3. **describe('resolveNextMilestone')** -- Test next milestone resolution:
       - Returns null when no LONG-TERM-ROADMAP.md exists
       - Returns null when LT roadmap has no planned/active milestones
       - Returns the next planned LT milestone info when available
       - Correctly skips shipped/completed LT milestones
       - Create LONG-TERM-ROADMAP.md fixtures with various milestone configurations

    4. **describe('buildNewMilestonePrompt')** -- Test prompt generation:
       - Returns string containing 'grd:new-milestone'
       - Returns string containing 'Skill tool'
       - Returns string containing 'Autonomous mode'

    5. **describe('buildMilestoneCompletePrompt')** -- Test prompt generation:
       - Returns string containing 'milestone complete'
       - Includes the version string passed as argument
       - Returns string containing 'grd-tools'
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts --verbose 2>&1 | tail -30 (Level 2: Proxy -- new tests pass)
  </verify>
  <done>
    All new helper function tests pass. isMilestoneComplete, resolveNextMilestone, buildNewMilestonePrompt, buildMilestoneCompletePrompt each have comprehensive test coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for multi-milestone orchestration and CLI handlers</name>
  <files>tests/unit/autopilot.test.ts, jest.config.js</files>
  <action>
    1. **describe('runMultiMilestoneAutopilot')** -- Test the main orchestration loop:

       a. **dry-run mode:** Provide a fixture where the current milestone is complete and a LONG-TERM-ROADMAP.md with a next milestone exists. Call with dryRun: true. Assert:
          - milestones_attempted >= 1
          - No actual claude processes spawned
          - Result includes milestone_results array with status 'dry-run'

       b. **stops when no next milestone:** Provide a fixture where the current milestone is complete but no LT roadmap exists. Assert:
          - stopped_at contains message about no next milestone
          - milestones_completed reflects the current milestone only

       c. **maxMilestones safety cap:** Call with maxMilestones: 1 and a fixture where the milestone is complete with a next milestone available. Assert:
          - Loop exits after 1 milestone even though more are available
          - milestones_attempted <= 1

       d. **handles spawn failure gracefully:** Mock spawnClaude to return exitCode: 1. Assert:
          - The function does not throw
          - Result includes failure information
          - stopped_at indicates the failure

       e. **processes incomplete phases before milestone transition:** Provide a fixture with some incomplete phases. Mock spawnClaude to succeed. Assert:
          - runAutopilot is called for the remaining phases before milestone completion

       For mocking, use the existing pattern from the autopilot tests:
       - `jest.spyOn(childProcess, 'spawnSync')` for spawnClaude
       - `jest.spyOn(childProcess, 'spawn')` for spawnClaudeAsync
       - Mock them to return/emit success exit codes

    2. **describe('cmdMultiMilestoneAutopilot')** -- Test CLI flag parsing:
       - Parses --max-milestones flag correctly
       - Parses --dry-run flag
       - Parses --resume flag
       - Parses --timeout and --max-turns flags
       - Parses --model flag
       - Uses captureOutput to verify JSON output format

    3. **describe('cmdInitMultiMilestoneAutopilot')** -- Test pre-flight:
       - Returns claude_available field
       - Returns lt_roadmap_exists field
       - Returns current_milestone info
       - Returns next_milestone info (when LT roadmap exists)
       - Returns milestone_complete status

    4. **Update jest.config.js coverage thresholds:**
       - The autopilot.ts file will grow by ~150-200 lines (new functions)
       - Adjust thresholds if needed, but aim to maintain or exceed current levels:
         - Current: lines: 93, functions: 93, branches: 80
         - After expansion the new functions should be well-tested, so thresholds should remain achievable
       - If any threshold needs a slight adjustment (e.g., branches from 80 to 78 due to new branch paths), make the minimal adjustment with a comment explaining why

    5. **Run the full test suite** to ensure no regressions:
       ```bash
       npm test
       ```
  </action>
  <verify>
    npx jest tests/unit/autopilot.test.ts --verbose --coverage 2>&1 | tail -40 (Level 2: Proxy -- all tests pass with coverage)
    npm test (Level 1: Sanity -- full suite passes)
  </verify>
  <done>
    30+ new tests covering all multi-milestone autopilot functions. Coverage thresholds met. Full test suite (2,676+ tests) passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx tsc --noEmit` passes
- All existing tests pass without modification
- ESLint passes on modified files

Level 2 (Proxy):
- New tests exercise all new functions with various inputs
- Coverage thresholds met for autopilot.ts
- Full `npm test` passes with 2,676+ tests
</verification>

<success_criteria>
- 30+ new tests added to tests/unit/autopilot.test.ts
- isMilestoneComplete tested with 5+ scenarios
- resolveNextMilestone tested with 4+ scenarios
- runMultiMilestoneAutopilot tested with 5+ scenarios (dry-run, no-next, cap, failure, incomplete)
- cmdMultiMilestoneAutopilot flag parsing tested
- cmdInitMultiMilestoneAutopilot pre-flight tested
- jest.config.js coverage thresholds maintained or minimally adjusted
- npm test passes with all tests
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/66-extend-autopilot-for-multi-milestone-automation/66-03-SUMMARY.md`
</output>
