---
phase: 65-integration-validation-documentation
plan: 04
type: execute
wave: 2
depends_on: [1]
files_modified:
  - CLAUDE.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "CLAUDE.md Commands table shows correct test count (2,646+ not 1,631) and includes npm run build / npm run build:check"
    - "CLAUDE.md Source Architecture tree shows .ts file extensions for all lib/ and bin/ files"
    - "CLAUDE.md Source Architecture includes decomposed modules: lib/commands/ (12 sub-modules), lib/context/ (7 sub-modules), lib/evolve/ (10 sub-modules)"
    - "CLAUDE.md Source Architecture includes lib/types.ts, lib/requirements.ts, lib/markdown-split.ts (new modules added during migration)"
    - "CLAUDE.md Code Style section reflects TypeScript conventions (strict mode, typed require, CJS proxy pattern, no any in core)"
    - "CLAUDE.md Testing section shows .ts test file extensions and mentions ts-jest transform"
    - "CLAUDE.md Key Files section mentions tsconfig.json, tsconfig.build.json, and dist/ build"
    - "CLAUDE.md test example uses .test.ts extension"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project documentation reflecting full TypeScript migration"
  key_links:
    - from: "CLAUDE.md"
      to: "lib/*.ts, bin/*.ts"
      via: "Source Architecture section accurately describes actual file structure"
    - from: "CLAUDE.md"
      to: "package.json"
      via: "Commands table matches actual npm scripts"
---

<objective>
Update CLAUDE.md to accurately reflect the fully migrated TypeScript codebase: file extensions (.ts), build commands, decomposed module structure, TypeScript code style conventions, and correct test counts.

Purpose: CLAUDE.md is the primary reference document that Claude Code agents read to understand the project. It currently describes the pre-migration JavaScript codebase with .js extensions, 19 modules, and 1,631 tests. After the v0.3.0 migration, the codebase has .ts files, 21+ modules (with 3 decomposed into sub-modules), 2,646+ tests, and new build commands. Stale documentation leads to incorrect agent behavior.

Output: Updated CLAUDE.md that accurately describes the TypeScript codebase.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@package.json
@tsconfig.json
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md Commands, Source Architecture, Key Files, Testing, and Code Style sections</name>
  <files>CLAUDE.md</files>
  <action>
    Update CLAUDE.md with the following changes. Preserve all sections that don't need updating (Planning Directory, R&D Workflow, Tiered Verification, etc.).

    **1. Commands table — update test count and add build commands:**

    Replace:
    ```
    | `npm test` | Run all tests with coverage (1,631 tests) |
    ```
    With:
    ```
    | `npm test` | Run all tests with coverage (2,646+ tests) |
    ```

    Add new rows after the format commands:
    ```
    | `npm run build` | Compile TypeScript to `dist/` |
    | `npm run build:check` | Type-check without emitting (`tsc --noEmit`) |
    ```

    Update the single test file example:
    ```
    Single test file: `npx jest tests/unit/state.test.ts`
    ```

    **2. Source Architecture tree — update to .ts extensions and show decomposed modules:**

    Replace the entire Source Architecture code block with:
    ```
    bin/
    ├── grd-tools.ts            # Main CLI — all deterministic operations
    ├── grd-tools.js            # CJS proxy (requires grd-tools.ts)
    ├── grd-mcp-server.ts       # MCP server for tool exposure
    ├── grd-mcp-server.js       # CJS proxy
    ├── grd-manifest.ts         # SHA256 file tracking for self-update
    └── postinstall.ts          # npm postinstall hook
    lib/                        # 21 modules + 3 decomposed sub-module directories
    ├── autopilot.ts            # Autopilot orchestration
    ├── backend.ts              # Claude Code backend detection + capabilities
    ├── cleanup.ts              # Phase-boundary quality analysis
    ├── commands/               # CLI command routing (decomposed from commands.js)
    │   ├── index.ts            # Barrel re-export + routing
    │   ├── dashboard.ts        # Dashboard rendering
    │   ├── health.ts           # Health check commands
    │   ├── progress.ts         # Progress display
    │   └── ... (12 sub-modules)
    ├── context/                # Context optimization (decomposed from context.js)
    │   ├── index.ts            # Barrel re-export
    │   ├── base.ts             # Shared init context builder
    │   ├── execute.ts          # Execute-phase init
    │   └── ... (7 sub-modules)
    ├── deps.ts                 # Dependency management
    ├── evolve/                 # Self-evolution loop (decomposed from evolve.js)
    │   ├── index.ts            # Barrel re-export
    │   ├── orchestrator.ts     # Evolution orchestration
    │   ├── discovery.ts        # Codebase discovery
    │   └── ... (10 sub-modules)
    ├── frontmatter.ts          # YAML frontmatter CRUD
    ├── gates.ts                # Research + confirmation gates
    ├── long-term-roadmap.ts    # LT milestone CRUD + protection rules
    ├── markdown-split.ts       # Markdown file splitting for context
    ├── mcp-server.ts           # MCP tool registration
    ├── parallel.ts             # Parallel execution engine
    ├── paths.ts                # Milestone-scoped path resolution for .planning/
    ├── phase.ts                # Phase lifecycle (add/insert/remove/complete)
    ├── requirements.ts         # Requirements parsing + traceability
    ├── roadmap.ts              # ROADMAP.md parsing + manipulation
    ├── scaffold.ts             # Directory/file scaffolding
    ├── state.ts                # STATE.md read/write/patch
    ├── tracker.ts              # GitHub Issues / MCP Atlassian sync
    ├── types.ts                # Shared TypeScript type definitions
    ├── utils.ts                # Shared utilities (slug, date, markdown)
    ├── verify.ts               # Plan/phase/commit verification suite
    └── worktree.ts             # Git worktree parallel execution
    commands/                   # 39 skill definitions (markdown with frontmatter)
    agents/                     # 19 subagent definitions (markdown with frontmatter)
    tests/
    ├── unit/                   # Unit tests — one per lib/ module (.test.ts)
    ├── integration/            # CLI + E2E workflow tests (.test.ts)
    ├── golden/                 # Golden output snapshot tests
    ├── fixtures/               # Shared test fixtures
    └── helpers/                # Test utilities (.ts)
    dist/                       # Compiled TypeScript output (generated by npm run build)
    docs/                       # Tutorials, quickstart, diagrams
    .claude-plugin/plugin.json  # Claude Code plugin manifest
    ```

    **3. Key Files — add TypeScript build files:**

    Replace:
    ```
    - `bin/grd-tools.js` — Entry point for all CLI operations; commands call this
    ```
    With:
    ```
    - `bin/grd-tools.ts` — Main CLI implementation (TypeScript)
    - `bin/grd-tools.js` — CJS proxy entry point; requires grd-tools.ts at runtime
    - `tsconfig.json` — TypeScript config (strict mode, noEmit for type-checking)
    - `tsconfig.build.json` — Build config (extends tsconfig.json, emits to dist/)
    ```

    Keep the existing entries for .planning/config.json, STATE.md, ROADMAP.md, jest.config.js, eslint.config.js.

    **4. Testing section — update for TypeScript:**

    Replace:
    ```
    - Tests mirror `lib/` structure: `lib/state.js` → `tests/unit/state.test.js`
    ```
    With:
    ```
    - Tests mirror `lib/` structure: `lib/state.ts` → `tests/unit/state.test.ts`
    - All 37 test files are TypeScript (.test.ts), transformed via ts-jest
    ```

    **5. Code Style section — replace with TypeScript conventions:**

    Replace the entire Code Style section with:
    ```
    ## Code Style

    - TypeScript with `strict: true` (all lib/ and bin/ files are .ts)
    - CommonJS `require()`/`module.exports` pattern (not ESM imports)
    - CJS proxy pattern: each lib/*.js is a thin proxy that `require()`s the .ts file
    - `'use strict'` at top of every file
    - ESLint flat config with `typescript-eslint` for .ts files
    - Prefix unused function args with `_` (e.g., `function handler(_req: Request, res: Response)`)
    - Prettier for formatting (no config file — uses defaults)
    - Node >=18 required
    - Zero `any` types in core lib/ modules (use `Record<string, unknown>` or specific interfaces)
    - Typed `require()` pattern: `const { fn } = require('./module') as { fn: (arg: Type) => ReturnType }`
    ```

    **6. Self-Update section — update file extension:**

    Change `bin/grd-manifest.js` to `bin/grd-manifest.ts`.

    **DO NOT** change sections that are still accurate: Planning Directory, R&D Workflow, Tiered Verification, Scale-Adaptive Ceremony, Autonomous Mode, Tracker Integration, Key Commands, Agent Model Profiles, Configuration, Git Isolation, CLI Tooling, Workflow Init.
  </action>
  <verify>
    1. grep "2,646" CLAUDE.md — confirms updated test count (Level 1: Sanity)
    2. grep "\.ts" CLAUDE.md | head -10 — confirms .ts extensions in architecture (Level 1: Sanity)
    3. grep "npm run build" CLAUDE.md — confirms build commands added (Level 1: Sanity)
    4. grep "commands/" CLAUDE.md — confirms decomposed module structure documented (Level 1: Sanity)
    5. grep "strict: true" CLAUDE.md — confirms TypeScript code style documented (Level 1: Sanity)
    6. grep "types.ts\|requirements.ts\|markdown-split.ts" CLAUDE.md — confirms new modules listed (Level 1: Sanity)
  </verify>
  <done>
    CLAUDE.md accurately reflects the TypeScript codebase: .ts file extensions, 2,646+ tests, build commands, decomposed module structure (commands/, context/, evolve/), TypeScript code style conventions, CJS proxy pattern documented, new modules (types.ts, requirements.ts, markdown-split.ts) listed, dist/ directory mentioned.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): CLAUDE.md contains .ts extensions, correct test count, build commands, decomposed modules, TypeScript code style.
Level 2 (Proxy): N/A (documentation-only plan).
Level 3 (Deferred): N/A.
</verification>

<success_criteria>
- CLAUDE.md Source Architecture shows .ts files and decomposed module directories
- CLAUDE.md Commands table includes `npm run build` and `npm run build:check`
- CLAUDE.md test count is 2,646+ (not 1,631)
- CLAUDE.md Code Style section describes TypeScript conventions
- All unchanged sections (Planning Directory through Self-Update) remain intact
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/65-integration-validation-documentation/65-04-SUMMARY.md`
</output>
