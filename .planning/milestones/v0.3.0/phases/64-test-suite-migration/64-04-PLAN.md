---
phase: 64-test-suite-migration
plan: 04
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/integration/cli.test.ts
  - tests/integration/e2e-workflow.test.ts
  - tests/integration/evolve-e2e.test.ts
  - tests/integration/golden.test.ts
  - tests/integration/npm-pack.test.ts
  - tests/integration/worktree-parallel-e2e.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "All 6 integration/E2E test files renamed from .test.js to .test.ts and compile under ts-jest"
    - "Subprocess spawn helpers (runCLI, execFileSync wrappers) have typed parameters and return types"
    - "CLI output parsing (JSON.parse of stdout) has typed results"
    - "execFileSync error catch blocks properly typed for ChildProcess errors"
    - "All integration tests pass with zero regressions"
    - "Golden test infrastructure works correctly with .ts test file"
  artifacts:
    - path: "tests/integration/cli.test.ts"
      provides: "Typed integration tests for all CLI commands (2,506 lines)"
    - path: "tests/integration/golden.test.ts"
      provides: "Typed golden snapshot comparison tests"
    - path: "tests/integration/worktree-parallel-e2e.test.ts"
      provides: "Typed cross-module integration tests"
  key_links:
    - from: "tests/integration/cli.test.ts"
      to: "bin/grd-tools.js"
      via: "execFileSync('node', [GRD_TOOLS, ...args]) subprocess invocation"
    - from: "tests/integration/golden.test.ts"
      to: "tests/golden/output/"
      via: "GOLDEN_DIR constant and fs.readdirSync"
    - from: "tests/integration/worktree-parallel-e2e.test.ts"
      to: "lib/worktree.ts, lib/deps.ts, lib/parallel.ts"
      via: "Cross-module require paths through CJS proxies"
---

<objective>
Migrate all 6 integration and E2E test files from .test.js to .test.ts with typed subprocess helpers and output parsing.

Purpose: Complete the test suite migration by converting integration tests that spawn real CLI processes. These tests use execFileSync to invoke bin/grd-tools.js and parse stdout -- the subprocess helper functions and error handling need proper TypeScript types.

Output: 6 .test.ts files replacing their .test.js counterparts in tests/integration/.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/helpers/setup.ts
@tests/helpers/fixtures.ts
@jest.config.js
@.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CLI integration and E2E workflow test files to TypeScript</name>
  <files>
    tests/integration/cli.test.ts, tests/integration/e2e-workflow.test.ts,
    tests/integration/evolve-e2e.test.ts
  </files>
  <action>
    For each of the 3 files, apply this migration pattern:

    1. **Rename** .test.js to .test.ts
    2. **Update file header comment**
    3. **Keep all require() calls as-is** (including `require('child_process')`, `require('fs')`, etc.)
    4. **Type the subprocess helper functions:**
       - cli.test.js has a local `runCLI(args, cwd)` function:
         ```typescript
         interface CLIResult {
           stdout: string;
           stderr: string;
           exitCode: number;
         }
         function runCLI(args: string[], cwd: string): CLIResult {
         ```
       - Type `execFileSync` error in catch blocks:
         ```typescript
         catch (err: unknown) {
           const e = err as { stdout?: string; stderr?: string; status?: number };
           return { stdout: e.stdout || '', stderr: e.stderr || '', exitCode: e.status || 1 };
         }
         ```
    5. **Type JSON.parse results** from CLI stdout:
       - `const result = JSON.parse(stdout) as Record<string, unknown>`
       - For known shapes, use inline types: `as { valid: boolean; errors: string[] }`
    6. **Type local parseFirstJson helper** (if present):
       ```typescript
       function parseFirstJson(text: string): Record<string, unknown> {
       ```
    7. **Type fixture creation helpers:**
       - `createTempDir(): string`
       - `copyFixtures(dest: string): void`
    8. **Delete the original .test.js file**

    **Files in this task (3):**
    - cli.test.js -> .ts (2,506 lines -- main CLI integration tests, heaviest subprocess use)
    - e2e-workflow.test.js -> .ts (340 lines -- v0.1.1 feature chain E2E)
    - evolve-e2e.test.js -> .ts (456 lines -- evolve loop E2E)

    **IMPORTANT for cli.test.ts:**
    - The `GRD_TOOLS` path constant points to `bin/grd-tools.js` -- keep as .js since that's the CJS proxy entry point
    - env object in execFileSync options: `{ ...process.env, NODE_NO_WARNINGS: '1' }` is type-safe
    - Some tests check `err.status` from execFileSync -- use the typed catch pattern above
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/integration/cli.test.ts --no-coverage
    npx jest tests/integration/e2e-workflow.test.ts --no-coverage
    npx jest tests/integration/evolve-e2e.test.ts --no-coverage
    ```
    All must pass (Level 2: Proxy)
  </verify>
  <done>
    3 integration test files migrated from .test.js to .test.ts.
    Subprocess helpers typed with CLIResult interface.
    Original .js files deleted. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate golden, npm-pack, and worktree-parallel E2E test files to TypeScript</name>
  <files>
    tests/integration/golden.test.ts, tests/integration/npm-pack.test.ts,
    tests/integration/worktree-parallel-e2e.test.ts
  </files>
  <action>
    Apply the same migration pattern to these 3 files:

    1. **Rename** .test.js to .test.ts
    2. **Update file header comment**
    3. **Keep all require() calls as-is**
    4. **Add type annotations** to helpers, subprocess calls, catch blocks
    5. **Delete the original .test.js file**

    **Files in this task (3):**
    - golden.test.js -> .ts (351 lines -- golden snapshot comparisons)
    - npm-pack.test.js -> .ts (269 lines -- npm distribution pipeline)
    - worktree-parallel-e2e.test.js -> .ts (1,275 lines -- cross-module integration)

    **Specific patterns for these files:**
    - golden.test.ts: Has its own `runCLI` helper -- type identically to cli.test.ts pattern. Golden file paths from `fs.readdirSync` need `Dirent` or `string` typing depending on options used.
    - npm-pack.test.ts: Uses `execSync` and `execFileSync` for npm commands. Type the pack result parsing. Note: 2 tests may fail on Node v24 (pre-existing, DEFER-59-01) -- this is expected and not a regression.
    - worktree-parallel-e2e.test.ts: Creates temporary git repos with complex fixture setup. Type the `createTestGitRepo` and similar factory functions. Heavy use of `execSync` for git commands -- type options objects.

    **Key typing patterns:**
    ```typescript
    // For execSync git commands
    const output = execSync('git status', { cwd: tmpDir, encoding: 'utf-8' }) as string;

    // For fs.readdirSync with string results
    const files = fs.readdirSync(dir) as string[];

    // For complex fixture factories
    function createTestGitRepo(options?: { withWorktree?: boolean }): string {
    ```
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/integration/golden.test.ts --no-coverage
    npx jest tests/integration/npm-pack.test.ts --no-coverage
    npx jest tests/integration/worktree-parallel-e2e.test.ts --no-coverage
    ```
    golden and worktree-parallel must pass. npm-pack may have 2 known failures (Node v24, DEFER-59-01) -- this is pre-existing, not a regression (Level 2: Proxy)
  </verify>
  <done>
    3 integration/E2E test files migrated from .test.js to .test.ts.
    Original .js files deleted. All tests pass (npm-pack known failures excluded).
  </done>
</task>

<task type="auto">
  <name>Task 3: Final full-suite verification and cleanup</name>
  <files>jest.config.js</files>
  <action>
    After Tasks 1-2 complete, run final verification across the entire test suite:

    1. **Verify no .test.js files remain anywhere:**
       ```
       find tests/ -name '*.test.js' -type f
       ```
       Should return empty -- all test files are now .ts

    2. **Verify no .js helper files remain:**
       ```
       ls tests/helpers/*.js
       ```
       Should return empty -- setup.js and fixtures.js deleted in Plan 01

    3. **Run full test suite with coverage:**
       ```
       npx jest --coverage
       ```
       Confirm:
       - All tests pass (or only known pre-existing failures)
       - Total test count matches or exceeds 2,184 (success criteria baseline)
       - All per-file coverage thresholds met

    4. **Run tsc --noEmit** for complete type-checking

    5. **Run lint** to ensure no lint regressions:
       ```
       npm run lint
       ```

    6. If jest.config.js testMatch needs updating to remove the .test.js pattern (since all tests are now .ts), consider whether to keep it for backward compatibility or clean it up. Recommendation: keep both patterns in testMatch for now -- the .js pattern is harmless and provides a safety net.
  </action>
  <verify>
    ```
    find tests/ -name '*.test.js' -type f | wc -l  # Should be 0
    npx jest --coverage 2>&1 | tail -40
    npx tsc --noEmit
    npm run lint
    ```
    Zero .test.js files, all tests pass, tsc clean, lint clean (Level 2: Proxy)
  </verify>
  <done>
    Complete test suite migration verified:
    - Zero .test.js files remain in tests/
    - Zero .js helper files remain in tests/helpers/
    - All tests pass with coverage thresholds met
    - tsc type-checks all test files successfully
    - Lint passes
    Phase 64 is complete.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- All 6 .test.ts files compile under ts-jest without syntax errors
- No .test.js files remain in tests/integration/ directory

Level 2 (Proxy):
- Each individual integration test file passes when run in isolation
- Full test suite passes with all per-file coverage thresholds met
- tsc --noEmit passes for all test files
- No .test.js or helper .js files remain anywhere in tests/
</verification>

<success_criteria>
- 6 integration/E2E test files migrated from .test.js to .test.ts
- Subprocess spawn helpers (runCLI) fully typed with CLIResult interface
- execFileSync error catch blocks properly typed
- Golden test infrastructure works with .ts test file
- Combined with Plans 02-03: entire test suite is TypeScript
- All tests pass, all coverage thresholds met, zero regressions
- Total test count >= 2,184 (or current baseline)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-04-SUMMARY.md`
</output>
