---
phase: 64-test-suite-migration
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/unit/paths.test.ts
  - tests/unit/backend.test.ts
  - tests/unit/backend-real-env.test.ts
  - tests/unit/utils.test.ts
  - tests/unit/frontmatter.test.ts
  - tests/unit/gates.test.ts
  - tests/unit/deps.test.ts
  - tests/unit/scaffold.test.ts
  - tests/unit/verify.test.ts
  - tests/unit/state.test.ts
  - tests/unit/roadmap.test.ts
  - tests/unit/roadmap-roundtrip.test.ts
  - tests/unit/long-term-roadmap.test.ts
  - tests/unit/markdown-split.test.ts
  - tests/unit/postinstall.test.ts
  - tests/unit/validation.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "All 16 unit test files renamed from .test.js to .test.ts and compile under ts-jest"
    - "require() paths to lib/ modules resolve correctly (through CJS proxies or direct .ts)"
    - "require() paths to helpers/setup.ts and helpers/fixtures.ts resolve correctly"
    - "Type annotations added to mock factories, local helper functions, and test fixture variables"
    - "All tests in these 16 files pass with zero regressions"
    - "Per-file coverage thresholds in jest.config.js still met for corresponding lib/ source files"
  artifacts:
    - path: "tests/unit/paths.test.ts"
      provides: "Typed unit tests for lib/paths.ts"
    - path: "tests/unit/state.test.ts"
      provides: "Typed unit tests for lib/state.ts"
    - path: "tests/unit/utils.test.ts"
      provides: "Typed unit tests for lib/utils.ts"
  key_links:
    - from: "tests/unit/*.test.ts"
      to: "tests/helpers/setup.ts"
      via: "require('../helpers/setup') for captureOutput/captureError"
    - from: "tests/unit/*.test.ts"
      to: "lib/*.ts (via CJS proxies)"
      via: "require('../../lib/state') etc."
---

<objective>
Migrate 16 unit test files (foundation and data layer modules) from .test.js to .test.ts with type annotations on mocks, helpers, and test fixtures.

Purpose: Convert the first batch of unit tests to TypeScript. These test files cover the foundation layer (paths, backend, utils), data layer (frontmatter, state, roadmap), and smaller specialized modules (gates, deps, scaffold, verify).

Output: 16 .test.ts files replacing their .test.js counterparts.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/helpers/setup.ts
@tests/helpers/fixtures.ts
@jest.config.js
@.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate foundation and utility test files to TypeScript</name>
  <files>
    tests/unit/paths.test.ts, tests/unit/backend.test.ts, tests/unit/backend-real-env.test.ts,
    tests/unit/utils.test.ts, tests/unit/frontmatter.test.ts, tests/unit/gates.test.ts,
    tests/unit/deps.test.ts, tests/unit/scaffold.test.ts
  </files>
  <action>
    For each of the 8 files, apply this mechanical migration pattern:

    1. **Rename** the file from .test.js to .test.ts
    2. **Update file header comment** to reference the .ts source (e.g., "Unit tests for lib/paths.ts")
    3. **Keep all require() calls as-is** -- they resolve through CJS proxies (e.g., `require('../../lib/state')` resolves to state.js which re-exports state.ts). Do NOT change to import syntax.
    4. **Add type annotations to local helper functions** defined within the test file:
       - `parseFirstJson(str: string): Record<string, unknown>` (if present)
       - Factory functions like `createTestState(overrides?: Record<string, unknown>): string`
       - Any other per-test helpers
    5. **Type mock implementations:**
       - `jest.spyOn(fs, 'readFileSync').mockReturnValue(content as string)`
       - `jest.spyOn(fs, 'writeFileSync').mockImplementation((_path: string, _content: string) => {})`
       - Use `as unknown as jest.SpyInstance` for complex mock chains if needed
    6. **Type catch blocks:** `catch (e: unknown)` with `(e as Error).message`
    7. **Type variables that TS cannot infer**, especially:
       - Variables assigned from `JSON.parse()`: `const result = JSON.parse(stdout) as Record<string, unknown>`
       - Variables assigned from `captureOutput()`: already typed via setup.ts
    8. **Delete the original .test.js file** after confirming the .ts version compiles

    **Files in this task (8):**
    - paths.test.js -> .ts (478 lines -- tests lib/paths.ts)
    - backend.test.js -> .ts (901 lines -- tests lib/backend.ts)
    - backend-real-env.test.js -> .ts (401 lines -- tests real env var manipulation)
    - utils.test.js -> .ts (1,334 lines -- tests lib/utils.ts)
    - frontmatter.test.js -> .ts (661 lines -- tests lib/frontmatter.ts)
    - gates.test.js -> .ts (492 lines -- tests lib/gates.ts)
    - deps.test.js -> .ts (582 lines -- tests lib/deps.ts)
    - scaffold.test.js -> .ts (458 lines -- tests lib/scaffold.ts)

    **Common patterns to watch for:**
    - `err.__EXIT__` checks in catch blocks: use `(err as ExitSentinelError).__EXIT__` (import type from helpers/setup.ts if ExitSentinelError is exported, or use inline `as { __EXIT__?: boolean }`)
    - `JSON.parse(stdout)` results: cast to the expected shape
    - `process.argv` manipulation: type-safe, no changes needed
    - `jest.mock()` calls: add type parameter if mocking a typed module
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/unit/paths.test.ts --no-coverage
    npx jest tests/unit/backend.test.ts --no-coverage
    npx jest tests/unit/utils.test.ts --no-coverage
    npx jest tests/unit/frontmatter.test.ts --no-coverage
    npx jest tests/unit/gates.test.ts --no-coverage
    npx jest tests/unit/deps.test.ts --no-coverage
    npx jest tests/unit/scaffold.test.ts --no-coverage
    npx jest tests/unit/backend-real-env.test.ts --no-coverage
    ```
    All must pass (Level 2: Proxy)
  </verify>
  <done>
    8 unit test files migrated from .test.js to .test.ts with type annotations.
    Original .js files deleted. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate data and domain layer test files to TypeScript</name>
  <files>
    tests/unit/state.test.ts, tests/unit/roadmap.test.ts, tests/unit/roadmap-roundtrip.test.ts,
    tests/unit/long-term-roadmap.test.ts, tests/unit/markdown-split.test.ts,
    tests/unit/verify.test.ts, tests/unit/postinstall.test.ts, tests/unit/validation.test.ts
  </files>
  <action>
    Apply the same mechanical migration pattern from Task 1 to these 8 files:

    1. **Rename** .test.js to .test.ts
    2. **Update file header comment** to reference .ts source
    3. **Keep all require() calls as-is** (CJS proxy resolution)
    4. **Add type annotations** to local helpers, mock implementations, catch blocks, and JSON.parse results
    5. **Delete the original .test.js file**

    **Files in this task (8):**
    - state.test.js -> .ts (1,211 lines -- tests lib/state.ts)
    - roadmap.test.js -> .ts (738 lines -- tests lib/roadmap.ts)
    - roadmap-roundtrip.test.js -> .ts (449 lines -- tests ROADMAP.md round-trip)
    - long-term-roadmap.test.js -> .ts (744 lines -- tests lib/long-term-roadmap.ts)
    - markdown-split.test.js -> .ts (534 lines -- tests lib/markdown-split.ts)
    - verify.test.js -> .ts (478 lines -- tests lib/verify.ts)
    - postinstall.test.js -> .ts (233 lines -- tests bin/postinstall.js)
    - validation.test.js -> .ts (303 lines -- tests CLI arg validation)

    **Specific patterns for these files:**
    - state.test.js: Heavy use of captureOutput/captureError -- types flow from setup.ts
    - roadmap.test.js: Uses createFixtureDir/cleanupFixtureDir -- types flow from fixtures.ts
    - roadmap-roundtrip.test.js: fs.readFileSync mock return values need string cast
    - long-term-roadmap.test.js: parseLongTermRoadmap accepts unknown -- match the source type
    - markdown-split.test.js: SplitResult discriminated union -- use imported types if available
    - postinstall.test.js: Tests bin/ file -- require path `../../bin/postinstall.js` stays .js (bin proxies)
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/unit/state.test.ts --no-coverage
    npx jest tests/unit/roadmap.test.ts --no-coverage
    npx jest tests/unit/roadmap-roundtrip.test.ts --no-coverage
    npx jest tests/unit/long-term-roadmap.test.ts --no-coverage
    npx jest tests/unit/markdown-split.test.ts --no-coverage
    npx jest tests/unit/verify.test.ts --no-coverage
    npx jest tests/unit/postinstall.test.ts --no-coverage
    npx jest tests/unit/validation.test.ts --no-coverage
    ```
    All must pass (Level 2: Proxy)
  </verify>
  <done>
    8 unit test files migrated from .test.js to .test.ts with type annotations.
    Original .js files deleted. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite and coverage thresholds</name>
  <files>jest.config.js</files>
  <action>
    After Tasks 1-2 complete, run the full test suite with coverage to verify:

    1. Run `npx jest --coverage` and confirm:
       - All tests pass (zero failures)
       - Total test count matches or exceeds baseline (2,184+ per success criteria, or current count)
       - All per-file coverage thresholds in jest.config.js are met
    2. If any coverage threshold is not met, investigate:
       - The test file migration may have accidentally removed test cases
       - A require() path may not resolve, silently skipping tests
    3. Run `npx tsc --noEmit` to confirm type-checking passes for all migrated test files
    4. If any type errors in migrated files, fix them (add type assertions, adjust annotations)

    **Do NOT modify per-file coverage thresholds** -- if a threshold fails, the test file migration has a bug.
  </action>
  <verify>
    ```
    npx jest --coverage 2>&1 | tail -30
    npx tsc --noEmit
    ```
    All tests pass, all coverage thresholds met, tsc clean (Level 2: Proxy)
  </verify>
  <done>
    16 unit test files successfully migrated with full coverage thresholds met.
    No regressions in test count or coverage.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- All 16 .test.ts files compile under ts-jest without syntax errors
- No .test.js files remain for migrated tests

Level 2 (Proxy):
- Each individual test file passes when run in isolation
- Full test suite passes with coverage thresholds met
- tsc --noEmit passes for test files
</verification>

<success_criteria>
- 16 unit test files migrated from .test.js to .test.ts
- Type annotations on mock factories, local helpers, catch blocks, and JSON.parse results
- All tests pass individually and as a full suite
- Per-file coverage thresholds in jest.config.js all met
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-02-SUMMARY.md`
</output>
