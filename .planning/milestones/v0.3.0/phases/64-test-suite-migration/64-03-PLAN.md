---
phase: 64-test-suite-migration
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/unit/commands.test.ts
  - tests/unit/context.test.ts
  - tests/unit/context-backend-compat.test.ts
  - tests/unit/phase.test.ts
  - tests/unit/cleanup.test.ts
  - tests/unit/cleanup-noninterference.test.ts
  - tests/unit/parallel.test.ts
  - tests/unit/tracker.test.ts
  - tests/unit/worktree.test.ts
  - tests/unit/mcp-server.test.ts
  - tests/unit/autopilot.test.ts
  - tests/unit/evolve.test.ts
  - tests/unit/coverage-gaps.test.ts
  - tests/unit/setup.test.ts
  - tests/unit/agent-audit.test.ts
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "All 15 unit test files renamed from .test.js to .test.ts and compile under ts-jest"
    - "require() paths to lib/ modules (including decomposed commands/, context/, evolve/) resolve correctly"
    - "Type annotations added to mock factories, local helper functions, and complex test fixtures"
    - "All tests in these 15 files pass with zero regressions"
    - "Per-file coverage thresholds in jest.config.js still met for corresponding lib/ source files"
  artifacts:
    - path: "tests/unit/commands.test.ts"
      provides: "Typed unit tests for lib/commands/ (4,088 lines)"
    - path: "tests/unit/context.test.ts"
      provides: "Typed unit tests for lib/context/ (3,031 lines)"
    - path: "tests/unit/evolve.test.ts"
      provides: "Typed unit tests for lib/evolve/ (2,672 lines)"
  key_links:
    - from: "tests/unit/commands.test.ts"
      to: "lib/commands/index.ts"
      via: "require('../../lib/commands') -> CJS proxy -> commands/index.ts"
    - from: "tests/unit/context.test.ts"
      to: "lib/context/index.ts"
      via: "require('../../lib/context') -> CJS proxy -> context/index.ts"
    - from: "tests/unit/evolve.test.ts"
      to: "lib/evolve/index.ts"
      via: "require('../../lib/evolve') -> CJS proxy -> evolve/index.ts"
---

<objective>
Migrate 15 unit test files (command layer, integration layer, and remaining specialized tests) from .test.js to .test.ts with type annotations.

Purpose: Convert the second batch of unit tests covering the larger, more complex modules (commands, context, evolve, autopilot, worktree) and remaining specialized tests (cleanup, parallel, tracker, mcp-server, coverage-gaps, setup, agent-audit).

Output: 15 .test.ts files replacing their .test.js counterparts.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/helpers/setup.ts
@tests/helpers/fixtures.ts
@jest.config.js
@.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate large command and context layer test files to TypeScript</name>
  <files>
    tests/unit/commands.test.ts, tests/unit/context.test.ts,
    tests/unit/context-backend-compat.test.ts, tests/unit/phase.test.ts,
    tests/unit/cleanup.test.ts, tests/unit/cleanup-noninterference.test.ts,
    tests/unit/parallel.test.ts, tests/unit/mcp-server.test.ts
  </files>
  <action>
    For each of the 8 files, apply the mechanical migration pattern:

    1. **Rename** .test.js to .test.ts
    2. **Update file header comment** to reference .ts source modules
    3. **Keep all require() calls as-is** -- CJS proxy resolution handles it:
       - `require('../../lib/commands')` -> commands.js proxy -> commands/index.ts
       - `require('../../lib/context')` -> context.js proxy -> context/index.ts
       - `require('../../lib/mcp-server')` -> mcp-server.js (still JS, last unmigrated lib module)
    4. **Add type annotations** to local helpers, mock implementations, catch blocks, JSON.parse results
    5. **Delete the original .test.js file**

    **Files in this task (8):**
    - commands.test.js -> .ts (4,088 lines -- largest test file, tests 17+ CLI commands)
    - context.test.js -> .ts (3,031 lines -- tests 30+ cmdInit* functions)
    - context-backend-compat.test.js -> .ts (411 lines -- cmdInit* under all 4 backends)
    - phase.test.js -> .ts (1,672 lines -- phase lifecycle: add/insert/remove/complete)
    - cleanup.test.js -> .ts (1,739 lines -- quality analysis module)
    - cleanup-noninterference.test.js -> .ts (659 lines -- cleanup system non-interference)
    - parallel.test.js -> .ts (1,084 lines -- parallel execution engine)
    - mcp-server.test.js -> .ts (2,004 lines -- MCP tool schema and handleMessage)

    **Specific patterns for these files:**
    - commands.test.js: Very heavy use of captureOutput/captureError. Many `parseFirstJson` calls -- type the helper as `(str: string) => Record<string, unknown>`. Many `process.argv` manipulations are type-safe already.
    - context.test.js: Tests cmdInit* functions that return JSON blobs -- type parsed results as `Record<string, unknown>`.
    - mcp-server.test.js: Tests the JSON-RPC protocol. Mock implementations for stdio -- type as `jest.SpyInstance`. Note mcp-server.js is still .js (not .ts), so require path stays as-is.
    - phase.test.js: Creates complex test fixtures (ROADMAP.md content strings) -- type as `string`.
    - cleanup.test.js: Mock fs operations extensively -- type mock implementations explicitly.
    - parallel.test.js: Tests dependency graph construction -- type node/edge objects.
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/unit/commands.test.ts --no-coverage
    npx jest tests/unit/context.test.ts --no-coverage
    npx jest tests/unit/context-backend-compat.test.ts --no-coverage
    npx jest tests/unit/phase.test.ts --no-coverage
    npx jest tests/unit/cleanup.test.ts --no-coverage
    npx jest tests/unit/cleanup-noninterference.test.ts --no-coverage
    npx jest tests/unit/parallel.test.ts --no-coverage
    npx jest tests/unit/mcp-server.test.ts --no-coverage
    ```
    All must pass (Level 2: Proxy)
  </verify>
  <done>
    8 unit test files migrated from .test.js to .test.ts with type annotations.
    Original .js files deleted. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate autonomous layer and remaining specialized test files to TypeScript</name>
  <files>
    tests/unit/tracker.test.ts, tests/unit/worktree.test.ts,
    tests/unit/autopilot.test.ts, tests/unit/evolve.test.ts,
    tests/unit/coverage-gaps.test.ts, tests/unit/setup.test.ts,
    tests/unit/agent-audit.test.ts
  </files>
  <action>
    Apply the same mechanical migration pattern to these 7 files:

    1. **Rename** .test.js to .test.ts
    2. **Update file header comment** to reference .ts source modules
    3. **Keep all require() calls as-is** (CJS proxy resolution)
    4. **Add type annotations** to local helpers, mock factories, catch blocks, JSON.parse results
    5. **Delete the original .test.js file**

    **Files in this task (7):**
    - tracker.test.js -> .ts (1,488 lines -- tracker config, mapping, GitHub/Atlassian)
    - worktree.test.js -> .ts (2,256 lines -- git worktree lifecycle)
    - autopilot.test.js -> .ts (1,692 lines -- autopilot orchestration)
    - evolve.test.js -> .ts (2,672 lines -- evolve iteration state layer)
    - coverage-gaps.test.js -> .ts (1,201 lines -- targeted coverage gaps)
    - setup.test.js -> .ts (165 lines -- cmdSetup JSON/raw output)
    - agent-audit.test.js -> .ts (161 lines -- agent frontmatter audit)

    **Specific patterns for these files:**
    - worktree.test.js: Creates temporary git repos with execSync -- type command strings and options
    - autopilot.test.js: Mock implementations for spawn/exec -- type as jest.SpyInstance
    - evolve.test.js: Complex state fixtures (WorkItem, EvolveState shapes) -- type as `Record<string, unknown>` or inline shapes
    - tracker.test.js: Mock HTTP/MCP responses -- type response objects
    - coverage-gaps.test.js: Tests edge cases across multiple modules -- various require paths
    - setup.test.js and agent-audit.test.js: Small files, minimal typing needed
  </action>
  <verify>
    Run each migrated file individually:
    ```
    npx jest tests/unit/tracker.test.ts --no-coverage
    npx jest tests/unit/worktree.test.ts --no-coverage
    npx jest tests/unit/autopilot.test.ts --no-coverage
    npx jest tests/unit/evolve.test.ts --no-coverage
    npx jest tests/unit/coverage-gaps.test.ts --no-coverage
    npx jest tests/unit/setup.test.ts --no-coverage
    npx jest tests/unit/agent-audit.test.ts --no-coverage
    ```
    All must pass (Level 2: Proxy)
  </verify>
  <done>
    7 unit test files migrated from .test.js to .test.ts with type annotations.
    Original .js files deleted. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite, coverage thresholds, and clean up sample.test.ts</name>
  <files>tests/unit/sample.test.ts, jest.config.js</files>
  <action>
    After Tasks 1-2 complete, verify the full suite and handle the sample test:

    1. **Verify sample.test.ts:** This file was already .ts from Phase 58-03. Confirm it still passes and has no changes needed.

    2. **Run full test suite with coverage:**
       ```
       npx jest --coverage
       ```
       Confirm:
       - All tests pass (zero failures)
       - Total test count matches or exceeds baseline
       - All per-file coverage thresholds met

    3. **Verify no .test.js files remain in tests/unit/:**
       ```
       ls tests/unit/*.test.js
       ```
       Should return empty (all migrated to .ts)

    4. **Run tsc --noEmit** to confirm type-checking passes for all migrated files

    5. If any issues, fix them. Do NOT modify coverage thresholds.
  </action>
  <verify>
    ```
    npx jest --coverage 2>&1 | tail -30
    ls tests/unit/*.test.js 2>&1
    npx tsc --noEmit
    ```
    All tests pass, no .test.js files remain, tsc clean (Level 2: Proxy)
  </verify>
  <done>
    All 31+1 unit test files are now .test.ts. Full test suite passes with coverage.
    No .test.js files remain in tests/unit/. tsc type-checks all test files.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- All 15 .test.ts files compile under ts-jest without syntax errors
- No .test.js files remain in tests/unit/ directory

Level 2 (Proxy):
- Each individual test file passes when run in isolation
- Full test suite passes with all per-file coverage thresholds met
- tsc --noEmit passes for all test files
</verification>

<success_criteria>
- 15 unit test files migrated from .test.js to .test.ts
- Type annotations on mock factories, local helpers, catch blocks, and JSON.parse results
- Combined with Plan 02, all 31 unit test .js files are now .ts
- sample.test.ts (already .ts) confirmed working
- All tests pass individually and as full suite
- Per-file coverage thresholds all met
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-03-SUMMARY.md`
</output>
