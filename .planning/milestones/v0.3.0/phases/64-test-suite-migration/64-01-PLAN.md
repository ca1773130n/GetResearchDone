---
phase: 64-test-suite-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/setup.ts
  - tests/helpers/fixtures.ts
  - jest.config.js
  - tsconfig.json
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "tests/helpers/setup.ts exports captureOutput, captureError, captureOutputAsync, EXIT_SENTINEL with full type signatures"
    - "tests/helpers/fixtures.ts exports createFixtureDir, cleanupFixtureDir, FIXTURE_SOURCE with typed returns"
    - "tsconfig.json includes tests/**/*.ts in its include array so tsc type-checks test files"
    - "jest.config.js testMatch includes *.test.ts pattern (already present -- verify preserved)"
    - "All existing tests still pass after infrastructure changes (zero regressions)"
  artifacts:
    - path: "tests/helpers/setup.ts"
      provides: "Typed test capture utilities with ExitSentinelError interface"
    - path: "tests/helpers/fixtures.ts"
      provides: "Typed fixture directory utilities"
  key_links:
    - from: "tests/helpers/setup.ts"
      to: "jest"
      via: "jest.spyOn mocks on process.exit, process.stdout.write, process.stderr.write"
    - from: "tests/helpers/fixtures.ts"
      to: "tests/fixtures/planning/"
      via: "FIXTURE_SOURCE path constant"
---

<objective>
Migrate test infrastructure to TypeScript: convert helper modules, update tsconfig to include test files for type-checking, and verify jest.config.js still supports the dual .js/.ts test pipeline.

Purpose: Establish typed test infrastructure that all subsequent test file migrations depend on. Helpers define shared types (ExitSentinelError, CaptureResult) that migrated test files will import.

Output: tests/helpers/setup.ts, tests/helpers/fixtures.ts with typed exports; tsconfig.json updated to include tests.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/helpers/setup.js
@tests/helpers/fixtures.js
@jest.config.js
@tsconfig.json
@.planning/milestones/v0.3.0/phases/58-typescript-toolchain-build-pipeline/58-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test helpers to TypeScript with typed interfaces</name>
  <files>tests/helpers/setup.ts, tests/helpers/fixtures.ts, tests/helpers/setup.js, tests/helpers/fixtures.js</files>
  <action>
    **Migrate tests/helpers/setup.js to tests/helpers/setup.ts:**

    1. Create an `ExitSentinelError` interface extending `Error` with `__EXIT__: boolean` and `code: number` properties
    2. Define `CaptureResult` type: `{ stdout: string; exitCode: number }`
    3. Define `CaptureErrorResult` type: `{ stderr: string; exitCode: number }`
    4. Add type annotations to all function parameters and return types:
       - `captureOutput(fn: () => void): CaptureResult`
       - `captureError(fn: () => void): CaptureErrorResult`
       - `captureOutputAsync(fn: () => Promise<void>): Promise<CaptureResult>`
    5. Type the `process.exit` mock implementation parameter: `(code: number | undefined)`
    6. Use `const err = new Error(EXIT_SENTINEL) as ExitSentinelError` for the sentinel error construction
    7. Type catch variables: `(e: unknown)` with `(e as ExitSentinelError).__EXIT__` guard
    8. Keep `module.exports = { captureOutput, captureError, captureOutputAsync, EXIT_SENTINEL }` (CJS)
    9. Delete the original setup.js after setup.ts is verified working

    **Migrate tests/helpers/fixtures.js to tests/helpers/fixtures.ts:**

    1. Add `'use strict'` at top
    2. Type all function parameters and return types:
       - `createFixtureDir(): string`
       - `cleanupFixtureDir(dir: string): void`
    3. Keep `module.exports = { createFixtureDir, cleanupFixtureDir, FIXTURE_SOURCE }` (CJS)
    4. Delete the original fixtures.js after fixtures.ts is verified working

    **IMPORTANT:** Use `const = require()` pattern (not ESM import) consistent with Phase 63 decisions. The `moduleDetection: "force"` in tsconfig.json ensures TS treats these as modules without needing `export {}`.
  </action>
  <verify>
    Run existing test suite to verify helpers still work:
    ```
    npx jest tests/unit/sample.test.ts --no-coverage
    npx jest tests/unit/state.test.js --no-coverage
    ```
    Both must pass -- sample.test.ts proves ts-jest, state.test.js proves CJS helper require still resolves (Level 1: Sanity)
  </verify>
  <done>
    tests/helpers/setup.ts and tests/helpers/fixtures.ts exist with full type annotations.
    Original .js files deleted. Existing tests that require('../helpers/setup') still resolve
    because ts-jest handles .ts resolution and Node CJS resolves .ts with the right config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tsconfig.json to include test files and verify jest configuration</name>
  <files>tsconfig.json, jest.config.js</files>
  <action>
    **Update tsconfig.json:**

    1. Add `"tests/**/*.ts"` to the `include` array so that `tsc --noEmit` type-checks test files
    2. The current include is `["lib/**/*.ts", "bin/**/*.ts"]` -- extend to `["lib/**/*.ts", "bin/**/*.ts", "tests/**/*.ts"]`
    3. Do NOT add `"tests/**/*.js"` -- JS test files are not type-checked (checkJs is false)
    4. Ensure `@types/jest` provides global Jest types (describe, it, expect, jest) -- this was installed in Phase 58-03

    **Verify jest.config.js:**

    1. Confirm testMatch already includes `'**/tests/**/*.test.ts'` (set in Phase 58-03)
    2. Confirm transform has `'^.+\\.ts$': 'ts-jest'` (set in Phase 58-03)
    3. No modifications needed to jest.config.js if above are confirmed
    4. If for any reason they are missing, add them

    **Verify the full pipeline:**

    1. Run `npx tsc --noEmit` -- should pass with test files now included
    2. If tsc fails on test files due to untyped requires (e.g., `require('../../lib/state')` resolving to .js proxy), this is expected -- those test files have not been migrated yet. The tsconfig include is forward-looking for when they are migrated.
    3. Run `npx jest --no-coverage` to verify all tests still pass
  </action>
  <verify>
    ```
    npx tsc --noEmit 2>&1 | head -20
    npx jest tests/unit/sample.test.ts --no-coverage
    ```
    tsc may show warnings for not-yet-migrated .js test files being excluded (expected).
    sample.test.ts must pass (Level 1: Sanity)
  </verify>
  <done>
    tsconfig.json includes tests/**/*.ts for type-checking.
    jest.config.js confirmed to support .ts test files.
    Foundation ready for Plans 02-04 to migrate individual test files.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- tests/helpers/setup.ts compiles under ts-jest (no syntax errors)
- tests/helpers/fixtures.ts compiles under ts-jest
- Existing JS test files still pass (helpers resolve correctly)
- tsconfig.json includes tests/**/*.ts

Level 2 (Proxy):
- Run full test suite (`npx jest --no-coverage`) -- all tests pass
- No regressions from infrastructure changes
</verification>

<success_criteria>
- Test helpers migrated to TypeScript with full type annotations
- ExitSentinelError, CaptureResult, CaptureErrorResult types defined and exported
- tsconfig.json includes test files for type-checking
- All existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/64-test-suite-migration/64-01-SUMMARY.md`
</output>
