---
phase: 60-data-domain-layer-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/frontmatter.ts
  - lib/frontmatter.js
  - lib/markdown-split.ts
  - lib/markdown-split.js
  - lib/types.ts
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/frontmatter.ts compiles under strict:true with zero any types in exported functions"
    - "lib/markdown-split.ts compiles under strict:true with zero any types in exported functions"
    - "All cmd* functions in frontmatter.ts have explicit parameter types and return types"
    - "FrontmatterObject type from types.ts is used for extractFrontmatter return type (not just Record<string, unknown>)"
    - "All existing unit tests for frontmatter and markdown-split pass with coverage thresholds met"
    - "CommonJS proxy pattern applied: frontmatter.js and markdown-split.js re-export from .ts files"
  artifacts:
    - path: "lib/frontmatter.ts"
      provides: "Typed frontmatter parsing with YAML extract/reconstruct/splice and command functions"
      exports: ["extractFrontmatter", "reconstructFrontmatter", "spliceFrontmatter", "parseMustHavesBlock", "cmdFrontmatterGet", "cmdFrontmatterSet", "cmdFrontmatterMerge", "cmdFrontmatterValidate", "FRONTMATTER_SCHEMAS", "getPhaseRoadmapMetadata"]
    - path: "lib/markdown-split.ts"
      provides: "Typed markdown splitting with token estimation, split boundaries, and transparent reassembly"
      exports: ["INDEX_MARKER", "DEFAULT_TOKEN_THRESHOLD", "estimateTokens", "findSplitBoundaries", "splitMarkdown", "isIndexFile", "reassembleFromIndex", "readMarkdownWithPartials"]
  key_links:
    - from: "lib/frontmatter.ts"
      to: "lib/utils.ts"
      via: "import of safeReadFile, output, error"
      pattern: "require.*utils"
    - from: "lib/frontmatter.ts"
      to: "lib/types.ts"
      via: "import type FrontmatterObject"
      pattern: "import type.*FrontmatterObject"
    - from: "lib/markdown-split.ts"
      to: "lib/utils.ts"
      via: "import of safeReadFile"
      pattern: "require.*utils"
---

<objective>
Migrate lib/frontmatter.js (488 lines) and lib/markdown-split.js (234 lines) to TypeScript with full type annotations under strict:true.

Purpose: These are the data-parsing layer modules that provide YAML frontmatter CRUD and markdown splitting/reassembly. They are imported by verify.js, scaffold.js, phase.js, long-term-roadmap.js, commands.js, and context.js -- typing them unblocks Wave 2 and Wave 3 migrations.

Output: lib/frontmatter.ts, lib/markdown-split.ts with CommonJS proxy .js files, updated jest.config.js thresholds.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/types.ts
@lib/frontmatter.js
@lib/markdown-split.js
@lib/paths.js (proxy pattern reference)
@lib/utils.ts (foundation dependency -- import patterns)
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/frontmatter.js to lib/frontmatter.ts</name>
  <files>lib/frontmatter.ts, lib/frontmatter.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern (paths.ts, backend.ts, utils.ts):

    1. Copy lib/frontmatter.js to lib/frontmatter.ts
    2. Add type annotations to all functions:
       - extractFrontmatter(content: string): FrontmatterObject -- import type { FrontmatterObject } from './types'
       - reconstructFrontmatter(obj: FrontmatterObject): string
       - spliceFrontmatter(content: string, updates: Record<string, unknown>): string
       - parseMustHavesBlock(content: string): { truths: string[]; artifacts: Array<{ path: string; provides?: string; exports?: string[]; min_lines?: number; contains?: string }>; key_links: Array<{ from: string; to: string; via: string; pattern?: string }> } | null
       - cmdFrontmatterGet(cwd: string, filePath: string, key: string, raw: boolean): void
       - cmdFrontmatterSet(cwd: string, filePath: string, key: string, value: string, raw: boolean): void
       - cmdFrontmatterMerge(cwd: string, filePath: string, mergeData: string, raw: boolean): void
       - cmdFrontmatterValidate(cwd: string, filePath: string, schema: string, raw: boolean): void
       - getPhaseRoadmapMetadata(cwd: string, phase: string): Record<string, string>
    3. Type the FRONTMATTER_SCHEMAS constant as Record<string, string[]>
    4. Type internal helper functions (parseYamlValue, etc.) with explicit parameter and return types
    5. Use import type for FrontmatterObject from types.ts
    6. Add any new interfaces needed to lib/types.ts (e.g., MustHavesBlock for the parseMustHavesBlock return type, FrontmatterSchema for schema validation)
    7. Use Record<string, unknown> with explicit type casts for dynamic YAML parsing (per established pattern from Phase 59)
    8. Convert lib/frontmatter.js to the thin CommonJS proxy pattern:
       ```javascript
       'use strict';
       module.exports = require('./frontmatter.ts');
       ```
    9. Ensure the 'use strict' directive is at the top of frontmatter.ts
    10. Run `npx tsc --noEmit` to verify strict:true compilation

    Avoid:
    - Do NOT use `any` types -- use Record<string, unknown> with explicit casts per Phase 59 pattern
    - Do NOT delete frontmatter.js -- convert to proxy (DEFER-59-01)
    - Do NOT change any runtime logic -- this is a pure type annotation migration
  </action>
  <verify>
    npx tsc --noEmit (zero errors on frontmatter.ts)
    npx jest tests/unit/frontmatter.test.js --coverage (all tests pass, coverage thresholds met)
    grep -c 'any' lib/frontmatter.ts should return 0 or only show 'any' in comments/strings, not in type annotations
  </verify>
  <done>
    lib/frontmatter.ts compiles under strict:true with zero any types in exported function signatures,
    all 10 exports have explicit parameter and return types,
    FrontmatterObject from types.ts is used as the return type for extractFrontmatter,
    all frontmatter unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/markdown-split.js to lib/markdown-split.ts and update jest.config.js</name>
  <files>lib/markdown-split.ts, lib/markdown-split.js, jest.config.js</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/markdown-split.js to lib/markdown-split.ts
    2. Add type annotations to all functions:
       - estimateTokens(text: string): number
       - findSplitBoundaries(content: string, options?: { maxTokens?: number }): number[]
       - splitMarkdown(content: string, options?: SplitMarkdownOptions): SplitResult
       - isIndexFile(content: string): boolean
       - reassembleFromIndex(indexPath: string): string
       - readMarkdownWithPartials(filePath: string): string
    3. Define interfaces for structured return types:
       - SplitMarkdownOptions: { maxTokens?: number; baseName?: string }
       - SplitResult: { index: string; partials: Array<{ filename: string; content: string }> }
       Add these to lib/types.ts or define locally with export type
    4. Type constants: INDEX_MARKER as string, DEFAULT_TOKEN_THRESHOLD as number
    5. Convert lib/markdown-split.js to the thin CommonJS proxy pattern
    6. Update jest.config.js coverage thresholds:
       - Change './lib/frontmatter.js' to './lib/frontmatter.ts' (same thresholds)
       - Change './lib/markdown-split.js' to './lib/markdown-split.ts' (same thresholds)
    7. Run full test suite to verify no regressions

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic
    - Do NOT lower coverage thresholds
  </action>
  <verify>
    npx tsc --noEmit (zero errors)
    npx jest tests/unit/markdown-split.test.js --coverage (all tests pass, coverage met)
    npx jest tests/unit/frontmatter.test.js --coverage (still passes after jest.config.js change)
    npm test (full suite passes, 2674+ tests)
  </verify>
  <done>
    lib/markdown-split.ts compiles under strict:true with zero any types,
    all 8 exports have explicit types,
    jest.config.js thresholds updated for frontmatter.ts and markdown-split.ts,
    full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors on both new .ts files
Level 2 (Proxy): Unit tests pass for both modules with coverage thresholds met; full test suite shows no regressions (2674+ tests passing)
Level 3 (Deferred): CommonJS interop validated across all downstream consumers (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/frontmatter.ts and lib/markdown-split.ts compile under strict:true with zero any types in exported functions
- All cmd* functions have explicit parameter types and return types
- FrontmatterObject type from types.ts is used (not just string/Record)
- Both .js files converted to CommonJS proxy pattern
- jest.config.js thresholds updated for .ts files
- All unit tests pass with existing coverage thresholds met or exceeded
- Full test suite (2674+ tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-01-SUMMARY.md`
</output>
