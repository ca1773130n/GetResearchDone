---
phase: 60-data-domain-layer-migration
plan: 04
type: execute
wave: 2
depends_on: ["60-01", "60-02"]
files_modified:
  - lib/deps.ts
  - lib/deps.js
  - lib/verify.ts
  - lib/verify.js
  - lib/scaffold.ts
  - lib/scaffold.js
  - lib/types.ts
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/deps.ts compiles under strict:true with zero any types in exported functions"
    - "lib/verify.ts compiles under strict:true with zero any types in exported functions"
    - "lib/scaffold.ts compiles under strict:true with zero any types in exported functions"
    - "buildDependencyGraph returns typed DependencyGraph with nodes and edges arrays"
    - "computeParallelGroups returns typed string[][] from Kahn's algorithm"
    - "All verify cmd* functions have explicit parameter types and return types"
    - "All existing unit tests for deps, verify, and scaffold pass with coverage thresholds met"
    - "CommonJS proxy pattern applied to all three .js files"
  artifacts:
    - path: "lib/deps.ts"
      provides: "Typed dependency graph computation with Kahn's algorithm parallel groups"
      exports: ["parseDependsOn", "buildDependencyGraph", "computeParallelGroups", "detectCycle", "cmdPhaseAnalyzeDeps"]
    - path: "lib/verify.ts"
      provides: "Typed artifact verification suite with 7 command functions"
      exports: ["cmdVerifySummary", "cmdVerifyPlanStructure", "cmdVerifyPhaseCompleteness", "cmdVerifyReferences", "cmdVerifyCommits", "cmdVerifyArtifacts", "cmdVerifyKeyLinks"]
    - path: "lib/scaffold.ts"
      provides: "Typed template selection and scaffolding with 3 command functions"
      exports: ["cmdTemplateSelect", "cmdTemplateFill", "cmdScaffold"]
  key_links:
    - from: "lib/deps.ts"
      to: "lib/roadmap.ts"
      via: "import of analyzeRoadmap"
      pattern: "require.*roadmap"
    - from: "lib/verify.ts"
      to: "lib/frontmatter.ts"
      via: "import of extractFrontmatter, parseMustHavesBlock"
      pattern: "require.*frontmatter"
    - from: "lib/scaffold.ts"
      to: "lib/frontmatter.ts"
      via: "import of reconstructFrontmatter"
      pattern: "require.*frontmatter"
    - from: "lib/scaffold.ts"
      to: "lib/paths.ts"
      via: "import of planningDir, phasesDir, researchDir"
      pattern: "require.*paths"
---

<objective>
Migrate lib/deps.js (254 lines), lib/verify.js (595 lines), and lib/scaffold.js (377 lines) to TypeScript with full type annotations under strict:true.

Purpose: These are domain utility modules that depend on the data layer migrated in Wave 1. deps.ts provides dependency graph computation for parallel execution. verify.ts provides artifact verification for plans, summaries, and phase completeness. scaffold.ts provides template selection and file scaffolding for planning workflows.

Output: lib/deps.ts, lib/verify.ts, lib/scaffold.ts with CommonJS proxy .js files, updated jest.config.js thresholds.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/types.ts
@lib/deps.js
@lib/verify.js
@lib/scaffold.js
@lib/frontmatter.ts (Wave 1 dependency -- Plan 01)
@lib/roadmap.ts (Wave 1 dependency -- Plan 02)
@lib/utils.ts (foundation dependency)
@lib/paths.ts (foundation dependency)
@.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-01-SUMMARY.md
@.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/deps.js to lib/deps.ts</name>
  <files>lib/deps.ts, lib/deps.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/deps.js to lib/deps.ts
    2. Define interfaces for the dependency graph domain:
       - DependencyNode: { id: string; name: string }
       - DependencyEdge: { from: string; to: string }
       - DependencyGraph: { nodes: DependencyNode[]; edges: DependencyEdge[] }
       Add to types.ts since DependencyGraph is used by parallel.js and autopilot.js
    3. Type all exported functions:
       - parseDependsOn(str: string): string[]
       - buildDependencyGraph(phases: Array<{ number: string; name: string; depends_on?: string }>): DependencyGraph
       - computeParallelGroups(graph: DependencyGraph): string[][]
       - detectCycle(graph: DependencyGraph): string[]
       - cmdPhaseAnalyzeDeps(cwd: string, phase: string, raw: boolean): void
    4. The phases parameter for buildDependencyGraph comes from analyzeRoadmap -- use the RoadmapPhase type or a compatible subset
    5. Convert lib/deps.js to thin CommonJS proxy pattern
    6. Run `npx tsc --noEmit` to verify

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic (Kahn's algorithm implementation stays identical)
  </action>
  <verify>
    npx tsc --noEmit (zero errors on deps.ts)
    npx jest tests/unit/deps.test.js --coverage (all tests pass, coverage thresholds met)
  </verify>
  <done>
    lib/deps.ts compiles under strict:true with zero any types,
    DependencyGraph interface defined and used,
    computeParallelGroups returns typed string[][],
    all deps unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/verify.js to lib/verify.ts</name>
  <files>lib/verify.ts, lib/verify.js</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/verify.js to lib/verify.ts
    2. Define interfaces for verification results:
       - VerifySummaryResult: { valid: boolean; errors: string[]; warnings: string[]; file_count?: number; commits_checked?: number }
       - VerifyPlanResult: { valid: boolean; errors: string[]; warnings: string[]; task_count: number; tasks: Array<{ name: string; type: string }> }
       - VerifyCompletenessResult: { complete: boolean; plans: string[]; missing_summaries: string[] }
       - VerifyReferencesResult: { valid: boolean; errors: string[]; checked: number }
       - VerifyArtifactsResult: { valid: boolean; errors: string[]; artifacts_checked: number }
       - VerifyKeyLinksResult: { valid: boolean; errors: string[]; links_checked: number }
       Keep local if only used in verify.ts
    3. Type all exported functions:
       - cmdVerifySummary(cwd: string, summaryPath: string, checkFileCount: boolean, raw: boolean): void
       - cmdVerifyPlanStructure(cwd: string, planPath: string, raw: boolean): void
       - cmdVerifyPhaseCompleteness(cwd: string, phase: string, raw: boolean): void
       - cmdVerifyReferences(cwd: string, filePath: string, raw: boolean): void
       - cmdVerifyCommits(cwd: string, hashes: string[], raw: boolean): void
       - cmdVerifyArtifacts(cwd: string, planPath: string, raw: boolean): void
       - cmdVerifyKeyLinks(cwd: string, planPath: string, raw: boolean): void
    4. verify.ts imports extractFrontmatter and parseMustHavesBlock from frontmatter -- these are now typed from Plan 01
    5. Use import type for FrontmatterObject from types.ts where frontmatter data is handled
    6. Convert lib/verify.js to thin CommonJS proxy pattern
    7. Run `npx tsc --noEmit` to verify

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic
  </action>
  <verify>
    npx tsc --noEmit (zero errors on verify.ts)
    npx jest tests/unit/verify.test.js --coverage (all tests pass, coverage thresholds met)
  </verify>
  <done>
    lib/verify.ts compiles under strict:true with zero any types,
    all 7 cmd* functions have explicit parameter and return types,
    uses typed frontmatter imports from frontmatter.ts,
    all verify unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate lib/scaffold.js to lib/scaffold.ts and update jest.config.js</name>
  <files>lib/scaffold.ts, lib/scaffold.js, jest.config.js</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/scaffold.js to lib/scaffold.ts
    2. Define interfaces for scaffold operations:
       - TemplateSelectResult: { template: string; reason: string; task_count: number; complexity: string }
       - ScaffoldOptions: { phase?: string; milestone?: string; type?: string; [key: string]: string | undefined }
       Keep local if only used in scaffold.ts
    3. Type all exported functions:
       - cmdTemplateSelect(cwd: string, planPath: string, raw: boolean): void
       - cmdTemplateFill(cwd: string, template: string, opts: Record<string, string>, raw: boolean): void
       - cmdScaffold(cwd: string, type: string, opts: Record<string, string>, raw: boolean): void
    4. scaffold.ts imports reconstructFrontmatter from frontmatter -- now typed from Plan 01
    5. scaffold.ts imports path functions from paths.ts -- already typed from Phase 59
    6. Convert lib/scaffold.js to thin CommonJS proxy pattern
    7. Update jest.config.js coverage thresholds:
       - Change './lib/deps.js' to './lib/deps.ts' (same thresholds)
       - Change './lib/verify.js' to './lib/verify.ts' (same thresholds)
       - Change './lib/scaffold.js' to './lib/scaffold.ts' (same thresholds)
    8. Run full test suite to verify no regressions

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic
    - Do NOT lower coverage thresholds
  </action>
  <verify>
    npx tsc --noEmit (zero errors)
    npx jest tests/unit/scaffold.test.js --coverage (all tests pass, coverage met)
    npm test (full suite passes, 2674+ tests)
  </verify>
  <done>
    lib/scaffold.ts compiles under strict:true with zero any types,
    all 3 cmd* functions have explicit parameter and return types,
    jest.config.js updated for deps.ts, verify.ts, scaffold.ts,
    full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors on all three new .ts files
Level 2 (Proxy): Unit tests pass for all three modules with coverage thresholds met; full test suite shows no regressions (2674+ tests passing)
Level 3 (Deferred): CommonJS interop validated across all downstream consumers (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/deps.ts, lib/verify.ts, lib/scaffold.ts compile under strict:true with zero any types in exported functions
- DependencyGraph interface defined in types.ts and used by deps.ts
- All cmd* functions have explicit parameter types and return types
- Typed imports from frontmatter.ts and roadmap.ts (Wave 1 outputs) work correctly
- All .js files converted to CommonJS proxy pattern
- jest.config.js thresholds updated for .ts files
- All unit tests pass with existing coverage thresholds met or exceeded
- Full test suite (2674+ tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-04-SUMMARY.md`
</output>
