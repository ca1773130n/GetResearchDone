---
phase: 60-data-domain-layer-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cleanup.ts
  - lib/cleanup.js
  - lib/gates.ts
  - lib/gates.js
  - lib/requirements.ts
  - lib/requirements.js
  - lib/types.ts
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/cleanup.ts compiles under strict:true with zero any types in exported functions"
    - "lib/gates.ts compiles under strict:true with zero any types in exported functions"
    - "lib/requirements.ts compiles under strict:true with zero any types in exported functions"
    - "All gate check functions have typed return signatures with {passed, errors, warnings} structure"
    - "GATE_REGISTRY has a typed structure with command-to-gate-function mapping"
    - "Quality analysis functions return typed violation/result structures (not untyped arrays)"
    - "parseRequirements returns typed Requirement[] with id, title, priority, category, description fields"
    - "All existing unit tests for cleanup, gates, and requirements pass with coverage thresholds met"
  artifacts:
    - path: "lib/cleanup.ts"
      provides: "Typed phase-boundary quality analysis with 17 exported functions"
      exports: ["getCleanupConfig", "analyzeComplexity", "analyzeDeadExports", "analyzeFileSize", "analyzeChangelogDrift", "analyzeReadmeLinks", "analyzeJsdocDrift", "analyzeTestCoverageGaps", "analyzeExportConsistency", "analyzeDocStaleness", "analyzeConfigSchemaDrift", "runQualityAnalysis", "generateCleanupPlan", "resetCleanupCache", "loadQualityHistory", "saveQualityMetrics", "computeTrends"]
    - path: "lib/gates.ts"
      provides: "Typed pre-flight validation gates with registry and runner"
      exports: ["checkOrphanedPhases", "checkPhaseInRoadmap", "checkPhaseHasPlans", "checkNoStaleArtifacts", "checkOldPhasesArchived", "checkMilestoneStateCoherence", "GATE_REGISTRY", "runPreflightGates", "resetGatesCache"]
    - path: "lib/requirements.ts"
      provides: "Typed REQUIREMENTS.md parsing with cached reading and status management"
      exports: ["_reqContentCache", "readCachedRequirements", "parseRequirements", "parseTraceabilityMatrix", "VALID_REQUIREMENT_STATUSES", "cmdRequirementGet", "cmdRequirementList", "cmdRequirementTraceability", "cmdRequirementUpdateStatus"]
  key_links:
    - from: "lib/cleanup.ts"
      to: "lib/paths.ts"
      via: "import of planningDir, phasesDir"
      pattern: "require.*paths"
    - from: "lib/gates.ts"
      to: "lib/utils.ts"
      via: "import of loadConfig, normalizePhaseName, safeReadFile, stripShippedSections, createRunCache"
      pattern: "require.*utils"
    - from: "lib/requirements.ts"
      to: "lib/utils.ts"
      via: "import of safeReadFile, output, error"
      pattern: "require.*utils"
---

<objective>
Migrate lib/cleanup.js (1412 lines), lib/gates.js (412 lines), and lib/requirements.js (415 lines) to TypeScript with full type annotations under strict:true.

Purpose: These are the validation and quality layer modules. cleanup.ts performs phase-boundary quality analysis (complexity, dead exports, file size, doc drift, test coverage gaps). gates.ts provides pre-flight validation checks run before workflow commands. requirements.ts parses and manages REQUIREMENTS.md. All three depend only on foundation modules (utils, paths) -- no cross-dependencies with other Phase 60 modules.

Output: lib/cleanup.ts, lib/gates.ts, lib/requirements.ts with CommonJS proxy .js files, updated jest.config.js thresholds.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/types.ts
@lib/cleanup.js
@lib/gates.js
@lib/requirements.js
@lib/paths.js (proxy pattern reference)
@lib/utils.ts (foundation dependency)
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/cleanup.js to lib/cleanup.ts</name>
  <files>lib/cleanup.ts, lib/cleanup.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/cleanup.js to lib/cleanup.ts
    2. Define interfaces for the quality analysis domain (add to types.ts if reused, otherwise local):
       - CleanupConfig: { enabled: boolean; complexity: boolean; dead_exports: boolean; file_size: boolean; changelog_drift: boolean; readme_links: boolean; jsdoc_drift: boolean; test_coverage_gaps: boolean; export_consistency: boolean; doc_staleness: boolean; config_schema_drift: boolean; ... }
       - QualityViolation: { type: string; severity: string; file: string; message: string; line?: number; details?: string }
       - QualityAnalysisResult: { violations: QualityViolation[]; summary: { total: number; byType: Record<string, number>; bySeverity: Record<string, number> } }
       - QualityMetrics: { timestamp: string; violations: number; byType: Record<string, number> }
       - TrendResult: { metric: string; current: number; previous: number; direction: 'improving' | 'stable' | 'degrading' }
    3. Add type annotations to all 17 exported functions:
       - getCleanupConfig(cwd: string): CleanupConfig
       - analyzeComplexity(cwd: string, files: string[], opts?: Record<string, unknown>): QualityViolation[]
       - analyzeDeadExports(cwd: string, files: string[], opts?: Record<string, unknown>): QualityViolation[]
       - analyzeFileSize(cwd: string, files: string[], opts?: Record<string, unknown>): QualityViolation[]
       - (similar for all other analyze* functions)
       - runQualityAnalysis(cwd: string, files: string[], opts?: Record<string, unknown>): QualityAnalysisResult
       - generateCleanupPlan(violations: QualityViolation[]): string
       - resetCleanupCache(): void
       - loadQualityHistory(cwd: string): QualityMetrics[]
       - saveQualityMetrics(cwd: string, metrics: QualityMetrics): void
       - computeTrends(history: QualityMetrics[]): TrendResult[]
    4. Type internal helper functions with explicit signatures
    5. Use import type for types from types.ts
    6. Convert lib/cleanup.js to thin CommonJS proxy pattern
    7. Note: cleanup uses execFileSync for ESLint subprocess -- type the return as Buffer and handle encoding
    8. Run `npx tsc --noEmit` to verify strict:true compilation

    Avoid:
    - Do NOT use `any` types -- cleanup has complex object structures that need careful typing
    - Do NOT change runtime logic
    - Do NOT delete cleanup.js -- convert to proxy
    - For exec/spawn return values, use typed casts (not any) per Phase 59 pattern
  </action>
  <verify>
    npx tsc --noEmit (zero errors on cleanup.ts)
    npx jest tests/unit/cleanup.test.js --coverage (all tests pass, coverage thresholds met)
    grep -c ': any' lib/cleanup.ts should return 0
  </verify>
  <done>
    lib/cleanup.ts compiles under strict:true with zero any types in exported function signatures,
    all 17 exports have explicit parameter and return types,
    quality violation structures are fully typed (not untyped arrays/objects),
    all cleanup unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/gates.js to lib/gates.ts and lib/requirements.js to lib/requirements.ts</name>
  <files>lib/gates.ts, lib/gates.js, lib/requirements.ts, lib/requirements.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern:

    **Gates migration:**
    1. Copy lib/gates.js to lib/gates.ts
    2. Define interfaces:
       - GateCheckResult: { passed: boolean; errors: string[]; warnings: string[] }
       - GateDefinition: { name: string; description: string; commands: string[]; check: (cwd: string, options: Record<string, unknown>) => GateCheckResult }
       - PreflightResult: { passed: boolean; errors: string[]; warnings: string[] }
    3. Type all exported functions:
       - checkOrphanedPhases(cwd: string): GateCheckResult
       - checkPhaseInRoadmap(cwd: string, phase: string): GateCheckResult
       - checkPhaseHasPlans(cwd: string, phase: string): GateCheckResult
       - checkNoStaleArtifacts(cwd: string): GateCheckResult
       - checkOldPhasesArchived(cwd: string): GateCheckResult
       - checkMilestoneStateCoherence(cwd: string): GateCheckResult
       - GATE_REGISTRY: GateDefinition[]
       - runPreflightGates(cwd: string, command: string, options?: Record<string, unknown>): PreflightResult
       - resetGatesCache(): void
    4. Convert lib/gates.js to thin CommonJS proxy

    **Requirements migration:**
    5. Copy lib/requirements.js to lib/requirements.ts
    6. Define interfaces:
       - Requirement: { id: string; title: string; priority: string; category: string; description: string; status?: string }
       - TraceabilityEntry: { requirement: string; phase: string; plan: string; status: string }
    7. Type all exported functions:
       - _reqContentCache: Map<string, string> (or Record<string, string>)
       - readCachedRequirements(reqFilePath: string): Requirement[]
       - parseRequirements(content: string): Requirement[]
       - parseTraceabilityMatrix(content: string): TraceabilityEntry[]
       - VALID_REQUIREMENT_STATUSES: readonly string[]
       - cmdRequirementGet(cwd: string, reqId: string, raw: boolean): void
       - cmdRequirementList(cwd: string, filters: Record<string, string>, raw: boolean): void
       - cmdRequirementTraceability(cwd: string, filters: Record<string, string>, raw: boolean): void
       - cmdRequirementUpdateStatus(cwd: string, reqId: string, newStatus: string, raw: boolean, dryRun?: boolean): void
    8. Convert lib/requirements.js to thin CommonJS proxy

    **Final validation:**
    9. Update jest.config.js coverage thresholds:
       - Change './lib/cleanup.js' to './lib/cleanup.ts' (same thresholds)
       - Change './lib/gates.js' to './lib/gates.ts' (same thresholds)
       - Add './lib/requirements.ts' threshold if not already present (requirements.js may not have one -- check first)
    10. Run full test suite

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic
  </action>
  <verify>
    npx tsc --noEmit (zero errors)
    npx jest tests/unit/gates.test.js --coverage (all tests pass, coverage met)
    npx jest tests/unit/requirements.test.js --coverage (all tests pass)
    npm test (full suite passes, 2674+ tests)
  </verify>
  <done>
    lib/gates.ts and lib/requirements.ts compile under strict:true with zero any types,
    GATE_REGISTRY is fully typed with GateDefinition[],
    parseRequirements returns typed Requirement[],
    all proxy .js files created,
    jest.config.js updated,
    full test suite passes with no regressions
  </done>
</task>

<task type="auto">
  <name>Task 3: Add domain-specific types to lib/types.ts</name>
  <files>lib/types.ts</files>
  <action>
    Review the interfaces created in Tasks 1 and 2, and promote any that are reused across modules to lib/types.ts.

    Types that should be in types.ts (shared across modules):
    - CleanupConfig (used by cleanup.ts and phase.ts)
    - QualityViolation (used by cleanup.ts and phase.ts)
    - GateCheckResult (used by gates.ts and context.ts)
    - PreflightResult (used by gates.ts and context.ts)
    - Requirement (used by requirements.ts and commands.ts)

    Types that can stay local (only used in one module):
    - SplitResult, SplitMarkdownOptions (if only used in markdown-split.ts)
    - TrendResult (if only used in cleanup.ts)

    For each type added to types.ts:
    1. Define with export interface or export type
    2. Update the importing module to use import type from './types'
    3. Verify tsc --noEmit still passes

    This task ensures types.ts remains the single source of truth for cross-module types.
  </action>
  <verify>
    npx tsc --noEmit (zero errors)
    npm test (no regressions)
  </verify>
  <done>
    All cross-module types are defined in lib/types.ts,
    modules import type from './types' consistently,
    no duplicate type definitions across modules
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors on all three new .ts files
Level 2 (Proxy): Unit tests pass for all three modules with coverage thresholds met; full test suite shows no regressions (2674+ tests passing)
Level 3 (Deferred): CommonJS interop validated across all downstream consumers (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/cleanup.ts, lib/gates.ts, lib/requirements.ts compile under strict:true with zero any types in exported functions
- All gate check functions return typed GateCheckResult structures
- Quality analysis functions return typed QualityViolation arrays
- parseRequirements returns typed Requirement[]
- All .js files converted to CommonJS proxy pattern
- jest.config.js thresholds updated for .ts files
- Cross-module types promoted to lib/types.ts
- All unit tests pass with existing coverage thresholds met or exceeded
- Full test suite (2674+ tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-03-SUMMARY.md`
</output>
