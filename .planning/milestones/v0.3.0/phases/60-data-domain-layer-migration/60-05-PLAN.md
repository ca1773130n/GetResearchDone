---
phase: 60-data-domain-layer-migration
plan: 05
type: execute
wave: 3
depends_on: ["60-01", "60-03", "60-04"]
files_modified:
  - lib/phase.ts
  - lib/phase.js
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/phase.ts compiles under strict:true with zero any types in exported functions"
    - "All 10 exported functions (9 cmd* + atomicWriteFile) have explicit parameter types and return types"
    - "cmdPhaseAdd, cmdPhaseInsert, cmdPhaseRemove, cmdPhaseComplete accept typed options objects (not untyped Record)"
    - "cmdMilestoneComplete handles the full archive workflow with typed parameters"
    - "atomicWriteFile has typed signature (filePath: string, content: string): void"
    - "All existing unit tests for phase pass with coverage thresholds met"
    - "jest.config.js has ./lib/phase.ts threshold entry replacing ./lib/phase.js"
    - "Full test suite (2674+ tests) passes with no regressions across all 11 migrated modules"
  artifacts:
    - path: "lib/phase.ts"
      provides: "Typed phase lifecycle CRUD with 10 exported functions covering add/insert/remove/complete/validate/milestone operations"
      exports: ["cmdPhasesList", "cmdPhaseAdd", "cmdPhaseInsert", "cmdPhaseRemove", "cmdPhaseComplete", "cmdMilestoneComplete", "cmdValidateConsistency", "cmdVersionBump", "cmdPhaseBatchComplete", "atomicWriteFile"]
  key_links:
    - from: "lib/phase.ts"
      to: "lib/frontmatter.ts"
      via: "import of extractFrontmatter"
      pattern: "require.*frontmatter"
    - from: "lib/phase.ts"
      to: "lib/cleanup.ts"
      via: "import of runQualityAnalysis, generateCleanupPlan"
      pattern: "require.*cleanup"
    - from: "lib/phase.ts"
      to: "lib/gates.ts"
      via: "import of runPreflightGates, checkOrphanedPhases"
      pattern: "require.*gates"
    - from: "lib/phase.ts"
      to: "lib/utils.ts"
      via: "import of utility functions"
      pattern: "require.*utils"
    - from: "lib/phase.ts"
      to: "lib/paths.ts"
      via: "import of path resolution functions"
      pattern: "require.*paths"
---

<objective>
Migrate lib/phase.js (1665 lines) to TypeScript with full type annotations under strict:true, and run final validation across all 11 migrated modules.

Purpose: phase.ts is the largest and most complex module in Phase 60, providing the complete phase lifecycle (add, insert, remove, complete, validate, milestone archive). It depends on frontmatter.ts (Plan 01), cleanup.ts and gates.ts (Plan 03), and foundation modules -- making it the natural final migration that validates all prior work integrates correctly. This plan also performs final validation of all 11 modules together.

Output: lib/phase.ts with CommonJS proxy .js file, final jest.config.js update, full validation of entire Phase 60 migration.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/types.ts
@lib/phase.js
@lib/frontmatter.ts (Plan 01 output)
@lib/cleanup.ts (Plan 03 output)
@lib/gates.ts (Plan 03 output)
@lib/utils.ts (foundation dependency)
@lib/paths.ts (foundation dependency)
@.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-01-SUMMARY.md
@.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-03-SUMMARY.md
@.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/phase.js to lib/phase.ts</name>
  <files>lib/phase.ts, lib/phase.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/phase.js to lib/phase.ts
    2. Define interfaces for phase operations (add to types.ts if reused by context.js/commands.js):
       - PhaseAddOptions: { type?: string; depends_on?: string; duration?: string; requirements?: string; verification_level?: string; goal?: string; raw?: boolean }
       - PhaseInsertOptions: same as PhaseAddOptions plus { after: string }
       - PhaseRemoveOptions: { remove_dir?: boolean; raw?: boolean }
       - PhaseCompleteOptions: { skip_cleanup?: boolean; raw?: boolean }
       - MilestoneCompleteOptions: { name?: string; raw?: boolean }
       - PhaseBatchCompleteOptions: { raw?: boolean }
       - ConsistencyResult: { valid: boolean; errors: string[]; warnings: string[]; phase_count: number }
       - PhaseListItem: { number: string; name: string; status: string; plans: number; complete: number }
    3. Type all 10 exported functions:
       - cmdPhasesList(cwd: string, options: Record<string, string>, raw: boolean): void
       - cmdPhaseAdd(cwd: string, description: string, opts: PhaseAddOptions): void
       - cmdPhaseInsert(cwd: string, after: string, description: string, opts: PhaseInsertOptions): void
       - cmdPhaseRemove(cwd: string, phase: string, opts: PhaseRemoveOptions): void
       - cmdPhaseComplete(cwd: string, phase: string, opts: PhaseCompleteOptions): void
       - cmdMilestoneComplete(cwd: string, version: string, opts: MilestoneCompleteOptions): void
       - cmdValidateConsistency(cwd: string, raw?: boolean): void
       - cmdVersionBump(cwd: string, type: string, raw?: boolean): void
       - cmdPhaseBatchComplete(cwd: string, phases: string[], options: PhaseBatchCompleteOptions, raw: boolean): void
       - atomicWriteFile(filePath: string, content: string): void
    4. Type internal helper functions with explicit signatures:
       - Phase insertion logic (decimal numbering computation)
       - ROADMAP.md manipulation helpers (section insertion, phase block formatting)
       - Quality analysis integration (runQualityAnalysis result handling)
    5. Use typed imports from all Wave 1-2 dependencies:
       - extractFrontmatter from frontmatter.ts (typed FrontmatterObject)
       - runQualityAnalysis, generateCleanupPlan from cleanup.ts (typed QualityViolation[])
       - runPreflightGates, checkOrphanedPhases from gates.ts (typed GateCheckResult)
       - Utility functions from utils.ts (already typed)
       - Path functions from paths.ts (already typed)
    6. Convert lib/phase.js to thin CommonJS proxy pattern
    7. Run `npx tsc --noEmit` to verify strict:true compilation

    Avoid:
    - Do NOT use `any` types -- phase.js is the largest module at 1665 lines; use precise types throughout
    - Do NOT change runtime logic -- this is a pure type annotation migration
    - Do NOT delete phase.js -- convert to proxy (DEFER-59-01)
    - For fs operations, use proper types (fs.readFileSync returns string with utf-8 encoding, fs.readdirSync returns string[] or Dirent[])
    - For atomicWriteFile's os.tmpdir() + rename pattern, type the temp path correctly
  </action>
  <verify>
    npx tsc --noEmit (zero errors on phase.ts)
    npx jest tests/unit/phase.test.js --coverage (all tests pass, coverage thresholds met)
    grep -c ': any' lib/phase.ts should return 0
  </verify>
  <done>
    lib/phase.ts compiles under strict:true with zero any types in exported function signatures,
    all 10 exports have explicit parameter and return types,
    typed option interfaces defined for add/insert/remove/complete operations,
    all phase unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 2: Final validation -- update jest.config.js and run full Phase 60 verification</name>
  <files>jest.config.js</files>
  <action>
    1. Update jest.config.js coverage threshold:
       - Change './lib/phase.js' to './lib/phase.ts' (same thresholds)

    2. Verify all 11 modules are now listed as .ts in jest.config.js:
       - lib/frontmatter.ts (Plan 01)
       - lib/markdown-split.ts (Plan 01)
       - lib/state.ts (Plan 02)
       - lib/roadmap.ts (Plan 02)
       - lib/cleanup.ts (Plan 03)
       - lib/gates.ts (Plan 03)
       - lib/requirements.ts (Plan 03 -- if threshold exists)
       - lib/deps.ts (Plan 04)
       - lib/verify.ts (Plan 04)
       - lib/scaffold.ts (Plan 04)
       - lib/phase.ts (this plan)

    3. Run full TypeScript compilation check:
       npx tsc --noEmit
       Verify zero errors across ALL .ts files

    4. Run full test suite:
       npm test
       Verify 2674+ tests pass, all per-file thresholds met

    5. Verify all 11 .js proxy files exist and contain the correct proxy pattern:
       For each of: frontmatter.js, markdown-split.js, state.js, roadmap.js, cleanup.js, gates.js, requirements.js, deps.js, verify.js, scaffold.js, phase.js
       Each should contain: module.exports = require('./{module}.ts')

    6. Verify no `any` types leak in exported functions:
       Run grep for ': any' across all 11 .ts files -- should find zero occurrences in type annotations

    This task serves as the integration verification for the entire Phase 60 migration.
  </action>
  <verify>
    npx tsc --noEmit (zero errors across entire codebase)
    npm test (full suite passes, 2674+ tests, all per-file thresholds met)
    Verify all 11 .ts files exist and compile
    Verify all 11 .js proxy files exist with correct proxy pattern
    Verify zero 'any' types in exported function signatures across all 11 modules
  </verify>
  <done>
    All 11 modules migrated to TypeScript:
      state.ts, roadmap.ts, frontmatter.ts, markdown-split.ts, phase.ts,
      deps.ts, gates.ts, verify.ts, scaffold.ts, cleanup.ts, requirements.ts
    All compile under strict:true with zero any types in exported functions,
    all cmd* functions have explicit parameter and return types,
    all unit tests pass with coverage thresholds met,
    full test suite (2674+ tests) passes with no regressions,
    Phase 60 Data & Domain Layer Migration is complete
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors; all .ts files exist; all .js proxies exist
Level 2 (Proxy): Full test suite (2674+ tests) passes with all per-file coverage thresholds met; zero any types in exported function signatures across all 11 modules
Level 3 (Deferred): CommonJS interop validated across all downstream consumers including context.js, commands.js, parallel.js, mcp-server.js, bin/grd-tools.js (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/phase.ts compiles under strict:true with zero any types in exported functions
- All 10 exported functions have explicit parameter types and return types
- Typed option interfaces defined for phase lifecycle operations
- jest.config.js thresholds updated for phase.ts
- All 11 Phase 60 modules confirmed: .ts files compile, .js proxies exist, tests pass
- Full test suite (2674+ tests) passes with no regressions
- Phase 60 success criteria from ROADMAP.md all met:
  1. 11 modules migrated to .ts
  2. All compile under strict:true with no any types in exported functions
  3. All cmd* functions have explicit parameter and return types
  4. Domain types defined and used (not just string)
  5. All unit tests pass with coverage thresholds met
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-05-SUMMARY.md`
</output>
