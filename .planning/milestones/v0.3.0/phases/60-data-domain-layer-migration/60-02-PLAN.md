---
phase: 60-data-domain-layer-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/state.ts
  - lib/state.js
  - lib/roadmap.ts
  - lib/roadmap.js
  - lib/types.ts
  - jest.config.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/state.ts compiles under strict:true with zero any types in exported functions"
    - "lib/roadmap.ts compiles under strict:true with zero any types in exported functions"
    - "All cmd* functions in state.ts and roadmap.ts have explicit parameter types and return types"
    - "StateFields type from types.ts is used for stateExtractField/stateReplaceField operations"
    - "RoadmapPhase type from types.ts is used for analyzeRoadmap return structures"
    - "All existing unit tests for state and roadmap pass with coverage thresholds met"
    - "CommonJS proxy pattern applied: state.js and roadmap.js re-export from .ts files"
  artifacts:
    - path: "lib/state.ts"
      provides: "Typed STATE.md read/write/patch operations with 14 exported functions"
      exports: ["stateExtractField", "stateReplaceField", "cmdStateLoad", "cmdStateGet", "cmdStatePatch", "cmdStateUpdate", "cmdStateAdvancePlan", "cmdStateRecordMetric", "cmdStateUpdateProgress", "cmdStateAddDecision", "cmdStateAddBlocker", "cmdStateResolveBlocker", "cmdStateRecordSession", "cmdStateSnapshot"]
    - path: "lib/roadmap.ts"
      provides: "Typed ROADMAP.md parsing with schedule computation and phase queries"
      exports: ["formatScheduleDate", "addDays", "computeSchedule", "getScheduleForPhase", "getScheduleForMilestone", "cmdRoadmapGetPhase", "cmdPhaseNextDecimal", "cmdRoadmapAnalyze", "analyzeRoadmap"]
  key_links:
    - from: "lib/state.ts"
      to: "lib/utils.ts"
      via: "import of loadConfig, safeReadMarkdown, output, error"
      pattern: "require.*utils"
    - from: "lib/state.ts"
      to: "lib/paths.ts"
      via: "import of phasesDir"
      pattern: "require.*paths"
    - from: "lib/roadmap.ts"
      to: "lib/utils.ts"
      via: "import of utility functions"
      pattern: "require.*utils"
    - from: "lib/roadmap.ts"
      to: "lib/paths.ts"
      via: "import of phasesDir"
      pattern: "require.*paths"
---

<objective>
Migrate lib/state.js (848 lines) and lib/roadmap.js (580 lines) to TypeScript with full type annotations under strict:true.

Purpose: These are the state management layer modules. state.ts manages the living memory (STATE.md) with field extraction, patching, and snapshot operations. roadmap.ts parses ROADMAP.md into structured phase data with schedule computation. Both are heavily imported by downstream modules (deps.js, phase.js, tracker.js, context.js, autopilot.js, commands.js).

Output: lib/state.ts, lib/roadmap.ts with CommonJS proxy .js files, updated jest.config.js thresholds.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@lib/types.ts
@lib/state.js
@lib/roadmap.js
@lib/paths.js (proxy pattern reference)
@lib/utils.ts (foundation dependency)
@.planning/milestones/v0.3.0/phases/59-foundation-layer-shared-types/59-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/state.js to lib/state.ts</name>
  <files>lib/state.ts, lib/state.js, lib/types.ts</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/state.js to lib/state.ts
    2. Add type annotations to all exported functions:
       - stateExtractField(content: string, fieldName: string): string | null
       - stateReplaceField(content: string, fieldName: string, newValue: string): string
       - cmdStateLoad(cwd: string, raw: boolean): void
       - cmdStateGet(cwd: string, field: string, raw: boolean): void
       - cmdStatePatch(cwd: string, args: string[], raw: boolean): void
       - cmdStateUpdate(cwd: string, field: string, value: string): void
       - cmdStateAdvancePlan(cwd: string, raw: boolean): void
       - cmdStateRecordMetric(cwd: string, args: string[], raw: boolean): void
       - cmdStateUpdateProgress(cwd: string, raw: boolean): void
       - cmdStateAddDecision(cwd: string, args: string[], raw: boolean): void
       - cmdStateAddBlocker(cwd: string, args: string[], raw: boolean): void
       - cmdStateResolveBlocker(cwd: string, args: string[], raw: boolean): void
       - cmdStateRecordSession(cwd: string, options: Record<string, string>, raw: boolean): void
       - cmdStateSnapshot(cwd: string, raw: boolean): void
    3. Define a StateLoadResult interface for the cmdStateLoad return structure (config + state fields + existence flags)
    4. Define a StateSnapshotResult interface for cmdStateSnapshot return structure
    5. Type internal helper functions with explicit signatures
    6. Use StateFields type from types.ts where appropriate for field name/value operations
    7. Add new interfaces to lib/types.ts if they are reused by other modules (e.g., StateLoadResult may be used by context.js)
    8. Use Record<string, unknown> with explicit casts for dynamic STATE.md section parsing (per Phase 59 pattern)
    9. Convert lib/state.js to the thin CommonJS proxy pattern
    10. Run `npx tsc --noEmit` to verify strict:true compilation

    Avoid:
    - Do NOT use `any` types -- use Record<string, unknown> with explicit casts
    - Do NOT delete state.js -- convert to proxy (DEFER-59-01)
    - Do NOT change any runtime logic
    - The cmd* functions call output()/error() which have return type `never` -- TypeScript control flow should handle this correctly
  </action>
  <verify>
    npx tsc --noEmit (zero errors on state.ts)
    npx jest tests/unit/state.test.js --coverage (all tests pass, coverage thresholds met)
    grep -c ': any' lib/state.ts should return 0
  </verify>
  <done>
    lib/state.ts compiles under strict:true with zero any types in exported function signatures,
    all 14 exports have explicit parameter and return types,
    StateFields from types.ts is used for field operations,
    all state unit tests pass with existing coverage thresholds
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/roadmap.js to lib/roadmap.ts and update jest.config.js</name>
  <files>lib/roadmap.ts, lib/roadmap.js, lib/types.ts, jest.config.js</files>
  <action>
    Following the established Phase 59 migration pattern:

    1. Copy lib/roadmap.js to lib/roadmap.ts
    2. Add type annotations to all exported functions:
       - formatScheduleDate(date: Date): string
       - addDays(date: Date, days: number): Date
       - computeSchedule(cwd: string): ScheduleResult
       - getScheduleForPhase(cwd: string, phase: string): PhaseSchedule | null
       - getScheduleForMilestone(cwd: string, version: string): MilestoneSchedule | null
       - cmdRoadmapGetPhase(cwd: string, phase: string, raw: boolean): void
       - cmdPhaseNextDecimal(cwd: string, basePhase: string, raw: boolean): void
       - cmdRoadmapAnalyze(cwd: string, raw: boolean): void
       - analyzeRoadmap(cwd: string): AnalyzedRoadmap
    3. Define interfaces for structured return types:
       - AnalyzedRoadmap: { milestones: Array<{ version: string; name: string; status: string; phases: RoadmapPhase[] }> }
       - ScheduleResult: { phases: PhaseSchedule[]; milestones: MilestoneSchedule[] }
       - PhaseSchedule: { phase: string; name: string; start: string; due: string; duration: number }
       - MilestoneSchedule: { version: string; name: string; start: string; target: string }
       Add to types.ts if reused by tracker.js/deps.js, otherwise define locally
    4. Use RoadmapPhase type from types.ts for parsed phase structures
    5. Type internal helper functions (parsePhaseBlock, stripShippedSections, etc.)
    6. Convert lib/roadmap.js to the thin CommonJS proxy pattern
    7. Update jest.config.js coverage thresholds:
       - Change './lib/state.js' to './lib/state.ts' (same thresholds)
       - Change './lib/roadmap.js' to './lib/roadmap.ts' (same thresholds)
    8. Run full test suite to verify no regressions

    Avoid:
    - Do NOT use `any` types
    - Do NOT change runtime logic
    - Do NOT lower coverage thresholds
  </action>
  <verify>
    npx tsc --noEmit (zero errors)
    npx jest tests/unit/roadmap.test.js --coverage (all tests pass, coverage met)
    npx jest tests/unit/state.test.js --coverage (still passes after jest.config.js change)
    npm test (full suite passes, 2674+ tests)
  </verify>
  <done>
    lib/roadmap.ts compiles under strict:true with zero any types,
    all 9 exports have explicit types,
    RoadmapPhase from types.ts is used for parsed phase structures,
    analyzeRoadmap returns typed AnalyzedRoadmap structure,
    jest.config.js thresholds updated for state.ts and roadmap.ts,
    full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `npx tsc --noEmit` passes with zero errors on both new .ts files
Level 2 (Proxy): Unit tests pass for both modules with coverage thresholds met; full test suite shows no regressions (2674+ tests passing)
Level 3 (Deferred): CommonJS interop validated across all downstream consumers (DEFER-59-01, Phase 65)
</verification>

<success_criteria>
- lib/state.ts and lib/roadmap.ts compile under strict:true with zero any types in exported functions
- All cmd* functions have explicit parameter types and return types
- StateFields and RoadmapPhase types from types.ts are used (not just string)
- Both .js files converted to CommonJS proxy pattern
- jest.config.js thresholds updated for .ts files
- All unit tests pass with existing coverage thresholds met or exceeded
- Full test suite (2674+ tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3.0/phases/60-data-domain-layer-migration/60-02-SUMMARY.md`
</output>
