---
phase: 03-modularize-grd-tools
plan: 07
type: execute
wave: 6
depends_on: ["03-06"]
files_modified:
  - lib/commands.js
  - bin/grd-tools.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "bin/grd-tools.js is a thin CLI router of <= 300 lines"
    - "lib/commands.js contains all remaining standalone command functions"
    - "Every CLI command produces identical output to golden references (full regression)"
    - "No circular dependencies between any lib/ modules"
    - "No lib/*.js file exceeds 500 lines (except tracker.js and context.js which are accepted overages)"
  artifacts:
    - path: "lib/commands.js"
      provides: "Standalone command functions (slug, timestamp, todos, config, history, progress, commit, etc.)"
    - path: "bin/grd-tools.js"
      provides: "Thin CLI router (~200-300 lines): argument parsing, command dispatch, output formatting"
  key_links:
    - from: "bin/grd-tools.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*lib/utils"
    - from: "bin/grd-tools.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*lib/frontmatter"
    - from: "bin/grd-tools.js"
      to: "lib/state.js"
      via: "require"
      pattern: "require.*lib/state"
    - from: "bin/grd-tools.js"
      to: "lib/roadmap.js"
      via: "require"
      pattern: "require.*lib/roadmap"
    - from: "bin/grd-tools.js"
      to: "lib/phase.js"
      via: "require"
      pattern: "require.*lib/phase"
    - from: "bin/grd-tools.js"
      to: "lib/verify.js"
      via: "require"
      pattern: "require.*lib/verify"
    - from: "bin/grd-tools.js"
      to: "lib/scaffold.js"
      via: "require"
      pattern: "require.*lib/scaffold"
    - from: "bin/grd-tools.js"
      to: "lib/tracker.js"
      via: "require"
      pattern: "require.*lib/tracker"
    - from: "bin/grd-tools.js"
      to: "lib/context.js"
      via: "require"
      pattern: "require.*lib/context"
    - from: "bin/grd-tools.js"
      to: "lib/commands.js"
      via: "require"
      pattern: "require.*lib/commands"
---

<objective>
Extract remaining standalone commands into `lib/commands.js` and slim bin/grd-tools.js to a thin CLI router.

Purpose: This is the final extraction. After this plan, bin/grd-tools.js contains ONLY argument parsing and command dispatch (~200-300 lines). This completes the modularization and achieves the phase success criteria.

Output: lib/commands.js with misc commands, bin/grd-tools.js as thin CLI router.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@lib/utils.js
@lib/frontmatter.js
@lib/state.js
@lib/verify.js
@lib/roadmap.js
@lib/scaffold.js
@lib/phase.js
@lib/tracker.js
@lib/context.js
@.planning/phases/03-modularize-grd-tools/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/commands.js with remaining standalone commands</name>
  <files>lib/commands.js, bin/grd-tools.js</files>
  <action>
    Create `lib/commands.js` containing all remaining command functions that haven't been extracted yet:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, loadConfig, execGit, findPhaseInternal, normalizePhaseName,
               generateSlugInternal, resolveModelInternal, pathExistsInternal,
               findCodeFiles, output, error } = require('./utils');
       const { extractFrontmatter } = require('./frontmatter');
       ```

    2. **Functions to move (preserve exact implementation):**
       - cmdGenerateSlug (line ~594) -- 14 lines
       - cmdCurrentTimestamp (line ~608) -- 20 lines
       - cmdListTodos (line ~628) -- 37 lines
       - cmdVerifyPathExists (line ~665) -- 18 lines
       - cmdConfigEnsureSection (line ~683) -- 73 lines
       - cmdConfigSet (line ~756) -- 45 lines
       - cmdHistoryDigest (line ~801) -- 85 lines
       - cmdPhasesList (line ~886) -- IF NOT already moved to lib/phase.js, move here
       - cmdPhasePlanIndex (line ~1871) -- 108 lines
       - cmdSummaryExtract (line ~2080) -- 57 lines
       - cmdProgressRender (line ~3399) -- 74 lines
       - cmdTodoComplete (line ~3473) -- 29 lines
       - cmdResolveModel (line ~1450) -- 20 lines
       - cmdFindPhase (line ~1470) -- 44 lines
       - cmdCommit (line ~1514) -- 48 lines

    3. **module.exports:** Export all functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/commands.js
       - Remove all moved function definitions
       - bin/grd-tools.js should now contain ONLY:
         a) Require statements for all lib/ modules
         b) The main() function with the CLI router switch statement
         c) The `main()` call at the bottom

    CRITICAL: After this extraction, bin/grd-tools.js should be ~200-300 lines total:
    - ~20 lines: require imports
    - ~10 lines: main() setup (args parsing, raw detection, cwd)
    - ~200 lines: switch/case router dispatching to lib/ functions
    - ~5 lines: main() call and error handling

    Verify the router switch statement still correctly dispatches to all command functions. Each case should call the same function name -- just now imported from a lib/ module instead of defined locally.

    NOTE: cmdPhasesList may have been moved to lib/phase.js in Plan 03-05. If so, skip it here. Check bin/grd-tools.js to see what remains.
  </action>
  <verify>
    Run: node bin/grd-tools.js generate-slug "hello world"
    Run: node bin/grd-tools.js current-timestamp
    Run: node bin/grd-tools.js history-digest
    Run: node bin/grd-tools.js progress json
    All must produce valid JSON/output. (Level 1: Sanity)

    Diff all outputs against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/commands.js exists with all standalone command functions.
    bin/grd-tools.js is reduced to a thin CLI router.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification against golden references</name>
  <files>bin/grd-tools.js</files>
  <action>
    Run the golden reference capture script from Plan 03-01 to capture POST-modularization output:

    1. Run `bash tests/golden/capture.sh` (or equivalent) to capture current output to a temp directory
    2. Diff every output file against the original golden references in tests/golden/output/
    3. Identify ANY differences
    4. For each difference, determine if it's:
       a) A real regression (FIX IT)
       b) An expected difference (timestamps, paths -- document why)
    5. Fix any real regressions

    Additionally verify:
    - `wc -l bin/grd-tools.js` <= 300 lines
    - `wc -l lib/*.js` -- no file exceeds 500 lines (with documented exceptions for tracker.js and context.js)
    - No circular dependencies: `node -e "require('./lib/utils'); require('./lib/frontmatter'); require('./lib/state'); require('./lib/verify'); require('./lib/roadmap'); require('./lib/scaffold'); require('./lib/phase'); require('./lib/tracker'); require('./lib/context'); require('./lib/commands'); console.log('OK')"` (run from project root)

    Final check:
    - Count total functions exported across all lib/ modules
    - Verify bin/grd-tools.js has NO function definitions (only require + switch router)
  </action>
  <verify>
    Full golden reference diff shows zero regressions. (Level 2: Proxy)
    bin/grd-tools.js <= 300 lines. (Level 1: Sanity)
    All lib/ modules load without circular dependency errors. (Level 1: Sanity)
  </verify>
  <done>
    Full regression passes. bin/grd-tools.js is a thin CLI router <= 300 lines.
    All 9 lib/ modules load correctly. Zero circular dependencies.
    Modularization is complete.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- bin/grd-tools.js <= 300 lines
- All lib/ modules load without errors
- No circular dependencies
- All commands produce valid JSON

Level 2 (Proxy):
- Full golden reference diff shows zero regressions across all CLI commands
- Module line counts verified (with documented overages)

Level 3 (Deferred):
- DEFER-03-01: All 40 commands work after modularization (validated in Phase 4)
- DEFER-03-02: CLI JSON output unchanged (validated in Phase 4 via golden snapshots)
</verification>

<success_criteria>
- bin/grd-tools.js <= 300 lines (thin CLI router only)
- lib/commands.js <= 500 lines
- All CLI commands produce identical output to golden references
- Zero circular dependencies across all lib/ modules
- Total lib/ modules: 9 files (utils, frontmatter, state, verify, roadmap, scaffold, phase, tracker, context, commands = 10 files)
- bin/grd-tools.js contains ZERO function definitions -- only require + router
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-07-SUMMARY.md`
</output>
