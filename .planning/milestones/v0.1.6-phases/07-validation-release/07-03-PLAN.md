---
phase: 07-validation-release
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified: [CONTRIBUTING.md, VERSION, .claude-plugin/plugin.json, CHANGELOG.md, grd-file-manifest.json]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "CONTRIBUTING.md exists with architecture overview, test guide, and PR guidelines"
    - "VERSION file contains exactly '0.0.5'"
    - ".claude-plugin/plugin.json version field is '0.0.5'"
    - "CHANGELOG.md has a [0.0.5] section with comprehensive release notes"
    - "All three version sources match: VERSION = plugin.json = CHANGELOG"
    - "grd-file-manifest.json is regenerated reflecting current file state"
    - "npm test passes (full test suite confirms no regressions)"
    - "npm run lint exits 0"
    - "All PRODUCT-QUALITY.md P0 targets verified met"
    - "All PRODUCT-QUALITY.md P1 targets verified met"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Contributor guide with architecture overview, test guide, and PR guidelines"
      contains: "Architecture"
    - path: "VERSION"
      provides: "Version file set to 0.0.5"
      contains: "0.0.5"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with version 0.0.5"
      contains: "0.0.5"
    - path: "CHANGELOG.md"
      provides: "Changelog with 0.0.5 release section"
      contains: "[0.0.5]"
    - path: "grd-file-manifest.json"
      provides: "Regenerated file manifest with current SHA256 hashes"
  key_links:
    - from: "VERSION"
      to: ".claude-plugin/plugin.json"
      via: "Version numbers must match"
      pattern: "0.0.5"
    - from: "VERSION"
      to: "CHANGELOG.md"
      via: "CHANGELOG must have a section for the current version"
      pattern: "0.0.5"
---

<objective>
Create CONTRIBUTING.md, bump version to 0.0.5 across all version files, regenerate the file manifest, and perform final product verification against all PRODUCT-QUALITY.md targets.

Purpose: Complete v0.0.5 release preparation — the final plan of the project. Close all P1/P2 gaps (contributor guide, version sync) and verify all P0/P1 targets are met.
Output: CONTRIBUTING.md, VERSION=0.0.5, plugin.json=0.0.5, CHANGELOG.md with 0.0.5 section, regenerated manifest, product verification checklist in SUMMARY.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT-QUALITY.md
@.planning/phases/07-validation-release/07-01-SUMMARY.md
@VERSION
@.claude-plugin/plugin.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md and bump version to 0.0.5</name>
  <files>CONTRIBUTING.md, VERSION, .claude-plugin/plugin.json, CHANGELOG.md</files>
  <action>
    1. **Create CONTRIBUTING.md** with the following sections:

       **Architecture Overview:**
       - `bin/grd-tools.js` — Thin CLI router (~200 lines), dispatches to lib/ modules
       - `lib/` — 10 modules with clear responsibilities:
         - `utils.js` — Shared helpers, constants, input validation, git operations
         - `frontmatter.js` — YAML frontmatter parse/serialize/validate
         - `state.js` — STATE.md read/write/patch operations
         - `roadmap.js` — ROADMAP.md parsing, schedule computation
         - `phase.js` — Phase lifecycle (add, insert, remove, complete)
         - `tracker.js` — GitHub/Jira issue tracker sync
         - `verify.js` — Verification suite (plan structure, references, artifacts)
         - `scaffold.js` — Template scaffolding
         - `context.js` — Init workflow context loading
         - `commands.js` — Standalone utility commands
       - `agents/` — Claude agent definitions (.md files)
       - `commands/` — Command definitions (.md files)
       - `.planning/` — Project planning state (not shipped)
       - `tests/` — Jest test suite with unit and integration tests

       **How to Add a New Command:**
       1. Create command function `cmdMyCommand(cwd, args, raw)` in appropriate `lib/*.js` module
       2. Add case to switch statement in `bin/grd-tools.js`
       3. Create `commands/my-command.md` with agent instructions
       4. Add unit test in `tests/unit/*.test.js`
       5. Add integration test in `tests/integration/cli.test.js`

       **How to Add a New Agent:**
       1. Create `agents/grd-my-agent.md` with role, tools, and execution flow
       2. Add model profile entry in `lib/utils.js` MODEL_PROFILES table
       3. Create orchestrator command in `commands/` that spawns the agent

       **Running Tests:**
       ```bash
       npm test              # Run full test suite with coverage
       npm run test:unit     # Run unit tests only (if script exists, otherwise: npx jest tests/unit/)
       npm run lint          # Run ESLint
       npm run format        # Auto-format with Prettier
       npm run format:check  # Check formatting without writing
       ```

       **PR Guidelines:**
       - All PRs must pass CI (lint + test + format check on Node 18/20/22)
       - New features require unit tests
       - CLI changes require integration tests
       - Follow existing code style (enforced by Prettier + ESLint)
       - Keep lib/ modules under 500 lines where feasible
       - Use `execFileSync` with argument arrays for shell commands (never string interpolation)
       - Zero runtime dependencies — dev dependencies only

       **Code Style:**
       - 2-space indent, single quotes, semicolons (configured in .prettierrc)
       - ESLint recommended rules (configured in eslint.config.js)
       - All exported functions must have JSDoc comments

    2. **Bump VERSION to 0.0.5:**
       - Write `0.0.5` to the `VERSION` file (replace current content)

    3. **Update .claude-plugin/plugin.json version to 0.0.5:**
       - Read the file, parse JSON, update `version` field to `"0.0.5"`, write back with 2-space indent

    4. **Update CHANGELOG.md with [0.0.5] section:**
       Insert a new `## [0.0.5] - 2026-02-15` section at the top (after the header), containing:

       ```markdown
       ## [0.0.5] - 2026-02-15

       ### Added
       - **Input validation layer:** All CLI entry points validate phase numbers, file paths, git refs, and subcommands before dispatch
       - **JSDoc documentation:** All exported functions in 10 lib/ modules have JSDoc comments with @param and @returns
       - **CONTRIBUTING.md:** Contributor guide with architecture overview, test guide, and PR guidelines
       - **Status dashboard commands:** `grd:dashboard`, `grd:phase-detail`, `grd:health` for project visibility

       ### Changed
       - **Version bump to 0.0.5:** First production-quality release
       - **Modular architecture:** Monolithic grd-tools.js (5,632 lines) decomposed into 10 lib/ modules (largest: 1,573 lines)
       - **Security hardening:** All execSync calls replaced with execFileSync + argument arrays; git operation whitelist enforced
       - **CI/CD pipeline:** GitHub Actions workflow with Node 18/20/22 matrix, lint, test, format check, security audit
       - **Code style enforcement:** ESLint + Prettier configured and enforced in CI

       ### Security
       - Zero command injection vectors (verified by code audit)
       - Path traversal blocked in all file path arguments
       - Git ref flag injection blocked
       - Git operation whitelist prevents destructive commands

       ### Testing
       - 543+ tests (unit + integration)
       - >= 80% line coverage on lib/ modules
       - 27 golden snapshot tests for CLI output stability
       - 78 integration tests for end-to-end CLI behavior
       ```

       Adjust the test count if it has changed since Phase 6.

    5. **Verify version consistency:**
       - Read VERSION, plugin.json version, and CHANGELOG.md [0.0.5] section
       - All three must contain "0.0.5"
  </action>
  <verify>
    - `cat CONTRIBUTING.md | head -1` shows "# Contributing" (Level 1: Sanity)
    - `cat VERSION` shows "0.0.5" (Level 1: Sanity)
    - `node -e "console.log(require('./.claude-plugin/plugin.json').version)"` prints "0.0.5" (Level 1: Sanity)
    - `grep '\[0.0.5\]' CHANGELOG.md` matches (Level 1: Sanity)
    - `npm run lint` exits 0 (Level 1: Sanity)
  </verify>
  <done>
    CONTRIBUTING.md created with architecture overview, command/agent addition guides, test instructions, and PR guidelines. VERSION=0.0.5, plugin.json=0.0.5, CHANGELOG.md has [0.0.5] section. All three version sources in sync.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate manifest and perform final product verification</name>
  <files>grd-file-manifest.json</files>
  <action>
    1. **Regenerate grd-file-manifest.json:**
       ```bash
       node bin/grd-manifest.js generate
       ```
       This regenerates SHA256 hashes for all tracked files reflecting the final v0.0.5 state.

    2. **Run full test suite:**
       ```bash
       npm test
       ```
       All tests must pass. Record the total test count and coverage percentages.

    3. **Run lint and format check:**
       ```bash
       npm run lint && npm run format:check
       ```
       Both must exit 0.

    4. **Product verification checklist — verify every target in PRODUCT-QUALITY.md:**

       **P0 Targets (must all pass):**
       - [ ] Test coverage >= 80% on lib/ modules → run `npm test -- --coverage` and check Jest output
       - [ ] No JS file exceeds 500 lines → run `wc -l lib/*.js bin/grd-tools.js` (NOTE: some modules were accepted above 500 lines in Phase 3 decisions — verify against those accepted thresholds: commands.js, context.js, tracker.js, phase.js were accepted as-is)
       - [ ] Zero command injection vectors → `grep -r "execSync" bin/ lib/` returns 0 matches (only execFileSync allowed)
       - [ ] CI pipeline exists → `.github/workflows/ci.yml` exists and contains Node 18/20/22 matrix
       - [ ] .gitignore exists → `.gitignore` file exists with standard exclusions

       **P1 Targets (must all pass):**
       - [ ] ESLint passes → `npm run lint` exits 0
       - [ ] Prettier compliance → `npm run format:check` exits 0
       - [ ] Version sync → VERSION, plugin.json, CHANGELOG all say 0.0.5
       - [ ] No deprecated config → `grep "github_integration" .planning/config.json` returns 0 matches
       - [ ] Input validation → Plan 07-01 completed (all CLI entry points validated)

       **P2 Targets (should pass):**
       - [ ] package.json present → exists with engines.node >= 18
       - [ ] .editorconfig present → exists
       - [ ] JSDoc on public functions → Plan 07-02 completed (all exports documented)
       - [ ] CONTRIBUTING.md → created in Task 1

       **Operational Requirements:**
       - [ ] Node.js >= 18 specified in package.json engines
       - [ ] CLI < 500ms → `time node bin/grd-tools.js current-timestamp` completes quickly
       - [ ] Zero external runtime deps → check package.json dependencies (should be empty or absent)

       **Deferred Validations:**
       - [ ] DEFER-03-01: RESOLVED (Phase 4: 78 integration tests)
       - [ ] DEFER-03-02: RESOLVED (Phase 4: 27 golden snapshot tests)
       - [ ] DEFER-06-01: RESOLVED (Phase 6: zero errors across all source)
       - [ ] DEFER-08-01: PENDING (post-v0.0.5 — user acceptance testing)
       - [ ] DEFER-08-02: RESOLVED (Phase 8 review)

       Document all verification results in the SUMMARY.md with pass/fail for each target.

    5. **If any P0 or P1 target is NOT met:**
       - Document what failed and why
       - Fix it if the fix is trivial (< 5 minutes)
       - If non-trivial, document as a known issue for post-1.0 resolution

    6. **Record final summary in SUMMARY.md** with:
       - Product verification results table (all P0/P1/P2 targets)
       - Final test count and coverage
       - Deferred validation status
       - v0.0.5 release readiness assessment
  </action>
  <verify>
    - `ls grd-file-manifest.json` exists (Level 1: Sanity)
    - `npm test` exits 0 (Level 2: Proxy — full regression)
    - `npm run lint && npm run format:check` exits 0 (Level 2: Proxy)
    - `grep -r "execSync" bin/ lib/ | grep -v execFileSync | grep -v "require.*child_process" | wc -l` returns 0 (Level 2: Proxy — no command injection)
    - `cat VERSION` shows "0.0.5" and `grep "0.0.5" .claude-plugin/plugin.json` matches (Level 2: Proxy — version sync)
    - All P0 and P1 targets documented as passed in SUMMARY.md (Level 2: Proxy)
  </verify>
  <done>
    grd-file-manifest.json regenerated. Full product verification completed: all P0 targets met, all P1 targets met, all P2 targets met. Deferred validations tracked (DEFER-08-01 remains post-v0.0.5). v0.0.5 release ready — VERSION, plugin.json, and CHANGELOG all synced to 0.0.5.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- CONTRIBUTING.md exists with architecture section
- VERSION = 0.0.5
- plugin.json version = 0.0.5
- CHANGELOG.md has [0.0.5] section
- grd-file-manifest.json exists

Level 2 (Proxy):
- npm test exits 0 (full regression)
- npm run lint exits 0
- npm run format:check exits 0
- Zero execSync usage (no command injection vectors)
- Version sync across all 3 files
- All PRODUCT-QUALITY.md P0/P1 targets verified

Level 3 (Deferred):
- DEFER-08-01: User acceptance testing of TUI commands (post-v0.0.5)
</verification>

<success_criteria>
- CONTRIBUTING.md exists with architecture overview, test guide, PR guidelines
- VERSION = 0.0.5, plugin.json = 0.0.5, CHANGELOG has 0.0.5 section
- All three version sources match
- grd-file-manifest.json regenerated
- All PRODUCT-QUALITY.md P0 targets verified met
- All PRODUCT-QUALITY.md P1 targets verified met
- All PRODUCT-QUALITY.md P2 targets verified met
- Full test suite passes
- Product verification checklist documented in SUMMARY.md
- v0.0.5 release ready
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation-release/07-03-SUMMARY.md`
</output>
