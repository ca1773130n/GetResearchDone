---
phase: 07-validation-release
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [lib/utils.js, lib/frontmatter.js, lib/state.js, lib/roadmap.js, lib/phase.js, lib/tracker.js, lib/verify.js, lib/scaffold.js, lib/context.js, lib/commands.js]
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Every exported function in all 10 lib/*.js modules has a JSDoc comment with @param and @returns"
    - "JSDoc comments accurately describe function purpose, parameter types, and return types"
    - "No behavioral changes — only comment additions"
    - "npm test passes unchanged (JSDoc comments are non-functional)"
    - "npm run lint exits 0 (JSDoc does not introduce lint errors)"
  artifacts:
    - path: "lib/utils.js"
      provides: "JSDoc-documented shared utility functions"
      contains: "@param"
    - path: "lib/commands.js"
      provides: "JSDoc-documented command functions"
      contains: "@param"
    - path: "lib/state.js"
      provides: "JSDoc-documented state management functions"
      contains: "@param"
    - path: "lib/context.js"
      provides: "JSDoc-documented context/init functions"
      contains: "@param"
  key_links:
    - from: "lib/utils.js"
      to: "lib/frontmatter.js"
      via: "Module imports unchanged after JSDoc additions"
      pattern: "require.*utils"
---

<objective>
Add JSDoc comments to all exported functions across all 10 lib/ modules, providing type documentation for every public function.

Purpose: Close the P2 gap "JSDoc on public functions: All exported functions" from PRODUCT-QUALITY.md. Enable IDE autocompletion and documentation generation for contributors.
Output: All 10 lib/*.js files updated with JSDoc comments on every exported function.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT-QUALITY.md
@.planning/phases/07-validation-release/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to utility, frontmatter, state, roadmap, and scaffold modules</name>
  <files>lib/utils.js, lib/frontmatter.js, lib/state.js, lib/roadmap.js, lib/scaffold.js</files>
  <action>
    Add JSDoc comments directly above each exported function definition in the following modules. Only add comments — do NOT change any function signatures, logic, or behavior.

    **JSDoc format to use:**
    ```javascript
    /**
     * Brief one-line description.
     * @param {string} cwd - Project working directory
     * @param {boolean} raw - Output raw text instead of JSON
     * @returns {void} Outputs result to stdout and exits
     */
    ```

    **lib/utils.js** — Document all exported functions (~20 functions):
    - `parseIncludeFlag(args)` — Parse --include flag from CLI args, returns Set of included items
    - `safeReadFile(filePath)` — Read file returning content or null on error
    - `loadConfig(cwd)` — Load and merge .planning/config.json with defaults
    - `isGitIgnored(cwd, targetPath)` — Check if file is git-ignored via git check-ignore
    - `execGit(cwd, args, opts)` — Execute git command with whitelist enforcement
    - `normalizePhaseName(phase)` — Normalize phase identifier (zero-pad, validate format)
    - `findCodeFiles(dir, maxDepth, found, depth)` — Recursively find code files up to maxDepth
    - `validatePhaseName(phase)` — Validate phase name format (strict)
    - `validateFilePath(filePath, cwd)` — Validate file path does not escape project
    - `validateGitRef(ref)` — Validate git ref format (no flag injection)
    - `validatePhaseArg(phase)` — Validate CLI phase number argument (from Plan 07-01)
    - `validateFileArg(filePath, cwd)` — Validate CLI file path argument (from Plan 07-01)
    - `validateSubcommand(sub, validSubs, parentCmd)` — Validate CLI subcommand (from Plan 07-01)
    - `validateRequiredArg(value, argName)` — Validate required CLI argument (from Plan 07-01)
    - `output(result, raw, rawValue)` — Write JSON or raw output to stdout and exit
    - `error(message)` — Write error message to stderr and exit with code 1
    - `resolveModelInternal(cwd, agentType)` — Resolve model name for agent type from config
    - `findPhaseInternal(cwd, phase)` — Find phase directory and enumerate its plans/summaries
    - `pathExistsInternal(cwd, targetPath)` — Check if path exists on filesystem
    - `generateSlugInternal(text)` — Generate kebab-case slug from text
    - `getMilestoneInfo(cwd)` — Extract milestone version and name from ROADMAP.md
    - `resolveModelForAgent(config, agentType)` — Resolve model from config object (no disk I/O)

    **lib/frontmatter.js** — Document all exported functions (~8 functions):
    - `extractFrontmatter(content)` — Parse YAML frontmatter from markdown content
    - `reconstructFrontmatter(obj)` — Serialize object back to YAML frontmatter string
    - `spliceFrontmatter(content, newObj)` — Replace frontmatter in markdown content
    - `parseMustHavesBlock(content, blockName)` — Parse must_haves block from frontmatter text
    - `cmdFrontmatterGet(cwd, filePath, field, raw)` — CLI: get frontmatter field(s)
    - `cmdFrontmatterSet(cwd, filePath, field, value, raw)` — CLI: set frontmatter field
    - `cmdFrontmatterMerge(cwd, filePath, data, raw)` — CLI: merge data into frontmatter
    - `cmdFrontmatterValidate(cwd, filePath, schemaName, raw)` — CLI: validate against schema

    **lib/state.js** — Document all exported functions (~13 functions):
    - `stateExtractField(content, fieldName)` — Extract field value from STATE.md markdown
    - `stateReplaceField(content, fieldName, newValue)` — Replace field value in STATE.md markdown
    - All 11 `cmdState*` functions with their specific parameters and purposes

    **lib/roadmap.js** — Document all exported functions (~8 functions):
    - `formatScheduleDate(date)` — Format Date as YYYY-MM-DD string
    - `addDays(date, days)` — Add N days to a Date, returning new Date
    - `computeSchedule(cwd)` — Compute milestone/phase schedule from ROADMAP.md durations
    - `getScheduleForPhase(schedule, phaseNum)` — Lookup phase schedule entry
    - `getScheduleForMilestone(schedule, version)` — Lookup milestone schedule entry
    - `cmdRoadmapGetPhase(cwd, phaseNum, raw)` — CLI: get phase info from roadmap
    - `cmdPhaseNextDecimal(cwd, basePhase, raw)` — CLI: compute next decimal phase number
    - `cmdRoadmapAnalyze(cwd, raw)` — CLI: analyze full roadmap structure

    **lib/scaffold.js** — Document all exported functions (3 functions):
    - `cmdTemplateSelect(cwd, planPath, raw)` — CLI: select template for plan type
    - `cmdTemplateFill(cwd, templateType, options, raw)` — CLI: fill template with values
    - `cmdScaffold(cwd, type, options, raw)` — CLI: scaffold directory structure

    After adding all JSDoc comments:
    - Run `npm run lint` — must exit 0
    - Run `npm run format:check` — if Prettier reformats JSDoc, run `npm run format` first
    - Run `npm test` — must pass unchanged
  </action>
  <verify>
    - `grep -c "@param" lib/utils.js` returns >= 40 (Level 1: Sanity — all functions documented)
    - `grep -c "@param" lib/frontmatter.js` returns >= 15 (Level 1: Sanity)
    - `grep -c "@param" lib/state.js` returns >= 20 (Level 1: Sanity)
    - `grep -c "@param" lib/roadmap.js` returns >= 15 (Level 1: Sanity)
    - `grep -c "@param" lib/scaffold.js` returns >= 8 (Level 1: Sanity)
    - `npm test` exits 0 (Level 1: Sanity — no behavioral changes)
    - `npm run lint` exits 0 (Level 1: Sanity)
  </verify>
  <done>
    JSDoc comments added to all exported functions in utils.js (22 functions), frontmatter.js (8 functions), state.js (13 functions), roadmap.js (8 functions), and scaffold.js (3 functions). All tests pass, lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JSDoc to phase, tracker, verify, context, and commands modules</name>
  <files>lib/phase.js, lib/tracker.js, lib/verify.js, lib/context.js, lib/commands.js</files>
  <action>
    Add JSDoc comments directly above each exported function definition in the remaining 5 modules. Only add comments — do NOT change any function signatures, logic, or behavior.

    **lib/verify.js** — Document all exported functions (7 functions):
    - `cmdVerifySummary(cwd, summaryPath, checkFileCount, raw)` — CLI: verify SUMMARY.md structure
    - `cmdVerifyPlanStructure(cwd, filePath, raw)` — CLI: verify PLAN.md structure and frontmatter
    - `cmdVerifyPhaseCompleteness(cwd, phase, raw)` — CLI: check all plans have summaries
    - `cmdVerifyReferences(cwd, filePath, raw)` — CLI: validate @-references and file paths
    - `cmdVerifyCommits(cwd, hashes, raw)` — CLI: batch verify git commit existence
    - `cmdVerifyArtifacts(cwd, planFilePath, raw)` — CLI: check must_haves.artifacts exist
    - `cmdVerifyKeyLinks(cwd, planFilePath, raw)` — CLI: validate must_haves.key_links patterns

    **lib/phase.js** — Document all exported functions (7 functions):
    - `cmdPhasesList(cwd, options, raw)` — CLI: list phases with optional type/phase filter
    - `cmdPhaseAdd(cwd, description, raw)` — CLI: add new phase to end of roadmap
    - `cmdPhaseInsert(cwd, afterPhase, description, raw)` — CLI: insert phase after specified phase
    - `cmdPhaseRemove(cwd, targetPhase, options, raw)` — CLI: remove phase and renumber subsequent
    - `cmdPhaseComplete(cwd, phaseNum, raw)` — CLI: mark phase as complete in roadmap and state
    - `cmdMilestoneComplete(cwd, version, options, raw)` — CLI: archive completed milestone
    - `cmdValidateConsistency(cwd, raw)` — CLI: validate phase numbering and disk/roadmap sync

    **lib/tracker.js** — Document all exported functions (6 functions):
    - `loadTrackerConfig(cwd)` — Load tracker configuration from config.json
    - `loadTrackerMapping(cwd)` — Load tracker ID mapping from TRACKER.md
    - `saveTrackerMapping(cwd, mapping)` — Save tracker ID mapping to TRACKER.md
    - `createGitHubTracker(cwd, config)` — Create GitHub Issues tracker operations object
    - `createTracker(cwd)` — Factory: create tracker instance based on config provider
    - `cmdTracker(cwd, subcommand, args, raw)` — CLI: dispatch tracker subcommand

    **lib/context.js** — Document all exported functions (14 functions):
    - `cmdInitExecutePhase(cwd, phase, includes, raw)` — CLI: initialize execute-phase context
    - `cmdInitPlanPhase(cwd, phase, includes, raw)` — CLI: initialize plan-phase context
    - `cmdInitNewProject(cwd, raw)` — CLI: initialize new-project context
    - `cmdInitNewMilestone(cwd, raw)` — CLI: initialize new-milestone context
    - `cmdInitQuick(cwd, description, raw)` — CLI: initialize quick-task context
    - `cmdInitResume(cwd, raw)` — CLI: initialize resume context from session continuity
    - `cmdInitVerifyWork(cwd, phase, raw)` — CLI: initialize verify-work context
    - `cmdInitPhaseOp(cwd, phase, raw)` — CLI: initialize phase-op context
    - `cmdInitTodos(cwd, area, raw)` — CLI: initialize todos context
    - `cmdInitMilestoneOp(cwd, raw)` — CLI: initialize milestone-op context
    - `cmdInitMapCodebase(cwd, raw)` — CLI: initialize map-codebase context
    - `cmdInitProgress(cwd, includes, raw)` — CLI: initialize progress context
    - `cmdInitResearchWorkflow(cwd, workflow, topic, includes, raw)` — CLI: initialize research workflow context
    - `cmdInitPlanMilestoneGaps(cwd, raw)` — CLI: initialize plan-milestone-gaps context

    **lib/commands.js** — Document all exported functions (17 functions):
    - `cmdGenerateSlug(text, raw)` — CLI: generate kebab-case slug from text
    - `cmdCurrentTimestamp(format, raw)` — CLI: output current timestamp in specified format
    - `cmdListTodos(cwd, area, raw)` — CLI: list todo files in .planning/todos/
    - `cmdTodoComplete(cwd, filename, raw)` — CLI: mark todo file as completed
    - `cmdVerifyPathExists(cwd, targetPath, raw)` — CLI: check if path exists in project
    - `cmdConfigEnsureSection(cwd, raw)` — CLI: ensure config.json has required sections
    - `cmdConfigSet(cwd, keyPath, value, raw)` — CLI: set config value by dot-path key
    - `cmdHistoryDigest(cwd, raw)` — CLI: aggregate metrics from all SUMMARY.md files
    - `cmdResolveModel(cwd, agentType, raw)` — CLI: resolve model name for agent type
    - `cmdFindPhase(cwd, phase, raw)` — CLI: find phase directory by number
    - `cmdCommit(cwd, message, files, raw, amend)` — CLI: create git commit with specified files
    - `cmdPhasePlanIndex(cwd, phase, raw)` — CLI: index plans with wave and status info
    - `cmdSummaryExtract(cwd, summaryPath, fields, raw)` — CLI: extract structured data from summary
    - `cmdProgressRender(cwd, format, raw)` — CLI: render project progress in specified format
    - `cmdDashboard(cwd, raw)` — CLI: full project dashboard with milestones, phases, and plans
    - `cmdPhaseDetail(cwd, phase, raw)` — CLI: detailed drill-down for single phase
    - `cmdHealth(cwd, raw)` — CLI: project health indicators and metrics

    After adding all JSDoc comments:
    - Run `npm run format` to ensure Prettier formats JSDoc consistently
    - Run `npm run lint` — must exit 0
    - Run `npm test` — must pass unchanged
  </action>
  <verify>
    - `grep -c "@param" lib/verify.js` returns >= 10 (Level 1: Sanity)
    - `grep -c "@param" lib/phase.js` returns >= 14 (Level 1: Sanity)
    - `grep -c "@param" lib/tracker.js` returns >= 10 (Level 1: Sanity)
    - `grep -c "@param" lib/context.js` returns >= 30 (Level 1: Sanity)
    - `grep -c "@param" lib/commands.js` returns >= 30 (Level 1: Sanity)
    - `npm test` exits 0 (Level 1: Sanity — no behavioral changes)
    - `npm run lint` exits 0 (Level 1: Sanity)
  </verify>
  <done>
    JSDoc comments added to all exported functions in verify.js (7), phase.js (7), tracker.js (6), context.js (14), and commands.js (17). Total: all ~108 exports across 10 modules are JSDoc-documented. All tests pass, lint clean. P2 metric "JSDoc on public functions" is met.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- Every lib/*.js file contains @param in JSDoc comments
- No function signature or logic changes (diff shows only comment additions)
- npm test exits 0 unchanged
- npm run lint exits 0
- npm run format:check exits 0

Level 2 (Proxy):
- None — JSDoc is non-functional; sanity checks are sufficient

Level 3 (Deferred):
- None
</verification>

<success_criteria>
- All exported functions in all 10 lib/*.js modules have JSDoc with @param and @returns
- Zero behavioral changes (only comment additions)
- npm test passes unchanged
- npm run lint exits 0
- P2 metric "JSDoc on public functions: All exported functions" is met
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation-release/07-02-SUMMARY.md`
</output>
