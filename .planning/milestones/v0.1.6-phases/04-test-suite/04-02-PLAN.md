---
phase: 04-test-suite
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/unit/state.test.js
  - tests/unit/verify.test.js
  - tests/unit/scaffold.test.js
  - tests/unit/commands.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "state.test.js covers load, get, patch, advance-plan, record-metric, add-decision, add/resolve-blocker (>= 18 test cases)"
    - "verify.test.js covers plan-structure, phase-completeness, references, artifacts, key-links (>= 12 test cases)"
    - "scaffold.test.js covers template-select, template-fill, scaffold operations (>= 8 test cases)"
    - "commands.test.js covers slug, timestamp, todos, path-exists, config, history-digest, progress, commit (>= 15 test cases)"
    - "All 4 test files pass with npm test"
    - "Each test file uses temp directories for mutating operations (no cross-test pollution)"
  artifacts:
    - path: "tests/unit/state.test.js"
      provides: "Unit tests for lib/state.js"
      contains: "describe"
    - path: "tests/unit/verify.test.js"
      provides: "Unit tests for lib/verify.js"
      contains: "describe"
    - path: "tests/unit/scaffold.test.js"
      provides: "Unit tests for lib/scaffold.js"
      contains: "describe"
    - path: "tests/unit/commands.test.js"
      provides: "Unit tests for lib/commands.js"
      contains: "describe"
  key_links:
    - from: "tests/unit/state.test.js"
      to: "lib/state.js"
      via: "require"
      pattern: "require.*lib/state"
    - from: "tests/unit/verify.test.js"
      to: "lib/verify.js"
      via: "require"
      pattern: "require.*lib/verify"
    - from: "tests/unit/scaffold.test.js"
      to: "lib/scaffold.js"
      via: "require"
      pattern: "require.*lib/scaffold"
    - from: "tests/unit/commands.test.js"
      to: "lib/commands.js"
      via: "require"
      pattern: "require.*lib/commands"
---

<objective>
Write unit tests for the four mid-complexity lib/ modules: state.js (STATE.md operations), verify.js (verification suite), scaffold.js (template scaffolding), and commands.js (14 standalone utility commands).

Purpose: These modules are heavily file-dependent, requiring temp directory isolation for each test. State and commands are the two largest export surfaces (14 exports each). Covering them brings total unit-tested modules to 7/10.

Output: 4 passing test files with >= 53 test cases total, using temp directories for all mutating operations.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/state.js
@lib/verify.js
@lib/scaffold.js
@lib/commands.js
@.planning/phases/04-test-suite/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for state.js and verify.js</name>
  <files>tests/unit/state.test.js, tests/unit/verify.test.js</files>
  <action>
    **tests/unit/state.test.js** — Test STATE.md operations using temp fixture directories:

    Use createFixtureDir/cleanupFixtureDir from tests/helpers/fixtures.js for each test or beforeEach.
    Use captureOutput from tests/helpers/setup.js for cmd* functions.

    1. stateExtractField (internal helper, exported for testing):
       - Extracts "Active phase" field from STATE.md content
       - Extracts "Current plan" field
       - Returns null for non-existent field
       - Handles multi-line field values

    2. stateReplaceField (internal helper):
       - Replaces a field value in STATE.md content
       - Preserves other fields
       - Handles field not found gracefully

    3. cmdStateLoad:
       - Using captureOutput with fixture dir: returns JSON with config, state, roadmap keys
       - Returns valid JSON structure

    4. cmdStateGet:
       - With no section: returns full state content
       - With section name ("Key Decisions"): returns just that section
       - With raw=true: returns plain text

    5. cmdStatePatch:
       - Updates phase and plan fields in STATE.md
       - Writes changes to disk (verify by reading back)
       - Returns success JSON

    6. cmdStateAdvancePlan:
       - Increments plan counter (01 -> 02)
       - Updates STATE.md on disk

    7. cmdStateRecordMetric:
       - Adds row to Performance Metrics table
       - Preserves existing metrics

    8. cmdStateAddDecision / cmdStateAddBlocker / cmdStateResolveBlocker:
       - Adds decision row to Key Decisions table
       - Adds blocker text to Blockers section
       - Removes resolved blocker from Blockers section

    9. cmdStateUpdateProgress:
       - Recomputes progress bar from roadmap phase completion
       - Updates progress field in STATE.md

    10. cmdStateRecordSession:
        - Updates Session Continuity section with stopped_at and resume_file

    11. cmdStateSnapshot:
        - Returns structured JSON parse of entire STATE.md

    **tests/unit/verify.test.js** — Test verification suite:

    1. cmdVerifyPlanStructure:
       - Valid plan (from fixture): returns {valid: true}
       - Plan missing required sections: returns {valid: false, errors: [...]}

    2. cmdVerifyPhaseCompleteness:
       - Phase 1 (has summary): returns {complete: true} or similar
       - Phase 2 (missing summary): returns {complete: false}

    3. cmdVerifyReferences:
       - Plan with valid @-references: passes
       - Plan with broken @-references: reports missing files

    4. cmdVerifyArtifacts:
       - Plan with must_haves.artifacts where files exist: passes
       - Plan with must_haves.artifacts where files missing: reports missing

    5. cmdVerifyKeyLinks:
       - Plan with key_links where patterns match: passes
       - Plan with key_links where patterns don't match: reports failures

    6. cmdVerifySummary:
       - Valid summary file: passes validation
       - Missing required sections: reports errors

    7. cmdVerifyCommits:
       - Skip or mock: requires git repo. Either skip with test.skip() and document,
         or create a temp git repo in the test. If creating temp git repo: git init,
         commit a file, then verify the commit hash.
  </action>
  <verify>
    npm test -- tests/unit/state.test.js (all pass) (Level 1: Sanity)
    npm test -- tests/unit/verify.test.js (all pass) (Level 1: Sanity)
  </verify>
  <done>
    state.test.js has >= 18 passing test cases covering all 14 exported functions.
    verify.test.js has >= 12 passing test cases covering all 7 exported functions.
    All tests use isolated temp directories (no cross-test pollution).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for scaffold.js and commands.js</name>
  <files>tests/unit/scaffold.test.js, tests/unit/commands.test.js</files>
  <action>
    **tests/unit/scaffold.test.js** — Test template scaffolding operations:

    1. cmdTemplateSelect:
       - Returns available templates for a given type (context, verification, etc.)
       - Returns error for invalid template type

    2. cmdTemplateFill:
       - Fills summary template with phase/plan/name variables
       - Fills plan template with phase/plan/type/wave variables
       - Returns markdown string with variables replaced

    3. cmdScaffold:
       - scaffold context --phase 1: creates CONTEXT.md in phase directory
       - scaffold phase-dir --phase 3 --name "new-feature": creates phase directory
       - Verify created files exist on disk after scaffold
       - scaffold with invalid type: returns error

    **tests/unit/commands.test.js** — Test 14 standalone utility commands:

    All cmd* functions use captureOutput helper. Some need fixture dir.

    1. cmdGenerateSlug:
       - "Hello World" -> JSON with slug field
       - Empty input -> error

    2. cmdCurrentTimestamp:
       - "full" mode -> JSON with iso, date, time fields
       - "date" mode -> JSON with date field
       - "filename" mode -> JSON with filename-safe timestamp
       - raw mode -> plain text timestamp

    3. cmdListTodos:
       - With fixture todos dir: returns array of pending todos
       - With empty todos dir: returns empty array

    4. cmdTodoComplete:
       - Moves todo from pending/ to completed/ directory
       - Returns success JSON

    5. cmdVerifyPathExists:
       - Existing file: returns {exists: true}
       - Missing file: returns {exists: false}

    6. cmdConfigEnsureSection:
       - Returns config with all expected sections present
       - Creates missing sections with defaults

    7. cmdConfigSet:
       - Sets a config key (e.g., autonomous_mode -> true)
       - Persists to disk (read back to verify)

    8. cmdHistoryDigest:
       - With fixture summaries: returns digest with phase metrics
       - With no summaries: returns empty digest

    9. cmdResolveModel:
       - "grd-executor" -> returns model name for current profile
       - Invalid agent -> returns error or default

    10. cmdFindPhase:
        - Phase 1 -> returns phase directory path and metadata
        - Nonexistent phase -> returns error

    11. cmdCommit:
        - Skip or mock: requires git repo. If testing, create temp git repo,
          stage a file, then verify cmdCommit creates a commit.
          Alternatively, test error path (no files to commit).

    12. cmdPhasePlanIndex:
        - Phase 1 with fixture: returns plan index with wave and status info

    13. cmdSummaryExtract:
        - With fixture summary: returns extracted frontmatter fields
        - With --fields filter: returns only specified fields

    14. cmdProgressRender:
        - "json" format: returns progress JSON
        - "table" format: returns ASCII table (raw mode)
        - "bar" format: returns progress bar (raw mode)
  </action>
  <verify>
    npm test -- tests/unit/scaffold.test.js (all pass) (Level 1: Sanity)
    npm test -- tests/unit/commands.test.js (all pass) (Level 1: Sanity)
    npm test -- --coverage tests/unit/ (check cumulative coverage)
  </verify>
  <done>
    scaffold.test.js has >= 8 passing test cases covering all 3 exported functions.
    commands.test.js has >= 15 passing test cases covering all 14 exported functions.
    All mutating tests (todo complete, config set, scaffold) use isolated temp directories.
    Cumulative test count across all 7 unit test files: >= 90 test cases.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All 4 new test files discovered by Jest and pass
Level 1 (Sanity): No test pollution (each test uses isolated temp dirs)
Level 2 (Proxy): >= 53 new test cases across 4 files (bringing total to >= 90)
Level 2 (Proxy): Coverage for state.js, verify.js, scaffold.js, commands.js > 0%
</verification>

<success_criteria>
- tests/unit/state.test.js: >= 18 test cases, all pass
- tests/unit/verify.test.js: >= 12 test cases, all pass
- tests/unit/scaffold.test.js: >= 8 test cases, all pass
- tests/unit/commands.test.js: >= 15 test cases, all pass
- No test file depends on another test file's state
- All mutating operations use temp directories with cleanup
- `npm test -- tests/unit/` passes with 0 failures
- Cumulative test execution < 30 seconds for all 7 unit test files
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-suite/04-02-SUMMARY.md`
</output>
