---
phase: 52-autopilot-command
plan: 03
type: verify
wave: 2
depends_on: ["52-01", "52-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "ROADMAP.md Phase 52 plan list shows 3 plans (52-01, 52-02, 52-03) all checked as complete"
    - "All 8 success criteria verified: command exists, separate agents, orchestrator loop, file-based communication, lightweight orchestrator, module+skill+MCP, testbed validation, graceful failure handling"
    - "Full test suite passes with 1,960+ tests"
    - "lint and format checks pass"
    - "STATE.md updated with Phase 52 completion status"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated plan list and progress table for Phase 52"
    - path: ".planning/STATE.md"
      provides: "Updated current position and session continuity for Phase 53"
  key_links:
    - from: "lib/autopilot.js"
      to: ".planning/ROADMAP.md"
      reason: "Verification that all success criteria are met before marking phase complete"
---

<objective>
Final verification of Phase 52: validate all 8 success criteria, update ROADMAP.md plan list, update STATE.md, and run the full test suite to confirm no regressions. This is the gate before Phase 53 integration.
</objective>

<execution_context>
- **Plans 01-02** added integration and edge case tests for the autopilot module
- **Implementation**: lib/autopilot.js (431 LOC), commands/autopilot.md, bin/grd-tools.js routing, lib/mcp-server.js MCP tools -- all already exist
- **Testbed validation**: Plan 01 validated dry-run against 3 consecutive testbed phases
- **Coverage**: autopilot.js at 93%+ lines, 80%+ branches per jest.config.js thresholds
</execution_context>

## Task 1: Verify all 8 success criteria

Check each success criterion from ROADMAP.md Phase 52:

### SC-1: `/grd:autopilot [start]-[end]` command exists
- `commands/autopilot.md` exists with frontmatter and usage instructions
- `bin/grd-tools.js` routes `autopilot` command to `cmdAutopilot`
- CLI accepts `--from N --to N` flags

### SC-2: Each phase spawns TWO separate fresh agents (plan + execute)
- `buildPlanPrompt(N)` generates `/grd:plan-phase N` prompt
- `buildExecutePrompt(N)` generates `/grd:execute-phase N` prompt
- `spawnClaude()` calls `claude -p` with fresh process (no shared state)

### SC-3: Orchestrator loop: plan -> disk -> execute -> disk -> next
- `runAutopilot()` loop: `spawnClaude(planPrompt)` -> `isPhasePlanned()` check -> `spawnClaude(executePrompt)` -> `isPhaseExecuted()` check -> next phase

### SC-4: Agents communicate through files on disk
- Plan agent writes PLAN.md files; execute agent writes SUMMARY.md files
- Orchestrator only checks file existence (`isPhasePlanned`, `isPhaseExecuted`)

### SC-5: Orchestrator stays lightweight
- `runAutopilot()` never reads plan/summary contents -- only checks for file existence
- No context accumulation across phases

### SC-6: Module + skill + MCP tools
- `lib/autopilot.js` exports `cmdAutopilot`, `cmdInitAutopilot`, + 9 helper functions
- `commands/autopilot.md` skill with frontmatter
- `grd_autopilot_run` and `grd_autopilot_init` MCP tools in `lib/mcp-server.js`

### SC-7: Tested with at least 3 consecutive phases on testbed
- Plan 01 Tasks 1-3: manual testbed validation (dry-run, resume, init)
- Plan 01 Tasks 4-5: automated integration and unit tests with 3-phase fixtures

### SC-8: Graceful failure handling with resume
- `runAutopilot()` breaks on plan failure or execute failure, reports phase + step
- `--resume` flag skips already-planned/executed phases
- Status markers written to `.planning/autopilot/` for progress tracking

## Task 2: Update ROADMAP.md Phase 52 plan list

Update the Phase 52 section in ROADMAP.md to show 3 plans:

```markdown
**Plans**: 3 plans
Plans:
- [x] 52-01-PLAN.md -- Integration testing against testbed (3 consecutive phases) and CLI integration tests
- [x] 52-02-PLAN.md -- Edge case hardening: boundary conditions, unusual inputs, failure modes
- [x] 52-03-PLAN.md -- Final verification of all 8 success criteria and ROADMAP/STATE update
```

## Task 3: Update ROADMAP.md progress table

Update Phase 52 row in the progress table:

```markdown
| 52. Autopilot Command | v0.2.7 | 3/3 | Complete | 2026-02-22 |
```

Mark Phase 52 checkbox as complete in the milestone section:
```markdown
- [x] **Phase 52: Autopilot Command** - Plan and execute multiple phases autonomously with context isolation between each `implement`
```

## Task 4: Update STATE.md

Update STATE.md fields:
- **Active phase:** Phase 53 of 53 (Integration & Regression Testing)
- **Current plan:** 53-01 (not yet planned)
- **Status:** Phase 52 complete (3 plans, N new tests added), Phase 53 ready
- **Progress:** [########--] 80%
- **Next:** `/grd:plan-phase 53`
- **Session Continuity:** Phase 52 complete. Autopilot command fully implemented and tested with 3 consecutive testbed phases. Integration tests, edge case tests, and testbed validation all pass.

## Task 5: Run full test suite and lint

```bash
npm test
npm run lint
npm run format:check
```

Verify:
- All tests pass (target: 1,960+ tests)
- Zero lint errors
- Zero format issues
- autopilot.js meets jest.config.js thresholds (93% lines, 97% functions, 80% branches)

## Task 6: Run consistency validation

```bash
node bin/grd-tools.js validate consistency
```

Verify no errors in phase numbering or disk/roadmap sync.
