---
phase: 52-autopilot-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/cli.test.js
  - tests/unit/autopilot.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "Autopilot dry-run against testbed validates 3 consecutive phases (1-3) with correct prompts and phase ordering"
    - "Autopilot --resume against testbed correctly skips Phase 1 (which has an existing plan) and processes Phase 2-3"
    - "Autopilot init against testbed returns accurate phase_range, incomplete_phases, and phase disk_status"
    - "Integration tests in cli.test.js cover autopilot subcommand and init autopilot via real CLI invocation"
    - "All existing 1,951 tests pass with zero regressions"
  artifacts:
    - path: "tests/integration/cli.test.js"
      provides: "Integration tests for autopilot CLI subcommand (dry-run, resume, init) using real grd-tools.js invocation"
    - path: "tests/unit/autopilot.test.js"
      provides: "Additional unit tests for testbed-like project structures with milestone-scoped paths"
  key_links:
    - from: "lib/autopilot.js"
      to: "tests/integration/cli.test.js"
      reason: "Success criterion 7 requires testing with 3+ consecutive phases on a real project structure"
    - from: "lib/autopilot.js"
      to: "tests/unit/autopilot.test.js"
      reason: "Existing tests use flat .planning/phases/; need milestone-scoped .planning/milestones/v1.0/phases/ to match testbed"
---

<objective>
Validate the autopilot command against the testbed project structure (3 consecutive phases) and add integration tests that verify the full CLI flow. This directly addresses success criterion 7: "Tested with at least 3 consecutive phases on the testbed, verifying plan and execute agents each get clean context windows."
</objective>

<execution_context>
- **lib/autopilot.js** already exists with 431 LOC, 11 exported functions, 96% line coverage, 43 passing tests
- **Testbed** at `testbed/` has 3 phases (1-3) in ROADMAP.md, Phase 1 has an existing plan (01-01-PLAN.md)
- **Success criterion 7** requires testing with at least 3 consecutive phases. The `--dry-run` mode validates the orchestration loop without spawning real claude processes
- **Coverage gap**: Lines 310-318 (execute step verification failure when no SUMMARY.md after spawn succeeds) are uncovered -- add test for milestone-scoped paths
- **Integration test pattern**: `tests/integration/cli.test.js` uses `execFileSync('node', [GRD_TOOLS, ...args], { cwd })` against temp fixtures
- The autopilot `resolvePhaseRange()` calls `analyzeRoadmap(cwd)` which finds phases from ROADMAP.md. The testbed has phases 1-3 with milestone-scoped directories under `.planning/milestones/v1.0.0/phases/`
- **Baseline**: 1,951 tests passing
</execution_context>

## Task 1: Validate autopilot dry-run against testbed (manual verification)

Run autopilot dry-run from testbed directory to verify 3 consecutive phases are processed:

```bash
cd testbed && node ../bin/grd-tools.js autopilot --dry-run --from 1 --to 3
```

Expected output: 6 results (plan + execute for each of phases 1, 2, 3) with status `dry-run` and correct prompts.

Verify:
1. All 3 phases appear in results
2. Each phase has both a `plan` and `execute` step
3. Prompts contain correct phase numbers (`/grd:plan-phase 1`, `/grd:execute-phase 1`, etc.)
4. `phases_completed` is 3, `stopped_at` is null

## Task 2: Validate autopilot resume against testbed (manual verification)

Run autopilot with `--resume --dry-run` against testbed:

```bash
cd testbed && node ../bin/grd-tools.js autopilot --dry-run --resume --from 1 --to 3
```

Expected: Phase 1 plan step shows `skipped` (already planned), Phase 1 execute shows `dry-run`, Phases 2-3 show `dry-run` for both steps.

## Task 3: Validate autopilot init against testbed (manual verification)

```bash
cd testbed && node ../bin/grd-tools.js init autopilot
```

Expected: `total_phases: 3`, `incomplete_phases: 3`, `phase_range.first: "1"`, `phase_range.last: "3"`, `phase_range.first_incomplete: "1"`, `phases[0].disk_status: "planned"`.

## Task 4: Add integration tests for autopilot CLI to tests/integration/cli.test.js

Add a new `describe('autopilot')` section in the integration test file:

1. **autopilot --dry-run outputs valid JSON for 3 phases**: Create temp fixture with 3-phase ROADMAP.md. Run `node grd-tools.js autopilot --dry-run --from 1 --to 3`. Parse JSON, verify `phases_attempted: 3`, `phases_completed: 3`, 6 results.

2. **autopilot --dry-run --skip-plan outputs only execute steps**: Same fixture, add `--skip-plan`. Verify no `plan` steps in results, only `execute` steps.

3. **autopilot --dry-run --resume skips planned phases**: Create fixture with Phase 1 having a PLAN.md file. Run with `--resume`. Verify Phase 1 plan shows `skipped`.

4. **init autopilot returns phase context**: Run `node grd-tools.js init autopilot`. Verify output has `total_phases`, `phase_range`, `phases` array.

The integration test fixture needs:
- `.planning/STATE.md` with milestone marker
- `.planning/config.json`
- `.planning/ROADMAP.md` with 3 phases in milestone heading format
- `.planning/milestones/v1.0/phases/01-first/` directory (optionally with a plan file for resume tests)

## Task 5: Add unit tests for milestone-scoped phase paths in autopilot

Add tests to `tests/unit/autopilot.test.js` that use milestone-scoped directory structures (matching the testbed layout):

1. **isPhasePlanned with milestone-scoped path**: Create fixture with `.planning/milestones/v1.0/phases/01-test/01-01-PLAN.md`. Verify `isPhasePlanned(dir, '1')` returns true via the existing `findPhaseInternal` lookup.

2. **isPhaseExecuted with milestone-scoped path**: Create fixture with plan + summary in milestone-scoped path. Verify returns true.

3. **runAutopilot stops when execute verification fails (no summary)**: Create fixture where spawnClaude succeeds (exit 0) for both plan and execute, but plan creates PLAN.md while execute does NOT create SUMMARY.md. Verify `stopped_at` contains "execute verification failed". This covers the uncovered lines 310-318.

4. **runAutopilot with multiple phases completes all**: Create 3-phase fixture. Mock spawnSync to create plan and summary files for each phase on the appropriate call. Verify `phases_completed: 3`, all 6 results show `completed`.

## Task 6: Run full test suite and verify

```bash
npm test
```

Verify:
- All tests pass (target: 1,960+ tests)
- autopilot.js coverage remains at 93%+ lines (per jest.config.js threshold)
- No lint errors
