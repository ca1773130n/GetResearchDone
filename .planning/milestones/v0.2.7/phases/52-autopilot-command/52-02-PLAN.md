---
phase: 52-autopilot-command
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/autopilot.test.js
  - lib/autopilot.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "Edge case: runAutopilot with both --skip-plan and --skip-execute produces zero results but still counts phases completed"
    - "Edge case: spawnClaude with null status (process killed) returns exit code 1"
    - "Edge case: updateStateProgress regex does not match non-standard STATE.md format (no-op)"
    - "Edge case: cmdAutopilot --max-turns flag correctly parses and passes to spawnClaude"
    - "Edge case: resolvePhaseRange with decimal phase numbers (e.g., 12.1) handles filtering correctly"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "tests/unit/autopilot.test.js"
      provides: "Edge case and boundary condition tests for autopilot functions"
  key_links:
    - from: "lib/autopilot.js"
      to: "tests/unit/autopilot.test.js"
      reason: "Hardening exported functions against unusual inputs and failure modes"
---

<objective>
Harden the autopilot module by adding edge case tests for boundary conditions, unusual inputs, and failure modes. These tests close the remaining 4% coverage gap and ensure robust behavior in production scenarios beyond the happy path.
</objective>

<execution_context>
- **Current coverage**: 96% lines, 84% branches, 100% functions (43 tests)
- **Uncovered lines**: 40 (empty phases array from analyzeRoadmap), 310-318 (execute verification failure -- covered in Plan 01 Task 5.3)
- **Branch gaps**: Several `||` fallback branches in flag parsing (`cmdAutopilot`) and optional parameter defaults
- **Target**: Maintain 93%+ lines threshold from jest.config.js, improve branch coverage
- **Baseline**: 1,951+ tests (may be higher after Plan 01)
</execution_context>

## Task 1: Edge case tests for resolvePhaseRange

Add to `tests/unit/autopilot.test.js` `describe('resolvePhaseRange')`:

1. **Empty phases array**: Create fixture with ROADMAP.md that has no Phase headings (just `# Roadmap\n## v1.0\n`). Verify `error` contains "No phases found" and `phases` is empty. This covers line 40.

2. **Decimal phase numbers**: Create fixture with phases numbered `12`, `12.1`, `13`. Test filtering with `from: '12.1'` returns 2 phases (12.1, 13). Test `to: '12'` returns 1 phase (12).

3. **Single phase range (from === to with decimal)**: Filter `from: '12.1', to: '12.1'`. Verify exactly 1 phase returned.

## Task 2: Edge case tests for spawnClaude

Add to `describe('spawnClaude')`:

1. **Null status with non-timeout error**: Mock spawnSync to return `{ status: null, error: new Error('SIGKILL') }`. Verify `exitCode: 1`, `timedOut: false`.

2. **Custom timeout is passed through**: Mock spawnSync, call with `timeout: 5000`. Verify `spawnSync` was called with timeout `5000` in the options object.

3. **No maxTurns or model (default args)**: Mock spawnSync, call with empty opts. Verify args are exactly `['-p', prompt, '--verbose']` -- no extra flags.

## Task 3: Edge case tests for cmdAutopilot flag parsing

Add to `describe('cmdAutopilot')`:

1. **--max-turns flag parsing**: Call with `['--dry-run', '--max-turns', '100', '--from', '48', '--to', '48']`. Verify result is valid (dry-run processes correctly).

2. **--model flag parsing**: Call with `['--dry-run', '--model', 'opus', '--from', '48', '--to', '48']`. Verify result processes correctly.

3. **--skip-execute flag**: Call with `['--dry-run', '--skip-execute', '--from', '48', '--to', '48']`. Verify only plan steps in results.

4. **No flags at all (defaults)**: Call with `['--dry-run']`. Verify all phases processed with both plan and execute steps.

## Task 4: Edge case tests for updateStateProgress

Add to `describe('updateStateProgress')`:

1. **STATE.md without Current Phase field**: Create STATE.md with content `# State\n\nSome text\n`. Call `updateStateProgress`. Verify content unchanged (regex doesn't match, no-op).

2. **Multiple updates overwrite previous**: Call `updateStateProgress` twice with different phase numbers. Verify final content has only the second update.

## Task 5: Edge case tests for runAutopilot

Add to `describe('runAutopilot')`:

1. **Both skipPlan and skipExecute**: Run with `{ dryRun: true, skipPlan: true, skipExecute: true }`. Verify `phases_completed: 3` but `results` is empty (no steps to perform).

2. **Custom timeout passed to spawnClaude**: Run with `{ timeout: 60 }`. Mock spawnSync. Verify spawnSync called with timeout of `60 * 60 * 1000 = 3600000`.

3. **Model override passed through**: Run with `{ model: 'haiku' }`. Mock spawnSync to create plan files. Verify spawnSync called with `--model haiku`.

## Task 6: Test writeStatusMarker with different phases and steps

Add to `describe('writeStatusMarker')`:

1. **Different step names**: Write markers for `'plan'`, `'execute'`, `'review'`. Verify separate files created: `phase-48-plan.json`, `phase-48-execute.json`, `phase-48-review.json`.

2. **Different phase numbers**: Write markers for phases `'1'`, `'12.1'`, `'99'`. Verify correct filenames.

## Task 7: Run tests and verify coverage

```bash
npx jest tests/unit/autopilot.test.js --coverage
```

Verify:
- All tests pass
- Line coverage >= 93% (jest.config.js threshold)
- Branch coverage improved from 84%
- No regressions
