---
phase: 48-dogfooding-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testbed/.planning/PROJECT.md
  - testbed/.planning/ROADMAP.md
  - testbed/.planning/STATE.md
  - testbed/.planning/config.json
  - testbed/.planning/REQUIREMENTS.md
  - testbed/.planning/LONG-TERM-ROADMAP.md
  - testbed/.planning/milestones/anonymous/phases/.gitkeep
  - testbed/.planning/milestones/anonymous/research/.gitkeep
  - testbed/.planning/milestones/anonymous/todos/pending/.gitkeep
  - testbed/.planning/milestones/anonymous/todos/completed/.gitkeep
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "testbed/.planning/ directory exists with PROJECT.md, ROADMAP.md, STATE.md, config.json"
    - "testbed/.planning/config.json is valid JSON with expected schema fields"
    - "testbed/.planning/ROADMAP.md has at least one milestone and one phase"
    - "testbed/.planning/STATE.md reflects the initialized project state"
    - "testbed/.planning/milestones/ directory structure matches GRD conventions"
  artifacts:
    - path: "testbed/.planning/PROJECT.md"
      provides: "Product vision for the testbed Flutter monorepo"
    - path: "testbed/.planning/ROADMAP.md"
      provides: "Phase structure for testbed development"
    - path: "testbed/.planning/STATE.md"
      provides: "Living memory for testbed project"
    - path: "testbed/.planning/config.json"
      provides: "GRD configuration for testbed"
    - path: "testbed/.planning/REQUIREMENTS.md"
      provides: "Requirements for testbed milestone"
  key_links:
    - from: "testbed/.planning/STATE.md"
      to: "testbed/.planning/ROADMAP.md"
      via: "milestone reference"
      pattern: "Active phase.*Phase"
---

<objective>
Initialize the testbed (multi-bootstrap Flutter monorepo copy) as a fully-configured GRD project.

Purpose: The testbed must look exactly like a real user's GRD project so that subsequent phases can run full GRD workflows against it and expose bugs in GRD's own source code (bin/, lib/, commands/, agents/).
Output: Complete .planning/ directory in testbed/ with PROJECT.md, ROADMAP.md, STATE.md, config.json, REQUIREMENTS.md, and milestone directory structure.
</objective>

<execution_context>
@.planning/milestones/v0.2.7/phases/48-dogfooding-infrastructure/48-01-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@testbed/README.md
@testbed/pubspec.yaml
@templates/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create testbed .planning/ directory structure and config</name>
  <files>testbed/.planning/config.json, testbed/.planning/milestones/anonymous/phases/.gitkeep, testbed/.planning/milestones/anonymous/research/.gitkeep, testbed/.planning/milestones/anonymous/todos/pending/.gitkeep, testbed/.planning/milestones/anonymous/todos/completed/.gitkeep</files>
  <action>
    Create the full .planning/ directory skeleton for the testbed:

    1. Create directory structure:
       - testbed/.planning/
       - testbed/.planning/milestones/anonymous/phases/
       - testbed/.planning/milestones/anonymous/research/
       - testbed/.planning/milestones/anonymous/research/deep-dives/
       - testbed/.planning/milestones/anonymous/todos/pending/
       - testbed/.planning/milestones/anonymous/todos/completed/
       - testbed/.planning/milestones/anonymous/quick/
       - testbed/.planning/milestones/anonymous/codebase/

    2. Create config.json based on templates/config.json but with settings appropriate for a Flutter monorepo:
       - model_profile: "balanced"
       - commit_docs: true
       - autonomous_mode: false (default user experience)
       - research_gates: all false (keep it simple for testing)
       - confirmation_gates: all false
       - eval_config with flutter-relevant metrics (test_pass_rate, build_success, lint_clean)

    3. Place .gitkeep files in empty directories to preserve structure.

    IMPORTANT: Do NOT use `node bin/grd-tools.js` for this task -- manually create files to avoid triggering the currentMilestone() bug (REQ-112). This is intentional: we need a clean testbed first, then Phase 49 will fix and validate the bug.
  </action>
  <verify>
    ls -la testbed/.planning/ confirms directory exists
    cat testbed/.planning/config.json | node -e "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('Valid JSON')" confirms valid JSON
    (Level 1: Sanity)
  </verify>
  <done>testbed/.planning/ directory tree exists with config.json containing valid GRD configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create testbed PROJECT.md, ROADMAP.md, STATE.md, and REQUIREMENTS.md</name>
  <files>testbed/.planning/PROJECT.md, testbed/.planning/ROADMAP.md, testbed/.planning/STATE.md, testbed/.planning/REQUIREMENTS.md, testbed/.planning/LONG-TERM-ROADMAP.md</files>
  <action>
    Create the core planning documents for the testbed project, treating it as a real Flutter monorepo that a user would manage with GRD:

    1. **PROJECT.md**: Write a realistic product vision for the multi-bootstrap Flutter monorepo:
       - Project name: "Multi-Bootstrap" (from the testbed's actual name)
       - Core value: Cross-platform Flutter app with modular package architecture
       - Research objectives: None (this is an engineering project, not R&D)
       - Quality targets: Build success, test pass rate, lint clean

    2. **ROADMAP.md**: Create a realistic roadmap with:
       - One milestone: v1.0.0 "Initial Release"
       - 3 phases:
         - Phase 1: Project Setup & CI (implement)
         - Phase 2: Core Feature Development (implement)
         - Phase 3: Testing & Polish (integrate)
       - Each phase with Goal, Type, Requirements, Verification Level, and Success Criteria
       - Progress table at bottom

    3. **STATE.md**: Initialize with:
       - Current position: Phase 1 of 3
       - Status: Ready to plan Phase 1
       - Milestone: v1.0.0 Initial Release
       - No blockers, no deferred validations

    4. **REQUIREMENTS.md**: Create 3-4 realistic requirements:
       - REQ-001: Build system configuration (P0)
       - REQ-002: Core app architecture (P0)
       - REQ-003: Test infrastructure (P1)
       - REQ-004: CI/CD pipeline (P2)
       - Include traceability matrix

    5. **LONG-TERM-ROADMAP.md**: Create a minimal LT roadmap:
       - LT-1: v1.0.0 Initial Release (linked)

    All documents must follow GRD naming conventions (UPPERCASE .md files in .planning/).
    All documents must be realistic enough that GRD commands can parse them correctly.
    ROADMAP.md must follow the exact format that lib/roadmap.js expects (### Phase N: Name, **Goal**:, **Type**:, etc.)
  </action>
  <verify>
    grep "## Current Position" testbed/.planning/STATE.md confirms STATE.md has expected structure
    grep "### Phase 1:" testbed/.planning/ROADMAP.md confirms ROADMAP.md has parseable phase
    grep "REQ-001" testbed/.planning/REQUIREMENTS.md confirms requirements exist
    (Level 1: Sanity)
  </verify>
  <done>All four core planning documents exist in testbed/.planning/ with realistic content that GRD commands can parse</done>
</task>

<task type="auto">
  <name>Task 3: Create milestone directory structure for testbed</name>
  <files>testbed/.planning/milestones/v1.0.0/phases/.gitkeep, testbed/.planning/milestones/v1.0.0/research/.gitkeep, testbed/.planning/milestones/v1.0.0/todos/pending/.gitkeep, testbed/.planning/milestones/v1.0.0/todos/completed/.gitkeep</files>
  <action>
    Create the v1.0.0 milestone directory tree under testbed/.planning/milestones/:

    1. Create directories:
       - testbed/.planning/milestones/v1.0.0/phases/
       - testbed/.planning/milestones/v1.0.0/research/
       - testbed/.planning/milestones/v1.0.0/research/deep-dives/
       - testbed/.planning/milestones/v1.0.0/todos/pending/
       - testbed/.planning/milestones/v1.0.0/todos/completed/
       - testbed/.planning/milestones/v1.0.0/quick/
       - testbed/.planning/milestones/v1.0.0/codebase/

    2. Place .gitkeep files in leaf directories.

    3. Verify the structure matches what lib/paths.js expects when resolving milestone-scoped paths.
       The key functions in paths.js are:
       - phasesDir(cwd, milestone) -> .planning/milestones/{milestone}/phases
       - researchDir(cwd, milestone) -> .planning/milestones/{milestone}/research
       - todosDir(cwd, milestone) -> .planning/milestones/{milestone}/todos
       - quickDir(cwd, milestone) -> .planning/milestones/{milestone}/quick
  </action>
  <verify>
    ls testbed/.planning/milestones/v1.0.0/phases/ confirms phases directory exists
    ls testbed/.planning/milestones/v1.0.0/research/ confirms research directory exists
    ls testbed/.planning/milestones/v1.0.0/todos/pending/ confirms todos structure exists
    (Level 1: Sanity)
  </verify>
  <done>testbed/.planning/milestones/v1.0.0/ directory tree fully matches GRD path conventions</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- testbed/.planning/ exists with all expected subdirectories
- config.json is valid JSON
- PROJECT.md, ROADMAP.md, STATE.md, REQUIREMENTS.md all exist and are non-empty
- ROADMAP.md contains parseable phase definitions (### Phase N: pattern)
- Milestone directory v1.0.0 exists with phases/, research/, todos/ subdirectories

Level 2 (Proxy): Not applicable for this plan
Level 3 (Deferred): Full GRD workflow validation against testbed (Phase 48 Plan 03)
</verification>

<success_criteria>
- testbed/.planning/ directory fully initialized with all core documents
- config.json contains valid GRD configuration
- ROADMAP.md has parseable phases that lib/roadmap.js can extract
- STATE.md reflects initialized project state
- Milestone directory structure matches GRD path resolution conventions
- All files use UPPERCASE naming convention for .planning/ markdown files
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.7/phases/48-dogfooding-infrastructure/48-01-SUMMARY.md`
</output>
