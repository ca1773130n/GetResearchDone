---
phase: 51-test-coverage-and-feature-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/phase.test.js
  - tests/unit/parallel.test.js
  - tests/unit/worktree.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "phase.js line coverage rises from 84% to 85%+ by testing decimal phase removal, version bump, and validation edge cases"
    - "parallel.js line coverage rises from 84% to 85%+ by testing buildParallelContext error paths"
    - "worktree.js line coverage rises from 85% to 85%+ by testing push/PR, merge, and hook edge cases"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "tests/unit/phase.test.js"
      provides: "Tests for decimal phase removal renumbering, cmdVersionBump, cmdValidateConsistency edge cases"
    - path: "tests/unit/parallel.test.js"
      provides: "Tests for buildParallelContext error branches (missing roadmap, missing phases, bad deps)"
    - path: "tests/unit/worktree.test.js"
      provides: "Tests for worktree push/PR, merge, and hook create/remove edge cases"
  key_links:
    - from: "lib/phase.js"
      to: "tests/unit/phase.test.js"
      reason: "Decimal removal renumbering and version bump are untested code paths"
    - from: "lib/parallel.js"
      to: "tests/unit/parallel.test.js"
      reason: "Error branches in buildParallelContext need coverage for robustness"
    - from: "lib/worktree.js"
      to: "tests/unit/worktree.test.js"
      reason: "Push/PR and merge paths are the most critical untested workflows"
---

<objective>
Raise phase.js, parallel.js, and worktree.js each to 85%+ line coverage by testing uncovered branches: decimal phase renumbering, version bump, parallel context error paths, and worktree push/PR/merge operations.
</objective>

<execution_context>
- **phase.js** (84% lines, 1227 LOC): Uncovered code includes decimal phase removal renumbering (lines 350-394 — the complex logic that renames sibling decimals when one is removed), cmdVersionBump (lines 1166-1213 — bumps version in VERSION, package.json, plugin.json), and cmdValidateConsistency edge cases (plan gap warnings at 1107-1108, orphan summary at 1122). Also uncovered: cmdPhaseComplete force flag (879), cmdPhaseInsert edge cases.
- **parallel.js** (84% lines, 231 LOC): Uncovered error paths in buildParallelContext (lines 159-219): missing ROADMAP.md, missing phase directories, dependency validation failure, missing config. These are defensive branches that fire when the project structure is incomplete.
- **worktree.js** (85% lines, 827 LOC): Just below threshold at 84.73%. Uncovered paths include: cmdWorktreePushAndPR (lines 474-532 — git push + gh pr create flow), cmdWorktreeMerge (lines 562-610 — merge worktree back to base), hook edge cases (lines 639-788 — hook create/remove when files already exist or are missing).
- **Test approach**: Mock fs, execFileSync, and child_process for git/gh operations. Test through public API entry points.
- **Baseline**: 1,853+ tests passing (after Plan 51-01 adds tracker/scaffold tests).
</execution_context>

## Task 1: Test phase.js — decimal phase removal renumbering

Add to `tests/unit/phase.test.js`:

1. **cmdPhaseRemove decimal phase — renumbers siblings**: Create temp dir with `06.1-feature-a/`, `06.2-feature-b/`, `06.3-feature-c/` containing plan files with phase prefix in filenames. Remove phase `06.2`. Verify:
   - `06.2-feature-b/` is deleted
   - `06.3-feature-c/` is renamed to `06.2-feature-c/`
   - Files inside renamed dir have updated prefixes (e.g., `06.3-01-PLAN.md` -> `06.2-01-PLAN.md`)
2. **cmdPhaseRemove decimal — last decimal**: Remove the highest-numbered decimal. Verify no renaming occurs (nothing above it).
3. **cmdPhaseRemove decimal — with --force on executed phase**: Verify summaries are removed when force flag is used.

## Task 2: Test phase.js — cmdVersionBump

Add to `tests/unit/phase.test.js`:

1. **cmdVersionBump — updates all 3 files**: Create temp dir with VERSION, package.json, .claude-plugin/plugin.json. Call `cmdVersionBump(cwd, 'v1.2.3', false)`. Verify:
   - VERSION contains `1.2.3\n` (no `v` prefix)
   - package.json `version` field is `1.2.3`
   - plugin.json `version` field is `1.2.3`
2. **cmdVersionBump — missing files**: Only VERSION exists. Verify it updates VERSION and reports `files_updated: ['VERSION']`.
3. **cmdVersionBump — no version arg**: Verify error "version required".
4. **cmdVersionBump — strips v prefix**: Pass `v2.0.0`, verify files contain `2.0.0`.

## Task 3: Test phase.js — cmdValidateConsistency edge cases

Add to `tests/unit/phase.test.js`:

1. **plan gap warning**: Create phase dir with `48-01-PLAN.md` and `48-03-PLAN.md` (no 02). Verify warnings include "Gap in plan numbering".
2. **orphan summary warning**: Create `48-02-SUMMARY.md` with no matching `48-02-PLAN.md`. Verify warnings include "has no matching PLAN.md".
3. **missing wave frontmatter**: Create plan file with frontmatter missing `wave` field. Verify warnings include "missing 'wave' in frontmatter".

## Task 4: Test phase.js — cmdPhaseComplete edge cases

Add to `tests/unit/phase.test.js`:

1. **cmdPhaseComplete — all plans complete**: Create phase with plan+summary pairs, verify completion succeeds and ROADMAP.md checkbox gets checked.
2. **cmdPhaseComplete — incomplete plans**: Create phase with plan but no summary. Verify error about incomplete plans (unless --force).

## Task 5: Test parallel.js — buildParallelContext error paths

Add to `tests/unit/parallel.test.js`:

1. **buildParallelContext — no ROADMAP.md**: Mock safeReadFile to return null for ROADMAP.md. Verify error output about missing roadmap.
2. **buildParallelContext — no phases directory**: Mock readdirSync to throw ENOENT. Verify graceful error.
3. **buildParallelContext — dependency validation failure**: Create phases where declared dependencies don't match available phases. Verify validation error reported.
4. **buildParallelContext — missing config.json**: Mock safeReadFile to return null for config. Verify uses defaults.

## Task 6: Test worktree.js — cmdWorktreePushAndPR

Add to `tests/unit/worktree.test.js`:

1. **cmdWorktreePushAndPR — successful push and PR creation**: Mock execFileSync for `git push` and `gh pr create`. Verify output includes PR URL.
2. **cmdWorktreePushAndPR — push failure**: Mock git push to throw. Verify error output.
3. **cmdWorktreePushAndPR — gh pr create failure**: Mock push success but PR creation failure. Verify error about PR creation.
4. **cmdWorktreePushAndPR — no worktree exists**: Verify error when worktree path doesn't exist.

## Task 7: Test worktree.js — cmdWorktreeMerge and hooks

Add to `tests/unit/worktree.test.js`:

1. **cmdWorktreeMerge — successful merge**: Mock git checkout + merge + branch delete. Verify success output.
2. **cmdWorktreeMerge — merge conflict**: Mock git merge to throw. Verify error about conflict.
3. **cmdWorktreeHookCreate — hook already exists**: Verify skip or overwrite behavior.
4. **cmdWorktreeHookRemove — hook does not exist**: Verify graceful handling (no error).

## Task 8: Verify coverage improvement

Run `npx jest --coverage` and verify:
- phase.js >= 85% line coverage
- parallel.js >= 85% line coverage
- worktree.js >= 85% line coverage
- All tests pass with zero regressions
