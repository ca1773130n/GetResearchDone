---
phase: 51-test-coverage-and-feature-discovery
plan: 04
type: execute
wave: 3
depends_on: ["51-01", "51-02", "51-03"]
files_modified:
  - jest.config.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "All 19 lib/ modules are at 85%+ line coverage in jest coverage report"
    - "jest.config.js per-file thresholds updated to enforce 85% line coverage floor for all modules"
    - "New features (coverage-report, health-check) work correctly via CLI and produce valid JSON"
    - "Full test suite passes (target: 1,900+ tests) with zero regressions"
    - "npm run lint exits 0, npm run format:check exits 0"
  artifacts:
    - path: "jest.config.js"
      provides: "Updated per-file coverage thresholds enforcing 85% line coverage for all 19 lib/ modules"
  key_links:
    - from: "jest.config.js"
      to: "lib/"
      reason: "Thresholds enforce the 85% floor achieved by Plans 01-02"
---

<objective>
Update jest.config.js per-file coverage thresholds to enforce the 85% line coverage floor for all 19 lib/ modules, run final verification to confirm all success criteria are met, and ensure both new features work correctly end-to-end.
</objective>

<execution_context>
- After Plans 01-02, all lib/ modules should be at 85%+ line coverage.
- After Plan 03, 2 new features (coverage-report, health-check) are implemented.
- Current jest.config.js has per-file thresholds for 14 of 19 modules. Missing: cleanup.js, deps.js, gates.js, long-term-roadmap.js, utils.js (though some were already above 85%).
- This plan adds thresholds for ALL modules and raises existing thresholds to the new 85% floor.
- This is the final verification plan — confirms all Phase 51 success criteria.
</execution_context>

## Task 1: Capture actual coverage numbers

Run `npx jest --coverage` and record the actual line coverage for all 19 modules. These become the new threshold floor values (rounded down to nearest integer for safety margin).

## Task 2: Update jest.config.js thresholds

Update `jest.config.js` `coverageThreshold` to include ALL 19 lib/ modules with at minimum 85% line coverage thresholds. For modules already well above 85%, set the threshold to the current value minus a small margin (2-3%) to allow minor fluctuations while still preventing regressions.

The updated config should look like:

```js
coverageThreshold: {
  './lib/autopilot.js': { lines: 93, functions: 95, branches: 80 },
  './lib/backend.js': { lines: 95, functions: 100, branches: 88 },
  './lib/cleanup.js': { lines: 92, functions: 96, branches: 80 },
  './lib/commands.js': { lines: 88, functions: 95, branches: 70 },
  './lib/context.js': { lines: 87, functions: 83, branches: 77 },
  './lib/deps.js': { lines: 94, functions: 100, branches: 87 },
  './lib/frontmatter.js': { lines: 89, functions: 100, branches: 78 },
  './lib/gates.js': { lines: 98, functions: 100, branches: 82 },
  './lib/long-term-roadmap.js': { lines: 97, functions: 100, branches: 83 },
  './lib/mcp-server.js': { lines: 87, functions: 87, branches: 56 },
  './lib/parallel.js': { lines: 85, functions: 100, branches: 78 },
  './lib/paths.js': { lines: 95, functions: 100, branches: 95 },
  './lib/phase.js': { lines: 85, functions: 88, branches: 65 },
  './lib/roadmap.js': { lines: 91, functions: 94, branches: 81 },
  './lib/scaffold.js': { lines: 85, functions: 100, branches: 60 },
  './lib/state.js': { lines: 85, functions: 88, branches: 77 },
  './lib/tracker.js': { lines: 85, functions: 85, branches: 70 },
  './lib/utils.js': { lines: 91, functions: 95, branches: 85 },
  './lib/verify.js': { lines: 85, functions: 100, branches: 70 },
  './lib/worktree.js': { lines: 85, functions: 100, branches: 73 },
},
```

Note: Exact numbers will be adjusted based on actual coverage results from Task 1. The above are targets based on current coverage + expected improvements from Plans 01-02.

## Task 3: Run full verification battery

Execute the following checks:

1. `npx jest --coverage` — all 1,900+ tests pass, all per-file thresholds met
2. `npm run lint` — exits 0
3. `npm run format:check` — exits 0
4. `node bin/grd-tools.js validate consistency` — passed: true
5. `node bin/grd-tools.js coverage-report --threshold 85` — all_above: true
6. `node bin/grd-tools.js health-check` — healthy: true

## Task 4: Verify success criteria

Confirm all Phase 51 success criteria:

| # | Criterion | Method | Expected |
|---|-----------|--------|----------|
| 1 | All 19 lib/ modules at 85%+ line coverage | `npm test` coverage table | All >= 85% |
| 2 | At least 2 new features implemented | Feature inventory | coverage-report + health-check |
| 3 | Each new feature has 3+ tests | Test count in commands.test.js | >= 3 per feature |
| 4 | jest.config.js thresholds updated | Config inspection | All 19 modules listed with 85%+ lines |

## Task 5: Record final metrics

Document in 51-04-SUMMARY.md:
- Total tests (expected: 1,900+)
- Coverage table (all 19 modules with line %)
- New features list with test counts
- Any deferred items
