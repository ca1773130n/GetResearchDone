---
phase: 51-test-coverage-and-feature-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/tracker.test.js
  - tests/unit/scaffold.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "tracker.js line coverage rises from 43% to 85%+ by testing all 12 handler functions through cmdTracker dispatcher"
    - "scaffold.js line coverage rises from 82% to 85%+ by testing template-fill error paths and scaffold type branches"
    - "All new tests use mocked fs/execFileSync -- no real file system or GitHub CLI calls"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "tests/unit/tracker.test.js"
      provides: "Comprehensive tests for all 12 tracker handler functions via cmdTracker entry point"
    - path: "tests/unit/scaffold.test.js"
      provides: "Tests for uncovered scaffold branches (template-fill errors, scaffold types)"
  key_links:
    - from: "lib/tracker.js"
      to: "tests/unit/tracker.test.js"
      reason: "Coverage gap is 42 percentage points -- largest in the codebase"
    - from: "lib/scaffold.js"
      to: "tests/unit/scaffold.test.js"
      reason: "Coverage gap is 3 percentage points with concentrated uncovered branches"
---

<objective>
Raise tracker.js from 43% to 85%+ line coverage and scaffold.js from 82% to 85%+ line coverage by adding targeted tests for uncovered handler functions and branch paths. These are the two worst offenders -- tracker.js alone accounts for the largest coverage gap in the project.
</objective>

<execution_context>
- **tracker.js** (43% lines, 1034 LOC): After Phase 50 decomposition, the 12 handler functions are cleanly separated but untested. The existing tests cover loadTrackerConfig, loadTrackerMapping, saveTrackerMapping, createTracker factory, and cmdTracker dispatch routing. The uncovered code is primarily: handleSyncRoadmap, handleSyncPhase, handleUpdateStatus, handleAddComment, handleSyncStatus, handlePrepareRoadmapSync, handlePreparePhaseSync, handleRecordMapping, handleRecordStatus, handleSchedule, handlePrepareReschedule, plus the createGitHubTracker method implementations (createPhaseIssue, createTaskIssue, updateIssueStatus, addComment, syncRoadmap, syncPhase).
- **scaffold.js** (82% lines, 374 LOC): Uncovered branches include: cmdTemplateFill error when no template type given (line 98), error when no --phase given (line 101), phase-not-found path (line 107), and multiple scaffold subtypes in cmdScaffold (context/uat/verification/eval/baseline types at lines 253-360).
- **Test approach**: All tracker handlers call `output()` (which calls `process.exit`). Existing test pattern captures output by mocking process.exit and intercepting console.log. Mock fs.readFileSync/writeFileSync/readdirSync for filesystem operations. Mock execFileSync for GitHub CLI calls.
- **Baseline**: 1,853 tests passing.
</execution_context>

## Task 1: Test tracker handler — handleGetConfig

Test through `cmdTracker(cwd, 'get-config', [], false)`:

1. **github provider with successful auth**: Mock config.json with `{ tracker: { provider: 'github' } }`, mock execFileSync to succeed for `gh auth status`. Verify output includes `auth_status: 'authenticated'`.
2. **github provider with failed auth**: Mock execFileSync to throw for `gh auth status`. Verify `auth_status: 'not_authenticated'`.
3. **mcp-atlassian provider**: Mock config.json with `{ tracker: { provider: 'mcp-atlassian' } }`. Verify `auth_status: 'mcp_server'`.
4. **no tracker configured**: Mock config.json with `{}`. Verify `auth_status: 'not_configured'`.

## Task 2: Test tracker handlers — handleSyncRoadmap and handleSyncPhase

Test through `cmdTracker(cwd, 'sync-roadmap', [], false)`:

1. **mcp-atlassian provider**: Returns error directing to prepare-roadmap-sync.
2. **no tracker**: Returns error "No tracker configured".
3. **no ROADMAP.md**: Mock safeReadFile to return null. Returns error "No ROADMAP.md found".
4. **github with phases**: Mock ROADMAP.md with phase headings, mock gh CLI to return issue URLs. Verify stats.created count and mapping saved.

Test through `cmdTracker(cwd, 'sync-phase', [phaseNum], false)`:

5. **missing phase-number arg**: Verify error "Usage: tracker sync-phase".
6. **mcp-atlassian provider**: Returns error directing to prepare-phase-sync.
7. **github with plans**: Mock phase directory with plan files. Verify stats.

## Task 3: Test tracker handlers — handleUpdateStatus, handleAddComment

Test `cmdTracker(cwd, 'update-status', [phaseNum, status], false)`:

1. **mcp-atlassian updates local mapping**: Verify status updated in mapping file.
2. **no tracker configured**: Returns error.
3. **phase not synced**: Returns error "Phase not synced to tracker".
4. **github tracker updates issue labels and closes on complete**: Mock gh CLI calls, verify label operations.

Test `cmdTracker(cwd, 'add-comment', [phaseNum, filePath], false)`:

5. **mcp-atlassian returns comment data for agent**: Verify output includes issue_key, content.
6. **github tracker adds comment**: Mock gh CLI, verify issue comment call.
7. **file not found**: Returns error "File not found".

## Task 4: Test tracker handlers — handleSyncStatus, handleSchedule, handlePrepareReschedule

Test `cmdTracker(cwd, 'sync-status', [], false)`:

1. **returns sync summary**: Mock ROADMAP.md and TRACKER.md, verify synced/unsynced counts.

Test `cmdTracker(cwd, 'schedule', [], false)`:

2. **returns computed schedule**: Mock ROADMAP.md with milestone dates and phase durations.

Test `cmdTracker(cwd, 'prepare-reschedule', [], false)`:

3. **non-mcp-atlassian returns error**: Verify error message.
4. **mcp-atlassian returns update operations**: Mock mapping with milestone/phase entries, verify operations array.

## Task 5: Test tracker handlers — handlePrepareRoadmapSync, handlePreparePhaseSync, handleRecordMapping, handleRecordStatus

Test `cmdTracker(cwd, 'prepare-roadmap-sync', [], false)`:

1. **non-mcp-atlassian returns error**: Verify error message.
2. **mcp-atlassian with phases**: Mock ROADMAP.md, verify operations include create/skip for milestones and phases.

Test `cmdTracker(cwd, 'prepare-phase-sync', [phaseNum], false)`:

3. **non-mcp-atlassian returns error**: Verify error message.
4. **mcp-atlassian with plans**: Mock phase directory with plan files, verify operations.

Test `cmdTracker(cwd, 'record-mapping', args, false)`:

5. **record milestone mapping**: Verify mapping saved with correct structure.
6. **record phase mapping**: Verify mapping saved.
7. **record plan mapping**: Verify mapping saved.
8. **missing required args**: Verify error.

Test `cmdTracker(cwd, 'record-status', args, false)`:

9. **update existing phase status**: Verify status updated.
10. **phase not synced**: Returns error.

## Task 6: Test createGitHubTracker methods

Test the GitHub tracker object returned by `createGitHubTracker(cwd, config)`:

1. **createPhaseIssue — gh unavailable**: Returns `{ issueRef: null, url: null }`.
2. **createPhaseIssue — success**: Mock gh to return URL, verify issueRef format `#N`.
3. **createTaskIssue — with parent**: Verify sub-issue link attempt.
4. **updateIssueStatus — complete**: Verify issue close call.
5. **addComment — success**: Verify comment posted.
6. **syncRoadmap — creates issues for new phases, skips existing**: Verify stats.
7. **syncPhase — creates task issues for plans**: Verify stats.

## Task 7: Test scaffold.js uncovered branches

Add to `tests/unit/scaffold.test.js`:

1. **cmdTemplateFill — no template type**: Verify error "template type required".
2. **cmdTemplateFill — no --phase**: Verify error "--phase required".
3. **cmdTemplateFill — phase not found**: Verify output `{ error: 'Phase not found' }`.
4. **cmdTemplateFill — verification template**: Verify correct frontmatter and filename for verification type.
5. **cmdScaffold — context type**: Verify CONTEXT.md scaffolded with correct content.
6. **cmdScaffold — uat type**: Verify UAT.md scaffolded.
7. **cmdScaffold — verification type**: Verify VERIFICATION.md scaffolded.
8. **cmdScaffold — eval type**: Verify EVAL.md scaffolded.
9. **cmdScaffold — baseline type**: Verify BASELINE.md scaffolded.

## Task 8: Verify coverage improvement

Run `npx jest --coverage` and verify:
- tracker.js >= 85% line coverage
- scaffold.js >= 85% line coverage
- All 1,853+ tests pass
- Zero regressions in existing tests
