---
phase: 53-integration-and-regression-testing
plan: 01
type: verify
wave: 1
depends_on: []
files_modified:
  - tests/integration/cli.test.js
  - tests/unit/roadmap.test.js
  - tests/unit/state.test.js
  - tests/unit/commands.test.js
  - tests/unit/paths.test.js
autonomous: true
verification_level: full
must_haves:
  truths:
    - "Full test suite passes with 1,900+ tests (current: 1,978) with zero regressions"
    - "`node bin/grd-tools.js validate consistency` reports zero errors on GRD's own `.planning/` directory"
    - "`npm run lint` exits 0 and `npm run format:check` exits 0"
    - "All Phase 49 bug fixes have regression tests that demonstrate the fix: each test constructs the pre-fix input and verifies the fixed behavior"
    - "Integration test validates all v0.2.7 init workflows (init plan-phase, init execute-phase, init autopilot) return correct current_milestone v0.2.7"
    - "Coverage thresholds in jest.config.js still enforced for all 20 lib/ modules at 85%+ lines"
  artifacts:
    - path: "tests/integration/cli.test.js"
      provides: "New integration tests for v0.2.7 cross-feature regression validation"
  key_links:
    - from: "lib/roadmap.js"
      to: "tests/unit/roadmap.test.js"
      reason: "BUG-48-002 regression test validates goal regex handles both **Goal:** and **Goal**: formats"
    - from: "lib/state.js"
      to: "tests/unit/state.test.js"
      reason: "BUG-48-003 and BUG-48-005 regression tests validate state-snapshot and state-patch underscore mapping"
    - from: "lib/context.js"
      to: "tests/unit/commands.test.js"
      reason: "BUG-48-004 regression test validates objective extraction from body (not frontmatter)"
    - from: "lib/paths.js"
      to: "tests/unit/paths.test.js"
      reason: "BUG-48-001 regression tests validate currentMilestone parsing edge cases"
---

<objective>
Run the full regression suite across all GRD source code changes from phases 48-52 and verify that existing bug-fix regression tests adequately cover the Phase 49 fixes. Add integration tests for v0.2.7 cross-feature validation (init workflows, consistency checks, coverage thresholds). This addresses success criteria 1, 2, 5, and 6.
</objective>

<execution_context>
- **Current test count**: 1,978 tests passing across 34 suites
- **Lint/format**: Both clean (zero errors)
- **Consistency**: `validate consistency` passes with 1 warning (Phase 53 dir missing -- now created)
- **Phase 49 bugs already have regression tests**:
  - BUG-48-001: 4 edge case tests in `tests/unit/paths.test.js` (lines 126-160)
  - BUG-48-002: dedicated describe block in `tests/unit/roadmap.test.js` (line 439)
  - BUG-48-003: dedicated describe block in `tests/unit/state.test.js` (line 764)
  - BUG-48-004: 2 tests in `tests/unit/commands.test.js` (lines 553-570)
  - BUG-48-005: dedicated describe block in `tests/unit/state.test.js` (line 885)
- **Coverage thresholds**: All 20 modules at 85%+ lines in jest.config.js
- **v0.2.7 deliverables**: autopilot.js, dogfooding infra, 5 bug fixes, complexity reduction, coverage improvements
</execution_context>

## Task 1: Run full regression suite and document results

Execute the complete test battery and capture results:

```bash
npm test
npm run lint
npm run format:check
node bin/grd-tools.js validate consistency
```

Verify:
1. All 1,978+ tests pass with zero failures
2. Zero lint errors
3. Zero formatting issues
4. Consistency validation reports `passed: true` with zero errors
5. Per-file coverage thresholds are all met (no coverage regressions)

Document the exact test count, coverage summary, and any warnings.

## Task 2: Verify Phase 49 bug-fix regression tests

For each of the 5 Phase 49 bugs, verify that the existing regression tests adequately demonstrate the fix. Read each test and confirm it:
- Constructs the specific input that triggered the original bug
- Verifies the corrected behavior (not just that it doesn't crash)

### BUG-48-001 (currentMilestone parsing)
- File: `tests/unit/paths.test.js`
- Tests: 4 edge cases (space-separated name, v0.0.5 long name, multiple versions, v1.0.0 format)
- Verify: Each test creates a STATE.md with specific Milestone field format and checks `currentMilestone()` returns the correct version string

### BUG-48-002 (goal regex)
- File: `tests/unit/roadmap.test.js`
- Tests: `BUG-48-002: goal regex both formats` describe block
- Verify: Tests both `**Goal:** text` and `**Goal**: text` formats and asserts goal is extracted (not null)

### BUG-48-003 (state-snapshot Active phase field)
- File: `tests/unit/state.test.js`
- Tests: `BUG-48-003: cmdStateSnapshot Active phase field` describe block
- Verify: Tests STATE.md with "Active phase:" field format (the format that was failing) and asserts current_phase is extracted correctly

### BUG-48-004 (objective extraction from body)
- File: `tests/unit/commands.test.js`
- Tests: Two tests for `<objective>` tag extraction and `files_modified` underscore key
- Verify: Objective is extracted from plan body (not matched inside frontmatter strings)

### BUG-48-005 (underscore-to-space field mapping in state patch)
- File: `tests/unit/state.test.js`
- Tests: `BUG-48-005: cmdStatePatch underscore-to-space mapping` describe block
- Verify: `Current_plan` maps to `Current plan` field, and non-existent underscore fields return not-found

## Task 3: Add v0.2.7 cross-feature integration tests

Add a new `describe('v0.2.7 integration regression')` section to `tests/integration/cli.test.js` with the following tests:

1. **init plan-phase returns current milestone v0.2.7**: Run `init plan-phase 53` against GRD's own `.planning/` structure. Verify output contains `roadmap_exists: true` and `planning_exists: true`. This validates the milestone-scoped path resolution works for the current project.

2. **validate consistency passes for GRD's own .planning/**: Run `validate consistency` against a fixture that mirrors GRD's milestone-scoped structure (with milestones/v1.0/phases/). Verify `passed: true` and `errors: []`.

3. **coverage-report command returns valid JSON**: Run `coverage-report` (new Phase 51 feature). Verify it returns valid JSON with `modules` array and `all_above` boolean.

4. **health-check command returns valid JSON**: Run `health-check` (new Phase 51 feature). Verify it returns valid JSON with `status` field.

5. **autopilot dry-run produces correct phase sequence**: Run `autopilot --dry-run --from 1 --to 3` against a 3-phase fixture. Verify results array has 6 entries (plan+execute per phase) in correct phase order (1, 1, 2, 2, 3, 3).

Note: Tests 1-2 need fixtures with milestone-scoped directory structures to match v0.2.7's hierarchy. Tests 3-4 can use the shared fixture. Test 5 can reuse the existing autopilot fixture helper.

## Task 4: Verify testbed CLI validation still works

Run the testbed validation battery against the testbed to confirm all GRD CLI commands work correctly against an external project:

```bash
cd testbed && node ../bin/grd-tools.js state load
cd testbed && node ../bin/grd-tools.js state-snapshot
cd testbed && node ../bin/grd-tools.js roadmap get-phase 1
cd testbed && node ../bin/grd-tools.js validate consistency
cd testbed && node ../bin/grd-tools.js init plan-phase 1
cd testbed && node ../bin/grd-tools.js init execute-phase 1
```

Verify each command returns valid JSON with correct data. This exercises the bug fixes from Phase 49 against a real external project structure.

## Task 5: Run the full test suite again after any test additions

```bash
npm test
```

Verify:
- Total test count is 1,978+ (accounting for any new tests added in Task 3)
- All coverage thresholds met
- Zero regressions from test additions
