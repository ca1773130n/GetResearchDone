---
wave: 1
depends_on: []
files_modified:
  - lib/paths.js
  - lib/context.js
  - lib/commands.js
  - tests/unit/paths.test.js
  - tests/unit/context.test.js
  - tests/unit/commands.test.js
autonomous: true
---

# Plan 42-01: Constitution, Standards, & Ceremony Infrastructure

<objective>
Add the core infrastructure for all three new layers: constitution (PRINCIPLES.md), standards discovery, and scale-adaptive ceremony. This covers path resolution, init workflow integration, config defaults, and ceremony level auto-inference.
</objective>

<tasks>
<task id="1" title="Add standardsDir to lib/paths.js">
Add `standardsDir()` function following the same milestone-scoped pattern as `todosDir` and `quickDir`. Export from module.
</task>

<task id="2" title="Add PRINCIPLES.md support to init workflows">
Add `principles_exists` detection to `cmdInitExecutePhase`, `cmdInitPlanPhase`, `cmdInitNewProject`, and `cmdInitQuick`. Add `--include principles` support for execute-phase and plan-phase.
</task>

<task id="3" title="Add ceremony level auto-inference">
Create `inferCeremonyLevel(config, phaseInfo, cwd)` function. Returns light/standard/full based on: user override > per-phase override > auto-inference from plan count, research refs, eval targets. Add `ceremony_level` to execute-phase and plan-phase init results.
</task>

<task id="4" title="Add ceremony config defaults">
Add `ceremony: { default_level: 'auto', phase_overrides: {} }` to the defaults in `cmdConfigEnsureSection`.
</task>

<task id="5" title="Add standards detection to init workflows">
Add `standards_exists` and `standards_dir` to all 4 init functions using the new `standardsDir` path resolver.
</task>
</tasks>

<must_haves>
- `standardsDir()` exported from lib/paths.js
- `principles_exists` in all 4 init function outputs
- `ceremony_level` in execute-phase and plan-phase init outputs
- `ceremony` section in default config
- `standards_exists` and `standards_dir` in all 4 init outputs
- All existing tests pass + new tests for each feature
</must_haves>
