---
phase: 41-command-documentation-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/context.js, tests/unit/context.test.js]
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "cmdInitNewMilestone scans milestones/{version}/phases/ directories (new-style) for highest phase number"
    - "cmdInitNewMilestone still scans old-style *-phases directories for backward compatibility"
    - "suggested_start_phase is correct when phases exist only in new-style milestone directories"
    - "All existing tests pass with zero regressions"
  artifacts:
    - path: "lib/context.js"
      provides: "Fixed cmdInitNewMilestone with new-style directory scanning"
    - path: "tests/unit/context.test.js"
      provides: "Tests for new-style milestone directory scanning"
  key_links:
    - from: "lib/context.js"
      to: "lib/paths.js"
      via: "milestonesDir function"
      pattern: "getMilestonesDirPath"
---

<objective>
Fix the `cmdInitNewMilestone` phase scanning bug so it scans new-style `milestones/{version}/phases/` directories in addition to old-style `*-phases/` directories when computing `suggested_start_phase`.

Purpose: REQ-76 bugfix. Without this fix, creating a new milestone after v0.2.1+ (which uses new-style directories) would suggest phase 1 instead of the correct next phase number.
Output: Patched `lib/context.js` with new-style directory scanning, tests proving correctness.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/context.js
@lib/paths.js
@tests/unit/context.test.js
@tests/helpers/fixtures.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cmdInitNewMilestone to scan new-style milestone directories</name>
  <files>lib/context.js</files>
  <action>
    In `cmdInitNewMilestone` (around line 439-455), the archived phase scanning loop only matches directories ending with `-phases` (old-style like `v0.2.0-phases/`). Add a second scan pattern inside the same try-catch block that also checks for new-style directories.

    New-style directories are structured as `milestones/{version}/phases/` where `{version}` is a directory name (like `v0.2.1`) that contains a `phases/` subdirectory. The `milestonesDir` variable already points to `.planning/milestones/`.

    After the existing `entry.name.endsWith('-phases')` block (line 442-451), add:

    ```javascript
    // Also scan new-style milestone directories: milestones/{version}/phases/
    const newStylePhasesDir = path.join(milestonesDir, entry.name, 'phases');
    if (entry.isDirectory() && entry.name.startsWith('v') && !entry.name.endsWith('-phases') && fs.existsSync(newStylePhasesDir)) {
      const newStylePhases = fs.readdirSync(newStylePhasesDir);
      for (const dir of newStylePhases) {
        const dm2 = dir.match(/^(\d+)/);
        if (dm2) {
          const num = parseInt(dm2[1], 10);
          if (num > highestArchivedPhase) highestArchivedPhase = num;
        }
      }
    }
    ```

    IMPORTANT: Do NOT break the existing old-style scanning. The new code must be additive. The `!entry.name.endsWith('-phases')` guard prevents double-counting directories that match the old pattern.

    Also skip `anonymous` and any non-version-like directories. Add a simple guard: only scan if `entry.name` starts with `v` (version directories are like `v0.2.1`).
  </action>
  <verify>
    npx jest tests/unit/context.test.js --no-coverage (Level 1: Sanity -- all existing tests still pass)
  </verify>
  <done>cmdInitNewMilestone scans both old-style (*-phases) and new-style (milestones/{version}/phases/) directories; existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for new-style milestone directory scanning</name>
  <files>tests/unit/context.test.js</files>
  <action>
    In the `cmdInitNewMilestone` describe block (around line 555), add new tests:

    1. **Test: scans new-style milestone directories for suggested_start_phase**
       - Create a temp fixture dir with `createFixtureDir()`
       - Create `milestones/v0.2.1/phases/36-some-phase/` directory inside it
       - Run `cmdInitNewMilestone(tmpDir, false)`
       - Assert `suggested_start_phase >= 37` (since phase 36 exists in new-style dir)

    2. **Test: combines old-style and new-style directories for highest phase**
       - Create a temp fixture dir
       - Create old-style: `milestones/v0.1.0-phases/10-old-phase/`
       - Create new-style: `milestones/v0.2.1/phases/36-new-phase/`
       - Run `cmdInitNewMilestone(tmpDir, false)`
       - Assert `suggested_start_phase >= 37` (picks highest from either style)

    3. **Test: new-style directory with only current-milestone phases**
       - Create a temp fixture dir
       - Verify that the current milestone phases directory (already has 01-test, 02-build) is found correctly
       - Ensure `suggested_start_phase >= 3` from the current milestone

    Use the existing test patterns: `createFixtureDir()`, `captureOutput()`, `JSON.parse(stdout)`. Create directories with `fs.mkdirSync(dirPath, { recursive: true })`. Clean up with `cleanupFixtureDir()`.
  </action>
  <verify>
    npx jest tests/unit/context.test.js --no-coverage -t "new-style" (Level 1: Sanity -- new tests pass)
    npx jest tests/unit/context.test.js --no-coverage (Level 1: Sanity -- all tests still pass)
  </verify>
  <done>At least 2 new tests exist covering new-style milestone directory scanning; all context tests pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `npx jest tests/unit/context.test.js --no-coverage` passes (all existing + new tests)
- `npm run lint` passes (no lint errors introduced)

Level 2 (Proxy):
- Manual inspection: cmdInitNewMilestone returns correct suggested_start_phase when only new-style dirs exist
</verification>

<success_criteria>
- cmdInitNewMilestone correctly computes suggested_start_phase from new-style milestones/{version}/phases/ directories
- Backward compatibility: old-style *-phases directories still scanned
- At least 2 new unit tests covering the fix
- All existing tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.3/phases/41-command-documentation-updates/41-01-SUMMARY.md`
</output>
