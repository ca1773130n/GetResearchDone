---
phase: 40-settings-interview-revision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/settings.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "The git workflow question asks 'Use worktree isolation for phase execution?' (Yes/No) instead of the old 3-way branching_strategy question"
    - "When user selects Yes for worktree isolation, sub-options appear for worktree_dir (default .worktrees/) and default_completion_action (merge/pr/keep/discard)"
    - "When user selects No, git.branching_strategy is set to 'none' in config.json"
    - "Execution settings (use_teams toggle and max_concurrent_teammates number) appear as interview questions"
    - "Code review settings (timing, severity_gate, auto_fix_warnings) appear as interview questions"
    - "Confirmation gate toggles (commit_confirmation, file_deletion, phase_completion, target_adjustment, approach_change) appear as interview questions"
    - "The update_config step writes all new sections (git, execution, confirmation_gates, code_review) to config.json correctly"
    - "The confirm step displays all new settings in the summary table"
  artifacts:
    - path: "commands/settings.md"
      provides: "Revised settings interview with git isolation, execution, code review, and confirmation gate questions"
      contains: "worktree isolation"
  key_links:
    - from: "commands/settings.md"
      to: ".planning/config.json"
      via: "Interview writes merged config with all new sections"
      pattern: "config.json"
---

<objective>
Rewrite the `/grd:settings` interview to expose all current features: replace the 3-way git branching question with a Yes/No worktree isolation question with sub-options, add execution settings (teams), code review settings, and confirmation gate toggles.

Purpose: The current settings interview is outdated -- it doesn't expose execution teams, code review, confirmation gates, or the new worktree-based git model from Phase 38. Users have no way to configure these features through the interactive interview.

Output: Updated `commands/settings.md` with 12+ questions covering all configurable features.
</objective>

<execution_context>
@commands/settings.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@commands/settings.md
@lib/utils.js (loadConfig defaults and field names)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace git question and add worktree sub-options</name>
  <files>commands/settings.md</files>
  <action>
    In `commands/settings.md`, replace the existing "Git branching strategy?" question (question 5 in the current AskUserQuestion array) with a new question structure:

    **Replace the old question:**
    ```
    {
      question: "Git branching strategy?",
      header: "Branching",
      multiSelect: false,
      options: [
        { label: "None (Recommended)", description: "Commit directly to current branch" },
        { label: "Per Phase", description: "Create branch for each phase (grd/phase-{N}-{name})" },
        { label: "Per Milestone", description: "Create branch for entire milestone (grd/{version}-{name})" }
      ]
    }
    ```

    **With:**
    ```
    {
      question: "Use worktree isolation for phase execution?",
      header: "Git Isolation",
      multiSelect: false,
      options: [
        { label: "Yes", description: "Each phase runs in a separate git worktree (recommended for active development)" },
        { label: "No (Default)", description: "Execute directly on current branch, no isolation" }
      ]
    }
    ```

    **Add conditional sub-options** (described in a note after the question array):

    After the AskUserQuestion call, add a conditional block:

    ```
    If user selected "Yes" for Git Isolation, ask follow-up questions:

    AskUserQuestion([
      {
        question: "Worktree directory location?",
        header: "Worktree Directory",
        multiSelect: false,
        options: [
          { label: ".worktrees/ (Default)", description: "Project-local .worktrees/ directory" },
          { label: "Custom", description: "Specify a custom directory path" }
        ]
      },
      {
        question: "Default action when phase execution completes?",
        header: "Completion Action",
        multiSelect: false,
        options: [
          { label: "Ask each time (Default)", description: "Present merge/PR/keep/discard options after execution" },
          { label: "Merge locally", description: "Auto-merge worktree branch into base branch" },
          { label: "Create PR", description: "Auto-push and create pull request" },
          { label: "Keep branch", description: "Leave worktree in place for manual review" }
        ]
      }
    ])
    ```

    If "Custom" is selected for worktree directory, prompt for the path.
  </action>
  <verify>
    Read commands/settings.md and verify:
    - The old 3-way branching question is gone (Level 1: Sanity)
    - The new Yes/No worktree isolation question is present
    - Sub-options for worktree_dir and default_completion_action exist
  </verify>
  <done>
    The git workflow question is replaced with a clear Yes/No worktree isolation question. Sub-options for directory and completion action appear when Yes is selected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add execution, code review, and confirmation gate questions</name>
  <files>commands/settings.md</files>
  <action>
    In `commands/settings.md`, add the following new questions to the AskUserQuestion array (after the existing questions and before the update_config step):

    **Execution Settings (REQ-78):**
    ```
    {
      question: "Use Agent Teams for parallel plan execution?",
      header: "Execution Teams",
      multiSelect: false,
      options: [
        { label: "No (Default)", description: "Execute plans sequentially in a single agent" },
        { label: "Yes", description: "Spawn parallel teammate agents for each wave" }
      ]
    }
    ```

    If "Yes" is selected, add a follow-up:
    ```
    {
      question: "Maximum concurrent teammates?",
      header: "Team Size",
      multiSelect: false,
      options: [
        { label: "2", description: "Conservative — lower resource usage" },
        { label: "4 (Default)", description: "Balanced concurrency" },
        { label: "6", description: "Higher parallelism for large phases" }
      ]
    }
    ```

    **Code Review Settings (REQ-79):**
    ```
    {
      question: "Automatic code review timing?",
      header: "Code Review",
      multiSelect: false,
      options: [
        { label: "Per Wave (Default)", description: "Review after each execution wave completes" },
        { label: "Per Phase", description: "Review once after entire phase execution" },
        { label: "Disabled", description: "No automatic code review" }
      ]
    },
    {
      question: "Minimum severity to block execution?",
      header: "Review Severity Gate",
      multiSelect: false,
      options: [
        { label: "Blocker (Default)", description: "Only blockers halt execution" },
        { label: "Critical", description: "Blockers and critical issues halt execution" },
        { label: "Warning", description: "All non-info issues halt execution" }
      ]
    },
    {
      question: "Auto-fix warnings found during code review?",
      header: "Auto-fix Warnings",
      multiSelect: false,
      options: [
        { label: "No (Default)", description: "Report warnings but do not auto-fix" },
        { label: "Yes", description: "Automatically fix warning-level review findings" }
      ]
    }
    ```

    **Confirmation Gates (REQ-80):**
    ```
    {
      question: "Confirmation gates — pause for human approval at these points?",
      header: "Confirmation Gates",
      multiSelect: true,
      options: [
        { label: "Commit Confirmation", description: "Confirm before each git commit" },
        { label: "File Deletion", description: "Confirm before deleting files" },
        { label: "Phase Completion", description: "Confirm before marking phase complete" },
        { label: "Target Adjustment", description: "Confirm before adjusting evaluation targets" },
        { label: "Approach Change", description: "Confirm before changing implementation approach" }
      ]
    }
    ```

    **Update the `read_current` step** to also parse:
    - `execution.use_teams` (default: false)
    - `execution.max_concurrent_teammates` (default: 4)
    - `code_review.timing` (default: "per_wave")
    - `code_review.severity_gate` (default: "blocker")
    - `code_review.auto_fix_warnings` (default: false)
    - `confirmation_gates.commit_confirmation` (default: false)
    - `confirmation_gates.file_deletion` (default: false)
    - `confirmation_gates.phase_completion` (default: false)
    - `confirmation_gates.target_adjustment` (default: false)
    - `confirmation_gates.approach_change` (default: false)
  </action>
  <verify>
    Read commands/settings.md and verify:
    - Execution teams question is present with conditional max_concurrent_teammates (Level 1: Sanity)
    - Code review timing, severity gate, and auto-fix questions are present
    - Confirmation gates multi-select is present with all 5 gate options
    - read_current step parses all new fields
  </verify>
  <done>
    All new questions (execution teams, code review, confirmation gates) are present in the interview. The read_current step parses all new config fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update config write step and confirmation display</name>
  <files>commands/settings.md</files>
  <action>
    **Update the `update_config` step** to include all new config sections in the merge:

    Replace the current config merge template:
    ```json
    {
      ...existing_config,
      "model_profile": "quality" | "balanced" | "budget",
      "autonomous_mode": true|false,
      "workflow": {
        "research": true/false,
        "plan_check": true/false,
        "verifier": true/false
      },
      "git": {
        "branching_strategy": "none" | "phase" | "milestone"
      },
      "research_gates": {
        "verification_design": true/false,
        "method_selection": true/false,
        "baseline_review": true/false
      },
      "tracker": {
        "provider": "none" | "github" | "mcp-atlassian"
      }
    }
    ```

    With the expanded version:
    ```json
    {
      ...existing_config,
      "model_profile": "quality" | "balanced" | "budget",
      "autonomous_mode": true|false,
      "workflow": {
        "research": true/false,
        "plan_check": true/false,
        "verifier": true/false
      },
      "git": {
        "branching_strategy": "none" | "phase",
        "worktree_dir": ".worktrees/" | custom_path,
        "default_completion_action": "ask" | "merge" | "pr" | "keep"
      },
      "execution": {
        "use_teams": true/false,
        "max_concurrent_teammates": 2|4|6
      },
      "code_review": {
        "enabled": timing !== "disabled",
        "timing": "per_wave" | "per_phase",
        "severity_gate": "blocker" | "critical" | "warning",
        "auto_fix_warnings": true/false
      },
      "confirmation_gates": {
        "commit_confirmation": true/false,
        "file_deletion": true/false,
        "phase_completion": true/false,
        "target_adjustment": true/false,
        "approach_change": true/false
      },
      "research_gates": {
        "verification_design": true/false,
        "method_selection": true/false,
        "baseline_review": true/false
      },
      "tracker": {
        "provider": "none" | "github" | "mcp-atlassian"
      }
    }
    ```

    **Mapping rules for the git section:**
    - "Yes" for worktree isolation -> `branching_strategy: "phase"`, write `worktree_dir` and `default_completion_action`
    - "No" for worktree isolation -> `branching_strategy: "none"` (omit worktree_dir and default_completion_action)
    - "Ask each time" -> `default_completion_action: "ask"` (or omit the field -- "ask" is the implicit default)
    - "Merge locally" -> `default_completion_action: "merge"`
    - "Create PR" -> `default_completion_action: "pr"`
    - "Keep branch" -> `default_completion_action: "keep"`

    **Mapping rules for code_review:**
    - "Per Wave" -> `timing: "per_wave"`, `enabled: true`
    - "Per Phase" -> `timing: "per_phase"`, `enabled: true`
    - "Disabled" -> `enabled: false`
    - severity_gate: map label to lowercase value ("Blocker" -> "blocker", "Critical" -> "critical", "Warning" -> "warning")

    **Mapping rules for execution:**
    - "Yes" for teams -> `use_teams: true`, write `max_concurrent_teammates` from follow-up
    - "No" for teams -> `use_teams: false`

    **Update the `confirm` step** to display all new settings in the summary table:

    ```
    | Setting              | Value |
    |----------------------|-------|
    | Model Profile        | {quality/balanced/budget} |
    | Plan Researcher      | {On/Off} |
    | Plan Checker         | {On/Off} |
    | Execution Verifier   | {On/Off} |
    | Git Isolation         | {On/Off} |
    | Worktree Directory   | {path or N/A} |
    | Completion Action    | {ask/merge/pr/keep or N/A} |
    | Agent Teams          | {On/Off} |
    | Max Teammates        | {N or N/A} |
    | Code Review          | {Per Wave/Per Phase/Off} |
    | Severity Gate        | {blocker/critical/warning} |
    | Auto-fix Warnings    | {On/Off} |
    | Autonomous Mode      | {On/Off} |
    | Gate: Eval Design    | {On/Off} |
    | Gate: Method Select  | {On/Off} |
    | Gate: Baseline Review| {On/Off} |
    | Gate: Commit Confirm | {On/Off} |
    | Gate: File Deletion  | {On/Off} |
    | Gate: Phase Complete | {On/Off} |
    | Gate: Target Adjust  | {On/Off} |
    | Gate: Approach Change| {On/Off} |
    | Tracker              | {None/GitHub/Jira} |
    ```

    **Update the `success_criteria` section** to reflect the new question count and all config sections.
  </action>
  <verify>
    Read commands/settings.md and verify:
    - The update_config step merges git, execution, code_review, and confirmation_gates sections (Level 1: Sanity)
    - The confirm step displays all new settings in the summary table
    - All mapping rules are documented (git isolation -> branching_strategy, review timing -> enabled+timing, etc.)
  </verify>
  <done>
    The config write step produces a valid config.json with all new sections. The confirmation display shows all settings. Running the full interview produces a complete config.json with git, execution, code_review, confirmation_gates, research_gates, workflow, and tracker sections.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- Read the final commands/settings.md and verify all question sections exist
- Verify the old 3-way branching_strategy question is removed
- Verify all 5 Success Criteria from the roadmap are satisfied:
  1. Git workflow question is Yes/No with sub-options for worktree_dir and default_completion_action
  2. Execution settings (use_teams, max_concurrent_teammates) appear as questions
  3. Code review settings (timing, severity_gate, auto_fix_warnings) appear as questions
  4. Confirmation gates (5 toggles) appear as multi-select question
  5. Config write step produces all sections in config.json

Level 2 (Proxy): N/A (command skill definition -- no executable code to proxy-test)

Level 3 (Deferred):
- Run /grd:settings end-to-end and verify config.json output (deferred to Phase 41 integration)
</verification>

<success_criteria>
1. The old 3-way git branching_strategy question is removed from the interview
2. A new Yes/No worktree isolation question with sub-options replaces it
3. Execution teams (use_teams + max_concurrent_teammates) questions are present
4. Code review (timing + severity_gate + auto_fix_warnings) questions are present
5. Confirmation gates (5 toggles) multi-select question is present
6. The update_config step writes all new sections to config.json
7. The confirm step displays all settings in the summary table
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2.3/phases/40-settings-interview-revision/40-01-SUMMARY.md`
</output>
