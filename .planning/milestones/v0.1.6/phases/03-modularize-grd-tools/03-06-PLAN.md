---
phase: 03-modularize-grd-tools
plan: 06
type: execute
wave: 5
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - lib/tracker.js
  - lib/context.js
  - bin/grd-tools.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/tracker.js exports all tracker operations (config, mapping, schedule, GitHub sync, cmdTracker dispatch)"
    - "lib/context.js exports all init workflow functions (execute-phase, plan-phase, new-project, resume, etc.)"
    - "bin/grd-tools.js requires lib/tracker.js and lib/context.js instead of defining these functions inline"
    - "All tracker and init CLI commands produce identical output to golden references"
    - "No lib/ module exceeds 600 lines (tracker is the largest, split if necessary)"
  artifacts:
    - path: "lib/tracker.js"
      provides: "Issue tracker integration (GitHub/Jira sync, schedule, prepare/record)"
    - path: "lib/context.js"
      provides: "Init workflow context loading for all 20 workflows"
  key_links:
    - from: "lib/tracker.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/context.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/context.js"
      to: "lib/state.js"
      via: "require"
      pattern: "require.*state"
---

<objective>
Extract `lib/tracker.js` (issue tracker integration) and `lib/context.js` (init workflow context loading).

Purpose: Tracker and context are the most downstream modules -- they depend on utils, state, roadmap, and phase. Extracting them last ensures all their dependencies are already in place. This plan removes the final ~1,650 lines of business logic from the monolith.

Output: Two new lib/ files. After this plan, bin/grd-tools.js should contain only the CLI router and a few standalone command functions.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@lib/utils.js
@lib/frontmatter.js
@lib/state.js
@lib/roadmap.js
@lib/phase.js
@.planning/phases/03-modularize-grd-tools/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/tracker.js with all tracker operations</name>
  <files>lib/tracker.js, bin/grd-tools.js</files>
  <action>
    Create `lib/tracker.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, loadConfig, execGit, normalizePhaseName, findPhaseInternal,
               resolveModelForAgent, output, error } = require('./utils');
       const { extractFrontmatter } = require('./frontmatter');
       ```

       Note: Check if tracker functions use execFileSync directly (for gh CLI calls). If so, also import:
       ```js
       const { execFileSync } = require('child_process');
       ```
       Or import it from utils if exported.

    2. **Helper functions to move (preserve exact implementation):**
       - formatScheduleDate (line ~4400)
       - addDays (line ~4407)
       - computeSchedule (line ~4413) -- reads ROADMAP.md, computes dates
       - getScheduleForPhase (line ~4506)
       - getScheduleForMilestone (line ~4511)
       - loadTrackerConfig (line ~4517) -- reads config.json tracker section
       - loadTrackerMapping (line ~4578) -- reads/creates TRACKER.md
       - saveTrackerMapping (line ~4631) -- writes TRACKER.md
       - createGitHubTracker (line ~4658) -- creates GitHub issues via gh CLI (~133 lines)
       - createTracker (line ~4791) -- factory function

    3. **Command function to move:**
       - cmdTracker (line ~4798) -- main dispatcher (~532 lines)

    4. **module.exports:** Export all functions.

    5. **Update bin/grd-tools.js:**
       - Add require for lib/tracker.js, destructure exported functions
       - Remove moved function definitions

    CRITICAL NOTES:
    - cmdTracker is ~532 lines and handles ~15 subcommands. It is the single largest function. Move it as-is -- do NOT split it into separate functions (that's a refactor, not an extraction).
    - createGitHubTracker calls execFileSync for gh CLI -- ensure the import is available.
    - loadTrackerConfig references process.env for Jira/MCP config.
    - This module will be ~930 lines, EXCEEDING the 500-line target significantly.
      ACCEPT this for now. cmdTracker's 532-line switch statement is a natural unit that would be worse if split across files. Phase 4 tests can drive internal refactoring to reduce size.
      Alternative: Split into `lib/tracker-helpers.js` (~398 lines: config, mapping, schedule, github) and `lib/tracker.js` (~532 lines: cmdTracker). But this creates unnecessary module boundary. KEEP as one file.
  </action>
  <verify>
    Run: node bin/grd-tools.js tracker get-config
    Must produce valid JSON (even if "no tracker configured"). (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/tracker.js exists with all tracker operations exported.
    bin/grd-tools.js imports from lib/tracker.js.
    Tracker commands produce identical output to golden references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract lib/context.js with all init workflow functions</name>
  <files>lib/context.js, bin/grd-tools.js</files>
  <action>
    Create `lib/context.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, loadConfig, findPhaseInternal, normalizePhaseName,
               resolveModelInternal, pathExistsInternal, generateSlugInternal,
               getMilestoneInfo, resolveModelForAgent, parseIncludeFlag,
               output, error } = require('./utils');
       const { extractFrontmatter } = require('./frontmatter');
       ```

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - cmdInitExecutePhase (line ~3673) -- 84 lines
       - cmdInitPlanPhase (line ~3757) -- 96 lines
       - cmdInitNewProject (line ~3853) -- 44 lines
       - cmdInitNewMilestone (line ~3897) -- 27 lines
       - cmdInitQuick (line ~3924) -- 47 lines
       - cmdInitResume (line ~3971) -- 27 lines
       - cmdInitVerifyWork (line ~3998) -- 29 lines
       - cmdInitPhaseOp (line ~4027) -- 31 lines
       - cmdInitTodos (line ~4058) -- 59 lines
       - cmdInitMilestoneOp (line ~4117) -- 61 lines
       - cmdInitMapCodebase (line ~4178) -- 34 lines
       - cmdInitProgress (line ~4212) -- 109 lines
       - cmdInitResearchWorkflow (line ~4321) -- 70 lines

    3. **module.exports:** Export all functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/context.js, destructure all exported functions
       - Remove moved function definitions

    NOTES:
    - This module will be ~718 lines, EXCEEDING the 500-line target.
      ACCEPT this. These are 13 independent init functions with no internal dependencies between them. Splitting into multiple files would fragment the init namespace unnecessarily.
      Alternative: Split into `lib/context-core.js` (execute/plan/new/quick/resume ~298 lines) and `lib/context-ops.js` (verify/phase-op/todos/milestone/map/progress/research ~420 lines). Only do this if the executor can handle it cleanly.

    - cmdInitExecutePhase and cmdInitPlanPhase are the most complex. They:
      a) Load config, state, roadmap
      b) Resolve model profiles
      c) Scan phase directories for plans
      d) Build comprehensive context JSON

    - cmdInitProgress (~109 lines) loads config, state, roadmap and builds progress data.

    - Several init functions call resolveModelInternal, findPhaseInternal, etc. -- all now in lib/utils.js.
  </action>
  <verify>
    Run: node bin/grd-tools.js init plan-phase 1
    Run: node bin/grd-tools.js init new-project
    Run: node bin/grd-tools.js init progress
    All must produce valid JSON. (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/context.js exists with all init workflow functions exported.
    bin/grd-tools.js imports from lib/context.js.
    All init commands produce identical output to golden references.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `lib/tracker.js` exists
- `lib/context.js` exists
- All tracker and init commands produce valid JSON
- No require errors

Level 2 (Proxy):
- Diff tracker get-config, init plan-phase, init progress against golden references -- identical
- bin/grd-tools.js should now be < 1500 lines (only CLI router + misc commands remaining)

Level 3 (Deferred): DEFER-03-01, DEFER-03-02
</verification>

<success_criteria>
- lib/tracker.js exports >= 10 functions
- lib/context.js exports >= 13 functions
- bin/grd-tools.js reduced to < 1500 lines (only router + remaining misc commands)
- All tracker and init commands produce identical JSON output
- No circular dependencies between lib/ modules
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-06-SUMMARY.md`
</output>
