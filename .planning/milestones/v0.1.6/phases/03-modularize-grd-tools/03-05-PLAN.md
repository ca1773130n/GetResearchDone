---
phase: 03-modularize-grd-tools
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - lib/phase.js
  - bin/grd-tools.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "lib/phase.js exports all phase lifecycle operations (add, insert, remove, complete, list) plus milestone complete and validate consistency"
    - "cmdPhaseRemove (~253 lines) correctly handles renumbering, roadmap rewrite, and directory operations after extraction"
    - "cmdPhaseComplete correctly updates STATE.md and ROADMAP.md after extraction"
    - "bin/grd-tools.js requires lib/phase.js instead of defining these functions inline"
    - "All phase, milestone, and validate CLI commands produce identical output to golden references"
  artifacts:
    - path: "lib/phase.js"
      provides: "Phase lifecycle operations (add, insert, remove, complete, list, milestone, validate)"
  key_links:
    - from: "lib/phase.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/phase.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*frontmatter"
    - from: "lib/phase.js"
      to: "lib/roadmap.js"
      via: "require"
      pattern: "require.*roadmap"
    - from: "lib/phase.js"
      to: "lib/state.js"
      via: "require"
      pattern: "require.*state"
---

<objective>
Extract `lib/phase.js` containing all phase lifecycle operations -- the largest and most complex module extraction.

Purpose: Phase operations (add, insert, remove, complete) are the most interconnected functions in the monolith. They depend on state (for updating STATE.md on completion), roadmap (for rewriting phase sections), and utils/frontmatter. This is extracted after its dependencies are in place.

Output: One new lib/ file. This is the HIGHEST RISK extraction -- cmdPhaseRemove alone is ~253 lines with complex renumbering logic.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@lib/utils.js
@lib/frontmatter.js
@lib/state.js
@lib/roadmap.js
@.planning/phases/03-modularize-grd-tools/03-03-SUMMARY.md
@.planning/phases/03-modularize-grd-tools/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/phase.js with all phase lifecycle, milestone, and validate operations</name>
  <files>lib/phase.js, bin/grd-tools.js</files>
  <action>
    Create `lib/phase.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, loadConfig, execGit, normalizePhaseName, findPhaseInternal,
               getMilestoneInfo, output, error } = require('./utils');
       const { extractFrontmatter, spliceFrontmatter } = require('./frontmatter');
       ```

       Note: Check if any phase functions call state functions (e.g., cmdPhaseComplete updates STATE.md). If so, import from lib/state.js:
       ```js
       const { stateReplaceField } = require('./state');
       ```
       BUT: Check for circular dependencies first. If state.js does NOT import phase.js, this is safe.

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - cmdPhasesList (line ~886) -- lists phases with status
       - cmdPhaseAdd (line ~2623) -- appends phase to roadmap + creates directory
       - cmdPhaseInsert (line ~2681) -- inserts decimal phase
       - cmdPhaseRemove (line ~2762) -- removes phase, renumbers subsequent (~253 lines, COMPLEX)
       - cmdPhaseComplete (line ~3015) -- marks phase done, updates state + roadmap (~142 lines)
       - cmdMilestoneComplete (line ~3157) -- archives milestone (~120 lines)
       - cmdValidateConsistency (line ~3277) -- checks phase numbering and disk/roadmap sync (~122 lines)

    3. **module.exports:** Export all functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/phase.js, destructure all exported functions
       - Remove moved function definitions

    CRITICAL EXTRACTION NOTES:
    - cmdPhaseRemove is the most complex function in the entire codebase. It:
      a) Reads ROADMAP.md and rewrites phase sections
      b) Renumbers phase directories on disk (fs.renameSync)
      c) Updates plan references inside renamed files
      d) Updates STATE.md
      It uses: safeReadFile, normalizePhaseName, findPhaseInternal, execGit, extractFrontmatter, spliceFrontmatter, fs operations

    - cmdPhaseComplete:
      a) Updates ROADMAP.md (marks plans complete, adds completion date)
      b) Updates STATE.md (advance phase, update progress)
      It uses: safeReadFile, findPhaseInternal, extractFrontmatter, fs operations, getMilestoneInfo
      It may directly manipulate STATE.md content strings rather than calling state.js functions. If so, keep the inline logic -- do NOT refactor to call state.js functions (that would change behavior).

    - cmdMilestoneComplete:
      a) Archives milestone phases
      b) Creates/updates MILESTONES.md
      It uses: safeReadFile, getMilestoneInfo, execGit, fs operations

    - This module will be ~848 lines, which EXCEEDS the 500-line target.
      SOLUTION: Accept the overage for this extraction wave. The alternative (splitting phase operations) would scatter related logic across files. In Phase 4 (testing), complex functions like cmdPhaseRemove can be refactored into smaller sub-functions within lib/phase.js to bring the line count down. For now, correctness > size target.

      If possible, consider splitting cmdMilestoneComplete and cmdValidateConsistency into a separate `lib/milestone.js` (~242 lines) to keep `lib/phase.js` at ~606 lines. Only do this if the split is clean (no circular deps).
  </action>
  <verify>
    Run: node bin/grd-tools.js phases list
    Run: node bin/grd-tools.js validate consistency
    Both must produce valid JSON. (Level 1: Sanity)

    Run: node bin/grd-tools.js roadmap analyze (verify phase data still correct)
    Diff against golden references. (Level 2: Proxy)

    For mutating operations (phase add/complete), verify in a temp directory:
    - Create temp .planning/ with minimal roadmap
    - Run: node bin/grd-tools.js phase add "Test phase"
    - Verify ROADMAP.md updated correctly
    (Level 2: Proxy)
  </verify>
  <done>
    lib/phase.js exists with all phase lifecycle operations exported.
    If split: lib/milestone.js also exists with milestone and validate functions.
    bin/grd-tools.js imports from lib/phase.js (and lib/milestone.js if split).
    All phase, milestone, and validate commands produce identical output.
    cmdPhaseRemove renumbering logic works correctly.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `lib/phase.js` exists
- `node bin/grd-tools.js phases list` produces valid JSON
- `node bin/grd-tools.js validate consistency` produces valid JSON
- No require errors

Level 2 (Proxy):
- Diff phases list, validate consistency against golden references -- identical
- Phase add in temp directory produces expected ROADMAP.md changes
- Phase numbering preserved after extraction

Level 3 (Deferred): DEFER-03-01, DEFER-03-02
</verification>

<success_criteria>
- lib/phase.js exists and exports >= 7 functions
- If split, lib/phase.js <= 650 lines AND lib/milestone.js <= 300 lines
- bin/grd-tools.js reduced by >= 800 additional lines
- All phase/milestone/validate commands produce identical output
- cmdPhaseRemove renumbering logic verified in temp directory
- No circular dependencies between lib/ modules
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-05-SUMMARY.md`
</output>
