---
phase: 08-status-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tests/unit/commands.test.js
  - tests/integration/cli.test.js
  - jest.config.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Unit tests for cmdDashboard cover: normal project, empty project, missing ROADMAP.md, milestone grouping"
    - "Unit tests for cmdPhaseDetail cover: existing phase, nonexistent phase, phase with no plans, phase with summaries"
    - "Unit tests for cmdHealth cover: blockers parsing, deferred validations, velocity computation, risk register, missing STATE.md"
    - "Integration tests verify CLI routes: dashboard, dashboard --raw, phase-detail <N>, phase-detail <N> --raw, health, health --raw"
    - "All new tests pass with npm test"
    - "Coverage thresholds for lib/commands.js still met (>= 80% lines)"
    - "No existing tests broken by new additions"
  artifacts:
    - path: "tests/unit/commands.test.js"
      provides: "Unit tests for cmdDashboard, cmdPhaseDetail, cmdHealth"
      contains: "cmdDashboard"
    - path: "tests/integration/cli.test.js"
      provides: "Integration tests for dashboard, phase-detail, health CLI routes"
      contains: "dashboard"
  key_links:
    - from: "tests/unit/commands.test.js"
      to: "lib/commands.js"
      via: "require and test cmdDashboard/cmdPhaseDetail/cmdHealth"
      pattern: "require.*lib/commands"
    - from: "tests/integration/cli.test.js"
      to: "bin/grd-tools.js"
      via: "child_process exec of CLI commands"
      pattern: "grd-tools.*dashboard|grd-tools.*phase-detail|grd-tools.*health"
---

<objective>
Write unit tests for the three new dashboard commands and integration tests for their CLI routes, ensuring full coverage of normal paths, edge cases, and error handling.

Purpose: Validate that all three commands produce correct output for various project states (complete project, empty project, missing files) and that the CLI routes correctly dispatch to the command functions.

Output: Extended tests/unit/commands.test.js and tests/integration/cli.test.js with new test cases, all passing.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/commands.js
@bin/grd-tools.js
@tests/unit/commands.test.js
@tests/integration/cli.test.js
@tests/helpers/setup.js
@tests/helpers/fixtures.js
@tests/fixtures/planning/STATE.md
@tests/fixtures/planning/ROADMAP.md
@.planning/phases/08-status-dashboard/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for cmdDashboard, cmdPhaseDetail, and cmdHealth</name>
  <files>tests/unit/commands.test.js</files>
  <action>
    Add new describe blocks to the existing tests/unit/commands.test.js file for the three new command functions.

    Use the established test patterns from Phase 4:
    - `captureOutput` from tests/helpers/setup.js to capture JSON output
    - `createFixtureDir` / `cleanupFixtureDir` from tests/helpers/fixtures.js for isolated test directories
    - Parse captured stdout with JSON.parse to verify structure

    **describe('cmdDashboard'):**

    Test cases (minimum 8):
    1. Returns milestones array with correct count from fixture ROADMAP.md
    2. Each milestone contains phases array with number, name, status
    3. Phase status correctly computed: complete (summaries >= plans), planned (plans > 0, summaries = 0), pending (no plans)
    4. Summary object contains total_milestones, total_phases, total_plans, total_summaries counts
    5. Handles missing ROADMAP.md gracefully (returns empty milestones array)
    6. Handles missing .planning/ directory gracefully (no crash)
    7. Includes active phase marker based on STATE.md current position
    8. Includes deferred validation and blocker counts from STATE.md

    For tests requiring a full project structure, use the existing fixture directory that has:
    - `.planning/ROADMAP.md` with milestone and phase headings
    - `.planning/STATE.md` with current position and deferred validations
    - `.planning/phases/01-test/` with plans and summaries
    - `.planning/phases/02-build/` with plans but no summaries

    Enhance fixture if needed: add a `## Risk Register` table to fixture ROADMAP.md and a `## Performance Metrics` table to fixture STATE.md to test health command parsing.

    **describe('cmdPhaseDetail'):**

    Test cases (minimum 6):
    1. Returns correct phase_number and phase_name for existing phase
    2. Plans array contains id, wave, type, status for each plan
    3. Completed plans have duration, tasks, files from SUMMARY frontmatter
    4. Returns error for nonexistent phase number
    5. Returns empty plans array for phase with no PLAN.md files
    6. Detects presence of CONTEXT.md, RESEARCH.md, EVAL.md, VERIFICATION.md, REVIEW.md

    **describe('cmdHealth'):**

    Test cases (minimum 8):
    1. Parses blockers section from STATE.md (count and items)
    2. Parses deferred validations table from STATE.md (total, pending, resolved counts)
    3. Computes velocity from performance metrics table (average duration)
    4. Computes recent_5_avg from last 5 entries in metrics table
    5. Parses risk register from ROADMAP.md (risk, probability, impact columns)
    6. Handles missing STATE.md gracefully
    7. Handles STATE.md with no blockers section
    8. Handles STATE.md with no performance metrics

    **Fixture enhancement:**
    If the existing fixture STATE.md does not have a `## Performance Metrics` table or `## Deferred Validations` table, add them to the fixture. Add in beforeEach using fs.writeFileSync to the temp fixture directory (do not modify the committed fixture files; instead augment them in the test's setup).

    The pattern from existing tests: copy fixtures to temp dir, then modify as needed in beforeEach.
  </action>
  <verify>
    npx jest tests/unit/commands.test.js --verbose 2>&1 | tail -20
    All new test cases pass (Level 1: Sanity)

    npx jest tests/unit/commands.test.js --coverage 2>&1 | grep commands.js
    Coverage for lib/commands.js remains >= 80% lines (Level 1: Sanity)
  </verify>
  <done>
    At least 22 new unit tests added across three describe blocks (cmdDashboard, cmdPhaseDetail, cmdHealth).
    All tests pass. Coverage threshold for lib/commands.js still met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for dashboard, phase-detail, and health CLI routes</name>
  <files>tests/integration/cli.test.js</files>
  <action>
    Add new describe blocks to the existing tests/integration/cli.test.js for the three new CLI routes.

    Use the established integration test pattern:
    - Execute CLI commands via `child_process.execSync` or `child_process.execFileSync`
    - Run `node bin/grd-tools.js <command> [args]` with cwd set to a temp fixture directory
    - Parse stdout as JSON for --raw tests
    - Check exit code 0 for success cases

    **describe('dashboard command'):**

    Test cases (minimum 3):
    1. `dashboard --raw` produces valid JSON with milestones array
    2. `dashboard` (without --raw) produces text output containing milestone names
    3. `dashboard --raw` with empty .planning/ returns empty milestones

    **describe('phase-detail command'):**

    Test cases (minimum 3):
    1. `phase-detail 1 --raw` produces valid JSON with plans array and phase_number
    2. `phase-detail 99 --raw` returns error for nonexistent phase
    3. `phase-detail 1` (without --raw) produces text containing phase name

    **describe('health command'):**

    Test cases (minimum 3):
    1. `health --raw` produces valid JSON with blockers, deferred_validations, velocity keys
    2. `health` (without --raw) produces text output containing "Blockers" and "Velocity" sections
    3. `health --raw` with minimal STATE.md returns zero blockers and empty velocity

    **Integration test setup:**
    Use the same fixture directory approach as existing integration tests. The fixture directory should have a `.planning/` structure with ROADMAP.md, STATE.md, and at least one phase directory with plans/summaries.

    If the existing integration test file uses a shared fixture setup, add the new tests to that same setup. If it creates fixtures per-describe, follow that pattern.

    Ensure the PATH includes the project's bin/ directory so the CLI can be invoked.
  </action>
  <verify>
    npx jest tests/integration/cli.test.js --verbose 2>&1 | tail -30
    All new integration tests pass (Level 1: Sanity)

    npm test 2>&1 | tail -5
    Full test suite passes with no regressions (Level 1: Sanity)
  </verify>
  <done>
    At least 9 new integration tests added for dashboard, phase-detail, and health CLI routes.
    All integration tests pass. Full npm test passes with no regressions.
    Coverage thresholds still met across all modules.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- All new unit tests pass: `npx jest tests/unit/commands.test.js`
- All new integration tests pass: `npx jest tests/integration/cli.test.js`
- Full test suite passes: `npm test`
- No existing tests broken by new additions
- Coverage thresholds still met for lib/commands.js

Level 2 (Proxy):
- cmdDashboard test verifies correct milestone count from fixture ROADMAP.md
- cmdPhaseDetail test verifies correct plan count from fixture phase directory
- cmdHealth test verifies correct deferred validation parsing from fixture STATE.md
- Integration tests verify JSON output is parseable and contains expected keys
</verification>

<success_criteria>
- >= 22 new unit tests across cmdDashboard, cmdPhaseDetail, cmdHealth describe blocks
- >= 9 new integration tests for dashboard, phase-detail, health CLI routes
- All tests pass: npm test exits 0
- lib/commands.js coverage >= 80% lines
- No regressions in any existing test file
- Edge cases covered: missing files, empty directories, nonexistent phases
</success_criteria>

<output>
After completion, create `.planning/phases/08-status-dashboard/08-02-SUMMARY.md`
</output>
