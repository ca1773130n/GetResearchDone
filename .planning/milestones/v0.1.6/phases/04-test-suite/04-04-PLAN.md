---
phase: 04-test-suite
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - tests/integration/cli.test.js
  - tests/integration/golden.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "cli.test.js tests all CLI commands via execFileSync (>= 40 test cases)"
    - "golden.test.js compares CLI output against surviving golden reference files (>= 12 snapshot comparisons)"
    - "All integration tests pass with npm test"
    - "npm test --coverage reports >= 80% line coverage on lib/ modules"
    - "Total test execution time < 60 seconds"
    - "DEFER-03-01 resolved: all commands produce valid output via integration tests"
    - "DEFER-03-02 resolved: golden snapshot tests confirm output unchanged"
    - "DEFER-02-01 resolved: full CLI regression covered by integration tests"
    - "DEFER-02-02 resolved: CLI output unchanged confirmed by golden tests"
  artifacts:
    - path: "tests/integration/cli.test.js"
      provides: "End-to-end CLI integration tests"
      contains: "execFileSync"
    - path: "tests/integration/golden.test.js"
      provides: "Golden reference snapshot comparison tests"
      contains: "golden"
  key_links:
    - from: "tests/integration/cli.test.js"
      to: "bin/grd-tools.js"
      via: "execFileSync"
      pattern: "execFileSync.*grd-tools"
    - from: "tests/integration/golden.test.js"
      to: "tests/golden/output/"
      via: "fs.readFileSync"
      pattern: "golden.*output"
---

<objective>
Write integration tests that exercise the full CLI via execFileSync, compare output against golden references, verify overall coverage meets 80% threshold, and resolve all 4 deferred validations (DEFER-03-01, DEFER-03-02, DEFER-02-01, DEFER-02-02).

Purpose: Integration tests are the final validation layer proving all commands work end-to-end after modularization and hardening. Golden snapshot tests prove output has not changed. This plan resolves all Phase 4 deferred validations.

Output: 2 integration test files with >= 52 test cases, >= 80% overall lib/ coverage, all 4 deferred validations resolved.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@tests/golden/capture.sh
@.planning/phases/04-test-suite/04-01-SUMMARY.md
@.planning/phases/04-test-suite/04-02-SUMMARY.md
@.planning/phases/04-test-suite/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLI integration tests</name>
  <files>tests/integration/cli.test.js</files>
  <action>
    **tests/integration/cli.test.js** -- End-to-end CLI tests via execFileSync:

    Use execFileSync('node', ['path/to/bin/grd-tools.js', ...args], {cwd: fixtureDir})
    to test every CLI command end-to-end. This uses execFileSync (safe, no shell injection)
    as established in the Phase 2 security hardening.

    Helper: `runCLI(args, cwd)` -- execFileSync wrapper that returns {stdout, stderr, exitCode}.
    Catch errors (non-zero exit) and capture stderr from the error object.

    **Read-only command tests** (use shared fixture dir, created in beforeAll):

    1. state load -> valid JSON with config, state, roadmap keys
    2. state get -> returns state content
    3. state get "Key Decisions" -> returns decisions section
    4. state-snapshot -> valid JSON with structured state parse
    5. resolve-model grd-executor -> returns model name
    6. find-phase 1 -> returns phase directory info
    7. generate-slug "Hello World" -> returns slug JSON
    8. current-timestamp -> returns timestamp (--raw: plain text)
    9. current-timestamp full -> JSON with iso, date, time
    10. current-timestamp date -> JSON with date
    11. current-timestamp filename -> JSON with filename-safe ts
    12. verify-path-exists src/index.js -> {exists: true}
    13. verify-path-exists nonexistent -> {exists: false}
    14. frontmatter get <plan> -> returns frontmatter JSON
    15. frontmatter get <plan> --field phase -> returns single field
    16. frontmatter validate <plan> --schema plan -> {valid: true}
    17. verify plan-structure <plan> -> {valid: true}
    18. verify phase-completeness 1 -> complete
    19. verify phase-completeness 2 -> incomplete
    20. verify references <plan> -> reference check results
    21. verify artifacts <plan> -> artifact check results
    22. verify key-links <plan> -> key link results
    23. roadmap get-phase 1 -> phase details
    24. roadmap analyze -> analysis JSON
    25. phase-plan-index 1 -> plan index JSON
    26. history-digest -> digest JSON
    27. summary-extract <summary> -> extracted fields
    28. summary-extract <summary> --fields phase,duration -> filtered fields
    29. progress json -> progress JSON
    30. progress table --raw -> ASCII table text
    31. progress bar --raw -> progress bar text
    32. validate consistency -> consistency check
    33. list-todos -> todo array
    34. phases list -> phases array
    35. phases list --type plan -> filtered phases
    36. phases list --phase 1 -> phase 1 items
    37. phase next-decimal 1 -> next decimal
    38. config-ensure-section -> config with sections
    39. tracker get-config -> tracker config
    40. template fill summary ... -> summary template
    41. template fill plan ... -> plan template
    42-51. init workflows (execute-phase, plan-phase, new-project, resume, verify-work, phase-op, todos, milestone-op, map-codebase, progress)
    52. verify-summary <summary> -> summary validation

    **Mutating command tests** (use per-test isolated fixture dirs):

    53-61. state mutations (patch, advance-plan, record-metric, update-progress, add-decision, add-blocker, resolve-blocker, record-session, update)
    62-63. frontmatter mutations (set, merge)
    64-67. phase mutations (add, insert, remove, complete)
    68. todo complete
    69-70. scaffold operations (context, phase-dir)
    71. config-set

    **Git-dependent tests:**
    72. commit (create temp git repo) -> creates commit
    73. verify commits (temp git repo) -> verifies commits

    For each test, verify:
    - Exit code is 0 (or expected non-zero for error cases)
    - Output is valid JSON (for JSON commands) or expected text
    - Key fields present in JSON output

    Group tests using describe blocks by command category (state, verify, roadmap, etc.).
  </action>
  <verify>
    npm test -- tests/integration/cli.test.js (all pass) (Level 2: Proxy)
    Count test cases: >= 40 (some commands may be grouped)
  </verify>
  <done>
    cli.test.js has >= 40 passing integration tests covering all major CLI commands.
    Each test verifies the command runs end-to-end without crashing.
    JSON output is parsed and validated for key fields.
    Mutating commands verified by reading back disk state.
    DEFER-03-01 and DEFER-02-01 resolved: all commands functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write golden snapshot tests and verify coverage</name>
  <files>tests/integration/golden.test.js</files>
  <action>
    **tests/integration/golden.test.js** -- Golden reference snapshot comparison:

    For each surviving golden output file in tests/golden/output/,
    run the same command against the fixture directory and compare output.

    The surviving golden files (from git status) are:
    - current-timestamp-date.json
    - current-timestamp-filename.json
    - current-timestamp-full.json
    - current-timestamp.txt
    - find-phase.json
    - frontmatter-get-field.json
    - frontmatter-get.json
    - generate-slug.json
    - resolve-model.json
    - state-get-decisions.json
    - state-get.json
    - state-load.json
    - state-snapshot.json
    - verify-path-exists-missing.json
    - verify-path-exists.json

    For each golden file:
    1. Run the corresponding CLI command against the fixture
    2. Parse both golden and actual output as JSON
    3. Compare using a smart diff that:
       - Ignores timestamp fields (iso, date, time, updated, timestamp) as non-deterministic
       - Ignores absolute path fields (they differ between machines)
       - Compares all other fields for exact match

    Implementation:
    - Create `normalizeForSnapshot(obj)` helper that:
      - Replaces ISO timestamps with "TIMESTAMP"
      - Replaces absolute paths with "PATH"
      - Replaces date strings matching YYYY-MM-DD with "DATE"
      - Sorts object keys for consistent comparison
    - Apply normalizeForSnapshot to both golden and actual output
    - Use expect(normalized_actual).toEqual(normalized_golden)

    **Coverage verification:**
    After all tests pass, run:
    ```
    npm test -- --coverage
    ```
    Verify:
    - Overall lib/ line coverage >= 80%
    - If coverage < 80%, identify uncovered functions and add targeted tests
    - Document coverage numbers in output

    **Deferred validation resolution:**
    - DEFER-03-01: "All 40 commands work after modularization" -- Resolved by cli.test.js
    - DEFER-03-02: "CLI JSON output unchanged" -- Resolved by golden.test.js
    - DEFER-02-01: "Full CLI regression" -- Resolved by cli.test.js
    - DEFER-02-02: "CLI output unchanged after hardening" -- Resolved by golden.test.js

    If coverage is below 80%, add targeted tests in this task:
    - Identify the lowest-coverage module
    - Add 5-10 tests for uncovered branches/functions
    - Focus on error paths and edge cases that unit tests may have missed
  </action>
  <verify>
    npm test -- tests/integration/golden.test.js (all pass) (Level 2: Proxy)
    npm test -- --coverage (line coverage >= 80% on lib/) (Level 2: Proxy)
    Verify total test count (all unit + integration): document the number
    Verify total test time < 60 seconds
  </verify>
  <done>
    golden.test.js has >= 12 snapshot comparison tests, all pass.
    Overall lib/ line coverage >= 80% (or documented justification if slightly under).
    Total test execution time < 60 seconds for full suite.
    All 4 deferred validations resolved:
    - DEFER-03-01: All commands work (integration tests pass)
    - DEFER-03-02: JSON output unchanged (golden snapshots match)
    - DEFER-02-01: Full CLI regression (integration tests pass)
    - DEFER-02-02: CLI output unchanged (golden snapshots match)
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Both integration test files discovered and pass
Level 2 (Proxy): >= 52 integration test cases total
Level 2 (Proxy): Golden snapshot tests match actual output for all surviving golden files
Level 2 (Proxy): npm test --coverage shows >= 80% line coverage on lib/
Level 2 (Proxy): Total suite execution < 60 seconds
Level 2 (Proxy): All 4 DEFER validations resolved
</verification>

<success_criteria>
- tests/integration/cli.test.js: >= 40 test cases, all pass
- tests/integration/golden.test.js: >= 12 snapshot tests, all pass
- `npm test` runs full suite (unit + integration) with 0 failures
- `npm test --coverage` shows lib/ line coverage >= 80%
- Total test execution time < 60 seconds
- DEFER-03-01 resolved (all commands work in integration tests)
- DEFER-03-02 resolved (golden snapshots match)
- DEFER-02-01 resolved (full CLI regression passes)
- DEFER-02-02 resolved (output unchanged from golden reference)
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-suite/04-04-SUMMARY.md`
</output>
