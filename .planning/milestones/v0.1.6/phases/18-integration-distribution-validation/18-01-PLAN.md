---
phase: 18-integration-distribution-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/e2e-workflow.test.js
autonomous: true
verification_level: full

must_haves:
  truths:
    - "E2E test chains backend detection -> context init -> quality analysis -> doc drift -> MCP tool call in sequence without errors"
    - "Full test suite passes with 0 failures; total count >= 950 (current: 1243)"
    - "No regressions from v0.1.0 baseline of 858 tests"
  artifacts:
    - path: "tests/integration/e2e-workflow.test.js"
      provides: "End-to-end workflow integration test"
      min_lines: 80
  key_links:
    - from: "tests/integration/e2e-workflow.test.js"
      to: "lib/backend.js"
      via: "require and function call"
      pattern: "require.*backend"
    - from: "tests/integration/e2e-workflow.test.js"
      to: "lib/context.js"
      via: "require and function call"
      pattern: "require.*context"
    - from: "tests/integration/e2e-workflow.test.js"
      to: "lib/cleanup.js"
      via: "require and function call"
      pattern: "require.*cleanup"
    - from: "tests/integration/e2e-workflow.test.js"
      to: "lib/mcp-server.js"
      via: "require and function call"
      pattern: "require.*mcp-server"
---

<objective>
Create an end-to-end integration test that validates all v0.1.1 features work together in sequence, and confirm the full test suite passes with no regressions.

Purpose: Satisfy REQ-20 (Integration & Cross-Feature Validation) by proving backend detection, context init, quality analysis, doc drift, and MCP server all chain correctly.
Output: tests/integration/e2e-workflow.test.js
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/integration/cli.test.js
@lib/backend.js
@lib/context.js
@lib/cleanup.js
@lib/mcp-server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E workflow integration test</name>
  <files>tests/integration/e2e-workflow.test.js</files>
  <action>
    Create an integration test file that validates all v0.1.1 features work together end-to-end.
    The test should exercise real module imports (not CLI subprocess calls) for speed and directness.

    Use a temp directory with .planning/ fixture (like cli.test.js pattern with createTestDir).

    Test groups to implement:

    1. **Backend detection** (~3 tests):
       - Import detectBackend from lib/backend.js
       - Call detectBackend() — verify it returns a valid backend object with { name, confidence } fields
       - Verify getBackendCapabilities() returns capabilities object with expected keys (supports_agent_teams, model_resolution, etc.)
       - Verify resolveModelForAgent works with the detected backend

    2. **Context init enrichment** (~3 tests):
       - Import cmdInitExecutePhase (or similar) from lib/commands.js
       - Call it with the fixture dir and phase "1" — verify it returns JSON with backend-enriched fields (backend_name, backend_capabilities, model)
       - Verify cmdInitPlanPhase similarly returns backend-enriched context
       - Verify cmdInitResume returns backend fields

    3. **Quality analysis pipeline** (~3 tests):
       - Import analyzeCodeQuality, generateCleanupPlan from lib/cleanup.js
       - Call analyzeCodeQuality on a fixture project dir — verify it returns quality report with eslint_summary, dead_exports, file_sizes fields
       - If doc_sync is enabled in config, verify doc_drift field appears in quality report
       - Call generateCleanupPlan with a quality result that has issues above threshold — verify it produces a valid PLAN.md string with frontmatter

    4. **MCP server tool call** (~4 tests):
       - Import McpServer from lib/mcp-server.js
       - Create McpServer instance with fixture dir as cwd
       - Send initialize message — verify protocol handshake (protocolVersion, capabilities, serverInfo)
       - Send tools/list — verify array of tools with length > 50
       - Send tools/call for state_load — verify it returns content with isError=false
       - Send tools/call for detect_backend — verify it returns backend info

    5. **Sequential pipeline** (~2 tests):
       - Chain: detectBackend() -> use result to verify backend name -> cmdInitExecutePhase with that context -> analyzeCodeQuality -> McpServer.handleMessage(tools/call state_load)
       - Each step uses output from previous step to prove integration; all complete without errors
       - Verify the entire chain completes in under 10 seconds

    Use beforeAll/afterAll for fixture setup/cleanup. Total: ~15 tests.

    Follow existing test conventions from tests/integration/cli.test.js:
    - Use os.tmpdir() for temp directories
    - Use fs.cpSync for fixture copying
    - Clean up in afterAll
  </action>
  <verify>
    npx jest tests/integration/e2e-workflow.test.js --no-coverage (Level 3: Full)
    All tests pass. No test takes more than 10 seconds.
  </verify>
  <done>
    E2E workflow test file exists with ~15 tests covering the full feature chain.
    Backend detection -> context init -> quality analysis -> doc drift -> MCP server tool call all validated in sequence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes with no regressions</name>
  <files>tests/integration/e2e-workflow.test.js</files>
  <action>
    Run the full test suite (`npx jest --no-coverage`) and verify:
    1. All tests pass (0 failures)
    2. Total test count >= 950 (v0.1.0 baseline was 858; current is 1243)
    3. New e2e-workflow.test.js tests are included in the count

    If any pre-existing test fails, investigate and fix the root cause.
    Do NOT modify existing tests to make them pass — fix the source code or the new test.

    After confirming, record the final test count in the SUMMARY.md.
  </action>
  <verify>
    npx jest --no-coverage exits 0 (Level 3: Full)
    Total test count printed by Jest >= 950
  </verify>
  <done>
    Full test suite passes. Total count confirmed >= 950. No regressions from v0.1.0 baseline.
  </done>
</task>

</tasks>

<verification>
Level 3 (Full): All integration tests pass; full suite shows 0 failures; test count exceeds v0.1.0 baseline by significant margin.
</verification>

<success_criteria>
- tests/integration/e2e-workflow.test.js exists with ~15 passing tests
- Full test suite passes with 0 failures
- Total test count >= 950 (target exceeded: currently 1243 + new tests)
- E2E chain proves backend -> context -> quality -> drift -> MCP work together
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-distribution-validation/18-01-SUMMARY.md`
</output>
