---
phase: 29-dependency-analysis
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/deps.js
  - tests/unit/deps.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "parseDependsOn extracts phase numbers from roadmap depends_on strings: 'Phase 27' -> ['27'], 'Phase 27, Phase 29' -> ['27','29'], 'Nothing' -> [], null -> []"
    - "buildDependencyGraph returns { nodes: [...], edges: [...] } with one node per phase and one edge per dependency"
    - "computeParallelGroups returns an array of arrays, each inner array is a set of phase numbers that can run concurrently"
    - "Phases with no dependencies appear in the first parallel group"
    - "A phase appears in a group only after all its dependencies have appeared in earlier groups"
    - "detectCycle returns null for acyclic graphs and returns the cycle path array for cyclic graphs"
    - "cmdPhaseAnalyzeDeps reads ROADMAP.md, builds graph, and outputs JSON with nodes, edges, parallel_groups, and has_cycle fields"
    - "cmdPhaseAnalyzeDeps returns an error message containing the cycle path when circular dependencies exist"
  artifacts:
    - path: "lib/deps.js"
      provides: "Dependency analysis module with parseDependsOn, buildDependencyGraph, computeParallelGroups, detectCycle, cmdPhaseAnalyzeDeps"
      exports: ["parseDependsOn", "buildDependencyGraph", "computeParallelGroups", "detectCycle", "cmdPhaseAnalyzeDeps"]
      min_lines: 100
    - path: "tests/unit/deps.test.js"
      provides: "Comprehensive tests for dependency analysis module"
      min_lines: 200
  key_links:
    - from: "lib/deps.js"
      to: "lib/roadmap.js"
      via: "Uses cmdRoadmapAnalyze output for phase data with depends_on fields"
      pattern: "cmdRoadmapAnalyze|roadmap"
    - from: "lib/deps.js"
      to: "lib/utils.js"
      via: "Uses output, error, stripShippedSections utilities"
      pattern: "output|error"
---

<objective>
Implement the core dependency analysis module (lib/deps.js) with TDD: parsing depends_on from roadmap phases, building a dependency graph, computing parallel execution groups via topological sort, and detecting circular dependencies.

Purpose: This module provides the algorithmic foundation for Phase 30's parallel execution. It determines which roadmap phases can run concurrently and which must be sequential.

Output: `lib/deps.js` with pure functions for dependency analysis, `tests/unit/deps.test.js` with comprehensive test coverage.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
@${CLAUDE_PLUGIN_ROOT}/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/roadmap.js
@lib/utils.js
@tests/unit/roadmap.test.js
@tests/helpers/setup.js
@tests/helpers/fixtures.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for dependency analysis functions (RED)</name>
  <files>tests/unit/deps.test.js</files>
  <action>
    Create `tests/unit/deps.test.js` with test cases for all four core functions plus the CLI command function. Use the `captureOutput` and `captureError` helpers from `tests/helpers/setup.js` and the `createFixtureDir`/`cleanupFixtureDir` helpers from `tests/helpers/fixtures.js`.

    **Describe: parseDependsOn**
    This function takes a raw `depends_on` string from ROADMAP.md and returns an array of phase number strings.

    Test cases (6 tests):
    1. **Parses single phase reference** — `'Phase 27'` -> `['27']`
    2. **Parses multiple phase references** — `'Phase 27, Phase 29'` -> `['27', '29']`
    3. **Returns empty array for "Nothing"** — `'Nothing (independent of worktree work)'` -> `[]`
    4. **Returns empty array for null** — `null` -> `[]`
    5. **Returns empty array for empty string** — `''` -> `[]`
    6. **Handles "Phase N" without comma separation** — `'Phase 27 and Phase 29'` -> `['27', '29']`
    7. **Handles decimal phase numbers** — `'Phase 06.1'` -> `['06.1']`

    **Describe: buildDependencyGraph**
    This function takes an array of phase objects (from cmdRoadmapAnalyze-like output, each with `number` and `depends_on` string) and returns `{ nodes, edges }`.

    Test cases (5 tests):
    1. **Builds graph with no dependencies** — 3 phases, all depends_on: null -> 3 nodes, 0 edges
    2. **Builds graph with linear chain** — A->B->C -> 3 nodes, 2 edges
    3. **Builds graph with diamond dependency** — A, B both depend on nothing; C depends on A and B -> 3 nodes, 2 edges
    4. **Nodes include phase number and name** — each node has { id, name } fields
    5. **Edges include from and to fields** — each edge has { from, to } with phase number strings

    **Describe: computeParallelGroups**
    This function takes a graph `{ nodes, edges }` and returns an array of arrays (parallel groups).

    Test cases (6 tests):
    1. **All independent phases in one group** — 3 phases, 0 edges -> `[['27','28','29']]`
    2. **Linear chain produces one phase per group** — A->B->C -> `[['A'],['B'],['C']]` (3 groups, each with 1 phase)
    3. **Diamond produces 3 groups** — A,B independent; C depends on both; D depends on C -> `[['A','B'],['C'],['D']]`
    4. **Complex graph with mixed deps** — using the actual v0.2.0 roadmap structure: 27 (none), 28 (27), 29 (none), 30 (27,29), 31 (27,28,29,30) -> `[['27','29'], ['28','30'], ['31']]`
    5. **Empty graph returns empty groups** — 0 phases -> `[]`
    6. **Single phase with no deps** — 1 phase -> `[['27']]`

    **Describe: detectCycle**
    This function takes a graph `{ nodes, edges }` and returns null if acyclic, or an array of phase numbers forming the cycle path.

    Test cases (4 tests):
    1. **Returns null for acyclic graph** — A->B->C, no cycles -> null
    2. **Detects simple two-node cycle** — A->B, B->A -> returns cycle containing both
    3. **Detects three-node cycle** — A->B, B->C, C->A -> returns cycle path
    4. **Returns null for diamond (no cycle)** — A->C, B->C, no back-edges -> null

    **Describe: cmdPhaseAnalyzeDeps**
    This is the CLI-facing function that calls cmdRoadmapAnalyze internally, builds the graph, and outputs JSON.

    **IMPORTANT:** Every test in this describe block must write its own custom ROADMAP.md (with `**Depends on:**` fields) to the fixture dir's `.planning/` directory. Do NOT rely on the default fixture content from `createFixtureDir()` — it may not contain `**Depends on:**` fields. Each test should call `createFixtureDir()` then write a ROADMAP.md like:
    ```
    ## Milestone v1.0
    ### Phase 1: First
    **Goal:** Do X
    **Depends on:** Nothing
    ### Phase 2: Second
    **Goal:** Do Y
    **Depends on:** Phase 1
    ```

    Test cases (5 tests):
    1. **Returns JSON with nodes, edges, parallel_groups for fixture roadmap** — use createFixtureDir, write a custom ROADMAP.md with `**Depends on:**` fields to `.planning/ROADMAP.md`, call cmdPhaseAnalyzeDeps(fixtureDir, false), parse output
    2. **Returns has_cycle: false for acyclic roadmap** — write acyclic ROADMAP.md to fixture dir, verify has_cycle is false
    3. **Returns error for missing ROADMAP.md** — use nonexistent dir (no ROADMAP.md), expect error output
    4. **Returns cycle path when circular dependency exists** — write a cyclic ROADMAP.md to fixture dir (Phase A depends on B, Phase B depends on A), verify cycle_path field
    5. **parallel_groups contains all phase numbers from roadmap** — write ROADMAP.md with 3+ phases with deps, verify every phase number appears exactly once across all groups

    **Total: ~26 tests**

    All tests should fail because lib/deps.js does not exist yet.
  </action>
  <verify>
    Run tests and confirm they FAIL (RED):
    ```bash
    npx jest tests/unit/deps.test.js --no-coverage 2>&1 | tail -20
    ```
    All tests fail with "Cannot find module" or similar because lib/deps.js doesn't exist.
    Full test suite excluding the new file still passes:
    ```bash
    npx jest --no-coverage --ignore tests/unit/deps.test.js 2>&1 | tail -5
    ```
  </verify>
  <done>
    tests/unit/deps.test.js has ~26 test cases across 5 describe blocks. All new tests fail (RED) because lib/deps.js does not exist. Committed as test commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/deps.js dependency analysis module (GREEN)</name>
  <files>lib/deps.js</files>
  <action>
    Create `lib/deps.js` with the following structure. Follow codebase conventions: JSDoc comments, `output()`/`error()` for CLI output, pure functions for the core algorithms.

    **Module header:**
    ```javascript
    /**
     * GRD Dependency Analysis — Phase dependency graph, parallel group computation, cycle detection
     *
     * Provides tooling for analyzing ROADMAP.md phase dependencies to determine
     * which phases can execute in parallel vs. which must be sequential.
     *
     * Depends on: lib/utils.js (output, error), lib/roadmap.js (cmdRoadmapAnalyze)
     */
    const { output, error } = require('./utils');
    const { cmdRoadmapAnalyze } = require('./roadmap');
    ```

    **Function 1: parseDependsOn(dependsOnStr)**
    - Input: raw string from `**Depends on:**` field in ROADMAP.md (e.g., "Phase 27, Phase 29", "Nothing", null)
    - Output: array of phase number strings (e.g., `['27', '29']`, `[]`)
    - Implementation:
      - Return `[]` for null, undefined, empty string
      - Return `[]` if string contains "nothing" (case-insensitive)
      - Use regex `/Phase\s+(\d+(?:\.\d+)?)/gi` to extract all phase number references
      - Return array of matched phase number strings

    **Function 2: buildDependencyGraph(phases)**
    - Input: array of objects with `{ number, name, depends_on }` (the depends_on is the raw string)
    - Output: `{ nodes: [{id, name}], edges: [{from, to}] }`
    - Implementation:
      - Map each phase to a node: `{ id: phase.number, name: phase.name }`
      - For each phase, call `parseDependsOn(phase.depends_on)` to get deps
      - For each dep, create edge: `{ from: dep, to: phase.number }` (dep must complete before phase)
      - Filter edges to only include deps whose `from` exists in the node set (ignore dangling refs)

    **Function 3: computeParallelGroups(graph)**
    - Input: `{ nodes, edges }` from buildDependencyGraph
    - Output: array of arrays (each inner array = set of phase IDs that can run together)
    - Implementation (Kahn's algorithm / topological sort by levels):
      - Build adjacency list and in-degree map from edges
      - Initialize: all nodes with in-degree 0 go into first group
      - Iteratively: remove current group's nodes, decrement in-degrees for their dependents, collect next group (new in-degree 0 nodes)
      - Continue until no nodes left (or cycle detected — but cycle detection is separate)
      - Sort phase IDs within each group for deterministic output
      - Return empty array for empty graph

    **Function 4: detectCycle(graph)**
    - Input: `{ nodes, edges }` from buildDependencyGraph
    - Output: null if acyclic, or array of phase number strings forming the cycle (e.g., `['27', '28', '27']`)
    - Implementation (DFS-based cycle detection):
      - Build adjacency list (forward edges: from -> [to])
      - Track node states: unvisited, visiting (in current DFS path), visited
      - For each unvisited node, run DFS
      - If DFS encounters a "visiting" node, reconstruct cycle path from the stack
      - Return null if all nodes complete DFS without finding a back-edge

    **Function 5: cmdPhaseAnalyzeDeps(cwd, raw)**
    - CLI command function following the established `cmd*` pattern
    - Implementation:
      1. Call `cmdRoadmapAnalyze(cwd, false)` to get phase data — this reuses the existing roadmap parser which already extracts `number`, `name`, and `depends_on` for each phase
      2. Capture its JSON output by temporarily intercepting `process.stdout.write` (same pattern used in tests with `captureOutput`), then `JSON.parse` the captured string
      3. If the result has an `error` field or empty `phases` array with no phases, call `output({ error: 'ROADMAP.md not found or empty' }, raw, '')` and return
      4. Extract the `phases` array from the parsed output — each phase already has `{ number, name, depends_on }` fields
      5. Build graph with `buildDependencyGraph(phases)`
      6. Check for cycles with `detectCycle(graph)`
      7. If cycle found: output `{ error: 'Circular dependency detected', cycle_path, has_cycle: true, nodes: graph.nodes, edges: graph.edges }`
      8. If no cycle: compute parallel groups with `computeParallelGroups(graph)`, output `{ nodes: graph.nodes, edges: graph.edges, parallel_groups, has_cycle: false, phase_count: graph.nodes.length, group_count: parallel_groups.length }`
    - Use `output()` for JSON and raw text output (matching codebase pattern)
    - IMPORTANT: By calling `cmdRoadmapAnalyze` internally, this satisfies the key_link pattern `cmdRoadmapAnalyze|roadmap` and avoids duplicating ROADMAP.md parsing logic

    **Exports:**
    ```javascript
    module.exports = {
      parseDependsOn,
      buildDependencyGraph,
      computeParallelGroups,
      detectCycle,
      cmdPhaseAnalyzeDeps,
    };
    ```

    After implementation, run all tests to verify GREEN:
    ```bash
    npx jest tests/unit/deps.test.js --no-coverage
    npx jest --no-coverage
    ```
  </action>
  <verify>
    Run tests and confirm they PASS (GREEN):
    ```bash
    npx jest tests/unit/deps.test.js --no-coverage 2>&1 | tail -20
    ```
    All ~26 new tests pass.

    Full suite has no regressions:
    ```bash
    npx jest --no-coverage 2>&1 | tail -5
    ```
    All 1,465+ existing tests still pass.
  </verify>
  <done>
    lib/deps.js implemented with parseDependsOn, buildDependencyGraph, computeParallelGroups, detectCycle, cmdPhaseAnalyzeDeps. All ~26 tests pass. Full suite passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): lib/deps.js exists, exports all 5 functions. tests/unit/deps.test.js passes.
Level 2 (Proxy): parseDependsOn correctly parses all depends_on string variants from ROADMAP.md. computeParallelGroups produces correct grouping for the v0.2.0 roadmap structure (27,29 parallel; 28,30 parallel; 31 last). detectCycle finds cycles and returns path. Full test suite passes.
Level 3 (Deferred): End-to-end parallel execution using parallel_groups output to spawn teammate agents (Phase 30/31).
</verification>

<success_criteria>
- lib/deps.js exports parseDependsOn, buildDependencyGraph, computeParallelGroups, detectCycle, cmdPhaseAnalyzeDeps
- parseDependsOn handles all depends_on string formats from ROADMAP.md
- buildDependencyGraph produces { nodes, edges } from phase array
- computeParallelGroups returns correct topological level grouping via Kahn's algorithm
- detectCycle returns null for acyclic, cycle path array for cyclic graphs
- cmdPhaseAnalyzeDeps calls cmdRoadmapAnalyze internally (not re-parsing ROADMAP.md) and outputs structured JSON
- Note: `depends_on` field parsing in ROADMAP.md is already handled by `cmdRoadmapAnalyze` in lib/roadmap.js (lines 343-344), which extracts `**Depends on:**` into each phase object. This plan reuses that parsing rather than duplicating it, satisfying the "parsed by roadmap module" requirement.
- ~26 tests passing, zero regressions on full suite (1,465+)
</success_criteria>

<output>
After completion, create `.planning/phases/29-dependency-analysis/29-01-SUMMARY.md`
</output>
