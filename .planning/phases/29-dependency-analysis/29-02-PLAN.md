---
phase: 29-dependency-analysis
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - bin/grd-tools.js
  - lib/mcp-server.js
  - tests/unit/deps.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "grd-tools phase analyze-deps reads ROADMAP.md and returns JSON with nodes, edges, parallel_groups, has_cycle"
    - "CLI subcommand 'phase analyze-deps' is routed correctly in bin/grd-tools.js"
    - "PHASE_SUBS constant includes 'analyze-deps' alongside existing subcommands"
    - "MCP tool grd_phase_analyze_deps is registered in COMMAND_DESCRIPTORS with correct execute function"
    - "CLI error handling: missing ROADMAP.md returns structured error, not crash"
    - "MCP tool calls cmdPhaseAnalyzeDeps(cwd, false) matching established dispatch pattern"
  artifacts:
    - path: "bin/grd-tools.js"
      provides: "CLI routing for phase analyze-deps subcommand"
      contains: "analyze-deps"
    - path: "lib/mcp-server.js"
      provides: "MCP tool descriptor for grd_phase_analyze_deps"
      contains: "grd_phase_analyze_deps"
    - path: "tests/unit/deps.test.js"
      provides: "Integration tests verifying CLI routing works end-to-end"
      min_lines: 250
  key_links:
    - from: "bin/grd-tools.js"
      to: "lib/deps.js"
      via: "require and command routing for analyze-deps"
      pattern: "cmdPhaseAnalyzeDeps"
    - from: "lib/mcp-server.js"
      to: "lib/deps.js"
      via: "COMMAND_DESCRIPTORS execute function dispatch"
      pattern: "cmdPhaseAnalyzeDeps"
---

<objective>
Wire the dependency analysis module into the CLI router and MCP server, adding the `phase analyze-deps` subcommand and `grd_phase_analyze_deps` MCP tool descriptor.

Purpose: Makes dependency analysis accessible via the standard GRD CLI (`grd-tools phase analyze-deps`) and MCP protocol, so execute-phase orchestrators and users can query dependency information programmatically.

Output: Updated `bin/grd-tools.js` with analyze-deps routing, updated `lib/mcp-server.js` with MCP tool descriptor, integration tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-dependency-analysis/29-01-SUMMARY.md

# Files to modify
@bin/grd-tools.js
@lib/mcp-server.js
@tests/unit/deps.test.js

# Module to integrate
@lib/deps.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire phase analyze-deps into CLI router and add MCP descriptor</name>
  <files>bin/grd-tools.js, lib/mcp-server.js</files>
  <action>
    **Part A: bin/grd-tools.js — Add analyze-deps subcommand**

    1. Add import of `cmdPhaseAnalyzeDeps` from `../lib/deps` at the top of the file (after existing requires):
       ```javascript
       const { cmdPhaseAnalyzeDeps } = require('../lib/deps');
       ```

    2. Add `'analyze-deps'` to the `PHASE_SUBS` constant array in `routeCommand`:
       ```javascript
       const PHASE_SUBS = ['next-decimal', 'add', 'insert', 'remove', 'complete', 'analyze-deps'];
       ```

    3. Add routing in the `phase` case block, after the `else if (sub === 'complete')` block:
       ```javascript
       else if (sub === 'analyze-deps') {
         cmdPhaseAnalyzeDeps(cwd, raw);
       }
       ```

    Note: `analyze-deps` takes NO arguments (it reads ROADMAP.md from cwd). No `validatePhaseArg` needed.

    **Part B: lib/mcp-server.js — Add MCP tool descriptor**

    1. Add import at the top of mcp-server.js (after existing requires):
       ```javascript
       const { cmdPhaseAnalyzeDeps } = require('./deps');
       ```

    2. Add a new entry to the COMMAND_DESCRIPTORS array, in the "Phase commands" section (near the existing `grd_phase_add`, `grd_phase_remove`, etc.):
       ```javascript
       {
         name: 'grd_phase_analyze_deps',
         description: 'Analyze roadmap phase dependencies and identify parallel execution groups',
         params: [],
         execute: (cwd, _args) => cmdPhaseAnalyzeDeps(cwd, false),
       },
       ```

    The tool takes no parameters because it reads ROADMAP.md from the project directory. This matches the pattern of `grd_roadmap_analyze` and `grd_worktree_list` which also take no params.

    After wiring, verify:
    ```bash
    node bin/grd-tools.js phase analyze-deps 2>&1 | head -20
    ```
    Should return JSON with nodes, edges, parallel_groups from the current ROADMAP.md.
  </action>
  <verify>
    CLI responds correctly:
    ```bash
    node bin/grd-tools.js phase analyze-deps 2>&1 | head -20
    ```
    Should output JSON with nodes, edges, parallel_groups, has_cycle fields. (Level 1: Sanity)

    Existing tests not broken:
    ```bash
    npx jest --no-coverage 2>&1 | tail -5
    ```
    All 1,465+ existing tests pass. (Level 2: Proxy)
  </verify>
  <done>
    bin/grd-tools.js routes `phase analyze-deps` to cmdPhaseAnalyzeDeps. MCP descriptor `grd_phase_analyze_deps` registered. CLI outputs structured dependency graph JSON from current ROADMAP.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI integration tests for phase analyze-deps</name>
  <files>tests/unit/deps.test.js</files>
  <action>
    Add a new describe block to the existing `tests/unit/deps.test.js` (created in Plan 01) to cover CLI integration scenarios. These tests verify the end-to-end path from the command function through to structured JSON output.

    **Describe: CLI integration — phase analyze-deps**

    Test cases (4-5 tests):

    1. **CLI outputs valid JSON with expected fields** — Create a fixture dir with a ROADMAP.md containing 3 phases with dependencies. Call `cmdPhaseAnalyzeDeps(fixtureDir, false)`. Capture stdout. Parse JSON. Verify: `nodes` is array, `edges` is array, `parallel_groups` is array of arrays, `has_cycle` is false.

    2. **Parallel groups match expected structure for v0.2.0 layout** — Create a fixture with the v0.2.0 roadmap dependency structure:
       - Phase 27: depends on nothing
       - Phase 28: depends on Phase 27
       - Phase 29: depends on nothing
       - Phase 30: depends on Phase 27, Phase 29
       - Phase 31: depends on Phase 27, Phase 28, Phase 29, Phase 30
       Verify parallel_groups = [['27','29'], ['28','30'], ['31']] (or equivalent sorted order).

    3. **Raw mode outputs human-readable text** — Call with `raw=true`, verify output is plain text (not JSON), contains phase numbers and group information.

    4. **Cycle detection returns error in output** — Create a fixture with circular deps: Phase 1 depends on Phase 2, Phase 2 depends on Phase 1. Verify output has `has_cycle: true` and `cycle_path` containing both phase numbers.

    5. **MCP descriptor exists in COMMAND_DESCRIPTORS** — Import `COMMAND_DESCRIPTORS` or the tool definition builder from mcp-server.js and verify a tool named `grd_phase_analyze_deps` exists with empty params array. (If COMMAND_DESCRIPTORS is not exported, skip this test and rely on the Sanity check of running the CLI.)

    For creating fixture ROADMAPs, write a minimal ROADMAP.md string with `### Phase N:` headers and `**Depends on:**` fields, save to the fixture dir's `.planning/ROADMAP.md`.

    After adding tests:
    ```bash
    npx jest tests/unit/deps.test.js --no-coverage
    npx jest --no-coverage
    ```
  </action>
  <verify>
    All deps tests pass:
    ```bash
    npx jest tests/unit/deps.test.js --no-coverage 2>&1 | tail -20
    ```
    All ~30+ tests pass (Plan 01's ~26 + Plan 02's ~5 new).

    Full suite passes:
    ```bash
    npx jest --no-coverage 2>&1 | tail -5
    ```
    All 1,465+ existing tests + new deps tests pass with zero regressions. (Level 2: Proxy)
  </verify>
  <done>
    tests/unit/deps.test.js has ~30+ total tests including CLI integration tests. bin/grd-tools.js routes phase analyze-deps. MCP descriptor registered. Full suite passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `grd-tools phase analyze-deps` returns JSON (not "Unknown subcommand"). MCP descriptor exists.
Level 2 (Proxy): CLI returns correct parallel_groups for v0.2.0 roadmap structure. All ~30+ deps tests pass. Full suite passes with zero regressions.
Level 3 (Deferred): execute-phase orchestrator uses analyze-deps output to spawn parallel teammate agents (Phase 30/31).
</verification>

<success_criteria>
- `grd-tools phase analyze-deps` returns JSON with nodes, edges, parallel_groups, has_cycle
- PHASE_SUBS includes 'analyze-deps'
- MCP tool grd_phase_analyze_deps registered with empty params
- CLI integration tests verify end-to-end correctness
- v0.2.0 roadmap structure produces expected parallel grouping
- ~30+ total deps tests pass, zero regressions on full suite (1,465+)
</success_criteria>

<output>
After completion, create `.planning/phases/29-dependency-analysis/29-02-SUMMARY.md`
</output>
