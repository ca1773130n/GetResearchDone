---
phase: 04-test-suite
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/unit/phase.test.js
  - tests/unit/tracker.test.js
  - tests/unit/context.test.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "phase.test.js covers phases list, phase add/insert/remove/complete, milestone complete, validate consistency (>= 14 test cases)"
    - "tracker.test.js covers loadTrackerConfig, loadTrackerMapping, cmdTracker get-config, and error paths (>= 8 test cases)"
    - "context.test.js covers init execute-phase, plan-phase, resume, todos, progress, and research workflow dispatch (>= 10 test cases)"
    - "All 3 test files pass with npm test"
    - "Tracker tests do not require external services (GitHub CLI, Jira)"
    - "Phase mutation tests verify disk state after add/insert/remove operations"
  artifacts:
    - path: "tests/unit/phase.test.js"
      provides: "Unit tests for lib/phase.js"
      contains: "describe"
    - path: "tests/unit/tracker.test.js"
      provides: "Unit tests for lib/tracker.js"
      contains: "describe"
    - path: "tests/unit/context.test.js"
      provides: "Unit tests for lib/context.js"
      contains: "describe"
  key_links:
    - from: "tests/unit/phase.test.js"
      to: "lib/phase.js"
      via: "require"
      pattern: "require.*lib/phase"
    - from: "tests/unit/tracker.test.js"
      to: "lib/tracker.js"
      via: "require"
      pattern: "require.*lib/tracker"
    - from: "tests/unit/context.test.js"
      to: "lib/context.js"
      via: "require"
      pattern: "require.*lib/context"
---

<objective>
Write unit tests for the three most complex lib/ modules: phase.js (phase lifecycle with roadmap mutations), tracker.js (issue tracker sync), and context.js (init workflow context loading). These are the hardest modules to test due to complex file I/O and external dependencies.

Purpose: Complete unit test coverage for all 10 lib/ modules. Tracker tests focus on configuration parsing and error paths (no external service calls). Context tests focus on the context assembly logic.

Output: 3 passing test files with >= 32 test cases, completing 10/10 lib/ module coverage.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/phase.js
@lib/tracker.js
@lib/context.js
@.planning/phases/04-test-suite/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for phase.js</name>
  <files>tests/unit/phase.test.js</files>
  <action>
    **tests/unit/phase.test.js** — Test phase lifecycle operations:

    All mutating tests must use createFixtureDir/cleanupFixtureDir for isolation.
    Use captureOutput from tests/helpers/setup.js for cmd* functions.

    1. cmdPhasesList:
       - Lists all phases from fixture ROADMAP.md
       - With --type plan: filters to plan-type items
       - With --phase 1: filters to phase 1 items only
       - Returns JSON array of phase objects

    2. cmdPhaseAdd:
       - Adds a new phase to ROADMAP.md after existing phases
       - Creates phase directory on disk
       - Verify ROADMAP.md contains new phase entry after operation
       - Verify directory exists: .planning/phases/{NN}-{slug}/

    3. cmdPhaseInsert:
       - Inserts phase at specified position (after phase 1)
       - Renumbers subsequent phases
       - Verify ROADMAP.md has correct phase order
       - Verify directories renamed on disk

    4. cmdPhaseRemove:
       - Removes phase from ROADMAP.md (with --force)
       - Renumbers subsequent phases
       - Verify ROADMAP.md no longer contains removed phase

    5. cmdPhaseComplete:
       - Marks phase as complete in ROADMAP.md
       - Updates plan checkmarks ([ ] -> [x])
       - Verify STATE.md active phase advances

    6. cmdMilestoneComplete:
       - Marks milestone as complete
       - Test with fixture that has completable milestone

    7. cmdValidateConsistency:
       - With consistent state: returns {valid: true}
       - Detect mismatches: e.g., phase directory exists but not in ROADMAP.md
  </action>
  <verify>
    npm test -- tests/unit/phase.test.js (all pass) (Level 1: Sanity)
    Verify no leftover temp directories after test run
  </verify>
  <done>
    phase.test.js has >= 14 passing test cases covering all 7 exported functions.
    All mutating operations verified by reading back disk state.
    Tests properly clean up temp directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for tracker.js and context.js</name>
  <files>tests/unit/tracker.test.js, tests/unit/context.test.js</files>
  <action>
    **tests/unit/tracker.test.js** — Test tracker operations (NO external service calls):

    Tracker is the hardest module to unit test because it calls GitHub CLI (gh) or
    MCP Atlassian tools. Strategy: test configuration parsing, mapping, and error paths only.
    Skip tests that require actual gh/Jira connectivity.

    1. loadTrackerConfig:
       - With config that has tracker section: returns tracker config
       - With config that has no tracker: returns null or default
       - With config that has provider "github": returns github-specific config

    2. loadTrackerMapping / saveTrackerMapping:
       - Loads TRACKER.md mapping file
       - Returns empty mapping when TRACKER.md doesn't exist
       - saveTrackerMapping writes mapping to disk (verify by read back)

    3. cmdTracker get-config:
       - Returns tracker configuration JSON
       - With no tracker configured: returns appropriate response

    4. Error paths:
       - Invalid subcommand: returns error
       - Missing required arguments: returns error
       - Test tracker subcommands that can run without external services

    5. createTracker / createGitHubTracker:
       - Can be called but skip actual API calls
       - Test that configuration parsing produces correct tracker object shape

    **tests/unit/context.test.js** — Test init workflow context loading:

    These functions assemble large context JSON objects from .planning/ files.
    Test that they produce correct structure with fixture data.

    1. cmdInitExecutePhase:
       - With fixture phase 1: returns JSON with config, state, roadmap excerpts, phase plans
       - Returns error for nonexistent phase

    2. cmdInitPlanPhase:
       - Returns JSON with planner_model, researcher_model, phase_dir, etc.
       - Includes phase number and has_research/has_context flags

    3. cmdInitNewProject:
       - Returns JSON with new project context (config, templates)

    4. cmdInitResume:
       - Returns JSON with session continuity from STATE.md

    5. cmdInitVerifyWork:
       - Returns JSON with verification context for given phase

    6. cmdInitPhaseOp:
       - Returns JSON with phase operation context

    7. cmdInitTodos:
       - Returns JSON with pending/completed todos

    8. cmdInitMilestoneOp:
       - Returns JSON with milestone context

    9. cmdInitMapCodebase / cmdInitProgress:
       - Returns JSON with codebase/progress context

    10. cmdInitResearchWorkflow:
        - Dispatches to correct research workflow type (survey, deep-dive, etc.)
        - Returns context for research workflows

    Note: Some init commands may produce large JSON. Focus on testing that:
    - The output is valid JSON (parsed without error)
    - Key fields are present (config, state sections)
    - Error cases return proper error messages
  </action>
  <verify>
    npm test -- tests/unit/tracker.test.js (all pass) (Level 1: Sanity)
    npm test -- tests/unit/context.test.js (all pass) (Level 1: Sanity)
    npm test -- tests/unit/ (all 10 unit test files pass) (Level 2: Proxy)
  </verify>
  <done>
    tracker.test.js has >= 8 passing test cases covering config/mapping/error paths.
    context.test.js has >= 10 passing test cases covering key init workflows.
    All 10 unit test files pass together: `npm test -- tests/unit/`.
    No external service calls made during any test.
    Total unit test count across all 10 files: >= 120 test cases.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All 3 new test files discovered by Jest and pass
Level 1 (Sanity): No external service calls (gh, Jira) during tests
Level 2 (Proxy): >= 32 new test cases across 3 files (bringing total to >= 120)
Level 2 (Proxy): All 10 lib/ modules now have corresponding test files
Level 2 (Proxy): `npm test -- tests/unit/` runs all 10 test files together
</verification>

<success_criteria>
- tests/unit/phase.test.js: >= 14 test cases, all pass
- tests/unit/tracker.test.js: >= 8 test cases, all pass
- tests/unit/context.test.js: >= 10 test cases, all pass
- All 10 unit test files run together without failures
- No external service calls during any test
- Phase mutation tests verify disk state (not just return values)
- Total test execution < 30 seconds for all 10 unit test files
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-suite/04-03-SUMMARY.md`
</output>
