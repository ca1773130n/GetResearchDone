---
phase: 33-lib-module-migration
plan: 04
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - lib/context.js
  - lib/cleanup.js
  - lib/roadmap.js
  - lib/tracker.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "lib/context.js has zero occurrences of `path.join(cwd, '.planning', 'phases')`, `path.join(cwd, '.planning', 'quick')`, `path.join(cwd, '.planning', 'todos')`, `path.join(cwd, '.planning', 'codebase')`, `path.join(cwd, '.planning', 'milestones')` — all replaced with paths.js calls"
    - "lib/context.js init function output JSON includes `phases_dir`, `research_dir`, `codebase_dir`, `quick_dir`, `todos_dir` fields pointing to milestone-scoped directories (via paths.js with backward-compat fallback)"
    - "lib/cleanup.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` — replaced with paths.js calls"
    - "lib/roadmap.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` — replaced with paths.js calls"
    - "lib/tracker.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` — replaced with paths.js calls"
    - "pathExistsInternal calls for `.planning/codebase`, `.planning/research/...`, `.planning/todos/...`, `.planning/phases` are updated to use paths.js-resolved paths"
    - "All 1,608+ existing tests continue to pass with zero regressions"
  artifacts:
    - path: "lib/context.js"
      provides: "Context/init module fully migrated to paths.js with enriched JSON output"
    - path: "lib/cleanup.js"
      provides: "Cleanup module migrated to paths.js"
    - path: "lib/roadmap.js"
      provides: "Roadmap module migrated to paths.js"
    - path: "lib/tracker.js"
      provides: "Tracker module migrated to paths.js"
  key_links:
    - from: "lib/context.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
    - from: "lib/cleanup.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
    - from: "lib/roadmap.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
    - from: "lib/tracker.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
---

<objective>
Migrate all hardcoded `.planning/` subdirectory path constructions in `lib/context.js` (15+ occurrences), `lib/cleanup.js` (2 occurrences), `lib/roadmap.js` (2 occurrences), and `lib/tracker.js` (2 occurrences) to use `paths.js` function calls. Additionally, enrich init function JSON output with milestone-scoped path fields (REQ-56).

Purpose: `lib/context.js` is the most complex migration target because it contains both path constructions AND output JSON fields that expose directory paths to consuming commands/agents. Every `cmdInit*` function must expose `phases_dir`, `research_dir`, `codebase_dir`, `quick_dir`, and `todos_dir` in their JSON output so that downstream consumers get correct milestone-scoped paths.

Output: Updated `lib/context.js`, `lib/cleanup.js`, `lib/roadmap.js`, `lib/tracker.js` with all path constructions delegated to `paths.js`
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lib-module-migration/33-01-PLAN.md — backward-compat fallback

@lib/paths.js — centralized path module
@lib/context.js — target module (15+ occurrences + REQ-56 JSON enrichment)
@lib/cleanup.js — target module (2 occurrences)
@lib/roadmap.js — target module (2 occurrences)
@lib/tracker.js — target module (2 occurrences)
@tests/unit/context.test.js — must still pass
@tests/unit/cleanup.test.js — must still pass
@tests/unit/roadmap.test.js — must still pass
@tests/unit/tracker.test.js — must still pass
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/context.js path constructions and enrich init JSON output (REQ-56)</name>
  <files>lib/context.js</files>
  <action>
    Add at the top of `lib/context.js` (after existing requires):
    ```javascript
    const {
      phasesDir: getPhasesDirPath,
      researchDir: getResearchDirPath,
      codebaseDir: getCodebaseDirPath,
      todosDir: getTodosDirPath,
      quickDir: getQuickDirPath,
      milestonesDir: getMilestonesDirPath,
    } = require('./paths');
    ```

    **Part A: Migrate path.join() constructions**

    **cmdInitNewProject (lines ~319, ~327):**
    - Line ~319: Replace `has_codebase_map: pathExistsInternal(cwd, '.planning/codebase'),`
      With `has_codebase_map: pathExistsInternal(cwd, path.relative(cwd, getCodebaseDirPath(cwd))),`
      Note: `pathExistsInternal(cwd, relPath)` joins cwd+relPath, so pass the relative portion.
      Actually, simpler: `has_codebase_map: fs.existsSync(getCodebaseDirPath(cwd)),`
      Check how `pathExistsInternal` works first. If it does `path.join(cwd, relPath)`, then we need to pass the relative path from cwd. Since the backward-compat fallback may return `.planning/codebase` or `.planning/milestones/.../codebase`, use: `has_codebase_map: fs.existsSync(getCodebaseDirPath(cwd)),`
    - Line ~327: Same pattern for `!pathExistsInternal(cwd, '.planning/codebase')`
      With `!fs.existsSync(getCodebaseDirPath(cwd))`

    **cmdInitNewMilestone (lines ~357, ~358):**
    - Line ~357: Replace `const milestonesDir = path.join(cwd, '.planning', 'milestones');`
      With `const milestonesDir = getMilestonesDirPath(cwd);`
    - Line ~358: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdInitQuick (lines ~444, ~479, ~480):**
    - Line ~444: Replace `const quickDir = path.join(cwd, '.planning', 'quick');`
      With `const quickDir = getQuickDirPath(cwd);`
    - Line ~479: Replace `quick_dir: '.planning/quick',`
      With `quick_dir: path.relative(cwd, quickDir),`
    - Line ~480: Replace `task_dir: slug ? \`.planning/quick/${nextNum}-${slug}\` : null,`
      With `task_dir: slug ? path.relative(cwd, path.join(quickDir, \`${nextNum}-${slug}\`)) : null,`

    **cmdInitTodos (lines ~629, ~651, ~675, ~676, ~680, ~681):**
    - Line ~629: Replace `const pendingDir = path.join(cwd, '.planning', 'todos', 'pending');`
      With:
      ```javascript
      const todosBase = getTodosDirPath(cwd);
      const pendingDir = path.join(todosBase, 'pending');
      ```
    - Line ~651: Replace `path: path.join('.planning', 'todos', 'pending', file),`
      With `path: path.relative(cwd, path.join(pendingDir, file)),`
    - Line ~675: Replace `pending_dir: '.planning/todos/pending',`
      With `pending_dir: path.relative(cwd, pendingDir),`
    - Line ~676: Replace `completed_dir: '.planning/todos/completed',`
      With `completed_dir: path.relative(cwd, path.join(todosBase, 'completed')),`
    - Line ~680: Replace `todos_dir_exists: pathExistsInternal(cwd, '.planning/todos'),`
      With `todos_dir_exists: fs.existsSync(todosBase),`
    - Line ~681: Replace `pending_dir_exists: pathExistsInternal(cwd, '.planning/todos/pending'),`
      With `pending_dir_exists: fs.existsSync(pendingDir),`

    **cmdInitMilestoneOp (lines ~701, ~754):**
    - Line ~701: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`
    - Line ~754: Replace `phases_dir_exists: pathExistsInternal(cwd, '.planning/phases'),`
      With `phases_dir_exists: fs.existsSync(phasesDir),`

    **cmdInitMapCodebase (lines ~771, ~791, ~799):**
    - Line ~771: Replace `const codebaseDir = path.join(cwd, '.planning', 'codebase');`
      With `const codebaseDir = getCodebaseDirPath(cwd);`
    - Line ~791: Replace `codebase_dir: '.planning/codebase',`
      With `codebase_dir: path.relative(cwd, codebaseDir),`
    - Line ~799: Replace `codebase_dir_exists: pathExistsInternal(cwd, '.planning/codebase'),`
      With `codebase_dir_exists: fs.existsSync(codebaseDir),`

    **cmdInitProgress (lines ~818, ~854):**
    - Line ~818: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`
    - Line ~854: Replace `directory: path.join('.planning', 'phases', dir),`
      With `directory: path.relative(cwd, path.join(phasesDir, dir)),`

    **cmdInitResearch (lines ~962, ~963, ~964):**
    - Line ~962: Replace `landscape_exists: pathExistsInternal(cwd, '.planning/research/LANDSCAPE.md'),`
      With `landscape_exists: fs.existsSync(path.join(getResearchDirPath(cwd), 'LANDSCAPE.md')),`
    - Line ~963: Replace `papers_exists: pathExistsInternal(cwd, '.planning/research/PAPERS.md'),`
      With `papers_exists: fs.existsSync(path.join(getResearchDirPath(cwd), 'PAPERS.md')),`
    - Line ~964: Replace `benchmarks_exists: pathExistsInternal(cwd, '.planning/research/BENCHMARKS.md'),`
      With `benchmarks_exists: fs.existsSync(path.join(getResearchDirPath(cwd), 'BENCHMARKS.md')),`

    **Part B: Enrich init function JSON output (REQ-56)**

    Add `phases_dir`, `research_dir`, `codebase_dir`, `quick_dir`, and `todos_dir` fields to the result objects of ALL init functions that don't already have them. Use relative paths from cwd:

    For `cmdInitExecutePhase`, `cmdInitPlanPhase`, `cmdInitNewProject`, `cmdInitNewMilestone`, `cmdInitMilestoneOp`, `cmdInitProgress`, `cmdInitResearch`, `cmdInitVerifyWork`, and any other `cmdInit*` functions — add these fields to the result object:
    ```javascript
    // Milestone-scoped paths (REQ-56)
    phases_dir: path.relative(cwd, getPhasesDirPath(cwd)),
    research_dir: path.relative(cwd, getResearchDirPath(cwd)),
    codebase_dir: path.relative(cwd, getCodebaseDirPath(cwd)),
    quick_dir: path.relative(cwd, getQuickDirPath(cwd)),
    todos_dir: path.relative(cwd, getTodosDirPath(cwd)),
    ```

    Only add fields that don't already exist (avoid duplicating fields like `codebase_dir` in `cmdInitMapCodebase` which already has it from Part A).

    Run tests after each major change:
    ```bash
    npx jest tests/unit/context.test.js --no-coverage
    ```

    Final full suite:
    ```bash
    npm test
    ```
  </action>
  <verify>
    grep -n "path.join.*\.planning.*(phases|todos|quick|codebase|milestones)" lib/context.js — zero results for path.join constructions (Level 1: Sanity)
    grep -n "pathExistsInternal.*\.planning/(phases|todos|codebase|research)" lib/context.js — zero results (Level 1: Sanity)
    npx jest tests/unit/context.test.js — all pass (Level 2: Proxy)
    npm test — all 1,608+ tests pass (Level 2: Proxy)
  </verify>
  <done>lib/context.js has zero hardcoded `.planning/` subdirectory path constructions; all init functions include phases_dir, research_dir, codebase_dir, quick_dir, todos_dir in JSON output; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Migrate lib/cleanup.js, lib/roadmap.js, and lib/tracker.js to paths.js</name>
  <files>lib/cleanup.js, lib/roadmap.js, lib/tracker.js</files>
  <action>
    **lib/cleanup.js (2 occurrences):**

    Add at the top (after existing requires):
    ```javascript
    const { phasesDir: getPhasesDirPath } = require('./paths');
    ```

    - Line ~368: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`
    - Line ~1153: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`

    **lib/roadmap.js (2 occurrences):**

    Add at the top (after existing requires):
    ```javascript
    const { phasesDir: getPhasesDirPath } = require('./paths');
    ```

    - Line ~242: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`
    - Line ~328: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`

    **lib/tracker.js (2 occurrences):**

    Add at the top (after existing requires):
    ```javascript
    const { phasesDir: getPhasesDirPath } = require('./paths');
    ```

    - Line ~509: Replace `const planningDir = path.join(cwd, '.planning', 'phases');`
      With `const planningDir = getPhasesDirPath(cwd);`
    - Line ~783: Replace `const planningDir = path.join(cwd, '.planning', 'phases');`
      With `const planningDir = getPhasesDirPath(cwd);`

    After all three files are modified, verify:
    ```bash
    grep -n "path.join.*\.planning.*phases" lib/cleanup.js lib/roadmap.js lib/tracker.js
    ```
    Should return zero results.

    Run tests:
    ```bash
    npx jest tests/unit/cleanup.test.js tests/unit/roadmap.test.js tests/unit/tracker.test.js --no-coverage
    npm test
    ```
  </action>
  <verify>
    grep -n "path.join.*\.planning.*phases" lib/cleanup.js lib/roadmap.js lib/tracker.js — zero results (Level 1: Sanity)
    npx jest tests/unit/cleanup.test.js tests/unit/roadmap.test.js tests/unit/tracker.test.js — all pass (Level 2: Proxy)
    npm test — all 1,608+ tests pass (Level 2: Proxy)
  </verify>
  <done>lib/cleanup.js, lib/roadmap.js, and lib/tracker.js have zero hardcoded `.planning/phases/` path constructions; all 6 occurrences replaced; all tests pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `grep -rn "path.join.*\.planning.*(phases|todos|quick|codebase|research)" lib/context.js lib/cleanup.js lib/roadmap.js lib/tracker.js` returns zero results
Level 2 (Proxy): `npm test` passes with all 1,608+ tests; unit tests for all four modules pass; init functions include milestone-scoped path fields
Level 3 (Deferred): Full hierarchy migration (Phase 35) validates paths resolve correctly end-to-end; Phase 34 validates commands/agents consume the new path fields
</verification>

<success_criteria>
- Zero hardcoded `.planning/` subdirectory path constructions in lib/context.js, lib/cleanup.js, lib/roadmap.js, lib/tracker.js
- All cmdInit* functions in context.js include phases_dir, research_dir, codebase_dir, quick_dir, todos_dir in JSON output (REQ-56)
- pathExistsInternal calls replaced with fs.existsSync on paths.js-resolved paths
- All 1,608+ tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/33-lib-module-migration/33-04-SUMMARY.md`
</output>
