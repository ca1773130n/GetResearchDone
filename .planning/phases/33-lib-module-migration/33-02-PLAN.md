---
phase: 33-lib-module-migration
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - lib/phase.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "lib/phase.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` — all replaced with paths.js calls"
    - "lib/phase.js uses `paths.milestonesDir(cwd)` instead of `path.join(cwd, '.planning', 'milestones')` in cmdMilestoneComplete"
    - "lib/phase.js uses `paths.archivedPhasesDir(cwd, version)` for archive directory construction"
    - "All relative `.planning/phases/${dirName}` string literals in output objects use `path.relative(cwd, ...)` from the resolved paths.js path"
    - "All 1,608+ existing tests continue to pass with zero regressions"
  artifacts:
    - path: "lib/phase.js"
      provides: "Phase lifecycle module migrated to paths.js"
      min_lines: 900
  key_links:
    - from: "lib/phase.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
---

<objective>
Migrate all 11 hardcoded `.planning/phases/` and `.planning/milestones` path constructions in `lib/phase.js` to use `paths.js` function calls.

Purpose: `lib/phase.js` has the most hardcoded path constructions of any single module (11 occurrences across `cmdPhasesList`, `cmdPhaseAdd`, `cmdPhaseInsert`, `cmdPhaseRemove`, `cmdPhaseComplete`, `cmdMilestoneComplete`, `cmdValidateConsistency`). Migrating it eliminates the largest cluster of technical debt.

Output: Updated `lib/phase.js` with all path constructions delegated to `paths.js`
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-centralized-path-resolution-module/32-01-PLAN.md
@.planning/phases/33-lib-module-migration/33-01-PLAN.md — backward-compat fallback in paths.js

@lib/paths.js — centralized path module with backward-compat fallback
@lib/phase.js — target module (11 hardcoded paths)
@tests/unit/phase.test.js — must still pass
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all hardcoded paths in lib/phase.js to paths.js calls</name>
  <files>lib/phase.js</files>
  <action>
    Add at the top of `lib/phase.js` (after existing requires, before the first function):
    ```javascript
    const {
      phasesDir: getPhasesDirPath,
      phaseDir: getPhaseDirPath,
      milestonesDir: getMilestonesDirPath,
      archivedPhasesDir: getArchivedPhasesDir,
    } = require('./paths');
    ```

    Migrate each occurrence systematically:

    **cmdPhasesList (line ~38):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhaseAdd (lines ~151, ~175):**
    - Line ~151: Replace `const dirPath = path.join(cwd, '.planning', 'phases', dirName);`
      With `const dirPath = getPhaseDirPath(cwd, null, dirName);`
      Note: `null` milestone triggers the default from currentMilestone(cwd), which with the backward-compat fallback resolves to the correct directory.
    - Line ~175: Replace `directory: \`.planning/phases/${dirName}\``
      With `directory: path.relative(cwd, dirPath)`
      Since `dirPath` is already computed above, use it to derive the relative path.

    **cmdPhaseInsert (lines ~221, ~238, ~272):**
    - Line ~221: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`
    - Line ~238: Replace `const dirPath = path.join(cwd, '.planning', 'phases', dirName);`
      With `const dirPath = path.join(phasesDir, dirName);`
      (Reuse `phasesDir` variable already resolved above)
    - Line ~272: Replace `directory: \`.planning/phases/${dirName}\``
      With `directory: path.relative(cwd, dirPath)`

    **cmdPhaseRemove (line ~296):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhaseComplete (line ~577):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdMilestoneComplete (lines ~767, ~768):**
    - Line ~767: Replace `const archiveDir = path.join(cwd, '.planning', 'milestones');`
      With `const archiveDir = getMilestonesDirPath(cwd);`
    - Line ~768: Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
      With `const phasesDir = getPhasesDirPath(cwd);`

    Note: `cmdMilestoneComplete` also constructs the archive destination. Check if there's a pattern like `path.join(archiveDir, version + '-phases')` — if so, replace with `getArchivedPhasesDir(cwd, version)`.

    **cmdValidateConsistency (line ~912):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    After all replacements, verify no hardcoded paths remain:
    ```bash
    grep -n "path.join.*\.planning.*phases" lib/phase.js
    grep -n "path.join.*\.planning.*milestones" lib/phase.js
    ```

    Both should return zero results (only `require('./paths')` should reference paths).

    Run tests:
    ```bash
    npx jest tests/unit/phase.test.js --no-coverage
    npm test
    ```
  </action>
  <verify>
    grep -n "path.join.*\.planning.*phases" lib/phase.js — returns zero results (Level 1: Sanity)
    grep -n "path.join.*\.planning.*milestones" lib/phase.js — returns zero results (Level 1: Sanity)
    npx jest tests/unit/phase.test.js — all phase tests pass (Level 2: Proxy)
    npm test — all 1,608+ tests pass (Level 2: Proxy)
  </verify>
  <done>lib/phase.js has zero hardcoded `.planning/phases/` or `.planning/milestones` path constructions; all 11 occurrences replaced with paths.js function calls; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Verify output JSON fields still produce correct relative paths</name>
  <files>lib/phase.js</files>
  <action>
    After the mechanical migration in Task 1, manually verify that the `directory` fields in JSON output objects produce correct relative paths. The functions `cmdPhaseAdd`, `cmdPhaseInsert`, and the `findPhaseInternal` return values all include `directory` fields like `.planning/phases/01-foo`.

    Specifically check:
    1. `cmdPhaseAdd` output includes `directory` field that is a valid relative path from cwd
    2. `cmdPhaseInsert` output includes `directory` field that is a valid relative path from cwd
    3. No output JSON now includes absolute paths where relative paths were expected

    Run the integration tests that exercise these commands:
    ```bash
    npx jest tests/integration/ --no-coverage -t "phase"
    ```

    If any integration test fails due to path format changes in JSON output, fix the `path.relative(cwd, ...)` computation to match the expected format.

    Final full suite run:
    ```bash
    npm test
    ```
  </action>
  <verify>
    npx jest tests/integration/ --no-coverage -t "phase" — integration tests pass (Level 2: Proxy)
    npm test — all tests pass (Level 2: Proxy)
  </verify>
  <done>All JSON output `directory` fields produce correct relative paths matching the format expected by consumers; integration tests pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `grep -rn "path.join.*\.planning.*phases" lib/phase.js` returns zero results; `grep -rn "path.join.*\.planning.*milestones" lib/phase.js` returns zero results
Level 2 (Proxy): All unit tests for phase.js pass; all integration tests pass; `npm test` passes with 1,608+ tests
Level 3 (Deferred): Physical directory migration (Phase 35) validates paths resolve correctly end-to-end
</verification>

<success_criteria>
- Zero occurrences of `path.join(cwd, '.planning', 'phases')` in lib/phase.js
- Zero occurrences of `path.join(cwd, '.planning', 'milestones')` in lib/phase.js
- All 11 hardcoded path constructions replaced with paths.js function calls
- JSON output `directory` fields produce correct relative paths
- All 1,608+ tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/33-lib-module-migration/33-02-SUMMARY.md`
</output>
