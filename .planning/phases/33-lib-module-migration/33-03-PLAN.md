---
phase: 33-lib-module-migration
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - lib/commands.js
  - lib/scaffold.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "lib/commands.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` or `path.join(cwd, '.planning', 'todos')` — all replaced with paths.js calls"
    - "lib/commands.js has zero occurrences of `path.join(cwd, '.planning', 'milestones')` — replaced with paths.milestonesDir(cwd)"
    - "lib/scaffold.js has zero occurrences of `path.join(cwd, '.planning', 'phases')` or `path.join(cwd, '.planning', 'research')` — all replaced with paths.js calls"
    - "All relative path string literals in output objects (`.planning/phases/...`, `.planning/todos/...`) derive from paths.js-resolved paths via path.relative(cwd, ...)"
    - "All 1,608+ existing tests continue to pass with zero regressions"
  artifacts:
    - path: "lib/commands.js"
      provides: "Commands module fully migrated to paths.js"
    - path: "lib/scaffold.js"
      provides: "Scaffold module fully migrated to paths.js"
  key_links:
    - from: "lib/commands.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
    - from: "lib/scaffold.js"
      to: "lib/paths.js"
      via: "require('./paths')"
      pattern: "require.*paths"
---

<objective>
Migrate all hardcoded `.planning/` subdirectory path constructions in `lib/commands.js` (16 occurrences) and `lib/scaffold.js` (4 occurrences) to use `paths.js` function calls.

Purpose: `lib/commands.js` is the second-largest migration target with 16 hardcoded path constructions across todo management, phase lookup, plan indexing, history digest, and requirement commands. `lib/scaffold.js` has 4 occurrences in phase-dir and research-dir scaffolding. Both import from `lib/utils.js` which is already migrated in Plan 01.

Output: Updated `lib/commands.js` and `lib/scaffold.js` with all path constructions delegated to `paths.js`
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-lib-module-migration/33-01-PLAN.md — backward-compat fallback and foundational migration

@lib/paths.js — centralized path module
@lib/commands.js — target module (16 hardcoded paths)
@lib/scaffold.js — target module (4 hardcoded paths)
@tests/unit/commands.test.js — must still pass
@tests/unit/scaffold.test.js — must still pass
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all hardcoded paths in lib/commands.js to paths.js calls</name>
  <files>lib/commands.js</files>
  <action>
    Add at the top of `lib/commands.js` (after existing requires):
    ```javascript
    const {
      phasesDir: getPhasesDirPath,
      todosDir: getTodosDirPath,
      milestonesDir: getMilestonesDirPath,
    } = require('./paths');
    ```

    Migrate each occurrence:

    **cmdListTodos (lines ~105, ~131):**
    - Line ~105: Replace `const pendingDir = path.join(cwd, '.planning', 'todos', 'pending');`
      With: `const pendingDir = path.join(getTodosDirPath(cwd), 'pending');`
    - Line ~131: Replace `path: path.join('.planning', 'todos', 'pending', file),`
      With: `path: path.relative(cwd, path.join(pendingDir, file)),`
      (Reuse `pendingDir` already resolved above)

    **cmdTodoComplete (lines ~153, ~154):**
    - Line ~153: Replace `const pendingDir = path.join(cwd, '.planning', 'todos', 'pending');`
      With: `const pendingDir = path.join(getTodosDirPath(cwd), 'pending');`
    - Line ~154: Replace `const completedDir = path.join(cwd, '.planning', 'todos', 'completed');`
      With: `const completedDir = path.join(getTodosDirPath(cwd), 'completed');`

    **cmdHistoryDigest (line ~345):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhasePlanIndex (line ~474):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhasePlanIndex output (line ~512):**
    - Replace `directory: path.join('.planning', 'phases', match),`
    - With `directory: path.relative(cwd, path.join(phasesDir, match)),`

    **cmdSummaryExtract (line ~606):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdProgress / cmdPhaseCount (line ~797):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdCommit (line ~886):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhaseDetail (line ~1299):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdPhaseDetail output (line ~1495):**
    - Replace `directory: path.join('.planning', 'phases', phaseDirName),`
    - With `directory: path.relative(cwd, path.join(phasesDir, phaseDirName)),`

    **cmdPhaseDetail TUI (line ~1524):**
    - Replace `tui += \`Directory: .planning/phases/${phaseDirName}\n\n\`;`
    - With `tui += \`Directory: ${path.relative(cwd, path.join(phasesDir, phaseDirName))}\n\n\`;`

    **cmdModelResolve or cmdPhaseSearch (line ~1692):**
    - Replace `const phasesDir = path.join(cwd, '.planning', 'phases');`
    - With `const phasesDir = getPhasesDirPath(cwd);`

    **cmdRequirementGet (line ~2353):**
    - Replace `const milestonesDir = path.join(cwd, '.planning', 'milestones');`
    - With `const milestonesDir = getMilestonesDirPath(cwd);`

    **cmdRequirementList (line ~2417):**
    - Replace `const milestonesDir = path.join(cwd, '.planning', 'milestones');`
    - With `const milestonesDir = getMilestonesDirPath(cwd);`

    After all replacements, verify:
    ```bash
    grep -n "path.join.*\.planning.*phases" lib/commands.js
    grep -n "path.join.*\.planning.*todos" lib/commands.js
    grep -n "path.join.*\.planning.*milestones" lib/commands.js
    ```
    All three should return zero results.

    Run tests:
    ```bash
    npx jest tests/unit/commands.test.js --no-coverage
    npm test
    ```
  </action>
  <verify>
    grep -n "path.join.*\.planning.*phases" lib/commands.js — zero results (Level 1: Sanity)
    grep -n "path.join.*\.planning.*todos" lib/commands.js — zero results (Level 1: Sanity)
    grep -n "path.join.*\.planning.*milestones" lib/commands.js — zero results (Level 1: Sanity)
    npx jest tests/unit/commands.test.js — all pass (Level 2: Proxy)
    npm test — all 1,608+ tests pass (Level 2: Proxy)
  </verify>
  <done>lib/commands.js has zero hardcoded `.planning/` subdirectory path constructions; all 16 occurrences replaced; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all hardcoded paths in lib/scaffold.js to paths.js calls</name>
  <files>lib/scaffold.js</files>
  <action>
    Add at the top of `lib/scaffold.js` (after existing requires):
    ```javascript
    const {
      phasesDir: getPhasesDirPath,
      researchDir: getResearchDirPath,
    } = require('./paths');
    ```

    Migrate each occurrence:

    **cmdScaffold 'phase-dir' case (lines ~317, ~322):**
    - Line ~317: Replace `const phasesParent = path.join(cwd, '.planning', 'phases');`
      With `const phasesParent = getPhasesDirPath(cwd);`
    - Line ~322: Replace `directory: \`.planning/phases/${dirName}\``
      With `directory: path.relative(cwd, dirPath)`
      (`dirPath` is already `path.join(phasesParent, dirName)` from line ~319)

    **cmdScaffold 'research-dir' case (lines ~329, ~332):**
    - Line ~329: Replace `const researchDir = path.join(cwd, '.planning', 'research');`
      With `const researchDir = getResearchDirPath(cwd);`
    - Line ~332: The output `directory: '.planning/research/'` should become:
      `directory: path.relative(cwd, researchDir) + '/'`

    After replacements, verify:
    ```bash
    grep -n "path.join.*\.planning.*phases" lib/scaffold.js
    grep -n "path.join.*\.planning.*research" lib/scaffold.js
    ```
    Both should return zero results.

    Run tests:
    ```bash
    npx jest tests/unit/scaffold.test.js --no-coverage
    npm test
    ```
  </action>
  <verify>
    grep -n "path.join.*\.planning.*phases" lib/scaffold.js — zero results (Level 1: Sanity)
    grep -n "path.join.*\.planning.*research" lib/scaffold.js — zero results (Level 1: Sanity)
    npx jest tests/unit/scaffold.test.js — all pass (Level 2: Proxy)
    npm test — all 1,608+ tests pass (Level 2: Proxy)
  </verify>
  <done>lib/scaffold.js has zero hardcoded `.planning/` subdirectory path constructions; all 4 occurrences replaced; all tests pass</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `grep -rn "path.join.*\.planning.*(phases|todos|milestones|research)" lib/commands.js lib/scaffold.js` returns zero results
Level 2 (Proxy): `npm test` passes with all 1,608+ tests; unit tests for commands.js and scaffold.js pass
Level 3 (Deferred): Full hierarchy migration (Phase 35) validates these paths resolve correctly end-to-end
</verification>

<success_criteria>
- Zero occurrences of hardcoded `.planning/phases/`, `.planning/todos/`, `.planning/milestones`, `.planning/research/` path constructions in lib/commands.js
- Zero occurrences of hardcoded `.planning/phases/`, `.planning/research/` path constructions in lib/scaffold.js
- All relative path string literals in output objects derive from paths.js
- All 1,608+ tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/33-lib-module-migration/33-03-SUMMARY.md`
</output>
