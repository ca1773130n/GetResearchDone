---
phase: 03-modularize-grd-tools
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/utils.js
  - lib/frontmatter.js
  - bin/grd-tools.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/utils.js exports all shared helpers (safeReadFile, loadConfig, execGit, isGitIgnored, normalizePhaseName, findCodeFiles, validatePhaseName, validateFilePath, validateGitRef, output, error, resolveModelInternal, findPhaseInternal, pathExistsInternal, generateSlugInternal, getMilestoneInfo, resolveModelForAgent, parseIncludeFlag)"
    - "lib/frontmatter.js exports extractFrontmatter, reconstructFrontmatter, spliceFrontmatter, parseMustHavesBlock"
    - "bin/grd-tools.js requires lib/utils.js and lib/frontmatter.js instead of defining these functions inline"
    - "All CLI commands produce identical output to golden references"
    - "GIT_ALLOWED_COMMANDS, GIT_BLOCKED_COMMANDS, GIT_BLOCKED_FLAGS, MODEL_PROFILES constants live in lib/utils.js"
  artifacts:
    - path: "lib/utils.js"
      provides: "Shared utility functions and constants"
    - path: "lib/frontmatter.js"
      provides: "YAML frontmatter parse/reconstruct/splice/validate"
  key_links:
    - from: "bin/grd-tools.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*lib/utils"
    - from: "bin/grd-tools.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*lib/frontmatter"
---

<objective>
Extract foundation modules: `lib/utils.js` (shared helpers + constants) and `lib/frontmatter.js` (YAML frontmatter operations).

Purpose: These are the two modules with ZERO internal dependencies -- every other module depends on them. Extracting them first establishes the module pattern and import structure that all subsequent extractions will follow.

Output: Two new lib/ files, bin/grd-tools.js updated to require them.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@.planning/phases/03-modularize-grd-tools/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/utils.js with all shared helpers and constants</name>
  <files>lib/utils.js, bin/grd-tools.js</files>
  <action>
    Create `lib/` directory if it doesn't exist.

    Create `lib/utils.js` containing:

    1. **Imports at top:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const os = require('os');
       const { execFileSync } = require('child_process');
       ```

    2. **Constants (move from bin/grd-tools.js):**
       - GIT_ALLOWED_COMMANDS, GIT_BLOCKED_COMMANDS, GIT_BLOCKED_FLAGS (lines 124-131)
       - MODEL_PROFILES (lines 135-157)

    3. **Functions (move from bin/grd-tools.js, preserving exact implementation):**
       - parseIncludeFlag (line 161)
       - safeReadFile (line 169)
       - loadConfig (line 177)
       - isGitIgnored (line 246)
       - execGit (line 259)
       - normalizePhaseName (line 296)
       - findCodeFiles (line 310)
       - validatePhaseName (line 336)
       - validateFilePath (line 347)
       - validateGitRef (line 355)
       - output (line 578)
       - error (line 587)
       - resolveModelInternal (line 3585)
       - findPhaseInternal (line 3593)
       - pathExistsInternal (line 3644)
       - generateSlugInternal (line 3654)
       - getMilestoneInfo (line 3659)
       - resolveModelForAgent (line 4391)

    4. **module.exports at bottom:**
       Export all constants and functions as named exports.

    5. **Update bin/grd-tools.js:**
       - Remove the `const fs = require('fs')`, `const path = require('path')`, `const os = require('os')`, `const { execFileSync } = require('child_process')` imports (they'll come from utils)
       - Add at top: `const { fs, path, os, execFileSync, ...allUtils } = require('../lib/utils');` -- actually, better pattern:
         ```js
         const utils = require('../lib/utils');
         const { safeReadFile, loadConfig, execGit, isGitIgnored, normalizePhaseName, findCodeFiles,
                 validatePhaseName, validateFilePath, validateGitRef, output, error,
                 resolveModelInternal, findPhaseInternal, pathExistsInternal, generateSlugInternal,
                 getMilestoneInfo, resolveModelForAgent, parseIncludeFlag,
                 GIT_ALLOWED_COMMANDS, GIT_BLOCKED_COMMANDS, GIT_BLOCKED_FLAGS, MODEL_PROFILES } = utils;
         ```
       - Remove the moved function definitions and constants from bin/grd-tools.js
       - Keep fs/path/os/execFileSync imports in grd-tools.js too (some cmd functions use them directly)

    CRITICAL: Copy functions EXACTLY as-is. Do not refactor, rename, or change any logic. The only change is moving code to a new file and adding require/exports.

    Note: resolveModelInternal references MODEL_PROFILES, so both must be in the same file (utils.js). findPhaseInternal references normalizePhaseName, so both must be in utils.js. loadConfig uses safeReadFile, fs, path -- all in utils.js.
  </action>
  <verify>
    Run: node bin/grd-tools.js state load (from project root with .planning/)
    Run: node bin/grd-tools.js generate-slug "test phrase"
    Run: node bin/grd-tools.js resolve-model grd-executor
    Run: node bin/grd-tools.js find-phase 1
    All must produce valid JSON output. (Level 1: Sanity)

    Diff key outputs against golden references from Plan 03-01. (Level 2: Proxy)
  </verify>
  <done>
    lib/utils.js exists with all shared helpers and constants exported.
    bin/grd-tools.js imports from lib/utils.js.
    All tested commands produce identical output to golden references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract lib/frontmatter.js with YAML frontmatter operations</name>
  <files>lib/frontmatter.js, bin/grd-tools.js</files>
  <action>
    Create `lib/frontmatter.js` containing:

    1. **Import from utils:**
       ```js
       const { safeReadFile, output, error } = require('./utils');
       const fs = require('fs');
       const path = require('path');
       ```

    2. **Core functions (move from bin/grd-tools.js):**
       - extractFrontmatter (line 364) -- pure parsing, no external deps
       - reconstructFrontmatter (line 439) -- pure serialization
       - spliceFrontmatter (line 503) -- uses extractFrontmatter + reconstructFrontmatter
       - parseMustHavesBlock (line 512) -- pure parsing

    3. **Command functions (move from bin/grd-tools.js):**
       - cmdFrontmatterGet (line 2137) -- uses extractFrontmatter, safeReadFile, output, error
       - cmdFrontmatterSet (line 2152) -- uses extractFrontmatter, spliceFrontmatter, fs.writeFileSync
       - cmdFrontmatterMerge (line 2166) -- uses extractFrontmatter, spliceFrontmatter, fs.writeFileSync
       - cmdFrontmatterValidate (line 2186) -- uses extractFrontmatter, output, error

    4. **module.exports:**
       Export all functions (both core and cmd variants).

    5. **Update bin/grd-tools.js:**
       - Add: `const { extractFrontmatter, reconstructFrontmatter, spliceFrontmatter, parseMustHavesBlock, cmdFrontmatterGet, cmdFrontmatterSet, cmdFrontmatterMerge, cmdFrontmatterValidate } = require('../lib/frontmatter');`
       - Remove the moved function definitions from bin/grd-tools.js

    CRITICAL: Copy functions EXACTLY as-is. The `extractFrontmatter` function is used extensively across the entire codebase (state, verify, scaffold, etc.). It must work identically.

    Note: cmdFrontmatterGet/Set/Merge use `path.resolve(cwd, filePath)` so they need `path` imported. cmdFrontmatterValidate has schema definitions for 'plan', 'summary', 'verification' inline -- move those schema defs into the function as-is.
  </action>
  <verify>
    Run: node bin/grd-tools.js frontmatter get .planning/phases/01-*/01-01-PLAN.md
    Run: node bin/grd-tools.js frontmatter validate .planning/phases/01-*/01-01-PLAN.md --schema plan
    Both must produce valid JSON output. (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/frontmatter.js exists with all frontmatter operations exported.
    bin/grd-tools.js imports from lib/frontmatter.js.
    Frontmatter commands produce identical output to golden references.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `lib/utils.js` exists, >= 250 lines
- `lib/frontmatter.js` exists, >= 200 lines
- `node bin/grd-tools.js state load` produces valid JSON
- `node bin/grd-tools.js frontmatter get <file>` produces valid JSON
- No `require` errors on startup

Level 2 (Proxy):
- Diff at least 5 command outputs against golden references -- all identical

Level 3 (Deferred): DEFER-03-01 (all commands work), DEFER-03-02 (output unchanged)
</verification>

<success_criteria>
- lib/utils.js <= 500 lines, exports >= 15 functions
- lib/frontmatter.js <= 500 lines, exports >= 8 functions
- bin/grd-tools.js reduced by >= 500 lines
- All commands using extracted functions produce identical JSON output
- No circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-02-SUMMARY.md`
</output>
