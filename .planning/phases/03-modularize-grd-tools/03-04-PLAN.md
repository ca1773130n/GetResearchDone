---
phase: 03-modularize-grd-tools
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/roadmap.js
  - lib/scaffold.js
  - bin/grd-tools.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/roadmap.js exports roadmap parsing and phase query functions (cmdRoadmapGetPhase, cmdRoadmapAnalyze, cmdPhaseNextDecimal)"
    - "lib/scaffold.js exports scaffolding and template operations (cmdScaffold, cmdTemplateFill, cmdTemplateSelect)"
    - "bin/grd-tools.js requires lib/roadmap.js and lib/scaffold.js instead of defining these functions inline"
    - "All roadmap and scaffold CLI commands produce identical output to golden references"
  artifacts:
    - path: "lib/roadmap.js"
      provides: "ROADMAP.md parsing, phase queries, next-decimal computation"
    - path: "lib/scaffold.js"
      provides: "Template scaffolding and fill operations"
  key_links:
    - from: "lib/roadmap.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/scaffold.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/scaffold.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*frontmatter"
---

<objective>
Extract `lib/roadmap.js` (ROADMAP.md parsing and queries) and `lib/scaffold.js` (template scaffolding operations).

Purpose: Roadmap and scaffold depend only on utils and frontmatter (already extracted in Plan 03-02). This plan runs in PARALLEL with Plan 03-03 (same wave) because they touch different functions with no file conflicts in lib/.

Output: Two new lib/ files, bin/grd-tools.js updated to require them.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@lib/utils.js
@lib/frontmatter.js
@.planning/phases/03-modularize-grd-tools/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/roadmap.js with roadmap parsing and queries</name>
  <files>lib/roadmap.js, bin/grd-tools.js</files>
  <action>
    Create `lib/roadmap.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, normalizePhaseName, findPhaseInternal, output, error } = require('./utils');
       const { extractFrontmatter } = require('./frontmatter');
       ```

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - cmdRoadmapGetPhase (line ~958) -- parses ROADMAP.md to extract a single phase section
       - cmdRoadmapAnalyze (line ~2494) -- full roadmap parse with disk status, milestone info
       - cmdPhaseNextDecimal (line ~1016) -- computes next decimal phase number

    3. **module.exports:** Export all three functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/roadmap.js, destructure all exported functions
       - Remove moved function definitions

    Note: cmdRoadmapAnalyze (~129 lines) is the largest function here. It reads ROADMAP.md, scans phase directories, and produces a comprehensive analysis JSON. It uses safeReadFile, normalizePhaseName, extractFrontmatter -- all available via imports.

    cmdPhaseNextDecimal reads ROADMAP.md and scans directories. It uses safeReadFile, normalizePhaseName.
  </action>
  <verify>
    Run: node bin/grd-tools.js roadmap get-phase 1
    Run: node bin/grd-tools.js roadmap analyze
    Run: node bin/grd-tools.js phase next-decimal 1
    All must produce valid JSON. (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/roadmap.js exists with ~256 lines, all roadmap functions exported.
    bin/grd-tools.js imports from lib/roadmap.js.
    All roadmap commands produce identical output to golden references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract lib/scaffold.js with template operations</name>
  <files>lib/scaffold.js, bin/grd-tools.js</files>
  <action>
    Create `lib/scaffold.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, findPhaseInternal, normalizePhaseName, output, error } = require('./utils');
       const { extractFrontmatter, reconstructFrontmatter, spliceFrontmatter } = require('./frontmatter');
       ```

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - cmdTemplateSelect (line ~1659) -- selects template based on plan type
       - cmdTemplateFill (line ~1705) -- fills template with runtime data (~166 lines)
       - cmdScaffold (line ~3502) -- scaffolds context/uat/verification/phase-dir/research-dir/eval/baseline

    3. **module.exports:** Export all three functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/scaffold.js, destructure all exported functions
       - Remove moved function definitions

    Note: cmdTemplateFill is ~166 lines and handles multiple template types (summary, plan, verification). It reads template files from a templates directory relative to the GRD plugin root. The path to templates uses `__dirname` or a path computed relative to grd-tools.js -- ensure the template path resolution still works from lib/scaffold.js.

    CRITICAL: Check how cmdTemplateFill and cmdTemplateSelect resolve template file paths. If they use `__dirname` to find templates, the path will change when moved to lib/. Solution: pass the template root as a parameter, or compute it relative to the project structure. Since templates live in the GRD plugin (not the user project), the path must be: `path.resolve(__dirname, '..', 'templates')` -- verify this resolves correctly from lib/scaffold.js.

    Actually, check the current implementation. If it uses `process.env.CLAUDE_PLUGIN_ROOT` or a config-based path, it may already be flexible. Preserve the exact path resolution logic.
  </action>
  <verify>
    Run: node bin/grd-tools.js scaffold context --phase 1
    Run: node bin/grd-tools.js template select .planning/phases/01-*/01-01-PLAN.md
    Both must produce valid output. (Level 1: Sanity)

    Diff against golden references where applicable. (Level 2: Proxy)
  </verify>
  <done>
    lib/scaffold.js exists with ~295 lines, all scaffold/template functions exported.
    bin/grd-tools.js imports from lib/scaffold.js.
    Scaffold and template commands produce identical output to golden references.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `lib/roadmap.js` exists, <= 500 lines
- `lib/scaffold.js` exists, <= 500 lines
- All roadmap and scaffold commands produce valid JSON/output
- No require errors

Level 2 (Proxy):
- Diff roadmap analyze, roadmap get-phase against golden references -- identical

Level 3 (Deferred): DEFER-03-01, DEFER-03-02
</verification>

<success_criteria>
- lib/roadmap.js <= 500 lines, exports >= 3 functions
- lib/scaffold.js <= 500 lines, exports >= 3 functions
- bin/grd-tools.js reduced by >= 500 additional lines
- All roadmap and scaffold commands produce identical output
- No circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-04-SUMMARY.md`
</output>
