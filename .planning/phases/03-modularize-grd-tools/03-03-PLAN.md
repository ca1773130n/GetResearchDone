---
phase: 03-modularize-grd-tools
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/state.js
  - lib/verify.js
  - bin/grd-tools.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "lib/state.js exports all STATE.md operations (load, get, patch, update, advance-plan, record-metric, update-progress, add-decision, add-blocker, resolve-blocker, record-session, snapshot)"
    - "lib/verify.js exports all verification functions (plan-structure, phase-completeness, references, commits, artifacts, key-links, verify-summary)"
    - "bin/grd-tools.js requires lib/state.js and lib/verify.js instead of defining these functions inline"
    - "All state and verify CLI commands produce identical output to golden references"
  artifacts:
    - path: "lib/state.js"
      provides: "STATE.md read/write/patch operations"
    - path: "lib/verify.js"
      provides: "Verification suite functions"
  key_links:
    - from: "lib/state.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/state.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*frontmatter"
    - from: "lib/verify.js"
      to: "lib/utils.js"
      via: "require"
      pattern: "require.*utils"
    - from: "lib/verify.js"
      to: "lib/frontmatter.js"
      via: "require"
      pattern: "require.*frontmatter"
---

<objective>
Extract `lib/state.js` (STATE.md operations) and `lib/verify.js` (verification suite).

Purpose: State and verify are the next-most independent modules. They depend only on utils and frontmatter (already extracted). Extracting them continues the bottom-up dependency chain and removes ~850 lines from the monolith.

Output: Two new lib/ files, bin/grd-tools.js updated to require them.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
@lib/utils.js
@lib/frontmatter.js
@.planning/phases/03-modularize-grd-tools/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract lib/state.js with all STATE.md operations</name>
  <files>lib/state.js, bin/grd-tools.js</files>
  <action>
    Create `lib/state.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, loadConfig, output, error, findPhaseInternal } = require('./utils');
       const { extractFrontmatter } = require('./frontmatter');
       ```

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - stateExtractField (line ~1217)
       - stateReplaceField (line ~1223)
       - cmdStateLoad (line ~1085) -- uses loadConfig, safeReadFile, extractFrontmatter
       - cmdStateGet (line ~1130) -- uses safeReadFile, stateExtractField
       - cmdStatePatch (line ~1165) -- uses safeReadFile, fs.writeFileSync
       - cmdStateUpdate (line ~1193) -- uses safeReadFile, stateReplaceField, fs.writeFileSync
       - cmdStateAdvancePlan (line ~1232)
       - cmdStateRecordMetric (line ~1261)
       - cmdStateUpdateProgress (line ~1296) -- uses findPhaseInternal, extractFrontmatter
       - cmdStateAddDecision (line ~1333)
       - cmdStateAddBlocker (line ~1360)
       - cmdStateResolveBlocker (line ~1383)
       - cmdStateRecordSession (line ~1415)
       - cmdStateSnapshot (line ~1979) -- uses safeReadFile, extractFrontmatter

    3. **module.exports:** Export all functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/state.js, destructure all exported functions
       - Remove moved function definitions

    CRITICAL: cmdStateUpdateProgress calls findPhaseInternal and extractFrontmatter internally. Verify these are properly imported. cmdStateSnapshot is ~100 lines and parses STATE.md into structured JSON -- move it completely.
  </action>
  <verify>
    Run: node bin/grd-tools.js state load
    Run: node bin/grd-tools.js state get
    Run: node bin/grd-tools.js state-snapshot
    All must produce valid JSON. (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/state.js exists with ~466 lines, all state operations exported.
    bin/grd-tools.js imports from lib/state.js.
    All state commands produce identical output to golden references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract lib/verify.js with verification suite</name>
  <files>lib/verify.js, bin/grd-tools.js</files>
  <action>
    Create `lib/verify.js` containing:

    1. **Imports:**
       ```js
       const fs = require('fs');
       const path = require('path');
       const { safeReadFile, execGit, findPhaseInternal, validateGitRef, output, error } = require('./utils');
       const { extractFrontmatter, parseMustHavesBlock } = require('./frontmatter');
       ```

    2. **Functions to move from bin/grd-tools.js (preserve exact implementation):**
       - cmdVerifyPlanStructure (line ~2201)
       - cmdVerifyPhaseCompleteness (line ~2262)
       - cmdVerifyReferences (line ~2309)
       - cmdVerifyCommits (line ~2354) -- uses execGit, validateGitRef
       - cmdVerifyArtifacts (line ~2377) -- uses extractFrontmatter, parseMustHavesBlock
       - cmdVerifyKeyLinks (line ~2432) -- uses extractFrontmatter, parseMustHavesBlock
       - cmdVerifySummary (line ~1562) -- uses safeReadFile, extractFrontmatter, execGit, validateGitRef

    3. **module.exports:** Export all functions.

    4. **Update bin/grd-tools.js:**
       - Add require for lib/verify.js, destructure all exported functions
       - Remove moved function definitions

    Note: cmdVerifySummary is defined at line ~1562 (before the other verify functions) but logically belongs with verification. Move it to lib/verify.js. It uses safeReadFile, extractFrontmatter, execGit, validateGitRef -- all available via utils and frontmatter imports.
  </action>
  <verify>
    Run: node bin/grd-tools.js verify plan-structure .planning/phases/01-*/01-01-PLAN.md
    Run: node bin/grd-tools.js verify phase-completeness 01
    Both must produce valid JSON. (Level 1: Sanity)

    Diff against golden references. (Level 2: Proxy)
  </verify>
  <done>
    lib/verify.js exists with ~390 lines, all verification functions exported.
    bin/grd-tools.js imports from lib/verify.js.
    All verify commands produce identical output to golden references.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `lib/state.js` exists, <= 500 lines
- `lib/verify.js` exists, <= 500 lines
- All state and verify commands produce valid JSON
- No require errors

Level 2 (Proxy):
- Diff state load, state-snapshot, verify plan-structure, verify phase-completeness against golden references -- all identical

Level 3 (Deferred): DEFER-03-01, DEFER-03-02
</verification>

<success_criteria>
- lib/state.js <= 500 lines, exports >= 14 functions
- lib/verify.js <= 500 lines, exports >= 7 functions
- bin/grd-tools.js reduced by >= 800 additional lines from Plan 03-02
- All state and verify commands produce identical JSON output
- No circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-03-SUMMARY.md`
</output>
