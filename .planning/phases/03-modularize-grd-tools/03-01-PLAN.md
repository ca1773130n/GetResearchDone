---
phase: 03-modularize-grd-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/golden/capture.sh
  - tests/golden/README.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Golden reference output captured for all CLI commands before any code changes"
    - "Capture script is deterministic and re-runnable"
    - "Each command's output saved as individual .json or .txt file for diff comparison"
  artifacts:
    - path: "tests/golden/capture.sh"
      provides: "Script to capture CLI output for all commands"
    - path: "tests/golden/README.md"
      provides: "Documentation of golden reference purpose and usage"
  key_links: []
---

<objective>
Capture golden reference output for every CLI command in bin/grd-tools.js BEFORE any modularization changes.

Purpose: This is the safety net for the entire phase. Every subsequent plan will diff its output against these golden references to detect regressions. Without this, we cannot verify that modularization preserves exact CLI behavior.

Output: `tests/golden/` directory with captured output for all CLI commands, plus a capture script for re-running.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden reference capture script and capture all CLI outputs</name>
  <files>tests/golden/capture.sh, tests/golden/README.md</files>
  <action>
    Create `tests/golden/` directory and a capture script that:

    1. Creates a temporary `.planning/` directory structure with minimal fixtures:
       - `.planning/config.json` (minimal valid config)
       - `.planning/STATE.md` (minimal valid state)
       - `.planning/ROADMAP.md` (minimal valid roadmap with 1 milestone, 2 phases)
       - `.planning/phases/01-test/01-01-PLAN.md` (minimal valid plan with frontmatter)
       - `.planning/phases/01-test/01-01-SUMMARY.md` (minimal valid summary)
       - `.planning/todos/pending/sample.md` (minimal todo)

    2. Runs each CLI command against the fixture directory and captures stdout to individual files:
       - `tests/golden/output/state-load.json`
       - `tests/golden/output/state-get.json`
       - `tests/golden/output/state-snapshot.json`
       - `tests/golden/output/resolve-model.json`
       - `tests/golden/output/find-phase.json`
       - `tests/golden/output/generate-slug.json`
       - `tests/golden/output/current-timestamp.json` (use --raw for date format only)
       - `tests/golden/output/verify-path-exists.json`
       - `tests/golden/output/frontmatter-get.json`
       - `tests/golden/output/frontmatter-validate.json`
       - `tests/golden/output/verify-plan-structure.json`
       - `tests/golden/output/verify-phase-completeness.json`
       - `tests/golden/output/roadmap-get-phase.json`
       - `tests/golden/output/roadmap-analyze.json`
       - `tests/golden/output/phase-plan-index.json`
       - `tests/golden/output/history-digest.json`
       - `tests/golden/output/progress-json.json`
       - `tests/golden/output/progress-table.txt`
       - `tests/golden/output/progress-bar.txt`
       - `tests/golden/output/validate-consistency.json`
       - `tests/golden/output/list-todos.json`
       - `tests/golden/output/phases-list.json`
       - `tests/golden/output/tracker-get-config.json`
       - And all other commands that don't mutate state

    3. For state-mutating commands (commit, phase add/remove/complete, state patch), capture output separately in `tests/golden/output/mutating/` using isolated temp directories.

    4. Commands that require git context (commit, verify-commits) should be tested in a temp git repo.

    5. Skip commands that require external services (GitHub CLI commands in tracker).

    6. Write `tests/golden/README.md` explaining:
       - What the golden references are
       - How to regenerate them
       - How to diff against them after modularization

    The capture script must:
    - Use `set -e` for fail-fast
    - Print each command as it runs
    - Exit with success only if all captures complete
    - Be re-runnable (clean up temp dirs first)
    - Use the project's own `bin/grd-tools.js` (not the plugin's)
  </action>
  <verify>
    bash tests/golden/capture.sh (Level 1: Sanity)
    - Script completes without error
    - tests/golden/output/ contains >= 20 .json files
    - At least 5 output files contain valid JSON (parse check)
  </verify>
  <done>
    Golden reference directory exists with captured output for all non-destructive CLI commands.
    Capture script is executable and re-runnable. README documents usage.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- `tests/golden/capture.sh` exists and is executable
- `tests/golden/output/` contains >= 20 files
- At least 5 output files contain valid JSON

Level 2 (Proxy): N/A for this plan
Level 3 (Deferred): Golden references will be used for diff comparison in all subsequent plans
</verification>

<success_criteria>
- Golden reference output captured for >= 20 CLI commands
- Capture script completes without errors
- Output files contain valid JSON where expected
- Script is deterministic (running twice produces identical output)
</success_criteria>

<output>
After completion, create `.planning/phases/03-modularize-grd-tools/03-01-SUMMARY.md`
</output>
