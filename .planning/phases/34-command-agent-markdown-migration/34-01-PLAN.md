---
phase: 34-command-agent-markdown-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/survey.md
  - commands/deep-dive.md
  - commands/compare-methods.md
  - commands/feasibility.md
  - commands/new-project.md
  - commands/eval-plan.md
  - commands/eval-report.md
  - commands/product-plan.md
  - commands/iterate.md
  - commands/plan-phase.md
  - commands/research-phase.md
autonomous: true
verification_level: sanity
must_haves:
  truths:
    - "Zero occurrences of hardcoded `.planning/research/` in operational bash commands (mkdir, cat, git add, Write-to paths) in survey.md, deep-dive.md, compare-methods.md, feasibility.md, new-project.md, eval-plan.md, eval-report.md, product-plan.md, iterate.md, plan-phase.md, research-phase.md — all replaced with `${research_dir}` or equivalent init-derived variables"
    - "Zero occurrences of hardcoded `.planning/phases/` in operational bash commands in eval-plan.md, eval-report.md, iterate.md, plan-phase.md, research-phase.md — all replaced with `${phases_dir}` or `${phase_dir}` from init context"
    - "Commands that previously lacked init calls (deep-dive.md, compare-methods.md, feasibility.md, eval-report.md, iterate.md, product-plan.md, research-phase.md) now call `grd-tools.js init` to obtain path variables before using them"
    - "Descriptive/documentation path references (context sections listing directory structure) are updated to use variable notation or generalized descriptions rather than hardcoded paths"
    - "All 11 command files remain syntactically valid markdown with intact YAML frontmatter"
  artifacts:
    - path: "commands/survey.md"
      provides: "Survey command migrated to init-derived research paths"
    - path: "commands/deep-dive.md"
      provides: "Deep-dive command migrated to init-derived research paths"
    - path: "commands/compare-methods.md"
      provides: "Compare-methods command migrated to init-derived research paths"
    - path: "commands/feasibility.md"
      provides: "Feasibility command migrated to init-derived research paths"
    - path: "commands/new-project.md"
      provides: "New-project command migrated to init-derived research paths"
    - path: "commands/eval-plan.md"
      provides: "Eval-plan command migrated to init-derived phase/research paths"
    - path: "commands/eval-report.md"
      provides: "Eval-report command migrated to init-derived phase/research paths"
    - path: "commands/product-plan.md"
      provides: "Product-plan command migrated to init-derived research/phase paths"
    - path: "commands/iterate.md"
      provides: "Iterate command migrated to init-derived phase/research paths"
    - path: "commands/plan-phase.md"
      provides: "Plan-phase command migrated to init-derived research paths"
    - path: "commands/research-phase.md"
      provides: "Research-phase command migrated to init-derived phase paths"
  key_links:
    - from: "commands/survey.md"
      to: "lib/context.js"
      via: "grd-tools.js init survey — provides research_dir"
      pattern: "init survey"
    - from: "commands/deep-dive.md"
      to: "lib/context.js"
      via: "grd-tools.js init deep-dive — provides research_dir"
      pattern: "init deep-dive"
    - from: "commands/compare-methods.md"
      to: "lib/context.js"
      via: "grd-tools.js init compare-methods — provides research_dir"
      pattern: "init compare-methods"
---

<objective>
Migrate the 11 research-focused and high-occurrence command markdown files from hardcoded `.planning/research/`, `.planning/phases/`, `.planning/codebase/` paths to init-context-derived variables. These 11 files account for ~120 of the 156 total hardcoded path occurrences in commands/.

Purpose: After Phase 33, init functions output milestone-scoped path fields (`research_dir`, `phases_dir`, `codebase_dir`, `quick_dir`, `todos_dir`). Command markdown files must consume these variables instead of constructing paths directly. This ensures commands work correctly with the new milestone-scoped hierarchy.

Output: 11 updated command markdown files with all operational paths using init-derived variables
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@commands/survey.md — target (11 occurrences)
@commands/deep-dive.md — target (13 occurrences)
@commands/compare-methods.md — target (9 occurrences)
@commands/feasibility.md — target (8 occurrences)
@commands/new-project.md — target (27 occurrences)
@commands/eval-plan.md — target (10 occurrences)
@commands/eval-report.md — target (8 occurrences)
@commands/product-plan.md — target (8 occurrences)
@commands/iterate.md — target (8 occurrences)
@commands/plan-phase.md — target (8 occurrences)
@commands/research-phase.md — target (3 occurrences)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add init calls and migrate research-focused commands (survey, deep-dive, compare-methods, feasibility, new-project, product-plan)</name>
  <files>commands/survey.md, commands/deep-dive.md, commands/compare-methods.md, commands/feasibility.md, commands/new-project.md, commands/product-plan.md</files>
  <action>
    **Migration pattern for all files:**

    After Phase 33, init functions include these fields in their JSON output:
    - `research_dir` — relative path to research directory (e.g., `.planning/milestones/v0.2.1/research` or `.planning/research` with fallback)
    - `phases_dir` — relative path to phases directory
    - `codebase_dir` — relative path to codebase directory
    - `quick_dir` — relative path to quick directory
    - `todos_dir` — relative path to todos directory

    For each command, apply these rules:
    1. **Operational bash paths** (mkdir, cat, ls, git add, Write-to targets): Replace `.planning/research/` with `${research_dir}/`, `.planning/phases/` with `${phases_dir}/`, `.planning/codebase/` with `${codebase_dir}/`
    2. **Context section descriptions** (listing directory structure): Update to use `${research_dir}/` notation or say "research directory" generically
    3. **@-references**: Update to use `@${research_dir}/LANDSCAPE.md` pattern
    4. **Output section file listings**: Update to use `${research_dir}/` pattern

    **commands/survey.md (11 occurrences):**

    This command already calls `grd-tools.js init survey "$TOPIC"`. After the init call, parse `research_dir` from the JSON output.

    Replace all operational `.planning/research/` paths:
    - Line 8: `.planning/research/LANDSCAPE.md` -> `${research_dir}/LANDSCAPE.md` (description)
    - Lines 16-19: Context section — update to use `${research_dir}/` in directory structure listing
    - Line 41: `mkdir -p .planning/research/deep-dives` -> `mkdir -p ${research_dir}/deep-dives`
    - Line 171: Path reference -> `${research_dir}/LANDSCAPE.md`
    - Lines 186-187: `git add .planning/research/LANDSCAPE.md` -> `git add ${research_dir}/LANDSCAPE.md` (and PAPERS.md)
    - Lines 215-216: Output section -> `${research_dir}/LANDSCAPE.md`, `${research_dir}/PAPERS.md`

    After the init call on line 36, add parsing of `research_dir`:
    ```
    Parse JSON for: research_dir, landscape_exists, papers_exists, ...
    ```

    **commands/deep-dive.md (13 occurrences):**

    This command does NOT currently call init. Add an init call at the beginning of Step 0:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init deep-dive "$PAPER_REF")
    ```
    Parse JSON for: `research_dir`, `landscape_exists`, `papers_exists`, `deep_dives`, `researcher_model`.

    Then replace all operational `.planning/research/` paths:
    - Lines 17-19: Context section -> `${research_dir}/` pattern
    - Line 46: `.planning/research/deep-dives/{paper-slug}.md` -> `${research_dir}/deep-dives/{paper-slug}.md`
    - Line 52: `mkdir -p .planning/research/deep-dives` -> `mkdir -p ${research_dir}/deep-dives`
    - Line 201: Path reference -> `${research_dir}/deep-dives/{paper-slug}.md`
    - Line 222: Deep-dive link -> `${research_dir}/deep-dives/{paper-slug}.md`
    - Lines 230-232: `git add .planning/research/...` -> `git add ${research_dir}/...`
    - Lines 255-257: Output section -> `${research_dir}/...`

    **commands/compare-methods.md (9 occurrences):**

    Add init call at beginning:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init compare-methods)
    ```
    Parse JSON for: `research_dir`, `landscape_exists`, `deep_dives`.

    Replace all `.planning/research/` paths:
    - Lines 16-19: Context section -> `${research_dir}/` pattern
    - Line 32: Path reference -> `${research_dir}/LANDSCAPE.md`
    - Line 42: `Scan .planning/research/deep-dives/` -> `Scan ${research_dir}/deep-dives/`
    - Line 200: Output path -> `${research_dir}/COMPARISON-{topic-slug}.md`
    - Line 215: `git add .planning/research/COMPARISON-...` -> `git add ${research_dir}/COMPARISON-...`
    - Line 235: Output section -> `${research_dir}/COMPARISON-{topic}.md`

    **commands/feasibility.md (8 occurrences):**

    Add init call at beginning:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init feasibility "$APPROACH")
    ```
    Parse JSON for: `research_dir`, `landscape_exists`, `deep_dives`.

    Replace all `.planning/research/` paths:
    - Lines 17-18: Context section -> `${research_dir}/` pattern
    - Line 38: `Load deep-dive analysis if available (.planning/research/deep-dives/{slug}.md)` -> `...${research_dir}/deep-dives/{slug}.md`
    - Line 224: Output path -> `${research_dir}/deep-dives/{paper-slug}-feasibility.md`
    - Lines 239-240: `git add .planning/research/...` -> `git add ${research_dir}/...`
    - Lines 259-261: Output section -> `${research_dir}/...`

    **commands/new-project.md (27 occurrences):**

    This already calls `grd-tools.js init new-project`. After Phase 33, init output includes `research_dir`, `codebase_dir`. Parse these from INIT JSON.

    Replace operational paths:
    - Line 194: `.planning/codebase/ARCHITECTURE.md` -> `${codebase_dir}/ARCHITECTURE.md`
    - Line 250: `mkdir -p .planning/research` -> `mkdir -p ${research_dir}`
    - Lines 253-319 (Create research files): Replace all `.planning/research/` -> `${research_dir}/` in write paths (LANDSCAPE.md, PAPERS.md, BENCHMARKS.md, KNOWHOW.md) and git commit --files paths
    - Lines 532, 588, 628, 668, 708: Research subagent output paths `Write to: .planning/research/...` -> `Write to: ${research_dir}/...`
    - Lines 724-727, 731: Research file references -> `${research_dir}/...`
    - Line 750: `Files: \`.planning/research/\`` -> `Files: \`${research_dir}/\``
    - Line 924: `@.planning/research/SUMMARY.md` -> `@${research_dir}/SUMMARY.md`
    - Lines 1114-1118: Output table -> `${research_dir}/...` pattern
    - Lines 1150-1159: Output file list -> `${research_dir}/...`

    **commands/product-plan.md (8 occurrences):**

    Add init call:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init product-plan)
    ```
    Parse JSON for: `research_dir`, `phases_dir`.

    Replace all paths:
    - Lines 21-23: Context section -> `${research_dir}/` pattern
    - Lines 43-44: Load research -> `${research_dir}/` paths
    - Line 253: `mkdir -p .planning/phases/{N}-{phase-name}/` -> `mkdir -p ${phases_dir}/{N}-{phase-name}/`
    - Line 263: `git add .planning/phases/` -> `git add ${phases_dir}/`
    - Line 285: Output -> `${phases_dir}/{N}-{name}/`

    **After all 6 files are modified, verify each file:**
    - YAML frontmatter is valid (has `---` delimiters)
    - No broken markdown structure
    - Zero occurrences of `.planning/research/` or `.planning/phases/` or `.planning/codebase/` in operational paths (grep check)
  </action>
  <verify>
    For each of the 6 files, run:
    grep -c "\.planning/research/" commands/{file}.md — should return 0 for operational paths (Level 1: Sanity)
    grep -c "\.planning/phases/" commands/{file}.md — should return 0 for operational paths (Level 1: Sanity)
    Frontmatter validation: each file starts with `---` and has valid YAML block (Level 1: Sanity)
  </verify>
  <done>6 research-focused command files migrated: all operational .planning/ subdirectory paths replaced with init-derived variables; init calls added where missing; YAML frontmatter intact</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining research/phase commands (eval-plan, eval-report, iterate, plan-phase, research-phase)</name>
  <files>commands/eval-plan.md, commands/eval-report.md, commands/iterate.md, commands/plan-phase.md, commands/research-phase.md</files>
  <action>
    **commands/eval-plan.md (10 occurrences):**

    Add init call at beginning:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init eval-plan "$PHASE")
    ```
    Parse JSON for: `research_dir`, `phases_dir`, `phase_dir`.

    Replace all paths:
    - Lines 18-20: Context section `.planning/phases/{N}-{name}/` -> `${phase_dir}/` and `.planning/research/` -> `${research_dir}/`
    - Line 23-24: Research references -> `${research_dir}/`
    - Line 39: Resolve to `.planning/phases/{N}-{name}/` -> resolve using `${phases_dir}/{N}-{name}/` or `${phase_dir}/`
    - Line 46: `ls .planning/phases/{N}-{name}/` -> `ls ${phase_dir}/`
    - Line 250: Path reference -> `${phase_dir}/{N}-EVAL.md`
    - Line 271: `git add .planning/phases/{N}-{name}/EVAL.md` -> `git add ${phase_dir}/*-EVAL.md`
    - Line 290: Output section -> `${phase_dir}/{N}-EVAL.md`

    **commands/eval-report.md (8 occurrences):**

    Add init call:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init eval-report "$PHASE")
    ```
    Parse JSON for: `research_dir`, `phases_dir`, `phase_dir`.

    Replace all paths:
    - Lines 17-21: Context section -> `${phase_dir}/`, `${research_dir}/`
    - Line 33: Resolve -> `${phase_dir}/`
    - Line 38: Path reference -> `${phase_dir}/EVAL.md`
    - Line 187: Populate -> `${phase_dir}/EVAL.md`
    - Line 243: `git add .planning/phases/{N}-{name}/EVAL.md` -> `git add ${phase_dir}/*-EVAL.md`
    - Line 275: Output -> `${phase_dir}/EVAL.md`

    **commands/iterate.md (8 occurrences):**

    Add init call:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init iterate "$PHASE")
    ```
    Parse JSON for: `research_dir`, `phases_dir`, `phase_dir`.

    Replace all paths:
    - Lines 18-22: Context section -> `${phase_dir}/`, `${research_dir}/`
    - Line 35: Resolve -> `${phase_dir}/`
    - Lines 254-255: `git add .planning/phases/{N}-{name}/...` -> `git add ${phase_dir}/...`
    - Lines 295-296: Output section -> `${phase_dir}/`

    **commands/plan-phase.md (8 occurrences):**

    This already calls `grd-tools.js init plan-phase`. After Phase 33, init output includes `research_dir`, `phases_dir`. Parse these from INIT JSON (add to the existing Parse JSON line on line 26).

    Replace remaining `.planning/research/` paths (the init-derived paths should replace hardcoded ones):
    - Lines 34, 37-39: Research loading section -> use `${research_dir}/`
    - Line 44: `ls .planning/research/deep-dives/*.md` -> `ls ${research_dir}/deep-dives/*.md`
    - Line 57: `mkdir -p ".planning/phases/${padded_phase}-${phase_slug}"` -> `mkdir -p "${phases_dir}/${padded_phase}-${phase_slug}"` (or use `phase_dir` if available from init)
    - Lines 473-474: References -> `${phase_dir}/*-PLAN.md`, `${phase_dir}/*-EVAL.md`

    **commands/research-phase.md (3 occurrences):**

    This command uses `${PHASE}` variable patterns. Check if it calls init.

    Replace paths:
    - Line 34: `ls .planning/phases/${PHASE}-*/RESEARCH.md` -> `ls ${phases_dir}/${PHASE}-*/RESEARCH.md` (need init for `phases_dir`)
    - Line 45: `cat .planning/phases/${PHASE}-*/*-CONTEXT.md` -> `cat ${phases_dir}/${PHASE}-*/*-CONTEXT.md`
    - Line 66: `Write to: .planning/phases/${PHASE}-{slug}/${PHASE}-RESEARCH.md` -> `Write to: ${phase_dir}/${PHASE}-RESEARCH.md`

    Add init call if not present:
    ```bash
    INIT=$(node ${CLAUDE_PLUGIN_ROOT}/bin/grd-tools.js init plan-phase "$PHASE")
    ```
    Parse JSON for: `phases_dir`, `phase_dir`, `research_dir`.

    **After all 5 files are modified, verify each file:**
    - YAML frontmatter is valid
    - No broken markdown structure
    - Zero occurrences of hardcoded `.planning/research/` or `.planning/phases/` in operational paths
  </action>
  <verify>
    For each of the 5 files, run:
    grep -c "\.planning/research/\|\.planning/phases/" commands/{file}.md — should return 0 for operational paths (Level 1: Sanity)
    Frontmatter validation: each file starts with `---` and has valid YAML block (Level 1: Sanity)
  </verify>
  <done>5 remaining research/phase command files migrated: all operational .planning/ subdirectory paths replaced with init-derived variables; init calls added where missing; YAML frontmatter intact</done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Each modified file has valid YAML frontmatter; `grep -rn "\.planning/research/\|\.planning/phases/\|\.planning/codebase/" commands/survey.md commands/deep-dive.md commands/compare-methods.md commands/feasibility.md commands/new-project.md commands/eval-plan.md commands/eval-report.md commands/product-plan.md commands/iterate.md commands/plan-phase.md commands/research-phase.md` returns zero results in operational paths
Level 2 (Proxy): N/A — markdown files have no unit tests; validation is structural
Level 3 (Deferred): Phase 36 integration tests validate that commands with new path variables work correctly end-to-end
</verification>

<success_criteria>
- Zero hardcoded `.planning/research/`, `.planning/phases/`, `.planning/codebase/` paths in operational bash commands across all 11 command files
- Commands that lacked init calls now include them (deep-dive, compare-methods, feasibility, eval-report, iterate, product-plan, research-phase)
- All 11 files have valid YAML frontmatter
- All path references use `${research_dir}`, `${phases_dir}`, `${phase_dir}`, or `${codebase_dir}` variables from init context
</success_criteria>

<output>
After completion, create `.planning/phases/34-command-agent-markdown-migration/34-01-SUMMARY.md`
</output>
