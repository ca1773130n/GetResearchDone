---
phase: 05-ci-cd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/ci.yml, README.md]
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "ci.yml triggers on push to main and pull_request to main"
    - "CI runs tests on Node.js 18, 20, and 22 via matrix strategy"
    - "CI executes lint, test with coverage, and npm audit steps"
    - "README.md contains a CI status badge linking to the workflow"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline definition"
      contains: "matrix:"
    - path: "README.md"
      provides: "Updated readme with CI badge"
      contains: "actions/workflows/ci.yml"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm scripts"
      pattern: "npm (run |ci|test|audit)"
---

<objective>
Create the GitHub Actions CI workflow and add a status badge to README.md.

Purpose: Automate lint, test, and security checks on every push/PR to main, ensuring code quality regressions are caught before merge. The CI badge in README.md provides at-a-glance build status.

Output: `.github/workflows/ci.yml` and updated `README.md` with badge.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@jest.config.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml` with the following configuration:

    **Trigger:**
    - `push` to `main` branch
    - `pull_request` targeting `main` branch

    **Job: test**
    - `runs-on: ubuntu-latest`
    - Strategy matrix: `node-version: [18, 20, 22]`
    - Steps:
      1. `actions/checkout@v4`
      2. `actions/setup-node@v4` with `node-version: ${{ matrix.node-version }}`
      3. `npm ci` (clean install from lockfile; if no lockfile exists, use `npm install`)
      4. `npm run lint` (will fail until Phase 6 replaces placeholder -- add `continue-on-error: true` with a comment noting this is temporary until ESLint is configured in Phase 6)
      5. `npm test` (runs jest --coverage)
      6. `npm audit --audit-level=moderate` (security audit; use `continue-on-error: true` since advisory resolution may not always be immediate)

    **Important details:**
    - Use `fail-fast: false` in the matrix strategy so all Node versions are tested even if one fails
    - The lint step MUST have `continue-on-error: true` because the current `npm run lint` script exits with error (placeholder from Phase 1 -- will be replaced in Phase 6)
    - Add a comment `# TODO(phase-6): remove continue-on-error after ESLint is configured` on the lint step
    - Do NOT install any additional dependencies beyond what's in package.json
    - The workflow file must use proper YAML syntax with no tabs
  </action>
  <verify>
    Validate YAML syntax:
    node -e "const fs=require('fs'); const y=require('yaml'); y.parse(fs.readFileSync('.github/workflows/ci.yml','utf8')); console.log('YAML valid')" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')"

    Verify key content:
    grep -q "node-version:" .github/workflows/ci.yml && echo "matrix: OK"
    grep -q "npm test" .github/workflows/ci.yml && echo "test step: OK"
    grep -q "npm audit" .github/workflows/ci.yml && echo "audit step: OK"
    grep -q "push:" .github/workflows/ci.yml && echo "push trigger: OK"
    grep -q "pull_request:" .github/workflows/ci.yml && echo "PR trigger: OK"
    (Level 1: Sanity -- YAML valid, key sections present)
  </verify>
  <done>
    .github/workflows/ci.yml exists with push/PR triggers for main, Node 18/20/22 matrix, and lint/test/audit steps. YAML parses without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CI status badge to README.md</name>
  <files>README.md</files>
  <action>
    Add a CI status badge to README.md immediately after the `# GRD -- Get Research Done` heading (line 1).

    The badge should use the GitHub Actions badge format. Since the repository is `ca1773130n/GRD` (visible from git remote or the README's existing GitHub link):

    ```markdown
    [![CI](https://github.com/ca1773130n/GRD/actions/workflows/ci.yml/badge.svg)](https://github.com/ca1773130n/GRD/actions/workflows/ci.yml)
    ```

    Insert this on a new line immediately after the `# GRD -- Get Research Done` heading, with a blank line before and after. If the heading is followed by text, ensure the badge goes between the heading and the first paragraph.

    Do NOT change any other content in README.md.
  </action>
  <verify>
    grep -q "actions/workflows/ci.yml/badge.svg" README.md && echo "badge: OK"
    grep -q "ca1773130n/GRD" README.md && echo "repo link: OK"
    (Level 1: Sanity -- badge text present)
  </verify>
  <done>
    README.md contains a CI status badge after the main heading, linking to the GitHub Actions CI workflow.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- .github/workflows/ci.yml exists and is valid YAML
- ci.yml contains push and pull_request triggers
- ci.yml contains node-version matrix with 18, 20, 22
- ci.yml contains npm test and npm audit steps
- README.md contains the CI badge markdown

Level 2 (Proxy): N/A -- workflow can only be fully tested when pushed to GitHub

Level 3 (Deferred): N/A
</verification>

<success_criteria>
- .github/workflows/ci.yml exists with correct triggers, matrix, and steps
- README.md has CI badge
- YAML syntax validates without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-cd-pipeline/05-01-SUMMARY.md`
</output>
