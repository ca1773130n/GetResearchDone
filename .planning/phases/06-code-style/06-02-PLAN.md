---
phase: 06-code-style
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [bin/grd-tools.js, bin/grd-manifest.js, lib/utils.js, lib/frontmatter.js, lib/state.js, lib/roadmap.js, lib/phase.js, lib/tracker.js, lib/verify.js, lib/scaffold.js, lib/context.js, lib/commands.js, .github/workflows/ci.yml, tests/helpers/setup.js, tests/helpers/fixtures.js, jest.config.js]
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "npm run lint exits 0 with zero errors and zero warnings"
    - "npm run format:check exits 0 (all files formatted)"
    - "npm test passes with existing coverage thresholds"
    - "CI lint step no longer has continue-on-error (lint failures fail CI)"
    - "DEFER-06-01 validated: lint rules do not break valid codebase patterns"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with lint enforcement (no continue-on-error on lint step)"
    - path: "bin/grd-tools.js"
      provides: "Formatted and lint-clean CLI router"
    - path: "lib/utils.js"
      provides: "Formatted and lint-clean shared utilities"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run lint (must pass for CI to succeed)"
      pattern: "npm run lint"
    - from: "lib/utils.js"
      to: "lib/frontmatter.js"
      via: "Module imports still work after formatting"
      pattern: "require.*utils"
---

<objective>
Auto-format all source files with Prettier, auto-fix lint errors with ESLint, manually fix any remaining errors, update CI to enforce lint, and validate DEFER-06-01.

Purpose: Apply code style tooling across the entire codebase, ensure zero lint errors and consistent formatting, and make lint failures block CI.
Output: All JS files formatted and lint-clean, CI enforcing lint, DEFER-06-01 resolved.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-code-style/06-01-SUMMARY.md
@.eslintrc.json
@.prettierrc
@.prettierignore
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-format and auto-fix all source and test files</name>
  <files>bin/grd-tools.js, bin/grd-manifest.js, lib/utils.js, lib/frontmatter.js, lib/state.js, lib/roadmap.js, lib/phase.js, lib/tracker.js, lib/verify.js, lib/scaffold.js, lib/context.js, lib/commands.js, tests/helpers/setup.js, tests/helpers/fixtures.js, jest.config.js</files>
  <action>
    1. Run Prettier auto-format on all JS files:
       ```
       npx prettier --write "bin/**/*.js" "lib/**/*.js" "tests/**/*.js" jest.config.js
       ```
       This reformats all files to match .prettierrc conventions. Since .prettierrc was designed to match existing style, changes should be minimal (trailing commas, line length adjustments).

    2. Run ESLint auto-fix on source files:
       ```
       npx eslint --fix bin/ lib/
       ```
       This fixes most lint errors automatically (unused imports, const vs let, etc.).

    3. Run ESLint WITHOUT --fix to see remaining errors:
       ```
       npx eslint bin/ lib/
       ```
       Capture the output. If errors remain, fix them manually:

       **Expected manual fixes (based on codebase patterns):**
       - `no-unused-vars`: Some variables may be declared but unused in edge-case branches. Options:
         a. Prefix with underscore: `_unusedVar`
         b. Remove if truly dead code
         c. Add eslint-disable-next-line comment ONLY as last resort
       - `no-fallthrough`: Switch cases without break in grd-tools.js router. Add `// falls through` comment or explicit break.
       - `no-redeclare`: Any re-declared variables in same scope. Rename or restructure.
       - `no-prototype-builtins`: Replace `obj.hasOwnProperty(key)` with `Object.prototype.hasOwnProperty.call(obj, key)` or `Object.hasOwn(obj, key)` (Node 18+ supports Object.hasOwn).

       **If a rule fights a valid codebase pattern:** Disable the specific rule for that line/block with an inline comment, and document why. Do NOT disable rules globally — DEFER-06-01 requires proving rules are compatible.

    4. Run Prettier format check to confirm all files are formatted:
       ```
       npx prettier --check "bin/**/*.js" "lib/**/*.js" "tests/**/*.js" jest.config.js
       ```

    5. Run the full test suite to confirm nothing broke:
       ```
       npm test
       ```
       All existing tests must pass. If Prettier reformatted string literals or test expectations, fix the tests to match. The key concern is golden snapshot tests — if Prettier changes output format, golden files may need regeneration.

    6. If any test fails due to formatting changes:
       - For golden snapshot tests: re-run the golden capture if output format changed
       - For unit tests: update string expectations to match new formatting
       - For integration tests: ensure CLI output is not affected by source formatting (it should not be, since output is JSON)
  </action>
  <verify>
    - `npx eslint bin/ lib/` exits 0 with no errors (Level 2: Proxy — proves lint rules compatible with all 6,779 lines of source code)
    - `npx prettier --check "bin/**/*.js" "lib/**/*.js" "tests/**/*.js" jest.config.js` exits 0 (Level 2: Proxy — proves all files formatted)
    - `npm test` exits 0 with all tests passing (Level 2: Proxy — proves formatting did not break functionality)
  </verify>
  <done>
    All bin/ and lib/ JS files pass ESLint with zero errors and zero warnings. All JS files formatted by Prettier. Full test suite passes confirming no behavioral regression from formatting changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI to enforce lint and validate DEFER-06-01</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. Edit `.github/workflows/ci.yml`:
       - Remove `continue-on-error: true` from the Lint step
       - Remove the `# TODO(phase-6)` comment above the Lint step
       - Add a format check step after Lint:
         ```yaml
         - name: Format check
           run: npm run format:check
         ```
       The updated Lint step should be:
       ```yaml
       - name: Lint
         run: npm run lint
       ```
       No continue-on-error, no TODO comment. Lint failures now fail CI.

    2. Validate DEFER-06-01 by confirming:
       - `npm run lint` exits 0 (zero errors, zero warnings)
       - `npm run format:check` exits 0 (all files formatted)
       - `npm test` exits 0 (all tests pass — lint rules did not break any code patterns)

       Document validation in the SUMMARY: DEFER-06-01 resolved because:
       - eslint:recommended with 3 rule overrides produces zero errors across all 12 JS source files (6,779 lines)
       - Overrides are targeted: argsIgnorePattern for unused args, allowEmpty catches, no checkLoops
       - All 300+ existing tests pass after auto-formatting
  </action>
  <verify>
    - `cat .github/workflows/ci.yml | grep -c "continue-on-error"` returns 1 (only the audit step, not lint) (Level 1: Sanity)
    - `npm run lint` exits 0 (Level 2: Proxy)
    - `npm run format:check` exits 0 (Level 2: Proxy)
    - `npm test` exits 0 (Level 2: Proxy)
  </verify>
  <done>
    CI workflow updated: lint step enforced without continue-on-error, format check added. DEFER-06-01 validated — ESLint rules are compatible with all codebase patterns, zero errors/warnings across 12 source files and 6,779 lines of code.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- .github/workflows/ci.yml has no continue-on-error on lint step
- .github/workflows/ci.yml has format check step

Level 2 (Proxy):
- npm run lint exits 0 with zero errors, zero warnings across all 12 source files
- npm run format:check exits 0 across all JS files
- npm test exits 0 with all tests passing and existing coverage thresholds met
- DEFER-06-01 validated: lint rules compatible with codebase (documented in SUMMARY)

Level 3 (Deferred):
- None — Phase 6 fully validates in-phase via proxy metrics
</verification>

<success_criteria>
- `npm run lint` passes with zero errors, zero warnings
- `npm run format:check` passes (all files formatted)
- `npm test` passes with existing coverage thresholds
- CI runs lint and format checks without continue-on-error
- DEFER-06-01 resolved and documented
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-style/06-02-SUMMARY.md`
</output>
