---
phase: deferred-validations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/unit/roadmap-roundtrip.test.js"
autonomous: true
verification_level: full

must_haves:
  truths:
    - "Full roadmap lifecycle round-trips without data loss: generateLongTermRoadmap -> parseLongTermRoadmap -> refineMilestone -> parseLongTermRoadmap -> promoteMilestone -> parseLongTermRoadmap preserves all milestone data (DEFER-11-01)"
    - "Tier promotion chain works: Later -> Next -> Now, with each step preserving goal, success_criteria, and dependencies (DEFER-11-01)"
    - "Refinement of multiple fields (goal, success_criteria, rough_phase_sketch, open_questions) across multiple milestones does not corrupt other milestones (DEFER-11-01)"
    - "updateRefinementHistory appends entries correctly across multiple operations without duplicating or losing prior history rows (DEFER-11-01)"
    - "Generated ROADMAP.md content from a round-tripped long-term roadmap matches the original data semantically (DEFER-11-01)"
  artifacts:
    - path: "tests/unit/roadmap-roundtrip.test.js"
      provides: "Long-term roadmap lifecycle round-trip integrity tests"
      min_lines: 150
  key_links:
    - from: "tests/unit/roadmap-roundtrip.test.js"
      to: "lib/long-term-roadmap.js"
      via: "import and exercise full lifecycle: generate, parse, validate, refine, promote, updateRefinementHistory"
      pattern: "require.*long-term-roadmap"
---

<objective>
Validate the full long-term roadmap lifecycle round-trips without data loss (DEFER-11-01).

Purpose: Confirm that the hierarchical roadmap can survive create -> refine -> promote through all tiers -> regenerate without losing data, corrupting format, or dropping milestones.

Output: A comprehensive round-trip test file that exercises the complete lifecycle including multi-step promotion chains and concurrent refinements.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/long-term-roadmap.js
@tests/unit/long-term-roadmap.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full lifecycle round-trip integrity tests (DEFER-11-01)</name>
  <files>tests/unit/roadmap-roundtrip.test.js</files>
  <action>
    Create `tests/unit/roadmap-roundtrip.test.js` that validates the complete long-term roadmap lifecycle. The existing `tests/unit/long-term-roadmap.test.js` tests individual functions in isolation. This file must test the COMPLETE lifecycle chain with multi-step operations.

    Import from lib/long-term-roadmap.js: parseLongTermRoadmap, validateLongTermRoadmap, generateLongTermRoadmap, refineMilestone, promoteMilestone, updateRefinementHistory, getMilestoneTier.

    **Test Group 1: Full Create->Parse Round-Trip**
    1. Create a milestone array with 5 milestones (1 Now, 2 Next, 2 Later) including all fields: goal, success_criteria, rough_phase_sketch, open_questions, open_research_questions, dependencies, estimated dates.
    2. Generate with generateLongTermRoadmap().
    3. Parse back with parseLongTermRoadmap().
    4. Verify ALL data survives: frontmatter fields, all 5 milestones in correct tiers, all success criteria items, all phase sketch items, all open questions, all dependency strings.
    5. Validate parsed result passes validateLongTermRoadmap() with valid=true and no errors.

    **Test Group 2: Refine->Parse Round-Trip**
    1. Start from generated roadmap content.
    2. Refine a Next milestone's goal.
    3. Parse the result and verify: refined milestone has new goal, ALL other milestones unchanged (verify each field explicitly, not just count).
    4. Refine the same milestone's success_criteria.
    5. Parse again: verify both goal and success_criteria reflect changes, everything else intact.
    6. Refine a DIFFERENT milestone (Later tier) rough_phase_sketch.
    7. Parse: verify both refinements are present, unrelated milestones unchanged.

    **Test Group 3: Multi-Step Promotion Chain**
    1. Start with 5 milestones: 1 Now, 2 Next, 2 Later.
    2. Promote first Later -> Next. Parse. Verify: 1 Now, 3 Next, 1 Later. Promoted milestone has its goal and success_criteria preserved.
    3. Promote first Next -> Now. Parse. Verify: new Now is correct, old Now is gone from output (or archived), 2 Next, 1 Later.
    4. At each step verify parseLongTermRoadmap and validateLongTermRoadmap succeed.

    **Test Group 4: Combined Refine + Promote Chain**
    1. Generate roadmap.
    2. Refine a Later milestone (add detailed success_criteria, rough_phase_sketch).
    3. Promote it Later -> Next.
    4. Refine it again in Next tier (update goal).
    5. Promote it Next -> Now.
    6. Parse final result. Verify: all refinements from steps 2 and 4 are present in the Now milestone.

    **Test Group 5: Refinement History Accumulation**
    1. Generate roadmap (has initial history entry).
    2. updateRefinementHistory with "Refined v0.2.0 goal".
    3. updateRefinementHistory with "Promoted v0.4.0 to Next".
    4. updateRefinementHistory with "Refined v0.4.0 success criteria".
    5. Parse final: refinement_history has 4 entries (initial + 3 updates), all with correct dates and actions, in chronological order.

    **Test Group 6: Edge Cases for Data Preservation**
    1. Milestone with special characters in goal (quotes, parentheses, hyphens, colons).
    2. Success criteria with markdown formatting (bold, inline code, bullet sub-items).
    3. Open questions with question marks, colons, links.
    4. Verify all survive round-trip without corruption.

    Each test group should be a describe() block. Use the same milestone fixture data but each test should be independent (start from the generated content, not share mutable state between tests).
  </action>
  <verify>
    npx jest tests/unit/roadmap-roundtrip.test.js --verbose
    All tests pass. Minimum 15 test cases covering: create+parse round-trip, multi-step refinement, multi-step promotion chain, combined refine+promote, history accumulation, edge cases.
  </verify>
  <done>
    tests/unit/roadmap-roundtrip.test.js exists with 15+ passing tests validating the full long-term roadmap lifecycle including multi-step operations. No data loss through any lifecycle path. DEFER-11-01 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate generateLongTermRoadmap -> ROADMAP.md generation integrity</name>
  <files>tests/unit/roadmap-roundtrip.test.js</files>
  <action>
    Add a test group to `tests/unit/roadmap-roundtrip.test.js` (created in Task 1):

    **Test Group 7: ROADMAP.md Generation Integrity**

    This validates the ROADMAP.md generation aspect of the round-trip, which is the final step in the lifecycle (create long-term roadmap -> refine -> promote -> generate ROADMAP.md for active milestone).

    Note: The ROADMAP.md is a DIFFERENT document from LONG-TERM-ROADMAP.md. The long-term roadmap module generates the hierarchical LONG-TERM-ROADMAP.md. The cmdLongTermRoadmap command in lib/commands.js orchestrates viewing/editing. Verify:

    1. After a full promotion chain (Later->Next->Now), the final Now milestone has correct data that could be used to populate a ROADMAP.md phase list.
    2. generateLongTermRoadmap produces valid markdown that passes both parseLongTermRoadmap and validateLongTermRoadmap.
    3. The Milestone Dependency Graph section is present and contains version strings.
    4. The Refinement History table is present and well-formed after multiple operations.

    If cmdLongTermRoadmap from lib/commands.js has a "generate" subcommand, test it too. Otherwise, focus on the lib/long-term-roadmap.js functions and their round-trip integrity.
  </action>
  <verify>
    npx jest tests/unit/roadmap-roundtrip.test.js --verbose
    All test groups pass including the new generation integrity tests.
  </verify>
  <done>
    Roadmap generation integrity validated: generated markdown is parseable, validatable, and contains all expected sections (dependency graph, refinement history). Full lifecycle tested end-to-end. DEFER-11-01 fully resolved.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Test file parses, all tests pass
Level 2 (Proxy): Individual lifecycle steps (generate, refine, promote) produce valid output
Level 3 (Full): Multi-step lifecycle chains preserve all data, combined operations work correctly, edge cases handled
</verification>

<success_criteria>
- tests/unit/roadmap-roundtrip.test.js: 15+ tests all passing
- Full lifecycle chain validated: create -> refine -> promote -> refine -> promote -> validate
- No data loss at any step (milestones, fields, history entries)
- Edge cases (special characters, markdown formatting) survive round-trip
- npx jest tests/unit/roadmap-roundtrip.test.js passes with 0 failures
- DEFER-11-01 resolved
</success_criteria>

<output>
After completion, create `.planning/phases/15-deferred-validations/15-02-SUMMARY.md`
</output>
