---
phase: deferred-validations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/unit/backend-real-env.test.js"
  - "tests/unit/context-backend-compat.test.js"
autonomous: true
verification_level: full

must_haves:
  truths:
    - "detectBackend() returns correct backend for each of the 4 backend environment variable patterns when set individually in isolated test environments (DEFER-09-01)"
    - "detectBackend() waterfall priority is config > env > filesystem > default, verified with multi-signal scenarios (DEFER-09-01)"
    - "All 14 cmdInit* functions produce valid JSON with backend and backend_capabilities fields under each of the 4 backends (DEFER-10-01)"
    - "When backend is claude (default), all cmdInit* output matches the established fixture expectations with no regressions (DEFER-10-01)"
    - "Edge cases documented: AGENT env var ignored for OpenCode, malformed config falls through, multiple env vars from different backends resolved by waterfall order (DEFER-09-01)"
  artifacts:
    - path: "tests/unit/backend-real-env.test.js"
      provides: "Real-environment backend detection validation tests"
      min_lines: 80
    - path: "tests/unit/context-backend-compat.test.js"
      provides: "Context init backward compatibility tests across all 4 backends"
      min_lines: 100
  key_links:
    - from: "tests/unit/backend-real-env.test.js"
      to: "lib/backend.js"
      via: "import and exercise detectBackend, getBackendCapabilities"
      pattern: "require.*backend"
    - from: "tests/unit/context-backend-compat.test.js"
      to: "lib/context.js"
      via: "import and exercise all 14 cmdInit* functions"
      pattern: "require.*context"
---

<objective>
Validate backend detection accuracy and context init backward compatibility across all 4 backends (DEFER-09-01 + DEFER-10-01).

Purpose: Confirm that detectBackend() returns correct results with real environment variable patterns and that all 14 cmdInit* functions produce correct output under each backend, with no regressions when backend is claude.

Output: Two new test files validating the deferred items under real-ish conditions (process.env manipulation without mocking the detection function itself).
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/backend.js
@lib/context.js
@tests/unit/backend.test.js
@tests/unit/context.test.js
@tests/helpers/setup.js
@tests/helpers/fixtures.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-environment backend detection validation (DEFER-09-01)</name>
  <files>tests/unit/backend-real-env.test.js</files>
  <action>
    Create a dedicated test file `tests/unit/backend-real-env.test.js` that validates detectBackend() under real-ish conditions. The existing `tests/unit/backend.test.js` already tests basic detection but uses simple env var patterns. This test file must go deeper:

    1. **Complete waterfall priority tests**: Set up scenarios where multiple detection signals are present simultaneously (e.g., config says codex, env says claude, filesystem says gemini) and verify the waterfall order: config > env > filesystem > default.

    2. **All 4 backend environment variable patterns**: For each backend, set the actual env vars that real users would have:
       - Claude: CLAUDE_CODE_ENTRYPOINT, CLAUDE_CODE_ACTION, CLAUDE_CODE_ENABLE_TELEMETRY, CLAUDE_CODE_SSE_PORT (test each individually + combinations)
       - Codex: CODEX_HOME, CODEX_THREAD_ID (test each individually + together)
       - Gemini: GEMINI_CLI_HOME (single var)
       - OpenCode: OPENCODE (single var)

    3. **Edge case documentation tests**: Create tests that explicitly document known edge cases with descriptive test names:
       - `AGENT env var is NOT used for OpenCode detection (PITFALLS.md P5)` — already in basic tests, but add test with AGENT + other signals
       - `Multiple CLAUDE_CODE_* vars all trigger claude detection` — test with 2-3 different CLAUDE_CODE_ vars
       - `Empty string env vars are still detected as present` — verify hasEnvPrefix behavior
       - `Config with invalid backend value falls through to env/filesystem/default`
       - `Config with non-string backend value falls through`

    4. **Filesystem clue isolation**: Create temp dirs with filesystem clues for each backend (.claude-plugin/plugin.json, .codex/config.toml, .gemini/settings.json, opencode.json) and verify detection with NO env vars set.

    5. **getBackendCapabilities cross-verification**: For each backend detected, verify that getBackendCapabilities returns the expected capability set.

    Use the same save/restore process.env pattern from tests/unit/backend.test.js (save in beforeEach, clear detection vars, restore in afterEach). Use createTempDir helper from the existing test. Each test should be independent.

    IMPORTANT: Do NOT mock detectBackend itself. The point is to exercise the real detection waterfall with real env var manipulation.
  </action>
  <verify>
    npx jest tests/unit/backend-real-env.test.js --verbose
    All tests pass. Minimum 15 test cases covering: 4 backends x env detection, 4 backends x filesystem clues, 4+ waterfall priority scenarios, 3+ edge cases.
  </verify>
  <done>
    tests/unit/backend-real-env.test.js exists with 15+ passing tests covering all 4 backends' real env patterns, waterfall priority, filesystem clues, and documented edge cases. DEFER-09-01 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Context init backward compatibility under all backends (DEFER-10-01)</name>
  <files>tests/unit/context-backend-compat.test.js</files>
  <action>
    Create a dedicated test file `tests/unit/context-backend-compat.test.js` that validates all 14 cmdInit* functions under each of the 4 backends.

    1. **All 14 cmdInit* functions**: Test each function from lib/context.js:
       - cmdInitExecutePhase, cmdInitPlanPhase, cmdInitNewProject, cmdInitNewMilestone,
         cmdInitQuick, cmdInitResume, cmdInitVerifyWork, cmdInitPhaseOp,
         cmdInitTodos, cmdInitMilestoneOp, cmdInitMapCodebase, cmdInitProgress,
         cmdInitResearchWorkflow, cmdInitPlanMilestoneGaps

    2. **Backend-per-function matrix**: For each of the 14 functions, create a test that:
       - Sets up a config.json with `backend: "{backend}"` for codex, gemini, opencode
       - Calls the function with the test fixture directory
       - Verifies the output JSON has `backend: "{backend}"` and `backend_capabilities` matching BACKEND_CAPABILITIES[backend]
       - Verifies model fields are backend-resolved (e.g., codex executor_model contains 'gpt-5.3-codex')

    3. **Claude backward compatibility (regression check)**: Run all 14 functions with default config (no backend field = claude) and verify:
       - Output structure matches expected keys (backend, backend_capabilities, phase_found, etc.)
       - Model fields use claude model names (opus, sonnet, haiku)
       - No new fields or missing fields compared to baseline

    4. Use `captureOutput` from tests/helpers/setup.js for process.exit capture.
    5. Use `createFixtureDir` / `cleanupFixtureDir` from tests/helpers/fixtures.js for temp dirs.
    6. Use the test.each pattern for efficient backend x function matrix coverage.
    7. Each function may need specific args: cmdInitExecutePhase needs phase number, cmdInitPlanPhase needs phase number, cmdInitQuick needs description, cmdInitResearchWorkflow needs workflow+topic, etc.

    IMPORTANT: Use config override (writing backend to .planning/config.json) rather than env vars for backend switching. This tests the highest-priority detection path AND avoids env var interference between parallel tests.
  </action>
  <verify>
    npx jest tests/unit/context-backend-compat.test.js --verbose
    All tests pass. Minimum 20 test cases covering: 14 functions x claude baseline, representative sample (4+ functions) x 3 non-claude backends, model resolution verification.
  </verify>
  <done>
    tests/unit/context-backend-compat.test.js exists with 20+ passing tests. All 14 cmdInit* functions verified under claude, and representative functions verified under codex/gemini/opencode. Model fields are backend-resolved. No regressions under claude backend. DEFER-10-01 resolved.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Both test files parse without syntax errors, all tests pass
Level 2 (Proxy): Backend detection returns correct results for each env pattern; cmdInit* output shapes are valid JSON with expected fields
Level 3 (Full): All 4 backends fully exercised, waterfall priority verified, all 14 cmdInit* functions produce correct output under multiple backends, edge cases documented
</verification>

<success_criteria>
- tests/unit/backend-real-env.test.js: 15+ tests all passing, covering all 4 backends, waterfall priority, edge cases
- tests/unit/context-backend-compat.test.js: 20+ tests all passing, all 14 cmdInit* functions verified under claude, representative sample under other backends
- npx jest tests/unit/backend-real-env.test.js tests/unit/context-backend-compat.test.js passes with 0 failures
- DEFER-09-01 and DEFER-10-01 both resolved
</success_criteria>

<output>
After completion, create `.planning/phases/15-deferred-validations/15-01-SUMMARY.md`
</output>
