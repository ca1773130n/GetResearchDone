---
phase: 02-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/grd-tools.js
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "Zero shell-concatenated user input in execGit or isGitIgnored"
    - "execGit uses execFileSync with argument array, no shell interpolation"
    - "isGitIgnored uses execFileSync with argument array, no shell interpolation"
    - "Shell-based find command replaced with fs.readdirSync"
    - "process.env.HOME has null/undefined guard with os.homedir() fallback"
    - "Input validation functions exist: validatePhaseName, validateFilePath, validateGitRef"
    - "Path traversal (../) rejected in phase names and file paths"
    - "Git refs validated against safe character set"
  artifacts:
    - path: "bin/grd-tools.js"
      provides: "Hardened execGit, isGitIgnored, find replacement, input validators"
  key_links:
    - from: "bin/grd-tools.js execGit"
      to: "child_process.execFileSync"
      via: "direct call with argument array"
      pattern: "execFileSync\\('git'"
    - from: "bin/grd-tools.js isGitIgnored"
      to: "child_process.execFileSync"
      via: "direct call with argument array"
      pattern: "execFileSync\\('git'"
    - from: "bin/grd-tools.js normalizePhaseName callers"
      to: "validatePhaseName"
      via: "validation before use in file paths"
      pattern: "validatePhaseName|validateFilePath"
---

<objective>
Harden core shell execution functions and add input validation to bin/grd-tools.js.

Purpose: Eliminate command injection vectors in execGit, isGitIgnored, and the shell-based code file detection. Add input validation helpers that block path traversal and invalid git refs. Fix process.env.HOME null safety.

Output: bin/grd-tools.js with execFileSync-based git operations, input validators, and HOME fallback.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/workflows/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/grd-tools.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace execGit and isGitIgnored with execFileSync and fix HOME fallback</name>
  <files>bin/grd-tools.js</files>
  <action>
    1. Update the child_process require at line 119 to also import execFileSync:
       `const { execFileSync } = require('child_process');`
       Remove the execSync import entirely (it will no longer be needed after Plan 02 completes the gh CLI hardening).
       Actually, keep execSync for now since ghExec still uses it -- Plan 02 will remove it.
       So: `const { execSync, execFileSync } = require('child_process');`
       Also add `const os = require('os');` on the next line if not already present.

    2. Rewrite `isGitIgnored` function (currently at lines 234-243):
       - Replace the shell-concatenated execSync call with execFileSync('git', ['check-ignore', '-q', '--', targetPath])
       - Pass { cwd, stdio: 'pipe' } as options
       - No manual character escaping needed -- execFileSync passes args directly to the process
       - Keep the try/catch structure returning true/false

    3. Rewrite `execGit` function (currently at lines 246-265):
       - Remove the `escaped` mapping that manually shell-escapes arguments
       - Replace `execSync('git ' + escaped.join(' '), ...)` with `execFileSync('git', args, ...)`
       - Pass { cwd, stdio: 'pipe', encoding: 'utf-8' } as options
       - Keep the try/catch structure returning { exitCode, stdout, stderr }
       - The function signature `execGit(cwd, args)` stays the same, args is already an array at all call sites

    4. Replace the shell `find` command (currently at line ~3770) with Node.js fs:
       - Create a helper function `findCodeFiles(dir, maxDepth, found, depth)` that:
         * Returns early if depth > maxDepth or found.length >= 5
         * Uses fs.readdirSync(dir, { withFileTypes: true })
         * Skips 'node_modules' and '.git' directories
         * Checks file extensions against Set(['.ts', '.js', '.py', '.go', '.rs', '.swift', '.java'])
         * Recursively descends into subdirectories
       - Replace the execSync('find ...') call with: `const codeFiles = findCodeFiles(cwd, 3, [], 0);`
       - Set `hasCode = codeFiles.length > 0;`

    5. Fix the process.env.HOME usage at line ~2234:
       - Change `process.env.HOME || ''` to `process.env.HOME || os.homedir() || ''`
       - This ensures a valid home directory even if HOME env var is unset
  </action>
  <verify>
    Run these checks (Level 1: Sanity):
    1. Verify no shell-concatenated git calls remain:
       `grep -n "execSync('git " bin/grd-tools.js` -- must return nothing
    2. Verify execFileSync is used for git:
       `grep -n "execFileSync('git'" bin/grd-tools.js` -- must show results in execGit and isGitIgnored
    3. Verify find command is gone:
       `grep -n "execSync('find " bin/grd-tools.js` -- must return nothing
    4. Verify HOME fallback exists:
       `grep -n "os.homedir()" bin/grd-tools.js` -- must show at least 1 result
    5. Smoke test:
       `node bin/grd-tools.js state load` -- must produce valid JSON without errors
       `node bin/grd-tools.js frontmatter get .planning/STATE.md` -- must not crash
  </verify>
  <done>
    execGit uses execFileSync('git', argsArray) with no shell string concatenation.
    isGitIgnored uses execFileSync('git', ['check-ignore', ...]) with no shell string concatenation.
    Shell-based find command replaced with fs.readdirSync recursive implementation.
    process.env.HOME has os.homedir() fallback.
    All existing CLI commands still work (smoke tested).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add input validation helper functions and integrate at entry points</name>
  <files>bin/grd-tools.js</files>
  <action>
    Add three validation functions near the top of the file (after normalizePhaseName, around line 274):

    1. `validatePhaseName(phase)`:
       - Input: string phase identifier (e.g., "02", "02.1", "02-security-hardening")
       - Reject if contains `..` (path traversal) -- throw Error('Phase name must not contain path traversal (..)')
       - Reject if contains `/` or `\` (directory traversal) -- throw Error('Phase name must not contain directory separators')
       - Reject if contains null bytes -- throw Error('Phase name must not contain null bytes')
       - Reject if does not match `/^\d+(?:\.\d+)?(?:-[a-zA-Z0-9-]+)?$/` -- throw Error('Invalid phase name format: must be digits with optional decimal and kebab-case suffix')
       - Return the validated phase string on success

    2. `validateFilePath(filePath, cwd)`:
       - Input: string file path and string cwd (project root)
       - Reject if contains null bytes -- throw Error('File path must not contain null bytes')
       - Reject if the normalized path (path.resolve(cwd, filePath)) does not start with cwd -- throw Error('File path must not escape project directory')
       - This catches `../` traversal, absolute paths outside project, and symlink-based escapes
       - Return the validated file path on success

    3. `validateGitRef(ref)`:
       - Input: string git reference (hash, branch, tag)
       - Reject if starts with `-` -- throw Error('Git ref must not start with a dash (flag injection)')
       - Reject if contains `..` -- throw Error('Git ref must not contain path traversal (..)')
       - Reject if does not match `/^[a-zA-Z0-9._\-\/~^]+$/` -- throw Error('Git ref contains invalid characters')
       - Reject if length > 256 -- throw Error('Git ref too long')
       - Return the validated ref on success

    Then integrate validators at key entry points:

    4. In `normalizePhaseName` (line ~267): Add at the very start of the function:
       ```javascript
       if (typeof phase !== 'string') throw new Error('Phase must be a string');
       if (phase.includes('..')) throw new Error('Phase name must not contain path traversal (..)');
       if (phase.includes('/') || phase.includes('\\')) throw new Error('Phase name must not contain directory separators');
       ```
       This is a lightweight inline check -- the full `validatePhaseName` is for external call sites.

    5. In commit verification that accepts git hashes (search for `execGit(cwd, ['cat-file', '-t', hash])`):
       - At line ~1532 and ~2271, wrap the hash with validateGitRef before passing to execGit
       - Use try/catch around validateGitRef: if validation fails, return error result instead of passing bad input

    6. In `isGitIgnored`: Before the execFileSync call, validate the targetPath does not contain null bytes:
       `if (targetPath.includes('\0')) return false;`
  </action>
  <verify>
    Run these checks (Level 1: Sanity):
    1. `grep -c "validatePhaseName\|validateFilePath\|validateGitRef" bin/grd-tools.js` -- should show >= 6 (3 definitions + 3+ usages)
    2. `node bin/grd-tools.js state load` -- must produce valid JSON (regression check)
    3. `node bin/grd-tools.js verify plan-structure .planning/phases/01-security-foundation/01-01-PLAN.md` -- must not crash
    4. Test that path traversal is blocked: A phase name like "../etc" should be rejected by normalizePhaseName
  </verify>
  <done>
    Three input validation functions defined: validatePhaseName, validateFilePath, validateGitRef.
    Path traversal (..) blocked in normalizePhaseName and validateFilePath.
    Git refs validated before passing to execGit in commit verification.
    Null byte injection blocked in isGitIgnored and file path validation.
    All existing CLI commands continue to work (smoke tests pass).
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity):
- grep confirms zero shell-concatenated git commands
- grep confirms execFileSync usage in execGit and isGitIgnored
- grep confirms find shell command removed
- grep confirms validation functions exist
- node bin/grd-tools.js state load produces valid JSON
- node bin/grd-tools.js --help does not crash

Level 2 (Proxy -- verified in Plan 02-02):
- Manual test of git-touching commands (commit, verify commits, phase complete)
- Manual test that path traversal inputs are rejected

Level 3 (Deferred):
- Full CLI regression in Phase 4 (DEFER-03-01, DEFER-03-02)
</verification>

<success_criteria>
- Zero shell-concatenated git commands in execGit and isGitIgnored (verified by grep)
- execGit and isGitIgnored use execFileSync with argument arrays
- Shell-based find command replaced with Node.js fs implementation
- process.env.HOME has os.homedir() fallback
- Input validators (validatePhaseName, validateFilePath, validateGitRef) defined and integrated
- Path traversal blocked in phase names and file paths
- All existing CLI commands work (smoke test passes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-01-SUMMARY.md`
</output>
