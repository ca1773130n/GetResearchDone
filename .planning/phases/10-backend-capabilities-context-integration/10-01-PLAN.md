---
phase: 10-backend-capabilities-context-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/commands.js
  - bin/grd-tools.js
  - tests/unit/commands.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "`node bin/grd-tools.js detect-backend` returns JSON with `backend`, `models`, and `capabilities` fields"
    - "`node bin/grd-tools.js detect-backend --raw` returns just the backend name string (e.g., 'claude')"
    - "JSON `models` field contains resolved names for `opus`, `sonnet`, `haiku` tiers for the detected backend"
    - "JSON `capabilities` field contains boolean flags for `subagents`, `parallel`, `teams`, `hooks`, `mcp`"
    - "All existing tests pass (674+ total, zero regressions)"
  artifacts:
    - path: "lib/commands.js"
      provides: "cmdDetectBackend function"
      contains: "cmdDetectBackend"
    - path: "bin/grd-tools.js"
      provides: "detect-backend CLI route"
      contains: "detect-backend"
    - path: "tests/unit/commands.test.js"
      provides: "Unit tests for detect-backend"
      contains: "detect-backend"
  key_links:
    - from: "lib/commands.js"
      to: "lib/backend.js"
      via: "import detectBackend, resolveBackendModel, getBackendCapabilities"
      pattern: "require.*backend"
    - from: "bin/grd-tools.js"
      to: "lib/commands.js"
      via: "import and dispatch cmdDetectBackend"
      pattern: "cmdDetectBackend"
---

<objective>
Add `detect-backend` CLI command to grd-tools.js that returns comprehensive backend information as JSON (backend name, resolved model names, capability flags) or plain backend name with `--raw`.

Purpose: REQ-04 (detect-backend CLI) and REQ-05 (capabilities exposure). getBackendCapabilities() already exists in lib/backend.js from Phase 9. This plan exposes it through the CLI.
Output: Working `detect-backend` command with TDD tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@lib/backend.js
@lib/commands.js
@bin/grd-tools.js
@tests/unit/commands.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for detect-backend command</name>
  <files>tests/unit/commands.test.js</files>
  <action>
    Add a new `describe('cmdDetectBackend')` block to `tests/unit/commands.test.js`.

    Write tests that call `cmdDetectBackend(cwd, raw)` directly (unit-level), mocking `loadConfig` and backend detection as needed.

    Test cases to cover:
    1. **JSON output (raw=false):** Returns object with `backend` (string), `models` (object with opus/sonnet/haiku), `capabilities` (object with subagents/parallel/teams/hooks/mcp)
    2. **Raw output (raw=true):** Returns just the backend name string
    3. **Claude backend:** models are 'opus', 'sonnet', 'haiku'; capabilities all true
    4. **Codex backend:** models are 'gpt-5.3-codex', etc.; capabilities: subagents=true, parallel=true, teams=false, hooks=false, mcp=true
    5. **Gemini backend:** models are 'gemini-3-pro', etc.; capabilities: subagents='experimental', parallel=false, teams=false, hooks=true, mcp=true
    6. **OpenCode backend:** models use 'anthropic/claude-*' format; capabilities: subagents=true, parallel=true, teams=false, hooks=true, mcp=true
    7. **Config model overrides:** When config.backend_models has overrides, models field reflects them
    8. **Unknown backend (edge case):** Falls back to claude defaults

    Use the same pattern as existing tests in commands.test.js — mock `process.stdout.write` and `process.exit` to capture output. Set up CLAUDE_CODE_* env var cleanup like the utils.test.js integration tests do (dynamically discover and clean all CLAUDE_CODE_* env vars).

    Run the tests — they MUST fail (RED phase).

    Commit: `test(10-01): add failing tests for detect-backend command`
  </action>
  <verify>
    npx jest tests/unit/commands.test.js --testNamePattern="cmdDetectBackend" (Level 1: Sanity)
    Tests should FAIL because cmdDetectBackend does not exist yet.
  </verify>
  <done>
    8+ test cases written for cmdDetectBackend, all failing because the function does not exist. Tests committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Implement cmdDetectBackend and wire CLI route</name>
  <files>lib/commands.js, bin/grd-tools.js</files>
  <action>
    **Step 1: Implement cmdDetectBackend in lib/commands.js**

    Add import at top of lib/commands.js:
    ```js
    const { detectBackend, resolveBackendModel, getBackendCapabilities } = require('./backend');
    ```

    Add function `cmdDetectBackend(cwd, raw)`:
    1. Call `loadConfig(cwd)` to get config
    2. Call `detectBackend(cwd)` to get backend name
    3. For each tier ('opus', 'sonnet', 'haiku'), call `resolveBackendModel(backend, tier, config)` to build `models` object
    4. Call `getBackendCapabilities(backend)` to get `capabilities` object
    5. Build result: `{ backend, models: { opus, sonnet, haiku }, capabilities }`
    6. Call `output(result, raw, backend)` — raw mode returns just the backend name

    Export `cmdDetectBackend` from module.exports.

    **Step 2: Wire CLI route in bin/grd-tools.js**

    Add `cmdDetectBackend` to the destructured import from `../lib/commands`.

    Add a new case in the `routeCommand` switch:
    ```js
    case 'detect-backend':
      cmdDetectBackend(cwd, raw);
      break;
    ```

    Update the usage string in the error message at the top of main() to include `detect-backend`.

    **Step 3: Run all tests**

    Run `npx jest tests/unit/commands.test.js --testNamePattern="cmdDetectBackend"` to confirm all tests pass (GREEN).
    Run `npx jest` to confirm zero regressions (674+ total pass).

    Commit: `feat(10-01): implement detect-backend CLI command`
  </action>
  <verify>
    npx jest tests/unit/commands.test.js --testNamePattern="cmdDetectBackend" passes (Level 1: Sanity)
    npx jest passes with 674+ total tests and zero failures (Level 2: Proxy)
    node bin/grd-tools.js detect-backend outputs valid JSON (Level 2: Proxy)
    node bin/grd-tools.js detect-backend --raw outputs backend name string (Level 2: Proxy)
  </verify>
  <done>
    cmdDetectBackend implemented in lib/commands.js, CLI route wired in bin/grd-tools.js, all tests pass including 8+ new tests and 674+ existing tests.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Tests compile and run, function exists and exports correctly
Level 2 (Proxy): `node bin/grd-tools.js detect-backend` returns valid JSON with correct structure; `--raw` returns backend name; all 4 backends produce expected model/capability mappings; 674+ total tests pass
Level 3 (Deferred): End-to-end detection accuracy across real backend environments (Phase 15)
</verification>

<success_criteria>
1. `node bin/grd-tools.js detect-backend` returns JSON with `backend`, `models`, and `capabilities` fields
2. `node bin/grd-tools.js detect-backend --raw` returns just the backend name (e.g., 'claude')
3. All 4 backends produce correct model and capability mappings in tests
4. 674+ total tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-capabilities-context-integration/10-01-SUMMARY.md`
</output>
