---
phase: 36-test-updates-documentation-and-integration-validation
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/fixtures/planning/phases/
  - tests/fixtures/planning/todos/
  - tests/helpers/fixtures.js
  - tests/unit/coverage-gaps.test.js
  - tests/unit/verify.test.js
  - tests/unit/cleanup.test.js
  - tests/unit/cleanup-noninterference.test.js
  - tests/unit/scaffold.test.js
  - tests/unit/context.test.js
  - tests/unit/commands.test.js
  - tests/unit/phase.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Test fixture directory uses milestone-scoped hierarchy (.planning/milestones/{milestone}/phases/ instead of .planning/phases/)"
    - "All 8 unit test files with old-path references updated to use new hierarchy"
    - "createFixtureDir() produces a temp directory with milestone-scoped layout"
    - "All unit tests pass after fixture and test updates (zero regressions)"
  artifacts:
    - path: "tests/fixtures/planning/milestones/anonymous/phases/01-test/01-01-PLAN.md"
      provides: "Relocated test fixture plan file"
      contains: "phase: 01-test"
    - path: "tests/fixtures/planning/milestones/anonymous/phases/01-test/01-01-SUMMARY.md"
      provides: "Relocated test fixture summary file"
      contains: "phase: 01-test"
    - path: "tests/fixtures/planning/milestones/anonymous/phases/02-build/02-01-PLAN.md"
      provides: "Relocated test fixture plan file for phase 02"
      contains: "phase:"
    - path: "tests/fixtures/planning/milestones/anonymous/todos/pending/sample.md"
      provides: "Relocated test fixture todo file"
      contains: "Todo"
  key_links:
    - from: "tests/helpers/fixtures.js"
      to: "tests/fixtures/planning/milestones/"
      via: "fs.cpSync copies fixture to temp directory"
      pattern: "cpSync"
    - from: "tests/unit/coverage-gaps.test.js"
      to: "lib/verify.js"
      via: "cmdVerifyArtifacts and cmdVerifyKeyLinks with milestone-scoped paths"
      pattern: "milestones.*phases"
---

<objective>
Migrate the shared test fixture directory to the new milestone-scoped hierarchy and update all unit test files that reference old `.planning/phases/` paths.

Purpose: REQ-64 requires all unit tests to use the new hierarchy. The fixture directory at `tests/fixtures/planning/` is the single source copied into every test's temp directory. Migrating the fixture layout and updating path references in tests achieves the requirement in one coordinated change.

Output: Migrated fixture directory, updated unit test files, all unit tests passing.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-updates-documentation-and-integration-validation/36-01-SUMMARY.md
@tests/helpers/fixtures.js
@lib/paths.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test fixture directory to milestone-scoped hierarchy</name>
  <files>
    tests/fixtures/planning/milestones/anonymous/phases/01-test/01-01-PLAN.md,
    tests/fixtures/planning/milestones/anonymous/phases/01-test/01-01-SUMMARY.md,
    tests/fixtures/planning/milestones/anonymous/phases/02-build/02-01-PLAN.md,
    tests/fixtures/planning/milestones/anonymous/todos/pending/sample.md,
    tests/helpers/fixtures.js
  </files>
  <action>
    The fixture STATE.md has `**Milestone:** M1: Foundation` which has no vX.Y.Z pattern,
    so currentMilestone() returns 'anonymous'. Therefore the milestone-scoped paths use 'anonymous'.

    1. Inside tests/fixtures/planning/, create the new hierarchy:
       mkdir -p milestones/anonymous/phases/01-test/
       mkdir -p milestones/anonymous/phases/02-build/
       mkdir -p milestones/anonymous/todos/pending/
       mkdir -p milestones/anonymous/todos/completed/

    2. Move fixture files to new locations:
       - phases/01-test/01-01-PLAN.md -> milestones/anonymous/phases/01-test/01-01-PLAN.md
       - phases/01-test/01-01-SUMMARY.md -> milestones/anonymous/phases/01-test/01-01-SUMMARY.md
       - phases/02-build/02-01-PLAN.md -> milestones/anonymous/phases/02-build/02-01-PLAN.md
       - todos/pending/sample.md -> milestones/anonymous/todos/pending/sample.md

    3. Remove the now-empty old directories:
       rm -rf tests/fixtures/planning/phases/
       rm -rf tests/fixtures/planning/todos/

    4. Update tests/helpers/fixtures.js: No code changes needed since it copies
       the entire .planning/ directory tree. The layout change in the fixture source
       is sufficient. Verify the cpSync still works with the deeper hierarchy.

    5. Verify the fixture STATE.md, config.json, ROADMAP.md, REQUIREMENTS.md, and
       milestones/ directory at the top level are all preserved.
  </action>
  <verify>
    ls -R tests/fixtures/planning/ confirms new hierarchy exists (Level 1: Sanity)
    Old directories (tests/fixtures/planning/phases/, tests/fixtures/planning/todos/) no longer exist (Level 1: Sanity)
    All fixture files preserved at new locations (Level 1: Sanity)
  </verify>
  <done>
    Test fixture directory reorganized to milestone-scoped hierarchy with 'anonymous' milestone.
    Old top-level phases/ and todos/ directories removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update unit test files to use milestone-scoped paths</name>
  <files>
    tests/unit/coverage-gaps.test.js,
    tests/unit/verify.test.js,
    tests/unit/cleanup.test.js,
    tests/unit/cleanup-noninterference.test.js,
    tests/unit/scaffold.test.js,
    tests/unit/context.test.js,
    tests/unit/commands.test.js,
    tests/unit/phase.test.js
  </files>
  <action>
    Use the grep audit from Plan 36-01's SUMMARY to identify exact lines. For each file,
    replace old-style path references with milestone-scoped equivalents.

    The general pattern is:
    - '.planning/phases/' -> '.planning/milestones/anonymous/phases/'
    - '.planning/todos/' -> '.planning/milestones/anonymous/todos/'
    - '.planning/research/' -> '.planning/milestones/anonymous/research/'
    - '.planning/codebase/' -> '.planning/milestones/anonymous/codebase/'
    - '.planning/quick/' -> '.planning/milestones/anonymous/quick/'

    IMPORTANT: Some tests construct paths with path.join(). Update BOTH:
    - String literals like '.planning/phases/01-test/01-01-PLAN.md'
    - path.join() calls like path.join(tmpDir, '.planning', 'phases', '01-test')
      -> path.join(tmpDir, '.planning', 'milestones', 'anonymous', 'phases', '01-test')

    IMPORTANT: Some tests create directories with fs.mkdirSync(). Update those too:
    - fs.mkdirSync(path.join(tmpDir, '.planning', 'phases', '01-test'), { recursive: true })
      -> fs.mkdirSync(path.join(tmpDir, '.planning', 'milestones', 'anonymous', 'phases', '01-test'), { recursive: true })

    For tests that pass relative path arguments to CLI commands (like cmdVerifyArtifacts):
    - '.planning/phases/01-test/01-01-PLAN.md'
      -> '.planning/milestones/anonymous/phases/01-test/01-01-PLAN.md'

    For tests/unit/context.test.js:
    - The pending_dir expectation may change from '.planning/todos/pending'
      to '.planning/milestones/anonymous/todos/pending' (check actual init output after merge)

    For tests/unit/phase.test.js:
    - The comment '.planning/phases/ is empty after milestone complete' references
      old hierarchy in a test description string -- update the test description too

    After all updates, run: npx jest tests/unit/ --verbose
    Confirm all unit tests pass.
  </action>
  <verify>
    grep -rn '\.planning/\(phases\|research\|codebase\|todos\|quick\)/' tests/unit/ returns
    zero results (only milestones/ paths remain) (Level 2: Proxy)
    npx jest tests/unit/ passes with zero failures (Level 2: Proxy)
  </verify>
  <done>
    All 8 unit test files updated to use milestone-scoped paths,
    zero old-path references remain in tests/unit/,
    all unit tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): Fixture files exist at new milestone-scoped locations
Level 2 (Proxy): grep confirms zero old-path references in tests/unit/ and tests/fixtures/
Level 2 (Proxy): npx jest tests/unit/ passes with zero failures
</verification>

<success_criteria>
- tests/fixtures/planning/ uses milestones/anonymous/phases/ hierarchy (REQ-64)
- Zero occurrences of '.planning/phases/' in tests/unit/ (outside test description comments)
- Zero occurrences of '.planning/todos/' in tests/unit/ (outside test description comments)
- All unit tests pass after updates
- No tests deleted -- all tests migrated to new paths
</success_criteria>

<output>
After completion, create `.planning/phases/36-test-updates-documentation-and-integration-validation/36-02-SUMMARY.md`
</output>
