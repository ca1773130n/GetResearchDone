---
phase: 36-test-updates-documentation-and-integration-validation
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/golden/capture.sh
  - tests/golden/output/find-phase.json
  - tests/golden/output/frontmatter-get.json
  - tests/golden/output/frontmatter-get-field.json
  - tests/integration/cli.test.js
  - tests/integration/golden.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "Golden test capture.sh creates milestone-scoped fixture directories"
    - "Golden output JSON files contain milestone-scoped paths"
    - "Integration tests use milestone-scoped paths for fixture setup and assertions"
    - "All golden and integration tests pass after updates"
  artifacts:
    - path: "tests/golden/output/find-phase.json"
      provides: "Updated golden output with milestone-scoped directory path"
      contains: "milestones"
    - path: "tests/golden/output/frontmatter-get.json"
      provides: "Updated golden output with milestone-scoped path"
      contains: "milestones"
    - path: "tests/golden/output/frontmatter-get-field.json"
      provides: "Updated golden output with milestone-scoped path"
      contains: "milestones"
  key_links:
    - from: "tests/golden/capture.sh"
      to: "tests/golden/output/"
      via: "capture.sh generates golden outputs that golden.test.js compares against"
      pattern: "mkdir.*milestones"
    - from: "tests/integration/golden.test.js"
      to: "tests/golden/output/"
      via: "golden.test.js loads expected outputs and compares with actual CLI output"
      pattern: "frontmatter.*get"
---

<objective>
Update golden tests (capture.sh, golden output JSON files) and integration tests (cli.test.js, golden.test.js) to use milestone-scoped paths.

Purpose: REQ-65 requires all integration and golden tests to use the new hierarchy. These tests are separate from unit tests (Plan 02) because they have a different fixture setup mechanism (capture.sh creates fixtures inline instead of using the shared fixture directory) and different verification approach (snapshot comparison).

Output: Updated golden test infrastructure and integration tests, all passing.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-updates-documentation-and-integration-validation/36-01-SUMMARY.md
@tests/golden/capture.sh
@tests/integration/golden.test.js
@lib/paths.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update golden test infrastructure (capture.sh and output files)</name>
  <files>
    tests/golden/capture.sh,
    tests/golden/output/find-phase.json,
    tests/golden/output/frontmatter-get.json,
    tests/golden/output/frontmatter-get-field.json
  </files>
  <action>
    capture.sh creates a temporary fixture directory with inline fixture files, then runs
    CLI commands and saves output to tests/golden/output/. The golden.test.js compares
    actual CLI output against these saved golden files.

    1. In capture.sh, update all mkdir and file creation paths:
       - mkdir -p "${tmpdir}/.planning/phases/01-test"
         -> mkdir -p "${tmpdir}/.planning/milestones/anonymous/phases/01-test"
       - mkdir -p "${tmpdir}/.planning/phases/02-build"
         -> mkdir -p "${tmpdir}/.planning/milestones/anonymous/phases/02-build"
       - mkdir -p "${tmpdir}/.planning/todos/pending"
         -> mkdir -p "${tmpdir}/.planning/milestones/anonymous/todos/pending"
       - mkdir -p "${tmpdir}/.planning/todos/completed"
         -> mkdir -p "${tmpdir}/.planning/milestones/anonymous/todos/completed"

    2. In capture.sh, update all file creation paths (cat > ... << 'FIXTURE_EOF'):
       - ${tmpdir}/.planning/phases/01-test/01-01-PLAN.md
         -> ${tmpdir}/.planning/milestones/anonymous/phases/01-test/01-01-PLAN.md
       - ${tmpdir}/.planning/phases/01-test/01-01-SUMMARY.md
         -> ${tmpdir}/.planning/milestones/anonymous/phases/01-test/01-01-SUMMARY.md
       - ${tmpdir}/.planning/phases/02-build/02-01-PLAN.md
         -> ${tmpdir}/.planning/milestones/anonymous/phases/02-build/02-01-PLAN.md
       - ${tmpdir}/.planning/todos/pending/sample.md
         -> ${tmpdir}/.planning/milestones/anonymous/todos/pending/sample.md

    3. In capture.sh, update all CLI command arguments that reference plan/summary paths:
       - .planning/phases/01-test/01-01-PLAN.md
         -> .planning/milestones/anonymous/phases/01-test/01-01-PLAN.md
       - .planning/phases/01-test/01-01-SUMMARY.md
         -> .planning/milestones/anonymous/phases/01-test/01-01-SUMMARY.md

    4. Update golden output JSON files to match new expected CLI output:
       - find-phase.json: Update "directory" field from
         ".planning/phases/01-security-foundation" to
         the new milestone-scoped path. NOTE: After merge, check what findPhase
         actually returns for the fixture -- the directory field comes from
         lib/utils.js findPhaseInternal which now uses paths.js. Run the actual
         command to determine the correct expected output, then update the JSON.
       - frontmatter-get.json: Update "path" field from
         ".planning/phases/01-test/01-01-PLAN.md" to
         ".planning/milestones/anonymous/phases/01-test/01-01-PLAN.md"
         NOTE: Check if the error case still applies (file not found) or if the
         path reported changes. Regenerate if needed by running capture.sh.
       - frontmatter-get-field.json: Same update as frontmatter-get.json.

    5. After all edits, run capture.sh to regenerate golden outputs:
       cd tests/golden && bash capture.sh
       Compare regenerated output with expected files to confirm alignment.

    6. IMPORTANT: capture.sh also sets up a STATE.md with the fixture's milestone.
       Ensure the STATE.md fixture in capture.sh includes a milestone field that
       currentMilestone() can parse. The current fixture STATE.md has
       "**Milestone:** M1: Foundation" which returns 'anonymous'. Either:
       (a) Keep it returning 'anonymous' (paths resolve to milestones/anonymous/), OR
       (b) Change to "**Milestone:** v1.0 -- Foundation" so paths resolve to milestones/v1.0/
       Option (a) is simpler and consistent with the test fixture directory approach in Plan 02.
       Use option (a).
  </action>
  <verify>
    bash tests/golden/capture.sh runs without errors (Level 1: Sanity)
    diff between capture.sh output and golden files shows no unexpected differences (Level 2: Proxy)
    No old-path references remain in capture.sh: grep '\.planning/phases/' tests/golden/capture.sh returns 0 results (Level 2: Proxy)
  </verify>
  <done>
    Golden test infrastructure updated: capture.sh creates milestone-scoped fixtures,
    golden output JSON files reflect milestone-scoped paths,
    capture.sh generates output matching golden files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update integration tests (cli.test.js and golden.test.js)</name>
  <files>
    tests/integration/cli.test.js,
    tests/integration/golden.test.js
  </files>
  <action>
    1. In tests/integration/cli.test.js, update old-path references:
       - All .planning/phases/ path strings in CLI argument arrays:
         '.planning/phases/01-test/01-01-PLAN.md'
         -> '.planning/milestones/anonymous/phases/01-test/01-01-PLAN.md'
       - All .planning/phases/ path strings in CLI argument arrays for SUMMARY:
         '.planning/phases/01-test/01-01-SUMMARY.md'
         -> '.planning/milestones/anonymous/phases/01-test/01-01-SUMMARY.md'
       - Path arguments for frontmatter set/merge commands:
         '.planning/phases/01-test/01-01-PLAN.md'
         -> '.planning/milestones/anonymous/phases/01-test/01-01-PLAN.md'

       IMPORTANT: cli.test.js uses its own fixture setup (different from createFixtureDir).
       Check whether it creates fixtures inline or uses the shared fixture.
       If inline: update the mkdir and writeFileSync calls too.
       If shared: the fixture migration in Plan 02 handles it.

    2. In tests/integration/golden.test.js, update old-path references:
       - The args arrays reference '.planning/phases/01-test/01-01-PLAN.md'
         -> '.planning/milestones/anonymous/phases/01-test/01-01-PLAN.md'

    3. DO NOT modify the migrate-dirs tests in cli.test.js (added by Phase 35).
       Those tests intentionally create old-style directories to test the migration
       command -- they are CORRECT as-is.

    4. After updates, run: npx jest tests/integration/ --verbose
       Confirm all integration tests pass.
  </action>
  <verify>
    grep -n '\.planning/phases/' tests/integration/cli.test.js shows only migrate-dirs test
    references (which are intentionally old-style) (Level 2: Proxy)
    grep -n '\.planning/phases/' tests/integration/golden.test.js returns 0 results (Level 2: Proxy)
    npx jest tests/integration/ passes with zero failures (Level 2: Proxy)
  </verify>
  <done>
    Integration and golden test files updated to milestone-scoped paths,
    migrate-dirs tests preserved as-is (intentionally old-style),
    all integration tests pass.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): All golden and integration test files updated without syntax errors
Level 2 (Proxy): Zero old-path references in golden/integration tests (except migrate-dirs)
Level 2 (Proxy): npx jest tests/integration/ and npx jest tests/golden/ pass
Level 2 (Proxy): npm test (full suite) passes with 1,631+ tests, zero failures
</verification>

<success_criteria>
- capture.sh creates milestone-scoped fixtures (REQ-65)
- Golden output JSON files contain milestone-scoped paths (REQ-65)
- cli.test.js uses milestone-scoped paths (except migrate-dirs tests) (REQ-65)
- golden.test.js uses milestone-scoped paths (REQ-65)
- All integration and golden tests pass
- npm test full suite passes with zero regressions (REQ-66)
</success_criteria>

<output>
After completion, create `.planning/phases/36-test-updates-documentation-and-integration-validation/36-03-SUMMARY.md`
</output>
