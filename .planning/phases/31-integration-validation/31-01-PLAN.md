---
phase: 31-integration-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/integration/worktree-parallel-e2e.test.js
autonomous: true
verification_level: proxy
must_haves:
  truths:
    - "Cross-module integration test proves worktree create -> deps analysis -> parallel context -> push-pr -> worktree remove works as a connected pipeline"
    - "Sequential fallback produces identical context structure (same phase_number, worktree_path, worktree_branch, status_tracker) as parallel mode minus the team fields"
    - "Worktree lifecycle is validated: create returns path+branch, list shows it, remove cleans it, remove-stale handles orphans"
    - "Concurrent worktree creation for two independent phases produces separate paths and branches with no collision"
    - "Status tracker transitions (pending -> running -> complete/failed) are correctly keyed per phase"
    - "All new integration tests pass without modifying any production code"
  artifacts:
    - path: "tests/integration/worktree-parallel-e2e.test.js"
      provides: "Cross-module integration tests for the full v0.2.0 pipeline"
      min_lines: 200
  key_links:
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/worktree.js"
      via: "require and call cmdWorktreeCreate/Remove/List/RemoveStale"
      pattern: "require.*worktree"
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/deps.js"
      via: "require and call buildDependencyGraph/computeParallelGroups"
      pattern: "require.*deps"
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/parallel.js"
      via: "require and call validateIndependentPhases/buildParallelContext"
      pattern: "require.*parallel"
    - from: "tests/integration/worktree-parallel-e2e.test.js"
      to: "lib/context.js"
      via: "require and call cmdInitExecutePhase for worktree fields"
      pattern: "require.*context"
---

<objective>
Write cross-module integration tests that validate the full v0.2.0 worktree-parallel pipeline end-to-end. These tests prove that modules built in Phases 27-30 (worktree.js, deps.js, parallel.js, context.js) work correctly together as an integrated system.

Purpose: Resolve deferred validations DEFER-22-01, DEFER-27-01, DEFER-27-02, and DEFER-30-01 (to the extent testable without a real Claude Code runtime). Satisfy Phase 31 success criteria #1 (E2E single-phase), #2 (E2E parallel), #3 (sequential equivalence), and partially #5 (test coverage).

Output: `tests/integration/worktree-parallel-e2e.test.js` with 20+ integration tests covering cross-module workflows.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/worktree.js
@lib/deps.js
@lib/parallel.js
@lib/context.js
@tests/unit/worktree.test.js
@tests/unit/parallel.test.js
@tests/unit/deps.test.js
@tests/helpers/setup.js
@tests/helpers/fixtures.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write cross-module integration tests for the full worktree-parallel pipeline</name>
  <files>tests/integration/worktree-parallel-e2e.test.js</files>
  <action>
    Create a comprehensive integration test file that tests the v0.2.0 modules working together as a connected system. Follow the existing test patterns (captureOutput, createFixtureDir, createTestGitRepo from tests/helpers/).

    The test file MUST contain these describe blocks, each exercising cross-module interactions:

    **1. `describe('E2E: Single-phase worktree execution pipeline')`** (resolves DEFER-22-01, DEFER-27-01):
    - Use a real temp git repo (createTestGitRepo pattern from worktree.test.js).
    - Test: cmdWorktreeCreate for a phase -> verify worktree exists on disk -> verify .planning/ is accessible in worktree -> cmdWorktreeList shows the worktree -> simulate work (write file, git add, git commit in worktree) -> cmdWorktreePushAndPR (push will succeed to bare remote, PR may fail - assert push_succeeded) -> cmdWorktreeRemove -> verify worktree is gone from disk and git worktree list.
    - Test: cmdInitExecutePhase returns worktree_path and worktree_branch fields that match the values cmdWorktreeCreate would produce (same path format, same branch template).
    - Test: worktree_path uses fs.realpathSync(os.tmpdir()) consistently between context.js and worktree.js (macOS symlink resolution).

    **2. `describe('E2E: Parallel execution of independent phases')`** (resolves DEFER-30-01 at test level):
    - Use a fixture dir with two independent phases (Depends on: Nothing) in ROADMAP.md, with phase directories containing plan files.
    - Test: buildDependencyGraph -> computeParallelGroups confirms both phases are in the same parallel group.
    - Test: validateIndependentPhases confirms they have no conflicts.
    - Test: buildParallelContext returns per-phase context with DIFFERENT worktree_path and worktree_branch for each phase (no collision).
    - Test: Each phase in context has its own status_tracker entry initialized to 'pending'.
    - Test: cmdInitExecuteParallel for two independent phases returns mode (parallel or sequential depending on backend), phases array with length 2, and independence_validated: true.
    - Test (with real git repo): cmdWorktreeCreate for phase A and phase B simultaneously -> cmdWorktreeList shows 2 worktrees -> both have different paths and branches -> cmdWorktreeRemove for both -> list shows 0.

    **3. `describe('E2E: Sequential fallback equivalence')`** (success criterion #3):
    - Use the same fixture with two independent phases.
    - Test: buildParallelContext with backend='codex' (no teams) returns mode='sequential' and a fallback_note string mentioning Claude Code.
    - Test: buildParallelContext with backend='claude' (teams) returns mode='parallel' and fallback_note is null/falsy.
    - Test: Both sequential and parallel contexts contain the SAME per-phase structure: phase_number, phase_name, worktree_path, worktree_branch, plans, plan_count.
    - Test: Both have status_tracker with identical phase keys and 'pending' status.
    - The ONLY differences should be: mode field, fallback_note field, and backend_capabilities.

    **4. `describe('E2E: Stale worktree cleanup')`** (resolves DEFER-27-02):
    - Use a real temp git repo.
    - Test: Create a worktree -> manually delete the worktree directory from disk (simulating crash) -> cmdWorktreeRemoveStale detects and removes it -> cmdWorktreeList returns empty.
    - Test: Create two worktrees -> delete only one from disk -> cmdWorktreeRemoveStale only removes the stale one -> cmdWorktreeList returns 1 remaining.

    **5. `describe('E2E: Dependency graph integration with parallel context')`**:
    - Use a fixture with the exact v0.2.0 roadmap structure (phases 27-31 with their real depends_on).
    - Test: cmdPhaseAnalyzeDeps returns the expected parallel_groups: [['27','29'], ['28','30'], ['31']].
    - Test: validateIndependentPhases(['27','29']) returns valid:true (they are in the same parallel group).
    - Test: validateIndependentPhases(['27','28']) returns valid:false (28 depends on 27).
    - Test: buildParallelContext(['27','29']) (fixture version with these two phases) returns two phase contexts with correct worktree paths.

    **6. `describe('E2E: Status tracker per-phase tracking')`**:
    - Test: buildParallelContext for N phases returns status_tracker with exactly N entries, all 'pending'.
    - Test: Status tracker keys match the actual phase_number values from the phases array.
    - Test: Programmatically mutating status_tracker.phases[key].status to 'running' then 'complete' demonstrates the intended state machine (this is a data structure test, not an execution test).

    Implementation notes:
    - Reuse createTestGitRepo and createTestGitRepoWithRemote helper patterns from tests/unit/worktree.test.js (copy the helpers or factor them out).
    - Reuse createFixtureDir/cleanupFixtureDir from tests/helpers/fixtures.js for fixture-based tests.
    - Use captureOutput from tests/helpers/setup.js for all cmd* function calls.
    - Every test must properly clean up (afterEach with cleanup functions).
    - Do NOT import or test any MCP server descriptors in this file (those are covered in unit tests).
    - Target: 20+ individual test cases across the 6 describe blocks.
  </action>
  <verify>
    cd /Users/edward.seo/dev/private/project/harness/GetResearchDone && npx jest tests/integration/worktree-parallel-e2e.test.js --verbose 2>&1 | tail -30
    All tests pass (Level 2: Proxy — tests validate cross-module E2E flows with real git repos).
  </verify>
  <done>
    tests/integration/worktree-parallel-e2e.test.js exists with 20+ passing integration tests.
    Cross-module pipeline validated: worktree -> deps -> parallel -> push-pr -> cleanup.
    Sequential vs parallel equivalence proven via structural comparison.
    Stale worktree cleanup validated with simulated crash scenario.
    Status tracker state machine demonstrated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and verify zero failures</name>
  <files>tests/integration/worktree-parallel-e2e.test.js</files>
  <action>
    After Task 1 creates the integration test file, run the complete test suite to verify:
    1. All 1,552+ existing tests still pass (success criterion #4).
    2. The new integration tests pass alongside existing tests.
    3. No test interference between the new integration tests and existing unit/integration tests.

    Run:
    ```bash
    cd /Users/edward.seo/dev/private/project/harness/GetResearchDone && npx jest --verbose 2>&1 | tail -5
    ```

    If any existing tests fail:
    - Investigate whether the failure is pre-existing or caused by the new test file.
    - If caused by the new file (e.g., test isolation issue, leftover temp files), fix the new test file.
    - If pre-existing, document in the SUMMARY but do NOT modify production code.

    Record the total test count for the SUMMARY.
  </action>
  <verify>
    npx jest 2>&1 | grep -E "Tests:|Test Suites:" (Level 1: Sanity — no regressions)
    Expected: All test suites pass. Total tests >= 1,552 + new integration tests.
  </verify>
  <done>
    Full test suite passes with zero regressions.
    Total test count recorded (expected: 1,552 + ~20-25 new = ~1,572-1,577).
    No test interference between new integration file and existing tests.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): New test file exists, is valid JavaScript, imports from correct modules.
Level 2 (Proxy): All integration tests pass individually. Full test suite passes with zero regressions.
Level 3 (Deferred): Real Claude Code teammate spawning with actual parallel worktree execution (documented in VERIFICATION.md, not automatable in test env).
</verification>

<success_criteria>
1. tests/integration/worktree-parallel-e2e.test.js exists with 20+ passing tests.
2. Cross-module E2E pipeline: worktree create -> work -> push-pr -> cleanup validated.
3. Sequential vs parallel context structural equivalence proven.
4. Stale worktree cleanup tested with simulated crash scenario.
5. Full test suite passes with 1,570+ tests (zero regressions).
6. Deferred validations DEFER-22-01, DEFER-27-01, DEFER-27-02, DEFER-30-01 have test evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/31-integration-validation/31-01-SUMMARY.md`
</output>
