---
phase: 31-integration-validation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/31-integration-validation/31-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
verification_level: full
must_haves:
  truths:
    - "VERIFICATION.md documents resolution status for all 4 deferred validations (DEFER-22-01, DEFER-27-01, DEFER-27-02, DEFER-30-01)"
    - "Each deferred validation references specific test names from 31-01 that prove it"
    - "DEFER-30-01 (real teammate spawning) is correctly marked as partially resolved with clear documentation of what was tested vs what requires runtime verification"
    - "STATE.md is updated to reflect Phase 31 completion, active phase advanced, deferred validation statuses updated"
    - "ROADMAP.md progress table and Phase 31 entry are updated with plan count and completion status"
    - "Total test count from full suite run is recorded in STATE.md performance metrics"
  artifacts:
    - path: ".planning/phases/31-integration-validation/31-VERIFICATION.md"
      provides: "Tiered verification report with deferred validation resolution"
      min_lines: 80
  key_links:
    - from: ".planning/phases/31-integration-validation/31-VERIFICATION.md"
      to: "tests/integration/worktree-parallel-e2e.test.js"
      via: "References specific test names as evidence for deferred validation resolution"
      pattern: "worktree-parallel-e2e"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Active phase and milestone status consistency"
      pattern: "Phase 31"
---

<objective>
Produce the Phase 31 verification report resolving all deferred validations, update STATE.md and ROADMAP.md to reflect milestone completion readiness.

Purpose: Close the v0.2.0 milestone by documenting what was validated, what deferred items are now resolved, and what (if anything) requires runtime verification. Update project state so the next milestone can start cleanly.

Output: 31-VERIFICATION.md, updated STATE.md, updated ROADMAP.md.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-integration-validation/31-01-SUMMARY.md
@tests/integration/worktree-parallel-e2e.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VERIFICATION.md with deferred validation resolution</name>
  <files>.planning/phases/31-integration-validation/31-VERIFICATION.md</files>
  <action>
    Create a comprehensive verification report that documents the resolution of each deferred validation. The report must follow this structure:

    ```markdown
    # Phase 31 Verification Report

    **Phase:** 31 — Integration & Validation
    **Milestone:** v0.2.0 — Git Worktree Parallel Execution
    **Verification Level:** Full
    **Date:** 2026-02-19

    ## Success Criteria Verification

    For each of the 5 success criteria from ROADMAP.md Phase 31:
    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | E2E single-phase worktree execution | PASS | Test: "E2E: Single-phase worktree execution pipeline" (N tests) |
    | 2 | E2E parallel execution | PASS | Test: "E2E: Parallel execution of independent phases" (N tests) |
    | 3 | Sequential fallback equivalence | PASS | Test: "E2E: Sequential fallback equivalence" (N tests) |
    | 4 | All existing tests pass | PASS | Full suite: {total} tests, 0 failures |
    | 5 | Comprehensive test coverage | PASS | 119 new tests in Phases 27-30 + N integration tests in Phase 31 |

    ## Deferred Validation Resolution

    ### DEFER-22-01: End-to-end git branching workflow validation
    - **From:** Phase 22
    - **Status:** RESOLVED
    - **Evidence:** Test "creates worktree, simulates work, pushes to remote, and cleans up" in E2E: Single-phase worktree execution pipeline
    - **What was validated:** init -> worktree create -> work in worktree -> push branch -> cleanup. Branch follows template grd/{milestone}/{phase}-{slug}.
    - **What requires runtime:** PR creation via gh CLI (requires GitHub auth). Push to remote was validated with bare clone.

    ### DEFER-27-01: End-to-end worktree creation during actual execute-phase run
    - **From:** Phase 27
    - **Status:** RESOLVED
    - **Evidence:** Test "worktree_path is accessible and contains .planning directory" and "cmdInitExecutePhase worktree fields match worktree.js path computation"
    - **What was validated:** worktree_path exists during execution, .planning accessible, worktree removed after completion.

    ### DEFER-27-02: Stale worktree cleanup on crashed session recovery
    - **From:** Phase 27
    - **Status:** RESOLVED
    - **Evidence:** Tests in "E2E: Stale worktree cleanup" — simulated crash (directory deletion), cmdWorktreeRemoveStale detection and removal.
    - **What was validated:** Stale detection works when disk directory missing, locked worktrees preserved.

    ### DEFER-30-01: Full parallel execution with real teammate spawning on Claude Code
    - **From:** Phase 30
    - **Status:** PARTIALLY RESOLVED
    - **Evidence:** Tests in "E2E: Parallel execution of independent phases" — independence validation, parallel context building, concurrent worktree creation, status tracker.
    - **What was validated:** Module-level parallel execution: two phases validated as independent, separate worktree paths computed, status tracker initialized per-phase, mode correctly selected based on backend.
    - **What requires runtime:** Actual teammate agent spawning on Claude Code. This cannot be automated in test because it requires the Claude Code Teams API. The modules (validateIndependentPhases, buildParallelContext, cmdInitExecuteParallel) are fully tested; the gap is the orchestrator actually invoking the Teams API to spawn agents.
    - **Recommended:** First real use should be monitored manually. After successful use, mark fully resolved.

    ## Tiered Verification Summary

    ### Level 1 (Sanity) — All Pass
    - New test file is valid JavaScript
    - All imports resolve correctly
    - No syntax errors or module-not-found

    ### Level 2 (Proxy) — All Pass
    - Cross-module integration tests pass with real git repos
    - Sequential vs parallel equivalence validated structurally
    - Full regression suite: {total} tests, 0 failures

    ### Level 3 (Full) — Mostly Pass
    - 4/4 deferred validations addressed (3 fully resolved, 1 partially)
    - DEFER-30-01 partial: teammate spawning untestable without Claude Code runtime
    - All success criteria met

    ## Test Coverage Summary

    | Phase | New Tests | Cumulative |
    |-------|-----------|------------|
    | 27 | 23 | 1,456 |
    | 28 | 9 | 1,465 |
    | 29 | 32 | 1,519 |
    | 30 | 32 | 1,552 |
    | 31 | {N} | {total} |
    | **v0.2.0 Total** | **{sum}** | **{total}** |
    ```

    Fill in the actual test counts from Plan 01's SUMMARY.md results.
    Reference specific test names from the integration test file for each deferred validation.
  </action>
  <verify>
    cat .planning/phases/31-integration-validation/31-VERIFICATION.md | head -5
    File exists and has verification report structure (Level 1: Sanity).
  </verify>
  <done>
    31-VERIFICATION.md exists with all 4 deferred validations documented.
    Each validation references specific test evidence.
    DEFER-30-01 correctly marked as partially resolved with clear documentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and ROADMAP.md for Phase 31 completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Update both files to reflect Phase 31 completion:

    **STATE.md updates:**
    1. Change "Active phase" to: `31 — Integration & Validation (COMPLETE)`
    2. Change "Current Plan" to: `31-02 complete; Phase 31 done, v0.2.0 milestone ready for completion`
    3. Change "Progress" to: `[██████████] 100%`
    4. Change "Next" to: `Phase 31 complete; v0.2.0 milestone ready for completion`
    5. Update deferred validations table:
       - DEFER-22-01: Status → RESOLVED (Phase 31)
       - DEFER-27-01: Status → RESOLVED (Phase 31) (note: this was tracked informally, add row if not present)
       - DEFER-27-02: Status → RESOLVED (Phase 31) (note: this was tracked informally, add row if not present)
       - DEFER-30-01: Status → PARTIALLY RESOLVED (Phase 31) — runtime verification pending
    6. Add performance metrics row for Phase 31 plans (01 and 02) with duration, tasks, files, test delta.
    7. Update "Velocity (v0.2.0)" section: Phases completed → 5 (27, 28, 29, 30, 31), Tests → {total from full suite run}
    8. Update "Session Continuity" to reflect Phase 31 completion.

    **ROADMAP.md updates:**
    1. Mark Phase 31 as complete: `- [x] **Phase 31: Integration & Validation** ...`
    2. Add completion date: `(completed 2026-02-19)`
    3. Update Phase 31 details: `**Plans:** 2 plans` and add plan list:
       ```
       Plans:
       - [x] 31-01-PLAN.md — Cross-module integration tests (worktree + deps + parallel E2E)
       - [x] 31-02-PLAN.md — Verification report, state update, milestone readiness
       ```
    4. Update Progress table: Phase 31 row with Complete status and date.
  </action>
  <verify>
    grep "Phase 31" .planning/STATE.md (Level 1: Sanity — state updated)
    grep "Phase 31" .planning/ROADMAP.md (Level 1: Sanity — roadmap updated)
  </verify>
  <done>
    STATE.md reflects Phase 31 completion with updated deferred validation statuses.
    ROADMAP.md shows Phase 31 as complete with 2 plans listed.
    Performance metrics include Phase 31 test counts.
    v0.2.0 milestone is ready for completion.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): VERIFICATION.md, STATE.md, ROADMAP.md all exist and contain expected content.
Level 2 (Proxy): Deferred validations reference real test names. State is internally consistent.
Level 3 (Full): All 5 success criteria met. Milestone v0.2.0 ready for completion.
</verification>

<success_criteria>
1. 31-VERIFICATION.md documents all 4 deferred validations with specific test evidence.
2. DEFER-30-01 correctly marked as partially resolved (runtime gap documented).
3. STATE.md reflects Phase 31 completion, all metrics updated.
4. ROADMAP.md shows Phase 31 as complete with plans listed.
5. v0.2.0 milestone is objectively ready for completion.
</success_criteria>

<output>
After completion, create `.planning/phases/31-integration-validation/31-02-SUMMARY.md`
</output>
