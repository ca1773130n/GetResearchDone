---
phase: 35-migration-script-and-archive-simplification
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - bin/grd-tools.js
  - tests/integration/cli.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "node bin/grd-tools.js migrate-dirs executes cmdMigrateDirs and produces JSON output"
    - "node bin/grd-tools.js migrate-dirs --raw executes cmdMigrateDirs and produces raw text output"
    - "The migrate-dirs command is listed in the CLI usage/help text"
    - "npm test passes with zero regressions after all Phase 35 changes"
  artifacts:
    - path: "bin/grd-tools.js"
      provides: "CLI routing for migrate-dirs command"
      contains: "migrate-dirs"
    - path: "tests/integration/cli.test.js"
      provides: "Integration test for migrate-dirs CLI invocation"
  key_links:
    - from: "bin/grd-tools.js"
      to: "lib/commands.js"
      via: "import cmdMigrateDirs"
      pattern: "cmdMigrateDirs"
---

<objective>
Wire the `migrate-dirs` command into the CLI router and run a full verification sweep.

Purpose: Plan 01 created the `cmdMigrateDirs` function in `lib/commands.js`. This plan connects it to the CLI entry point so users can invoke `node bin/grd-tools.js migrate-dirs` from the command line. It also runs a comprehensive verification to ensure all Phase 35 changes work together without regressions.

Output: Working CLI command and full test suite passing.
</objective>

<execution_context>
@.claude-plugin/references/execute-plan.md
@.claude-plugin/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@bin/grd-tools.js
@lib/commands.js
@.planning/phases/35-migration-script-and-archive-simplification/35-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire migrate-dirs command into CLI router</name>
  <files>bin/grd-tools.js</files>
  <action>
    Make two changes to bin/grd-tools.js:

    1. Add cmdMigrateDirs to the require('../lib/commands') destructuring at the top of the file (around line 82-108). Add it to the existing destructured import list.

    2. Add a new case in the routeCommand switch statement. Place it after the 'scaffold' case and before the 'init' case (around line 452):

    ```javascript
    case 'migrate-dirs':
      cmdMigrateDirs(cwd, raw);
      break;
    ```

    3. Update the usage/help text in the error message (around line 126) to include 'migrate-dirs' in the commands list.

    Verify the change compiles by running: node bin/grd-tools.js migrate-dirs (should produce output, even if it's an error about missing .planning/).
  </action>
  <verify>
    node bin/grd-tools.js 2>&1 | grep -o 'migrate-dirs' | head -1
    Should output "migrate-dirs" confirming it's in the help text. (Level 1: Sanity)
  </verify>
  <done>migrate-dirs command is wired in CLI router and appears in help text</done>
</task>

<task type="auto">
  <name>Task 2: Add integration test and run full verification</name>
  <files>tests/integration/cli.test.js</files>
  <action>
    1. Check if tests/integration/cli.test.js exists. If it does, add a test for the migrate-dirs command. Follow the existing integration test pattern in the file (examine how other commands are tested — likely using execFileSync or similar to invoke grd-tools.js as a subprocess). Important: use execFileSync or execFile (NOT exec) for safety.

    Add a test like:
    ```javascript
    test('migrate-dirs command produces valid JSON', () => {
      // Create temp .planning/ with old-style layout
      // Run: node bin/grd-tools.js migrate-dirs
      // Assert: exit code 0, output is valid JSON
    });
    ```

    Follow whatever pattern the existing integration tests use for subprocess invocation.

    2. Run the full test suite to verify zero regressions:
    ```
    npm test
    ```

    3. Run lint check:
    ```
    npm run lint
    ```

    4. Run format check:
    ```
    npm run format:check
    ```

    5. Verify the specific Phase 35 requirements are met by running targeted checks:
    - REQ-61: `node bin/grd-tools.js migrate-dirs` exists as a command
    - REQ-62: Idempotency tested in unit tests (Plan 01)
    - REQ-63: Milestone detection tested in unit tests (Plan 01)
    - REQ-59: Simplified archive tested in unit tests (Plan 02)
    - REQ-60: Archive marker tested in unit tests (Plan 02)
  </action>
  <verify>
    npm test 2>&1 | tail -20
    All tests must pass. (Level 2: Proxy — full suite validates no regressions)

    npm run lint 2>&1 | tail -5
    Zero lint errors.

    npm run format:check 2>&1 | tail -5
    Format check passes.
  </verify>
  <done>
    migrate-dirs CLI command works end-to-end.
    Full test suite passes with zero regressions.
    Lint and format checks pass.
    All REQ-59 through REQ-63 requirements verified.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `node bin/grd-tools.js migrate-dirs` command exists and runs
Level 2 (Proxy): Full npm test suite passes, lint passes, format passes
Level 3 (Deferred): Real-world migration on an actual project with old-style layout (Phase 36 integration)
</verification>

<success_criteria>
1. `node bin/grd-tools.js migrate-dirs` produces valid JSON output
2. `node bin/grd-tools.js migrate-dirs --raw` produces raw text output
3. The command appears in CLI help/usage text
4. `npm test` passes all 1,615+ tests with zero regressions
5. `npm run lint` reports zero errors
6. `npm run format:check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/35-migration-script-and-archive-simplification/35-03-SUMMARY.md`
</output>
