---
phase: 21-mcp-extension-wiring
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp-server.js
  - tests/unit/mcp-server.test.js
autonomous: true
verification_level: proxy

must_haves:
  truths:
    - "COMMAND_DESCRIPTORS includes 5 new entries: grd_requirement_get, grd_requirement_list, grd_requirement_traceability, grd_requirement_update_status, grd_search"
    - "COMMAND_DESCRIPTORS total count is 102 (was 97)"
    - "Each new descriptor has correct params with types, required flags, and descriptions matching the CLI command signatures"
    - "MCP tools/call executes each new tool successfully with valid input and returns structured JSON"
    - "Invalid input to new MCP tools returns proper MCP error responses (missing required params returns -32602)"
    - "MCP server tests cover all 5 new tool definitions with schema spot-checks and bulk execute lambda tests"
  artifacts:
    - path: "lib/mcp-server.js"
      provides: "5 new COMMAND_DESCRIPTORS entries + imports"
      contains: "grd_requirement_get"
    - path: "tests/unit/mcp-server.test.js"
      provides: "Tests for new MCP tools"
      contains: "grd_requirement_get"
  key_links:
    - from: "lib/mcp-server.js"
      to: "lib/commands.js"
      via: "import of cmdRequirementGet, cmdRequirementList, cmdRequirementTraceability, cmdRequirementUpdateStatus, cmdSearch"
      pattern: "cmdRequirementGet.*require.*commands"
---

<objective>
Wire all 5 new CLI commands from v0.1.2 (requirement get/list/traceability/update-status, search) into the MCP server's COMMAND_DESCRIPTORS table with correct JSON Schema parameter definitions, and add comprehensive test coverage.

Purpose: Complete MCP tool coverage so all GRD CLI commands are accessible via the MCP protocol (increasing from 97 to 102 tools).
Output: Updated lib/mcp-server.js with 5 new descriptors, updated tests/unit/mcp-server.test.js with schema and execution tests.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/mcp-server.js
@lib/commands.js
@tests/unit/mcp-server.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COMMAND_DESCRIPTORS entries and imports for 5 new CLI commands</name>
  <files>lib/mcp-server.js</files>
  <action>
    1. Add imports to the destructured require('./commands') block (line ~97). Add these 5 functions:
       - cmdRequirementGet
       - cmdRequirementList
       - cmdRequirementTraceability
       - cmdRequirementUpdateStatus
       - cmdSearch

    2. Add 5 new COMMAND_DESCRIPTORS entries before the closing `];` of the array (after the grd_quality_analysis entry, around line 1259). Follow the exact pattern of existing entries. The new entries are:

       a) `grd_requirement_get`:
          - description: 'Get a requirement by ID with status and phase from traceability matrix'
          - params: [{ name: 'req_id', type: 'string', required: true, description: 'Requirement ID (e.g. REQ-37)' }]
          - execute: (cwd, args) => cmdRequirementGet(cwd, args.req_id, false)

       b) `grd_requirement_list`:
          - description: 'List requirements with optional filters (phase, priority, status, category)'
          - params:
            - { name: 'phase', type: 'string', required: false, description: 'Filter by phase number' }
            - { name: 'priority', type: 'string', required: false, description: 'Filter by priority (e.g. P1, P2)' }
            - { name: 'status', type: 'string', required: false, description: 'Filter by status (Pending, In Progress, Done, Deferred)' }
            - { name: 'category', type: 'string', required: false, description: 'Filter by category' }
            - { name: 'all', type: 'boolean', required: false, description: 'Include archived milestone requirements' }
          - execute: (cwd, args) => cmdRequirementList(cwd, { phase: args.phase || null, priority: args.priority || null, status: args.status || null, category: args.category || null, all: args.all || false }, false)

       c) `grd_requirement_traceability`:
          - description: 'Get the traceability matrix with optional phase filter'
          - params: [{ name: 'phase', type: 'string', required: false, description: 'Filter by phase number' }]
          - execute: (cwd, args) => cmdRequirementTraceability(cwd, { phase: args.phase || null }, false)

       d) `grd_requirement_update_status`:
          - description: 'Update the status of a requirement in the traceability matrix'
          - params:
            - { name: 'req_id', type: 'string', required: true, description: 'Requirement ID (e.g. REQ-37)' }
            - { name: 'status', type: 'string', required: true, description: 'New status (Pending, In Progress, Done, Deferred)' }
          - execute: (cwd, args) => cmdRequirementUpdateStatus(cwd, args.req_id, args.status, false)

       e) `grd_search`:
          - description: 'Search across all .planning/ markdown files for a text query'
          - params: [{ name: 'query', type: 'string', required: true, description: 'Text query to search for' }]
          - execute: (cwd, args) => cmdSearch(cwd, args.query, false)

    3. Add a section comment before the new entries: `// ── Requirement & Search commands ──`

    IMPORTANT: Pass raw=false to all execute lambdas (not raw=true) because these commands output JSON, not TUI text. This matches the pattern used by most existing tools (only grd_dashboard, grd_phase_detail, and grd_health pass raw=true because they are TUI commands).
  </action>
  <verify>
    node -e "const {COMMAND_DESCRIPTORS} = require('./lib/mcp-server'); console.log('Count:', COMMAND_DESCRIPTORS.length); const names = ['grd_requirement_get','grd_requirement_list','grd_requirement_traceability','grd_requirement_update_status','grd_search']; for (const n of names) { const d = COMMAND_DESCRIPTORS.find(x=>x.name===n); console.log(n, d ? 'OK' : 'MISSING'); }" — must output Count: 102 and OK for all 5.
    (Level 2: Proxy — verifies descriptor registration and count)
  </verify>
  <done>
    lib/mcp-server.js imports 5 new cmd* functions from ./commands, COMMAND_DESCRIPTORS has 102 entries total, each new entry has correct name, description, params with types/required/description, and execute lambda.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MCP server tests for new tool definitions and execution</name>
  <files>tests/unit/mcp-server.test.js</files>
  <action>
    Add tests to the existing test file for the 5 new MCP tools. Insert in 3 locations:

    1. **In buildToolDefinitions() section** (after existing spot-check tests, around line 135):
       Add spot-check tests for new tools' schema correctness:
       - test('grd_requirement_get has required req_id param of type string')
       - test('grd_requirement_list has 5 optional params and no required array')
       - test('grd_requirement_update_status has required req_id and status params')
       - test('grd_search has required query param of type string')

    2. **In COMMAND_DESCRIPTORS section** (update existing count test):
       - Update "has 90+ entries" test to check for >= 100 entries (was >= 90)
       - Add: test('covers requirement command family') — checks >= 4 grd_requirement_* entries
       - Add: test('covers search command') — checks grd_search exists

    3. **In bulk tool execute lambda coverage section** (after existing bulk tests, around line 1019):
       Add execution tests for all 5 new tools:
       - test('grd_requirement_get execute lambda') — callTool('grd_requirement_get', { req_id: 'REQ-1' })
       - test('grd_requirement_list execute lambda') — callTool('grd_requirement_list', {})
       - test('grd_requirement_list with filters execute lambda') — callTool('grd_requirement_list', { phase: '1', priority: 'P1' })
       - test('grd_requirement_traceability execute lambda') — callTool('grd_requirement_traceability', {})
       - test('grd_requirement_traceability with phase filter execute lambda') — callTool('grd_requirement_traceability', { phase: '1' })
       - test('grd_requirement_update_status execute lambda') — callTool('grd_requirement_update_status', { req_id: 'REQ-1', status: 'Done' })
       - test('grd_search execute lambda') — callTool('grd_search', { query: 'test' })

       Each test asserts: expect(r.result || r.error).toBeDefined() (matching existing pattern — these may error on fixture but exercise the lambda).

    4. **In error paths section** (around line 1127): Optionally verify that calling grd_requirement_update_status with missing req_id returns -32602. (This tests error handling for new tools.)

    5. **Update existing count assertions**:
       - In test "returns an array with 50+ tool definitions" — keep >= 50 (still valid)
       - In "tools/list" test "returns tools array with all tool definitions" — keep >= 50 (still valid)
       These are lower bounds and remain correct.

    Run: npx jest tests/unit/mcp-server.test.js --no-coverage — all tests must pass.
  </action>
  <verify>
    npx jest tests/unit/mcp-server.test.js --no-coverage — all existing + new tests pass with 0 failures.
    npx jest tests/unit/mcp-server.test.js --coverage -- check that lib/mcp-server.js has >= 80% line coverage.
    (Level 2: Proxy — full test execution with coverage check)
  </verify>
  <done>
    Tests file includes schema spot-checks for new tools, updated descriptor count assertions, bulk execution tests for all 5 new tools, and all tests pass with >= 80% line coverage on mcp-server.js.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): `node -e "require('./lib/mcp-server')"` — no import errors.
Level 2 (Proxy): `npx jest tests/unit/mcp-server.test.js --no-coverage` — all tests pass. `node -e "const {COMMAND_DESCRIPTORS} = require('./lib/mcp-server'); console.log(COMMAND_DESCRIPTORS.length)"` outputs 102.
</verification>

<success_criteria>
- 5 new COMMAND_DESCRIPTORS entries registered with correct parameter schemas
- Total tool count is 102
- All new tools callable via MCP tools/call path
- Tests cover new tool schemas and execution lambdas
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-extension-wiring/21-01-SUMMARY.md`
</output>
