---
phase: 21-mcp-extension-wiring
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mcp-server.md
autonomous: true
verification_level: sanity

must_haves:
  truths:
    - "docs/mcp-server.md includes entries for grd_requirement_get, grd_requirement_list, grd_requirement_traceability, grd_requirement_update_status, and grd_search"
    - "Tool count references updated from 97 to 102 in docs/mcp-server.md"
    - "New tools documented in a 'Requirement & Search' section consistent with existing doc table format"
  artifacts:
    - path: "docs/mcp-server.md"
      provides: "Documentation for all new MCP tools"
      contains: "grd_requirement_get"
  key_links:
    - from: "docs/mcp-server.md"
      to: "lib/mcp-server.js"
      via: "tool names match COMMAND_DESCRIPTORS names"
      pattern: "grd_requirement_get"
---

<objective>
Update MCP server documentation to include the 5 new tool definitions with descriptions and parameter schemas, and update all tool count references from 97 to 102.

Purpose: Ensure docs/mcp-server.md accurately reflects the complete MCP tool inventory after wiring new commands.
Output: Updated docs/mcp-server.md.
</objective>

<execution_context>
@${CLAUDE_PLUGIN_ROOT}/references/execute-plan.md
@${CLAUDE_PLUGIN_ROOT}/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/mcp-server.md
@lib/mcp-server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/mcp-server.md with new tool entries and corrected counts</name>
  <files>docs/mcp-server.md</files>
  <action>
    1. **Update tool count references** — change all occurrences of "97" that refer to tool count to "102":
       - Line 3: "all 97 GRD CLI commands" → "all 102 GRD CLI commands"
       - Line 75 (tools/list row): "Returns all 97 tool definitions" → "Returns all 102 tool definitions"
       - Any other references to "97 tools" in the file

    2. **Add a new section "Requirement & Search (5 tools)"** in the Tool Reference area.
       Place it after the "Dashboard & Navigation" section and before the "Utility" section (or at a logical location near the end of the tool groups). Use the same markdown table format as existing sections:

       ```markdown
       ### Requirement & Search (5 tools)

       | Tool | Description |
       |------|-------------|
       | `grd_requirement_get` | Get a requirement by ID with status and phase from traceability matrix |
       | `grd_requirement_list` | List requirements with optional filters (phase, priority, status, category) |
       | `grd_requirement_traceability` | Get the traceability matrix with optional phase filter |
       | `grd_requirement_update_status` | Update the status of a requirement in the traceability matrix |
       | `grd_search` | Search across all .planning/ markdown files for a text query |
       ```

    3. **Update the Utility tools count** if it currently says "(9 tools)" — verify and update to reflect the correct count (it should remain unchanged since these are new tools in a new section, not additions to Utility).

    4. **Add an example** at the end of the Examples section showing how to search:

       ```markdown
       ### Search planning files

       ```json
       {"jsonrpc":"2.0","id":7,"method":"tools/call","params":{"name":"grd_search","arguments":{"query":"MCP"}}}
       ```

       ### Get a requirement

       ```json
       {"jsonrpc":"2.0","id":8,"method":"tools/call","params":{"name":"grd_requirement_get","arguments":{"req_id":"REQ-37"}}}
       ```
       ```

    IMPORTANT: Preserve the existing document structure. Only add/update — do not rewrite existing sections.
  </action>
  <verify>
    grep -c "102" docs/mcp-server.md — should show >= 2 (count references updated).
    grep "grd_requirement_get" docs/mcp-server.md — should match at least 1 line.
    grep "grd_search" docs/mcp-server.md — should match at least 1 line.
    (Level 1: Sanity — verify content presence)
  </verify>
  <done>
    docs/mcp-server.md reflects 102 total tools, includes a "Requirement & Search" section with all 5 new tools documented, and has example JSON-RPC calls for the new tools.
  </done>
</task>

</tasks>

<verification>
Level 1 (Sanity): grep for all 5 new tool names in docs/mcp-server.md — all present. Verify "97" does not appear as a tool count reference.
</verification>

<success_criteria>
- All 5 new tools documented in docs/mcp-server.md
- Tool count references updated to 102
- Documentation format consistent with existing sections
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-extension-wiring/21-02-SUMMARY.md`
</output>
